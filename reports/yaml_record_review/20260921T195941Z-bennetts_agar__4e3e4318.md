# YAML Record Review: Bennett's Agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml
- Started UTC: 2026-09-21T19:58:10Z
- Finished UTC: 2026-09-21T19:59:41Z
- Verdict: needs curation

## Target

- Reviewed `data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml`.
- Class: `MediaRecipe`.
- ID: `CultureMech:007997`.
- Name: `bennetts_agar`.
- Original name: `Bennett's Agar`.
- Source identity: `TOGO:M1456`, label `Bennett's Agar`; TOGO metadata says `original_media_id: NBRC_M228` and links NBRC Medium 228.
- Generated status: derived merge output under `data/merge_yaml/merged/`; the maintained owner is `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml`.
- Merge metadata: one source record, `TOGO_M1456_Bennett_s_Agar`, on fingerprint `4e3e4318ebe6a1e86ba54ffd54d955707141ae3b5f9919352c3850b37087d956`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml` via the no-project Python 3.11 workaround | Passed |
| Closed-schema strict validation, `scripts/validate_strict.py data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml --out /private/tmp/bennetts_agar__4e3e4318.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks were applicable |
| `linkml-term-validator validate-data data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not a focused embedded-history validator for one merged record |

The documented `just` commands were not run directly because the project runtime currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation.

## Identity and Grounding

- `CultureMech:007997` resolves in `data/culturemech_id_registry.tsv` to `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml`.
- TOGO M1456, NBRC Medium 228, the `Bennett's Agar` label, and the linked NBRC URL all denote one NBRC Bennett's Agar recipe.
- This NBRC 228 recipe is related to, but source-distinct from, the JCM/DSMZ Bennett's Agar records reviewed immediately before it; its NBRC source uses 20 g/L agar instead of 15 g/L.
- Glucose, agar, and water are grounded to exact ChEBI terms. Yeast extract, beef extract, and N-Z-Amine A are appropriately left as complex mixture ingredients without forced ChEBI groundings.

## Evidence

- TOGO M1456 supports the source identity, its `NBRC_M228` provenance, pH 7.3, 1 L distilled water, 1 g yeast extract, 10 g glucose, 20 g agar, 1 g beef extract, and 2 g N-Z-Amine A.
- The live NBRC Medium 228 page agrees with TOGO M1456 on the Bennett's Agar label, medium number 228, all six component rows, and pH 7.3.
- The maintained owner stores TOGO's 1 L distilled water item as `value: '1'` with `unit: G_PER_L`; this is neither the source unit nor a dimensionally meaningful final water concentration.
- The maintained owner drops the pH 7.3 value from TOGO and NBRC; `ph_value` is absent.
- A gitignore-independent exact search over the maintained owner, generated merge, ID registry, recipe catalog, and TOGO indexes found the expected `CultureMech:007997` owner and generated record references plus a separate active `NBRC_229.yaml` Bennett source. It did not find a raw TOGO M1456 or NBRC 228 source payload committed for this source record.

## Completeness

- The five non-water ingredients are complete and source-supported.
- Consequentially incorrect: distilled water has the wrong unit.
- Consequentially incomplete: the supported pH 7.3 value is absent.
- Empty preparation steps are acceptable here; TOGO and NBRC provide only the pH row, not a textual preparation protocol.
- Empty target-organism and growth-evidence slots are acceptable here; TOGO M1456 and NBRC Medium 228 do not name a strain-specific growth result.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | TOGO's `1 L` distilled water row was normalized as `1` `G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml` |
| Major | TOGO M1456 and NBRC Medium 228 both support pH 7.3, but `ph_value` is absent. | `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml` |

## Recommended Edits

1. Correct the distilled-water amount to source-faithful `1 L` or omit water consistently if this importer treats solvent water as implied.
2. Add `ph_value: 7.3` from the TOGO/NBRC pH row.
3. Regenerate `data/merge_yaml/merged/bennetts_agar__4e3e4318.yaml` after the normalized owner is corrected.

## Follow-up Checks

- Rerun the open schema validator, `scripts/validate_strict.py`, `linkml-term-validator`, and `linkml-reference-validator` on `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml` and the regenerated merged record.
- Manually compare the regenerated record against the TOGO M1456 JSON and NBRC Medium 228 to verify all six TOGO component rows and pH 7.3 survive with source-faithful units.

## Additional Notes

- NBRC Medium 228 agrees with the adjacent JCM/DSMZ Bennett's Agar sources on beef extract, glucose, N-Z-Amine A, yeast extract, and pH, but NBRC specifies 20 g agar rather than 15 g.
- Reports are read-only review artifacts; no normalized recipe, generated merge, page, history entry, or GitHub issue was edited for this review.
