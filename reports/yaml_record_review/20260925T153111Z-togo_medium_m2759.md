# YAML Record Review: togo_medium_m2759

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m2759.yaml
- Started UTC: 2026-09-25T15:31:12Z
- Finished UTC: 2026-09-25T15:31:12Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | data/merge_yaml/merged/togo_medium_m2759.yaml |
| Class | MediaRecipe |
| ID | CultureMech:009309 |
| Name | togo_medium_m2759 |
| Media term | TOGO:M2759 |
| Category | bacterial |
| Source | TOGO Medium M2759 |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m2759.yaml |
| Generated status | Generated merged record; do not edit directly |

Exact owner resolution found one generated target and one identical normalized
owner for `togo_medium_m2759.yaml`. The inspected TOGO M2759 API payload has no
original-provider URL and is the source used for this review.

## Validation

| Check | Command | Result |
|---|---|---|
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m2759.yaml` | Passed; `No issues found` |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m2759.yaml --out /private/tmp/m2759.strict.tsv --workers 1 --quiet` | Passed; 0 error rows and the TSV had only its header |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m2759.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks because the merged record has no structured `references` entries |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m2759.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded history | `just validate-history` | Not checked: the documented history validator targets standalone `history/` records, not embedded `MediaRecipe.curation_history` in merged YAML |

## Identity and Grounding

The record denotes TOGO M2759. The source contains a final per-liter medium with
327 mM NaCl, 5.7 mM K2HPO4, 12.5 mM ammonium sulfate, 19.8 mM Na2CO3, 10 mM
Na2B4O7, 0.025 g yeast extract, 10 ml 100x mineral solution, and an N2/CO2
80:20 headspace. TOGO then defines the 100x mineral solution as its own
per-liter stock.

The generated record collapses the custom 100x mineral stock into the root
ingredient list. The stock itself is still present under `solutions`, but it has
an empty composition and the 10 ml final-medium addition is encoded as
`10 G_PER_L`.

## Evidence

- Supported by TOGO M2759: the final medium uses 10 ml 100x mineral solution
  per liter.
- Supported by TOGO M2759: the 100x stock contains 1 L distilled water, 1 g
  NaCl, 0.01 g H3BO3, 0.02 g NiCl2.6H2O, 0.13 g ZnCl, 1.5 g nitrilotriacetic
  acid, 0.1 g CaCl2, 0.03 g Na2WO4, 0.01 g AlK(SO4), 3 g MgSO4, 0.01 g CuSO4,
  0.1 g CoCl2, 0.1 g FeSO4, 0.03 g Na2MoO4, and 0.5 g MnSO4 per stock liter.
- Supported by TOGO M2759: pH is adjusted to approximately 9.5 and the headspace
  is N2/CO2 80:20.
- Unsupported as represented: the stock ingredients were promoted to
  final-medium root ingredients, while the actual `solutions` entry has no
  composition.
- Unsupported as represented: N2 and CO2 are variable-concentration root
  ingredients rather than an atmospheric condition.

## Completeness

The root formulation is incomplete as a final medium and as a stock definition:
stock water was summed with the final water to 2.0 `G_PER_L`, the stock NaCl row
was merged into the final 327 mM NaCl row as a documented unit conflict, and the
custom 100x stock composition is no longer nested where the 10 ml addition can
point to it.

No organism, incubation temperature, vessel, or storage information appears in
the inspected TOGO payload. Leaving those optional slots empty is acceptable.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| blocker | None found | None found | None found |
| major | The 100x mineral stock was flattened into the final recipe. | TOGO adds 10 ml of the custom stock per final liter, but the generated root ingredients contain the stock-only H3BO3, NiCl2.6H2O, ZnCl, nitrilotriacetic acid, CaCl2, Na2WO4, AlK(SO4), MgSO4, CuSO4, CoCl2, FeSO4, Na2MoO4, and MnSO4 rows. | data/normalized_yaml/bacterial/togo_medium_m2759.yaml |
| major | The 100x stock addition has the wrong unit and no composition. | The generated `solutions` entry stores value `10`, unit `G_PER_L`, and `composition: []`; TOGO specifies a 10 ml addition of a defined per-liter stock. | data/normalized_yaml/bacterial/togo_medium_m2759.yaml |
| major | pH and headspace are not represented structurally. | TOGO records pH approximately 9.5 and N2/CO2 80:20 headspace; the merged record has no pH or atmosphere field and keeps N2 and CO2 as variable root ingredients. | data/normalized_yaml/bacterial/togo_medium_m2759.yaml |
| minor | None found | None found | None found |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/togo_medium_m2759.yaml`, move the
   100x mineral solution components out of the root final medium and into an
   exact stock solution composition.
2. Encode the final-medium stock addition as 10 ml per liter rather than
   `10 G_PER_L`.
3. Add source-scoped pH 9.5 and N2/CO2 80:20 headspace metadata; remove the N2
   and CO2 gas rows from root ingredients.
4. Regenerate `data/merge_yaml/merged/togo_medium_m2759.yaml` from the
   normalized owner.

## Follow-up Checks

- Rerun LinkML schema, strict, term, and reference validation on
  `data/merge_yaml/merged/togo_medium_m2759.yaml` after regeneration.
- Manually compare every final ingredient and every 100x mineral stock
  ingredient against the TOGO M2759 JSON payload.
- Rerun `just verify-merges` and `just audit-merge-freshness` after regenerated
  merged YAML is available.

## Additional Notes

The exact owner search for `M2759` included ignored files via
`rg --no-ignore --hidden` and found only the generated target, the normalized
owner, index entries, and a non-target mention in `scripts/resolve_unit_conflicts.py`.
