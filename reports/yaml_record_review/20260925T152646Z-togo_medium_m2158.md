# YAML Record Review: togo_medium_m2158

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m2158.yaml
- Started UTC: 2026-09-25T15:26:47Z
- Finished UTC: 2026-09-25T15:26:47Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | data/merge_yaml/merged/togo_medium_m2158.yaml |
| Class | MediaRecipe |
| ID | CultureMech:008752 |
| Name | togo_medium_m2158 |
| Media term | TOGO:M2158 |
| Category | bacterial |
| Source | TOGO Medium M2158; original NBRC_M1518 |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m2158.yaml |
| Generated status | Generated merged record; do not edit directly |

Exact owner resolution found one generated target and one identical normalized
owner for `togo_medium_m2158.yaml`. The source identity is also embedded in the
TOGO payload: `original_media_id` is `NBRC_M1518`, the original URL is the NBRC
NO=1518 page, and the imported source name is `(Unnamed medium)`.

## Validation

| Check | Command | Result |
|---|---|---|
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m2158.yaml` | Passed; `No issues found` |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m2158.yaml --out /private/tmp/m2158.strict.tsv --workers 1 --quiet` | Passed; 0 error rows and the TSV had only its header |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m2158.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks because the merged record has no structured `references` entries |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m2158.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded history | `just validate-history` | Not checked: the documented history validator targets standalone `history/` records, not embedded `MediaRecipe.curation_history` in merged YAML |

## Identity and Grounding

The record denotes TOGO M2158 imported from NBRC medium 1518. The TOGO payload
and live NBRC NO=1518 page both list 4 g yeast extract, 3.5 g Na2HPO4 x 12H2O,
1 g K2HPO4, 0.03 g MgSO4 x 7H2O, 0.5 g NH4Cl, 1 L distilled water, and 15 g agar
if needed. The TOGO term `TOGO:M2158` and the generated `media_term` agree.

The ingredient groundings are mostly consistent with the imported TOGO labels.
MgSO4 x 7H2O is correctly grounded to magnesium sulfate heptahydrate and water,
NH4Cl, and K2HPO4 are grounded to the expected ChEBI salts. The Na2HPO4 x 12H2O
entry is still grounded to generic `CHEBI:34683` / `disodium hydrogenphosphate`
rather than to a dodecahydrate-specific identity.

## Evidence

- Supported by TOGO M2158 and NBRC M1518: the yeast extract, MgSO4 x 7H2O,
  NH4Cl, K2HPO4, Na2HPO4 x 12H2O, and optional agar ingredient names and numeric
  gram amounts.
- Supported by TOGO M2158 and NBRC M1518: NBRC records pH 5.0 for the recipe.
- Unsupported as represented: 1 L distilled water was imported as `1 G_PER_L`.
- Unsupported as represented: `Agar (if needed)` was imported as an ordinary
  required ingredient and the generated record asserts `physical_state:
  SOLID_AGAR`.
- Unsupported as represented: the source hydrate in `Na2HPO4 x 12H2O` is more
  specific than the current generic ChEBI grounding.

## Completeness

The consequential missing field is structural pH: NBRC M1518 and TOGO M2158
carry pH 5.0, but the merged record has no pH slot populated. The source URL and
NBRC original URL are recoverable from `notes`; no `references` entry exists, so
the reference validator had nothing to dereference.

Empty organism, growth, storage, and preparation fields are not defects here; no
organism or procedural metadata was present in the inspected NBRC and TOGO source
texts beyond pH and optional agar.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| blocker | None found | None found | None found |
| major | Distilled water has the wrong dimension. | TOGO/NBRC list 1 L distilled water, but the generated ingredient says value `1`, unit `G_PER_L`. | data/normalized_yaml/bacterial/togo_medium_m2158.yaml |
| major | The source pH is omitted. | TOGO M2158 exposes `ph: 5.0` and NBRC M1518 lists pH 5.0, but the record has no structural pH field. | data/normalized_yaml/bacterial/togo_medium_m2158.yaml |
| major | Optional agar is represented as required solid medium. | NBRC M1518 and TOGO M2158 list `Agar (if needed)`; the record has a normal agar ingredient and `physical_state: SOLID_AGAR`, which loses the optional liquid/solid distinction. | data/normalized_yaml/bacterial/togo_medium_m2158.yaml |
| major | The Na2HPO4 x 12H2O hydrate is over-broadened. | The source labels the dodecahydrate, while the record grounds the ingredient to generic `CHEBI:34683` / `disodium hydrogenphosphate`. | data/normalized_yaml/bacterial/togo_medium_m2158.yaml |
| minor | None found | None found | None found |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/togo_medium_m2158.yaml`, replace the
   solvent encoding with a q.s. or volume-aware representation for 1 L
   distilled water, then regenerate `data/merge_yaml/merged/`.
2. Add structural pH 5.0 to the normalized record with TOGO/NBRC source
   provenance, then regenerate the merge.
3. Split optional agar from the base formulation or otherwise represent that
   15 g agar is used only when agar is needed; correct `physical_state` so the
   base record no longer unconditionally denotes a solid agar medium.
4. Re-resolve `Na2HPO4 x 12H2O` with the packaged MediaIngredientMech index and
   OAK before either assigning a hydrate-specific ChEBI term or leaving the
   term unresolved explicitly.

## Follow-up Checks

- Rerun LinkML schema, strict, term, and reference validation on
  `data/merge_yaml/merged/togo_medium_m2158.yaml` after regeneration.
- Manually compare the normalized record against TOGO M2158 and NBRC NO=1518 to
  confirm pH, water, optional agar, and Na2HPO4 x 12H2O semantics.
- Rerun `just verify-merges` and `just audit-merge-freshness` once the
  normalized owner is changed and merged YAML is regenerated.

## Additional Notes

The exact file-existence checks for this batch used `find` over
`data/merge_yaml/merged`, which includes ignored files. The live NBRC page
agrees with the TOGO payload for the formulation and pH.
