# YAML Record Review: m9_gupta

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/m9_gupta.yaml
- Started UTC: 2026-09-23T21:38:43Z
- Finished UTC: 2026-09-23T21:40:06Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:006995` for `M9 (gupta) with lactose`.

| Field | Value |
|---|---|
| Generated path | `data/merge_yaml/merged/m9_gupta.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |
| Category | `bacterial` |
| Type | `DEFINED` |
| Composition | `DEFINED` |
| Physical state | `LIQUID` |
| Source medium | `MEDIADB:109`, `M9 (gupta) with lactose` |
| Merge fingerprint | `a07fe05bee556ccd03e59d1f4b49a38fc9ed2c61a7fc3cab11c866009aeede68` |

The generated merge contains one maintained source record, `MEDIADB_109_M9_gupta.yaml`.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m9_gupta.yaml` | Passed; no issues reported. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m9_gupta.yaml --out /private/tmp/m9_gupta.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m9_gupta.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m9_gupta.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` validation | `just validate-history` | Not checked: this project validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

The intended external identity is MediaDB medium 109, `M9 (gupta) with lactose`. MediaDB medium 109 marks the medium as not minimal, lists eight compounds, links MediaDB source 33, and links growth data row 236 for *Escherichia coli* HB101 on the same medium. The MediaDB text endpoint supports the eight ingredient names and millimolar concentrations: lactose 15.0, calcium chloride anhydrous 0.1, dibasic sodium phosphate 43.0, sodium chloride 8.6, thiamine HCl 0.033, potassium dihydrogen phosphate 22.0, magnesium sulfate 2.0, and ammonium chloride 19.0.

The generated record still has stale truncated source labels: `original_name` and `media_term.term.label` are `"'M9 (gupta"`. The maintained owner has already restored `M9 (gupta) with lactose` in those two fields with a `repair_mediadb_names.py` curation event, so the generated merge has not been rebuilt from current normalized input.

The record name is ambiguous. An ignored-file-inclusive exact search of `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive` found a separate Gupta glucose record with the same normalized `name: m9_gupta` and the generated path `data/merge_yaml/merged/m9_gupta__f45f4b4f.yaml`. The current lactose variant and the glucose variant are different MediaDB media with different carbon sources, concentrations, source growth rows, and CultureMech IDs; a future slug should preserve the lactose/glucose distinction.

The narrow source provenance is incomplete. MediaDB source 33 resolves to Gupta et al. 1995, PubMed `7640001`, DOI `10.1016/0168-1656(95)00049-v`, but the record only carries a generic MediaDB note and a `curation_history` note for Mazumdar et al. 2014.

The ignored-file-inclusive exact search for `CultureMech:006995`, `MEDIADB:109`, and the merge fingerprint found only the maintained owner, this generated record, generated indexes, and historical validation reports; no duplicate lactose record was found in that bounded scope.

## Evidence

The eight ingredient names and concentrations are supported by MediaDB medium 109.

One source cross-reference needs curator review: the MediaDB text endpoint links `Magnesium sulfate` to CHEBI `31795`, while the YAML records `CHEBI:32599`. The local term validator accepts `CHEBI:32599` as magnesium sulfate, but the imported record does not preserve the source CHEBI cross-reference.

The record correctly treats lactose as the carbon source for MediaDB 109. The similarly named glucose variant is a separate medium, MediaDB 108, and should not be merged with this record.

The defined, liquid, and bacterial filing is plausible for this M9 bacterial medium. MediaDB 109 explicitly marks the medium as not minimal; the YAML carries `medium_type: DEFINED` and `composition_type: DEFINED`, not a minimal-medium flag, so this is not internally contradictory.

The record is missing the narrow source that supports this formulation. MediaDB points medium 109 to Gupta et al. 1995 via source 33; Mazumdar et al. 2014 documents the MediaDB database, not this specific M9 lactose formulation.

The three preparation steps are unsupported by inspected MediaDB evidence. MediaDB 109 supplies ingredient rows and source/growth metadata, but the reviewed pages did not supply instructions to dissolve all ingredients in distilled water, adjust pH only if specified in the original formulation, or filter sterilize through a 0.22 um filter.

## Completeness

The ingredient list is complete relative to MediaDB medium 109 by names and concentrations, subject to resolving the magnesium sulfate CHEBI cross-reference mismatch.

The generated record is incomplete relative to the maintained owner because it is missing the repaired source labels and the `repair_mediadb_names.py` curation event.

The record is incomplete for recoverable source provenance because it lacks a structured reference for MediaDB source 33, PMID `7640001`, and DOI `10.1016/0168-1656(95)00049-v`.

The record is incomplete for organism support. MediaDB exposes growth data row 236 for *Escherichia coli* HB101 on `M9 (gupta) with lactose` with growth rate 0.18 1/h and temperature 37.0 C, but the YAML has no `target_organisms` entry or equivalent structured growth assertion.

Optional solution slots, discussion fields, and strain subfields are correctly absent for this simple MediaDB ingredient import.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record is stale and still has a truncated MediaDB label. | MediaDB 109 and `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` name the medium `M9 (gupta) with lactose`, but `data/merge_yaml/merged/m9_gupta.yaml` still has `"'M9 (gupta"`. | Regenerate `data/merge_yaml/merged/m9_gupta.yaml` from `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |
| Major | The normalized slug collides with the distinct Gupta glucose medium. | This lactose record and the MediaDB 108 glucose record both use `name: m9_gupta`; the generated corpus has to disambiguate the glucose record as `m9_gupta__f45f4b4f.yaml`. | `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml`; also review `data/normalized_yaml/bacterial/m9_gupta.yaml` |
| Major | MediaDB's organism growth row is omitted. | MediaDB growth data 236 specifically records *Escherichia coli* HB101 growth on `M9 (gupta) with lactose` with rate and temperature metadata, but the YAML carries no structured target organism or growth-condition claim. | `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |
| Major | The imported MediaDB preparation protocol is unsupported and may over-prescribe filtration. | Inspected MediaDB pages for medium 109 expose the formula, source, and growth row, but not a three-step protocol requiring distilled water, conditional pH adjustment, or 0.22 um filtration. The same boilerplate is produced for MediaDB imports, so the likely durable fix belongs in the importer. | `src/culturemech/import/mediadb_importer.py`; then regenerate `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |
| Minor | The magnesium sulfate source cross-reference changed during import/enrichment. | MediaDB's tabular export for medium 109 lists `Magnesium sulfate` with CHEBI `31795`; the YAML uses `CHEBI:32599`. | `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |
| Minor | Source provenance points to the MediaDB publication instead of the narrow formula source. | MediaDB source 33 identifies Gupta et al. 1995 with PubMed `7640001` and DOI `10.1016/0168-1656(95)00049-v`; the normalized record only names MediaDB and Mazumdar et al. 2014. | `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` |

## Recommended Edits

1. Rename the lactose record to a collision-free slug such as `m9_gupta_with_lactose`, and keep the glucose record distinct, for example as `m9_gupta_with_glucose`.
2. Regenerate `data/merge_yaml/merged/m9_gupta.yaml` from the repaired `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml` so the MediaDB label is `M9 (gupta) with lactose`.
3. Add the MediaDB 236 *Escherichia coli* HB101 growth observation, including 0.18 1/h and 37.0 C as growth-condition metadata, to `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml`.
4. Remove unsupported generic preparation steps from this record, or change `src/culturemech/import/mediadb_importer.py` so MediaDB imports do not emit a synthetic filtration and pH-adjustment protocol when MediaDB has no recipe text supporting those steps.
5. Check whether the magnesium sulfate ingredient should preserve the MediaDB CHEBI `31795` cross-reference or use the normalized generic `CHEBI:32599`, then document the decision in `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml`.
6. Add structured narrow source provenance for MediaDB source 33, PubMed `7640001`, and DOI `10.1016/0168-1656(95)00049-v` in `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml`.

## Follow-up Checks

Run these after future curation:

1. Use an ignored-file-inclusive exact search for `name: m9_gupta` under `data/normalized_yaml` and `data/merge_yaml` to confirm the glucose and lactose records no longer collide.
2. Re-open the regenerated lactose record and confirm its `original_name` and `media_term.term.label` are `M9 (gupta) with lactose`.
3. `just validate-schema data/merge_yaml/merged/m9_gupta.yaml` or the focused `linkml-validate` command above.
4. `python scripts/validate_strict.py data/merge_yaml/merged/m9_gupta.yaml --out /tmp/m9_gupta.strict.tsv --workers 1 --quiet`.
5. `linkml-reference-validator validate data data/merge_yaml/merged/m9_gupta.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`.
6. `linkml-term-validator validate-data data/merge_yaml/merged/m9_gupta.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`.
7. Manually re-open MediaDB medium 109, MediaDB text 109, MediaDB source 33, MediaDB growth data 236, and PubMed `7640001` to confirm the regenerated YAML still preserves the formula and the added source and growth metadata.

## Additional Notes

No record edits were made during this read-only review. The generated file should stay derived from `data/normalized_yaml/bacterial/MEDIADB_109_M9_gupta.yaml`; a direct patch to `data/merge_yaml/merged/m9_gupta.yaml` would be overwritten.
