# YAML Record Review: 355

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/355.yaml`
- Started UTC: 2026-09-21T06:48:52Z
- Finished UTC: 2026-09-21T06:49:48Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007489` |
| Name | `355` |
| Original name | `355` |
| Category | `bacterial` |
| Source | NBRC Medium 352 |
| Source URL | `http://www.nbrc.nite.go.jp/NBRC2/NBRCMediumDetailServlet?NO=352` |
| Merge fingerprint | `e60dd0d1b759caef431509b260fe1845d0ca32827c196f69c70bc465949dfe0f` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_355.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC 352 import. Future corrections belong in `data/normalized_yaml/bacterial/NBRC_355.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/355.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/355.yaml --out /private/tmp/355.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/355.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/355.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated target is stale relative to the maintained owner. `data/normalized_yaml/bacterial/NBRC_355.yaml` keeps the same `CultureMech:007489` identity, adds `media_term.term.id: nbrc.medium:352`, adds `ph_value: 7.0`, adds the current NBRC reference, and replaces the generated target's two malformed ingredient rows with six NBRC Medium 352 rows.

The current normalized owner is still only partially curated. The live NBRC 352 page titles the recipe `Methylobacterium Medium`, but the owner still has `name: '355'` and `original_name: '355'`. The owner also leaves `MgSO4.7H2O` ungrounded even though the hydrate has a specific CHEBI term used elsewhere in the repository. Hipolypepton is reasonably left unmapped because NBRC marks it as a Wako Pure Chemical Industries product.

The old NBRC URL recorded in the import history now redirects to `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=352` with HTTP 200.

## Evidence

The live NBRC Medium 352 page identifies the medium as `Methylobacterium Medium` and contains a composition table with:

- 10 g Hipolypepton.
- 2 g yeast extract.
- 5 ml Methanol.
- 1 g MgSO4.7H2O.
- 15 g agar, if needed.
- 1 L distilled water.
- pH 7.0.

The page footnotes Hipolypepton as a Wako Pure Chemical Industries product and instructs separate filter sterilization for methanol. The maintained owner captures the six source rows, the final pH, and the methanol filtration step. The generated target instead has one row whose preferred term is the whole concatenated NBRC formula plus a second row with an empty `preferred_term` and `concentration.value: Hipolypepton*`.

The repaired TOGO M1553 `Methylobacterium Medium` record also treats TOGO M1553 as an NBRC Medium 352 import and carries NBRC-supported row notes for Hipolypepton, methanol, MgSO4.7H2O, and distilled water.

## Completeness

Consequential gaps:

- The generated target is missing the maintained owner's recovered six-row composition.
- The generated target is missing the NBRC source term, final pH, structured NBRC reference, and filtered-methanol preparation step.
- The generated target still carries a malformed row with an empty ingredient label and a nonnumeric `Hipolypepton*` concentration value.
- The maintained owner still uses the numeric `355` name instead of NBRC's official `Methylobacterium Medium` title.
- The maintained owner still lacks a CHEBI grounding for magnesium sulfate heptahydrate.

Empty optional slots:

- `solutions` is correctly absent. NBRC 352 is a flat final-volume recipe in the live NBRC table.
- `target_organisms` and `growth_metrics` are correctly absent. The NBRC medium page supports the recipe composition, pH, and methanol-filtration handling, but it does not make a strain-specific growth claim.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-355.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, `data/raw`, and `scripts` for `CultureMech:007489`, `NBRC_355`, and `NBRC, Media No. 352` found the normalized owner, generated merge, indexes/manifests, and archived validation rows.
- A gitignore-independent `rg --no-ignore --hidden` search for `NBRC_355` under `scripts` found no record-specific repair script.
- A gitignore-independent `find` under `data/raw` for `*352*` found no cached NBRC 352 raw source file. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still carries the broken pre-recovery NBRC import. | The generated target has one collapsed full-formula ingredient and a second empty-label row whose value is `Hipolypepton*`; the maintained owner has already recovered NBRC Medium 352 as six ingredient rows, added `ph_value: 7.0`, added `nbrc.medium:352`, added the redirected NBRC reference, and recorded `RESOLVED_NBRC_PARTIAL_SCORE25`. | `data/normalized_yaml/bacterial/NBRC_355.yaml` |
| Minor | The maintained owner still uses the numeric import label rather than the official NBRC title. | The live NBRC 352 page labels the medium `Methylobacterium Medium`; `NBRC_355.yaml` still has `name: '355'` and `original_name: '355'`. | `data/normalized_yaml/bacterial/NBRC_355.yaml` |
| Minor | Magnesium sulfate heptahydrate is still ungrounded in the maintained owner. | The `MgSO4 x 7H2O` row in `NBRC_355.yaml` has no `term`, while the same ingredient is routinely grounded to `CHEBI:31795` in nearby NBRC and TOGO records. | `data/normalized_yaml/bacterial/NBRC_355.yaml` |

## Recommended Edits

1. Rename `data/normalized_yaml/bacterial/NBRC_355.yaml` to NBRC's official `Methylobacterium Medium` title.
2. Add `CHEBI:31795` to the `MgSO4 x 7H2O` row.
3. Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml` so `355.yaml` picks up the existing recovered six-row composition, source term, pH, reference, and preparation step plus the edits above.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/NBRC_355.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/NBRC_355.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/NBRC_355.yaml` after grounding magnesium sulfate heptahydrate.
- `just verify-merges` after regenerating generated records.
- `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 352 for the six component amounts, pH 7.0, the Hipolypepton footnote, and the methanol filtration footnote.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
- The current owner carries a generic `repair_nbrc_partial_score25.py` curation event, but a gitignore-independent repository-wide exact search found that filename only in normalized YAML histories; there is no current `scripts/repair_nbrc_partial_score25.py` file to inspect.
