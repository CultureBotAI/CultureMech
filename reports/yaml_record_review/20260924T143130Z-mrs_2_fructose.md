# YAML Record Review: MRS + 2 % fructose

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_2_fructose.yaml
- Started UTC: 2026-09-24T14:29:45Z
- Finished UTC: 2026-09-24T14:31:30Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009560 |
| Name | mrs_2_fructose |
| Original name | MRS + 2 % fructose |
| Category | bacterial |
| Media term | TOGO:M3047 |
| Source | NBRC_M1597, NBRC medium 1597 |
| Reviewed path | data/merge_yaml/merged/mrs_2_fructose.yaml |
| Maintained source | data/normalized_yaml/bacterial/mrs_2_fructose.yaml |
| Generated status | Generated by recipe-merger / merge_recipes.py on 2026-08-06 |

The reviewed generated record keeps the TOGO M3047 and NBRC 1597 identity for
`MRS + 2 % fructose`, but the duplicate-merge step also merged
`mrs_1_fructose` and `mrs_10_fructose` into this output.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_2_fructose.yaml` exited 0 with no diagnostics under Python 3.11. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_2_fructose.yaml --out /private/tmp/mrs_2_fructose.strict.tsv --workers 1 --quiet` exited 0 and wrote only the TSV header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_2_fructose.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_2_fructose.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The file is structurally valid; the failing issues are semantic and
provenance-related.

## Identity and Grounding

- `media_term.id` is `TOGO:M3047`, `media_term.label` is
  `MRS + 2 % fructose`, and the notes point to NBRC medium 1597. TOGO M3047
  reports `original_media_id: NBRC_M1597`, the same NBRC URL, and pH
  `6.0 - 6.5`; NBRC 1597 is the same `MRS + 2 % fructose` formulation.
- The TOGO M3047 and NBRC 1597 source rows support the common MRS ingredient
  list and support `Fructose` at 20 g per liter. The reviewed generated record
  instead lists `Fructose` as `100 G_PER_L`.
- The 100 g fructose concentration belongs to a distinct sibling,
  `TOGO:M2129` / NBRC 1461, named `MRS + 10% Fructose`.
- `TOGO:M1852` / NBRC 1091 is another distinct sibling, named
  `MRS + 1% Fructose`, with 10 g fructose.
- The post-merge `synonyms`, `merged_from`, and `merge_fingerprint` fields
  show that `merge_recipes.py` collapsed the 1%, 2%, and 10% fructose records
  on one fingerprint. Those variants differ in the value that defines the
  medium, so they must not be merged as duplicates.

## Evidence

TOGO and NBRC agree on the three fructose-dose records:

| Variant | TOGO record | NBRC record | Fructose amount |
| --- | --- | --- | --- |
| MRS + 1% Fructose | TOGO:M1852 | NBRC_M1091 / medium 1091 | 10 g |
| MRS + 2 % fructose | TOGO:M3047 | NBRC_M1597 / medium 1597 | 20 g |
| MRS + 10% Fructose | TOGO:M2129 | NBRC_M1461 / medium 1461 | 100 g |

The reviewed generated record's source identity is the middle row but its
fructose amount is the last row.

The generated record otherwise preserves the shared MRS rows at TOGO/NBRC
amounts: peptone 10 g, meat extract 10 g, yeast extract 5 g, glucose 20 g,
Tween 80 1 g, K2HPO4 2 g, sodium acetate 5 g, diammonium hydrogen citrate 2 g,
MgSO4 x 7H2O 0.2 g, MnSO4 x nH2O 0.05 g, distilled water 1 L, and agar 15 g if
needed.

## Completeness

- The source pH is absent from the generated record even though TOGO M3047 and
  NBRC 1597 both supply `6.0 - 6.5`.
- The source marks `Agar` as conditional with `if needed`; the generated record
  stores the medium as `physical_state: SOLID_AGAR` and makes agar a required
  ingredient.
- The source's 1 L distilled-water amount is represented as `1 G_PER_L`, which
  is a volume-to-mass import artifact.
- `Agar (if needed)` has no `term` or `mediaingredientmech_chebi_term`
  grounding even though the TOGO row is a specific agar component.
- Empty optional fields such as `comments` are not defects.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `TOGO:M3047`, `TOGO:M2129`, `TOGO:M1852`,
  `NBRC_M1597`, `mrs_1_fructose`, `mrs_10_fructose`, `CultureMech:009560`,
  and `CultureMech:008427` found the expected three normalized fructose-dose
  siblings and the generated merged `mrs_2_fructose` output.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| blocker | The generated 2% record carries the 10% fructose concentration. | `data/merge_yaml/merged/mrs_2_fructose.yaml` has `media_term: TOGO:M3047` and `Fructose` at `100 G_PER_L`; TOGO M3047/NBRC 1597 support 20 g, while TOGO M2129/NBRC 1461 support 100 g for the distinct `MRS + 10% Fructose` record. | `scripts/merge_recipes.py` and regenerated `data/merge_yaml/merged/*.yaml` |
| blocker | Distinct 1%, 2%, and 10% NBRC/TOGO media were merged as duplicate recipes. | The generated `merged_from` list contains `mrs_10_fructose`, `mrs_1_fructose`, and `mrs_2_fructose`; the three source records intentionally differ by 10 g, 20 g, and 100 g fructose. | `scripts/merge_recipes.py` fingerprint logic or its maintained merge exception inputs, then regeneration |
| major | The record drops the source pH. | TOGO M3047 and NBRC 1597 both carry pH `6.0 - 6.5`; the generated YAML has no pH field or note. | `data/normalized_yaml/bacterial/mrs_2_fructose.yaml` |
| major | Conditional agar is promoted to a mandatory solid formulation. | NBRC and TOGO phrase the agar row as `Agar (if needed)`; the YAML sets `physical_state: SOLID_AGAR` and stores agar as an ordinary ingredient. | `data/normalized_yaml/bacterial/mrs_2_fructose.yaml` |
| major | The imported water concentration uses the wrong unit. | The source row is `Distilled water` 1 L; the YAML stores `Distilled water` as `1 G_PER_L`. | TOGO/NBRC import normalization for `data/normalized_yaml/bacterial/mrs_2_fructose.yaml` |
| major | The agar component is ungrounded. | `Agar (if needed)` has a solidifying role note but no `term` or `mediaingredientmech_chebi_term`. | CHEBI enrichment for `data/normalized_yaml/bacterial/mrs_2_fructose.yaml` |

## Recommended Edits

1. Fix the duplicate-merge fingerprint or add a maintained merge exception so
   TOGO M1852, M3047, and M2129 regenerate as three records, not one merged
   `mrs_2_fructose` output.
2. Regenerate `data/merge_yaml/merged/mrs_2_fructose.yaml` from
   `data/normalized_yaml/bacterial/mrs_2_fructose.yaml` so its fructose value
   returns to `20 G_PER_L` and its `merged_from`, `synonyms`, and
   `merge_fingerprint` fields stop claiming identity with the 1% and 10%
   siblings.
3. Preserve the TOGO/NBRC pH `6.0 - 6.5` on
   `data/normalized_yaml/bacterial/mrs_2_fructose.yaml`.
4. Rework the TOGO/NBRC import representation for `Agar (if needed)` so the
   optional solidifying additive stays conditional and does not force the base
   medium to `SOLID_AGAR`.
5. Preserve `Distilled water` as 1 L, or otherwise omit it from normalized
   `G_PER_L` ingredient rows instead of casting it to `1 G_PER_L`.
6. Add agar ontology grounding during enrichment when a conditional agar row is
   retained.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated `data/merge_yaml/merged/mrs_2_fructose.yaml`.
- Search with `rg --no-ignore --hidden` for `TOGO:M3047`, `TOGO:M2129`, and
  `TOGO:M1852` across `data/normalized_yaml` and `data/merge_yaml` to confirm
  the three records remain distinct after the merge step.
- Manually compare the regenerated `mrs_2_fructose` record against TOGO M3047
  and NBRC 1597 to confirm the fructose value is 20 g, pH is present, agar is
  conditional, and the record no longer has 1% or 10% variant synonyms.

## Additional Notes

- The maintained normalized 2% record already has `Fructose` at `20 G_PER_L`
  and lacks the 2026-08-06 merge fields, so the reviewed wrong value is a
  generated merge artifact.
- The normalized 2% record intentionally points to
  `data/normalized_yaml/bacterial/mrs_1_fructose.yaml` as a
  `CONCENTRATION_VARIANT`; that parent relationship is not the same thing as a
  duplicate merge and should survive the merge fix.
