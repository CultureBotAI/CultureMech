# YAML Record Review: bhi_2_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHI_2_MEDIUM.yaml
- Started UTC: 2026-09-21T20:58:10Z
- Finished UTC: 2026-09-21T20:59:39Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:002815 |
| Name | bhi_2_medium |
| Original name | BHI/2 MEDIUM |
| Category | bacterial |
| Primary source identity | JCM Medium J467, `mediadive.medium:J467` |
| Merged sources | `KOMODO_217_BHI_2_medium`; `bhi_2_medium` / DSMZ Medium 217 |
| Reviewed artifact | `data/merge_yaml/merged/BHI_2_MEDIUM.yaml` |
| Maintained owners | `data/normalized_yaml/bacterial/JCM_J467_BHI_2_MEDIUM.yaml`; `data/normalized_yaml/bacterial/KOMODO_217_BHI_2_medium.yaml`; `data/normalized_yaml/bacterial/bhi_2_medium.yaml` |
| Generated state | Derived duplicate merge from three normalized parents, `merge_fingerprint: 9ff91edcbada32fcc2b78691a8154170782f204c48157ecb0544cb737a7a5423` |

The registry maps `CultureMech:002815` to the JCM parent. KOMODO Medium 217
explicitly cites DSMZ Medium 217, and the DSMZ parent has
`mediadive.medium:217`.

The inspected JCM Medium 467 page lists BHI/2 MEDIUM as 37.0 g Brain Heart
Infusion (BD-Difco), 10.0 g Casein hydrolysate, 5.0 g Glucose, 5.0 g Yeast
extract (BD-Difco), and 1.0 L distilled water. The inspected DSMZ Medium 217
PDF agrees at the aggregate level by saying to add 10.0 g casein hydrolysate,
5.0 g glucose, and 5.0 g yeast extract to DSMZ Medium 215; DSMZ 215 is 37.0 g
Brain Heart Infusion (Difco) plus 1000.0 ml distilled water.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHI_2_MEDIUM.yaml` | Passed; `No issues found` |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHI_2_MEDIUM.yaml --out /private/tmp/BHI_2_MEDIUM.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 ERROR rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHI_2_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 reference checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHI_2_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | Not checked | No focused embedded-`MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone records under `history/` |

The direct `just validate-*` wrappers were not rerun for this record because
this checkout currently fails before target-specific validation while building
`llvmlite==0.46.0` under Python 3.13. The no-project commands above exercise
the same narrow validators under Python 3.11 from a cache outside the project
environment.

## Identity and Grounding

- **The JCM and DSMZ identities are coherent.** JCM J467 is BHI/2 MEDIUM, and
  DSMZ Medium 217 describes the same BHI/2 additives over DSMZ Medium 215.
- **The three-source merge is plausible.** The JCM, KOMODO, and DSMZ
  normalized parents all converge on BHI/2 and share the same local ingredient
  signature.
- **The BHI premix is expanded without source support.** The inspected JCM and
  DSMZ sources keep the commercial BHI powder as one 37.0 g ingredient. The
  record instead decomposes it into Calf brains, Beef heart, Proteose peptone,
  Dextrose, Sodium chloride, and Disodium phosphate, with variable tissue
  amounts and full-strength BHI powder subcomponent masses.
- **JCM's default autoclave instruction is absent.** The JCM page states that
  media should be autoclaved at 121 C for 15 min unless otherwise stated; the
  YAML has no preparation step.

## Evidence

| Claim | Review |
|---|---|
| `mediadive.medium:J467` / BHI/2 MEDIUM | Supported by the inspected JCM GRMD 467 page. |
| `komodo.medium:217` / DSMZ 217 duplicate | Supported by the KOMODO owner note and by the inspected DSMZ 217 wrapper over DSMZ 215. |
| Casein hydrolysate 10 g/L, glucose 5 g/L, yeast extract 5 g/L | Supported by both JCM 467 and DSMZ 217. |
| Six expanded BHI powder constituents | Unsupported. JCM and DSMZ list Brain Heart Infusion as a 37 g aggregate and do not expose these ingredients as independently weighed recipe rows. |
| BD/Difco catalog 237500 plus MicrobeNotes evidence | Unsupported. JCM says BD-Difco, DSMZ says Difco; neither source names catalog 237500, and the URL is a secondary BHI Agar article rather than an official catalog record. |

## Completeness

- The three additive rows after BHI are present and correctly measured.
- The 37.0 g/L Brain Heart Infusion aggregate and 1 L distilled water are
  missing.
- JCM's default 121 C for 15 min autoclave instruction is missing.
- Empty organism and growth slots are acceptable because the inspected medium
  recipe pages do not assert a target organism for this formulation.
- JCM, DSMZ, KOMODO, and BHI catalog evidence are stored in free-text `notes`
  and `supplier_catalog` blocks rather than in structured `references`; the
  reference validator therefore ran zero URL checks.
- A gitignore-independent search with `rg --no-ignore --hidden` over `data`
  and `reports/yaml_record_review` for `CultureMech:002815`,
  `mediadive.medium:J467`, `komodo.medium:217`, `mediadive.medium:217`, and the
  merge fingerprint found the expected normalized owners, generated merge,
  generated indexes, and import-tracking references. A `find` search over the
  ignored `reports/yaml_record_review` directory found no pre-existing
  `*-BHI_2_MEDIUM.md` report before this one was written.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The BHI powder has been replaced by unsupported expanded constituents. | JCM 467 lists 37.0 g Brain Heart Infusion (BD-Difco); DSMZ 217 uses DSMZ 215, which lists 37.0 g Brain Heart Infusion (Difco). All three normalized parents replace that source ingredient with six subcomponent rows from a secondary MicrobeNotes BHI Agar page. | `data/normalized_yaml/bacterial/JCM_J467_BHI_2_MEDIUM.yaml`; `data/normalized_yaml/bacterial/KOMODO_217_BHI_2_medium.yaml`; `data/normalized_yaml/bacterial/bhi_2_medium.yaml`; the BHI premix enrichment source. |
| minor | JCM's default autoclaving instruction is not represented. | The inspected JCM page states its default sterilization condition before the BHI/2 table; the YAML has no `preparation_steps`. | At least `data/normalized_yaml/bacterial/JCM_J467_BHI_2_MEDIUM.yaml`; add to shared duplicate parents only if their source supports the same step. |
| minor | Evidence is free-text and secondary where a structured official reference is needed. | Source URLs live in `notes`, and every BHI subcomponent carries MicrobeNotes as `supplier_catalog.product_url`. No structured `references` exist. | All three normalized parents and the JCM/KOMODO/MediaDive import normalization paths. |

## Recommended Edits

1. Replace the six BHI subcomponent rows in all three normalized parents with
   37.0 g/L `Brain Heart Infusion (BD-Difco)` or a source-specific
   `Brain Heart Infusion (Difco)` aggregate, plus 10.0 g/L Casein hydrolysate,
   5.0 g/L Glucose, 5.0 g/L Yeast extract, and 1 L distilled water.
2. Remove the MicrobeNotes/catalog 237500 supplier rows unless an official BD
   catalog 237500 specification is inspected and is used only to annotate, not
   replace, the source BHI aggregate.
3. Add structured references for JCM GRMD 467, DSMZ Medium 217, and DSMZ Medium
   215 as appropriate for each parent.
4. Add JCM's 121 C / 15 min autoclave step to the JCM parent; preserve the step
   in a merged record only if the selected merge semantics allow source-specific
   preparation details.
5. Regenerate and manually inspect `data/merge_yaml/merged/BHI_2_MEDIUM.yaml`.

## Follow-up Checks

- Rerun schema, strict, term, and reference validation on all three normalized
  owners after curation.
- Manually inspect the regenerated merge to confirm the 37.0 g/L BHI aggregate
  and three additives survive without the unsupported BHI decomposition.
- Run `just verify-merges` and `just audit-merge-freshness` after regeneration.

## Additional Notes

None found.
