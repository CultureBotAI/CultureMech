# YAML Record Review: mp

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mp__49a698dd.yaml
- Started UTC: 2026-09-24T14:10:37Z
- Finished UTC: 2026-09-24T14:11:18Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mp__49a698dd.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:000368`
- Label: `mp`
- Original label: `MP`
- Category: `bacterial`
- Media source: `mediadive.medium:C57`, CCAP Medium `C57`
- Generated status: generated merge output from one normalized source on fingerprint `49a698dd8840a60c06f4e222fefd81ebb638d54293ec0cb79749488a3c2850c8`
- Maintained owner: `data/normalized_yaml/bacterial/mp.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mp__49a698dd.yaml`
  - Result: passed with `No issues found`.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mp__49a698dd.yaml --out /private/tmp/mp__49a698dd.strict.tsv --workers 1 --quiet`
  - Result: passed with 0 errors; `/private/tmp/mp__49a698dd.strict.tsv` contained only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mp__49a698dd.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 0 reference checks were evaluated.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mp__49a698dd.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record resolves to bacterial CCAP Medium `C57`, `MP`, as exposed by the live MediaDive REST record for `C57`.

An ignored-inclusive owner search found two normalized `mp.yaml` files:

| Path | ID | Source | Relation to this record |
| --- | --- | --- | --- |
| `data/normalized_yaml/bacterial/mp.yaml` | `CultureMech:000368` | MediaDive / CCAP `C57` | exact maintained owner |
| `data/normalized_yaml/algae/mp.yaml` | `CultureMech:000092` | CCAP `MP_Modified_Provasoli` | same short label, different CCAP recipe |

The `CultureMech:000368` ID, bounded `mediadive.medium:C57` CURIE, bounded `ID: C57` import note, and exact CCAP PDF URL occur only in `data/normalized_yaml/bacterial/mp.yaml` and the generated `data/merge_yaml/merged/mp__49a698dd.yaml` within `data/normalized_yaml` and `data/merge_yaml/merged`; ignored files were included in that search.

All hydrated salt CHEBI groundings agree with the ingredient strings that the record uses.

## Evidence

The live MediaDive `C57` payload identifies the medium as CCAP `MP` and has two nested solutions:

- Main solution `C57`, final volume `1000 ml`:
  - `1 ml` each of `Ca(NO3)2 x 4 H2O`, `MgSO4 x 7 H2O`, `Na2HPO4 x 2 H2O`, and `KCl` stock solutions.
  - Each of those four rows carries a stock-concentration attribute: `20 g/100 ml`, `2 g/100 ml`, `2 g/100 ml`, and `2.6 g/100 ml`, respectively.
  - `1 ml` of `Stock solution 1`.
  - Deionized water to `1000 ml`.
- `Stock solution 1`, final volume `100 ml`:
  - `0.2 g` `Fe SO4 x 7 H2O`.
  - `0.1 ml` concentrated `H2SO4`.

The CultureMech record does not preserve that two-level structure:

| Row | MediaDive expression | CultureMech expression | Status |
| --- | --- | --- | --- |
| `Ca(NO3)2 x 4 H2O` | `1 ml` of `20 g/100 ml` stock into `1000 ml` | `1 G_PER_L` | stock addition imported as a final mass concentration |
| `MgSO4 x 7 H2O` | `1 ml` of `2 g/100 ml` stock into `1000 ml` | `1 G_PER_L` | stock addition imported as a final mass concentration |
| `Na2HPO4 x 2 H2O` | `1 ml` of `2 g/100 ml` stock into `1000 ml` | `1 G_PER_L` | stock addition imported as a final mass concentration |
| `KCl` | `1 ml` of `2.6 g/100 ml` stock into `1000 ml` | `1 G_PER_L` | stock addition imported as a final mass concentration |
| `Fe SO4 x 7 H2O` | `2 g/L` stock, then `1 ml` stock into `1000 ml` | `2 G_PER_L` | stock concentration imported as a final concentration |
| `H2SO4` | `0.1 ml` concentrated acid per `100 ml` stock, then `1 ml` stock into `1000 ml` | `0.1 G_PER_L` | stock volume imported as a final mass concentration |

The autoclave step in CultureMech is supported by MediaDive.

## Completeness

The record is incomplete in its present flattened form because it has no explicit stock solution representation and does not capture the stock-addition attributes that determine the final amounts for every non-water component.

The direct CCAP PDF URL recorded in CultureMech and returned by MediaDive, `https://www.ccap.ac.uk/wp-content/uploads/MR_Mod_Pringsheim.pdf`, returned HTTP 404 during this review. No live CCAP PDF was inspected.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The four main-solution stock additions were imported as `1 G_PER_L` final ingredient rows. | MediaDive expresses each row as `1 ml` of a stock solution added to `1000 ml`; the stock strengths are in `attribute`, not in `amount`. | `data/normalized_yaml/bacterial/mp.yaml` |
| Major | `Stock solution 1` was flattened at stock strength and its acid volume was assigned the wrong unit semantics. | MediaDive has `0.2 g` `Fe SO4 x 7 H2O` in `100 ml` stock and `0.1 ml` concentrated `H2SO4`; CultureMech stores `2 G_PER_L` Fe salt and `0.1 G_PER_L` `H2SO4` as final rows. | `data/normalized_yaml/bacterial/mp.yaml` |
| Minor | The CCAP source link is stale. | The URL in `notes` and in the live MediaDive `C57` JSON returned HTTP 404. | `data/normalized_yaml/bacterial/mp.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/mp.yaml`, remodel the MediaDive `C57` nested recipe so the main solution adds `1 ml` of each named stock and `1 ml` of `Stock solution 1` rather than casting those additions to final `G_PER_L` values.
2. Preserve each stock formula with its own volume: `20 g/100 ml` for `Ca(NO3)2 x 4 H2O`, `2 g/100 ml` for `MgSO4 x 7 H2O`, `2 g/100 ml` for `Na2HPO4 x 2 H2O`, `2.6 g/100 ml` for `KCl`, and the two `Stock solution 1` rows.
3. If the canonical representation should store only final concentrations, compute them from the stock strength and addition volume instead of importing the MediaDive `amount` fields directly.
4. Update the CCAP PDF URL only if follow-up curation locates a live equivalent for `MR_Mod_Pringsheim.pdf`; otherwise preserve the stale MediaDive provenance with a note that the upstream link is dead.
5. Regenerate `data/merge_yaml/merged/mp__49a698dd.yaml` from the maintained normalized YAML after the owner is corrected.

## Follow-up Checks

- Re-run open schema validation on the regenerated merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm every retained CHEBI and MediaIngredientMech grounding still resolves.
- Re-fetch `https://mediadive.dsmz.de/rest/medium/C57` and manually compare the regenerated YAML to solution IDs `6048` and `6200`.
- Re-check the CCAP PDF URL before changing the source link, because the current MediaDive payload still points at the same 404 URL.

## Additional Notes

- The direct CCAP PDF failure does not block the stock-flattening finding: the live MediaDive `C57` JSON is sufficient to show that the YAML imported stock additions and stock concentrations as final grams per liter.
- The algae `mp.yaml` file is a separate `MP_Modified_Provasoli` record and should not be merged with CCAP `C57`.
