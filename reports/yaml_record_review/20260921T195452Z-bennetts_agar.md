# YAML Record Review: BENNETT'S AGAR

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bennetts_agar.yaml
- Started UTC: 2026-09-21T19:52:20Z
- Finished UTC: 2026-09-21T19:54:52Z
- Verdict: needs curation

## Target

- Reviewed `data/merge_yaml/merged/bennetts_agar.yaml`.
- Class: `MediaRecipe`.
- ID: `CultureMech:002800`.
- Name: `bennetts_agar`.
- Original name: `BENNETT'S AGAR`.
- Source identity: canonical JCM/MediaDive import `mediadive.medium:J44`, label `BENNETT'S AGAR`, with a JCM GRMD 44 link in `notes`.
- Generated status: derived merge output under `data/merge_yaml/merged/`.
- Maintained owners merged into this record: `data/normalized_yaml/bacterial/JCM_J44_BENNETT_S_AGAR.yaml`, `data/normalized_yaml/bacterial/bennetts_agar.yaml`, and `data/normalized_yaml/bacterial/medium_548_modified_for_dsm_41755.yaml`.
- Merge metadata: three source recipes, `JCM_J44_BENNETT_S_AGAR`, `bennetts_agar`, and `medium_548_modified_for_dsm_41755`, on fingerprint `3537c3f57c2f034ab493afd826e8a723947121250feb1dd4c32ec56672ad72a8`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bennetts_agar.yaml` via the no-project Python 3.11 workaround | Passed |
| Closed-schema strict validation, `scripts/validate_strict.py data/merge_yaml/merged/bennetts_agar.yaml --out /private/tmp/bennetts_agar.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/bennetts_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks were applicable |
| `linkml-term-validator validate-data data/merge_yaml/merged/bennetts_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not a focused embedded-history validator for one merged record |

The documented `just` commands were not run directly because the project runtime currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation.

## Identity and Grounding

- `CultureMech:002800` resolves in `data/culturemech_id_registry.tsv` to `data/normalized_yaml/bacterial/JCM_J44_BENNETT_S_AGAR.yaml`.
- JCM GRMD 44, DSMZ Medium 548, MediaDive `J44`, the imported DSMZ `bennetts_agar` owner, and the generated label all denote base Bennett's Agar.
- The KOMODO `medium_548_modified_for_dsm_41755` source has been merged as a `SOURCE_DUPLICATE` of the JCM record, but its maintained input still carries a source-era `ph_range` of 4.5-5.5 alongside the DSMZ-enriched pH value 7.3. That unresolved conflict is lost in the generated canonical record.
- `Glucose` and `Agar` are exactly grounded. `Yeast extract`, `Beef extract`, and `N-Z amine` are intentionally mixture-like ingredients; the first two are FOODON-grounded in the source owners, while `N-Z amine` remains explicitly ungrounded.

## Evidence

- JCM GRMD 44 supports the canonical JCM source identity, 1 g/L yeast extract, 1 g/L beef extract, 2 g/L N-Z amine type A, 10 g/L glucose, 15 g/L agar, 1 L distilled water, and pH 7.3.
- The fetched DSMZ Medium 548 PDF supports the same five non-water ingredients, 1000 ml distilled water, pH 7.3, and the same base Bennett's Agar identity for the DSMZ `bennetts_agar` owner.
- DSMZ Medium 548 additionally says pH 7.3 is adjusted with NaOH and gives a modified Bennett's agar note in which 10 g glycerol replaces glucose and 2 g Bacto casein replaces N-Z amine. The generated merge keeps only the shorter JCM preparation step, `Adjust pH to 7.3.`
- The KOMODO owner records `MEDIUM 548 MODIFIED FOR DSM 41755` as its original source term and keeps the five DSMZ Medium 548 ingredients, but it also contains both `ph_range: 4.5-5.5` and `ph_value: 7.3`. The generated merge emits the pH 7.3 scalar and a `SOURCE_DUPLICATE` child relation without carrying the contradictory KOMODO pH range forward as an unresolved conflict.
- A gitignore-independent exact search over the three maintained owners, the generated Bennett merge siblings, relevant TOGO/KOMODO Bennett sources, ID registries, and recipe catalogs found the JCM and DSMZ owners that feed this merge, the KOMODO `CultureMech:005992` duplicate child, other active Bennett-family records that do not share this exact fingerprint, and the generated `bennetts_agar` merge. It did not find a raw MediaDive, KOMODO, JCM, or DSMZ source payload committed for these three source records.

## Completeness

- The direct five-ingredient formulation is complete and source-supported; omission of distilled water as an explicit ingredient is acceptable because both inspected sources bring the five components to 1 L.
- Consequentially incomplete: the KOMODO pH range conflict is not surfaced in the canonical merge or in a quality flag, so a reader sees only pH 7.3 even though one merged source owner still says pH 4.5-5.5.
- Minor source detail gap: DSMZ's NaOH pH-adjuster text and modified Bennett's agar option are retained in `data/normalized_yaml/bacterial/bennetts_agar.yaml` but do not survive into the generated merge.
- Empty target-organism and growth-evidence slots are acceptable here; the inspected JCM and DSMZ recipe sources do not name a strain-specific growth result.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `medium_548_modified_for_dsm_41755.yaml` is merged as a source duplicate even though it still has an unresolved `ph_range` 4.5-5.5 conflicting with the DSMZ-derived pH 7.3 used by the canonical merge. | `data/normalized_yaml/bacterial/medium_548_modified_for_dsm_41755.yaml` |
| Minor | DSMZ-only preparation details are dropped from the canonical merge: pH adjustment with NaOH and the modified Bennett's agar substitution note are present in the DSMZ owner but absent from `data/merge_yaml/merged/bennetts_agar.yaml`. | `data/normalized_yaml/bacterial/bennetts_agar.yaml`; merge preservation logic |
| Minor | The JCM-specific N-Z amine supplier detail is reduced to `notes: ' ( type A)'`, which preserves the type but loses the Sheffield supplier and leaves a malformed leading space. | `data/normalized_yaml/bacterial/JCM_J44_BENNETT_S_AGAR.yaml` |

## Recommended Edits

1. Recheck the KOMODO `548_41755` source behind `data/normalized_yaml/bacterial/medium_548_modified_for_dsm_41755.yaml`; if pH 4.5-5.5 is source-supported, stop treating it as a source duplicate of pH 7.3 Bennett's Agar, and if it was a stale import artifact, remove it with a curation history entry.
2. Decide whether the DSMZ modified Bennett's agar note should become a structured variant with glycerol and Bacto casein, a source-specific preparation note retained through merging, or an explicit discussion item.
3. Preserve DSMZ's NaOH pH-adjuster detail during merge, either as a source-specific preparation detail or as a non-conflicting elaboration on the JCM pH 7.3 instruction.
4. Normalize the JCM N-Z amine note to carry `type A` and the Sheffield supplier without a malformed leading parenthetical.
5. Regenerate `data/merge_yaml/merged/bennetts_agar.yaml` after the normalized owners are corrected.

## Follow-up Checks

- Rerun the open schema validator, `scripts/validate_strict.py`, `linkml-term-validator`, and `linkml-reference-validator` on each changed normalized owner and the regenerated merged record.
- Manually compare the regenerated merge against JCM GRMD 44 and the DSMZ Medium 548 PDF for the five base ingredients, pH 7.3, the NaOH pH-adjuster note, and any retained modified Bennett's agar variant detail.
- Run `just verify-merges` after regenerating to confirm `medium_548_modified_for_dsm_41755.yaml` is no longer silently merged if its pH 4.5-5.5 range is real.

## Additional Notes

- The exact Bennett search that supports this review included ignored files by using `rg --no-ignore --hidden`, but it was scoped to the three maintained owners, generated Bennett merge siblings, relevant TOGO/KOMODO Bennett sources, ID registries, and recipe catalogs to avoid unrelated `J44x` JCM records.
- Reports are read-only review artifacts; no normalized recipe, generated merge, page, history entry, or GitHub issue was edited for this review.
