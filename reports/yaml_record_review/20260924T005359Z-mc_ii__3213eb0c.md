# YAML Record Review: mc_ii

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mc_ii__3213eb0c.yaml
- Started UTC: 2026-09-24T00:52:36Z
- Finished UTC: 2026-09-24T00:54:09Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000361 |
| Label | mc_ii |
| Original label | MC II |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mc_ii__3213eb0c.yaml |
| Maintained owner | data/normalized_yaml/bacterial/mc_ii.yaml |
| Source accession | mediadive.medium:C50 |

The generated record is derived from `data/normalized_yaml/bacterial/mc_ii.yaml`
and only differs by generated merge metadata. Future fixes belong in that
normalized owner or in source-copy deduplication for the repeated CCAP MC II
records.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mc_ii__3213eb0c.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mc_ii__3213eb0c.yaml --out /private/tmp/mc_ii__3213eb0c.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mc_ii__3213eb0c.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mc_ii__3213eb0c.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The record is schema-valid despite final-volume and serum-unit errors.

## Identity and Grounding

- MediaDive C50 and the CCAP `MR_MCII.pdf` both identify this source as `MC II`
  / Modified Chang's Medium (MC).
- Exact `find` and `rg --no-ignore --hidden` checks found the bacterial C50
  owner, `data/normalized_yaml/bacterial/mc_ii.yaml`, plus two algae records
  citing `MR_MCII.pdf`: `data/normalized_yaml/algae/mc_ii.yaml` and
  `data/normalized_yaml/algae/CCAP_MC II .yaml`.
- The filename-collision report already flags the bacterial/algae `mc_ii.yaml`
  pair as `DIFFERENT`, and the two algae MC II files have different
  CultureMech IDs but the same CCAP source.
- A gitignore-independent exact scan of the generated bacterial target and its
  normalized owner found no `references` or `target_organisms` sections.

## Evidence

### Supported

- The five dry ingredient names and amounts come from the CCAP PDF and MediaDive
  C50: 10 g casein digest, 1.325 g anhydrous Na2HPO4, 0.8 g KH2PO4, 5 g yeast
  extract, and 2.5 g D-glucose.
- pH 6.9, 5 x 180 ml aliquots, pressure cooking at 10 psi for 15 minutes,
  aseptic foetal calf serum addition, and storage at 4 C are supported by the
  CCAP PDF.
- The `Na2HPO4` grounding to `CHEBI:34683` / `disodium hydrogenphosphate` is
  compatible with the CCAP PDF's anhydrous sodium phosphate label.

### Unsupported or Over-scoped

- The dry ingredient concentrations are normalized against 900 ml instead of the
  final liter after serum addition. CCAP labels the formula `per litre` and adds
  serum to make a final 10 percent concentration.
- The bacterial record omits the 900 ml deionised-water row.
- `Calf serum` is not a 20 g/l ingredient. The CCAP PDF adds 5 x 20 ml sterile
  foetal calf serum to 5 x 180 ml aliquots.
- Casein and Yeast extract remain ungrounded in the bacterial C50 owner even
  though the same exact labels are grounded elsewhere in the corpus.
- The two algae MC II records are malformed partial extractions, so they are not
  reliable duplicates to collapse into yet.

## Completeness

- The bacterial record is missing structured CCAP source references.
- `target_organisms` is empty and acceptable; the CCAP PDF gives a broad
  freshwater-protozoa use case, not a specific strain-level growth claim.
- The MC II source family needs deduplication across at least three normalized
  records that point to the same CCAP PDF.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | Three normalized records cite the same CCAP MC II source without being reconciled. | The exact search found the bacterial C50 record and two algae records, `mc_ii.yaml` and `CCAP_MC II .yaml`, all citing `MR_MCII.pdf`; the two algae records have malformed three-row formulas. | `data/normalized_yaml/bacterial/mc_ii.yaml`, `data/normalized_yaml/algae/mc_ii.yaml`, `data/normalized_yaml/algae/CCAP_MC II .yaml`, and merge/dedupe inputs. |
| major | Dry ingredient concentrations are inflated by using 900 ml rather than the source final liter. | The PDF's per-liter dry masses are 10, 1.325, 0.8, 5, and 2.5 g, whereas the MediaDive-derived rows store 11.1111, 1.47222, 0.888889, 5.55556, and 2.77778 G_PER_L. | `data/normalized_yaml/bacterial/mc_ii.yaml` or the MediaDive importer. |
| major | The foetal calf serum addition has the wrong unit and amount. | CCAP adds 20 ml serum to each 180 ml aliquot for a final concentration of 10 percent; the bacterial record stores `Calf serum` as `20 G_PER_L`. | `data/normalized_yaml/bacterial/mc_ii.yaml` or the MediaDive importer. |
| major | The source water row is missing. | CCAP says to add the first five constituents to 900 ml deionised water; the bacterial record has no water row. | `data/normalized_yaml/bacterial/mc_ii.yaml`. |
| minor | Casein and Yeast extract are ungrounded. | The normalized C50 owner has no `term` for either complex ingredient even though its C49 sibling now has exact FoodOn mappings for the same labels. | `data/normalized_yaml/bacterial/mc_ii.yaml`. |
| minor | Preparation actions are poorly structured. | `To be added later:` is imported as a `MIX` step, the aliquoting and pressure-cooking instruction is labeled `POUR_PLATES`, and `Store at 4 C` is labeled `HEAT`. | `data/normalized_yaml/bacterial/mc_ii.yaml` or the MediaDive importer. |

## Recommended Edits

1. Repair all CCAP MC II source copies against `MR_MCII.pdf`, then either
   collapse them or link them explicitly as source duplicates.
2. Replace the 900 ml-normalized dry ingredient concentrations with the CCAP
   per-liter masses and add 900 ml deionized water.
3. Represent sterile foetal calf serum as 100 ml/l, not 20 g/l.
4. Ground Casein and Yeast extract using the same verified exact labels already
   applied to the bacterial MC owner.
5. Split preparation into dissolve, pH adjustment, aliquot, pressure-cook,
   aseptic serum-addition, and 4 C storage actions.
6. Add structured references for CCAP MC II.

## Follow-up Checks

- Rerun focused open-schema, strict, term, and reference validation on each
  repaired owner and regenerated merged record.
- Recompare the bacterial and algae MC II owners against the CCAP PDF to ensure
  they agree on the five dry components, 900 ml deionized water, and 10 percent
  final serum addition.
- Run merge freshness and duplicate/collision checks so the three source copies
  either collapse or remain intentionally linked.
- Repeat a gitignore-independent exact search for `CultureMech:000361`,
  `mediadive.medium:C50`, and `MR_MCII.pdf` to confirm regenerated outputs and
  indexes all reflect the chosen curation.

## Additional Notes

- The source PDF misspells anhydrous as `anydrous`; this review preserved the
  intended anhydrous sodium phosphate identity without copying the typo into a
  recommendation.
