# YAML Record Review: BG11

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/bg11__d1732022.yaml`
- Started UTC: 2026-09-21T20:18:15Z
- Finished UTC: 2026-09-21T20:20:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Generated record | `data/merge_yaml/merged/bg11__d1732022.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M2499_BG11.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:009072` |
| Name | `bg11` |
| Original name | `BG11` |
| Source accession | `TOGO:M2499` |
| Merge fingerprint | `d173202212f4de0e28918715a9b95d5b944aefab80a7c902e015009ad1d2a353` |
| Merge sources | `TOGO_M2499_BG11` |

This is a generated single-source TOGO M2499 merge. Future curation belongs in `data/normalized_yaml/bacterial/TOGO_M2499_BG11.yaml` or in the TOGO importer/solution-nesting path, followed by regenerated merge and page output.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bg11__d1732022.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bg11__d1732022.yaml --out /private/tmp/bg11__d1732022.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bg11__d1732022.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bg11__d1732022.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | Not run | Not checked: this repository documents `just validate-history` for standalone records under `history/`; there is no focused embedded-`MediaRecipe.curation_history` validator for one generated merge record. |

All focused validators pass; they do not catch stale generated output, source pH loss, or the flattened A5 trace-stock boundary.

## Identity and Grounding

- **Source identity:** supported. The live TOGO API for M2499 reports `gm = http://togomedium.org/medium/M2499`, `name = BG11`, and pH `7.1`, matching `TOGO:M2499` in `media_term`.
- **Maintained owner:** resolved. Exact `rg --no-ignore --hidden` searches for `CultureMech:009072`, `TOGO:M2499`, `TOGO_M2499_BG11`, `M2499`, and the merge fingerprint across `data/normalized_yaml`, this generated record, and reports resolved `data/normalized_yaml/bacterial/TOGO_M2499_BG11.yaml` as the owner.
- **Ingredient grounding:** partly supported. Hydrated magnesium, calcium, sodium molybdate, zinc sulfate, and cobalt nitrate have exact-looking CHEBI labels; `CuSO2*5H2O` is malformed in TOGO while TOGO's own label says copper(II) sulfate pentahydrate, so the copper row should remain unresolved until source normalization confirms `CuSO4*5H2O`.
- **Generated freshness:** stale. The normalized owner has a Sep 2, 2026 `repair_merged_duplicates.py` event that collapses the duplicate water row to one litre; the generated merge still has `Distilled water` at `2.0 G_PER_L`.

## Evidence

- TOGO M2499 has a final BG11 component table with 1 L water, six direct g/L salts, `EDTA (disodium magnesium salt)` at 0.001 g/L, and `Trace-metal mix A5` at 1 ml/L.
- TOGO M2499 separately defines the `Trace-metal mix A5` stock as 1 L water plus `Na2MoO4*2H2O`, `H3BO3`, `MnCl2*4H2O`, `ZnSO4*7H2O`, `CuSO2*5H2O`, and `Co(NO3)2*6H2O` at stock-strength g/L amounts.
- The record keeps both levels as top-level `ingredients`: `Trace-metal mix A5` appears as `1 G_PER_L`, and every A5 component appears as if its stock-strength amount were a final BG11 ingredient.
- TOGO reports pH 7.1 in metadata and a comment saying that after autoclaving and cooling the medium pH is 7.1; the generated record has no `ph_value` and no `preparation_steps`.
- The source does not name a target organism or growth experiment. Empty growth-evidence slots are acceptable for this TOGO-only projection.

## Completeness

- The direct BG11 g/L components are present and source-supported.
- The `Trace-metal mix A5` stock is not complete enough: its ingredients need to be nested under the stock or represented by a referenced stock solution so their stock concentrations are not confused with final BG11 concentrations.
- pH/autoclaving context is missing from both the generated record and the normalized owner.
- The source typo `CuSO2*5H2O` is carried through without a discussion or quality flag, even though the label says copper(II) sulfate pentahydrate.
- A gitignore-independent `find reports/yaml_record_review -maxdepth 1 -type f -name '*-bg11__d1732022.md'` found no pre-existing review report for this generated record.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Trace-metal mix A5` is flattened into final-medium ingredients. | TOGO defines A5 as a stock added at 1 ml/L, but the YAML also lists A5's molybdate, boric acid, manganese, zinc, copper, and cobalt salts as top-level ingredients at stock g/L amounts. | `data/normalized_yaml/bacterial/TOGO_M2499_BG11.yaml` or TOGO solution-nesting logic. |
| Major | pH and autoclaving/cooling instructions are missing. | TOGO metadata gives pH 7.1 and its comments state that after autoclaving and cooling the pH is 7.1; no corresponding `ph_value` or `preparation_steps` exists. | Normalized owner or TOGO comment importer. |
| Major | The generated merge is stale relative to the normalized owner. | `Distilled water` is `2.0 G_PER_L` in the generated merge but `1.0 G_PER_L` in the normalized owner after the Sep 2 duplicate-repair event. | Regenerate `data/merge_yaml/merged/` and verify merge freshness. |
| Major | The copper sulfate formula is malformed and unresolved. | TOGO and the YAML carry `CuSO2*5H2O` while the TOGO label says copper(II) sulfate pentahydrate; no ontology term is attached. | Normalized owner; verify whether the source should be corrected to `CuSO4*5H2O` or left with a quality flag. |
| Minor | The medium is classified as complex/undefined despite having only defined chemical components. | TOGO M2499's direct medium and A5 stock list only defined salts plus water. | Normalized owner or TOGO importer defaults. |

## Recommended Edits

1. Nest `Trace-metal mix A5` as a stock solution used at 1 ml/L and move its six salt components out of final-medium `ingredients`.
2. Preserve the TOGO pH and autoclaving/cooling note as `ph_value: 7.1` and a source-supported preparation step.
3. Regenerate the merged record after the normalized Sep 2 water repair so `Distilled water` is no longer doubled.
4. Resolve the `CuSO2*5H2O` row by inspecting TOGO/Japanese source provenance if available; use `CuSO4*5H2O` only if source text supports that correction, otherwise add a quality flag that TOGO's formula and label disagree.
5. Change `medium_type` and `composition_type` to defined classifications if the repository's classifier agrees that this source has no undefined components.

## Follow-up Checks

- Re-run open-schema, strict-schema, reference, and term validation on the normalized owner and regenerated merge.
- Re-run `just verify-merges` and `just audit-merge-freshness` after regeneration to prove the Sep 2 water repair is propagated.
- Manually compare the regenerated TOGO M2499 tree against `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2499`, checking that A5 remains nested and only its 1 ml/L addition appears in the final medium.
- Search with `rg --no-ignore --hidden "CuSO2"` after copper-formula curation to make sure any remaining malformed formula is intentional and flagged.

## Additional Notes

- The TOGO source API has no `src_url` for this record, so the review could not chase a non-TOGO original recipe page.
- This report is read-only and intentionally did not edit `data/normalized_yaml/bacterial/TOGO_M2499_BG11.yaml`, `data/merge_yaml/merged/bg11__d1732022.yaml`, generated pages, or GitHub state.
