# YAML Record Review: tap_auto

- Repository: CultureMech
- Record: `data/merge_yaml/merged/tap_auto.yaml`
- Started UTC: 2026-09-25T09:00:41Z
- Finished UTC: 2026-09-25T09:01:27Z
- Verdict: needs curation

## Target

Generated merged record `CultureMech:007283` for `tap_auto`, with
`MEDIADB:37`, fingerprint
`af810fcbbd1feb8632c4fef72b005d6b0fbca5f809a0d8bb76bdfe1624492739`, and one
merged source, `tap_auto`.

## Validation

- LinkML schema: passed.
- Strict validator: passed with 0 ERROR rows.
- Reference validator: passed with 0 checks.
- Term validator: passed.
- Embedded `curation_history`: Not checked: the available history validator checks
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The record is correctly grounded to MediaDB medium 37, `Tap (auto)`, but the
generated merge is stale relative to
`data/normalized_yaml/bacterial/tap_auto.yaml`. The normalized source has
already repaired `original_name` and `media_term.term.label` to `TAP (auto)`,
grounded every one of the 17 MediaDB components, added source-backed pH and
temperature conditions, linked Chlamydomonas reinhardtii growth evidence, and
made `tap_hetero_mixo` a 17.4 mM acetate-supplemented child variant.

## Evidence

MediaDB 37 lists 17 millimolar components for TAP (auto), and its
`media_text/37` export matches the generated ingredient amounts exactly. MediaDB
also links source 15, Boyle et al. 2009, and growth-data record 86 for
Chlamydomonas reinhardtii on TAP (auto).

MediaDB growth-data record 86 reports Chlamydomonas reinhardtii on MediaDB 37 at
growth rate `0.059` 1/h, pH 7.0, temperature 25.0 C, carbon dioxide uptake 2.04
mmol/gDW/h, and max solar flux 2100 uE/m2/s. PubMed record `PMID:19128495`
confirms the Boyle et al. article DOI `10.1186/1752-0509-3-4`.

## Completeness

The generated ingredient amounts are complete for the MediaDB 37 export, but the
generated record is missing most of the already curated normalized fields:
ingredient CHEBI links for ions, `ph_value`, `temperature_value`,
`target_organisms`, MediaDB source/growth references, `data_quality_flags`, and
the `variant_children` relationship to `tap_hetero_mixo`.

The generated record still has generic `ADJUST_PH` and 0.22 um
`FILTER_STERILIZE` preparation steps that are not present on the fetched MediaDB
37 page or tab-delimited export.

## Findings

- `original_name` and `media_term.term.label` still contain the old MediaDB
  parenthesis-truncation artifact.
- The generated merge is missing most MediaDB TAP curation already present on
  the normalized source from `repair_mediadb_tap_score15.py`.
- Most ion ingredients still lack CHEBI-keyed `term` and
  `mediaingredientmech_chebi_term` entries in the generated record.
- The generated merge has no `target_organisms`, no Boyle et al. reference, no
  growth-data record 86 evidence, and no TAP hetero/mixo child variant link.
- The generic preparation steps are not source-backed by MediaDB 37.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/tap_auto.yaml` from the curated normalized
  MediaDB 37 source so the repaired name, ion groundings, pH/temperature fields,
  growth evidence, DOI, and TAP hetero/mixo variant link are carried forward.
- Drop the generic `DISSOLVE`, `ADJUST_PH`, and 0.22 um filtration placeholders
  unless a MediaDB SQL field or Boyle et al. passage supports them.
- Keep the 17 millimolar MediaDB components unchanged while regenerating; they
  already match `media_text/37`.

## Follow-up Checks

- After regeneration, verify that `tap_hetero_mixo` still points back to
  `CultureMech:007283` as an acetate-supplemented variant.
- Confirm that the generated record includes MediaDB growth-data record 86 and
  DOI `10.1186/1752-0509-3-4`.
- Confirm the generated record no longer contains the broken label
  `'''TAP (auto`.

## Additional Notes

None found
