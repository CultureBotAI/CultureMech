# YAML Record Review: bg11_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BG11_medium.yaml
- Started UTC: 2026-09-21T20:26:21Z
- Finished UTC: 2026-09-21T20:28:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/BG11_medium.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` |
| ID | `CultureMech:009592` |
| name / original_name | `bg11_medium` / `BG11 medium` |
| source identity | `TOGO Medium M3120`, `TOGO:M3120` |
| source URL in record | `https://togomedium.org/medium/M3120` |
| merge fingerprint | `89299c92a9e679b75b912b413c607e9dc36bfeec6324cd564eaedda8c8fb60e7` |
| merged_from | `TOGO_M3120_BG11_medium` |

The reviewed file is a generated one-source merge of
`data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml`. A `find` search
under `reports/yaml_record_review` found no existing
`*-BG11_medium.md` report; `find` includes ignored files.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BG11_medium.yaml` | Passed: `No issues found` |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BG11_medium.yaml --out /private/tmp/BG11_medium.strict.tsv --workers 1 --quiet` | Passed |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BG11_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with no diagnostics |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BG11_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| TOGO source fetch | `curl -L -o /private/tmp/togo_M3120.json 'https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M3120'` | Passed and returned `gm = http://togomedium.org/medium/M3120`, `name = BG11 medium`, and `ph = 7.4` |
| Embedded `curation_history` validation | Not checked: this repository documents `just validate-history` for standalone `history/` records, not for embedded `MediaRecipe.curation_history` entries in one generated merge |

## Identity and Grounding

The generated record's stable ID resolves to the expected maintained owner:
`data/culturemech_id_registry.tsv` maps `CultureMech:009592` to
`data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml`, and generated
source indexes also list `source_id: TOGO:M3120` for that path.

An exact gitignore-independent search for `CultureMech:009592`,
`TOGO_M3120_BG11_medium`, `TOGO:M3120`, and the merge fingerprint across
`data` and `reports/yaml_record_review` found the maintained owner, generated
merge, generated indexes, and import diagnostics for this same TOGO M3120
record. It found no competing normalized record with this CultureMech ID.

The source is TOGO M3120 "BG11 medium", but several component identities are
incomplete or distorted:

- TOGO reports `K2HPO4.3H2O` as `GMO_001125`, label "Potassium phosphate
  dibasic trihydrate"; the YAML grounds it to `CHEBI:131527`, dipotassium
  hydrogen phosphate, without retaining the trihydrate identity.
- TOGO reports `MnCI2.4H2O` as `GMO_001020`, label "Manganese chloride
  tetrahydrate"; the YAML leaves it ungrounded.
- TOGO reports `EDTA (disodium magnesium salt)` as `GMO_001958`, but the YAML
  leaves it ungrounded.
- TOGO reports `Trace metal mix A5 + Co*` as a 1 ml stock addition; the YAML
  creates a solution with an empty `composition`, `name: Unknown solution`, and
  `concentration: 1 G_PER_L`.

## Evidence

The TOGO API represents BG11 in two formulation blocks:

| TOGO block | Source content |
|---|---|
| main medium | `MgSO4.7H2O` 0.075 g, `CaCl2.2H2O` 0.036 g, `Na2CO3` 0.02 g, `NaNO3` 1.5 g, `K2HPO4.3H2O` 0.04 g, ferric ammonium citrate 0.006 g, deionized water 1000 ml, citric acid 0.006 g, `EDTA (disodium magnesium salt)` 0.001 g, and `Trace metal mix A5 + Co*` 1 ml |
| `Trace metal mix A5 + Co*` | `Na2MoO4.2H2O` 0.39 g, `H3BO3` 2.86 g, `MnCI2.4H2O` 1.81 g, `ZnSO4.7H2O` 0.222 g, `CuSO4.5H2O` 0.079 g, `Co(NO3)2.6H2O` 0.0494 g, and deionized water 1000 ml |

The root-level main-medium amounts for magnesium sulfate, calcium chloride,
sodium carbonate, sodium nitrate, ferric ammonium citrate, and citric acid
match TOGO's main block. The trace metals do not: TOGO nests them inside the
A5 + Co stock, whereas the YAML lists them as final-medium root ingredients and
also leaves an empty stock-solution shell at the end of the record.

TOGO also stores `ph: 7.4`; neither the generated merge nor the maintained
normalized file has `ph_value`.

There is no literature `evidence` block to inspect. The TOGO source has a
comment about routine cultivation of Synechocystis seed cultures in liquid BG11
or 1.25% BG11 agar at 30 C under 30 umol photons m-2 s-1, but the imported
record does not promote that comment into target-organism or growth claims.

## Completeness

The generated merge is stale. The maintained owner contains a
2026-09-02 `REPAIRED_SUMMED_DUPLICATE_MERGE` curation event that collapsed the
`Deionized water` row from `2000.0` back to `1000.0`; the generated merge still
contains the pre-repair `2000.0 G_PER_L` water ingredient and lacks that event.

The maintained owner is still incomplete after that repair:

- 1000 ml of main-medium water is represented as `1000.0 G_PER_L` instead of a
  preparation volume.
- The 1 ml `Trace metal mix A5 + Co*` addition is represented as `1 G_PER_L`.
- The trace stock's composition is not attached to the solution.
- The trace salts are duplicated into the final medium at stock strength.
- `ph_value: 7.4` is absent.

Empty growth evidence and target-organism slots are acceptable for the imported
source block because M3120 is primarily a formulation record and the TOGO
comment does not report a quantitative growth metric.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The reviewed generated merge is stale relative to its normalized owner. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` has a 2026-09-02 event that repaired the duplicate water merge to `1000.0`; `data/merge_yaml/merged/BG11_medium.yaml` still has `2000.0`. | Regenerate `data/merge_yaml/merged/BG11_medium.yaml` from normalized inputs |
| major | The trace-metal stock is both flattened into the final medium and represented as an empty solution. | TOGO M3120 adds 1 ml of `Trace metal mix A5 + Co*`; the YAML records an empty `solutions` entry with `1 G_PER_L` and also places the stock's gram quantities at the root ingredient level. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` or the TOGO importer |
| major | The source pH is dropped. | The fetched TOGO M3120 API response has `meta.ph = 7.4`; the YAML has no `ph_value`. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` or the TOGO importer |
| major | `K2HPO4.3H2O` is grounded to an anhydrous phosphate term. | TOGO identifies the component as potassium phosphate dibasic trihydrate; the YAML uses `CHEBI:131527` for dipotassium hydrogen phosphate. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` |
| minor | Two source-resolved TOGO components remain ungrounded in the YAML. | TOGO provides GMO identifiers and labels for `MnCI2.4H2O` and `EDTA (disodium magnesium salt)`, but the imported rows have no `term` or MIM link. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` |
| minor | BG11 is classified as `COMPLEX` / `UNDEFINED` even though TOGO lists only defined chemical components and water. | The fetched source rows for the main medium and A5 + Co stock are all named chemical or water components. | `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` |

## Recommended Edits

1. Regenerate the merged BG11 output so the 2026-09-02 normalized duplicate-water
   repair is reflected in `data/merge_yaml/merged/BG11_medium.yaml`.

2. Repair `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml` so
   `Trace metal mix A5 + Co*` is a populated stock solution with the TOGO
   trace-salt composition and with a 1 ml final-medium addition, not a
   `1 G_PER_L` placeholder.

3. Move trace-salt stock components out of root `ingredients`; they belong in
   the A5 + Co stock composition unless a direct-equivalent final concentration
   is explicitly calculated from the 1 ml/L dilution.

4. Convert deionized water from a root concentration to final-volume and stock
   make-up instructions.

5. Add `ph_value: 7.4` from TOGO M3120.

6. Reground `K2HPO4.3H2O` to the exact hydrated phosphate, resolve
   `MnCI2.4H2O` from TOGO's manganese chloride tetrahydrate identity, and leave
   `EDTA (disodium magnesium salt)` explicitly unresolved only if no exact
   chemical grounding is available.

7. Revisit `medium_type` and `composition_type`; BG11 M3120 appears chemically
   defined once the trace mix is represented as a stock solution rather than an
   unknown complex additive.

## Follow-up Checks

- Rerun open-schema, strict, term, and reference validation on the repaired
  normalized owner and on regenerated `data/merge_yaml/merged/BG11_medium.yaml`.
- Run `just verify-merges` and `just audit-merge-freshness` to prove the
  generated merge includes the 2026-09-02 owner repair and any new curation
  event.
- Fetch TOGO M3120 again and compare all ten main-medium rows plus all seven
  A5 + Co rows against the normalized YAML.
- Re-run the exact gitignore-independent search for `CultureMech:009592` and
  `TOGO:M3120` after regeneration to verify the generated indexes still map
  that ID and source to `data/normalized_yaml/bacterial/TOGO_M3120_BG11_medium.yaml`.

## Additional Notes

The gitignore-independent exact search for `M3120`, `TOGO_M3120_BG11_medium`,
and `TOGO:M3120` covered `data/raw`, `data/normalized_yaml`,
`data/import_tracking`, and `data/merge_yaml`. It found this maintained owner,
generated merge, generated indexes, and import diagnostics; no raw TOGO M3120
payload was checked in under `data/raw`.

The current source URL in the YAML is a SPA route that returns only a shell in
non-browser fetches. The inspected source data came from TOGO's JSON endpoint:
`https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M3120`.
