# YAML Record Review: reinforced_clostridial_medium_with_4_sea_salt

- Repository: CultureMech
- Record: `data/merge_yaml/merged/reinforced_clostridial_medium_with_4_sea_salt.yaml`
- Started UTC: 2026-09-25T01:54:29Z
- Finished UTC: 2026-09-25T01:55:55Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:009870` for TOGO Medium M483 / `TOGO:M483`, generated by an August merge of `data/normalized_yaml/bacterial/TOGO_M482_Reinforced_Clostridial_Medium_With_3_Sea_Salt.yaml` and `data/normalized_yaml/bacterial/TOGO_M483_Reinforced_Clostridial_Medium_With_4_Sea_Salt.yaml`.

## Validation

- Open schema validation: passed with `No issues found`.
- Strict validation: passed; `scripts/validate_strict.py` reported 0 errors and wrote only the TSV header.
- Reference validation: passed; 0 checks.
- Term validation: passed with the known `eutils` / `pkg_resources` deprecation warning.
- Embedded history: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

Exact ignored-inclusive searches found `TOGO:M482` on the 3% owner, the stale generated synonym, and the manifest row, and found `TOGO:M483` on the 4% owner, the generated primary media term, and the manifest row. TOGO M482 maps to JCM Medium 481 / 3% sea salt, while TOGO M483 maps to JCM Medium 482 / 4% sea salt.

## Evidence

TOGO M482 and JCM 481 list 38 g Reinforced clostridial medium (BD-Difco), 30 g Sea salts (Sigma), 15 g agar, and 1 L distilled water. TOGO M483 and JCM 482 list the same base recipe except Sea salts (Sigma) is 40 g. The maintained normalized owners have already been repaired as separate salinity variants with 30.0 g/L and 40.0 g/L sea salts respectively.

## Completeness

The generated target is internally contradictory because it combines the 4% source identity and child-variant metadata with the 3% ingredient amount and a stale merged synonym for the 3% source.

## Findings

- The generated target falsely merged the 3% and 4% JCM sea-salt variants under one `merge_fingerprint`. JCM 481 and JCM 482 are not duplicate recipes; the sea-salt amount changes from 30 g/L to 40 g/L.
- The generated record is named `Reinforced Clostridial Medium With 4%  Sea Salt` and uses `TOGO:M483`, but its `Sea salts (Sigma)` row is 30 g/L from the 3% parent instead of the 40 g/L listed by TOGO M483 and JCM 482.
- The generated record is stale relative to both September-repaired normalized owners: it still has `Distilled water` as `1 G_PER_L`, lacks the source-scoped owner notes, lacks the JCM default autoclave step and sterilization object, and lacks the TOGO/JCM references.

## Recommended Edits

- Regenerate `data/merge_yaml/merged` so TOGO M482 and TOGO M483 emit as separate records or as explicit parent/child salinity variants, not one duplicate merge.
- Ensure the regenerated 4% target carries 40.0 g/L Sea salts (Sigma), 38.0 g/L Reinforced clostridial medium (BD-Difco), 15.0 g/L agar, and 1.0 L distilled water.
- Preserve the normalized `parent_media`, `variant_relationship`, and `variant_modifications` fields from the 4% owner.

## Follow-up Checks

- Confirm the generated 3% and 4% targets have distinct merge fingerprints after regeneration.
- Confirm the 4% target has 40 g/L sea salts and the 3% target has 30 g/L sea salts.
- Re-run open schema, strict, reference, and term validation for both regenerated targets.

## Additional Notes

The normalized owners already represent the intended salinity-variant relationship; this file needs regeneration from those repaired records.
