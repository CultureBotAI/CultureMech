# YAML Record Review: 892

- Repository: CultureMech
- Record: data/merge_yaml/merged/892.yaml
- Started UTC: 2026-09-21T07:43:38Z
- Finished UTC: 2026-09-21T07:44:35Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/892.yaml`.
- Stable identifier: `CultureMech:007502`.
- Source identity asserted by the record: NBRC medium 891, imported as `NBRC_892.yaml`.
- The generated record was merged from one owner, `NBRC_892.yaml`, on fingerprint `0c666f1a498fadd42fc771ee03d800d52c62f321d4b9248fec285280bc534b85`.
- Current authoritative owner: `data/normalized_yaml/bacterial/NBRC_892.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/892.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/892.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/892.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/892.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The intended NBRC identity is recoverable from curation history, but the generated record is stale relative to `data/normalized_yaml/bacterial/NBRC_892.yaml`.
- The current normalized owner now carries the official name `Soybean Casein Digest Agar`, identifies `nbrc.medium:891`, stores pH 7.1 to 7.5, and has a structured NBRC medium 891 reference.
- A gitignore-independent exact search for `NO=891` and `Media No. 891` found this NBRC owner and the Togo M1686 import that points at the same NBRC medium 891 URL.
- `data/merge_yaml/merged/Soybean_Casein_Digest_Agar.yaml` is a second active generated record for the same NBRC source through Togo `M1686`.

## Evidence

- The current NBRC medium 891 page names the recipe `Soybean Casein Digest Agar`.
- NBRC lists 15 g casein peptone, 5 g soybean peptone, 5 g NaCl, 15 g agar, 1 L distilled water, and pH 7.1-7.5.
- The normalized NBRC owner already preserves those five source rows, the official name, the pH range, source-specific preparation steps, and the NBRC source reference.
- The generated target instead has one concatenated source-table blob as an ingredient name and one empty-name ingredient whose concentration value is `Casein peptone`.

## Completeness

- The generated target is missing all five structured NBRC ingredient rows that exist in the repaired normalized owner.
- The generated target has no `media_term`, no `ph_range`, no NBRC reference entry, and no useful ontology grounding.
- NBRC medium 891 is duplicated by the Togo M1686 generated record, which has the same ingredient amounts but remains a separate CultureMech ID.

## Findings

- BLOCKER: `data/merge_yaml/merged/892.yaml` is stale and unusable: it still contains the pre-recovery concatenated NBRC table rather than the five repaired ingredients in `data/normalized_yaml/bacterial/NBRC_892.yaml`.
- BLOCKER: the generated target does not preserve the official NBRC recipe name, pH 7.1-7.5, or the NBRC medium 891 structured source identity now present in the normalized owner.
- BLOCKER: NBRC medium 891 is represented by two separate active generated records: this stale direct NBRC record and the Togo M1686 `Soybean_Casein_Digest_Agar.yaml` record.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/892.yaml` from `data/normalized_yaml/bacterial/NBRC_892.yaml`; do not edit the generated YAML directly.
- Reconcile `NBRC_892.yaml` and `soybean_casein_digest_agar.yaml` so one canonical NBRC medium 891 / Soybean Casein Digest Agar record is emitted with both source identities retained.
- Preserve casein peptone 15 g/L, soybean peptone 5 g/L, NaCl 5 g/L, agar 15 g/L, distilled water 1 L, and pH 7.1-7.5 during reconciliation.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge artifacts.
- Re-run an exact ignored-file-inclusive search for `NO=891`; only one active generated NBRC medium 891 recipe should remain.
- Confirm that the generated page for `892` or its canonical successor has five readable ingredient rows and no concatenated source-table blob.

## Additional Notes

- The normalized NBRC owner is already repaired and better grounded than the adjacent Togo import; the failing artifact is the stale generated merge.
