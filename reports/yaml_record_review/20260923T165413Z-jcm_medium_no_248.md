# YAML Record Review: JCM MEDIUM No. 248

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_248.yaml
- Started UTC: 2026-09-23T16:52:44Z
- Finished UTC: 2026-09-23T16:54:13Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Target file | `data/merge_yaml/merged/jcm_medium_no_248.yaml` |
| Class | `MediaRecipe` |
| Identifier | `CultureMech:002609` |
| Name | `jcm_medium_no_248` |
| Original name | `JCM MEDIUM No. 248` |
| Category | `bacterial` |
| Maintained owner | `data/normalized_yaml/bacterial/jcm_medium_no_248.yaml` |
| Generated status | Generated by `scripts/merge_recipes.py` on 2026-08-06 from `jcm_medium_no_248.yaml`; it predates the 2026-09-10 normalized-owner repair. |

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_248.yaml` | Passed; no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_248.yaml --out /private/tmp/jcm248.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference integrity | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_248.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated and 0 reference checks were available. |
| Ontology terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_248.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not run. | Not checked: `just validate-history` validates standalone records under `history/`; it does not validate `MediaRecipe.curation_history` embedded in a merged recipe. |

## Identity and Grounding

- The target is intended to represent JCM accession `J248`; its name, `original_name`, JCM source URL, and `media_term.term.id: mediadive.medium:J248` all point to `JCM MEDIUM No. 248`.
- The live JCM `GRMD=248` endpoint returned `Nothing found`, but MediaDive `J248` still resolves as `JCM MEDIUM No. 248` with the original JCM link, and the Togo `M240` API identifies its original source as `JCM_M248`.
- The record also has `kg_microbe_match: mediadive.medium:12`. MediaDive medium 12 is DSMZ `SOIL EXTRACT MEDIUM` with air-dried garden soil, tap water, agar, pH 6.8-7.0, and a DSMZ PDF source. That is adjacent but not identical to JCM 248, which uses dry cow manure, distilled water, and agar, so the maintained JCM owner should not assert this exact cross-database match.
- A digit-bounded, ignored-file-inclusive search over `data/normalized_yaml`, `data/merge_yaml/merged`, the ID registry, and the recipe catalog found the direct JCM owner and Togo M240 duplicate owners and did not find another exact JCM 248 owner outside indexes.

## Evidence

| Claim | Review |
|---|---|
| Cow manure | Supported. MediaDive lists cow manure at 50 g in 1 L; Togo `M240` preserves the same 50 g amount from `JCM_M248`. |
| Agar | Supported. MediaDive lists agar at 15 g in 1 L; Togo `M240` preserves the same 15 g amount and the solidifying role. The CHEBI grounding to `CHEBI:2509` is consistent with the source label. |
| Distilled water | Missing from the generated JCM 248 record. MediaDive lists 1000 ml distilled water and Togo lists 1 L distilled water. The normalized JCM and Togo owners were repaired on 2026-09-10 to include `1000 ML_PER_L`. |
| Preparation | Partially supported. MediaDive and Togo both support suspending 50 g dry cow manure in 1 L water, boiling for 1 hr, filtering through cheesecloth and paper, and making up the volume to 1 L. The stale generated record misclassifies that boiling and paper filtration workflow as `FILTER_STERILIZE`. |
| Physical state | Supported. The 15 g/L agar source row supports `physical_state: SOLID_AGAR`. |
| `kg_microbe_match` | Unsupported. MediaDive `J248` supports JCM 248; MediaDive integer ID `12` resolves to DSMZ Soil Extract Medium, a different recipe. |

## Completeness

- Consequential stale output: `data/merge_yaml/merged/jcm_medium_no_248.yaml` omits `Distilled water` and still reflects the August generated state even though `data/normalized_yaml/bacterial/jcm_medium_no_248.yaml` now has water, source-scoped notes, a Togo reference, and `data_quality_flags`.
- Consequential stale duplicate: `data/merge_yaml/merged/togo_medium_m240.yaml` still has `Distilled water` as `1 G_PER_L`; the maintained Togo owner has already been repaired to `1000 ML_PER_L`.
- Consequential grounding gap: the maintained JCM owner still carries an unsupported `kg_microbe_match` to DSMZ medium 12.
- Empty optional slots for strains, growth outcomes, pH, storage, and atmosphere are acceptable; the inspected MediaDive `J248` and Togo `M240` source records did not supply those values.
- The exact owner search used `rg --no-ignore --hidden`, so ignored generated artifacts and indexes under the searched paths were included.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated canonical JCM 248 record is stale and lacks the repaired source water row. | The merged record was emitted on 2026-08-06 with cow manure and agar only. The maintained normalized JCM owner was repaired on 2026-09-10 to include `Distilled water` at `1000 ML_PER_L`, matching MediaDive and Togo. | Regenerate `data/merge_yaml/merged/jcm_medium_no_248.yaml` from `data/normalized_yaml/bacterial/jcm_medium_no_248.yaml`. |
| Major | The direct JCM owner asserts a wrong exact match to DSMZ medium 12. | JCM 248/MediaDive `J248` uses dry cow manure, distilled water, and agar. `https://mediadive.dsmz.de/rest/medium/12` resolves to DSMZ `SOIL EXTRACT MEDIUM` with air-dried garden soil, tap water, and agar. | Remove or re-curate `kg_microbe_match` in `data/normalized_yaml/bacterial/jcm_medium_no_248.yaml`. |
| Major | The source-equivalent Togo M240 generated record is stale and still has the old water-unit bug. | Togo `M240` has `Distilled water` at 1 L; normalized Togo was repaired to `1000 ML_PER_L`, but generated `data/merge_yaml/merged/togo_medium_m240.yaml` still stores `value: '1'`, `unit: G_PER_L`. | Regenerate `data/merge_yaml/merged/togo_medium_m240.yaml` from `data/normalized_yaml/bacterial/togo_medium_m240.yaml`. |
| Minor | The stale generated JCM 248 record uses `FILTER_STERILIZE` for a descriptive boil/filter instruction. | The source describes boiling cow manure suspension for 1 hr, filtering through cheesecloth and paper, and making volume up to 1 L; it does not say to sterilize by filtration. The maintained normalized owners now encode this as `MIX`. | Regenerate merged YAML from the repaired normalized owners. |

## Recommended Edits

1. Remove or correct `kg_microbe_match: mediadive.medium:12` in `data/normalized_yaml/bacterial/jcm_medium_no_248.yaml`; do not keep the DSMZ Soil Extract Medium as an exact match to JCM 248.
2. Regenerate merged YAML so both `jcm_medium_no_248` and `togo_medium_m240` pick up their September water-row repairs.
3. Rerun merge duplicate detection; JCM 248 and Togo M240 have the same source formulation after the repaired normalized water rows and should no longer remain as contradictory canonical generated recipes unless the merge intentionally keeps JCM and Togo accessions distinct.
4. Confirm the regenerated preparation step is a descriptive `MIX` or another non-sterilization action unless a source explicitly states that this medium is filter-sterilized.
5. Revalidate the regenerated canonical records with open-schema, strict, term, and reference validators.

## Follow-up Checks

- Re-fetch MediaDive `J248` and Togo `M240` after regeneration and confirm cow manure 50 g, agar 15 g, distilled water 1 L or 1000 ml, and the boil/filter/make-up-volume instruction still agree.
- Search with `rg --no-ignore --hidden` for digit-bounded `JCM_M248`, `GRMD=248`, and `TOGO:M240` again after regeneration to confirm only the intended canonical owner remains for this source formulation.
- Recheck MediaDive medium `12` before removing `kg_microbe_match` so the edit records the resolved near miss explicitly.

## Additional Notes

- The live JCM `GRMD=248` page currently says `Nothing found`; all formulation evidence in this review came from MediaDive's JCM `J248` snapshot and Togo's `JCM_M248` snapshot.
- An intermediate duplicate search accidentally included `^id: CultureMech:[0-9]+$` over all merged records and produced broad noise; it was discarded and replaced with the digit-bounded ignored-file-inclusive owner search described above.
- No GitHub issue, pull request, or comment was created for this read-only review.
