# YAML Record Review: OM-2 MEDIUM

- Repository: CultureMech
- Record: data/merge_yaml/merged/om_2_medium.yaml
- Started UTC: 2026-09-24T19:21:29Z
- Finished UTC: 2026-09-24T19:21:46Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:006481`, the generated `om_2_medium` source-duplicate merge of `data/normalized_yaml/bacterial/KOMODO_782_OM-2_MEDIUM.yaml` and `data/normalized_yaml/bacterial/om_2_medium.yaml`.

## Validation

Passed LinkML validation against `MediaRecipe` with no issues reported.

Passed strict validation; the TSV contained only the header row.

Passed LinkML reference validation with zero reference checks.

Passed LinkML term validation.

Embedded `curation_history` validation was not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` blocks in generated YAML.

## Identity and Grounding

The identity is grounded to DSMZ/MediaDive Medium 782, OM-2 MEDIUM. The generated record keeps the KOMODO wrapper ID, `komodo.medium:782`, and records the direct DSMZ owner as `parent_media` with `relationship: SOURCE_DUPLICATE`.

An ignored-inclusive exact search for `komodo.medium:782`, `mediadive.medium:782`, `KOMODO_782_OM-2_MEDIUM`, and `om_2_medium` under `data/merge_yaml` and `data/normalized_yaml` found only the two normalized owners, the one generated merge, and generated index mentions.

The source-duplicate classification is plausible because the KOMODO notes state DSMZ Medium 782 provenance and the normalized KOMODO and MediaDive ingredient signatures both contain the same eight salts and conditional 20 g/L agar projection.

## Evidence

MediaDive REST for Medium 782 reports `OM-2 MEDIUM`, DSMZ source, pH range 8.5-9.0, a PDF link for `DSMZ_Medium782.pdf`, and a 1000 ml main solution containing 15 g NH4-oxalate, 10 g NaHCO3, 10 g NaH2PO4 x 2 H2O, 0.7 g NaCl, 0.57 g KCl, 0.1 g MgCl2 x 6 H2O, 0.01 g CaCl2 x 2 H2O, 1 g Na2S2O3 x 5 H2O, 1000 ml distilled water, and conditional agar.

The DSMZ PDF confirms the same base recipe and states that the first three salts must be dissolved in the indicated sequence; after autoclaving for 20 min at 121 C the pH shifts to 8.5-9.0; solid medium contains 1.5 to 2.0% (w/v) Bacto-Agar (Difco).

## Completeness

The eight required salts are complete and match the DSMZ amounts after per-liter normalization.

The generated `ph_range` captures the post-autoclave pH range reported by DSMZ.

Preparation evidence is present in the normalized direct MediaDive owner but absent from the generated merge because the merge body was taken from the KOMODO child.

Target organisms are absent. This is not a defect for this generated DSMZ/KOMODO record because the fetched DSMZ and MediaDive medium definitions do not list strains or organisms.

## Findings

- The 1000 ml distilled water row is absent from both normalized owners and therefore from the generated record, even though MediaDive and the DSMZ PDF include it as the final solution basis.
- The generated source-duplicate merge chose the KOMODO child as the output body. That dropped the richer direct DSMZ `media_term`, DSMZ PDF link, and MediaDive preparation steps from `data/normalized_yaml/bacterial/om_2_medium.yaml`.
- Conditional agar is flattened to a required `20 G_PER_L` ingredient with only `notes: ' (if required)'`. DSMZ states solid medium contains 1.5 to 2.0% agar; MediaDive preserves the agar row as conditional with the range in `attribute`, but the YAML loses both the range and the conditional semantics.
- The initial pre-autoclave adjustment to pH 6.8-7.0 is not represented in the generated record because the normalized MediaDive preparation steps were dropped during the source-duplicate merge.

## Recommended Edits

- Add `Distilled water` at `1000 ML_PER_L` to the source-normalized owners before regenerating the merge.
- Preserve the direct MediaDive/DSMZ owner as the canonical representative of this source duplicate so `mediadive.medium:782`, the DSMZ PDF link, and DSMZ preparation steps survive generation.
- Represent the agar row as a conditional 1.5 to 2.0% solid-medium option instead of an unconditional 20 g/L ingredient, or otherwise carry the conditional wording in a structured variant/child record.
- Regenerate `data/merge_yaml/merged/om_2_medium.yaml` after the normalized owners or merge precedence are repaired.

## Follow-up Checks

- Rerun open-schema, strict, reference, and term validation on the regenerated `data/merge_yaml/merged/om_2_medium.yaml`.
- Recheck DSMZ Medium 782 and MediaDive REST Medium 782 after regeneration to confirm water, agar conditionality, pH shift, and autoclave preparation evidence are represented.

## Additional Notes

None found.
