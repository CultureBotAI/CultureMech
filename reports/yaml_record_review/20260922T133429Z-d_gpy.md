# YAML Record Review: d_gpy

- Repository: CultureMech
- Record: data/merge_yaml/merged/d_gpy.yaml
- Started UTC: 2026-09-22T13:31:02Z
- Finished UTC: 2026-09-22T13:34:29Z
- Verdict: needs curation

## Target

Reviewed generated record `data/merge_yaml/merged/d_gpy.yaml` with generated identifier `CultureMech:008698`, media term `TOGO:M2106`, original name `d-GPY`, category `bacterial`, and one merged source, `d_gpy`.

## Validation

- LinkML open-schema validation: passed.
- Strict validation: passed with 0 error rows in `/private/tmp/d_gpy.strict.tsv`.
- Reference validation: passed with 0 references checked.
- Term validation: passed.
- Embedded curation history: not checked; `just validate-history` targets standalone files under `history/`, not `MediaRecipe.curation_history` blocks.

## Identity and Grounding

TOGO M2106 resolves to NBRC Medium 1429, `d-GPY`, and the TOGO API metadata points back to the NBRC medium-detail URL for `NO=1429`. The TOGO API and NBRC page agree on a five-part recipe: 1 g Polypeptone, 0.5 g yeast extract, 2 g glucose, 1 L seawater at 1.75% salinity, and 15 g agar.

An exact gitignore-independent search for `M2106`, `d_gpy`, and `d-GPY` across normalized YAML, merged YAML, and prior YAML record reviews found only the normalized `data/normalized_yaml/bacterial/d_gpy.yaml` source and this generated `data/merge_yaml/merged/d_gpy.yaml` target, so no same-source duplicate was visible in those reviewed trees.

The record's `kg_microbe_match: mediadive.medium:7` is not grounded for `d-GPY`: MediaDive medium 7 is DSMZ `ANCYLOBACTER - SPIROSOMA MEDIUM`, with distilled water, 1 g/L glucose, 1 g/L peptone, 1 g/L yeast extract, 15 g/L agar, and pH 7.0, not the NBRC 1429 seawater medium.

## Evidence

The external sources support the identity, ingredient list, and seawater unit captured in the maintained normalized source:

- TOGO M2106 reports `name` `d-GPY`, `original_media_id` `NBRC_M1429`, and source URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=1429`.
- TOGO M2106 lists one main solution with yeast extract 0.5 g, seawater 1 L, glucose 2 g, agar 15 g, and Polypeptone 1 g.
- NBRC Medium 1429 lists medium `d-GPY` with Polypeptone 1 g, yeast extract 0.5 g, glucose 2 g, Seawater (1.75 % salinity) 1 L, and agar 15 g.

## Completeness

The maintained source `data/normalized_yaml/bacterial/d_gpy.yaml` is mostly complete for the externally visible formula: it preserves per-ingredient NBRC provenance, stores seawater as `1.0 L`, includes both external references, and has a 2026-09-11 repair history entry for the original TOGO seawater conversion artifact.

The generated target is stale relative to that curated source. It lacks the repaired ingredient order, per-ingredient `source` fields, normalized reference list, `data_quality_flags`, FOODON mapping for yeast extract, and the 2026-09-11 repair history entry.

## Findings

1. **Seawater was converted to the wrong unit in generated YAML.** NBRC and TOGO both list `Seawater (1.75 % salinity)` as `1 L`; the generated record stores it as `1 G_PER_L`, turning the solvent into a 1 g/L solute. The normalized source has already been repaired to `1.0 L`, so this is a stale generated artifact.

2. **The record carries a false MediaDive match.** `kg_microbe_match: mediadive.medium:7` points to DSMZ `ANCYLOBACTER - SPIROSOMA MEDIUM`, whose formula uses distilled water and different glucose, peptone, and yeast amounts. That is not equivalent to NBRC 1429 `d-GPY`.

3. **Generated provenance is stale.** The generated file still has the January 2026 import notes and omits the curated September 2026 repair note and normalized external `references`, making it impossible to tell from the generated output that the authoritative maintained source has already corrected the seawater issue.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/d_gpy.yaml` from `data/normalized_yaml/bacterial/d_gpy.yaml`, or fix the merge step that failed to propagate the repaired `1.0 L` seawater unit and 2026-09-11 curation fields into the generated output.
- Remove or correct `kg_microbe_match: mediadive.medium:7`; no reviewed source supports DSMZ medium 7 as a match for NBRC Medium 1429.

## Follow-up Checks

- After regeneration, confirm seawater remains `unit: L` in the generated merged YAML.
- Re-run strict, reference, and term validation on the generated `d_gpy.yaml`.
- Re-query MediaDive if a KG-Microbe cross-reference is still desired, because MediaDive 7 is formula-incompatible with this NBRC recipe.

## Additional Notes

The review used gitignore-independent `rg --no-ignore --hidden` searches for `M2106`, `d_gpy`, and `d-GPY` so ignored files were included in the duplicate/source scan.
