# YAML Record Review: cgy_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/CGY_Medium.yaml
- Started UTC: 2026-09-22T06:50:53Z
- Finished UTC: 2026-09-22T06:51:34Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:008763 |
| Name | cgy_medium |
| Original name | CGY Medium |
| Category | bacterial |
| Source | TOGO Medium M2169, original source NBRC_M1553 |
| Generated status | Generated merged output |
| Maintained owner | data/normalized_yaml/bacterial/cgy_medium.yaml |

The reviewed file is the generated merge of one maintained TOGO/NBRC owner.
Future fixes belong in `data/normalized_yaml/bacterial/cgy_medium.yaml` or the
TOGO import transform, followed by merge/page regeneration.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/CGY_Medium.yaml` | Passed; no issues found. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/CGY_Medium.yaml --out /private/tmp/CGY_Medium.strict.tsv --workers 1 --quiet` | Passed with 0 errors; TSV written to `/private/tmp/CGY_Medium.strict.tsv`. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/CGY_Medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/CGY_Medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not checked | `just validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in the merged YAML. |

## Identity and Grounding

- `TOGO:M2169`, the label `CGY Medium`, and the original source `NBRC_M1553`
  agree on the target identity.
- The direct NBRC URL in the source notes currently returns an NBRC error page,
  so source-level claims were checked against the live TOGO M2169 API.
- The generated record is stale relative to its maintained owner: the owner
  gained an exact MIM grounding for `Polymyxin B sulfate` on 2026-08-20, after
  the generated record was last merged on 2026-08-06.

## Evidence

TOGO M2169 lists the following one-litre CGY formula: 1 L distilled water,
0.5 g glycerol, 16 g agar if needed, 30 mg polymyxin B sulfate, 0.1 g Bacto
Yeast Extract, and 0.5 g Bacto Casamino Acid. It also records pH 7.4 and says
to mix all ingredients except polymyxin B sulfate, autoclave at 121 C for
20 minutes, and then add filter-sterile concentrated polymyxin B solution after
autoclaving.

The reviewed record preserves the component identities but loses three source
details:

| Source claim | Reviewed representation |
|---|---|
| 30 mg polymyxin B sulfate per litre | `30` `G_PER_L` |
| pH 7.4 | No `ph_value` |
| Polymyxin added from a filter-sterile concentrated stock after autoclaving | No preparation step |

## Completeness

- The post-autoclave polymyxin addition is absent.
- pH 7.4 is absent.
- Distilled water is represented as `1` `G_PER_L`, which preserves the numeric
  `1` from the TOGO litre field but not its source unit.
- `Agar (if needed)` is retained as a top-level ingredient while the record is
  classified as `SOLID_AGAR`; the source leaves agar optional.
- Exact `rg --no-ignore --hidden` searches over YAML for exact
  `name: cgy_medium`, `CGY Medium`, `CultureMech:008763`, and `TOGO:M2169`
  identities included ignored and hidden files. They found only
  `data/normalized_yaml/bacterial/cgy_medium.yaml` and the reviewed generated
  output for this source identity.
- Empty optional slots such as target organisms and explicit incubation
  temperature were not treated as defects.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Polymyxin B sulfate is off by a factor of 1000. | TOGO records 30 mg in the 1 L recipe, while the generated record stores 30 g/L. | `data/normalized_yaml/bacterial/cgy_medium.yaml`; TOGO unit conversion |
| Major | Preparation and pH were dropped. | TOGO records pH 7.4 and instructs the curator to autoclave without polymyxin, then add filter-sterile concentrated polymyxin B solution after autoclaving. | `data/normalized_yaml/bacterial/cgy_medium.yaml`; TOGO comment import |
| Major | Source volumes are imported with the wrong unit scope. | The source says 1 L distilled water; the generated record says `1` `G_PER_L`. | `data/normalized_yaml/bacterial/cgy_medium.yaml`; TOGO volume handling |
| Minor | The generated record lacks a maintained owner grounding that has already been curated. | The normalized owner has CHEBI:8310 for Polymyxin B sulfate from 2026-08-20, but the generated record predates that change. | Regenerate from `data/normalized_yaml/bacterial/cgy_medium.yaml` |

## Recommended Edits

1. Fix the TOGO importer or this normalized owner so milligram ingredients are
   converted to g/L values when the recipe volume is 1 L; polymyxin B sulfate
   should not remain at 30 g/L.
2. Preserve TOGO comments as preparation and condition fields: pH 7.4,
   autoclaving at 121 C for 20 minutes before adding polymyxin, and the
   filter-sterile concentrated polymyxin addition.
3. Preserve the source unit for 1 L distilled water or represent it as the
   final solvent volume rather than `1` g/L.
4. Decide whether optional agar should be modeled as an agar variant or an
   optional preparation addition; avoid marking the whole medium
   unconditionally `SOLID_AGAR` if the source medium can omit agar.
5. Regenerate the merged YAML so the already curated CHEBI:8310 polymyxin
   grounding is published.

## Follow-up Checks

- Re-run open-schema, strict, term, and reference validation on the normalized
  owner and the regenerated `data/merge_yaml/merged/CGY_Medium.yaml`.
- Manually compare the regenerated record against the TOGO M2169 API for the
  30 mg polymyxin amount, 1 L water amount, pH 7.4, and post-autoclave
  polymyxin step.
- Recheck the NBRC Medium 1553 URL; if it starts returning the authoritative
  page again, compare it with TOGO before finalizing.

## Additional Notes

- The NBRC source URL was fetched but currently returns an error page from the
  NBRC catalogue.
- Shape validation passes because the incorrect 30 g/L polymyxin quantity and
  missing pH/preparation fields are schema-valid.
