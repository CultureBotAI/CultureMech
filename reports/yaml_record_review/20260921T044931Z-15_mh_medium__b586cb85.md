# YAML Record Review: 15_mh_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/15_mh_medium__b586cb85.yaml`
- Started UTC: 20260921T044824Z
- Finished UTC: 20260921T044931Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:002764` |
| Label | `15_mh_medium` |
| Original label | `15% MH MEDIUM` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml`, `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml`, their import normalization, or merge regeneration |
| Merge owner | `merged_from: [JCM_J408_15_MH_MEDIUM]` |
| Maintained owner | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` |

This generated singleton is the MediaDive/JCM import of JCM Medium 408, `15% MH MEDIUM`.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/15_mh_medium__b586cb85.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/15_mh_medium__b586cb85.yaml --out /private/tmp/15_mh_medium__b586cb85.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/15_mh_medium__b586cb85.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/15_mh_medium__b586cb85.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/15_mh_medium__b586cb85.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated target correctly denotes JCM Medium 408, the solid `15% MH MEDIUM`, and should stay distinct from the direct DSMZ Medium 582 liquid record:

- The JCM 408 source page lists the same NaCl, MgCl2 hexahydrate, MgSO4 heptahydrate, CaCl2 dihydrate, KCl, NaHCO3, proteose peptone no. 3, yeast extract, glucose, agar, and distilled water recipe at final pH 7.5.
- The record's `physical_state: SOLID_AGAR` agrees with the 20 g agar row.
- JCM 408 has no NaBr row, while DSMZ 582 has NaBr and an optional agar note for solid media.

However, `data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml` / TOGO Medium M406 is also imported from JCM 408. It should be a source duplicate of this JCM record after water, supplier-label, and source-accession normalization are reconciled.

## Evidence

Supported by inspected source text:

- The JCM page supports the `408 15% MH MEDIUM` identity, pH 7.5, 20 g/L agar, and hydrate-specific magnesium and calcium ingredient labels in the generated record.
- The same page supports the absence of NaBr from JCM 408, distinguishing this solid JCM record from DSMZ Medium 582.
- The TOGO M406 normalized record names `JCM_M408` and the same JCM URL as its original source, so it is an alternate import of the same JCM medium rather than a separate biological formulation.

Unsupported, incomplete, or lossy in the generated record:

- The global JCM instruction to autoclave media at 121 degrees C for 15 min unless otherwise stated is not represented.
- The source's 1 L distilled-water row is absent.
- JCM's `BD-Difco` supplier qualifiers are not represented in the proteose peptone no. 3 and yeast extract rows.
- The JCM URL is present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The core JCM solutes, agar amount, and final pH are present.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the JCM source page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:002764`, `CultureMech:009790`, `mediadive.medium:J408`, `TOGO:M406`, `JCM_M408`, the two JCM-derived source slugs, and their merge fingerprints found only this JCM singleton, the TOGO M406 singleton, and their ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The JCM 408 and TOGO M406 imports of the same source recipe failed to merge as source duplicates. | This target and `data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml` both trace to the JCM 408 URL, but they have distinct merge fingerprints because the direct JCM import omits water and supplier labels while the TOGO import stores water as `1 G_PER_L` and keeps `BD-Difco` qualifiers. | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` and `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml`, followed by merge regeneration. |
| Major | The JCM default sterilization condition is missing. | The JCM page says media should be autoclaved at 121 degrees C for 15 min unless otherwise stated; the generated record only has `Adjust pH to 7.5.` | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` or the JCM importer. |
| Minor | The 1 L distilled-water row is absent. | JCM 408 lists distilled water at 1.0 L, but the generated record has no water or final-volume row. | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` or the JCM importer. |
| Minor | JCM supplier qualifiers are dropped from the two undefined complex ingredients. | The JCM page qualifies proteose peptone no. 3 and yeast extract as `BD-Difco`; the generated record stores both as bare names with no note preserving that provider detail. | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` or the JCM importer. |
| Minor | Structured references are absent. | The JCM URL is present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` or the MediaDive/JCM import mapping. |

## Recommended Edits

1. Normalize the direct JCM 408 and TOGO M406 imports so both represent water, hydrate salts, agar, BD-Difco supplier qualifiers, and source accessions consistently enough to collapse into one generated source-duplicate cluster.
2. Add the JCM default autoclave instruction as a preparation step in `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` and the equivalent TOGO owner.
3. Represent the JCM 1 L distilled-water row, using a volume-aware unit or final-volume convention instead of TOGO's current `1 G_PER_L` water artifact.
4. Preserve the `BD-Difco` qualifiers for proteose peptone no. 3 and yeast extract.
5. Add a structured reference for the JCM 408 source page.
6. Regenerate `data/merge_yaml/merged/` and confirm the direct JCM and TOGO copies merge with a `SOURCE_DUPLICATE` relationship while DSMZ Medium 582 remains separate.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` after adding a structured JCM reference.
- `just verify-merges` to prove the JCM and TOGO generated outputs coalesce after regeneration.
- `just validate-strict` and `just validate-terms` on the regenerated 15% MH JCM merge record.
- Manual comparison to the JCM 408 page for water amount, autoclave conditions, BD-Difco qualifiers, agar, pH, and the absence of NaBr.

## Additional Notes

- The direct TOGO Medium M406 fetch returned the TogoMedium JavaScript shell rather than embedded recipe rows; the local TOGO record points back to the JCM 408 source page, and that source page resolved.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
