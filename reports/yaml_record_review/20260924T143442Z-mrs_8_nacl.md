# YAML Record Review: MRS+8% NaCl

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_8_nacl.yaml
- Started UTC: 2026-09-24T14:33:10Z
- Finished UTC: 2026-09-24T14:34:42Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008262 |
| Name | mrs_8_nacl |
| Original name | MRS+8% NaCl |
| Category | bacterial |
| Media term | TOGO:M1700 |
| Source | NBRC_M906, NBRC medium 906 |
| Reviewed path | data/merge_yaml/merged/mrs_8_nacl.yaml |
| Maintained source | data/normalized_yaml/bacterial/mrs_8_nacl.yaml |
| Generated status | Generated by recipe-merger / merge_recipes.py on 2026-08-06 |

The reviewed generated record keeps the TOGO M1700 and NBRC 906 identity for
`MRS+8% NaCl`, but the duplicate-merge step collapsed it with the 5% NaCl
sibling.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_8_nacl.yaml` exited 0 and printed `No issues found`. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_8_nacl.yaml --out /private/tmp/mrs_8_nacl.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_8_nacl.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_8_nacl.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The record passes structural validation. The wrong NaCl concentration is a
generated merge artifact.

## Identity and Grounding

- `media_term.id` is `TOGO:M1700`, `media_term.label` is `MRS+8% NaCl`, and the
  notes point to NBRC medium 906. TOGO M1700 reports the same name,
  `original_media_id: NBRC_M906`, and the same NBRC URL; NBRC medium 906 is the
  same 8% NaCl source.
- TOGO M1700 and NBRC 906 list `NaCl` at 80 g. The generated record has `NaCl`
  at `50 G_PER_L`, which belongs to the distinct `MRS + 5% NaCl` sibling,
  TOGO:M1764 / NBRC_M977.
- The generated `merged_from` list and `mrs_5_nacl` synonym show that the
  5% and 8% salinity variants were merged as duplicates despite different NaCl
  amounts.
- `MnSO4 x 4H2O` is correctly grounded to CHEBI:86358, manganese(II) sulfate
  tetrahydrate. `Sorbitan mono-oleate` has no ontology term even though the
  source identifies it as a defined organic simple component.

## Evidence

TOGO and NBRC agree on the NaCl-salinity pair:

| Variant | TOGO record | NBRC record | NaCl amount |
| --- | --- | --- | --- |
| MRS + 5% NaCl | TOGO:M1764 | NBRC_M977 / medium 977 | 50 g |
| MRS+8% NaCl | TOGO:M1700 | NBRC_M906 / medium 906 | 80 g |

The reviewed generated record's identity is the second row but its NaCl amount
is the first row.

Aside from NaCl, TOGO M1700 and NBRC 906 support the shared recipe rows:
peptone 10 g, Lab-Lemco powder 8 g, yeast extract 4 g, glucose 20 g, sorbitan
mono-oleate 1 ml, K2HPO4 2 g, sodium acetate 5 g, triammonium citrate 2 g,
MgSO4 x 7H2O 0.2 g, MnSO4 x 4H2O 0.05 g, distilled water 1 L, and agar 15 g if
needed.

## Completeness

- The pH range `6.0 - 6.4` is absent from the generated record.
- `Sorbitan mono-oleate` is a 1 ml source row but is stored as `1 G_PER_L`.
- The source's 1 L distilled-water amount is represented as `1 G_PER_L`.
- The source marks `Agar` as conditional with `if needed`; the generated record
  stores the medium as `physical_state: SOLID_AGAR` and makes agar a required
  ingredient.
- `Agar (if needed)` has no `term` or `mediaingredientmech_chebi_term`
  grounding.
- Empty optional fields such as `comments` are not defects.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `TOGO:M1700`, `TOGO:M1764`, `NBRC_M906`,
  `mrs_8_nacl`, `mrs_5_nacl`, `CultureMech:008262`, and
  `CultureMech:008329` found the expected two normalized salinity siblings and
  the generated merged `mrs_8_nacl` output.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| blocker | The generated 8% record carries the 5% NaCl concentration. | `data/merge_yaml/merged/mrs_8_nacl.yaml` has `media_term: TOGO:M1700` and `NaCl` at `50 G_PER_L`; TOGO M1700/NBRC 906 support 80 g, while TOGO M1764/NBRC 977 support 50 g for the distinct `MRS + 5% NaCl` record. | `scripts/merge_recipes.py` and regenerated `data/merge_yaml/merged/*.yaml` |
| blocker | Distinct 5% and 8% NBRC/TOGO salinity variants were merged as duplicate recipes. | The generated `merged_from` list contains `mrs_5_nacl` and `mrs_8_nacl`; the two source records intentionally differ by 50 g versus 80 g NaCl. | `scripts/merge_recipes.py` fingerprint logic or its maintained merge exception inputs, then regeneration |
| major | The record drops the source pH. | TOGO M1700 and NBRC 906 both carry pH `6.0 - 6.4`; the generated YAML has no pH field or note. | `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` |
| major | The sorbitan mono-oleate volume was cast to a mass concentration. | TOGO M1700 and NBRC 906 specify 1 ml; the YAML stores `1 G_PER_L`. | TOGO/NBRC import normalization for `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` |
| major | The imported water concentration uses the wrong unit. | The source row is `Distilled water` 1 L; the YAML stores `Distilled water` as `1 G_PER_L`. | TOGO/NBRC import normalization for `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` |
| major | Conditional agar is promoted to a mandatory solid formulation. | NBRC and TOGO phrase the agar row as `Agar (if needed)`; the YAML sets `physical_state: SOLID_AGAR` and stores agar as an ordinary ingredient. | `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` |
| major | Defined ingredients are missing ontology grounding. | `Sorbitan mono-oleate` has no `term`; `Agar (if needed)` has a solidifying role note but no `term` or `mediaingredientmech_chebi_term`. | CHEBI enrichment for `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` |

## Recommended Edits

1. Fix the duplicate-merge fingerprint or add a maintained merge exception so
   TOGO M1764 and M1700 regenerate as separate records.
2. Regenerate `data/merge_yaml/merged/mrs_8_nacl.yaml` from
   `data/normalized_yaml/bacterial/mrs_8_nacl.yaml` so its NaCl value returns
   to `80 G_PER_L` and the output stops carrying `mrs_5_nacl` in `synonyms` and
   `merged_from`.
3. Preserve the TOGO/NBRC pH `6.0 - 6.4` on
   `data/normalized_yaml/bacterial/mrs_8_nacl.yaml`.
4. Preserve `Sorbitan mono-oleate` as a 1 ml volume addition instead of casting
   it to `1 G_PER_L`.
5. Preserve `Distilled water` as 1 L, or omit it from normalized concentration
   rows instead of casting it to `1 G_PER_L`.
6. Rework `Agar (if needed)` so the optional solidifying additive stays
   conditional and does not force the base medium to `SOLID_AGAR`.
7. Add CHEBI or explicit unresolved grounding for sorbitan mono-oleate and
   agar during enrichment.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated `data/merge_yaml/merged/mrs_8_nacl.yaml`.
- Search with `rg --no-ignore --hidden` for `TOGO:M1700` and `TOGO:M1764`
  across `data/normalized_yaml` and `data/merge_yaml` to confirm the 5% and 8%
  salinity records remain distinct after the merge step.
- Manually compare the regenerated `mrs_8_nacl` record against TOGO M1700 and
  NBRC 906 to confirm NaCl is 80 g, sorbitan is 1 ml, pH is present, agar is
  conditional, and no 5% variant synonym remains.

## Additional Notes

- The maintained normalized 8% record already has `NaCl` at `80 G_PER_L` and
  lacks the 2026-08-06 merge fields, so the wrong 50 g value is a generated
  merge artifact.
- The normalized 8% record intentionally points to
  `data/normalized_yaml/bacterial/mrs_5_nacl.yaml` as a `SALINITY_VARIANT`;
  that relationship is correct and should not be collapsed into a duplicate
  merge.
