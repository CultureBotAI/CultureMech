# YAML Record Review: nss_low

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/nss_low.yaml
- Started UTC: 2026-09-24T18:00:52Z
- Finished UTC: 2026-09-24T18:02:33Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| CultureMech ID | CultureMech:000107 |
| Name | nss_low |
| Original names in file | NSC3-MEDIUM WITH ACETATE, then NSS Low |
| Category | algae |
| Generated path | data/merge_yaml/merged/nss_low.yaml |
| Maintained sources | data/normalized_yaml/algae/asw_ses.yaml, data/normalized_yaml/algae/nss_low.yaml |
| Merge fingerprint | f6c12750325bfbc5d1cc74e70cb6d50438635b1d2e636d7884540b310ed35aae |
| Merged from | asw_ses, nss_low |

This is a generated merged record that incorrectly combines the repaired CCAP NSS Low algae record with a stale ASW:SES algae record.

## Validation

| Check | Result |
| --- | --- |
| Open schema validation with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/nss_low.yaml` | Passed with "No issues found". |
| Strict validation with `scripts/validate_strict.py data/merge_yaml/merged/nss_low.yaml --out /private/tmp/nss_low.strict.tsv --workers 1 --quiet` | Passed; the TSV contained only the header line. |
| Reference validation with `linkml-reference-validator validate data data/merge_yaml/merged/nss_low.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 total checks and no failures. |
| Term validation with `linkml-term-validator validate-data data/merge_yaml/merged/nss_low.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the documented history validator validates standalone `history/` entries, not embedded generated `MediaRecipe.curation_history`. |

## Identity and Grounding

The intended identity is CCAP NSS Low, an algae recipe at `https://www.ccap.ac.uk/wp-content/uploads/MR_NSS_low.pdf`. The generated record preserves the `nss_low` name and `CCAP:NSS_low` reference, but it is not a faithful NSS Low recipe. It lost the repaired NSS Low source structure from `data/normalized_yaml/algae/nss_low.yaml` and instead uses flat ASW:SES ingredient rows from the old ASW:SES curation.

An ignored-file-inclusive search for `CCAP:NSS_low`, `MR_NSS_low`, `nss_low`, `CCAP:ASW_SES`, `MR_ASW_SES`, and `asw_ses` across `data/normalized_yaml` and `data/merge_yaml/merged` found both algae records, old bacterial copies of both CCAP records, generated `data/merge_yaml/merged/asw_ses.yaml`, and this generated NSS Low merge. The record is therefore affected by both a bad concentration-variant merge and duplicate normalized source ownership.

## Evidence

The live CCAP NSS Low PDF lists Tricine at 0.50 g/L, Extra salts stock at 7.50 ml/L, Vitamin solution at 5.00 ml/L, Soil Extract 1 at 12.50 ml/L, final make-up to 1 L with filtered natural seawater or deionized water plus Instant Ocean sea salts, pH 7.6-7.8, and autoclaving at 15 psi for 15 minutes. The repaired maintained algae source matches those stock boundaries.

The generated merged record instead lists NaNO, Na2HPO4, K2HPO4, biotin, calcium pantothenate, cyanocobalamin, folic acid, inositol, nicotinic acid, thiamine HCl, thymine, and tricine as direct top-level ingredients from `CCAP Medium MR_ASW_SES.pdf`. Those are stale ASW:SES stock rows, not direct NSS Low medium ingredients. It also maps `NaNO` to CHEBI:34545 / Azimilide, while the CCAP source chemistry is sodium nitrate.

## Completeness

The generated record omits the correct NSS Low solution tree, the Soil Extract 1 addition, the filtered natural seawater make-up instruction, the Instant Ocean alternative, the pH 7.6-7.8 range, and the autoclave instruction that exist in `data/normalized_yaml/algae/nss_low.yaml` and in the CCAP source PDF.

Empty target-organism slots are acceptable for the imported CCAP provider recipe. Citations remain as provider references; no strain-specific target organism source is expected in this record.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| Blocker | The generated NSS Low record carries ASW:SES ingredients rather than the repaired NSS Low recipe. | `data/normalized_yaml/algae/nss_low.yaml` has Tricine, filtered natural seawater, 7.50 ml/L Extra salts, 5.00 ml/L Vitamin solution, and 12.50 ml/L Soil Extract 1. The generated file merged `asw_ses` and `nss_low` and kept the old ASW:SES flat ingredient list sourced to `MR_ASW_SES.pdf`. | Merge normalization for `data/normalized_yaml/algae/nss_low.yaml` and `data/normalized_yaml/algae/asw_ses.yaml`, then merged-record regeneration. |
| Major | Stale bacterial normalized copies of both CCAP algae records remain active in the corpus. | Ignored-file-inclusive searches found `data/normalized_yaml/bacterial/nss_low.yaml` and `data/normalized_yaml/bacterial/asw_ses.yaml` in addition to the algae records. The bacterial NSS Low record has a MediaDive `C70` term and the repaired NSS Low composition but still says `category: bacterial` and `applications: Microbial cultivation`. | Normalized CCAP source ownership and category cleanup for `data/normalized_yaml/bacterial/nss_low.yaml` and `data/normalized_yaml/bacterial/asw_ses.yaml`, then index and merge regeneration. |
| Major | The generated record exposes unsupported ASW:SES stock rows as final NSS Low ingredients and contains a wrong sodium nitrate ontology mapping. | The generated `NaNO` row is sourced to `MR_ASW_SES.pdf` and is grounded to CHEBI:34545 / Azimilide; the CCAP NSS Low and ASW:SES PDFs both specify nitrate stock chemistry, not Azimilide. | The stale ASW:SES normalized input and merge regeneration; this row should disappear from generated NSS Low after the blocker is fixed. |

## Recommended Edits

1. Prevent the repaired NSS Low algae source from merging with ASW:SES as a duplicate recipe; keep the concentration-variant relationship as metadata only.
2. Regenerate `data/merge_yaml/merged/nss_low.yaml` from `data/normalized_yaml/algae/nss_low.yaml` so its top-level ingredients are Tricine and filtered natural seawater and its solution tree contains Extra salts, Vitamin solution, and Soil Extract 1 with the 7.50, 5.00, and 12.50 ml/L additions.
3. Remove or reclassify the stale bacterial `nss_low` and `asw_ses` normalized copies so CCAP algae recipes do not appear in bacterial indexes or future bacterial-side merges.
4. Repair the ASW:SES input separately so it uses stock-solution additions and CHEBI:63005 for NaNO3 if `data/merge_yaml/merged/asw_ses.yaml` remains in the corpus.

## Follow-up Checks

1. Regenerate normalized indexes and merged YAML, then inspect `data/merge_yaml/merged/nss_low.yaml` to verify it has only `nss_low` in `merged_from` and no ASW:SES ingredient rows.
2. Search with ignored files included for `MR_NSS_low`, `CCAP:NSS_low`, `MR_ASW_SES`, and `CCAP:ASW_SES` to confirm the old bacterial duplicates no longer participate.
3. Run the focused open schema, strict, reference, and term validators on the regenerated NSS Low and ASW:SES records.
4. Recompare the regenerated NSS Low record against the CCAP NSS Low PDF, checking stock volumes, pH 7.6-7.8, the filtered-natural-seawater base, and the Instant Ocean alternative.

## Additional Notes

The generated YAML has two `original_name` keys: `NSC3-MEDIUM WITH ACETATE` near the top and `NSS Low` later. The parser accepts the later value, but the stale first value is another merge-output corruption signal.
