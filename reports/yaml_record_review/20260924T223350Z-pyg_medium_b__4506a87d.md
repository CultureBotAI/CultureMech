# YAML Record Review: PYG Medium (B)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml
- Started UTC: 2026-09-24T22:33:50Z
- Finished UTC: 2026-09-24T22:33:56Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009809 |
| Name | pyg_medium_b |
| Original name | PYG Medium (B) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml |
| Merge source | TOGO_M423_PYG_Medium_B |
| Source grounding | TOGO:M423, JCM_M423 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO normalization and in the TOGO solution migration
logic that separated the `Salts solution` header while leaving the stock
components in the top-level ingredient list.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml` | Passed with `No issues found`. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml --out /private/tmp/pyg_medium_b__4506a87d.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_medium_b__4506a87d.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml` is the generated TOGO
  M423 branch for CultureMech:009809 and JCM_M423 `PYG Medium (B)`.
- The TOGO M423 API reports the same label, original medium `JCM_M423`, pH
  7.2, and the same JCM GRMD 423 URL embedded in the YAML.
- The live JCM 423 page confirms the PYG MEDIUM (B) identity, pH 7.2, and the
  direct main recipe plus `Salts solution` stock formula.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M423`, TOGO M423
  URLs, `JCM_M423`, `GRMD=423`, and `TOGO_M423_PYG_Medium_B` found the
  reviewed TOGO branch, its maintained owner, a metal-analysis index entry,
  and the equivalent direct JCM J423 branch.

## Evidence

- JCM 423 and TOGO M423 support Polypepton 5 g, Tryptone 5 g, yeast extract
  10 g, glucose 10 g, 40 ml salts solution, 960 ml distilled water, pH 7.2,
  and a salt stock containing 0.2 g CaCl2, 0.4 g MgSO4.7H2O, 1 g K2HPO4,
  1 g KH2PO4, 10 g NaHCO3, 2 g NaCl, and 1 L water.
- The reviewed record preserves the main peptide/extract/glucose amounts.
- The reviewed record stores `Salts solution (see below)` as 40 `G_PER_L` with
  an empty composition and stores the salt-stock components as top-level
  final-medium ingredients. Those rows are the stock recipe values, not the
  final 40 ml/L contribution.
- The reviewed record contains 961.0 `G_PER_L` distilled water because the
  960 ml final-medium water and 1 L salt-stock water were merged into one row.
- The reviewed record omits pH 7.2.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- The salt solution composition is empty even though JCM and TOGO provide the
  stock composition.
- `ph_value` is missing even though both TOGO and JCM give pH 7.2.
- The equivalent direct JCM branch remains separate and needs reconciliation
  after both imports represent the same salt-stock scope.
- Exact ignored-file search included ignored files before assessing exact
  TOGO/JCM duplicate scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The JCM 423 salt stock was flattened into final-medium concentrations. | JCM 423 and TOGO M423 add 40 ml `Salts solution` to the final medium and separately define the stock in 1 L water. The TOGO YAML stores `Salts solution` as 40 `G_PER_L`, leaves the solution composition empty, and exposes every stock salt as a top-level final row. | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml; solution migration |
| Major | Final and stock water were merged. | The source uses 960 ml main-medium water plus 1 L stock water; the YAML merges them into one 961.0 `G_PER_L` row. | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml; data-quality cleanup |
| Minor | pH is absent. | JCM 423 and TOGO M423 specify pH 7.2, but the reviewed YAML has no `ph_value`. | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml |
| Minor | MgSO4.7H2O lacks a MediaIngredientMech CHEBI back-link. | The row has primary `CHEBI:31795` magnesium sulfate heptahydrate but no `mediaingredientmech_chebi_term`, unlike the equivalent direct JCM branch. | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml; MediaIngredientMech enrichment |
| Minor | The equivalent direct JCM branch remains a separate generated record. | Exact ignored-file search found `data/merge_yaml/merged/pyg_medium_b.yaml` for the same JCM 423 recipe. | data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml; merge_recipes.py |

## Recommended Edits

1. Recurate `data/normalized_yaml/bacterial/TOGO_M423_PYG_Medium_B.yaml` so
   `Salts solution` is a 40 ml/L stock aliquot with CaCl2, MgSO4.7H2O, K2HPO4,
   KH2PO4, NaHCO3, NaCl, and 1 L water scoped under its composition.
2. Keep 960 ml main water separate from 1 L salt-stock water.
3. Add pH 7.2 from JCM/TOGO.
4. Refresh the MediaIngredientMech CHEBI link for MgSO4.7H2O.
5. Regenerate the merged output and verify this TOGO M423 branch converges with
   the fixed direct JCM/KOMODO branch.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_medium_b__4506a87d.yaml`.
- Manually confirm the regenerated record keeps salts scoped to `Salts
  solution` and emits only the 40 ml/L aliquot at the top level.
- Manually compare the regenerated TOGO M423 branch against
  `data/merge_yaml/merged/pyg_medium_b.yaml` after the direct branch is fixed.
- Run an exact ignored-file search for `TOGO:M423`, `JCM_M423`, and `GRMD=423`
  to verify no stale duplicate remains after regeneration.

## Additional Notes

- The local TOGO fetch initially failed with a sandbox DNS error. The official
  URL was then fetched successfully with the approved escalated `curl -L` rule.
