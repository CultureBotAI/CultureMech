# YAML Record Review: miura_medium_lca__40e6b647

- Repository: CultureMech
- Record: data/merge_yaml/merged/miura_medium_lca__40e6b647.yaml
- Started UTC: 2026-09-24T07:34:10Z
- Finished UTC: 2026-09-24T07:34:38Z
- Verdict: needs curation

## Target

- Generated record: `CultureMech:002703`
- Generated name: `miura_medium_lca`
- Generated source file: `data/merge_yaml/merged/miura_medium_lca__40e6b647.yaml`
- Normalized owner: `data/normalized_yaml/bacterial/miura_medium_lca.yaml`
- Upstream source: MediaDive/JCM medium `J343`, `MIURA MEDIUM (LCA)`

## Validation

- Open LinkML validation passed: `No issues found`.
- Strict validation passed with 0 ERROR rows in `/private/tmp/miura_medium_lca__40e6b647.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

- The generated record identity is aligned with MediaDive/JCM `J343`.
- The `media_term` points to `mediadive.medium:J343`.
- Six of seven generated ingredients have primary CHEBI or agar grounding.
- `Yeast extract` remains ungrounded.
- `NaNO3` still carries legacy `mediaingredientmech_term: MediaIngredientMech:000171`, despite the June 2026 curation history claiming legacy MediaIngredientMech links were refreshed to CHEBI keying.

## Evidence

- MediaDive REST for `J343` lists eight components in `Main sol. J343`: 1 g glucose, 1 g `KH2PO4`, 0.2 g `MgSO4 x 7 H2O`, 0.2 g KCl, 2 g `NaNO3`, 0.2 g `Yeast extract` with attribute `BD-Difco`, 15 g agar, and 1000 ml distilled water.
- The generated record preserves the seven non-water MediaDive ingredients with matching gram quantities.
- The generated record omits the 1000 ml distilled water row from MediaDive.
- MediaDive/JCM `J343` is the same source formula as TOGO `M338`, which appears in the adjacent generated record `miura_medium_lca.yaml`.
- The old JCM `GRMD=343` page returned an empty body during review, so the current formula was checked against MediaDive REST and TOGO rather than the JCM page.

## Completeness

- The 1000 ml distilled water row is missing.
- The pH note is preserved as a generic `MIX` preparation step; it would be cleaner as a pH or source-context note.
- This MediaDive/JCM import duplicates TOGO `M338`; both records should collapse to one source-backed Miura Medium (LCA) record.

## Findings

- High: MediaDive/JCM `J343` and TOGO `M338` are the same JCM Miura Medium (LCA) formula, but they survive as separate CultureMech IDs and separate generated records.
- Medium: Distilled water is missing from this JCM generated record, while the MediaDive REST payload includes 1000 ml in `Main sol. J343`.
- Medium: Sodium nitrate still uses the deprecated `mediaingredientmech_term` slot.
- Low: `pH unadjusted.` is stored as a `MIX` preparation step even though it is only a pH note.

## Recommended Edits

- De-duplicate `data/normalized_yaml/bacterial/miura_medium_lca.yaml` and `data/normalized_yaml/bacterial/TOGO_M338_Miura_Medium_LCA.yaml`, preserving both `mediadive.medium:J343` and `TOGO:M338` source identifiers on the surviving record.
- Ensure the surviving recipe has the distilled-water row and the seven non-water ingredients from JCM/TOGO.
- Replace the legacy sodium nitrate `mediaingredientmech_term` with the ingredient's primary CHEBI keying.
- Move `pH unadjusted.` out of a `MIX` step and into a pH or source-context note.
- Regenerate merged records after de-duplication.

## Follow-up Checks

- Re-fetch MediaDive/JCM `J343` and TOGO `M338` and verify both resolve to the same curated formula.
- Confirm only one generated Miura Medium (LCA) record remains after curation.
- Retry the JCM `GRMD=343` URL in case its empty response was transient.
- Re-run open LinkML, strict, reference, and term validation after regenerating the merged record.
- The normalized-owner and duplicate-source lookups used `rg --no-ignore --hidden`; repeat ignored-file-inclusive checks for exact owner and duplicate source paths after the edit.

## Additional Notes

- None found.
