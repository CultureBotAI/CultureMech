# YAML Record Review: BHI

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHI.yaml
- Started UTC: 2026-09-21T20:54:05Z
- Finished UTC: 2026-09-21T20:56:05Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:015492 |
| Name | BHI |
| Category | specialized |
| Source identity | CultureBotHT `media_yaml/BHI.yaml`, database_id `BHI` |
| Reviewed artifact | `data/merge_yaml/merged/BHI.yaml` |
| Maintained owner | `data/normalized_yaml/specialized/bhi.yaml` |
| Generated state | Derived single-source merge from `merged_from: [bhi]`, `merge_fingerprint: 1f1e48a8e4b86fe14db46cdcf6f5d02f12b052d14efdac85fe1936d921df7070` |

`data/culturemech_id_registry.tsv` maps `CultureMech:015492` to
`data/normalized_yaml/specialized/bhi.yaml`. The reviewed merge contains the
same CultureBotHT BHI content plus the generated 2026-08-06 merge event.

The current CultureBotHT repository resolves the import-history source to
`media_yaml/BHI.yaml` at blob `172ab12bf60fbc8eb61bd12ca91796686941ba22`.
That file defines a liquid, complex BHI formulation with Calf brains 7.7 g/L,
Beef heart 9.8 g/L, Proteose Peptone 10 g/L, Sodium Chloride 5 g/L, and
Disodium phosphate 2.5 g/L.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHI.yaml` | Passed; `No issues found` |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHI.yaml --out /private/tmp/BHI.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 ERROR rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHI.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 reference checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHI.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | Not checked | No focused embedded-`MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone records under `history/` |

The direct `just validate-*` wrappers were not rerun for this record because
this checkout currently fails before target-specific validation while building
`llvmlite==0.46.0` under Python 3.13. The no-project commands above exercise
the same narrow validators under Python 3.11 from a cache outside the project
environment.

## Identity and Grounding

- **Record identity is coherent.** `CultureMech:015492`, the source
  `database_id: BHI`, and CultureBotHT `media_yaml/BHI.yaml` all identify the
  same BHI base recipe.
- **Composition is faithful to CultureBotHT.** The five ingredients, amounts,
  units, liquid state, and "Brain heart infusion salt broth minus supplements"
  description match the current upstream BHI YAML.
- **Two animal-tissue groundings lost specificity.** CultureBotHT maps
  `Beef heart` to `FOODON:00004410` with label `beef heart`; CultureMech maps
  it to generic `UBERON:0000948` / `heart`. `Calf brains` is likewise collapsed
  to generic `UBERON:0000955` / `brain`. Generic anatomy terms do not preserve
  the bovine source material stated by the formulation.

## Evidence

| Claim | Review |
|---|---|
| CultureBotHT `database_id: BHI` | Supported by the inspected upstream `media_yaml/BHI.yaml` and the local `sources` block. |
| Five g/L ingredients | Supported exactly by CultureBotHT `media_yaml/BHI.yaml`. |
| `medium_type: COMPLEX`, `composition_type: UNDEFINED`, `physical_state: LIQUID` | Supported by the source BHI YAML and by the peptone/animal-tissue recipe composition. |
| Calf brains and beef heart ontology groundings | Over-broad. The formulation uses species-specific animal tissue ingredients; the current UBERON organ terms identify only the organs. |
| `kg_microbe_match: mediadive.medium:C39` | Not checked; this review did not inspect an external KG-Microbe mapping source for the match. |

## Completeness

- The imported CultureBotHT formula is complete relative to the inspected
  source file.
- No pH, sterilization, organism, or growth evidence is present in
  CultureBotHT `media_yaml/BHI.yaml`; empty condition and target-organism slots
  are not defects for this record.
- `sources[0].url` points only at the CultureBotHT repository root. It should
  be narrowed to `media_yaml/BHI.yaml` plus a commit or blob SHA so future
  curators can recover the exact upstream source state.
- A gitignore-independent search with `rg --no-ignore --hidden` over `data`
  and `reports/yaml_record_review` for `CultureMech:015492`, `database_id:
  BHI`, and the merge fingerprint found the expected normalized owner,
  generated merge, generated indexes, and import-tracking references. A `find`
  search over the ignored `reports/yaml_record_review` directory found no
  pre-existing `*-BHI.md` report before this one was written.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The Calf brains and Beef heart groundings erase the bovine source material. | CultureBotHT `media_yaml/BHI.yaml` names `Calf brains` and `Beef heart`; the record grounds them to generic UBERON `brain` and `heart` anatomy terms. | `data/normalized_yaml/specialized/bhi.yaml` and the importer or mapping table that canonicalized those ingredients. |
| minor | The source URL is too broad for reproducible evidence. | `sources[0].url` points to the CultureBotHT repository root and does not identify `media_yaml/BHI.yaml` or blob `172ab12bf60fbc8eb61bd12ca91796686941ba22`. | `data/normalized_yaml/specialized/bhi.yaml`; consider changing `import_from_culturebotht.py` to retain per-file source paths. |

## Recommended Edits

1. Replace the animal-tissue groundings with exact ingredient terms for calf
   brains and beef heart. If no exact ontology terms exist, remove the generic
   UBERON terms and add concrete unresolved identity flags.
2. Narrow `sources[0].url` or add a `references` entry to the exact upstream
   CultureBotHT source path and blob or commit used for curation.
3. Add an append-only curation event to
   `data/normalized_yaml/specialized/bhi.yaml` describing the grounding and
   provenance repairs, then regenerate `data/merge_yaml/merged/BHI.yaml`.

## Follow-up Checks

- Rerun `just validate-schema data/normalized_yaml/specialized/bhi.yaml` and
  `just validate-strict data/normalized_yaml/specialized/bhi.yaml`, or the
  no-project equivalents, after editing the normalized owner.
- Rerun `just validate-terms data/normalized_yaml/specialized/bhi.yaml` after
  the Calf brains and Beef heart grounding changes.
- Rerun `just validate-references data/normalized_yaml/specialized/bhi.yaml`
  after adding exact CultureBotHT source references.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  the BHI merge.

## Additional Notes

None found.
