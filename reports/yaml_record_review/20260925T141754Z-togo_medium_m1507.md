# YAML Record Review: togo_medium_m1507

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1507.yaml
- Started UTC: 2026-09-25T14:13:00Z
- Finished UTC: 2026-09-25T14:17:54Z
- Verdict: pass with minor issues

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008051 |
| Label | togo_medium_m1507 |
| Generated record | data/merge_yaml/merged/togo_medium_m1507.yaml |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m1507.yaml |
| Source | TOGO Medium M1507, NBRC M288-2 |

The reviewed file is a generated one-source merge of TOGO M1507. Future fixes
belong in the normalized owner or the TOGO/NBRC import path, not in the
generated merge.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed; exited 0 with no diagnostics |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/togo_medium_m1507.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; 0 checks |
| `linkml-term-validator validate-data` on the generated record | Passed; exited 0 with only the expected `eutils/pkg_resources is deprecated` warning |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

## Identity and Grounding

`CultureMech:008051`, `TOGO:M1507`, the `NBRC_M288-2` original source note,
and the NBRC medium 288 page all identify the semisolid form of the same NBRC
288 formulation reviewed as M1506. TOGO M1507 keeps the M1506 salts, acetate,
ferric citrate, yeast extract, sodium thioglycolate, and 1 L water, then adds
1 g agar from the NBRC note that good growth can be obtained in semisolid
medium with 1 g agar in 1 L.

The ingredient rows match that semisolid source formula.

## Evidence

The TOGO M1507 payload and the NBRC 288 page support all nine ingredient rows.
The source pH is 6.8, but the generated record has no `ph_value`. The generated
record also classifies 1 g/L agar as `SOLID_AGAR`; the schema has a
`SEMISOLID` state that more precisely fits this source branch.

## Completeness

The absent `target_organisms`, growth evidence, references, and stock
solutions are not defects for this imported source record. The source pH is
missing, and M1507 is not linked as a physical-state variant of M1506 even
though both descend from NBRC 288.

Before this report was written, an exact `find` search under the ignored
`reports/yaml_record_review` directory found no pre-existing
`*-togo_medium_m1507.md` report.

## Findings

| Severity | Finding |
| --- | --- |
| minor | pH 6.8 is absent. Owner: `data/normalized_yaml/bacterial/togo_medium_m1507.yaml` or the TOGO/NBRC importer. |
| minor | The 1 g/L agar branch is modeled as `SOLID_AGAR` and is not linked to the liquid NBRC 288 parent as a semisolid variant. Owner: `data/normalized_yaml/bacterial/togo_medium_m1507.yaml` and `data/normalized_yaml/bacterial/togo_medium_m1506.yaml`. |

## Recommended Edits

1. Add `ph_value: 6.8`.
2. Reclassify the 1 g/L agar branch as `SEMISOLID` if the schema state is
   intended for source formulations, not only motility tests.
3. Link M1506 and M1507 as liquid and semisolid NBRC 288 variants.
4. Regenerate `data/merge_yaml/merged/togo_medium_m1507.yaml`.

## Follow-up Checks

Run the focused schema, strict, reference, and term validators on the
regenerated record, then manually compare it with TOGO M1507 and NBRC 288 for
the 1 g/L agar semisolid branch and pH 6.8.

## Additional Notes

An exact gitignore-independent source search covered `data/import_tracking`,
`data/normalized_yaml/bacterial`, `data/merge_yaml/merged`, and ignored
`reports/yaml_record_review`; it found the expected normalized owner,
generated merge, and import-tracking rows for `CultureMech:008051` /
`TOGO:M1507`.
