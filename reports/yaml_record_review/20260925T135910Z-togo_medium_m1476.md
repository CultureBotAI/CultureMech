# YAML Record Review: togo_medium_m1476

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1476.yaml`
- Started UTC: 2026-09-25T13:59:10Z
- Finished UTC: 2026-09-25T13:59:10Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1476, imported from NBRC medium 256.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1476.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1476`; the TOGO API identifies NBRC_M256 as its original medium ID and links NBRC medium 256.

The TOGO payload and NBRC page agree on 5 g yeast extract, 5 g Bacto Casamino Acids, 1 g KH2PO4, 1 g KCl, 1 g NH4Cl, 0.24 g MgSO4 x 7 H2O, 0.17 g CaSO4 x 2 H2O, 1 g sodium glutamate, 5 g Na2CO3, 250 g NaCl, distilled water made up to 1 L, 20 g agar if needed, an initial pH 6.5 adjustment before autoclaving, separate Na2CO3 sterilization, a final pH of 9.0-9.5, and agar heating before NaCl addition.

## Evidence

The generated non-water gram quantities match NBRC medium 256.

The generated record moves the starred optional agar into an empty `Unknown solution` and drops the source preparation text about pH staging, Na2CO3 separate sterilization, and agar heating.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The pH and preparation text are required to reconstruct the source formulation because the recipe has an initial pH before autoclaving and a different final pH after adding separately sterilized Na2CO3.

## Findings

- Major issue: `Agar (if needed)*` is modeled as an empty solution named `Unknown solution` instead of an optional final-medium solidifying agent.
- Minor issue: the initial pH 6.5 adjustment and final pH 9.0-9.5 target are absent.
- Minor issue: `Distilled water, make up to` is represented as 1 g/L instead of a final volume of 1 L.
- Minor issue: the instructions to sterilize Na2CO3 separately, add it to the autoclaved ingredients, and dissolve agar by heating before adding NaCl are absent.

## Recommended Edits

- Move optional agar out of the empty solution wrapper and keep the source instruction to dissolve it by heating before adding NaCl.
- Add preparation steps for pH 6.5 adjustment before autoclaving and separate Na2CO3 sterilization followed by addition to the other autoclaved ingredients.
- Add `ph_range` 9.0-9.5 for the final source pH.
- Correct or omit the final-volume water row according to the repo's convention.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Add or run a regression check for starred optional agar rows so TOGO importer output does not convert them into empty solutions.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1476 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
