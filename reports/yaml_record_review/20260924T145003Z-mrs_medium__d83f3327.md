# YAML Record Review: MRS Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_medium__d83f3327.yaml
- Started UTC: 2026-09-24T14:48:45Z
- Finished UTC: 2026-09-24T14:50:03Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:008586` / `mrs_medium` at `data/merge_yaml/merged/mrs_medium__d83f3327.yaml`.

- Generated source: `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`
- Merge source: `TOGO_M1_MRS_Medium`
- Merge fingerprint: `d83f3327021995cd559b23142d77419f1a806fc402c413594ad890b4ceade158`
- Category: `bacterial`
- Medium term: `TOGO:M1`, label `MRS Medium`
- Original source: JCM `JCM_M1`, GRMD 1

## Validation

| Check | Result |
|---|---|
| Open LinkML schema with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__d83f3327.yaml` | Passed. |
| Strict validator with `scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__d83f3327.yaml --out /private/tmp/mrs_medium__d83f3327.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator with `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__d83f3327.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were applicable. |
| Term validator with `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__d83f3327.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml` records, not embedded `MediaRecipe.curation_history` events. |

## Identity and Grounding

The record identifies the JCM MRS Medium imported through TOGO Medium M1. TOGO reports `gm` `http://togomedium.org/medium/M1`, name `MRS Medium`, original media ID `JCM_M1`, source URL `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1`, and pH `6.0-6.5`; JCM GRMD 1 is also titled `MRS MEDIUM`.

The broad ingredient identities match TOGO/JCM MRS Medium, but two concentration and grounding details do not:

- JCM and TOGO state 1 L distilled water; YAML records it as 1 `G_PER_L`.
- JCM and TOGO state 50 mg of the variable manganese sulfate hydrate; YAML records it as 50 `G_PER_L` and grounds the variable hydrate row to generic `CHEBI:86360`.

The exact `CultureMech:008586` search across `data/normalized_yaml` and `data/merge_yaml` used `rg --no-ignore --hidden`. It found the maintained top-level owner, this generated merge, generated indexes, and two pH 5.5 variant records that reference `CultureMech:008586` as their `parent_media`.

## Evidence

The TOGO API and JCM GRMD 1 agree on the source formula: 10 g casein peptone, 10 g beef extract, 5 g yeast extract, 20 g glucose, 1 g Tween 80, 2 g K2HPO4, 5 g sodium acetate, 2 g diammonium citrate, 0.2 g MgSO4 x 7H2O, 50 mg manganese sulfate variable hydrate, and 1 L distilled water.

JCM lists an `Adjust pH to 6.0-6.5.` instruction, and TOGO preserves the same pH as both `ph: 6.0-6.5` metadata and an adjustment comment. The reviewed generated record has no `ph_range` and no `preparation_steps`, so those source claims are absent. The JCM source page also carries its standard page-level autoclaving instruction for media without a contrary sterilization note, but no sterilization step is represented.

The current maintained owner adds two pH 5.5 children, `TOGO_M103_MRS_Medium_pH_5.5.yaml` and `mrs_medium_ph_5_5.yaml`. Those reciprocal variant links are absent from the generated merge because the merge was generated before the September 2026 variant-link repairs.

## Completeness

The recipe is incomplete and one mineral amount is incorrect:

- pH 6.0-6.5 is missing.
- The pH-adjustment and default JCM autoclaving steps are missing.
- Distilled water is dimensionally wrong.
- Manganese sulfate variable hydrate is 1000-fold too high.
- Variant-child links from the maintained normalized record have not been propagated to `data/merge_yaml/merged/`.

No target-organism or growth-metric claims are present. Their absence is acceptable for this source recipe because TOGO M1 and JCM GRMD 1 only define the medium formula.

## Findings

### Major

1. The distilled-water row uses grams instead of the source volume.
   - Evidence: TOGO and JCM both list `Distilled water`, 1 L; the generated and maintained YAML have `value: '1'` and `unit: G_PER_L`.
   - Impact: the final-volume solvent is dimensionally wrong.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__d83f3327.yaml`.

2. The manganese sulfate variable hydrate row is 1000-fold too concentrated and over-grounded.
   - Evidence: TOGO and JCM both list 50 mg for the variable manganese sulfate hydrate; the YAML stores 50 `G_PER_L` and grounds the variable hydrate to generic `CHEBI:86360`.
   - Impact: the generated recipe overstates the mineral amount and presents a variable hydrate as an exact CHEBI identity.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__d83f3327.yaml`.

3. The source pH and preparation instructions are missing.
   - Evidence: TOGO records pH `6.0-6.5`; JCM and TOGO both carry an `Adjust pH to 6.0-6.5.` comment; JCM has the default 121 C for 15 min autoclave instruction.
   - Impact: the generated recipe lacks the pH target and sterilization context needed to reproduce JCM MRS Medium.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__d83f3327.yaml`.

4. The generated merge is stale relative to its maintained normalized owner.
   - Evidence: `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml` has two September 2026 `variant_children` links to pH 5.5 variants; `data/merge_yaml/merged/mrs_medium__d83f3327.yaml` ends at the August 2026 merge history and has neither link.
   - Impact: generated pages or exports built from `data/merge_yaml/merged/` will omit known pH-variant relationships until the merge is regenerated.
   - Owner: regenerate `data/merge_yaml/merged/mrs_medium__d83f3327.yaml` from `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`.

### Minor

None found.

### Blocker

None found.

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`, change distilled water from `1 G_PER_L` to a supported 1 L volume representation.
2. Change `MnSO4 xH2O` from `50 G_PER_L` to the JCM-supported 50 mg amount and remove the exact CHEBI grounding unless a variable manganese sulfate hydrate term is available.
3. Add pH 6.0-6.5, the pH-adjustment step, and the JCM default autoclave instruction.
4. Append a `curation_history` event that names the TOGO M1 and JCM GRMD 1 evidence and describes the water, manganese, pH, and preparation changes.
5. Regenerate `data/merge_yaml/merged/mrs_medium__d83f3327.yaml` so the corrected formula and current pH-variant links are present in generated output.

## Follow-up Checks

After curation, run the same focused generated-record checks:

- `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__d83f3327.yaml`
- `python scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__d83f3327.yaml --workers 1 --quiet`
- `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__d83f3327.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__d83f3327.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`

Also rerun the merge freshness check that owns `data/merge_yaml/merged/` to ensure the September 2026 normalized variant links are present in the regenerated record.

## Additional Notes

- This generated record is a one-source TOGO/JCM merge. The exact ignored-file-inclusive `CultureMech:008586` search found pH-variant parent links but no second maintained top-level record with the same ID.
- The adjacent DSMZ MRS Medium record is a distinct imported source record with DSMZ / MediaDive identifiers and a triammonium citrate row; it is not a direct duplicate of TOGO M1.
