# YAML Record Review: Modified HM Medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_hm_medium__63e6e47a.yaml`
- Started UTC: 2026-09-24T11:23:41Z
- Finished UTC: 2026-09-24T11:24:22Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007525` |
| Name | `modified_hm_medium` |
| Original name | Modified HM Medium |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_hm_medium__63e6e47a.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/TOGO_M1011_Modified_HM_Medium.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `TOGO_M1011_Modified_HM_Medium` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_hm_medium__63e6e47a.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_hm_medium__63e6e47a.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_hm_medium__63e6e47a.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record identifies TOGO M1011, Modified HM Medium, with source
metadata for JCM_M961-2 and the direct JCM 961 page. The TOGO M1011 formula
includes 20 g/L agar, so its generated `SOLID_AGAR` physical state reflects a
solid variant of JCM 961 rather than the liquid MediaDive J961 import.

The generated inorganic and agar groundings resolve to the intended chemicals.
Bacto peptone and BD-Difco yeast extract are intentionally opaque undefined
ingredients.

## Evidence

TOGO M1011 supports the generated 100 g/L NaCl, 1 g/L MgSO4 x 7 H2O, 2 g/L
KCl, 0.36 g/L CaCl2 x 2 H2O, 0.06 g/L NaHCO3, 0.23 g/L NaBr, 0.1 g/L glucose,
20 g/L agar, 1 g/L yeast extract, and 0.5 g/L Bacto peptone.

Two generated ingredient quantities disagree with TOGO M1011:

| Component | TOGO amount | Generated amount |
|---|---:|---:|
| Distilled water | 1 L | 1 `G_PER_L` |
| FeCl3 x 6 H2O | 1 mg | 1 `G_PER_L` |

TOGO M1011 also carries source comments to adjust pH to 7.0 and, for solid
medium, add 20 g/L agar. The generated record has the agar ingredient but omits
the pH value and the source preparation comments.

JCM 961 also falls under the page-level default to autoclave at 121 C for 15
min unless otherwise stated. That sterilization step is absent from the
generated record.

## Completeness

The generated record is incomplete for source preparation because it omits the
source pH 7.0 adjustment and default JCM autoclave sterilization. It is also
dimensionally unsafe until the 1 L water and 1 mg ferric chloride rows are
corrected in the maintained TOGO source.

Optional `target_organisms` and `discussion` are empty. Those omissions are not
defects because TOGO M1011 provides a medium formula, not strain-specific growth
curation for a target organism.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/TOGO_M1011_Modified_HM_Medium.yaml` imports
   1 mg FeCl3 x 6 H2O as 1 `G_PER_L` and 1 L distilled water as 1 `G_PER_L`.
   The ferric chloride value is 1000-fold too high, and the water row has the
   wrong dimension.

2. `data/normalized_yaml/bacterial/TOGO_M1011_Modified_HM_Medium.yaml` drops
   the TOGO/JCM pH 7.0 adjustment and the JCM page-level autoclave default, so
   the generated solid-agar HM recipe is missing supported preparation data.

### Minor

None found.

## Recommended Edits

1. Repair `data/normalized_yaml/bacterial/TOGO_M1011_Modified_HM_Medium.yaml`
   so FeCl3 x 6 H2O is represented as 0.001 g/L, distilled water remains a
   1 L volume, and pH 7.0 is retained from the TOGO comment.

2. Add a supported autoclave preparation step scoped to the JCM page-level
   default of 121 C for 15 min.

3. Regenerate merged recipes and downstream browser/page artifacts from the
   repaired normalized source.

## Follow-up Checks

- Run the focused open schema, strict schema, term, and reference validators on
  the regenerated `data/merge_yaml/merged/modified_hm_medium__63e6e47a.yaml`.
- Re-fetch TOGO M1011 and JCM 961 and manually verify the regenerated record
  preserves the 20 g/L agar variant, pH 7.0, 1 mg FeCl3 x 6 H2O, 1 L water, and
  JCM default sterilization.

## Additional Notes

- None found.
