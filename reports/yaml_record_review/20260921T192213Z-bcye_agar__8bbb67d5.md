# YAML Record Review: bcye_agar__8bbb67d5

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bcye_agar__8bbb67d5.yaml
- Started UTC: 2026-09-21T19:20:16Z
- Finished UTC: 2026-09-21T19:22:13Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bcye_agar__8bbb67d5.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bcye_agar.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:001711` |
| Name | `bcye_agar` |
| Original name | `BCYE AGAR` |
| Source | DSMZ Medium 585, imported as `mediadive.medium:585` |
| Merge status | Generated one-source merge from `bcye_agar` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated DSMZ 585 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this MediaDive import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated DSMZ 585 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bcye_agar__8bbb67d5.yaml` denotes DSMZ Medium 585, `BCYE AGAR`, and is a one-source generated merge from `data/normalized_yaml/bacterial/bcye_agar.yaml`.
- The generated merge and normalized owner match exactly after removing `merge_fingerprint` and `merged_from`, so future fixes belong in the normalized owner followed by merge regeneration.
- The inspected DSMZ 585 PDF identifies BCYE AGAR and gives two preparation routes: a commercial OXOID Legionella CYE-Agar base with Legionella BCYE supplement, and an explicit CYE-ACES AGAR alternative with solutions A through D.
- The source supports the medium identity but not the YAML's flattened quantities or its `OXOID Legionella CYE-Agar base` row at `1000 G_PER_L`.
- Exact gitignore-independent searches for `CultureMech:001711`, exact `mediadive.medium:585`, `DSMZ_Medium585.pdf`, `KOMODO_585_BCYE_AGAR`, and `DSMZ Medium 585` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, generated merge, KOMODO sibling, DSMZ 585a sibling, indexes, and import-priority reports, but no local raw DSMZ Medium 585 PDF capture under `data`.

## Evidence

- DSMZ 585 lists explicit CYE-ACES stock solutions: solution A has 10 g yeast extract, 10 g ACES, 2 g activated charcoal, and 490 ml distilled water; solution B has 15 g agar and 490 ml distilled water; solution C has 0.40 g cysteine-HCl x H2O in 10 ml distilled water; solution D has 0.25 g Fe4(PO4)2 in 10 ml distilled water.
- The PDF combines solutions A through D into a complete 1 L medium. The final amounts are therefore 10 g/L yeast extract, 10 g/L ACES, 2 g/L activated charcoal, 15 g/L agar, 0.4 g/L cysteine-HCl x H2O, and 0.25 g/L Fe4(PO4)2.
- The YAML instead records 20.4082 g/L yeast extract and ACES, 4.08163 g/L activated charcoal, 30.6122 g/L agar, 40 g/L cysteine-HCl x H2O, and 25 g/L Fe4(PO4)2, which are the individual stock concentrations rather than final-medium concentrations.
- The PDF adjusts the complete medium to pH 6.9 with a tolerance of 0.5 at 50 C; the YAML has no `ph_value` or `ph_range`.
- The PDF names ACES, activated charcoal, and Fe4(PO4)2; the YAML grounds ACES and ferric pyrophosphate but leaves activated charcoal unresolved.

## Completeness

- Consequential gaps:
  - Solution A through D boundaries are missing, so stock volumes and final amounts cannot be reconstructed from the YAML.
  - All listed stock-solution solutes have final concentration errors.
  - The 490 ml, 490 ml, 10 ml, and 10 ml distilled-water rows are absent.
  - The final pH 6.9 adjustment is absent.
  - The preparation sequence is flattened into unscoped source text and does not model solution preparation before final assembly.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the DSMZ 585 PDF is a formulation/protocol source and not a strain-specific growth assay.
  - The commercial OXOID shortcut and any dehydrated medium base should not be forced to a narrow ChEBI small-molecule term.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw DSMZ Medium 585 PDF capture under `data/raw`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record stores stock concentrations as final concentrations. | DSMZ's CYE-ACES alternative sums to 1 L; the YAML divides solution A rows by 0.49 L, solution B agar by 0.49 L, and solution C/D rows by 0.01 L instead of preserving 10 g, 10 g, 2 g, 15 g, 0.4 g, and 0.25 g per complete liter. | `data/normalized_yaml/bacterial/bcye_agar.yaml`. |
| Major | `OXOID Legionella CYE-Agar base` at `1000 G_PER_L` is unsupported. | DSMZ says it uses an OXOID dehydrated commercial preparation but gives no 1000 g/L amount for the base; the numeric formula printed in the PDF belongs to the CYE-ACES alternative. | `data/normalized_yaml/bacterial/bcye_agar.yaml`. |
| Major | DSMZ's solution structure and final pH are missing. | The PDF prepares solutions A, B, C, and D separately, cools or filter-sterilizes them differently, then adjusts the complete medium to pH 6.9 before adding solution B; the YAML has no solution nodes and no pH slot. | `data/normalized_yaml/bacterial/bcye_agar.yaml`. |
| Minor | The ACES row still uses the legacy `mediaingredientmech_term` slot. | This row has `term: CHEBI:39061` but retains `mediaingredientmech_term: MediaIngredientMech:000555` while neighboring migrated rows use `mediaingredientmech_chebi_term`. | `data/normalized_yaml/bacterial/bcye_agar.yaml`; broad repeats may need the MIM legacy migration. |

## Recommended Edits

1. Rebuild the normalized DSMZ 585 record around the explicit CYE-ACES formulation: solutions A through D with their source amounts, water volumes, sterilization conditions, and the final assembly step.
2. Convert or represent final concentrations as 10 g/L yeast extract, 10 g/L ACES, 2 g/L activated charcoal, 15 g/L agar, 0.4 g/L cysteine-HCl x H2O, and 0.25 g/L Fe4(PO4)2.
3. Remove the unsupported `OXOID Legionella CYE-Agar base` `1000 G_PER_L` ingredient, or represent the OXOID route as a separate commercial shortcut without inventing an amount.
4. Add the supported final pH condition: 6.9 with the source tolerance of 0.5 at 50 C.
5. Replace ACES' legacy `mediaingredientmech_term` with a CHEBI-keyed MIM link if the packaged MIM index still supports ACES.
6. Regenerate `data/merge_yaml/merged/bcye_agar__8bbb67d5.yaml` from the repaired normalized owner.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Manually compare the repaired record against the DSMZ Medium 585 PDF line by line, checking all four stock solutions, pH adjustment, autoclave/filter boundaries, cooling temperatures, and final mixing order.
- Run `just verify-merges` and `just audit-merge-freshness --json --list` after regenerating to prove the generated merge is current.
- Re-check exact `CultureMech:001711`, exact `mediadive.medium:585`, and `DSMZ_Medium585.pdf` occurrences with a gitignore-independent search to verify only the normalized owner and fresh generated merge carry the repaired DSMZ 585 representation.

## Additional Notes

- The inspected upstream source was the DSMZ Medium 585 PDF fetched from the URL embedded in the record.
- The KOMODO 585 sibling still mirrors the same concentration inflation in a separate normalized record; this review records that sibling as related context but did not fully review it.
- Exact gitignore-independent searches included ignored files where present.
