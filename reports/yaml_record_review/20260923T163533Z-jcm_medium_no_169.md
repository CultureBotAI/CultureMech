# YAML Record Review: JCM MEDIUM No. 169

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_169.yaml
- Started UTC: 2026-09-23T16:35:08Z
- Finished UTC: 2026-09-23T16:35:33Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_169.yaml`
- Stable ID: `CultureMech:002528`
- Name: `jcm_medium_no_169`
- Original name: `JCM MEDIUM No. 169`
- Category: `bacterial`
- Media term: `mediadive.medium:J169`
- Source: MediaDive import of JCM Medium 169, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=169`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_169.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_169`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_169.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_169.yaml --out /private/tmp/jcm_medium_no_169.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_169.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_169.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators do not compare source accessions for duplicate JCM imports or check supplier qualifiers on undefined ingredients.

## Identity and Grounding

The record identifies MediaDive `J169` for JCM Medium 169. MediaDive, the live JCM `GRMD=169` page, and TogoMedium M161 all identify the same pH 7.4 high-salt Casamino acids/yeast extract agar.

The `COMPLEX`, `UNDEFINED`, and `SOLID_AGAR` classification is supported by the Casamino acids, yeast extract, and agar rows. Casamino acids are correctly left without a CHEBI `term` because the curation history explicitly de-grounded that mixture.

## Evidence

Inspected evidence:

- The live JCM `GRMD=169` page lists 7.5 g Casamino acids (BD-Difco), 10 g yeast extract (BD-Difco), 3 g trisodium citrate, 20 g MgSO4 x 7 H2O, 2 g KCl, 250 g NaCl, 0.05 g FeSO4 x 7 H2O, 0.2 mg MnSO4 x n H2O, and 20 g agar.
- JCM instructs the curator to add components to distilled water, bring the volume to 1.0 L, and adjust pH to 7.4.
- MediaDive REST `medium/J169` reports the same nine ingredients and two preparation steps.
- TogoMedium M161 points to `JCM_M169` and `GRMD=169` and carries the same core formula, with a derived 1 L distilled-water row.

Supported:

- The MediaDive ID, JCM link, category, complex/undefined/solid-agar classification, pH value, all ingredient quantities, and both preparation steps are supported by MediaDive and JCM.
- The 0.2 mg manganese sulfate source row is correctly represented as 0.2 `MG_PER_L`, not as a gram-per-liter concentration.
- Omitting a separate water ingredient in the direct MediaDive record is acceptable here because JCM gives water as part of the instruction to bring the recipe to 1.0 L, not as a table row.

Unsupported or incomplete:

- The source qualifies Casamino acids and yeast extract as `BD-Difco`; the generated direct record stores both rows generically.

## Completeness

Consequential gap:

- The two supplier qualifiers need restoration so the undefined ingredients point at the exact JCM materials.

Empty slots that are acceptable:

- No stock solution appears in the source formulation.
- No target-organism or growth-metric claim is represented.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=169`, exact `JCM_M169`, and exact `jcm_medium_no_169` IDs found only the direct MediaDive/JCM record and the TOGO M161 wrapper. No other normalized or merged copy of this JCM 169 source was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/togo_medium_m161.yaml` and `data/merge_yaml/merged/togo_medium_m161.yaml` pointing to `JCM_M169` and `GRMD=169`. The TOGO wrapper adds a water row with `1 G_PER_L`, while the direct source correctly represents the 1 L water volume procedurally. | Both normalized owners, plus `scripts/merge_recipes.py` after source repair. |
| Minor | The `BD-Difco` supplier qualifiers are missing from Casamino acids and yeast extract. | JCM lists `Casamino acids (BD-Difco)` and `Yeast extract (BD-Difco)`, and MediaDive stores `attribute: "BD-Difco"` for both rows. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_169.yaml`. |

## Recommended Edits

1. Repair `data/normalized_yaml/bacterial/togo_medium_m161.yaml` so the JCM instruction to bring the recipe to 1 L with distilled water is not represented as `distilled water`, 1 `G_PER_L`.
2. Preserve `BD-Difco` qualifiers on the Casamino acids and yeast-extract rows in `data/normalized_yaml/bacterial/jcm_medium_no_169.yaml`.
3. Regenerate `data/merge_yaml/merged/` and verify whether `jcm_medium_no_169` and `togo_medium_m161` collapse to one canonical generated recipe.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Compare the regenerated direct record against the live JCM `GRMD=169` table and MediaDive `medium/J169`, paying particular attention to the 0.2 mg manganese sulfate row.
- Search with `rg --no-ignore --hidden 'GRMD=169([^0-9]|$)|JCM_M169([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to verify duplicate-source handling with ignored files included.

## Additional Notes

- No pH, unit, or water-volume defect was found in the direct MediaDive record under review. The water-volume defect is in the sibling TOGO import.
