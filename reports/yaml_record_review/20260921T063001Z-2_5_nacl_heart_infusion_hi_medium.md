# YAML Record Review: 2_5_nacl_heart_infusion_hi_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_5_nacl_heart_infusion_hi_medium.yaml`
- Started UTC: 2026-09-21T06:28:39Z
- Finished UTC: 2026-09-21T06:30:01Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008810` |
| Name | `2_5_nacl_heart_infusion_hi_medium` |
| Original name | `2.5% NaCl heart infusion (HI) medium` |
| Category | `bacterial` |
| Source term | `TOGO:M2222`, `2.5% NaCl heart infusion (HI) medium` |
| Source URL | `https://togomedium.org/medium/M2222` |
| Merge fingerprint | `a01f24d7b393eccf4a9ac194557c4759644db7c5e8d3da114fb7e5c81a2807bf` |
| Maintained owner | `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO M2222 import. Future corrections belong in `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_5_nacl_heart_infusion_hi_medium.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_5_nacl_heart_infusion_hi_medium.yaml --out /private/tmp/2_5_nacl_heart_infusion_hi_medium.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_5_nacl_heart_infusion_hi_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_5_nacl_heart_infusion_hi_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M2222`, the label is `2.5% NaCl heart infusion (HI) medium`, and the live TOGO `gmdb_medium_by_gmid?gm_id=M2222` API payload names `2.5% NaCl heart infusion (HI) medium` at `http://togomedium.org/medium/M2222`.

Ingredient identity is supported at the string level by the TOGO API payload: distilled water, NaCl, chloramphenicol, ampicillin, tetracycline, and heart infusion broth all appear in the single component paragraph. The CHEBI terms for water, sodium chloride, chloramphenicol, ampicillin, and tetracycline match those labels. `Heart infusion broth` is correctly left ungrounded in the target because it is a complex commercial medium rather than one simple chemical.

Fetching `https://togomedium.org/medium/M2222` returned the TOGO single-page application shell without a static M2222 recipe table, so the inspected live source was the TOGO REST API route used by the repository's own `src/culturemech/fetch/togo_fetcher.py`.

## Evidence

The inspected TOGO M2222 API payload supports this ingredient list:

- 1 L distilled water.
- 2.5% NaCl.
- 2 ug/ml chloramphenicol.
- 20 ug/ml ampicillin.
- 2 ug/ml tetracycline.
- 37 g heart infusion broth.

The generated target preserves the ingredient set and the 2.5% NaCl row. It stores `Distilled water` as `1 G_PER_L`, however, which turns a solvent volume into a mass concentration. More seriously, it preserves the numeric antibiotic values but changes all three units from `ug/ml` to `G_PER_L`: chloramphenicol is recorded as 2 g/L instead of 2 ug/ml, ampicillin is 20 g/L instead of 20 ug/ml, and tetracycline is 2 g/L instead of 2 ug/ml.

## Completeness

Consequential gaps:

- The three antibiotic concentrations use the wrong unit and are off by a factor of 1,000,000.
- The distilled-water amount has the wrong dimension.
- The record has no structured `references`, so the TOGO M2222 API or page URL is not validator-visible.

Empty optional slots:

- `solutions` is correctly absent. The inspected TOGO M2222 payload is a flat formulation with no named stock solutions.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected TOGO payload supports a formulation, not organism-specific growth evidence.
- Preparation and pH are absent in the inspected TOGO M2222 payload, so their absence in the record is not a defect from this source.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_5_nacl_heart_infusion_hi_medium.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `history`, `reports`, and `references_cache` for `CultureMech:008810`, `TOGO:M2222`, `M2222`, `2.5% NaCl heart infusion`, `2_5_nacl_heart_infusion_hi_medium`, and the merge fingerprint found only the expected normalized owner, generated merge, derived registry/index/manifest rows, TOGO analysis rows, unresolved-ingredient reports, and historical archive report rows. Ignored files were included under the searched paths.
- A gitignore-independent `rg --no-ignore --hidden` search of `data/raw/togo` for `M2222`, the medium label, and the four non-water/non-NaCl ingredient names found nothing beyond `data/raw/togo/README.md`; ignored files were included.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | All antibiotic concentrations use `G_PER_L` where the TOGO source uses `ug/ml`. | The live TOGO M2222 API payload lists chloramphenicol 2 `ug/ml`, ampicillin 20 `ug/ml`, and tetracycline 2 `ug/ml`. The generated and normalized records store the same numeric values with `G_PER_L`, which changes each amount by a factor of 1,000,000. | `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` |
| Major | Distilled water is represented with the wrong dimension. | TOGO M2222 lists 1 L distilled water. The generated and normalized records store `Distilled water` as `1 G_PER_L`, turning a final solvent volume into a 1 g/L mass concentration. | `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO URL only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` |

## Recommended Edits

1. Correct chloramphenicol, ampicillin, and tetracycline in `data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` so they retain the TOGO source unit `ug/ml`, or the schema's exact microliter-equivalent unit if one exists, instead of `G_PER_L`.
2. Correct the distilled-water row so it represents the source's 1 L solvent volume rather than `1 G_PER_L`.
3. Add structured TOGO M2222 source provenance, preferably including both `https://togomedium.org/medium/M2222` and the `gmdb_medium_by_gmid?gm_id=M2222` API route used for verification.
4. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` after repairing units.
- `just validate-references data/normalized_yaml/bacterial/2_5_nacl_heart_infusion_hi_medium.yaml` after adding structured TOGO references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against live TOGO M2222 for 1 L distilled water, 2.5% NaCl, 2 ug/ml chloramphenicol, 20 ug/ml ampicillin, 2 ug/ml tetracycline, and 37 g heart infusion broth.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
