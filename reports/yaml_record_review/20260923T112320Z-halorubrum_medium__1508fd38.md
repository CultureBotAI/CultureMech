# YAML Record Review: HALORUBRUM MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml`
- Started UTC: 2026-09-23T11:22:20Z
- Finished UTC: 2026-09-23T11:23:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000873` |
| Name | `halorubrum_medium` |
| Original name | `HALORUBRUM MEDIUM` |
| Category | `archaea` |
| Physical state | `SOLID_AGAR` |
| pH range | 7.0-7.5 |
| Generated from | `data/normalized_yaml/archaea/halorubrum_medium.yaml` |
| Source accession | `mediadive.medium:1411` |
| Source PDF | `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium1411.pdf` |
| Merge fingerprint | `1508fd38aa57b7be0ea7f5781dcbb721d048157241836db777b67909d8d33f42` |

I reviewed the generated merged record, its direct MediaDive/DSMZ normalized
owner, the DSMZ Medium 1411 PDF, and the MediaDive 1411 REST payload.

I searched `data/normalized_yaml` and `data/merge_yaml` YAML files with
`rg --no-ignore --hidden` for `halorubrum_medium`, `HALORUBRUM MEDIUM`,
`mediadive.medium:1411`, `DSMZ Medium 1411`, and `DSMZ_Medium1411`. Ignored
files were included. The search found this DSMZ 1411 branch, a same-label
DSMZ 1422 branch, a distinct CM1A Halorubrum medium, and the generated outputs
for those branches; only `halorubrum_medium.yaml` feeds this reviewed
fingerprint.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml` reported `No issues found`. |
| Strict schema | Passed: `python scripts/validate_strict.py data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml --out /private/tmp/halorubrum_medium_1508fd38.strict.tsv --workers 1 --quiet` exited 0 and wrote a header-only TSV. |
| References | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with no diagnostics. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported `Validation passed`. |
| Embedded history | Not checked: `just validate-history` validates standalone records under `history/`, not embedded `MediaRecipe.curation_history` entries. |

The `just` validator entrypoints were not used because this checkout attempts
to resolve a Python 3.13 environment with `llvmlite==0.46.0`; the focused
validators above were run offline with Python 3.11 against the local uv cache.

## Identity and Grounding

The record correctly identifies DSMZ Medium 1411, `HALORUBRUM MEDIUM`, and
preserves the source pH range of 7.0-7.5. The ingredient identities and
amounts for `NaCl`, `MgCl2`, `MgSO4 x 7 H2O`, `CaCl2 x 2 H2O`, `KCl`,
`NaHCO3`, `NaBr`, yeast extract, and agar match the DSMZ and MediaDive rows.

The top-level physical state is too narrow. DSMZ Medium 1411 is a liquid
mineral-salts solution that may be solidified by adding 20 g/L agar; the
generated record sets the whole recipe to `SOLID_AGAR`.

## Evidence

DSMZ 1411 names a `Mineral Salts Solution` with 195 g NaCl, 32.5 g MgCl2,
50.8 g `MgSO4 x 7 H2O`, 0.8 g `CaCl2 x 2H2O`, 5 g KCl, 0.16 g NaHCO3, 0.6 g
NaBr, 5 g yeast extract, and 1000 ml distilled water. MediaDive 1411 preserves
the same rows and records agar as a 20 g conditional ingredient for solid
medium.

The generated record keeps the salts, yeast extract, pH range, and conditional
agar note. It omits the 1000 ml distilled water row and turns the optional
solid agar row into a top-level `SOLID_AGAR` state. Its single preparation step
also uses `action: MIX` even though the source instruction is to autoclave
after adjusting pH.

## Completeness

The only consequential formulation gap is the missing 1000 ml distilled water
component. DSMZ 1411 and MediaDive 1411 do not provide target organisms, growth
evidence, incubation conditions, or stock solutions, so those absent optional
fields are not defects.

The related DSMZ 1422 and CM1A Halorubrum records are separate media and do
not fill gaps in DSMZ 1411.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The liquid base and conditional solid variant are collapsed into one top-level `SOLID_AGAR` record. | DSMZ says the medium may be solidified by adding 20 g/L agar; MediaDive marks the agar row with `condition: "for solid medium"`. | `data/normalized_yaml/archaea/halorubrum_medium.yaml` or a variant-aware MediaDive import rule |
| Major | The 1000 ml distilled-water component is missing. | DSMZ and MediaDive both list 1000 ml distilled water in the main recipe. | `data/normalized_yaml/archaea/halorubrum_medium.yaml` or the MediaDive importer |
| Minor | The preparation action is too generic. | The source says to adjust pH and sterilize by autoclaving; the generated preparation step uses `action: MIX`. | `data/normalized_yaml/archaea/halorubrum_medium.yaml` or the MediaDive preparation importer |

## Recommended Edits

1. In `data/normalized_yaml/archaea/halorubrum_medium.yaml` or the MediaDive
   importer, preserve the liquid base and the 20 g/L agar solid option as
   distinct variant structure instead of forcing `SOLID_AGAR` onto the
   top-level medium.
2. Restore the 1000 ml distilled-water row.
3. Change the preparation action to a pH adjustment followed by autoclaving
   without losing the source pH range of 7.0-7.5.
4. Regenerate merged recipes from the corrected normalized owner; do not patch
   `data/merge_yaml/merged/halorubrum_medium__1508fd38.yaml` directly.

## Follow-up Checks

1. Run `just validate data/normalized_yaml/archaea/halorubrum_medium.yaml`
   after the normalized DSMZ 1411 record or importer is corrected.
2. Run `just validate-media-variant-links` if the conditional agar branch is
   represented as a linked variant.
3. Run `just verify-merges` to prove the generated DSMZ 1411 branch
   regenerates from the corrected normalized source.
4. Manually compare the regenerated record with DSMZ Medium 1411 and MediaDive
   1411 to confirm the 20 g/L agar condition remains optional.

## Additional Notes

None found.
