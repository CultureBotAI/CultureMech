# YAML Record Review: sna_5__a63033ae

- Repository: CultureMech
- Record: data/merge_yaml/merged/sna_5__a63033ae.yaml
- Started UTC: 2026-09-25T05:44:51Z
- Finished UTC: 2026-09-25T05:44:51Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:000412`, `sna_5`, from `data/merge_yaml/merged/sna_5__a63033ae.yaml`.

The target record merges CCAP `SNA/5` with CCAP `SNA`.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is not cleanly grounded to one CCAP recipe.

`SNA/5` is CCAP C97 and `SNA` is CCAP C95; the two sources share a Nutrient Agar base but differ in seawater concentration.

## Evidence

CCAP `MR_SNA5.pdf` defines `SNA/5` as brackish Seawater Nutrient Agar: follow the SNA recipe, but use 200 ml filtered natural seawater and 800 ml distilled water per liter.

CCAP `MR_SNA.pdf` defines `SNA` as 28.0 g Nutrient Agar Oxoid CM3 plus 500.0 ml filtered natural seawater, made up to 1 liter with deionized water.

## Completeness

The generated record keeps only a 500 g/L natural-seawater row, which belongs to `SNA`, not `SNA/5`.

The generated record omits the 800 ml/L distilled-water row required for `SNA/5`.

The generated record expands 28 g/L Nutrient Agar Oxoid CM3 into generic peptone, yeast extract, beef extract, sodium chloride, and agar rows from a third-party Nutrient Agar description.

The generated record lacks the 28 g/L Nutrient Agar Oxoid CM3 product row asserted by CCAP.

The generated merge collapses brackish `SNA/5` and seawater `SNA` even though their seawater fractions differ.

## Findings

Different CCAP SNA and SNA/5 source recipes were falsely merged.

The SNA seawater amount overwrote the SNA/5 seawater dilution.

An opaque Oxoid CM3 product was replaced by a generic composition not asserted by the CCAP source.

Final distilled water was lost.

## Recommended Edits

Split `SNA` and `SNA/5` into separate canonical records or make `SNA/5` an explicit brackish variant under `SNA`.

Represent `SNA/5` with 28 g/L Nutrient Agar Oxoid CM3, 200 ml/L filtered natural seawater, and 800 ml/L distilled water.

Represent `SNA` with 28 g/L Nutrient Agar Oxoid CM3, 500 ml/L filtered natural seawater, and enough deionized water to reach 1 L.

Remove the generic Nutrient Agar constituent expansion unless there is a direct Oxoid CM3 product specification that supports it.

Regenerate the merged YAML after repairing the CCAP sources.

## Follow-up Checks

Confirm the regenerated `SNA/5` record has 200 ml/L filtered natural seawater, not 500 ml/L.

Confirm the regenerated `SNA` and `SNA/5` records no longer collapse without an explicit variant relationship.

Confirm no generic MicrobeNotes-derived Nutrient Agar expansion remains unless directly sourced to Oxoid CM3.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
