# YAML Record Review: AG MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/AG_MEDIUM.yaml
- Started UTC: 2026-09-21T09:55:36Z
- Finished UTC: 2026-09-21T09:57:33Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/AG_MEDIUM.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:003057`
- Label: `ag_medium`
- Original name: `AG MEDIUM`
- Category: `bacterial`
- Media term: `mediadive.medium:J710`
- Generated status: generated single-source record from `data/normalized_yaml/bacterial/JCM_J710_AG_MEDIUM.yaml`

## Validation

All narrow record-level validators passed for the generated YAML shape:

- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/AG_MEDIUM.yaml`
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/AG_MEDIUM.yaml --out /private/tmp/ag_medium.strict.tsv --workers 1 --quiet`
  - Summary: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/AG_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Summary: 1 file validated, 0 total checks, all validations passed.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/AG_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Summary: term validation passed; the command also printed the expected `eutils` / `pkg_resources` deprecation warning.
- Not checked: embedded `MediaRecipe.curation_history` semantics. The repository documents `just validate-history` for standalone records under `history/`, not as a focused one-file validator for generated embedded history.

The documented `just validate-schema`, `just validate-strict`, and `just validate-terms` entrypoints still fail before target-specific validation because this project resolves `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`; the no-project Python 3.11 invocations above exercise the same validators against the target file.

## Identity and Grounding

The target correctly identifies JCM Medium 710 / `AG MEDIUM`. Live JCM GRMD 710 is available and lists exactly the same non-water components at the same gram-per-liter concentrations: D-glucose 10, glycerol 15, Bacto peptone 5, yeast extract 2, malt extract 2, CaCO3 7, and agar 15.

The ingredient-level groundings on the defined chemicals are appropriate: D-glucose maps to `CHEBI:17634`, glycerol to `CHEBI:17754`, CaCO3 to `CHEBI:3311`, and agar to `CHEBI:2509`. The undefined BD-Difco peptone, yeast extract, and malt extract rows are intentionally ungrounded mixtures.

## Evidence

Supported:

- The direct JCM J710 import is faithful to the currently live JCM page for all non-water formula rows.
- Omitting the source `Distilled water 1.0 L` row is acceptable here because the remaining rows are represented as per-liter concentrations.
- The record is correctly `SOLID_AGAR` because the source includes 15 g agar.

Unsupported or over-scoped:

- The generated record has no structured `references`, so the live JCM GRMD 710 URL in `notes` is not checkable by reference validation.
- The same JCM 710 recipe is also present as TOGO M733 in `data/normalized_yaml/bacterial/TOGO_M733_AG_Medium.yaml` and as generated `data/merge_yaml/merged/ag_medium__761a97fe.yaml`; it did not merge with this target because the TOGO import carries a spurious `Distilled water` value of `1 G_PER_L` and spells `D-Glucose` as `D--Glucose`.

## Completeness

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*AG_MEDIUM*' -print` searched the ignored timestamped-report directory and found no pre-existing AG_MEDIUM report.
- Exact `rg --no-ignore --hidden` searches for `mediadive.medium:J710\b`, `TOGO:M733\b`, `komodo.medium:955\b`, `mediadive.medium:955\b`, `JCM_J710_AG_MEDIUM`, `TOGO_M733_AG_Medium`, and `KOMODO_955_AG_medium` covered tracked and ignored files. They found the JCM 710 target/source, the duplicate TOGO M733 JCM 710 import, and the distinct DSMZ/KOMODO 955 AG-family formula.
- Live JCM GRMD 710 and live TOGO M733 were fetched. Both are copies of JCM M710 and list the 10/15/5/2/2/7/15 gram formula in 1 L distilled water.
- The DSMZ/KOMODO 955 AG medium has glucose 1 g/L, glycerol 1.5 g/L, yeast extract 5 g/L, pH 6.8, and a DSMZ 955 source identity, so it is a distinct formula that should remain separate from JCM 710.

## Findings

### major: JCM 710 is split across direct JCM and TOGO generated records

`AG_MEDIUM.yaml` and `ag_medium__761a97fe.yaml` both represent JCM Medium 710. The TOGO M733 side introduced a 1 L water row as `1 G_PER_L` and a `D--Glucose` spelling variant, so the two source-equivalent imports were fingerprinted into separate generated recipes.

### minor: structured references are absent

The JCM GRMD 710 page is currently live, but the target preserves it only in `notes`. With no `references` array, the reference validator performs zero checks.

## Recommended Edits

1. Fix `data/normalized_yaml/bacterial/TOGO_M733_AG_Medium.yaml`, not either generated merge YAML.
2. Drop or correctly represent the TOGO `Distilled water 1.0 L` row so it no longer appears as `1 G_PER_L`.
3. Normalize `D--Glucose` to the same `D-Glucose` spelling and `CHEBI:17634` grounding used by the direct JCM import.
4. Add structured JCM GRMD 710 references to the direct JCM and TOGO M733 normalized records.
5. Regenerate merge YAML and confirm TOGO M733 and direct JCM J710 collapse into one AG MEDIUM generated record while the DSMZ/KOMODO 955 family remains separate.

## Follow-up Checks

- Rerun the four one-file validators above against regenerated `data/merge_yaml/merged/AG_MEDIUM.yaml`.
- Fetch JCM GRMD 710 and TOGO M733 again and compare the regenerated record against all seven non-water formula rows.
- Search with `rg --no-ignore --hidden 'TOGO:M733\\b|mediadive\\.medium:J710\\b|D--Glucose|value: '\\''1'\\''' data/normalized_yaml/bacterial data/merge_yaml/merged` and confirm the duplicate JCM 710 generated record is gone and the bogus water value is absent.

## Additional Notes

- Optional pH, preparation, organism, and growth metric fields are absent from the live JCM 710 evidence and were not treated as defects.
