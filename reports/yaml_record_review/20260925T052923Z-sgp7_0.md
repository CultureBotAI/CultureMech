# YAML Record Review: sgp7_0

- Repository: CultureMech
- Record: data/merge_yaml/merged/sgp7_0.yaml
- Started UTC: 2026-09-25T05:29:23Z
- Finished UTC: 2026-09-25T05:29:23Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:009681`, `sgp7_0`, from `data/merge_yaml/merged/sgp7_0.yaml`.

The target record is a TOGO M3249 import for `SGP7.0`.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is correctly grounded to TOGO medium M3249.

No same-source duplicate was found in the sorted generated YAML list.

## Evidence

The TOGO payload for M3249 names the medium `SGP7.0` and reports source pH 7.0.

Its final ingredient list has MgSO4 x 7H2O, CaCl2 x 2H2O, Na2HPO4 x 12H2O, HEPES, Na2SiO3 x 9H2O, KH2PO4 x 2H2O, glucose, ultrapure water, and soy peptone.

TOGO reports ultrapure water as 1 L and carries the preparation comment `Medium was adjusted to pH 7.0 with 0.1N NaOH, then brought to 1 liter with UPW (ultrapure water).`

## Completeness

The generated record preserves the gram-scale solutes from TOGO.

The generated record converts the 1 L ultrapure water row to 1 g/L.

The generated record imports `0.1N NaOH` as a variable ingredient even though the source uses it to adjust the medium to pH 7.0.

The source pH 7.0 is not represented in a structured pH field.

The source comment about pH adjustment and bringing the medium to 1 liter is absent from `preparation_steps`.

## Findings

TOGO liter units were normalized into `G_PER_L`, causing a wrong water concentration.

The pH adjustment reagent was imported as a pseudo-ingredient.

Structured pH and preparation instructions were lost.

## Recommended Edits

Repair `data/normalized_yaml/bacterial/sgp7_0.yaml` to represent ultrapure water as the 1 liter solvent row from TOGO.

Remove the variable `0.1N NaOH` ingredient and model the source adjustment as a pH-7.0 preparation step.

Add a structured pH 7.0 field if the schema slot is available for this recipe.

Regenerate the merged YAML after repairing the normalized source.

## Follow-up Checks

Confirm the regenerated record has 1 L or 1000 ml/L ultrapure water, not 1 g/L.

Confirm `0.1N NaOH` no longer appears as a top-level ingredient.

Confirm the TOGO M3249 pH 7.0 metadata is preserved.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
