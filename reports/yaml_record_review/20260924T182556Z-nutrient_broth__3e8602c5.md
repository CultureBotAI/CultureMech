# YAML Record Review: nutrient_broth__3e8602c5

- Repository: CultureMech
- Record: data/merge_yaml/merged/nutrient_broth__3e8602c5.yaml
- Started UTC: 2026-09-24T18:25:56Z
- Finished UTC: 2026-09-24T18:25:56Z
- Verdict: needs curation

## Target

- `id`: `CultureMech:015471`
- `name`: `nutrient broth`
- `category`: `bacterial`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `LIQUID`
- `source_data.origin`: `CultureBotHT`
- `target_organisms`: `Mycolicibacterium cosmeticum` with a PMID:16461697 doubling-time metric
- `kg_microbe_match`: `mediadive.medium:681`
- `merged_from`: `nutrient_broth`

## Validation

- LinkML validation: passed; exited 0 with no diagnostics.
- Strict validation: passed with 0 ERROR rows; `/private/tmp/nutrient_broth__3e8602c5.strict.tsv` was header-only.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked; the available `just validate-history` target validates standalone `history/` files, not embedded `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is the CultureBotHT FEBA `nutrient broth` import: 15 g/L Bacto Peptone, 3 g/L Yeast Extract, 1 g/L D-Glucose, and 6 g/L Sodium Chloride. It is a singleton merge and is not the JCM 663 / TOGO M681 Nutrient Broth recipe.

An ignored-inclusive exact search for `CultureMech:015471`, the DM-11 variant name, PMID:16461697, and the exact CultureBotHT `database_id: nutrient broth` string found only this maintained source, this generated record, and indexes/catalog entries for this record. The broader CultureBotHT repository URL in the local record was still enough to identify the provenance family, but the repository itself was not reachable through the unauthenticated GitHub API and a guessed raw `nutrient_broth.yaml` path returned 404, so the upstream CultureBotHT YAML was not rechecked directly.

## Evidence

The maintained `data/normalized_yaml/bacterial/nutrient_broth.yaml` has already been partially repaired. It adds exact Bacto Peptone and Yeast Extract ontology mappings, adds `sources`, adds `ingredients_curated` and `has_ontology_mappings`, adds `CultureBotHT:nutrient broth` / GitHub references, and removes the stale generated `kg_microbe_match`.

The generated record is stale against those repair edits: it lacks the repaired `sources` and `references` blocks, lacks the ingredient term mappings for Bacto Peptone and Yeast Extract, still has empty `data_quality_flags`, and still carries `kg_microbe_match: mediadive.medium:681`. MediaDive 681 is DSMZ `TGA-MEDIUM`, not this FEBA/CultureBotHT Nutrient Broth formula.

PMID:16461697 does not support the attached Nutrient Broth growth evidence. Its abstract reports the 25 C optimum and 29.2 h doubling time for Mycobacterium sp. strain DM-11, but the same abstract also says that the strain did not grow on complex media including nutrient broth.

## Completeness

The review checked the generated record, the maintained normalized source, MediaDive Medium 681, the PubMed XML for PMID:16461697, schema validation, strict validation, reference validation, term validation, and an ignored-inclusive exact search for this CultureMech ID, CultureBotHT source identity, variant name, and PubMed ID.

## Findings

1. The target-organism evidence is false for this medium. PMID:16461697 explicitly reports that strain DM-11 did not grow on nutrient broth, so the `Mycolicibacterium cosmeticum` doubling-time metric and `nutrient_broth_mycolicibacterium_cosmeticum_dm11_25c` SUPPORT variant should not be attached to this record.
2. The generated `kg_microbe_match: mediadive.medium:681` is stale and wrong. MediaDive 681 is DSMZ `TGA-MEDIUM` with Bacto Tryptone, Glucose, NaCl, and Distilled water; it is not the CultureBotHT nutrient-broth formula with Bacto Peptone, Yeast Extract, D-Glucose, and Sodium Chloride.
3. The generated record is stale relative to the repaired normalized source. It lacks the Bacto Peptone and Yeast Extract ontology mappings, nonempty data-quality flags, the `sources` block, and the local source references.

## Recommended Edits

- Remove the PMID:16461697 SUPPORT variant and `target_organisms` metric from `data/normalized_yaml/bacterial/nutrient_broth.yaml`, or reattach the doubling time to the correct DM-11 growth medium if that medium is curated elsewhere.
- Regenerate the merged record so the stale `kg_microbe_match: mediadive.medium:681` disappears and the repaired CultureBotHT source metadata, data-quality flags, and ingredient mappings are preserved.
- Keep this CultureBotHT FEBA formula separate from JCM 663 / TOGO M681 Nutrient Broth and DSMZ Medium 681; all three are different recipes despite colliding labels or numeric IDs.

## Follow-up Checks

- Re-run generation and confirm the generated `nutrient_broth__3e8602c5` record has no `kg_microbe_match`, no PMID:16461697 target-organism metric, and nonempty source references.
- Re-run schema, strict, reference, and term validation against the regenerated YAML.

## Additional Notes

None found.
