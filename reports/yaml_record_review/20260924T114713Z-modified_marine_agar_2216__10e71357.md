# YAML Record Review: MODIFIED MARINE AGAR 2216

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml
- Started UTC: 2026-09-24T11:47:13Z
- Finished UTC: 2026-09-24T11:47:13Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:015424` |
| Name | `modified_marine_agar_2216` |
| Source identity | `mediadive.medium:J913` |
| Category | `specialized` |
| Maintained owner | MediaDive J913 source normalized as `JCM_J913_MODIFIED_MARINE_AGAR_2216.yaml` |
| Generated artifact | yes; generated under `data/merge_yaml/merged/` from one MediaDive-normalized source |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema validation | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml` reported `No issues found`. |
| Strict CultureMech validation | Passed; `scripts/validate_strict.py data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml --out /private/tmp/modified_marine_agar_2216__10e71357.strict.tsv --workers 1 --quiet` reported 0 errors and wrote only the TSV header line. |
| Reference validation | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` checked one file, ran 0 reference checks, and reported no errors. |
| Term validation | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/modified_marine_agar_2216__10e71357.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after a harmless `pkg_resources` deprecation warning. |
| Embedded history validation | Not checked: the available `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

The record denotes JCM medium 913, "MODIFIED MARINE AGAR 2216", through MediaDive `mediadive.medium:J913`. The source identity and pH value are correct, and the four non-water recipe rows have the correct numeric amounts.

The ingredient grounding is not fully aligned. `Marine agar 2216 (BD-Difco)` is a dehydrated complex product in the source, not the pure gelling agent agar, so grounding the whole 55.1 g/L product to `CHEBI:2509` over-narrows the ingredient. The already repaired TOGO M958 copy for the same JCM 913 source leaves Marine agar 2216 intentionally unmapped and marks it as a solidifying agent.

## Evidence

The inspected JCM 913 page lists:

| Ingredient | Amount |
|---|---|
| Marine agar 2216 (BD-Difco) | 55.1 g |
| Casitone (BD-Difco) | 1.0 g |
| Soytone (BD-Difco) or Phytone peptone (BD-BBL) | 1.0 g |
| Malt extract (BD-Difco) | 1.0 g |
| Distilled water | 1.0 L |

JCM instructs adjustment to pH 7.5 and applies its default sterilization instruction to autoclave at 121C for 15 min unless otherwise stated. TOGO M958 preserves the same ingredient list, the 1 L water row, the pH, and the original JCM source pointer. MediaDive J913 preserves the pH and the four non-water recipe rows, but its REST recipe omits the water row.

The generated MediaDive artifact additionally loses source detail by narrowing `Soytone (BD-Difco) or Phytone peptone (BD-BBL)` to `Soytone` and by dropping the BD-Difco/BD-BBL vendor text from all complex nutrients.

## Completeness

The recipe is materially incomplete because the generated MediaDive artifact has no 1 L water ingredient, does not retain the alternative Soytone/Phytone source wording, and omits JCM's default autoclave condition.

No target organism claims were expected from the inspected JCM, TOGO, or MediaDive medium records.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The generated record omits the source 1 L distilled-water row. | JCM 913 and TOGO M958 both list 1.0 L distilled water; the generated MediaDive record has no water ingredient. | Repair the MediaDive J913 normalized source or reconcile it with `data/normalized_yaml/bacterial/TOGO_M958_Modified_Marine_Agar_2216.yaml`, then regenerate `data/merge_yaml/merged/`. |
| major | The Marine agar 2216 product is incorrectly grounded to pure agar. | JCM and TOGO name 55.1 g Marine agar 2216 (BD-Difco), an undefined commercial complex mixture; the generated ingredient has `CHEBI:2509`/`agar`. | Remove the CHEBI grounding from the MediaDive J913 normalized ingredient and preserve the commercial product label as an intentionally unmapped complex nutrient. |
| major | The Soytone/Phytone alternative is narrowed to Soytone. | JCM and TOGO both allow `Soytone (BD-Difco) or Phytone peptone (BD-BBL)` at 1.0 g; the generated record only says `Soytone`. | Preserve the alternative source text in the MediaDive J913 normalized ingredient or model the two alternatives explicitly. |
| major | The same JCM 913 source is maintained twice with different CultureMech IDs. | TOGO M958 declares original `JCM_M913` for the same `Modified Marine Agar 2216`; the reviewed MediaDive source is `mediadive.medium:J913`; both exact same-slug records are under `data/normalized_yaml/bacterial/`. | Reconcile the repaired TOGO M958 record with the MediaDive J913 record so the merged output has one JCM 913 identity. |
| minor | The generated preparation omits JCM's default autoclave instruction. | The direct JCM page says to autoclave media at 121C for 15 min unless otherwise stated; the generated record only has `Adjust pH to 7.5.` | Add a source-backed autoclave step to the maintained JCM 913 record if the repository chooses to capture JCM page defaults. |

## Recommended Edits

1. Merge or cross-reconcile the MediaDive J913 and TOGO M958 sources, preferring the September 2026 repaired ingredient representation from `data/normalized_yaml/bacterial/TOGO_M958_Modified_Marine_Agar_2216.yaml`.
2. Add the 1 L distilled-water row to the J913 generated output or select the TOGO M958 row as the canonical JCM 913 record.
3. De-ground Marine agar 2216 from `CHEBI:2509` because the source ingredient is a commercial complex mixture rather than pure agar.
4. Preserve the Soytone-or-Phytone alternative wording, including the vendor qualifiers.
5. Add the JCM default autoclave instruction if page-level JCM default sterilization text is in curation scope.

## Follow-up Checks

- Rerun focused LinkML, strict, reference, and term validation on the regenerated JCM 913 YAML.
- Re-open the JCM 913 page, the TOGO M958 API record, and the MediaDive J913 REST payload and confirm the generated record keeps the 1 L water row and pH 7.5.
- Confirm the Marine agar 2216 row is not grounded to `CHEBI:2509`.
- Run an exact gitignore-independent search for `JCM_M913`, `mediadive.medium:J913`, and `TOGO:M958` under `data/normalized_yaml/` and verify they no longer generate duplicate CultureMech IDs for the same JCM 913 formulation.

## Additional Notes

- `just` validators were not used because project dependency resolution attempts to build `llvmlite==0.46.0` under Python 3.13; the focused validators were run with `/usr/local/bin/python3.11` and the offline review cache instead.
- JCM 1251 / TOGO M1345 is a second Modified Marine Agar 2216 source with a similar but not identical peptone row; it should stay distinguishable from JCM 913 when reconciling exact source accessions.
