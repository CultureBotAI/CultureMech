# YAML Record Review: GYPF With 2.5% NaCl

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gypf_with_2_5_nacl.yaml`
- Started UTC: 2026-09-23T08:35:00Z
- Finished UTC: 2026-09-23T08:35:54Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:010247`, `gypf_with_2_5_nacl`, imported from Togo `M833` / `JCM_M798`.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gypf_with_2_5_nacl.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The source identity is coherent: Togo `M833` wraps JCM `GRMD=798` and names `GYPF With 2.5% NaCl`. Exact ignored-file-inclusive search found the expected Togo parent/generated pair and a separate direct JCM/MediaDive `J798` parent/generated pair for the same formula.

NaCl, K2HPO4, Glucose, Agar, and water are grounded. Yeast extract and Bacto peptone are ungrounded complex components.

## Evidence

The Togo `M833` payload has one main solution with 1 L Distilled water, 25 g NaCl, 1 g K2HPO4, 10 g Glucose, 15 g Agar, 5 g Yeast extract, and 5 g Bacto peptone. It also records `ph: "8.5"` and repeats `Adjust pH to 8.5.` in comments. The direct JCM/MediaDive normalized parent carries the same non-water ingredient amounts and `ph_value: 8.5`.

The generated Togo record preserves the six non-water source ingredient values but omits pH 8.5 and emits the 1 L water row as 1 `G_PER_L`.

## Completeness

The mass ingredient list is materially complete. The recipe is incomplete for pH and source grouping because the Togo `M833` and direct JCM/MediaDive `J798` records are generated separately.

## Findings

- The generated record is missing pH 8.5 from Togo metadata and comments.
- Distilled water is misunitized as `1 G_PER_L` even though Togo records a 1 L solvent volume.
- The Togo `M833` source is not grouped with the equivalent direct JCM/MediaDive `J798` source.
- Yeast extract and Bacto peptone remain ungrounded. These are complex ingredients, but they should be checked against available mappings.

## Recommended Edits

- Preserve pH 8.5 from Togo.
- Correct Togo liter-water handling so 1 L Distilled water is not emitted as `1 G_PER_L`.
- Group Togo `M833` with the direct JCM/MediaDive `J798` record as the same JCM formula.
- Attempt complex-component groundings for Yeast extract and Bacto peptone.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Recompare the regenerated record against Togo `M833` and the direct JCM/MediaDive `J798` parent, verifying pH 8.5.

## Additional Notes

Fetching the embedded JCM `GRMD=798` URL during review returned a `Nothing found` page, so source verification relied on the Togo API payload and the existing direct JCM/MediaDive normalized record.
