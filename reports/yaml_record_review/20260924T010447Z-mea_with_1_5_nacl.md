# YAML Record Review: mea_with_1_5_nacl

- Repository: CultureMech
- Record: data/merge_yaml/merged/mea_with_1_5_nacl.yaml
- Started UTC: 2026-09-24T01:04:47Z
- Finished UTC: 2026-09-24T01:05:44Z
- Verdict: needs curation

## Target

Reviewed generated merged record `data/merge_yaml/merged/mea_with_1_5_nacl.yaml`, a duplicate merge of TOGO `M2068` and TOGO `M117`. Exact owner lookups with ignored files included found one `mea_with_1_5_nacl.yaml` owner and two same-named `malt_extract_agar_with_15_nacl.yaml` owners:

- `data/normalized_yaml/bacterial/mea_with_1_5_nacl.yaml`
- `data/normalized_yaml/bacterial/malt_extract_agar_with_15_nacl.yaml`
- `data/normalized_yaml/fungal/malt_extract_agar_with_15_nacl.yaml`

Exact source-ID scans with ignored files included found `TOGO:M2068` and `TOGO:M117` on the generated record, the bacterial owners, and their bacterial/recipe index entries. The fungal owner carries `mediadive.medium:J125` and has exact fungal/recipe index entries, but it is not part of this generated merge.

## Validation

- Open LinkML validation against `src/culturemech/schema/culturemech.yaml` as `MediaRecipe`: passed with `No issues found`.
- Strict validation with `scripts/validate_strict.py`: passed with 0 error rows; the strict TSV had a header only.
- LinkML reference validation: passed; 0 reference checks were emitted for this file.
- LinkML term validation with labels and `conf/oak_config.yaml`: passed.
- Embedded `curation_history`: Not checked. The repository `just validate-history` target validates standalone `history/` files rather than embedded generated-record history blocks.

## Identity and Grounding

The generated record is identified as TOGO medium M2068, `MEA with 1.5% NaCl`, from NBRC medium 1375. Live TOGO M2068 and the NBRC 1375 page agree that this formula has Malt extract 20 g, Glucose 20 g, Peptone 1 g, NaCl 15 g, water to 1 L, Agar 20 g, and pH 6.0.

The duplicate synonym is TOGO M117, `Malt Extract Agar With 15% NaCl`, from JCM medium 125. Live TOGO M117, the JCM page for GRMD 125, and MediaDive J125 agree that the JCM formula has the same Malt extract, Glucose, Peptone, and Agar amounts but NaCl 150 g.

## Evidence

The generated merge is not source-faithful to its primary M2068 identity. It carries `original_name: MEA with 1.5% NaCl` and `media_term.term.id: TOGO:M2068`, but the `NaCl` row is 150 g/L from the 15% JCM recipe instead of 15 g/L from the 1.5% NBRC recipe.

The two TOGO recipes differ ten-fold in NaCl and should not have been fingerprint-merged as duplicates. The true JCM 125/15% recipe is also already present as `data/normalized_yaml/fungal/malt_extract_agar_with_15_nacl.yaml` from `mediadive.medium:J125`, which exactly represents the five non-water JCM solutes.

## Completeness

NBRC 1375's pH 6.0 appears in live TOGO M2068 and in the NBRC page, but the generated record has no `ph_value`.

The TOGO-derived bacterial owners and generated record also preserve `Distilled water` as an ingredient at `1` g/L after converting `1 L` water into a mass concentration. The MediaDive J125 fungal owner omits that water row and is the cleaner representation of the JCM 125 formula.

## Findings

1. `needs curation` - TOGO M2068 and TOGO M117 are not duplicates: M2068/NBRC 1375 has 15 g/L NaCl, while M117/JCM 125 has 150 g/L NaCl.
2. `needs curation` - The generated M2068 record has the wrong 150 g/L `NaCl` value and is therefore ten-fold too salty for `MEA with 1.5% NaCl`.
3. `needs curation` - The equivalent JCM 125 MediaDive owner under `data/normalized_yaml/fungal/` was not merged with or deduplicated against the TOGO M117 bacterial owner.
4. `minor` - The M2068 pH 6.0 was dropped, `Distilled water` is retained as `1` g/L, and `Malt extract`/`Peptone` remain ungrounded.

## Recommended Edits

Split the generated M2068 and M117 records so `MEA with 1.5% NaCl` keeps NaCl 15 g/L and pH 6.0, while `Malt Extract Agar With 15% NaCl` keeps NaCl 150 g/L. Merge or otherwise deduplicate the TOGO M117 bacterial owner with the MediaDive J125 fungal owner instead of with NBRC M2068.

Remove the water pseudo-ingredient from TOGO-derived agar recipes when water only sets the final volume, and ground `Malt extract` and `Peptone` if stable terms are available.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validators on both regenerated merged records.
- Confirm exact `TOGO:M2068`, `TOGO:M117`, and `mediadive.medium:J125` source-index entries with ignored files included.
- Confirm M2068 has 15 g/L NaCl and JCM 125 has 150 g/L NaCl after regeneration.
- Confirm M2068 carries pH 6.0.

## Additional Notes

The NBRC 1375 page, TOGO M2068 API response, TOGO M117 API response, JCM GRMD 125 page, and MediaDive J125 REST/rendered pages were inspected directly.
