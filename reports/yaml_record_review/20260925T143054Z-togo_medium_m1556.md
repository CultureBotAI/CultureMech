# YAML Record Review: togo_medium_m1556

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1556.yaml
- Started UTC: 2026-09-25T14:28:00Z
- Finished UTC: 2026-09-25T14:30:54Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008104
- Name: togo_medium_m1556
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1556.yaml` and then regeneration of the generated merge.
- Source identity in the generated record: TOGO:M1556, original source NBRC_M357, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=357`.
- Merge lineage in the generated record: only `togo_medium_m1556`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1556.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1556.yaml --out /private/tmp/m1556.strict.tsv --workers 1 --quiet`; it reported zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1556.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it exited 0 with no diagnostics.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1556.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1556 resolves to NBRC_M357. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- The maintained normalized owner has a D-Alanine grounding to CHEBI:15570, but the generated record is stale and still has no term for that ingredient.

## Evidence

- TOGO M1556 and NBRC 357 support 10 g Hipolypepton, 2 g yeast extract, 1 g MgSO4 x 7 H2O, 50 mg D-Alanine, 15 g agar if needed, 1 L distilled water, and pH 7.0.
- The generated and normalized records store 50 D-Alanine as `G_PER_L`, a 1000x error relative to the 50 mg source amount.
- The NBRC asterisk on Hipolypepton is a Wako Pure Chemical supplier footnote. The generated record moved Hipolypepton into an empty solution, so the 10 g nutrient component is no longer in `ingredients`.
- The source pH 7.0 is absent.

## Completeness

- Consequential missing or wrong fields: 50 mg D-Alanine, Hipolypepton as a 10 g ingredient, the Wako supplier note, pH 7.0, optional agar scope, and regenerated D-Alanine grounding.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1556` found no existing report for this record stem.

## Findings

- major: `data/normalized_yaml/bacterial/togo_medium_m1556.yaml` stores the 50 mg D-Alanine source amount as 50 g/L.
- major: Hipolypepton is migrated into an empty solution even though its asterisk is only a supplier footnote.
- minor: the normalized owner omits the NBRC_M357 pH 7.0 value and treats optional agar as an unconditional solid physical state.
- minor: the generated record is stale relative to the August MIM grounding in the normalized owner; D-Alanine should resolve to CHEBI:15570 after regeneration.

## Recommended Edits

- Correct D-Alanine to the source 50 mg amount or its exact per-liter equivalent.
- Move Hipolypepton back to `ingredients` at 10 g and preserve the Wako Pure Chemical supplier note as a note.
- Add pH 7.0 and preserve optional agar scope.
- Regenerate `data/merge_yaml/merged/` so the D-Alanine CHEBI grounding added to the normalized owner appears in the generated record.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1556.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1556.yaml` and verify that D-Alanine is 50 mg and grounded, Hipolypepton is not an unknown solution, and pH 7.0 appears.

## Additional Notes

- Exact source inspection was bounded to TOGO M1556 and NBRC 357.
