# YAML Record Review: Beer Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/Beer_Medium.yaml
- Started UTC: 2026-09-21T19:33:58Z
- Finished UTC: 2026-09-21T19:36:33Z
- Verdict: needs curation

## Target

- Reviewed `data/merge_yaml/merged/Beer_Medium.yaml`.
- Class: `MediaRecipe`.
- ID: `CultureMech:009811`.
- Name: `beer_medium`.
- Original name: `Beer Medium`.
- Source identity: `TOGO:M426`, label `Beer Medium`; TOGO metadata says `original_media_id: JCM_M426` and links JCM GRMD 426.
- Generated status: derived merge output under `data/merge_yaml/merged/`; the maintained owner is `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml`.
- Merge metadata: one source record, `TOGO_M426_Beer_Medium`, on fingerprint `859ff6f2aaa2f2961f309c7d684bc5a1a9a0bd485883d50814cf96600ca386f7`.
- Sibling checked for disambiguation: `data/normalized_yaml/bacterial/beer_medium.yaml` is a distinct `CultureMech:002778` JCM Beer Medium record that later recovered composition from TOGO M426 and has different unit and pH values. It is not the owner of this generated TOGO record.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/Beer_Medium.yaml` via the no-project Python 3.11 workaround | Passed |
| Closed-schema strict validation, `scripts/validate_strict.py data/merge_yaml/merged/Beer_Medium.yaml --out /private/tmp/Beer_Medium.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/Beer_Medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks were applicable |
| `linkml-term-validator validate-data data/merge_yaml/merged/Beer_Medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not a focused embedded-history validator for one merged record |

The documented `just` commands were not run directly because the project runtime currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation.

## Identity and Grounding

- `CultureMech:009811` resolves in `data/culturemech_id_registry.tsv` to `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml`.
- TOGO M426, JCM GRMD 426, the record name, and the `TOGO:M426` media term all denote Beer Medium from JCM.
- JCM GRMD 426 defines the recipe as MRS medium prepared in normally hopped beer instead of distilled water and adjusted to pH 5.2. TOGO expands the linked JCM MRS Medium 1 table into 10 MRS solute rows plus the 1 L hopped-beer replacement.
- The generated record correctly preserves the 10 source-supported MRS solute labels and amounts except for the manganese sulfate hydrate unit, but it misrepresents the 1 L hopped-beer solvent as `1` `G_PER_L` and drops JCM's pH 5.2 preparation claim.

## Evidence

- TOGO M426 carries the same `src_url` as the record notes, `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=426`, and reports pH `5.2`.
- The inspected JCM GRMD 426 page says to prepare MRS medium in normally hopped beer instead of distilled water and adjust to pH 5.2.
- The inspected JCM GRMD 1 page supports the MRS base quantities that TOGO expands into M426: 10 g casein peptone, 10 g beef extract, 5 g yeast extract, 20 g glucose, 1 g Tween 80, 2 g K2HPO4, 5 g sodium acetate, 2 g diammonium citrate, 0.2 g magnesium sulfate heptahydrate, 50 mg manganese sulfate hydrate, and 1 L solvent.
- TOGO M426 agrees that the manganese sulfate hydrate item is `50` `mg`; the current generated record says `50` `G_PER_L`, a 1000-fold unit error.
- TOGO M426 agrees that Beer Medium uses `1` `L` hopped beer. The generated record says `1` `G_PER_L`, which changes the solvent replacement into a gram quantity.
- A gitignore-independent search over `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md` for `CultureMech:009811`, `TOGO:M426`, `TOGO_M426_Beer_Medium`, `JCM_M426`, the exact JCM URL token, `beer_medium`, and `Beer Medium` found the TOGO owner, the generated merge, generated indexes, import-tracking reports, and the distinct `CultureMech:002778` JCM sibling. It did not find a raw TOGO or JCM capture for the reviewed TOGO M426 record.

## Completeness

- Consequentially incomplete: `ph_value: 5.2` is present in TOGO and JCM but absent from the reviewed generated record and its normalized TOGO owner.
- Consequentially incomplete: the source instruction to prepare MRS in hopped beer instead of water and adjust pH to 5.2 is absent from `preparation_steps`, even though TOGO exposes it in the M426 comments.
- Optional target-organism, growth-evidence, variant, storage, and external DOI/PMID slots are empty. That is acceptable for this record because the inspected TOGO and JCM recipe sources do not name a strain, report a growth outcome, define a storage condition, or cite a paper.
- No duplicate merge was expected for `data/merge_yaml/merged/Beer_Medium.yaml`; the existing `CultureMech:002778` `beer_medium.yaml` sibling has the same normalized name but a different source accession and is not merged into this TOGO record.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | The manganese sulfate hydrate row has the wrong unit: TOGO and JCM Medium 1 say 50 mg, but the YAML says `50` `G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml` |
| Major | The hopped-beer solvent row has the wrong unit: TOGO M426 says 1 L, while the YAML says `1` `G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml` |
| Major | The TOGO/JCM pH 5.2 value and preparation instruction were not imported into `ph_value` or `preparation_steps`. | `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M426_Beer_Medium.yaml`, change the manganese sulfate hydrate row from `50` `G_PER_L` to `50` `MG_PER_L`.
2. Change the `hopped beer` solvent row from `1` `G_PER_L` to a volume-preserving `1` `L` representation, matching TOGO M426 and the source instruction to use beer instead of distilled water.
3. Add `ph_value: 5.2`.
4. Add a preparation step that preserves TOGO/JCM's instruction to prepare MRS medium in normally hopped beer instead of distilled water and adjust pH to 5.2.
5. Consider whether the reviewed TOGO M426 record and the recovered JCM `CultureMech:002778` sibling should share the same curated corrections or be merged after their source-identity boundaries are reviewed.
6. Regenerate `data/merge_yaml/merged/Beer_Medium.yaml`.

## Follow-up Checks

- Rerun the open schema validator, `scripts/validate_strict.py`, `linkml-term-validator`, and `linkml-reference-validator` on the normalized TOGO owner and the regenerated merged record.
- Rerun the repository merge verifier documented for merge-input changes to prove `data/merge_yaml/merged/Beer_Medium.yaml` is derived from the corrected normalized YAML.
- Manually compare the regenerated record with TOGO M426 and JCM GRMD 426 to verify pH 5.2, 50 mg manganese sulfate hydrate, and 1 L hopped beer survive regeneration.

## Additional Notes

- TOGO M426 and the repaired `CultureMech:002778` JCM sibling already agree on the two unit fixes and the pH/preparation recovery. The correction does not require inferring new chemistry beyond the inspected M426 payload.
- JCM GRMD 426 is an instruction over MRS Medium 1 rather than a full table. TOGO M426 expands MRS Medium 1 and replaces the 1 L distilled-water solvent with 1 L hopped beer, which is consistent with the JCM text.
- Reports are read-only review artifacts; no normalized recipe, generated merge, page, history entry, or GitHub issue was edited for this review.
