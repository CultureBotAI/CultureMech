# YAML Record Review: CY Agar

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cy_agar__a735039c.yaml`
- Started UTC: 2026-09-22T12:09:43Z
- Finished UTC: 2026-09-22T12:09:43Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:008123` / `cy_agar`, a one-source generated merge from `data/normalized_yaml/bacterial/TOGO_M1574_CY_Agar.yaml`.

## Validation

- Open schema validation passed for `MediaRecipe`.
- Strict validation passed and wrote `/private/tmp/cy_agar__a735039c.strict.tsv`.
- LinkML reference validation passed with zero checks.
- LinkML term validation passed.
- Embedded `curation_history` was not separately checked because the available `validate-history` target validates standalone files under `history/`, not history entries embedded in generated `MediaRecipe` YAML.

## Identity and Grounding

The source identity is NBRC Medium 378, `CY Agar`, imported through TogoMedium M1574. Its live NBRC page lists Bacto Casitone (Difco) 3 g/L, CaCl2 x 2 H2O 1.36 g/L, yeast extract 1 g/L, agar 15 g/L, distilled water 1 L, and pH 7.2.

The generated record carries the correct non-water ingredients and amounts, but the water row is normalized as `1 G_PER_L` and the pH 7.2 comment was dropped.

## Evidence

Primary source checks:

- TogoMedium M1574 was fetched to `/private/tmp/togo_M1574.json`; it names `NBRC_M378`, points to the NBRC 378 URL, and carries the `pH 7.2` source comment.
- `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=378` was fetched to `/private/tmp/nbrc_378.html`; its composition table lists the same five rows plus pH 7.2.

Local record checks:

- `data/merge_yaml/merged/CY_Agar.yaml` already merges the direct DSMZ Medium 67 owner, KOMODO Medium 67, and a KOMODO Medium 67 DSM 21789 source duplicate with the same Casitone/CaCl2/yeast extract/agar formula and pH 7.2.
- `data/merge_yaml/merged/cy_agar__2b917782.yaml` is another CY-Agar split duplicate from TOGO M2335 / DSMZ Medium 67.
- `data/merge_yaml/merged/cy_agar__a735039c.yaml` is the NBRC M1574 split duplicate.

## Completeness

The record has the complete NBRC non-water formula. It is incomplete because water is represented with a mass unit, pH 7.2 is absent, and the NBRC CY Agar source remains outside the existing CY-Agar duplicate cluster.

## Findings

1. Needs curation: NBRC M1574 is a split generated duplicate of CY-Agar.

   NBRC Medium 378 has the same formula and pH as the DSMZ Medium 67 CY-Agar group that is already generated as `CY_Agar.yaml`. M1574 should be normalized into that source-duplicate cluster or otherwise explicitly linked to the same canonical CY-Agar recipe.

2. Needs curation: distilled water is encoded as `1 G_PER_L`.

   NBRC 378 lists one liter of distilled water. The generated record preserved the numeric `1` but used grams per liter, which is not physically meaningful for the water solvent row.

3. Needs curation: pH 7.2 was dropped.

   Both the NBRC page and the TogoMedium M1574 structured record include pH 7.2, but neither the normalized owner nor the generated singleton carries `ph_value` or a pH adjustment step.

## Recommended Edits

- Convert the TOGO M1574 distilled-water row to 1000 mL/L or drop it if the canonical CY-Agar record remains non-water only.
- Add pH 7.2 to `TOGO_M1574_CY_Agar.yaml`.
- Link NBRC M1574 into the canonical CY-Agar source-duplicate cluster with direct DSMZ 67, KOMODO 67, the KOMODO DSM 21789 copy, and TOGO M2335.
- Regenerate merge output and confirm `TOGO_M1574_CY_Agar` no longer emits `cy_agar__a735039c.yaml`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation for regenerated CY Agar.
- Search generated output for `cy_agar__a735039c`, `TOGO_M1574_CY_Agar`, and `M1574` with ignored files included and confirm no NBRC singleton remains.
- Confirm the canonical generated CY-Agar keeps pH 7.2 and no invalid `1 G_PER_L` water row.

## Additional Notes

The local CY-Agar searches used `rg --no-ignore --hidden`, so ignored report files and generated outputs were included.
