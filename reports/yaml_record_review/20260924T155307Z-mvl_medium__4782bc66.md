# YAML Record Review: mvl_medium__4782bc66

- Repository: CultureMech
- Record: data/merge_yaml/merged/mvl_medium__4782bc66.yaml
- Started UTC: 2026-09-24T15:53:07Z
- Finished UTC: 2026-09-24T15:53:28Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mvl_medium__4782bc66.yaml`.

The generated record represents the direct MediaDive/JCM owner for JCM Medium
740:

- canonical `id`: `CultureMech:003083`
- canonical `media_term`: `mediadive.medium:J740`
- canonical `name`: `mvl_medium`
- canonical `original_name`: `MVL MEDIUM`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `SOLID_AGAR`
- `ingredients`: 14
- `solutions`: 0
- `merged_from`: `mvl_medium`

## Validation

- Open schema validation: Passed with `No issues found`.
- Strict schema validation: Passed with 0 errors; the TSV contained only the
  header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked; `just validate-history` validates
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The high-level JCM 740 identity is correct, but the generated record is stale.
The repaired `data/normalized_yaml/bacterial/mvl_medium.yaml` owner has a
2026-09-11 curation action that nests Mineral solution 1, Mineral solution 2,
0.1% Resazurin, 0.07% Hemin, 8% Na2CO3, and 3% L-Cysteine HCl x H2O as stock
additions and links `data/normalized_yaml/bacterial/TOGO_M765_MVL_Medium.yaml`
as the TOGO M765 source duplicate. This generated record still reflects the
older 2026-08-06 merge.

An exact ignored-file-inclusive search for `CultureMech:003083`, `GRMD=740`,
`TOGO:M765`, and `CultureMech:010172` found two active normalized owners for
JCM 740:

- `CultureMech:003083`:
  `data/normalized_yaml/bacterial/mvl_medium.yaml`
- `CultureMech:010172`:
  `data/normalized_yaml/bacterial/TOGO_M765_MVL_Medium.yaml`

Those active owners still generate two different stale records. The TOGO M765
record is present as `data/merge_yaml/merged/MVL_MEDIUM.yaml`, which sorted
earlier and was not reviewed here.

## Evidence

The current JCM 740 page lists MVL Medium as 75 ml Mineral solution 1, 75 ml
Mineral solution 2, 1 ml 0.1% Resazurin solution, 10 g Trypticase peptone, 5 g
Yeast extract, 2 g Beef extract, 15 g Agar, 2 g Glucose, 10 ml 0.07% Hemin
solution, 50 ml 8.0% Na2CO3 solution, 10 ml 3.0% L-Cysteine HCl x H2O
solution, and 1.0 L distilled water. It then defines Mineral solution 1 as
0.6% KH2PO4 solution and Mineral solution 2 as its own 100 ml stock containing
K2HPO4, ammonium sulfate, NaCl, MgSO4 x 7 H2O, CaCl2 x 2 H2O, and distilled
water.

The current MediaDive REST record for `J740` mirrors that structure as a
1221 ml main solution with `Mineral solution 1` and `Mineral solution 2`
referenced by volume, not flattened into top-level final salts. The current
TOGO M765 API also points to JCM 740 and carries the same main recipe and two
mineral-solution sections.

## Completeness

The generated record is incomplete because it has no `solutions` entries and no
main-solution distilled water. Its only preparation step is the Mineral
solution 1 comment, `0.6% KH2PO4 solution.`, rather than a preparation of the
main medium.

The top-level `Trypticase peptone`, `Yeast extract`, `Beef extract`, `Agar`,
and `Glucose` quantities match the 1221 ml MediaDive denominator, but every
stock entry is misrepresented as a top-level final ingredient. For example,
the generated record lists `Resazurin` at 1 g/L, `Hemin` at 10 g/L, `Na2CO3`
at 50 g/L, and `L-Cysteine HCl x H2O` at 10 g/L instead of retaining the 1 ml,
10 ml, 50 ml, and 10 ml stock additions to the 1221 ml main solution.

The Mineral solution 2 components have the same issue: `K2HPO4` at 6 g/L,
`(NH4)2SO4` and `NaCl` at 12 g/L, and the two divalent-cation salts at 1.2 g/L
are stock strengths from 100 ml Mineral solution 2, not final concentrations in
MVL Medium.

## Findings

- The generated JCM 740 record is stale relative to the maintained direct JCM
  owner repaired on 2026-09-11.
- The main 1.0 L `Distilled water` ingredient is missing from the generated
  record.
- Six JCM stock additions were flattened or partially lost instead of being
  represented as nested `solutions`.
- Resazurin, hemin, Na2CO3, L-Cysteine HCl x H2O, and every Mineral solution 2
  salt are listed at stock quantities or stock concentrations as if they were
  final g/L main-medium ingredients.
- The generated record does not preserve the linked TOGO M765
  `SOURCE_DUPLICATE`; TOGO M765 still appears as a separate stale generated
  record.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/mvl_medium__4782bc66.yaml` from the
  repaired `data/normalized_yaml/bacterial/mvl_medium.yaml` owner.
- Confirm TOGO M765 is regenerated as a `SOURCE_DUPLICATE` of the JCM/MediaDive
  owner rather than as an independent `MVL_MEDIUM` generated record.
- Preserve Mineral solution 1, Mineral solution 2, 0.1% Resazurin solution,
  0.07% Hemin solution, 8% Na2CO3 solution, and 3% L-Cysteine HCl x H2O
  solution as stock additions if the generator can carry nested solutions.
- If any stock cannot be represented natively, scale it by the 1221 ml main
  recipe volume rather than copying stock strengths into top-level final
  ingredients.
- Preserve the 819.001 ml/L distilled-water contribution restored in the
  maintained normalized owner.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the
  regenerated YAML.
- Confirm the regenerated YAML contains nested Mineral solution 1 and Mineral
  solution 2 entries.
- Confirm `Resazurin`, `Hemin`, `Na2CO3`, and `L-Cysteine HCl x H2O` are no
  longer emitted as top-level g/L ingredients.
- Confirm an ignored-file-inclusive exact search for `TOGO:M765` and
  `GRMD=740` shows one generated MVL Medium record after regeneration.

## Additional Notes

The source URLs were reachable during review and agreed on the same MVL Medium
recipe. The data defect is local staleness plus stock flattening, not a
provider disagreement.
