# YAML Record Review: togo_medium_m1512

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1512.yaml
- Started UTC: 2026-09-25T14:13:00Z
- Finished UTC: 2026-09-25T14:17:58Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008057 |
| Label | togo_medium_m1512 |
| Generated record | data/merge_yaml/merged/togo_medium_m1512.yaml |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m1512.yaml |
| Source | TOGO Medium M1512, NBRC M297 |

The reviewed file is a stale generated one-source merge of TOGO M1512. The
normalized owner has already been repaired; future work should regenerate the
merged output rather than hand-edit this generated YAML.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/togo_medium_m1512.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; 0 checks |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected `eutils/pkg_resources is
deprecated` warning before passing.

## Identity and Grounding

`CultureMech:008057`, `TOGO:M1512`, the `NBRC_M297` original source note, and
the NBRC medium 297 page all identify the same pH 7.0 NBRC medium with 1 g
beef extract, 2 g yeast extract, 5 g peptone, 5 g NaCl, 1 L distilled water,
and 15 g optional agar.

The generated record has the right non-water ingredient amounts but predates
the September 12 normalized-owner repair that added pH 7.0, converted water
from 1 g/L to 1 L, grounded yeast extract, peptone, water, and agar, added
source references, and preserved source-scoped preparation steps.

## Evidence

The generated YAML still has no `ph_value`, no preparation steps, no
references, a 1 g/L water row, and ungrounded yeast extract, peptone, beef
extract, and optional agar. `data/normalized_yaml/bacterial/togo_medium_m1512.yaml`
has already fixed the first three groundable rows, retained beef extract as a
sourced unmapped component, kept optional agar as a source row with
`SOLIDIFYING_AGENT`, and added TOGO/NBRC references.

## Completeness

The absent `target_organisms`, growth evidence, stock solutions, and variants
are not defects for this imported source record. The generated layer is
consequentially incomplete only because it is stale relative to its repaired
normalized owner.

Before this report was written, an exact `find` search under the ignored
`reports/yaml_record_review` directory found no pre-existing
`*-togo_medium_m1512.md` report.

## Findings

| Severity | Finding |
| --- | --- |
| major | The generated merge is stale relative to the repaired normalized owner for pH, water volume, ingredient grounding, preparation steps, and references. Owner: generated `data/merge_yaml/merged/togo_medium_m1512.yaml`; regenerate from `data/normalized_yaml/bacterial/togo_medium_m1512.yaml`. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/togo_medium_m1512.yaml` from the repaired
   normalized owner.
2. Confirm the regenerated record keeps `ph_value: 7.0`, 1 L distilled water,
   source-scoped preparation steps, TOGO/NBRC references, and the existing
   unresolved beef-extract note.

## Follow-up Checks

Run the focused schema, strict, reference, and term validators on the
regenerated record, then manually compare it with TOGO M1512, NBRC 297, and
`data/normalized_yaml/bacterial/togo_medium_m1512.yaml` for pH 7.0, final
water, optional agar, and structured references.

## Additional Notes

An exact gitignore-independent source search covered `data/import_tracking`,
`data/normalized_yaml/bacterial`, `data/merge_yaml/merged`, and ignored
`reports/yaml_record_review`; it found the expected normalized owner,
generated merge, and import-tracking rows for `CultureMech:008057` /
`TOGO:M1512`.
