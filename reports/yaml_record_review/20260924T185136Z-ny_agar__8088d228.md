# YAML Record Review: ny_agar__8088d228

- Repository: CultureMech
- Record: `data/merge_yaml/merged/ny_agar__8088d228.yaml`
- Started UTC: 2026-09-24T18:51:36Z
- Finished UTC: 2026-09-24T18:52:06Z
- Verdict: needs curation

## Target

Generated bacterial record `CultureMech:004854`, `ny_agar__8088d228`, merging KOMODO Medium 306 with the direct DSMZ/MediaDive 306 record.

## Validation

Open LinkML validation passed with `No issues found`.

Strict validation passed; `/private/tmp/ny_agar__8088d228.strict.tsv` contained only the header row.

Reference validation passed with 0 checks.

Term validation passed.

Embedded `curation_history` was not checked: the history validator operates over standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The KOMODO and direct DSMZ parents are correctly recognized as source duplicates. The KOMODO record explicitly cites DSMZ Medium 306, the direct parent is `mediadive.medium:306`, the local ingredient and pH signatures match, and an existing source-duplicate audit also records this pair as `SOURCE_DUPLICATE`.

An exact ignored-inclusive, hidden-inclusive search for `KOMODO_306_NY-AGAR`, `komodo.medium:306`, `mediadive.medium:306`, `DSMZ Medium 306`, `ny_agar`, and `NY-AGAR` found the expected KOMODO and MediaDive 306 records and also found other JCM/TOGO NY Agar records that share the `ny_agar` slug but are separate source lineages.

## Evidence

MediaDive 306 is DSMZ `NY-AGAR`, pH 7.0, with Peptone 5 g/L, Meat extract 3 g/L, Agar 15 g/L if necessary, Yeast extract 0.5%, and Distilled water 1000 ml.

The generated merge preserves the Peptone, Meat extract, Agar, Yeast extract, and pH values from DSMZ 306, so the duplicate merge is based on the intended formula.

MediaDive 12 is DSMZ `SOIL EXTRACT MEDIUM`, with air-dried garden soil, tap water, and agar; it is unrelated to DSMZ Medium 306 NY-AGAR.

## Completeness

Both normalized parents omit the 1000 ml Distilled water row from DSMZ Medium 306.

The generated record is stale relative to both parents for exact `Peptone` and `Yeast extract` ontology grounding, and it also dropped the pH-adjustment/Bacillus sporulation note from the direct DSMZ parent.

## Findings

Needs curation:

- `Distilled water 1000 ml` is missing.
- `kg_microbe_match: mediadive.medium:12` points to DSMZ Soil Extract Medium, not DSMZ NY-AGAR.
- The generated YAML is stale relative to both parents for exact `Peptone` and `Yeast extract` ontology grounding.
- The generated YAML lost the DSMZ preparation step noting pH 7.0 adjustment and 10.0 mg MnSO4 x H2O for Bacillus sporulation.

## Recommended Edits

Repair both normalized parents before merge regeneration:

- Add `Distilled water` at `1000 ML_PER_L`.
- Remove or recompute `kg_microbe_match: mediadive.medium:12`.
- Preserve the DSMZ pH-adjustment/Bacillus sporulation preparation note in the canonical generated record.
- Keep the KOMODO parent as a `SOURCE_DUPLICATE` of the direct `mediadive.medium:306` parent.

Regenerate `data/merge_yaml/merged/ny_agar__8088d228.yaml` from the repaired normalized records instead of hand-editing the generated artifact.

## Follow-up Checks

After repair, rerun open schema, strict, reference, and term validation for this generated YAML. Re-fetch MediaDive 306 and confirm the regenerated formula includes 1000 ml/L water, keeps Yeast extract at 5 g/L, keeps Agar conditional on need, keeps pH 7.0, and no longer contains `mediadive.medium:12`.

## Additional Notes

None found.
