# YAML Record Review: CHM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/chm__38969ffa.yaml
- Started UTC: 2026-09-22T07:23:51Z
- Finished UTC: 2026-09-22T07:24:21Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000331 |
| Label | CHM |
| Generated record | data/merge_yaml/merged/chm__38969ffa.yaml |
| Maintained owner | data/normalized_yaml/bacterial/chm.yaml |
| Source | CCAP CHM through `mediadive.medium:C23` |

The reviewed file is a generated one-source merge of the direct MediaDive/CCAP
`chm` normalized record. Future fixes belong in
`data/normalized_yaml/bacterial/chm.yaml` for remaining source or ontology
issues, and stale generated output should be handled by regenerating
`data/merge_yaml/merged/chm__38969ffa.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/chm__38969ffa.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; 0 checks |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected
`eutils/pkg_resources is deprecated` warning before passing.

## Identity and Grounding

The core record identity is correct: `CultureMech:000331`, `CHM`, the cited
CCAP `MR_CHM.pdf` source, and the generated `chm` merge all denote CCAP CHM,
or Chilomonas Medium.

An ignored-independent `rg --no-ignore --hidden` search across
`data/normalized_yaml` and `data/merge_yaml/merged` for the CultureMech ID,
`mediadive.medium:C23`, the C23 import note, and the merge fingerprint found
only this generated record, its maintained owner, and normalized index entries.

## Evidence

The CCAP `MR_CHM.pdf` source supports a simple per-litre formula: 1.0 g sodium
acetate trihydrate, 1.0 g Lab-Lemco powder, and 1 litre deionised water,
followed by autoclaving at 15 psi for 15 minutes.

The generated record predates the repaired maintained owner. Generated
`chm__38969ffa.yaml` lacks deionised water, `references`, `data_quality_flags`,
and the September 2026 repair curation event now present in
`data/normalized_yaml/bacterial/chm.yaml`. It also still combines mixing and
autoclaving into a single `AUTOCLAVE` step, whereas the maintained owner now
separates them into `MIX` and `AUTOCLAVE` steps.

The remaining owner-level ingredient defect is ontology grounding:
`Sodium acetate x 3 H2O` denotes sodium acetate trihydrate, but its primary
term is the generic `CHEBI:32954` sodium acetate rather than
`CHEBI:32138` sodium acetate trihydrate.

## Completeness

The empty `target_organisms` and growth-observation fields are not defects for
this source record.

The generated record is incomplete relative to both its maintained owner and
`MR_CHM.pdf` until it is regenerated from the repaired normalized YAML. After
regeneration, the specific sodium acetate trihydrate ontology mapping should
still be corrected in the maintained owner.

## Findings

| Severity | Finding |
| --- | --- |
| Major | Generated `chm__38969ffa.yaml` is stale and lacks the deionised water, references, data-quality flags, and split preparation steps already present in `data/normalized_yaml/bacterial/chm.yaml`. **Owner:** generated merge output; regenerate after verifying the current normalized owner. |
| Major | Sodium acetate trihydrate is grounded to generic sodium acetate. **Owner:** `data/normalized_yaml/bacterial/chm.yaml`. |
| Minor | The generated record has `category: bacterial`, which is not supported by `MR_CHM.pdf`; the source identifies CHM as a Freshwater Protozoa medium from CCAP. **Owner:** `data/normalized_yaml/bacterial/chm.yaml` or the direct CCAP/MediaDive import categorization. |

## Recommended Edits

1. Regenerate merged YAML so the generated CHM record catches up with
   `data/normalized_yaml/bacterial/chm.yaml`.
2. Change the `Sodium acetate x 3 H2O` primary term to CHEBI sodium acetate
   trihydrate and refresh secondary ingredient links consistently.
3. Reconsider the bacterial category for a CCAP Freshwater Protozoa medium, or
   document why the direct MediaDive import must stay in the bacterial bucket.
4. Recompare the regenerated output against `MR_CHM.pdf` and the separate CCAP
   algae `CHM.yaml` import so the two CHM records remain source-consistent.

## Follow-up Checks

Run the narrow generated-record validators after regeneration:

1. Open LinkML validation against `MediaRecipe`.
2. `scripts/validate_strict.py` on the regenerated record.
3. `linkml-reference-validator validate data` on the regenerated record.
4. `linkml-term-validator validate-data` on the regenerated record.

Then manually confirm that the regenerated record includes 1.0 g/L sodium
acetate trihydrate, 1.0 g/L Lab-Lemco powder, 1 litre deionised water,
autoclaving at 15 psi for 15 minutes, the CCAP PDF reference, and the corrected
trihydrate ontology term.

## Additional Notes

The all-caps generated `CHM.yaml` from `data/normalized_yaml/algae/chm.yaml`
was reviewed separately because it has a different source path, a different
CultureMech ID, and a different merge fingerprint.
