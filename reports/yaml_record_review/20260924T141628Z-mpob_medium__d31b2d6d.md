# YAML Record Review: mpob_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml
- Started UTC: 2026-09-24T14:14:14Z
- Finished UTC: 2026-09-24T14:16:28Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:003337`
- Label: `mpob_medium`
- Original label: `MPOB MEDIUM`
- Category: `bacterial`
- Media source: MediaDive/JCM `J988`
- Generated status: generated merge output from one normalized source on fingerprint `d31b2d6d95771adae04d08573c023f35458c93740d113bbd5ffef6021f73bd38`
- Maintained owner: `data/normalized_yaml/bacterial/mpob_medium.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml`
  - Result: passed with `No issues found`.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml --out /private/tmp/mpob_medium__d31b2d6d.strict.tsv --workers 1 --quiet`
  - Result: passed with 0 errors; `/private/tmp/mpob_medium__d31b2d6d.strict.tsv` contained only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 0 reference checks were evaluated.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record correctly denotes JCM medium 988, `MPOB MEDIUM`.

The direct JCM GRMD 988 page, the live MediaDive `J988` JSON, and TOGO `M1041` all resolve to the same JCM source:

| Source | ID | Label | pH |
| --- | --- | --- | --- |
| JCM | `988` | `MPOB MEDIUM` | `7.0 - 7.2` |
| MediaDive | `J988` | `MPOB MEDIUM` | `7.1` |
| TOGO | `M1041`, original `JCM_M988` | `MPOB Medium` | `7.0 - 7.2` |

An exact gitignore-independent search of `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:003337`, bounded `mediadive.medium:J988`, bounded `ID: J988`, and `GRMD=988` found only this MediaDive record, its normalized owner, and the separate TOGO `M1041` normalized/generated pair for the same JCM page. Ignored files were included.

Most main-solution CHEBI terms match the source hydrate state. `NiCl2 x 6 H2O` is an exception: the preferred term says hexahydrate, but its CHEBI grounding is `CHEBI:34887` for nickel dichloride, not a hexahydrate term.

## Evidence

JCM GRMD 988 defines the main MPOB medium and calls for adding three subordinate stocks:

| JCM GRMD 988 row | Amount |
| --- | ---: |
| `Trace element solution SL-10` | 1.0 ml |
| `Selenite-tungstate solution` | 1.0 ml |
| `Trace vitamins` | 10.0 ml |

MediaDive expands those to JCM solution IDs `4178`, `777`, and `3861`, each made to `1000 ml`. CultureMech flattened the stock-strength contents of all three into the top-level `ingredients` list:

| Stock | Addition to MPOB | Example stock row in MediaDive | CultureMech top-level row | Status |
| --- | ---: | --- | --- | --- |
| `Trace element solution SL-10` | 1 ml | `FeCl2 x 4 H2O`, `1.5 g` per `1000 ml` | `FeCl2 x 4 H2O`, `1.5 G_PER_L` | stock strength, not final |
| `Selenite-tungstate solution` | 1 ml | `NaOH`, `0.5 g` per `1000 ml` | `NaOH`, `0.5 G_PER_L` | stock strength, not final |
| `Trace vitamins` | 10 ml | `Biotin`, `2 mg` per `1000 ml` | `Biotin`, `0.002 G_PER_L` | stock strength, not final |

The six basal salts, yeast extract, resazurin, NaHCO3, glycerol, and Na2S x 9 H2O rows match the MediaDive final grams-per-liter conversion over the `1012 ml` modeled final volume.

The preparation text is broadly supported by JCM GRMD 988: it excludes trace vitamins, NaHCO3, and Na2S x 9H2O from the initial mix, adds bicarbonate and sulfide as separately sterilized solutions under N2-CO2, adds trace vitamins by filter sterilization, checks final pH 7.0-7.2, and pressurizes inoculated bottles to N2-CO2 at 4:1.

## Completeness

The record is not complete enough for a stock-aware recipe:

- It has no structured representation for `Trace element solution SL-10`, `Selenite-tungstate solution`, or `Trace vitamins`.
- It drops the JCM references to the stock source pages that were inspected for this review: GRMD 433, 554, and 197.
- It drops the `BD-Difco` supplier attribute from the yeast extract row.

The pH range is reduced to `ph_value: 7.1`, which is the midpoint of the JCM `7.0 - 7.2` range. That is a schema limitation rather than a distinct curation error in this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | Three external JCM stock additions are flattened as final stock-strength ingredients. | JCM GRMD 988 adds 1 ml SL-10, 1 ml selenite-tungstate solution, and 10 ml trace vitamins; the reviewed YAML instead puts every constituent from those 1000 ml stocks at top level with the stock's own grams-per-liter values. | `data/normalized_yaml/bacterial/mpob_medium.yaml` |
| Major | A direct JCM duplicate is still present as a separate TOGO-derived generated record. | TOGO `M1041` has `original_media_id: JCM_M988` and the same GRMD 988 `src_url`; the exact ignored-inclusive search found `data/normalized_yaml/bacterial/TOGO_M1041_MPOB_Medium.yaml` and generated `MPOB_MEDIUM.yaml`. | `data/normalized_yaml/bacterial/mpob_medium.yaml`; `data/normalized_yaml/bacterial/TOGO_M1041_MPOB_Medium.yaml`; merge de-duplication logic |
| Major | `NiCl2 x 6 H2O` is grounded to an anhydrous nickel chloride CHEBI term. | The source and preferred term both name nickel chloride hexahydrate, but `term` and `mediaingredientmech_chebi_term` use `CHEBI:34887` / `nickel dichloride`. | `data/normalized_yaml/bacterial/mpob_medium.yaml` |
| Minor | The exact yeast extract supplier is lost. | JCM GRMD 988 and MediaDive identify the row as `Yeast extract (BD-Difco)`; CultureMech stores only `Yeast extract`. | `data/normalized_yaml/bacterial/mpob_medium.yaml` |
| Minor | The `media_term` CURIE uses a MediaDive-local identifier for a JCM source. | The direct provider is JCM GRMD 988; `mediadive.medium:J988` is a MediaDive ID for that JCM page rather than the JCM identifier itself. | `data/normalized_yaml/bacterial/mpob_medium.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/mpob_medium.yaml`, remodel the three external stocks as referenced or nested solutions and keep only the `1 ml`, `1 ml`, and `10 ml` additions in the main MPOB recipe.
2. Carry through the JCM stock provenance for GRMD 433, 554, and 197 so the SL-10, selenite-tungstate, and trace-vitamin contents are not presented as GRMD 988 top-level final concentrations.
3. Add a cross-source duplicate mapping so MediaDive `J988` and TOGO `M1041` / `JCM_M988` collapse to one maintained JCM GRMD 988 recipe.
4. Re-ground `NiCl2 x 6 H2O` to a supported nickel chloride hexahydrate term, or leave it ungrounded until a hydrate-specific CHEBI term is available.
5. Restore `BD-Difco` on the yeast extract row, either in the preferred term or in a row note.
6. Regenerate `data/merge_yaml/merged/mpob_medium__d31b2d6d.yaml` and remove the separate generated `MPOB_MEDIUM.yaml` duplicate through the normal merge pipeline.

## Follow-up Checks

- Re-run open schema validation on the regenerated canonical JCM GRMD 988 merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm every retained CHEBI and MediaIngredientMech grounding still resolves.
- Re-fetch JCM GRMD 988, 433, 554, and 197 plus MediaDive `J988`; verify that the canonical generated record distinguishes stock recipes from additions to MPOB medium.
- Re-fetch TOGO `M1041` and repeat the exact ignored-inclusive search for `GRMD=988`, `JCM_M988`, and `TOGO:M1041` to verify that the direct duplicate has been removed or explicitly linked.

## Additional Notes

- The separate TOGO duplicate is less complete than this MediaDive import: it keeps the three stock references as empty `Unknown solution` rows with `G_PER_L` units and adds unquantified N2 and CO2 gas rows. Consolidating the two source imports should use MediaDive or direct JCM for the expanded stock formulas, not the current TOGO solution placeholders.
