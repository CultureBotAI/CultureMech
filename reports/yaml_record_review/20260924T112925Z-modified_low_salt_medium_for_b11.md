# YAML Record Review: MODIFIED LOW SALT MEDIUM FOR B11

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_low_salt_medium_for_b11.yaml`
- Started UTC: 2026-09-24T11:28:34Z
- Finished UTC: 2026-09-24T11:29:25Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:015845` |
| Name | `modified_low_salt_medium_for_b11` |
| Original name | MODIFIED LOW SALT MEDIUM FOR B11 |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_low_salt_medium_for_b11.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/JCM_J1375_MODIFIED_LOW_SALT_MEDIUM_FOR_B11.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `JCM_J1375_MODIFIED_LOW_SALT_MEDIUM_FOR_B11` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_low_salt_medium_for_b11.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_low_salt_medium_for_b11.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_low_salt_medium_for_b11.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record identifies JCM Medium J1375, MODIFIED LOW SALT MEDIUM FOR
B11. The exact ignored-inclusive filename search found
`data/normalized_yaml/bacterial/JCM_J1375_MODIFIED_LOW_SALT_MEDIUM_FOR_B11.yaml`,
and the linked JCM page for medium 1375 supports the same identity.

The generated ChEBI groundings for salts, water, and Resazurin resolve to the
intended source ingredients. Yeast extract and the named stock solutions are
left as opaque ingredients, which is appropriate until the referenced JCM 284
stock recipes are curated.

## Evidence

JCM 1375 supports the generated component amounts for the base solution and the
post-autoclave stock additions:

| Component | Amount |
|---|---:|
| NaCl | 9.0 g/L |
| MgCl2 x 6 H2O | 3.0 g/L |
| KCl | 0.5 g/L |
| NH4Cl | 0.3 g/L |
| KH2PO4 | 0.2 g/L |
| CaCl2 x 2 H2O | 0.15 g/L |
| Yeast extract | 1.0 g/L |
| Trace element solution, see Medium No. 284 | 2.0 ml/L |
| Resazurin | 0.5 mg/L |
| Distilled water | 867.0 ml/L |
| 8% NaHCO3 solution | 25.0 ml/L |
| 10% Glucose solution | 100.0 ml/L |
| Trace vitamins solution, see Medium No. 284 | 2.0 ml/L |
| 5% Na2S x 9 H2O solution | 6.0 ml/L |

JCM 1375 supports the generated preparation sequence: mix the base components
and autoclave under N2-CO2 4:1, then aseptically and anaerobically add the
listed anaerobic stocks, with asterisks marking filter-sterilized NaHCO3 and
vitamin stocks.

The source says to adjust pH to 6.5-7.0 if necessary. The generated
preparation text preserves that range, but the scalar `ph_value` stores only
6.5.

JCM 1375 references JCM 284 for the trace element and trace vitamin stocks. JCM
284 exposes full recipes for both stocks, but the generated B11 record keeps
those rows as unresolved stock names without composition.

## Completeness

The generated record is complete for the final-medium masses and volume
additions listed directly in JCM 1375. It is incomplete for the two resolvable
JCM 284 stock solutions because their nested compositions are absent.

Optional `target_organisms` and `discussion` are empty. Those omissions are not
defects because JCM 1375 provides a recipe, not strain-specific growth curation
for a target organism.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/JCM_J1375_MODIFIED_LOW_SALT_MEDIUM_FOR_B11.yaml`
   leaves the JCM 284 trace element and trace vitamin stocks as opaque
   milliliter additions. JCM 284 provides the stock compositions, so the
   maintained source should model those referenced solutions instead of
   requiring a reader to manually resolve Medium No. 284.

### Minor

1. The generated scalar `ph_value` stores 6.5 although JCM 1375 gives an
   adjustment range of 6.5-7.0. The preparation text carries the complete range,
   so this is a representation gap rather than a lost source claim.

## Recommended Edits

1. Expand the JCM 284 trace element and trace vitamin references in
   `data/normalized_yaml/bacterial/JCM_J1375_MODIFIED_LOW_SALT_MEDIUM_FOR_B11.yaml`
   into modeled stock-solution compositions while retaining their 2.0 ml/L
   final-medium additions from JCM 1375.

2. Represent the pH 6.5-7.0 range in the most specific schema-supported form,
   or document why the scalar `ph_value` intentionally uses the lower bound.

3. Regenerate merged recipes and downstream media pages/browser artifacts from
   the repaired normalized source.

## Follow-up Checks

- Run the focused open schema, strict schema, term, and reference validators on
  the regenerated `data/merge_yaml/merged/modified_low_salt_medium_for_b11.yaml`.
- Re-fetch JCM 1375 and JCM 284 and verify the regenerated record preserves all
  base-solution amounts, all post-autoclave stock volumes, the N2-CO2
  atmosphere, the pH 6.5-7.0 adjustment range, and both nested stock recipes.

## Additional Notes

- The exact filename search for the maintained owner included ignored files and
  found no lowercase normalized owner; the generated record is owned by the
  direct JCM import file named in `merged_from`.
