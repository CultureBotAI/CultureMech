# YAML Record Review: myca_medium__f2fb6082

- Repository: CultureMech
- Record: data/merge_yaml/merged/myca_medium__f2fb6082.yaml
- Started UTC: 2026-09-24T16:05:37Z
- Finished UTC: 2026-09-24T16:07:26Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/myca_medium__f2fb6082.yaml`.

The generated record represents the TOGO M1567 import of NBRC Medium 369:

- canonical `id`: `CultureMech:008115`
- canonical `media_term`: `TOGO:M1567`
- canonical `name`: `myca_medium`
- canonical `original_name`: `MYCA Medium`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `LIQUID`
- `ingredients`: 5
- `merged_from`: `TOGO_M1567_MYCA_Medium`

## Validation

- Open schema validation: Passed with `No issues found`.
- Strict schema validation: Passed with 0 errors; the TSV contained only the
  header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked; `just validate-history` validates
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The high-level NBRC 369 / MYCA identity is correct. An exact
ignored-file-inclusive search for `CultureMech:008115`, `TOGO:M1567`,
`NO=369`, and `NBRC_M369` found no direct NBRC owner; the maintained NBRC 369
record is `data/normalized_yaml/bacterial/TOGO_M1567_MYCA_Medium.yaml`.

NBRC 369 is compositionally equivalent to JCM 138 MYCA Medium. The same review
pass found active JCM-side owners for `mediadive.medium:J138` and `TOGO:M129`,
but the NBRC M1567 owner is not linked to that MYCA source family.

## Evidence

The current NBRC 369 page lists MYCA Medium as 1 g Sodium DL-malate, 3 g Bacto
Yeast Extract `(Difco)`, 2 g Bacto Casamino Acids `(Difco)`, 0.5 g ammonium
sulfate, and 1 L Distilled water at pH 6.8.

The current TOGO M1567 API points to `NBRC_M369` and carries the same source
composition, including `Distilled water` at 1 L and pH 6.8.

## Completeness

The generated record preserves the dry solutes from NBRC 369, but
`Distilled water` is incorrect. The source specifies 1 L; the generated record
represents that as 1 g/L.

The generated record also lacks an explicit pH preparation step even though the
NBRC and TOGO records identify pH 6.8.

## Findings

- `Distilled water` is represented as 1 g/L instead of 1 L.
- The source pH 6.8 is present only as top-level `ph_value` and is not
  preserved as a preparation or adjustment step.
- NBRC M1567 is unlinked to the JCM/TOGO MYCA owners for the same formulation.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/TOGO_M1567_MYCA_Medium.yaml` so
  distilled water is a volume rather than 1 g/L.
- Add a pH 6.8 preparation or adjustment step if that source detail should be
  preserved operationally.
- Link NBRC M1567 to the corrected JCM 138 / TOGO M129 MYCA source family as an
  equivalent cross-provider recipe.
- Regenerate the NBRC M1567 YAML after repairing the maintained owner.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the
  regenerated YAML.
- Confirm the regenerated record carries the NBRC water volume.
- Confirm pH 6.8 is preserved.
- Confirm an ignored-file-inclusive exact search for `TOGO:M1567`,
  `TOGO:M129`, and `mediadive.medium:J138` shows the equivalent MYCA owners
  linked.

## Additional Notes

Unlike the direct JCM J138 path, TOGO/NBRC did not omit water; it imported the
water row with the wrong unit.
