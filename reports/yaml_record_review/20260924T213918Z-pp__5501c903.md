# YAML Record Review: pp__5501c903

- Repository: CultureMech
- Record: data/merge_yaml/merged/pp__5501c903.yaml
- Started UTC: 2026-09-24T21:39:18Z
- Finished UTC: 2026-09-24T21:39:18Z
- Verdict: needs curation

## Target

- MediaRecipe ID: CultureMech:000392
- Name: pp
- Source import: CCAP Medium C79
- Primary external ID: mediadive.medium:C79
- Maintained input: data/normalized_yaml/bacterial/pp.yaml

This generated record represents the bacterial/MediaDive-style import of CCAP PP, Proteose Peptone Medium.

## Validation

- Open LinkML validation: passed with no reported issues.
- Strict validation: passed for 1 file with 0 error rows; `/private/tmp/pp__5501c903.strict.tsv` is header-only.
- Reference validation: passed for 1 file with 0 reference checks.
- Term validation: passed.
- Embedded curation history: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` entries inside merged YAML.

## Identity and Grounding

The cited CCAP PDF is PP (Proteose Peptone Medium) for freshwater algae. It lists three SES stock solutions per litre: K2HPO4 at 1.0 g, MgSO4.7H2O at 1.0 g, and KNO3 at 10.0 g. The final medium uses 1.0 g/L proteose peptone plus 20 ml each of stock solutions 1 through 3, is brought to 1 litre with deionised water, and can optionally receive 15 g/L Bacteriological Agar for agar plates.

The generated record is grounded to the right PDF but flattens the stock addition rows incorrectly: it treats the three 20 ml stock-solution additions as 20 G_PER_L of the stock solutes in the final recipe. It also emits a solid agar record even though CCAP lists PP as a base liquid recipe with optional agar.

Exact ignored-file searches across `data/normalized_yaml` and `data/merge_yaml` for `MR_PP.pdf`, `CCAP Medium C79`, `mediadive.medium:C79`, `source: pp.yaml`, and exact `name: pp` found this bacterial import, a separate algae `data/normalized_yaml/algae/pp.yaml` import from the same CCAP PDF, and the corresponding `data/merge_yaml/merged/PP.yaml` output. The algae normalized input has a later `REPAIRED_CCAP_INSTRUCTION_ROWS` event that reduced the recipe to the CCAP source-asserted 1 g/L proteose peptone plus preparation steps for deionised water, optional agar, and autoclaving.

## Evidence

- The CCAP PP PDF lists 1.0 g K2HPO4, 1.0 g MgSO4.7H2O, and 10.0 g KNO3 under `Stocks per litre`.
- The same PDF lists 1.0 g Proteose peptone and `Stock solutions 1 - 3 20.0 ml each` under `Medium per litre`.
- The same PDF instructs making the medium up to 1 litre with deionised water and adding 15 g/L Bacteriological Agar only for agar.
- The separate algae normalized `pp.yaml` already carries a later source-backed repair for this CCAP PDF, while the bacterial normalized `pp.yaml` still carries the older flattened MediaDive import.

## Completeness

The generated record is incomplete and quantitatively wrong against the CCAP source. It should not contain K2HPO4, MgSO4.7H2O, or KNO3 as 20 G_PER_L final ingredients, should not make optional agar mandatory, and should not duplicate the same CCAP PP source as a bacterial medium while a repaired algae CCAP record exists.

## Findings

1. Major: SES stock additions are flattened as final 20 G_PER_L ingredients; the source adds 20 ml each of three stock solutions per litre, which is 0.02 g/L final K2HPO4, 0.02 g/L final MgSO4.7H2O, and 0.2 g/L final KNO3 if expanded.
2. Major: Optional agar is modelled as mandatory by `physical_state: SOLID_AGAR` and a required 15 G_PER_L agar ingredient, although the CCAP PDF presents PP as a freshwater algae liquid medium with an optional agar modification.
3. Major: The same CCAP PP PDF is represented twice, with this stale bacterial import split from the repaired `data/normalized_yaml/algae/pp.yaml` maintained input.
4. Minor: The generated record lacks the CCAP reference identity and source references that are present on the repaired algae input.

## Recommended Edits

- Retire or reconcile `data/normalized_yaml/bacterial/pp.yaml` in favor of the repaired algae CCAP PP input, or apply the same CCAP stock repair to the bacterial import if it must remain separate.
- Represent SES stock solutions as stock additions rather than 20 G_PER_L final salts, and keep the optional 15 g/L Bacteriological Agar as an agar variant or preparation option.
- Regenerate `data/merge_yaml/merged/pp__5501c903.yaml` only after the duplicate CCAP PP source path has been reconciled.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation after normalized curation and regeneration.
- Run exact ignored-file searches for `MR_PP.pdf`, `CCAP:PP`, `CCAP Medium C79`, and `mediadive.medium:C79` after reconciliation to confirm the CCAP PP source appears only in the intended generated record.
- Spot-check regenerated output for 1 g/L proteose peptone, three 20 ml SES stock additions, deionised water to 1 L, and optional 15 g/L agar.

## Additional Notes

None found.
