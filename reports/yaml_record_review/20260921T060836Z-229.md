# YAML Record Review: 229

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/229.yaml`
- Started UTC: 2026-09-21T06:08:00Z
- Finished UTC: 2026-09-21T06:08:36Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007479` |
| Name | `229` |
| Original name | `229` |
| Category | `bacterial` |
| Source term | Missing in generated artifact; maintained owner now uses `nbrc.medium:228` |
| Merge fingerprint | `56048a3a5730ce0fe575ead0bf45fec153ff4095b7451d7e57f0ef3eac2a6bed` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_229.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 228 Bennett's Agar record. The maintained owner was repaired on 2026-08-19, 2026-08-20, and 2026-09-11, so this generated artifact is stale relative to the source record that owns future fixes.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/229.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/229.yaml --out /private/tmp/229.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/229.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/229.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record's description and import history identify the source as NBRC Medium 228, but the generated artifact lacks a structured `media_term` and still carries the pre-recovery placeholder name `229`. The live NBRC Medium 228 page resolves, names the recipe Bennett's Agar, and supports the recovered source formulation now maintained in `data/normalized_yaml/bacterial/NBRC_229.yaml`.

Grounding is stale-incomplete in the generated artifact. It has no correctly parsed ingredients or ontology grounding, while the maintained owner now restores the six ingredient rows, grounds glucose to `CHEBI:17234`, distilled water to `CHEBI:15377`, and agar to `CHEBI:2509`, and intentionally leaves the yeast extract, beef extract, and N-Z-Amine A mixture rows ungrounded.

## Evidence

The inspected NBRC Medium 228 page supports exactly these composition rows:

- 1 g yeast extract.
- 1 g beef extract.
- 2 g N-Z-Amine A.
- 10 g glucose.
- 1 L distilled water.
- 20 g agar.
- pH 7.3.

The generated artifact has none of those rows correctly represented. It has one concatenated `Yeast extract1gBeef extract1gN-Z-Amine A2gGlucose10gDistilled water1LAgar20gpH 7.3` variable row, a second row with an empty preferred term and `value: Yeast extract`, and generic pH/autoclave preparation steps that NBRC Medium 228 does not state.

## Completeness

Consequential gaps:

- The six source ingredient rows are not represented as yeast extract 1 g, beef extract 1 g, N-Z-Amine A 2 g, glucose 10 g, distilled water 1 L, and agar 20 g.
- The source pH 7.3 is embedded in a concatenated ingredient label instead of `ph_value`.
- The second generated ingredient row has an empty label and a nonnumeric concentration value.
- The generated record lacks the maintained owner's official `Bennett's Agar` label, structured `nbrc.medium:228` media term, source reference, `ingredients_curated` flag, and recovered composition history.
- The generated preparation steps are placeholders unsupported by NBRC Medium 228.

Empty optional slots:

- `target_organisms`, `growth_metrics`, `variants`, and source-specific sterilization are correctly absent in the maintained owner. The inspected NBRC page verifies only the recipe rows and final pH.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/raw`, and `reports/yaml_record_review` for `CultureMech:007479`, `NBRC_229`, `NO=228`, `Media No. 228`, the `56048a3a` merge fingerprint, and the malformed concatenated ingredient label found the expected normalized owner, expected generated merge, TOGO's independent Bennett's Agar mirror for the same NBRC URL, and source index rows.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact still contains the original malformed NBRC composition parse. | NBRC Medium 228 lists six ingredient rows and pH 7.3; the generated merge stores the whole table and pH in one variable ingredient label plus a second empty-label row with `value: Yeast extract`. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_229.yaml` |
| Major | The generated artifact carries unsupported placeholder preparation. | NBRC Medium 228 states no pH-adjustment or autoclave instructions, while the generated merge has generic `Adjust pH if needed` and `Sterilize by autoclaving` steps. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_229.yaml` |
| Minor | Structured source provenance is stale-missing. | The maintained owner now has `name: Bennett's Agar`, `media_term: nbrc.medium:228`, `ph_value: 7.3`, a structured NBRC reference, and curation flags; the generated merge still has only the old free-text import note, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_229.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `229.yaml` receives the maintained owner's Bennett's Agar label, recovered six ingredient rows, pH 7.3, NBRC Medium 228 source term, structured reference, curated flags, and removal of unsupported generic preparation steps.
2. After regeneration, re-run focused schema, strict, term, and reference validation on `data/merge_yaml/merged/229.yaml`.

## Follow-up Checks

- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- `just validate-strict` and `just validate-terms` on the regenerated `229.yaml`.
- `just validate-references` on the regenerated merge to confirm the structured NBRC reference resolves.
- Manual comparison against NBRC Medium 228 for exactly 1 g yeast extract, 1 g beef extract, 2 g N-Z-Amine A, 10 g glucose, 1 L distilled water, 20 g agar, pH 7.3, and no unsupported pH-adjustment or sterilization statements.

## Additional Notes

- The maintained normalized owner is already repaired; no additional normalized edit was identified for NBRC Medium 228 in this review.
- `data/normalized_yaml/bacterial/TOGO_M1456_Bennett_s_Agar.yaml` is an independent TOGO mirror of NBRC Medium 228 and should remain a separate source record unless a future merge rule explicitly folds NBRC and TOGO mirrors of the same NBRC page.
- `linkml-reference-validator` performed zero checks because this generated record has no structured `references` entries.
