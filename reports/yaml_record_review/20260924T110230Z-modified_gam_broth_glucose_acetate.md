# YAML Record Review: Modified GAM broth + glucose & acetate

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml`
- Started UTC: 2026-09-24T11:02:00Z
- Finished UTC: 2026-09-24T11:02:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008755` |
| Name | `modified_gam_broth_glucose_acetate` |
| Original name | Modified GAM broth + glucose & acetate |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `modified_gam_broth_glucose_acetate` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_gam_broth_glucose_acetate.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record resolves to the intended TOGO/NBRC medium. Its `media_term`
uses `TOGO:M2161`, the notes point to NBRC medium 1534, and both inspected
sources identify the medium as Modified GAM broth + glucose & acetate.

The generated and normalized YAML preserve these source amounts from
TOGO M2161 and NBRC 1534:

| Source component | Source amount | Generated representation |
|---|---:|---|
| Nissui Modified GAM Broth | 41.7 g | Empty solution named `Unknown solution`, concentration 41.7 `G_PER_L` |
| Glucose | 9.5 g | Ingredient, 9.5 `G_PER_L`, `CHEBI:17234` glucose |
| Sodium acetate | 2.7 g | Ingredient, 2.7 `G_PER_L`, `CHEBI:32954` sodium acetate |
| Distilled water | 1 L | Ingredient, 1 `G_PER_L`, `CHEBI:15377` water |
| N2 | Atmosphere for autoclaving | Ingredient, `variable` `VARIABLE`, `CHEBI:17997` dinitrogen |

The ChEBI groundings for water, sodium acetate, glucose, and dinitrogen agree
with the corresponding generated ingredient labels. The Nissui complex
component is no longer represented as an ingredient with the imported TOGO
grounding; it was migrated into an empty `solutions` entry and given the
default name `Unknown solution`.

## Evidence

The embedded source metadata is internally consistent:

- TOGO M2161 records `original_media_id` as `NBRC_M1534` and its source URL as
  the NBRC detail page for medium 1534.
- NBRC medium 1534 has medium name Modified GAM broth + glucose & acetate.
- TOGO and NBRC both support the generated amounts for 41.7 g Nissui Modified
  GAM Broth, 9.5 g glucose, 2.7 g sodium acetate, 1 L distilled water, and N2
  as the atmosphere used while autoclaving the dispensed medium.

The record is over-scoped or incomplete for several source claims:

- NBRC lists distilled water as 1 L, and the generated record instead stores
  water as 1 `G_PER_L`.
- NBRC describes N2 as the autoclave atmosphere; the generated record stores
  it as an ingredient with a default variable concentration.
- NBRC says the medium is pH unadjusted and says to autoclave the dispensed
  medium under N2, then add glucose after autoclaving; those instructions are
  absent from the generated recipe.
- NBRC footnotes Nissui Pharmaceutical Co. Ltd., Tokyo, Japan for Nissui
  Modified GAM Broth; the generated record keeps the asterisked label but drops
  the footnote.

## Completeness

The record is complete enough to identify the imported medium and the simple
source ingredients, but not complete enough to reproduce the source protocol:
it loses the pH context, the post-autoclave glucose addition, the autoclave
atmosphere, and the supplier footnote for the named complex broth.

Optional `target_organisms`, `discussion`, and quality-flag fields are empty.
Those omissions are not defects for this record because the inspected NBRC and
TOGO pages provide a recipe, not strain-specific growth evidence or an explicit
curation discussion.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`
   models a dry complex medium component as an empty stock solution. NBRC and
   TOGO represent Nissui Modified GAM Broth as a 41.7 g main-medium component,
   but the generated record has `solutions[0].preferred_term: Nissui Modified
   GAM Broth*`, an empty `composition: []`, and the default `name: Unknown
   solution`. A future fix belongs in the normalized record and in the
   solution-migration rule that turns imported complex ingredients into empty
   solutions.

2. `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`
   stores source water volume as a mass concentration. The source formulation
   uses 1 L distilled water as make-up volume; the generated recipe records
   that as `value: '1'` with `unit: G_PER_L`. A future fix belongs in the TOGO
   importer or normalization path that converted the source `L` amount.

3. `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`
   drops source preparation and condition claims. NBRC specifies pH unadjusted,
   autoclaving the dispensed medium under N2, and adding glucose only after
   autoclaving. The generated record instead has N2 as a variable-concentration
   ingredient and has no preparation text preserving the pH or glucose-addition
   order. A future fix belongs in the normalized record and in the importer path
   that maps TOGO comments into procedural fields or curation notes.

### Minor

1. `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`
   keeps the asterisk in `Nissui Modified GAM Broth*` but omits the NBRC
   footnote identifying the supplier as Nissui Pharmaceutical Co. Ltd., Tokyo,
   Japan. A future fix should either preserve the footnote in a component note
   or remove the dangling asterisk while keeping the preferred term recoverable.

## Recommended Edits

1. In `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`,
   move Nissui Modified GAM Broth out of `solutions` and back to the main
   ingredient list as a 41.7 g/L undefined complex component, preserving source
   provenance to TOGO M2161 and NBRC 1534.

2. Correct distilled water so it preserves the source role as 1 L final make-up
   volume instead of 1 g/L.

3. Preserve NBRC's pH unadjusted note and its preparation order: dispense,
   autoclave under N2, then add glucose after autoclaving.

4. Represent N2 as an atmosphere condition or preparation note rather than a
   variable-dose ingredient.

5. Carry forward the NBRC footnote for Nissui Modified GAM Broth or normalize
   the preferred term so the asterisk is not left dangling.

6. Rerun the merge generator so
   `data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml` reflects the
   normalized fix.

## Follow-up Checks

- Run the focused open schema, strict schema, term, and reference validators on
  `data/normalized_yaml/bacterial/modified_gam_broth_glucose_acetate.yaml`.
- Regenerate merged recipes and confirm the same ingredient, water, pH, N2, and
  supplier corrections appear in
  `data/merge_yaml/merged/modified_gam_broth_glucose_acetate.yaml`.
- Re-open NBRC medium 1534 and TOGO M2161 to manually confirm the corrected
  record still matches the 41.7 g, 9.5 g, 2.7 g, 1 L, pH, N2, and
  post-autoclave glucose instructions.
- Run merge freshness checks after regeneration to verify the generated record
  is no longer stale.

## Additional Notes

- The earlier TOGO M2164 and NBRC 1544 fetches were a lead mismatch for 1/2
  Marine Broth (Agar) and Medium for haloarchaea. The generated record itself
  names TOGO M2161 and NBRC 1534, so only those exact sources were used for this
  verdict.
- TOGO M2161 did not include NBRC's optional 15 g agar entry, and the imported
  CultureMech record is a liquid broth. I did not treat absent optional agar as
  a defect in this generated record.
- `merge_recipes.py` merged exactly one source recipe; the merge layer did not
  introduce a cross-record conflict.
