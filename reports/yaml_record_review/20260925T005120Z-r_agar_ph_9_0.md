# YAML Record Review: r_agar_ph_9_0

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_agar_ph_9_0.yaml
- Started UTC: 2026-09-25T00:49:50Z
- Finished UTC: 2026-09-25T00:51:20Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:002269 |
| Name | r_agar_ph_9_0 |
| Class | MediaRecipe |
| Category | bacterial |
| Source identity | JCM Medium J1089 / R AGAR (pH 9.0) |
| Maintained owner | data/normalized_yaml/bacterial/r_agar_ph_9_0.yaml |
| Generated path | data/merge_yaml/merged/r_agar_ph_9_0.yaml |

The reviewed file is a generated merge product that folded JCM J26 base R Agar into JCM J1089 pH 9.0 R Agar because the two records shared the same copied base ingredient list.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_agar_ph_9_0.yaml` exited 0 and printed `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r_agar_ph_9_0.yaml --out /private/tmp/r_agar_ph_9_0.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r_agar_ph_9_0.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r_agar_ph_9_0.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

## Identity and Grounding

- MediaDive J1089 and TOGO M1159 agree that R Agar pH 9.0 is prepared from R Agar and adjusted after autoclaving to pH 9.0 with sterilized 10% Na2CO3 solution.
- The generated record keeps the J1089 name and pH 9.0 but merges in JCM J26 base `r_agar` as a synonym and `merged_from` source, which makes the pH-7.2 base appear source-equivalent to its pH-9.0 child.
- The maintained direct JCM J1089 owner lacks the variable Na2CO3 solution modeled in the maintained TOGO M1159 owner, so the generated record cannot represent the post-autoclave pH adjustment as a solution addition.

## Evidence

Supported source-backed claims:

- MediaDive J1089 supports the JCM J1089 identity, pH 9.0 value, and preparation instruction to prepare R Agar, autoclave it, then adjust to pH 9.0 with sterile 10% Na2CO3.
- TOGO M1159 supports the same JCM_M1089 source identity and makes the two recipe components explicit: 1 L of R Agar as reference medium M19 plus a 10% Na2CO3 solution.
- JCM GRMD 26 and the maintained J26 owner establish the base R Agar formulation and pH 7.2, not a duplicate source identity for JCM J1089.

Unsupported or over-scoped claims:

- The generated `merged_from` claim that direct JCM `r_agar` and `r_agar_ph_9_0` are duplicates is unsupported. They differ by pH and by a post-autoclave Na2CO3 pH adjustment.
- The generated `synonyms` block exposes `mediadive.medium:J26` as a synonym of `J1089`. J26 is the base recipe, not an accession for the pH 9.0 child.
- The generated record carries only the base ingredient list. It omits the explicit Na2CO3 solution row that distinguishes the pH variant.

## Completeness

- The review checked the generated YAML, direct JCM J1089 and J26 normalized owners, the TOGO M1159 and M19 normalized owners, MediaDive J1089, TOGO M1159, and the live JCM GRMD 26 source for the base R Agar formula.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `r_agar_ph_9_0`, `TOGO_M1159_R_Agar_pH_9.0`, `mediadive.medium:J1089`, and `TOGO:M1159` found the direct JCM and TOGO pH-9 owners plus the generated merge and source indexes.
- Empty `target_organisms` is not a defect because the inspected source records define formulations, not a strain-growth observation.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Blocker | The generated pH-9 record falsely merged its pH-7.2 base record. | `merged_from` lists both `r_agar` and `r_agar_ph_9_0`, and `synonyms` maps JCM J26 under the JCM J1089 record. The sources model J1089 as an adjusted variant of J26. | `src/culturemech/merge/fingerprint.py`, `src/culturemech/merge/matcher.py`, and regenerated `data/merge_yaml/merged/` |
| Major | The direct JCM pH-9 owner and generated record omit the 10% Na2CO3 solution as a represented component. | MediaDive J1089 mentions sterile 10% Na2CO3 in the preparation step; TOGO M1159 represents it as a solution with variable final amount. | data/normalized_yaml/bacterial/r_agar_ph_9_0.yaml or the JCM/MediaDive import/reference-resolution path |
| Minor | The generated record is stale relative to exact ingredient grounding. | The direct JCM owner gained exact Bacto peptone, yeast extract, malt extract, casamino acids, and beef extract terms on September 13, 2026; the generated record still has ungrounded rows. | Merge regeneration workflow |

## Recommended Edits

1. Change merge grouping so pH variants and solution-adjusted variants are not collapsed into their base media when the copied base ingredient set is identical.
2. Add a variable 10% Na2CO3 solution to the maintained direct JCM J1089 owner, or intentionally reconcile it with the maintained TOGO M1159 pH variant after confirming both source mirrors are identical.
3. Regenerate `data/merge_yaml/merged/r_agar_ph_9_0.yaml` after the merge and owner fixes.
4. Verify that regenerated JCM J1089 lists only itself in `merged_from`, keeps pH 9.0, retains the post-autoclave Na2CO3 adjustment, and no longer lists JCM J26 as a generated synonym.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation on the edited owner and regenerated pH-9 output.
- Run `just verify-merges` and `just audit-merge-freshness` after regeneration.
- Inspect the regenerated YAML for any lingering `mediadive.medium:J26` synonym under JCM J1089.
- Re-run a gitignore-independent exact search for `mediadive.medium:J1089`, `TOGO:M1159`, and `mediadive.medium:J26` to confirm the base and pH-9 generated records remain distinct and linked only as variants.

## Additional Notes

- This review did not edit generated YAML or the maintained R Agar records.
