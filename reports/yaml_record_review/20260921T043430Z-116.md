# YAML Record Review: 116

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/116.yaml`
- Started UTC: 20260921T043317Z
- Finished UTC: 20260921T043430Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007460` |
| Label | `116` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_116.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_116]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_116.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import for NBRC Medium 114. The maintained owner has since recovered the eight-row source table and curated the official NBRC Medium 114 title, source term, groundings, and reference.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/116.yaml` | Pass; `No issues found` |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/116.yaml --out /private/tmp/culturemech-review-116.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-116.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/116.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/116.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The intended identity is NBRC Medium 114:

- The import history says `Media No. 114` and links the NBRC `NO=114` page.
- Live NBRC Medium 114 is titled `Medium for the 2u Mapping Strains`.
- The maintained owner now carries `media_term: nbrc.medium:114`, the official title, the NBRC 114 HTTPS reference, and exact recovered ingredient rows.

The generated file is identity-stale:

- `name` and `original_name` are both `116`, but live NBRC Medium 116 is a different `25% Glucose-Yeast Extract-Peptone` recipe.
- The generated merge has no `media_term`, no `references`, and no ingredient groundings.

## Evidence

Supported by inspected source text:

- NBRC Medium 114 lists Bacto-Yeast Nitrogen Base w/o Amino acids (Difco) 6.7 g, glucose 20 g, L-Leucine 60 mg, L-Histidine 20 mg, L-Tryptophan 20 mg, L-Methionine 20 mg, distilled water 1 L, and agar 15 g.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all eight source rows into one `preferred_term` with `VARIABLE` concentration.
- The second ingredient row has `preferred_term: ''` and `concentration.value: Bacto-Yeast Nitrogen Base w/o Amino acids (Difco)`.
- The generated artifact has no separate milligram rows for L-Leucine, L-Histidine, L-Tryptophan, or L-Methionine.
- The placeholder `ADJUST_PH` and `Sterilize by autoclaving` preparation steps are not present in the inspected NBRC 114 table.

## Completeness

- The generated file omits the Aug 19 composition recovery, the Aug 20 MIM groundings, and the Sep 8 `RESOLVED_NBRC_EXACT_SCORE20` source-metadata repair.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_116`, `NO=114`, `NBRC Medium 114`, and `nbrc.medium:114` found this stale generated NBRC direct import, the repaired normalized owner, normalized index entries, a separate TOGO-derived `medium_for_the_2_mapping_strains` NBRC 114 copy, and unrelated NBRC URLs whose accessions only share the `114` prefix.
- Empty preparation and growth-evidence slots in the normalized owner are acceptable where the source table has no procedural or tested-organism claim; the stale generated placeholder steps are not.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading numeric identity for NBRC Medium 114. | The file is labeled `116`, while its import event points to NBRC Medium 114; live NBRC Medium 116 is a separate glucose/yeast-extract/peptone recipe. | `data/normalized_yaml/bacterial/NBRC_116.yaml` is repaired; regenerate `data/merge_yaml/merged/` and clean up the stale artifact. |
| Blocker | The eight-row NBRC composition is collapsed into two malformed rows. | NBRC Medium 114 lists eight components, including four milligram amino-acid rows; the generated file has one concatenated blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_116.yaml`; fix NBRC import or merge source selection if the malformed rows persist. |
| Major | The generated file lacks the recovered source term and ingredient groundings. | The normalized owner now has `nbrc.medium:114`, source notes, glucose/leucine/histidine/tryptophan/methionine/water/agar groundings, and the NBRC reference; the generated merge has none of those. | Regenerate the merge corpus. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 114 page supplies only the composition table; the generated file invented `ADJUST_PH` and autoclave text, while the maintained normalized record now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_116.yaml`. |
| Minor | A TOGO-derived copy of NBRC Medium 114 remains separate. | Gitignore-independent search found separate normalized/generated `medium_for_the_2_mapping_strains` records with the same NBRC 114 URL. | Merge-fingerprint rules for NBRC/TOGO duplicate handling. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the repaired NBRC normalized corpus and confirm `CultureMech:007460` carries `Medium for the 2u Mapping Strains`, not plain `116`.
2. Confirm the regenerated record has eight separate rows with the four amino acids in `MG_PER_L`, water as 1 L, and the NBRC Medium 114 source term/reference.
3. If regeneration preserves the concatenated blob, fix the NBRC source selection path that is still using the pre-recovery import output.
4. Review whether the repaired NBRC direct import and TOGO `medium_for_the_2_mapping_strains` record should merge on the same formulation fingerprint.

## Follow-up Checks

- `just verify-merges` to prove generated outputs are fresh relative to `data/normalized_yaml/bacterial/NBRC_116.yaml`.
- `just audit-merge-freshness` to catch sibling stale NBRC direct imports.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-114>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-114>.yaml` to verify all recovered ingredient labels.
- Manual comparison to live NBRC Medium 114 for the eight source rows and milligram amino-acid units.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
