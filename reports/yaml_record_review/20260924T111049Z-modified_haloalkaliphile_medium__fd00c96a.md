# YAML Record Review: Modified Haloalkaliphile Medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml`
- Started UTC: 2026-09-24T11:09:45Z
- Finished UTC: 2026-09-24T11:10:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007838` |
| Name | `modified_haloalkaliphile_medium` |
| Original name | Modified Haloalkaliphile Medium |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/TOGO_M1303_Modified_Haloalkaliphile_Medium.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `TOGO_M1303_Modified_Haloalkaliphile_Medium` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_haloalkaliphile_medium__fd00c96a.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record resolves to TOGO M1303, the solid-agar TOGO import of JCM
1215. TOGO M1303 identifies `original_media_id` as `JCM_M1215-2`, links to JCM
`GRMD=1215`, and represents the recipe as a 500 ml Solution A and a 500 ml
Solution B. MediaDive solutions 5342 and 5343 independently resolve to the same
Solution A and Solution B recipes embedded in TOGO M1303.

The generated YAML preserves the intended TOGO M1303 ID, physical state, 18 g
agar, 80 g Na2CO3, 2 g Yeast extract (BD-Difco), 2 g Casamino acids
(BD-Difco), 1 g sodium glutamate, 3 g Trisodium citrate x 2H2O, 0.1 g MgSO4 x
7H2O, 0.1 g CaSO4 x 2H2O, and 1 g KCl. Its source terms for the hydrated
magnesium and calcium sulfates point at the corresponding ChEBI hydrate terms,
and sodium carbonate, sodium chloride, potassium chloride, and monosodium
glutamate also have appropriate ChEBI groundings.

## Evidence

TOGO M1303 and MediaDive solution 5342 support this Solution A composition:
500 ml distilled water, 0.1 g MgSO4 x 7H2O, 100 g NaCl, 1 g KCl, 0.1 g CaSO4 x
2H2O, 1 g sodium glutamate, 3 g Trisodium citrate x 2H2O, 18 g agar, 2 g Yeast
extract (BD-Difco), and 2 g Casamino acids (BD-Difco).

TOGO M1303 and MediaDive solution 5343 support this Solution B composition:
500 ml distilled water, 100 g NaCl, and 80 g Na2CO3.

The generated file has two classes of stale or unscoped values:

- it still contains the August duplicate sums, 1000 `G_PER_L` distilled water
  and 200 `G_PER_L` NaCl, whereas the September normalized YAML has collapsed
  those to 500 and 100 respectively;
- the September normalized YAML also remains unscoped, because those equal
  source values belong to different 500 ml solutions and should not be treated
  as accidental duplicates.

TOGO M1303 and the MediaDive solution records also support keeping Solution A
and Solution B as real solution boundaries. The record instead duplicates their
ingredients at top level and stores two empty `Unknown solution` entries at 500
`G_PER_L`, even though the source unit is 500 ml for each solution.

## Completeness

The record is complete enough to identify the source medium and most of the
component names, amounts, and chemical groundings. It is not complete enough to
reproduce the source recipe because Solution A and Solution B are not scoped:
their water and NaCl rows are either summed in the generated file or collapsed
in the current normalized file, and the two `solutions` entries have no
composition.

Optional `target_organisms`, `discussion`, and quality-flag fields are empty.
Those omissions are not defects because the inspected TOGO and MediaDive records
provide recipe formulas, not strain-specific growth evidence or an explicit
curation discussion.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/TOGO_M1303_Modified_Haloalkaliphile_Medium.yaml`
   does not preserve the TOGO M1303 / JCM 1215 Solution A and Solution B
   boundaries. It promotes both solution compositions to top-level ingredients
   and also stores empty Solution A and Solution B references at 500 `G_PER_L`.
   A future fix should model the two 500 ml solution groups explicitly, with
   ingredients attached to their source solution rather than duplicated at top
   level.

2. `data/normalized_yaml/bacterial/TOGO_M1303_Modified_Haloalkaliphile_Medium.yaml`
   collapsed equal water and NaCl values that are not accidental duplicates.
   TOGO M1303 has 500 ml water and 100 g NaCl in Solution A plus 500 ml water
   and 100 g NaCl in Solution B. The current normalized file keeps only one
   500 g/L water row and one 100 g/L NaCl row; a future fix should restore the
   two solution-scoped water and NaCl entries, or an explicitly calculated final
   1 L / 200 g/L representation if the solution boundaries are intentionally
   flattened.

3. `data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml` is
   stale relative to the September duplicate repair. It still shows the older
   summed 1000.0 `G_PER_L` water and 200.0 `G_PER_L` NaCl rows. Regeneration
   will remove those stale sums, but the normalized source still needs the
   solution-scoping fix above to avoid replacing one unscoped representation
   with another.

### Minor

None found.

## Recommended Edits

1. In
   `data/normalized_yaml/bacterial/TOGO_M1303_Modified_Haloalkaliphile_Medium.yaml`,
   model Solution A and Solution B as separate 500 ml solution groups instead
   of empty 500 g/L `Unknown solution` entries.

2. Restore the Solution A and Solution B water and NaCl rows to those scoped
   groups so the recipe no longer depends on whole-record duplicate collapsing.

3. Preserve the source instruction to autoclave Solution A and B separately and
   combine them after cooling.

4. Rerun the merge generator so
   `data/merge_yaml/merged/modified_haloalkaliphile_medium__fd00c96a.yaml`
   reflects the corrected normalized TOGO M1303 source.

## Follow-up Checks

- Run focused open schema, strict schema, term, and reference validation on the
  normalized TOGO M1303 record after curation.
- Regenerate merged recipes and verify this generated record no longer contains
  stale 1000 g/L water or 200 g/L NaCl rows at top level.
- Re-fetch TOGO M1303 and MediaDive solutions 5342 and 5343, then manually
  verify that the regenerated record preserves all Solution A and Solution B
  components, 500 ml amounts, and separate-autoclave preparation text.

## Additional Notes

- The direct JCM 1215 page currently returns "Nothing found", so TOGO M1303 and
  MediaDive solutions 5342 and 5343 were used as the inspected sources for this
  JCM_M1215-2 import.
