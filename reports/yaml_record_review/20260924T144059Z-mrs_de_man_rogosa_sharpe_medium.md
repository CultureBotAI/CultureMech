# YAML Record Review: MRS (de Man, Rogosa, Sharpe) medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml
- Started UTC: 2026-09-24T14:39:22Z
- Finished UTC: 2026-09-24T14:40:59Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009477 |
| Name | mrs_de_man_rogosa_sharpe_medium |
| Original name | MRS (de Man, Rogosa, Sharpe) medium |
| Category | bacterial |
| Media term | TOGO:M2950 |
| Source | TOGO M2950 |
| Reviewed path | data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml |
| Maintained source | data/normalized_yaml/bacterial/mrs_de_man_rogosa_sharpe_medium.yaml |
| Generated status | Generated by recipe-merger / merge_recipes.py on 2026-08-06 |

The generated output merges TOGO M2950 with TOGO M2238. The inspected source
formulae match at the recipe level, so the merge itself is not a defect; both
upstream normalized records share the same unit-normalization errors.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml` exited 0 and printed `No issues found`. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml --out /private/tmp/mrs_de_man_rogosa_sharpe_medium.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The record passes structural validators, but the unit errors make the recipe
chemically unrealistic.

## Identity and Grounding

- `media_term.id` is `TOGO:M2950`, `media_term.label` is
  `MRS (de Man, Rogosa, Sharpe) medium`, and the live TOGO M2950 record has the
  same name.
- TOGO M2950 has no external `src_url`; the generated record correctly points
  only to the TOGO page.
- TOGO M2238, `MRS broth`, has the same ingredient formula and source pH range.
  Keeping it as a duplicate synonym is therefore supportable.
- The magnesium sulfate and manganese sulfate hydrate identities are grounded
  correctly, but their source milligram values were imported as grams per
  liter.
- `CH3COONa.3H2O` and `polyoxyethylene sorbitan mono-oleate (Tween 80)` are
  specific source chemicals with GMO identifiers in TOGO but no term grounding
  in the YAML.

## Evidence

TOGO M2950 supports:

| Ingredient | Source amount | YAML amount |
| --- | --- | --- |
| Distilled water | 1 L | 1 G_PER_L |
| MgSO4 x 7H2O | 200 mg | 200 G_PER_L |
| K2HPO4 | 2 g | 2 G_PER_L |
| Polyoxyethylene sorbitan mono-oleate, Tween 80 | 1 ml | 1 G_PER_L |
| MnSO4 x 4H2O | 50 mg | 50 G_PER_L |
| Triammonium citrate | 2 g | 2 G_PER_L |
| CH3COONa.3H2O | 5 g | 5 G_PER_L |
| Glucose | 20 g | 20 G_PER_L |
| Oxoid peptone | 10 g | 10 G_PER_L |
| Lab-Lemco, Oxoid | 10 g | 10 G_PER_L |
| Yeast extract, Difco or Oxoid | 5 g | 5 G_PER_L |

The TOGO M2950 comment reports pH 6.0 to 6.5 after sterilizing, about 6.2 to
6.6 before sterilization, and recommends sterilizing for 15 min at 120 C.
Those procedural details are absent from the YAML.

## Completeness

- The magnesium sulfate amount should be 0.2 g/L if the source's 200 mg is
  normalized into the one-liter recipe.
- The manganese sulfate amount should be 0.05 g/L if the source's 50 mg is
  normalized into the one-liter recipe.
- The source's 1 L distilled-water amount is represented as `1 G_PER_L`.
- The source's 1 ml Tween 80 amount is represented as `1 G_PER_L`.
- The pH range and sterilization recommendation are missing.
- The generated and maintained M2950 records retain `high_metal: true`; this is
  an artifact of the milligram-to-gram import bug rather than source evidence
  that the medium is high-metal.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` found TOGO M2950 only in the expected maintained M2950
  source, this generated output, and generated indexes. The same search also
  found the expected TOGO M2238 synonym source and multiple unrelated MRS broth
  records because `mrs_broth` is a generic label.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The trace-metal milligram rows were imported as grams per liter. | TOGO M2950 lists 200 mg MgSO4 x 7H2O and 50 mg MnSO4 x 4H2O; the YAML stores `200 G_PER_L` and `50 G_PER_L`, a 1000-fold inflation. | `data/normalized_yaml/bacterial/mrs_de_man_rogosa_sharpe_medium.yaml` and `data/normalized_yaml/bacterial/mrs_broth.yaml` |
| major | The generated record has a false `high_metal` flag. | The flag follows from the inflated 200 g/L and 50 g/L trace-metal rows; the source values are 0.2 g/L and 0.05 g/L after unit normalization. | TOGO import normalization and regenerated `data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml` |
| major | Source volume rows were cast to mass concentrations. | TOGO M2950 lists 1 L water and 1 ml Tween 80; the YAML stores both as `G_PER_L`. | TOGO import normalization for the M2950 and M2238 normalized sources |
| major | The record omits the source pH and sterilization recommendation. | TOGO M2950 reports pH 6.0 to 6.5 after sterilizing and recommends 15 min at 120 C; the YAML has no pH or preparation field. | `data/normalized_yaml/bacterial/mrs_de_man_rogosa_sharpe_medium.yaml` |
| major | Defined ingredients are missing ontology grounding. | TOGO identifies Tween 80 as Polysorbate 80 and identifies sodium acetate trihydrate; both rows lack `term` and `mediaingredientmech_chebi_term`. | CHEBI enrichment for `data/normalized_yaml/bacterial/mrs_de_man_rogosa_sharpe_medium.yaml` and `data/normalized_yaml/bacterial/mrs_broth.yaml` |

## Recommended Edits

1. Normalize TOGO `mg` source rows by converting MgSO4 x 7H2O to
   `0.2 G_PER_L` and MnSO4 x 4H2O to `0.05 G_PER_L` in both M2950 and M2238.
2. Remove `high_metal: true` after the milligram rows are corrected.
3. Preserve the 1 L distilled-water and 1 ml Tween 80 rows as non-`G_PER_L`
   volume rows, or otherwise model them without casting liters and milliliters
   to grams per liter.
4. Preserve pH 6.0 to 6.5 and the 15 min at 120 C sterilization recommendation
   from TOGO M2950.
5. Add ontology grounding for Tween 80 and sodium acetate trihydrate where
   supported by TOGO's GMO mappings.
6. Regenerate the merged YAML from the repaired M2950 and M2238 normalized
   sources.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated `data/merge_yaml/merged/mrs_de_man_rogosa_sharpe_medium.yaml`.
- Search with `rg --no-ignore --hidden` for `TOGO:M2950`, `TOGO:M2238`,
  `CultureMech:009477`, and the M2238 CultureMech ID across
  `data/normalized_yaml` and `data/merge_yaml` to confirm the merge remains
  limited to the two inspected MRS formulae.
- Manually compare the regenerated record against TOGO M2950 and M2238 to
  confirm the trace-metal rows are 0.2 and 0.05 g/L, Tween 80 remains a 1 ml
  row, pH is present, and `high_metal` is absent.

## Additional Notes

The maintained M2950 and M2238 records have not yet received the M2242/M2804
repair pattern that corrected liter and milliliter units in the neighboring
Oxoid MRS records.
