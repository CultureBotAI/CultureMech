# YAML Record Review: bbm_v

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bbm_v.yaml
- Started UTC: 2026-09-21T19:12:29Z
- Finished UTC: 2026-09-21T19:14:45Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bbm_v.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bbm_v.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:001244` |
| Name | `bbm_v` |
| Original name | `BBM + V` |
| Source | DSMZ/MediaDive Medium `1802` |
| Merge status | Generated one-source merge from `bbm_v` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated MediaDive 1802 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this MediaDive import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated MediaDive 1802 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bbm_v.yaml` denotes DSMZ/MediaDive Medium 1802, BBM + V, and resolves to `CultureMech:001244` from `data/normalized_yaml/bacterial/bbm_v.yaml`.
- The historical `DSMZ_Medium1802.pdf` URL returned a DSMZ 404 page, but the public MediaDive text endpoint for Medium 1802 resolved and exposed the source recipe.
- Exact gitignore-independent searches for `CultureMech:001244`, `mediadive.medium:1802`, `Source: DSMZ, ID: 1802`, and `name: bbm_v` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, generated merge, generated indexes, and concentration-plausibility reports; no local raw Medium 1802 dump was present.
- The source is a nested stock recipe. The YAML instead stores every numeric addition volume from the Bristol's Solution, PIV metal solution, and BBM + V Vitamine Mix sections as `G_PER_L` top-level ingredient concentrations.

## Evidence

- MediaDive Medium 1802 lists six Bristol's Solution stock additions in `ml`: `0.833` ml `NaNO3 (300 g/l stock solution)`, `6.94` ml `CaCl2 x 2 H2O (3.6 g/l stock solution)`, `10` ml `MgSO4 x 7 H2O (7.5 g/l stock solution)`, `17.24` ml `K2HPO4 x 3 H2O (4.35 g/l stock solution)`, `10` ml `KH2PO4 (17.5 g/l stock solution)`, and `0.1` ml `NaCl (250 g/l stock solution)`. The YAML has the same six values as `G_PER_L`.
- The same source says to make Bristol's Solution to 1000 ml with Milli-Q water, autoclave it, and then add 6 ml filter-sterilized PIV metal solution plus 300 ul filter-sterilized BBM + V Vitamine Mix. The YAML flattens both stocks' components into top-level ingredient rows.
- PIV metal solution is its own 1000 ml stock containing 750 mg Na-EDTA plus five metal-salt stock additions, with pH adjustment to 6.4 before filter sterilization. The YAML stores `30.97`, `10.25`, `4.16`, `0.33`, and `20` as gram-per-litre top-level concentrations.
- BBM + V Vitamine Mix is its own 30 ml stock containing thiamine, biotin, and vitamin B12 stock additions. The YAML stores `10`, `10`, and `0.75` as gram-per-litre top-level concentrations.

## Completeness

- Consequential gaps:
  - All 15 ingredient quantities have the wrong unit and scope: source addition volumes were imported as final `G_PER_L` concentrations.
  - PIV metal solution and BBM + V Vitamine Mix are flattened instead of modeled as separate filter-sterilized stock solutions added to Bristol's Solution.
  - The record scopes PIV metal solution's pH 6.4 adjustment to the top-level medium via `ph_value: 6.4`.
  - Water make-up instructions are represented as literal HTML fragments rather than normalized volume instructions.
  - `NaNO3` and `Thiamine` still carry legacy `mediaingredientmech_term` values after the June 2026 MIM migration.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the inspected MediaDive text gives only a medium formulation and no strain-specific growth assay.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw DSMZ/MediaDive Medium 1802 dump under `data/raw` or `data/import_tracking`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | Source addition volumes were imported as gram-per-litre final concentrations. | MediaDive 1802 gives `ml` or `ul` additions from stock solutions for all 15 rows; the YAML stores those same numbers as `G_PER_L`. | `data/normalized_yaml/bacterial/bbm_v.yaml` and likely the MediaDive importer. |
| Major | Nested PIV and vitamin stocks were flattened into the top-level medium. | MediaDive 1802 says the Bristol's Solution receives 6 ml PIV metal solution and 300 ul BBM + V Vitamine Mix after cooling, while the YAML lists the PIV salts and vitamins directly as top-level ingredients. | The normalized owner; broad repeats may require the MediaDive importer. |
| Major | The PIV stock's pH is over-scoped to the final medium. | The source pH 6.4 adjustment is inside the PIV metal solution section after adding metal salts; the YAML stores `ph_value: 6.4` on the `MediaRecipe`. | The normalized owner. |
| Minor | Preparation text still contains HTML fragments from the MediaDive text export. | The YAML step descriptions contain `<span class='amount' data-volume='1000'>1000</span>` and `<span class='amount' data-volume='30'>30</span>` instead of plain quantities. | The normalized owner or text importer cleanup. |
| Minor | Two ingredients still use legacy MIM identifiers. | `NaNO3` and `Thiamine` retain `mediaingredientmech_term` values while the other thirteen rows were migrated to CHEBI-keyed `mediaingredientmech_chebi_term` links. | The normalized owner. |

## Recommended Edits

1. Rebuild `data/normalized_yaml/bacterial/bbm_v.yaml` from the MediaDive 1802 text with addition volumes kept as volumes, source stock concentrations kept in notes or nested stock definitions, and no volume values stored as `G_PER_L`.
2. Model PIV metal solution and BBM + V Vitamine Mix as filter-sterilized nested stocks added to the autoclaved Bristol's Solution at 6 ml/L and 300 ul/L.
3. Scope the pH 6.4 adjustment to the PIV metal solution instead of the top-level BBM + V medium.
4. Strip MediaDive HTML from preparation steps while preserving the final 1000 ml and 30 ml make-up volumes.
5. Replace the legacy `NaNO3` and `Thiamine` MediaIngredientMech links with CHEBI-keyed representations.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Re-fetch MediaDive `/text/1802` and manually compare each addition volume, stock concentration, pH scope, make-up volume, autoclave step, and filter-sterilization step against the YAML.
- Regenerate the generated merge and run `just verify-merges` plus `just audit-merge-freshness`.
- Rebuild import/content manifests and confirm `bacterial/bbm_v.yaml` no longer appears in `data/import_tracking/reports/concentration_plausibility.tsv` as trace-salt stock or vitamin unit slips.

## Additional Notes

- The generated merge is in sync with `data/normalized_yaml/bacterial/bbm_v.yaml`; the scientific issues above are inherited from that maintained input.
- Exact gitignore-independent searches included ignored files where present.
