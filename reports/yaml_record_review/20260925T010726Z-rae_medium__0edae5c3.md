# YAML Record Review: RAE Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/rae_medium__0edae5c3.yaml
- Started UTC: 2026-09-25T01:06:08Z
- Finished UTC: 2026-09-25T01:07:26Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/rae_medium__0edae5c3.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008639` |
| Label | `RAE Medium` |
| Category | `bacterial` |
| Source identity | `TOGO:M2049`, imported from NBRC `NBRC_M1349` |
| Generated status | Generated singleton with `merge_fingerprint: 0edae5c35d9ec23f80a259e11056f91e97a0ac403f0e88df9779f39d996ce1a1`; owned upstream by `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/rae_medium__0edae5c3.yaml` exited 0 with no diagnostics. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/rae_medium__0edae5c3.yaml --out /private/tmp/rae_medium_0edae5c3.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed trivially; `linkml-reference-validator validate data data/merge_yaml/merged/rae_medium__0edae5c3.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 checks, and reported all validations passed. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/rae_medium__0edae5c3.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- TOGO M2049 identifies the medium as `RAE Medium`, records `original_media_id: NBRC_M1349`, cites the NBRC Medium 1349 URL, and records pH 3.8.
- Live NBRC Medium 1349 resolves to `RAE Medium` and lists the same eight components and the same preparation guidance exposed through TOGO.
- The CultureMech ID, label, category, and source identity are coherent.
- Three liquid ingredients are unit-converted incorrectly, and the pH/preparation text is missing.

## Evidence

- Supported:
  - TOGO M2049 and NBRC Medium 1349 support the target identity, liquid state, eight component names, and pH 3.8.
  - Yeast extract, Na2HPO4 x 2 H2O, glucose, citric acid x H2O, and peptone have source-supported gram amounts.
- Unsupported or over-scoped:
  - Distilled water is represented as `980 G_PER_L`, but TOGO and NBRC list 980 ml.
  - Absolute ethanol is represented as `10 G_PER_L`, but TOGO and NBRC list 10 ml.
  - Glacial acetic acid is represented as `10 G_PER_L`, but TOGO and NBRC list 10 ml.
  - The NBRC pH statement and the comments about omitting acetic acid and ethanol before autoclaving are not represented.

## Completeness

- The current maintained owner has the same three liquid-unit errors and also lacks `ph_value`, structured `preparation_steps`, and `references`.
- NBRC Medium 1349 describes both liquid and solid preparation. The record's `LIQUID` state is not itself wrong, but the solid double-layer option should be preserved as source preparation context or a variant note.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `TOGO:M2049`, `NBRC_M1349`, `NO=1349`, `CultureMech:008639`, `TOGO_M2049_RAE_Medium`, and `rae_medium__0edae5c3` found the maintained owner, the generated target, source indexes, a corpus-scope report entry, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Major | Distilled water has the wrong unit. | NBRC Medium 1349 and TOGO M2049 list 980 ml distilled water; the generated artifact and maintained owner encode it as `980 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml` |
| Major | Absolute ethanol has the wrong unit. | NBRC Medium 1349 and TOGO M2049 list 10 ml absolute ethanol; the YAML encodes it as `10 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml` |
| Major | Glacial acetic acid has the wrong unit. | NBRC Medium 1349 and TOGO M2049 list 10 ml glacial acetic acid; the YAML encodes it as `10 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml` |
| Major | The pH and sterilization/preparation guidance are missing. | TOGO M2049 records pH 3.8, and NBRC explains that the base medium is autoclaved without glacial acetic acid and absolute ethanol, which are sterilized separately by filtration or in sealed screw-cap glass bottles. | `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml` |

## Recommended Edits

1. Change Distilled water, Absolute ethanol, and Glacial acetic acid from `G_PER_L` to source-faithful liquid-volume units in `data/normalized_yaml/bacterial/TOGO_M2049_RAE_Medium.yaml`.
2. Add `ph_value: 3.8` and structured references for TOGO M2049 and NBRC Medium 1349.
3. Add preparation steps preserving the NBRC sterilization order: autoclave without glacial acetic acid and ethanol, then add those two components after separate sterilization.
4. Add a note or a child variant for the NBRC solid-medium double-layer preparation instead of losing that source-supported option.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/rae_medium__0edae5c3.yaml`.
- Re-run a gitignore-independent exact search for `TOGO:M2049`, `NBRC_M1349`, `NO=1349`, and `CultureMech:008639` to confirm the NBRC source remains auditable after the owner and generated copy are repaired.
- Re-fetch TOGO M2049 and NBRC Medium 1349 to verify the 980 ml, 10 ml, 10 ml, pH 3.8, and separate-sterilization claims.

## Additional Notes

- The current `physical_state: LIQUID` is source-compatible because NBRC gives the liquid recipe first. The solid double-layer method is an additional supported preparation, not evidence that this record's top-level liquid state must be replaced.
