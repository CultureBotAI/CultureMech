# YAML Record Review: maf6_se

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/maf6_se__bd9e60e0.yaml`
- Started UTC: `2026-09-23T22:34:44Z`
- Finished UTC: `2026-09-23T22:34:44Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:000356` |
| Record name | `maf6_se` |
| Original name | `MAF6:SE` |
| Source accession | `mediadive.medium:C46` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/maf6_se.yaml` |
| Generated record | `data/merge_yaml/merged/maf6_se__bd9e60e0.yaml` |

`data/merge_yaml/merged/maf6_se__bd9e60e0.yaml` is a generated merge from
`data/normalized_yaml/bacterial/maf6_se.yaml` with fingerprint
`bd9e60e0bc2f54e1b1145023ca3e84fa76d10764a2bf8c029cd09eaadad498c4`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/maf6_se__bd9e60e0.yaml` | Passed. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/maf6_se__bd9e60e0.yaml --out /private/tmp/maf6_se__bd9e60e0.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows emitted to `/private/tmp/maf6_se__bd9e60e0.strict.tsv`. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/maf6_se__bd9e60e0.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/maf6_se__bd9e60e0.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The MediaDive source identity is correct: the `C46` REST record is named
  `MAF6:SE`, has source `CCAP`, links to the CCAP `MR_MAF6_SE.pdf`, and carries
  pH 6.6.
- The biological category is wrong. The inspected CCAP PDF describes MAF6:SE as
  a freshwater green algae medium, and the same CCAP accession already has a
  maintained algae import at `data/normalized_yaml/algae/maf6_se.yaml`.
- The bacterial import is an unreconciled duplicate of `CultureMech:000081`.
  An ignored-file-inclusive exact search for `MAF6:SE` across
  `data/normalized_yaml`, `data/merge_yaml`, import-tracking reports,
  `reports/media_content_review_manifest.tsv`, and `reports/archive` found both
  the algae `CultureMech:000081` CCAP import and this bacterial
  `CultureMech:000356` MediaDive/CCAP C46 import.
- An ignored-file-inclusive exact search for `CultureMech:000356` across
  `data`, `src`, `reports`, and `.claude` found this maintained bacterial
  input, this generated merge, current catalog/index entries, and historical
  import reports. It did not find any other live maintained record with this ID.
- An ignored-file-inclusive exact search for `mediadive.medium:C46` across
  `data`, `src`, `reports`, and `.claude` found only this maintained input,
  this generated merge, and indexes/reports derived from them.

## Evidence

- The inspected CCAP PDF and MediaDive C46 agree that final MAF6:SE is prepared
  per liter from 100 ml stock solution 1, 6 ml trace element solution, 1 ml
  vitamin B1 solution, 1 ml vitamin B12 solution, 30 ml SE2 soil extract, and
  distilled water to final volume.
- The generated record flattens all stock recipes at stock strength as direct
  final ingredients. `NaNO3`, `NH4NO3`, `MgSO4 x 7 H2O`, `K2HPO4 x 3 H2O`,
  `KH2PO4`, `CaCl2 x 2 H2O`, and `MES` belong to a 100 ml/L stock addition, not
  to seven direct final-medium rows at the stock g/L values.
- `Na2-EDTA`, `FeCl3 x 6 H2O`, `MnCl2 x 4 H2O`, `ZnCl2 x 6 H2O`,
  `CoCl2 x 6 H2O`, and `Na2MoO4 x 2 H2O` belong to a trace-element stock that is
  added at 6 ml/L.
- The vitamin B1 row is a 1.2 g/L stock of thiamine hydrochloride added at
  1 ml/L, not a 1.2 g/L direct final ingredient.
- The vitamin B12 row loses the source dilution: CCAP makes an initial
  cyanocobalamin solution, dilutes 1 ml of it into 99 ml water, filter
  sterilizes that working solution, and adds 1 ml/L to the final medium.
- `Soil` at 159.091 g/L is the MediaDive representation of the SE2 stock made
  from 105 g soil and 660 ml deionized water. It should not appear as a direct
  final MAF6:SE ingredient.
- The source supports the final pH 6.6 and final autoclave step recorded on this
  import.
- The record has source evidence only in `notes`, `media_term`, and import
  history. It lacks structured `sources` or `references`, so reference
  validation executed 0 checks.

## Completeness

- Consequentially incomplete: the final medium has no representation of nested
  stock additions as nested stocks, solution links, or final dilutions.
- Consequentially incomplete: the bacterial category and duplicate
  `CultureMech` ID have not been reconciled with the existing algae MAF6:SE
  import.
- Consequentially incomplete: the CCAP PDF URL and MediaDive C46 accession are
  not present in structured provenance fields.
- Empty optional target-organism and growth-evidence fields were not treated as
  defects. The inspected CCAP and MediaDive sources are recipe sources, not
  primary growth reports for a named organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | This live record is a bacterial duplicate of the algae MAF6:SE record. | CCAP MAF6:SE is a freshwater green algae recipe; `data/normalized_yaml/algae/maf6_se.yaml` already represents the same source as `CultureMech:000081`, while this MediaDive import stores CCAP C46 as `category: bacterial` and `CultureMech:000356`. | `data/normalized_yaml/bacterial/maf6_se.yaml`, the MediaDive import pipeline, and the CCAP/MediaDive deduplication workflow. |
| Major | Every nested stock recipe is flattened as if it were final-medium chemistry. | CCAP and MediaDive C46 model stock solution 1 at 100 ml/L, trace element solution at 6 ml/L, vitamin B1 at 1 ml/L, vitamin B12 at 1 ml/L, and SE2 at 30 ml/L; the generated record stores stock-strength ingredient rows directly on MAF6:SE. | `data/normalized_yaml/bacterial/maf6_se.yaml` or, preferably, the de-duplicated surviving MAF6:SE source record. |
| Major | SE2 is reduced to direct final soil chemistry. | The source prepares SE2 separately from 105 g air-dried sieved soil and 660 ml deionized water, lets it settle, and adds 30 ml extract per liter of final MAF6:SE; the generated record stores `Soil` at `159.091 G_PER_L` on the final medium. | `data/normalized_yaml/bacterial/maf6_se.yaml` and the MediaDive nested-solution import logic. |
| Major | Vitamin stock concentrations and dilutions are over-scoped to the final medium. | The `1.2 G_PER_L` thiamine and `1 G_PER_L` cyanocobalamin values are stock concentrations in MediaDive, and CCAP further dilutes the B12 stock before final addition. | `data/normalized_yaml/bacterial/maf6_se.yaml` and the MediaDive nested-solution import logic. |
| Minor | Structured provenance is missing. | The CCAP PDF and MediaDive accession appear in free-text slots, but there are no `sources` or `references` entries; the focused reference validator therefore had 0 checks. | `data/normalized_yaml/bacterial/maf6_se.yaml` if it remains active after deduplication. |

## Recommended Edits

1. Reconcile `data/normalized_yaml/bacterial/maf6_se.yaml` with the existing
   `data/normalized_yaml/algae/maf6_se.yaml`: either retire the bacterial
   duplicate or merge `mediadive.medium:C46` into the algae record as secondary
   source provenance.
2. Preserve the final-medium boundary for MAF6:SE as additions of stock solution
   1, trace element solution, vitamin B1 solution, vitamin B12 solution, and SE2
   rather than direct stock-strength ingredients.
3. Represent SE2 as a 30 ml/L constituent stock addition, not as final `Soil`.
4. Preserve the vitamin B12 dilution before the final 1 ml/L addition.
5. Add structured provenance for CCAP `MAF6_SE`, the CCAP PDF URL, and
   `mediadive.medium:C46` on the surviving record.
6. Regenerate `data/merge_yaml/merged/` after the normalized-source repair.

## Follow-up Checks

- Run open-schema validation for the corrected normalized record and generated
  merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and generated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference validation after adding structured CCAP and MediaDive
  provenance.
- Run term validation after any nested stock repair changes ingredient rows.
- Rerun a gitignore-independent exact search for `CultureMech:000356`,
  `CultureMech:000081`, `MAF6:SE`, and `mediadive.medium:C46` to verify the
  duplicate has been resolved.
- Manually compare the surviving regenerated record against the CCAP PDF and
  MediaDive C46 REST record to verify that the five stock additions, pH 6.6,
  final autoclaving, and SE2 boundary are all represented.

## Additional Notes

- MediaDive C46 itself preserves the stock boundaries in separate nested
  solutions; the flattening happened in the CultureMech MediaDive import.
- `data/normalized_yaml/algae/maf6_se.yaml` has already had some CCAP
  instruction-row repairs, but it still needs deeper final-medium curation
  before it can fully subsume this MediaDive provenance.
