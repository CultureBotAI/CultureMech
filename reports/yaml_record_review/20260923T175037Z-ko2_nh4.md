# YAML Record Review: ko2_nh4

- Repository: CultureMech
- Record: data/merge_yaml/merged/ko2_nh4.yaml
- Started UTC: 2026-09-23T17:50:02Z
- Finished UTC: 2026-09-23T17:50:37Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:008320`, the generated `ko2_nh4` record merged from `data/normalized_yaml/bacterial/ko2_nh4.yaml`. The record represents TOGO `M1756`, NBRC `M968`, "KO2(NH4)", as a bacterial, complex, undefined, solid agar medium.

## Validation

- LinkML open schema validation passed for `data/merge_yaml/merged/ko2_nh4.yaml`.
- Strict validation passed with 0 error rows in `/private/tmp/ko2_nh4.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` entries were not checked: the available history validator validates standalone files under `history/`, not embedded generated YAML history.

## Identity and Grounding

An ignored-inclusive exact search for `CultureMech:008320`, `TOGO:M1756`, `NBRC_M968`, and `NO=968` found only the active TOGO/NBRC owner, this generated record, and exact index rows for the source. No exact local duplicate was found.

## Evidence

NBRC Medium 968 and TOGO M1756 list, per 1 L distilled water, 1.32 g ammonium sulfate, 0.27 g KH2PO4, 0.25 g MgSO4.7H2O, 0.11 g CaCl2.6H2O, 0.02 g FeSO4.7H2O, 0.03 g EDTA-2Na, 1 ml N1 Metals solution, 20 g agar if needed, and pH 2. The N1 Metals stock is a separate 1 L stock containing 2.86 g H3BO3, 1.81 g MnCl2.4H2O, 0.22 g ZnSO4.7H2O, 0.08 g CuSO4.5H2O, 15 mg MoO3, and 1 L distilled water.

## Completeness

The base salts, optional agar, and N1 Metals labels are present, but the generated record loses the pH, final/stock boundary, optional agar semantics, and the source water volumes.

## Findings

- The generated record is stale relative to `data/normalized_yaml/bacterial/ko2_nh4.yaml`: the owner has already collapsed the duplicate water row back to `1.0`, while the generated file still stores `Distilled water` as `2.0 G_PER_L`.
- `ph_value: 2.0` is absent even though NBRC and TOGO both list pH 2.
- `N1 Metals solution*` is an empty `Unknown solution` with `1 G_PER_L`, but the source lists it as a 1 ml stock addition.
- N1 Metals stock components are also flattened as top-level ingredients at stock strength instead of remaining inside the 1 ml stock solution.
- `MoO3` has an additional 1000x unit slip: the N1 Metals source row is 15 mg per 1 L stock, but the generated record stores `15 G_PER_L`.
- The optional `Agar (if needed)` row forces the whole record to `physical_state: SOLID_AGAR`; the liquid base and optional 20 g/L agar form should be represented without making agar unconditional.
- Once the N1 stock is structured, the recipe appears chemically defined; the current `COMPLEX`/`UNDEFINED` classification is an artifact of the empty `Unknown solution` placeholder.

## Recommended Edits

- Repair the normalized owner so N1 Metals is a 1 ml stock solution with its own 1 L composition and water.
- Convert N1 MoO3 to `0.015 G_PER_L` inside the stock, then let the 1 ml stock addition supply the final trace amount.
- Add the pH 2 source value.
- Preserve agar as optional or split the liquid and agar variants rather than making every KO2(NH4) record solid.
- Regenerate `data/merge_yaml/merged/ko2_nh4.yaml` so the generated water row reflects the already repaired owner.

## Follow-up Checks

- Re-run the open schema, strict, reference, and term validators against regenerated KO2(NH4).
- Confirm the generated record has a structured 1 ml N1 Metals solution and no top-level N1 stock-only components.
- Confirm MoO3 is no longer present as a `15 G_PER_L` top-level ingredient.

## Additional Notes

The exact search included ignored files and hidden files.
