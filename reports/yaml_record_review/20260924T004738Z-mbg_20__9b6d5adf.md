# YAML Record Review: mbg_20

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mbg_20__9b6d5adf.yaml
- Started UTC: 2026-09-24T00:46:47Z
- Finished UTC: 2026-09-24T00:47:38Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:003008 |
| Label | mbg_20 |
| Original label | MBG-20 |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mbg_20__9b6d5adf.yaml |
| Maintained owner | data/normalized_yaml/bacterial/mbg_20.yaml |
| Source accession | mediadive.medium:J662 |

The reviewed YAML is the stale 2026-08-06 generated copy of the MediaDive J662
MBG-20 record. Its maintained owner was corrected on 2026-09-11, so future work
should regenerate `data/merge_yaml/merged/` and then verify the J662 and TOGO
M680 source-duplicate pair.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mbg_20__9b6d5adf.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mbg_20__9b6d5adf.yaml --out /private/tmp/mbg_20__9b6d5adf.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mbg_20__9b6d5adf.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mbg_20__9b6d5adf.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The record is schema-valid but stale relative to its maintained owner.

## Identity and Grounding

- MediaDive J662 and JCM GRMD 662 both identify this source as JCM `MBG-20`,
  pH 7.0, with 16 g peptone, 4 g yeast extract, 5 g glucose, 40 g Sea salts
  (Sigma), and 1 L distilled water.
- A scoped, gitignore-independent exact search found `CultureMech:003008` and
  `mediadive.medium:J662` pointing to `data/normalized_yaml/bacterial/mbg_20.yaml`
  in the ID registry and normalized MediaDive/recipe indexes.
- The exact same search found the repaired duplicate TOGO M680 record,
  `CultureMech:010085`, in `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`.
- A gitignore-independent scan of the generated target and normalized owner
  found `variant_children`, `data_quality_flags`, and `references` only in the
  normalized owner.

## Evidence

### Supported

- The generated record correctly carries `mediadive.medium:J662`, the MBG-20
  source identity, pH 7.0, and the source amounts for peptone, yeast extract,
  glucose, and Sea Salt.
- Its compressed preparation text preserves the pH adjustment, Sea-salts
  filtration, separate autoclaving of peptone/yeast/glucose solutions, sterile
  addition to Sea salts, and three argon degassing/charging cycles.

### Unsupported or Over-scoped

- The generated record omits the source's 1 L distilled-water row entirely.
  MediaDive J662 and JCM 662 both include distilled water, and the normalized
  owner already restored it as `unit: L`.
- The generated `Sea Salt` row drops the Sigma supplier scope from JCM's `Sea
  salts (Sigma)` and MediaDive's Sigma attribute.
- The generated preparation structure assigns the whole second source sentence
  to one `AUTOCLAVE` action even though that sentence also contains filtration,
  sterile mixing, and anaerobiosis by argon cycling.
- The generated record omits the owner's references and reciprocal
  source-duplicate relationship to TOGO M680.

## Completeness

- The September normalized owner has already added exact JCM/MediaDive
  references, restored water, normalized Sea salts (Sigma), kept the sea-salt
  mixture intentionally ungrounded, and linked `CultureMech:010085` as a
  `SOURCE_DUPLICATE`.
- `target_organisms` is empty in both the generated target and normalized owner.
  JCM 662 and MediaDive J662 do not assert a strain-specific growth claim, so
  that empty optional slot is acceptable.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The reviewed generated MediaDive artifact is stale relative to its normalized owner. | `data/merge_yaml/merged/mbg_20__9b6d5adf.yaml` stops at its 2026-08-06 merge event; `data/normalized_yaml/bacterial/mbg_20.yaml` has a 2026-09-11 repair that restored the JCM 662 water row, normalized Sea salts (Sigma), added structured references, and linked TOGO M680 as a source duplicate. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/mbg_20.yaml`. |
| major | The generated record omits the JCM/MediaDive distilled-water row. | JCM 662 lists 1.0 L Distilled water and MediaDive J662 preserves 1000 ml Distilled water; the generated YAML has no water row, while the normalized owner has `Distilled water` with `unit: L`. | Already fixed in `data/normalized_yaml/bacterial/mbg_20.yaml`; regenerate and verify. |
| minor | The generated record loses the Sigma supplier and action boundaries. | JCM says `Sea salts (Sigma)`, and the preparation sentence mixes filtration, autoclaving, sterile addition, and argon cycling; the generated YAML says `Sea Salt` and stores the full second sentence under `AUTOCLAVE`. | Already fixed in `data/normalized_yaml/bacterial/mbg_20.yaml`; regenerate and verify. |
| minor | The generated record omits structured source references and duplicate metadata. | The normalized owner has JCM/MediaDive `references` and `variant_children` pointing to `CultureMech:010085`; the reviewed generated file has neither. | Already fixed in `data/normalized_yaml/bacterial/mbg_20.yaml`; regenerate and verify merge support for these fields. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the current normalized MBG-20
   owners.
2. Confirm the regenerated MediaDive J662 artifact includes the 1 L water row,
   `Sea salts (Sigma)`, split preparation steps, JCM/MediaDive references, and
   the `SOURCE_DUPLICATE` link to `CultureMech:010085`.
3. Revisit merge deduplication if the desired generated output is one collapsed
   MBG-20 source record rather than two records linked as source duplicates.

## Follow-up Checks

- Rerun the focused open-schema, strict, term, and reference validators on the
  regenerated `data/merge_yaml/merged/mbg_20__9b6d5adf.yaml`.
- Run merge freshness and duplicate checks for `CultureMech:003008` and
  `CultureMech:010085`.
- Repeat gitignore-independent exact searches for `mediadive.medium:J662` and
  `TOGO:M680` in the normalized and generated indexes after regeneration.

## Additional Notes

- This review found a stale generated-artifact problem. The normalized J662 owner
  is already aligned with the inspected JCM 662 and MediaDive J662 source
  records.
