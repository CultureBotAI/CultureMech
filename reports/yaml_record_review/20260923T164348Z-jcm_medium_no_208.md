# YAML Record Review: JCM MEDIUM No. 208

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_208.yaml
- Started UTC: 2026-09-23T16:43:21Z
- Finished UTC: 2026-09-23T16:43:48Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_208.yaml`
- Stable ID: `CultureMech:002569`
- Name: `jcm_medium_no_208`
- Original name: `JCM MEDIUM No. 208`
- Category: `bacterial`
- Media term: `mediadive.medium:J208`
- Source: MediaDive import of JCM Medium 208, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=208`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_208.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_208`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_208.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_208.yaml --out /private/tmp/jcm_medium_no_208.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_208.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_208.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators do not detect the dimension error where a milliliter YM-broth addition becomes a gram-per-liter mass concentration.

## Identity and Grounding

The record identifies MediaDive `J208` for JCM Medium 208. Live JCM, MediaDive, and TogoMedium M201 all point to `GRMD=208`/`JCM_M208` and agree that the recipe is a solid glucose/YM broth/agar medium.

The generated record has the correct source accession, physical state, and glucose and agar amounts normalized to the 75 ml recipe volume. Its YM broth row is not exact: JCM and MediaDive specify 75 ml YM broth (BD-Difco), while the generated row stores `75 G_PER_L`.

## Evidence

Inspected evidence:

- Live JCM `GRMD=208` lists 25 g glucose, 75 ml YM broth (BD-Difco), and 2 g agar.
- MediaDive REST `medium/J208` preserves the same three rows and reports a `volume` of 75 ml for the main solution.
- TogoMedium M201 points to `JCM_M208` and the same JCM `GRMD=208` page.

Supported:

- The MediaDive ID, JCM link, bacterial category, solid agar state, and glucose and agar quantities are supported by JCM and MediaDive.
- The high `333.333 G_PER_L` glucose and `26.6667 G_PER_L` agar values match MediaDive's normalization of 25 g and 2 g against a 75 ml main volume.

Unsupported or incomplete:

- The generated `YM broth` row is dimensionally unsupported: the source gives 75 ml of prepared YM broth, not 75 g/L.
- The generated `YM broth` row drops the `BD-Difco` supplier qualifier.

## Completeness

Consequential gaps:

- The YM broth source row needs a volume or prepared-broth representation rather than a mass concentration.

Empty slots that are acceptable:

- The source does not state pH, preparation steps beyond the default JCM autoclaving statement, or target-organism/growth-metric claims.

Bounded negative search:

- An ignored-file-inclusive owner search with `rg --no-ignore --hidden` across `data/normalized_yaml` and `data/merge_yaml/merged` for `Source: JCM, ID: J208`, `Original source: JCM - JCM_M208`, and exact `jcm_medium_no_208` found only the direct MediaDive/JCM record and the TOGO M201 wrapper as source owners for JCM 208.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | YM broth is modeled as a gram-per-liter ingredient even though the source gives a volume. | JCM and MediaDive state 75 ml YM broth (BD-Difco). MediaDive does not supply a `g_l` value for this row, but the generated record stores `value: '75'`, `unit: G_PER_L`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_208.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive owner search found `data/normalized_yaml/bacterial/togo_medium_m201.yaml` and `data/merge_yaml/merged/togo_medium_m201.yaml` pointing to `JCM_M208` and `GRMD=208`. The TOGO wrapper also models 75 ml YM broth as `75 G_PER_L` and keeps source amounts for glucose and agar where the direct MediaDive record normalizes them to the 75 ml recipe volume. | Both normalized owners, plus merge regeneration after source repair. |
| Minor | The YM broth supplier qualifier is missing. | JCM, MediaDive, and TogoMedium preserve `BD-Difco`; the target stores only `YM broth`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_208.yaml`. |

## Recommended Edits

1. Represent YM broth as a 75 ml prepared-broth addition or an equivalent structured stock/solution, not as `75 G_PER_L`.
2. Preserve the `BD-Difco` supplier qualifier on YM broth.
3. Repair `data/normalized_yaml/bacterial/togo_medium_m201.yaml` in parallel so its duplicate JCM 208 import uses the same YM-broth representation and comparable quantity normalization, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on repaired normalized records and regenerated merged records.
- Compare the regenerated direct JCM 208 record against MediaDive `medium/J208` and confirm the three source rows remain present.
- Manually verify that YM broth is no longer represented as a mass concentration.
- Search with `rg --no-ignore --hidden 'Source: JCM, ID: J208|Original source: JCM - JCM_M208' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm same-source duplicate handling with ignored files included.

## Additional Notes

- No standalone JCM preparation paragraph exists on `GRMD=208`; applying the JCM page default of autoclaving at 121 C for 15 min is enough unless a later curated source overrides it.
