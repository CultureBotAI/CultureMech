# YAML Record Review: bcye_agar__f1fbf90e

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bcye_agar__f1fbf90e.yaml
- Started UTC: 2026-09-21T19:25:38Z
- Finished UTC: 2026-09-21T19:27:06Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bcye_agar__f1fbf90e.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:006055` |
| Name | `bcye_agar` |
| Original name | `BCYE AGAR` |
| Source | KOMODO Medium `585`, annotated as DSMZ Medium 585 |
| Merge status | Generated one-source merge from `KOMODO_585_BCYE_AGAR` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated KOMODO 585 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this KOMODO import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated KOMODO 585 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bcye_agar__f1fbf90e.yaml` denotes KOMODO Medium 585, a KOMODO copy of DSMZ Medium 585 `BCYE AGAR`.
- The generated one-source merge and normalized KOMODO owner match for the scientific fields, so the issue is in `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml`.
- The inspected DSMZ 585 PDF gives two preparation routes: a commercial OXOID Legionella CYE-Agar base with Legionella BCYE supplement, and an explicit CYE-ACES AGAR alternative with solutions A through D.
- The KOMODO YAML says it copied DSMZ Medium 585, but it mixes the unquantified OXOID commercial shortcut with the explicit CYE-ACES alternative and stores the CYE-ACES stock concentrations as if they were final-medium concentrations.
- Exact gitignore-independent searches for `CultureMech:006055`, exact `komodo.medium:585`, `KOMODO_585_BCYE_AGAR`, `DSMZ Medium: 585`, and `DSMZ Medium 585` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, generated merge, DSMZ MediaDive sibling, DSMZ 585a sibling, indexes, and import reports, but no local raw KOMODO 585 or DSMZ Medium 585 source capture under `data`.

## Evidence

- DSMZ 585 lists explicit CYE-ACES stock solutions: solution A has 10 g yeast extract, 10 g ACES, 2 g activated charcoal, and 490 ml distilled water; solution B has 15 g agar and 490 ml distilled water; solution C has 0.40 g cysteine-HCl x H2O in 10 ml distilled water; solution D has 0.25 g Fe4(PO4)2 in 10 ml distilled water.
- The PDF combines solutions A through D into a complete 1 L medium. The final amounts are therefore 10 g/L yeast extract, 10 g/L ACES, 2 g/L activated charcoal, 15 g/L agar, 0.4 g/L cysteine-HCl x H2O, and 0.25 g/L Fe4(PO4)2.
- The YAML instead records 20.4082 g/L yeast extract and ACES, 4.08163 g/L activated charcoal, 30.6122 g/L agar, 40 g/L cysteine-HCl x H2O, and 25 g/L Fe4(PO4)2.
- DSMZ 585 does not support `OXOID Legionella CYE-Agar base` at `1000 G_PER_L`; it only states that DSMZ uses that commercial base with Legionella BCYE supplement as a dehydrated commercial preparation.
- The YAML has a pH range of 6.4 to 7.4, matching the DSMZ final adjustment to pH 6.9 with tolerance 0.5, but it does not preserve DSMZ's stock-solution preparation, final assembly, cooling, or filtration boundaries.

## Completeness

- Consequential gaps:
  - Stock-solution concentrations are used as final concentrations.
  - The OXOID base amount is unsupported.
  - The record has no solution A through D structure or distilled-water rows.
  - The final preparation sequence is absent.
  - The DSMZ source URL is only embedded in prose and should be represented as a structured reference when this copy is repaired.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the DSMZ 585 PDF is a formulation/protocol source and not a strain-specific growth assay.
  - The commercial OXOID shortcut should not be forced to a narrow ChEBI small-molecule term.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw KOMODO 585 source dump or DSMZ Medium 585 PDF capture under `data/raw`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record stores DSMZ stock concentrations as final concentrations. | DSMZ's CYE-ACES alternative sums to 1 L, but the KOMODO record preserves the same 20.4082, 4.08163, 30.6122, 40, and 25 g/L stock-concentration inflation seen in the DSMZ MediaDive sibling. | `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml`. |
| Major | `OXOID Legionella CYE-Agar base` at `1000 G_PER_L` is unsupported. | DSMZ says it uses an OXOID commercial preparation but gives no 1000 g/L amount for that base; the printed numeric formula belongs to the CYE-ACES alternative. | `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml`. |
| Major | The stock-solution and preparation boundaries are missing. | DSMZ prepares solutions A, B, C, and D separately, cools or filter-sterilizes them differently, then adjusts the complete medium to pH 6.9 before adding solution B; the YAML has no solution nodes or preparation steps. | `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml`. |
| Minor | The ACES row still uses the legacy `mediaingredientmech_term` slot. | This row has `term: CHEBI:39061` but retains `mediaingredientmech_term: MediaIngredientMech:000555` while neighboring migrated rows use `mediaingredientmech_chebi_term`. | `data/normalized_yaml/bacterial/KOMODO_585_BCYE_AGAR.yaml`; broad repeats may need the MIM legacy migration. |

## Recommended Edits

1. Rebuild the normalized KOMODO 585 record from the DSMZ 585 CYE-ACES formulation or mark it as a source duplicate of the repaired DSMZ 585 MediaDive record once that parent is corrected.
2. Preserve the solution A through D amounts, water volumes, sterilization conditions, and final assembly order.
3. Correct final concentrations to 10 g/L yeast extract, 10 g/L ACES, 2 g/L activated charcoal, 15 g/L agar, 0.4 g/L cysteine-HCl x H2O, and 0.25 g/L Fe4(PO4)2.
4. Remove the unsupported `1000 G_PER_L` OXOID ingredient or represent the OXOID route as a commercial shortcut without an invented amount.
5. Replace ACES' legacy `mediaingredientmech_term` with a CHEBI-keyed MIM link if the packaged MIM index still supports ACES.
6. Regenerate `data/merge_yaml/merged/bcye_agar__f1fbf90e.yaml` from the repaired normalized owner.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Manually compare the repaired KOMODO record against the DSMZ Medium 585 PDF line by line, checking all four stock solutions, pH adjustment, autoclave/filter boundaries, cooling temperatures, and final mixing order.
- Run `just verify-merges` and `just audit-merge-freshness --json --list` after regenerating.
- Re-check exact `CultureMech:006055`, exact `komodo.medium:585`, and `KOMODO_585_BCYE_AGAR` occurrences with a gitignore-independent search to verify only the normalized owner and fresh generated merge carry the repaired KOMODO 585 representation.

## Additional Notes

- The inspected upstream formula source was the DSMZ Medium 585 PDF.
- Exact gitignore-independent searches included ignored files where present.
