# YAML Record Review: cgm

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/CGM.yaml
- Started UTC: 2026-09-22T06:49:02Z
- Finished UTC: 2026-09-22T06:49:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:000058 |
| Name | cgm |
| Original name | CGM |
| Category | algae |
| Source | CCAP CGM, MR_CGM.pdf |
| Generated status | Generated merged output |
| Maintained owner | data/normalized_yaml/algae/cgm.yaml |

The reviewed file is the generated merge of one algae CCAP owner. Future fixes
belong in `data/normalized_yaml/algae/cgm.yaml` or the CCAP import/repair
logic, followed by merge/page regeneration.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/CGM.yaml` | Passed; no issues found. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/CGM.yaml --out /private/tmp/CGM.strict.tsv --workers 1 --quiet` | Passed with 0 errors; TSV written to `/private/tmp/CGM.strict.tsv`. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/CGM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/CGM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not checked | `just validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in the merged YAML. |

## Identity and Grounding

- The generated record, its `CCAP:CGM` reference, and the CCAP `MR_CGM.pdf`
  source all identify CGM, Bold Basal Medium with 3-fold nitrogen and vitamins,
  modified with added nutrients.
- The generated record is stale. It was produced on 2026-08-06, before the
  maintained owner gained the 2026-08-25 `REPAIRED_CCAP_INSTRUCTION_ROWS`
  event and the 2026-09-13 structured CCAP `sources` repair.
- A separate same-name bacterial/MediaDive owner,
  `data/normalized_yaml/bacterial/cgm.yaml`, also points at the CCAP CGM PDF
  through `mediadive.medium:C21`. It is a separate generated target
  (`data/merge_yaml/merged/cgm__c10d46e1.yaml`) and did not contribute to this
  algae `CultureMech:000058` record.

## Evidence

The inspected CCAP PDF gives a stock-based recipe:

| Source group | Source concentration | Dose into CGM |
|---|---:|---:|
| Stocks 1-6 | Six macronutrient stocks prepared per 100 ml | Stock 1 at 10 ml/L; stocks 2-6 at 1 ml/L each |
| Stock 7 | Trace elements prepared per 1 L | 6 ml/L |
| Stock 8 | Vitamin B1 prepared per 100 ml and filter-sterilized | 1 ml/L |
| Stock 9 | Vitamin B12 prepared per 100 ml, diluted 1:100, and filter-sterilized | 1 ml/L |

The reviewed generated record keeps proteose peptone, yeast extract, and
D-glucose from the final medium. Its Na EDTA, FeCl3 x 6 H2O, MnCl2 x 4 H2O,
ZnCl2, CoCl2 x 6 H2O, and Na2MoO4 x 2 H2O rows are the internal Stock 7 trace
element concentrations, not their final concentrations after adding 6 ml of
Stock 7 per litre. It also has no rows or stock references for CCAP Stocks 1-6,
Vitamin B1, or the diluted Vitamin B12 stock.

The generated ingredient named `Make up to 1 litre with deionised water. For
agar, add` is parsed instruction text. The maintained algae owner has already
removed it and moved the 15 g/L Bacteriological Agar instruction into
`preparation_steps` for the agar formulation.

## Completeness

- The generated record is missing structured `sources` from its maintained
  owner.
- The generated record is missing preparation steps for bringing the medium to
  1 L, adding 15 g/L agar only for the agar formulation, and autoclaving at
  15 psi for 15 minutes.
- Required CCAP Stocks 1-6 and vitamin stocks 8-9 are absent.
- Stock 7 trace element amounts are present in the wrong concentration scope.
- Exact `rg --no-ignore --hidden` searches over YAML for `CCAP:CGM`,
  `MR_CGM.pdf`, `CultureMech:000058`, and exact `name: cgm` included ignored
  and hidden files. They found the maintained algae owner, the separate
  bacterial/MediaDive CGM owner, and their two generated outputs.
- Empty optional slots such as pH were not treated as defects.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record is stale relative to its maintained owner. | `CGM.yaml` still contains a parsed agar-instruction ingredient and lacks `preparation_steps` and structured `sources`; `data/normalized_yaml/algae/cgm.yaml` removed that row on 2026-08-25 and added structured CCAP sources on 2026-09-13. | Regenerate from `data/normalized_yaml/algae/cgm.yaml` |
| Major | The CGM stock structure is incomplete and flattened with the wrong concentration scope. | CCAP doses six macronutrient stocks, one trace-elements stock, and two vitamin stocks into the final medium. The algae record only carries the trace-elements stock at stock concentrations plus three direct final-medium ingredients. | `data/normalized_yaml/algae/cgm.yaml`; CCAP repair logic |
| Major | Optional agar was represented as a liquid-medium ingredient in the generated record. | The source says to add 15 g/L Bacteriological Agar for agar; the generated liquid CGM record stores the prose as an ingredient with 15 g/L. | Already repaired in `data/normalized_yaml/algae/cgm.yaml`; regenerate |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/CGM.yaml` from
   `data/normalized_yaml/algae/cgm.yaml` so the stale parsed-instruction row is
   removed and the repaired preparation steps and structured sources are
   published.
2. Extend the maintained algae owner or CCAP repair transform to represent
   Stocks 1-6, Stock 7, Stock 8, and Stock 9 explicitly with their source
   concentrations and final addition volumes.
3. Either keep trace-element components inside Stock 7 or recalculate them
   using the 6 ml/L final dose before representing them as final-medium
   ingredients.
4. Recheck the separate bacterial/MediaDive `cgm.yaml` before any future
   de-duplication; it denotes the same CCAP PDF through `mediadive.medium:C21`
   but uses a solid-agar physical state and flattens many stock concentrations.

## Follow-up Checks

- Re-run open-schema and strict validation on the maintained algae owner and
  the regenerated `data/merge_yaml/merged/CGM.yaml`.
- Re-run term validation after adding Stocks 1-6 and vitamin stocks 8-9 because
  the new nitrate, calcium, magnesium, phosphate, chloride, thiamine, and B12
  rows will need exact chemical grounding.
- Manually compare the regenerated YAML against `MR_CGM.pdf` and verify each
  stock has the correct per-100-ml or per-litre source concentration and the
  correct final dose.

## Additional Notes

- The CCAP PDF text was extractable with `pypdf`; no OCR was needed.
- Shape validation passes because a prose ingredient label with a numeric agar
  concentration is schema-valid even when it is semantically malformed.
