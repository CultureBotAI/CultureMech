# YAML Record Review: ses_pj

- Repository: CultureMech
- Record: data/merge_yaml/merged/ses_pj.yaml
- Started UTC: 2026-09-25T05:27:30Z
- Finished UTC: 2026-09-25T05:27:30Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:000131`, `ses_pj`, from `data/merge_yaml/merged/ses_pj.yaml`.

The target record is the collection-level CCAP import for CCAP `SES:PJ`.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is correctly grounded to CCAP `MR_SES_PJ.pdf`.

It is a same-source duplicate of the direct MediaDive/CCAP C94 import in `data/merge_yaml/merged/ses_pj__43a41929.yaml`, but the two generated branches currently need different repairs before they can collapse cleanly.

## Evidence

CCAP `MR_SES_PJ.pdf` defines `SES:PJ` as a 1:1 mixture of separately autoclaved SES and PJ media.

SES is built from K2HPO4, MgSO4 x 7H2O, KNO3, soil extract, deionized water, and separate stock preparation steps.

PJ is built from CaCl2 x 2H2O, KCl, K2HPO4, MgSO4 x 7H2O, deionized water, and separate stock preparation steps.

## Completeness

The generated record has only `KCl` and `Soil is prepared as above.` parent ingredients; it does not represent the final 1:1 mixture.

The SE2 soil-extract preparation text is represented as a 105 g/L parent ingredient instead of being scoped under the SES constituent medium.

The direct MediaDive/CCAP C94 duplicate flattens SES and PJ stock components into a single parent ingredient list, sums same-named stock components across SES and PJ, duplicates SE2 soil-preparation prose, and keeps stale `bacterial` category metadata.

The repaired normalized collection source already models this medium with 500 ml/L SES and 500 ml/L PJ.

## Findings

Nested CCAP constituent media and stock recipes were not retained in the generated collection-level record.

Process text for SE2 soil extract was imported as a pseudo-ingredient.

The older direct MediaDive/CCAP branch remains separate from the repaired collection-level CCAP branch.

## Recommended Edits

Keep the repaired two-solution structure in `data/normalized_yaml/algae/ses_pj.yaml`.

Repair the direct CCAP branch or suppress it in favor of the repaired collection-level CCAP source.

Represent `SES:PJ` as 500 ml/L SES plus 500 ml/L PJ; do not flatten the two constituent recipes into parent rows.

Remove the 105 g/L `Soil is prepared as above.` ingredient from the generated parent record and keep SE2 soil handling only as scoped preparation text on the SES constituent medium.

Set the category to algae if the direct CCAP branch is retained.

Regenerate the merged YAML after the direct and collection CCAP branches are reconciled.

## Follow-up Checks

Confirm the regenerated `SES:PJ` duplicate set has one canonical record for `MR_SES_PJ.pdf`.

Confirm the regenerated parent has no top-level K2HPO4, MgSO4 x 7H2O, KNO3, CaCl2 x 2H2O, KCl, variable `Soil`, or `Soil is prepared as above.` rows.

Confirm the final record carries CCAP `SES_PJ` provenance and not bacterial category metadata.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
