# YAML Record Review: 1158

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1158.yaml`
- Started UTC: 20260921T043139Z
- Finished UTC: 20260921T043317Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007459` |
| Label | `1158` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1158.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1158]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1158.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import for NBRC Medium 1155. The maintained owner was later recovered from NBRC source HTML on Aug 19 and curated on Sep 11 to `TG agar`.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1158.yaml` | Pass; `No issues found` |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1158.yaml --out /private/tmp/culturemech-review-1158.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-1158.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1158.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1158.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The intended identity is NBRC Medium 1155:

- The import history says `Media No. 1155` and links the NBRC `NO=1155` page.
- Live NBRC Medium 1155 is titled `TG agar`.
- The maintained owner now carries `media_term: nbrc.medium:1155`, the official title, and a direct NBRC reference.

The generated file is identity-stale:

- `name` and `original_name` are both `1158`, but live NBRC Medium 1158 is a different `LB + Chloramphenicol medium`.
- The generated file has no `media_term`, no `references`, and only a stale `merged_from: [NBRC_1158]` source handle.
- `medium_type: DEFINED` and `composition_type: DEFINED` overstate a source formulation that includes commercial tryptone.

## Evidence

Supported by inspected source text:

- NBRC Medium 1155 is `TG agar`.
- NBRC Medium 1155 lists Tryptone (Diffico) 10 g, glucose 3 g, agar 10 g, and distilled water 1 L.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all four NBRC source rows into `Tryptone (Diffico)10gGlucose3gAgar10gDistilled water1L` with `VARIABLE` concentration.
- The second ingredient row has an empty `preferred_term` and another `VARIABLE` concentration.
- No generated ingredient separately represents tryptone 10 g/L, glucose 3 g/L, agar 10 g/L, or 1 L distilled water.
- The generated `ADJUST_PH` placeholder has no support in the inspected NBRC 1155 source.
- `preparation_steps[3].action` is `DISSOLVE` while its description is `Sterilize by autoclaving`.

## Completeness

- The generated record omits the Aug 19 recovered NBRC composition, the Aug 20 MIM ingredient groundings, and the Sep 11 `RESOLVED_NBRC_1158_SCORE15` normalized repair.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_1158`, `NO=1155`, `NBRC Medium 1155`, and `nbrc.medium:1155` found this stale generated NBRC direct import, the repaired normalized owner, normalized index entries, and a separate TOGO-derived `TG_agar` copy of NBRC Medium 1155.
- Empty growth-evidence fields are not a defect in this source-import record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading numeric identity for NBRC Medium 1155. | The file is labeled `1158`, while its import event points to NBRC Medium 1155; live NBRC Medium 1158 is LB + Chloramphenicol medium. | `data/normalized_yaml/bacterial/NBRC_1158.yaml` is repaired; regenerate `data/merge_yaml/merged/` and clean up the stale artifact. |
| Blocker | The NBRC composition is collapsed into malformed ingredient rows. | NBRC Medium 1155 has four source rows; the generated file has one concatenated ingredient blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1158.yaml`; fix NBRC import or merge source selection if the malformed rows persist. |
| Major | The generated medium/composition classification is unsupported. | NBRC Medium 1155 uses commercial Tryptone (Diffico), but the stale merge says `DEFINED` / `DEFINED`; the maintained owner now uses `COMPLEX` / `UNDEFINED`. | Regenerate from the repaired normalized owner. |
| Major | Curated source grounding and ingredient groundings are missing. | The maintained owner has `nbrc.medium:1155`, source-scoped notes, tryptone/glucose/agar/water rows, and CHEBI/MIM groundings where available; the generated merge has no `media_term` or grounded ingredients. | Regenerate the merge corpus. |
| Major | Placeholder preparation steps do not match the recovered source. | The generated record includes unsupported `ADJUST_PH` and marks autoclaving as `DISSOLVE`; the maintained owner now has dissolve plus `AUTOCLAVE` only. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1158.yaml`. |
| Minor | A TOGO-derived copy of NBRC Medium 1155 remains separate. | Gitignore-independent search found a separate generated `TG_agar.yaml` and normalized `tg_agar.yaml` with the same NBRC 1155 URL. | Merge-fingerprint rules for NBRC/TOGO duplicate handling. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the recovered NBRC normalized corpus and confirm `CultureMech:007459` is emitted as `TG agar`, not plain `1158`.
2. Confirm the regenerated record has separate rows for Tryptone (Diffico), glucose, agar, and distilled water; NBRC Medium 1155 source grounding; complex/undefined classification; and `AUTOCLAVE` sterilization.
3. If regeneration leaves the stale blob intact, fix the NBRC source selection path that is still using the pre-recovery import output.
4. Review fingerprinting between the repaired NBRC direct import and TOGO `TG_agar` so the two NBRC 1155 copies are either intentionally merged or intentionally kept separate.

## Follow-up Checks

- `just verify-merges` to prove generated outputs are fresh relative to `data/normalized_yaml/bacterial/NBRC_1158.yaml`.
- `just audit-merge-freshness` to catch sibling stale NBRC direct imports.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1155>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1155>.yaml` to verify glucose, agar, and water labels.
- Manual comparison to live NBRC Medium 1155 for all four source rows.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
