# YAML Record Review: 1_10_r2a_with_mes_ph6

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_r2a_with_mes_ph6.yaml`
- Started UTC: 20260921T050008Z
- Finished UTC: 20260921T050119Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008720` |
| Label | `1_10_r2a_with_mes_ph6` |
| Original label | `1/10 R2A with MES (pH6)` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml`, TOGO/NBRC import normalization, or merge regeneration |
| Merge owner | `merged_from: [1_10_r2a_with_mes_ph6]` |
| Maintained owner | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` |

This generated singleton is the TOGO Medium M2127 import of NBRC Medium 1459.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_r2a_with_mes_ph6.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_r2a_with_mes_ph6.yaml --out /private/tmp/1_10_r2a_with_mes_ph6.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_r2a_with_mes_ph6.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_r2a_with_mes_ph6.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_r2a_with_mes_ph6.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly denotes TOGO Medium M2127 / NBRC Medium 1459, `1/10 R2A with MES (pH6)`. NBRC supports the MES, agar-if-needed, water, and R2A Broth DAIGO rows.

The `solution-migrator-v1.0` entry moved the R2A Broth DAIGO weighed ingredient into an empty solution:

```yaml
solutions:
- preferred_term: R2A Broth \"DAIGO\"*
  composition: []
  concentration:
    value: '0.32'
    unit: G_PER_L
  name: Unknown solution
```

NBRC lists that row as 0.32 g of R2A Broth DAIGO powder, not as an unnamed stock solution.

## Evidence

Supported by inspected source text:

- NBRC Medium 1459 supports the 1/10 R2A with MES identity, 3.9 g MES, optional 15 g agar, 1 L distilled water, and pH 6.0.
- NBRC lists R2A Broth DAIGO at 0.32 g and has a footnote identifying Wako Pure Chemical Industries product code 395-01681.

Unsupported or malformed in the generated record:

- R2A Broth DAIGO is absent from `ingredients` and incorrectly present as an empty solution.
- NBRC pH 6.0 is absent.
- The 1 L distilled-water row is stored as `1 G_PER_L`.
- The Wako product-code footnote is absent.
- The NBRC and TOGO source URLs are present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- Agar is complete enough as an optional solidifying row because the preferred term preserves `if needed`.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the NBRC page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:008720`, `TOGO:M2127`, `NBRC_M1459`, the NBRC 1459 URL slug, and the merge fingerprint found only this normalized owner, this generated singleton, and ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | R2A Broth DAIGO is mis-modeled as an empty solution. | NBRC lists 0.32 g R2A Broth DAIGO powder; the YAML stores it under `solutions` with `composition: []` and `name: Unknown solution`. | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` and the legacy `solution-migrator-v1.0` rule that classified this row as a solution. |
| Major | The pH 6.0 condition is missing. | NBRC Medium 1459 states pH 6.0; the YAML has no `ph_value`. | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` or the TOGO/NBRC import mapping. |
| Major | The distilled-water row has the wrong dimension. | NBRC lists distilled water as 1 L; the generated and normalized YAML store it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` or the TOGO/NBRC volume import mapping. |
| Minor | The R2A Broth DAIGO supplier footnote is missing. | NBRC identifies the R2A Broth DAIGO supplier and product code; the YAML has no supplier information on the moved row. | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml`. |
| Minor | Structured references are absent. | The TOGO and NBRC URLs are present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` or the TOGO import mapping. |

## Recommended Edits

1. Move `R2A Broth "DAIGO"` out of `solutions` and back into `ingredients` as a 0.32 g/L opaque powder row.
2. Add the Wako Pure Chemical Industries product-code footnote to that R2A Broth row.
3. Add `ph_value: 6.0` from NBRC Medium 1459.
4. Correct the distilled-water quantity so NBRC's 1 L volume is not represented as grams per liter.
5. Add structured references for TOGO Medium M2127 and NBRC Medium 1459.
6. Regenerate `data/merge_yaml/merged/` after the normalized owner is corrected.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/1_10_r2a_with_mes_ph6.yaml` after adding structured references.
- `just verify-merges` to prove merge regeneration preserves a singleton M2127 canonical.
- `just validate-strict` and `just validate-terms` on the regenerated `1_10_r2a_with_mes_ph6.yaml` merge artifact.
- Manual comparison to NBRC Medium 1459 for R2A Broth DAIGO, MES, agar optionality, distilled water, pH 6.0, and the Wako product-code footnote.

## Additional Notes

- A direct fetch of TOGO Medium M2127 returned only the TogoMedium JavaScript shell; the local record points to NBRC Medium 1459, and that NBRC source page resolved.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
