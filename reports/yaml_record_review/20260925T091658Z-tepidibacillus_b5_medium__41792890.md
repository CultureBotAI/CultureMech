# YAML Record Review: tepidibacillus_b5_medium__41792890

- Repository: CultureMech
- Record: data/merge_yaml/merged/tepidibacillus_b5_medium__41792890.yaml
- Started UTC: 2026-09-25T09:16:58Z
- Finished UTC: 2026-09-25T09:18:17Z
- Verdict: needs curation

## Target

Generated merged MediaRecipe `CultureMech:002285`, `tepidibacillus_b5_medium`, grounded to `mediadive.medium:J1108` / JCM Medium 1108.

## Validation

- LinkML schema validation passed.
- Strict validation passed: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- Reference validation passed: 1 file validated, 0 reference checks, all validations passed.
- Term validation passed.
- Embedded `curation_history` was not checked because the standalone history validator targets files under `history/`, not merged `MediaRecipe.curation_history`.

## Identity and Grounding

- MediaDive `J1108` currently resolves to status 200, count 1, source `JCM`, name `TEPIDIBACILLUS B5 MEDIUM`, pH 7.0, and the JCM `GRMD=1108` source URL.
- The live JCM `GRMD=1108` page currently reports `Nothing found`; MediaDive `J1108` and TOGO `M1185` still preserve the JCM-derived formulation.
- TOGO `M1185` identifies the same medium as `Tepidibacillus B5 Medium`, `original_media_id: JCM_M1108`, with pH 7.0 and the same JCM `GRMD=1108` source URL.
- An exact `J1108` / `TOGO:M1185` search scoped to `data/merge_yaml/merged` and `data/normalized_yaml/bacterial` with ignored files included found only the JCM normalized file, the TOGO normalized duplicate, and the two corresponding generated merge files.

## Evidence

- MediaDive `J1108` has `Main sol. J1108` at 1003 ml total volume. The direct final-medium additions are 1 g NaCl, 0.5 g KCl, 0.4 g `MgCl2 x 6 H2O`, 0.1 g `CaCl2 x 2 H2O`, 0.2 g KH2PO4, 0.5 g NH4Cl, 0.42 g NaNO3, and 1 g yeast extract.
- The same MediaDive recipe then adds 1 ml trace element solution, 1 ml selenite-tungstate solution, 980 ml distilled water, 1 ml vitamin solution, 5 ml of 1 M glucose, and 15 ml of 8% NaHCO3.
- MediaDive keeps the trace element, selenite-tungstate, and vitamin stocks as separate solution objects. Their stock concentrations, such as 12.8 g/L nitrilotriacetic acid in the trace element solution, 0.006 g/L `Na2SeO3 x 5 H2O` in the selenite-tungstate solution, and 0.04 g/L p-aminobenzoic acid in the vitamin solution, are not final-medium concentrations.
- MediaDive records the preparation as autoclaving under an `N2-CO2` 80:20 gas atmosphere, adding the autoclaved or filter-sterilized stocks after cooling, and adjusting pH to 7.0 if necessary.
- TOGO `M1185` preserves the same JCM source as a 980 ml base plus cross-references to Medium `M296` trace element solution, Medium `M431` selenite-tungstate solution, an 8% NaHCO3 solution, a 1 M glucose solution, and Medium `M401` vitamin solution.

## Completeness

- The direct base salts, yeast extract, liquid state, and pH 7.0 are present.
- The generated target captures the JCM/MediaDive source identity but loses the nested solution topology and preparation sequence.
- The generated target omits the explicit `N2-CO2` atmosphere from MediaDive's preparation step.
- The source-equivalent TOGO import remains a separate generated record in `data/merge_yaml/merged/TEPIDIBACILLUS_B5_MEDIUM.yaml`.

## Findings

- The JCM/MediaDive record flattened all nested solutions into top-level final-medium ingredients at stock strength. A 1 ml addition to 1003 ml final volume should not yield 12.8 G_PER_L nitrilotriacetic acid, 0.42 G_PER_L `FeSO4 x 7 H2O`, 0.006 G_PER_L `Na2SeO3 x 5 H2O`, or 0.04 G_PER_L p-aminobenzoic acid in the final medium.
- The 5 ml 1 M glucose and 15 ml 8% NaHCO3 additions are represented as 5 G_PER_L glucose and 15 G_PER_L NaHCO3. Those YAML concentrations are the added liquid volumes, not valid final solute masses per liter.
- The TOGO duplicate `TEPIDIBACILLUS_B5_MEDIUM.yaml` has the same JCM source but was not merged with this generated target. It keeps five cross-referenced stock additions as empty `Unknown solution` entries with `G_PER_L` units, so both source branches encode the same recipe incorrectly.
- `NaNO3` still carries a stale legacy `mediaingredientmech_term` despite having a CHEBI primary term.

## Recommended Edits

- Fix the MediaDive/JCM import or merge expansion so stock recipes remain nested under explicit `solutions`, with the 1 ml, 5 ml, and 15 ml additions represented as liquid stock additions rather than final `G_PER_L` solute rows.
- Do not hand-edit `data/merge_yaml/merged/tepidibacillus_b5_medium__41792890.yaml`; repair `data/normalized_yaml/bacterial/tepidibacillus_b5_medium.yaml` or the merge generation code, then regenerate this file.
- Merge the source-equivalent TOGO `M1185` record into the JCM/MediaDive `J1108` record by recognizing the shared `JCM_M1108` / `GRMD=1108` grounding.
- Migrate the remaining `NaNO3` legacy `mediaingredientmech_term` to the CHEBI-keyed slot.
- Preserve the MediaDive preparation note that the medium is autoclaved under `N2-CO2` 80:20 and that several stocks are added after cooling.

## Follow-up Checks

- After importer or merge fixes, rerun schema, strict, reference, and term validation on the regenerated merged record.
- Re-run an exact duplicate search for `J1108`, `JCM_M1108`, `TOGO:M1185`, and `GRMD=1108` with ignored files included.
- Verify the regenerated record does not contain stock solution solutes as undiluted final-medium ingredients.
- Verify the TOGO duplicate is gone or merged into the same `CultureMech` record.

## Additional Notes

None found
