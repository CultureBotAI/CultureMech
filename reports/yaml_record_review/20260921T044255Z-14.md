# YAML Record Review: 14

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/14.yaml`
- Started UTC: 20260921T044141Z
- Finished UTC: 20260921T044255Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007476` |
| Label | `14` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_14.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_14]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_14.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import whose import history points at NBRC Medium 13. The maintained owner later recovered the four-row NBRC 13 table, pH 8.2, the seawater dilution note, and `nbrc.medium:13`, but it still retains the stale numeric label `14`.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/14.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/14.yaml --out /private/tmp/14.strict.tsv --workers 1 --quiet` | Pass; command exited 0 and TSV at `/private/tmp/14.strict.tsv` contained only the header |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/14.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/14.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The target's source identity is NBRC Medium 13:

- The generated import history says `Media No. 13` and links the legacy NBRC `NO=13` page.
- Live NBRC Medium 13 is titled `Seawater Starch Agar (SWS)`.
- The maintained owner now has `media_term: nbrc.medium:13`, pH 8.2, and the four components listed by the NBRC table.

Two identity issues remain:

- The generated record is still labeled `14`; live NBRC Medium 14 is the unrelated `Corn and Fish Meal Agar`.
- The maintained owner also still has `name: '14'` and `original_name: '14'` rather than the official `Seawater Starch Agar (SWS)` title.

## Evidence

Supported by inspected source text:

- NBRC Medium 13 lists Soytone 1 g, soluble starch 10 g, 2% salinity seawater 1 L, agar 15 g, and pH 8.2.
- NBRC Medium 13 says to dilute artificial seawater or natural seawater with distilled water.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all four NBRC ingredient rows, the pH text, and the seawater dilution footnote into one `preferred_term`.
- The second ingredient row has an empty `preferred_term` and stores `Soytone` as a concentration value.
- The generated artifact has no separate rows for soluble starch, 2% salinity seawater, or agar.
- The generated file omits pH 8.2, the seawater dilution footnote as an explicit note, `nbrc.medium:13`, the NBRC reference, and all ingredient term mappings.
- The generated `ADJUST_PH` and autoclaving steps are not in the inspected NBRC 13 table.

## Completeness

- The generated file omits the Aug 19 recovered NBRC composition, the Aug 20 MIM grounding, and the Sep 7 NBRC source-metadata repair in `data/normalized_yaml/bacterial/NBRC_14.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRC_14.yaml`, `NO=13[^0-9]`, `Seawater Starch Agar`, `nbrc.medium:13`, and `CultureMech:007476` found the stale generated NBRC direct import, the partially repaired normalized owner, its ID/catalog entries, and a separate TOGO-derived `seawater_starch_agar_sws` copy of the same NBRC source.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRCMediumDetailServlet?NO=14[^0-9]`, `nbrc.medium:14`, and `Corn and Fish Meal Agar` found no NBRC direct import for live NBRC Medium 14, but did find the separate TOGO-derived `corn_and_fish_meal_agar` record.
- Empty preparation and growth-evidence slots are acceptable in the normalized NBRC 13 source import because the NBRC table does not state a preparation sequence or an organism-specific growth observation.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated merge is stale and malformed relative to its repaired normalized owner. | The Aug 6 generated file still has the pre-recovery two-row parse, while `data/normalized_yaml/bacterial/NBRC_14.yaml` now carries four NBRC 13 ingredients, pH 8.2, `nbrc.medium:13`, and the direct NBRC reference. | Regenerate from `data/normalized_yaml/bacterial/NBRC_14.yaml` after the title fix; fix merge source selection if the stale rows persist. |
| Blocker | The generated label `14` is misleading for an NBRC Medium 13 record. | The import event points to `NO=13`, live NBRC 13 is `Seawater Starch Agar (SWS)`, and live NBRC 14 is the distinct `Corn and Fish Meal Agar`. | `data/normalized_yaml/bacterial/NBRC_14.yaml` plus merge regeneration; the normalized owner still needs the official title. |
| Major | The official medium title is still missing from both the generated and normalized NBRC direct records. | Live NBRC Medium 13 is `Seawater Starch Agar (SWS)`, but both the generated file and `data/normalized_yaml/bacterial/NBRC_14.yaml` retain `name: '14'`. | `data/normalized_yaml/bacterial/NBRC_14.yaml`. |
| Major | The generated file omits source pH and all structured components except a corrupt Soytone row. | NBRC Medium 13 lists four component rows and pH 8.2; the generated file has one concatenated formula blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_14.yaml` after the title fix. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 13 page supplies the composition table, pH row, and seawater footnote; it does not state the generated autoclaving step, and the maintained owner now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_14.yaml`. |
| Minor | A TOGO-derived copy of the same NBRC Medium 13 remains separate. | Gitignore-independent search found `seawater_starch_agar_sws` with the same NBRC 13 source URL; that TOGO copy represents the 2% salinity seawater as an empty solution with `1 G_PER_L`. | Merge-fingerprint rules for NBRC/TOGO duplicate handling after the direct NBRC import receives its official title. |

## Recommended Edits

1. Curate `data/normalized_yaml/bacterial/NBRC_14.yaml` to use `Seawater Starch Agar (SWS)` as `name` and `original_name`, preserving `nbrc.medium:13`.
2. Regenerate `data/merge_yaml/merged/` and confirm the generated NBRC Medium 13 artifact carries the four recovered ingredient rows, pH 8.2, the seawater footnote, the NBRC 13 reference, and the Sep 7 curation event.
3. If regeneration preserves the concatenated formula blob, fix the NBRC merge source path that is still using the pre-recovery import output.
4. Review duplicate handling between the NBRC direct import and the TOGO `seawater_starch_agar_sws` copy.
5. Review the `corn_and_fish_meal_agar` TOGO record to decide whether live NBRC Medium 14 needs an NBRC direct normalized owner or whether the existing TOGO-derived record is enough.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/NBRC_14.yaml` after the normalized title edit.
- `just verify-merges` to prove generated outputs are fresh after regeneration.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-13>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-13>.yaml` for the grounded subset.
- Manual comparison to live NBRC Medium 13 for all four ingredient rows, pH 8.2, and the seawater dilution footnote.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
