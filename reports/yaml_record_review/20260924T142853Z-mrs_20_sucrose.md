# YAML Record Review: mrs_20_sucrose

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_20_sucrose.yaml
- Started UTC: 2026-09-24T14:27:46Z
- Finished UTC: 2026-09-24T14:28:53Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mrs_20_sucrose.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:009562`
- Label: `mrs_20_sucrose`
- Original label: `MRS + 20 % sucrose`
- Category: `bacterial`
- Media source: `TOGO:M3049`, original `NBRC_M1599`
- Generated status: generated merge output from one normalized source on fingerprint `674aac9286d3a1f525b60a31ddf654be85504d340fff4668c978bb2d5d903c5a`
- Maintained owner: `data/normalized_yaml/bacterial/mrs_20_sucrose.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_20_sucrose.yaml`
  - Result: passed; exited 0 with no diagnostics.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mrs_20_sucrose.yaml --out /private/tmp/mrs_20_sucrose.strict.tsv --workers 1 --quiet`
  - Result: passed; exited 0 after its startup line and wrote a TSV with only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mrs_20_sucrose.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; exited 0 with no diagnostics.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mrs_20_sucrose.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record correctly denotes NBRC medium 1599 via TOGO `M3049`, `MRS + 20 % sucrose`.

An exact gitignore-independent search of `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:009562`, `TOGO:M3049`, `NBRC_M1599`, `NO=1599`, and the slug found only `data/normalized_yaml/bacterial/mrs_20_sucrose.yaml` and this generated output. Ignored files were included.

Hydrate-aware groundings are correct for `MgSO4 x 7H2O`, and sucrose is grounded to `CHEBI:17992` even though TOGO's inherited GMO label for the sucrose row is stale and says `Glucose`. The complex ingredients `Yeast extract`, `Meat extract`, `Peptone`, and `Gellan gum` are acceptably ungrounded.

## Evidence

TOGO `M3049` and the live NBRC medium 1599 page agree on all formula rows:

| Ingredient | Source amount | CultureMech amount | Status |
| --- | ---: | ---: | --- |
| `Peptone` | 10 g | 10 g/L | supported |
| `Meat extract` | 10 g | 10 g/L | supported |
| `Yeast extract` | 5 g | 5 g/L | supported |
| `Sucrose` | 200 g | 200 g/L | supported |
| `Tween 80` | 1 g | 1 g/L | supported |
| `K2HPO4` | 2 g | 2 g/L | supported |
| `Sodium acetate` | 5 g | 5 g/L | supported |
| `Diammonium hydrogen citrate` | 2 g | 2 g/L | supported |
| `MgSO4 x 7H2O` | 0.2 g | 0.2 g/L | supported |
| `MnSO4 x nH2O` | 0.05 g | 0.05 g/L | supported |
| `Distilled water` | 1 L | `1 G_PER_L` | wrong unit |
| `Gellan gum (if needed)` | 8 g | 8 g/L | amount supported; optional row retained |

Unsupported or unresolved:

- The pH 4.5-5.0 source comment is absent from the record.

## Completeness

The formula ingredients are complete, including the optional gellan gum row. The pH range is missing.

Empty optional fields are not defects in this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The pH range is missing. | TOGO `M3049` reports `ph: 4.5 - 5.0`, and the NBRC 1599 composition table prints the same pH comment; the YAML has no `ph_value` or `ph_range`. | `data/normalized_yaml/bacterial/mrs_20_sucrose.yaml` |
| Minor | The distilled-water row has the wrong unit. | NBRC and TOGO list `1 L`; CultureMech stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/mrs_20_sucrose.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/mrs_20_sucrose.yaml`, add `ph_range` 4.5-5.0 from NBRC 1599 / TOGO `M3049`.
2. Convert `Distilled water` from `1 G_PER_L` to a liter solvent row or remove it if one-liter water is represented structurally elsewhere.
3. Keep `Gellan gum (if needed)` optional if the schema can express optionality; if not, preserve `if needed` in the row label or note.
4. Regenerate `data/merge_yaml/merged/mrs_20_sucrose.yaml` from the maintained normalized record.

## Follow-up Checks

- Re-run open schema validation on the regenerated merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm existing groundings still resolve.
- Re-fetch TOGO `M3049` and NBRC `NO=1599` and verify all 12 source rows plus the pH 4.5-5.0 comment are represented.
- Repeat the exact ignored-inclusive search for `TOGO:M3049`, `NBRC_M1599`, and `NO=1599` to verify no duplicate NBRC 1599 recipe was introduced.

## Additional Notes

- The 20% sucrose label is consistent with 200 g sucrose per liter, so the source name and ingredient amount agree.
- `MnSO4 x nH2O` lacks a `mediaingredientmech_chebi_term` mirror even though the primary CHEBI term is present.
