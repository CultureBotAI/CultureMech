# YAML Record Review: AN1 Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/an1_medium__92dd8890.yaml
- Started UTC: 2026-09-21T11:49:20Z
- Finished UTC: 2026-09-21T11:50:47Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009167 |
| Source | TOGO:M259 |
| Original source | JCM_M267 |
| Generated path | data/merge_yaml/merged/an1_medium__92dd8890.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M259_AN1_Medium.yaml |
| Merge fingerprint | 92dd8890fee8bdeb03830a223f829e3b69f7f90482fe9f6cfa00f8bc7572c054 |
| Merge source | TOGO_M259_AN1_Medium.yaml |

`data/merge_yaml/merged/an1_medium__92dd8890.yaml` is a generated singleton merge from the TOGO/JCM M259 normalized owner; any correction belongs in `data/normalized_yaml/bacterial/TOGO_M259_AN1_Medium.yaml` or the TOGO importer.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/an1_medium__92dd8890.yaml` under the cached Python 3.11 no-project environment. |
| Strict schema | Passed with `scripts/validate_strict.py data/merge_yaml/merged/an1_medium__92dd8890.yaml --workers 1 --quiet`; the TSV contained 0 ERROR rows. |
| Reference validator | Passed with `linkml-reference-validator validate data ...`. |
| Term validator | Passed with `linkml-term-validator validate-data ... --labels -c conf/oak_config.yaml`. |
| Embedded curation history | Not checked: this repository exposes `just validate-history` for standalone records under `history/`; no documented focused validator targets embedded `MediaRecipe.curation_history` inside one merge record. |

The project-level `just validate-schema`, `just validate-strict`, and `just validate-terms` routes were not usable for this target because project installation currently attempts to build `llvmlite==0.46.0` on Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The equivalent LinkML validators passed through an offline `uv run --no-project` invocation against `/usr/local/bin/python3.11`.

## Identity and Grounding

- The stable CultureMech ID, `TOGO:M259` source term, JCM_M267 provenance, and merge fingerprint identify a singleton TOGO import for JCM GRMD 267, AN1 Medium.
- A gitignore-independent search across `data`, `src`, `scripts`, `history`, `conf`, `reports`, `.claude`, `justfile`, and `CLAUDE.md` found exactly one current maintained owner for `CultureMech:009167`.
- The TOGO API for M259 and the inspected JCM GRMD 267 page agree on the AN1 Medium identity and source composition.
- `Sulfur (powder)` is grounded to `CHEBI:33403` / elemental sulfur in this TOGO record, which is a better exact match than the sulfur atom grounding in the DSMZ/KOMODO duplicate pair.
- `Trypticase peptone (BD-BBL)` is intentionally explicit and ungrounded; the JCM source states that exact undefined component.

## Evidence

- The imported mass rows for NaCl, K2HPO4, sulfur, trypticase peptone, and sodium thioglycolate match JCM GRMD 267 and the TOGO M259 API at a name-and-amount level.
- JCM and TOGO both give `Resazurin` as `1.0 mg`, but the record stores `1 G_PER_L`; this is a 1000x unit error for a 1 L final volume.
- JCM and TOGO both give `Distilled water` as `1.0 L`, a final volume. The record converts that to `1 G_PER_L`, which makes water look like a one-gram-per-liter solute.
- JCM and TOGO both describe adjusting pH to 7.3, N2 autoclaving, separate 10% thioglycolate sterilization, steaming sulfur for 3 hours on each of 3 successive days, combining medium and sulfur under an N2 stream in culture vessels, sealing with butyl rubber stoppers, and adding thioglycolate anaerobically before inoculation. None of that appears under `ph_value` or `preparation_steps`.

## Completeness

- Consequentially incomplete: the source pH and preparation paragraph were not imported.
- Consequentially incorrect: final volume and milligram rows were both coerced into `G_PER_L` ingredient concentrations without the necessary volume and unit handling.
- The `N2` row should probably become preparation-atmosphere context rather than a variable-concentration ingredient; the source uses it in the autoclave and assembly instructions, not as a dissolved recipe mass.
- Empty target-organism, temperature, storage, and salinity slots are acceptable here; the inspected TOGO and JCM medium records did not provide those claims.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | Resazurin is inflated from 1 mg per liter to `1 G_PER_L`. | JCM GRMD 267 and TOGO M259 both list Resazurin as `1.0 mg`; the normalized owner and generated merge store `value: '1'`, `unit: G_PER_L`. | Fix the TOGO unit conversion in `data/normalized_yaml/bacterial/TOGO_M259_AN1_Medium.yaml` or, preferably, in the TOGO importer so mg per 1 L becomes `0.001 G_PER_L`. |
| Major | Distilled water is represented as a solute concentration. | JCM GRMD 267 and TOGO M259 list Distilled water as `1.0 L`; the record stores `1 G_PER_L`. | Remove final-volume water rows from TOGO normalized recipes or model them as final volume metadata if the schema supports that. |
| Major | The source pH and preparation instructions were not imported. | JCM and TOGO both contain a pH 7.3 adjustment plus anaerobic N2 autoclave, sulfur steaming, vessel sealing, and thioglycolate addition instructions; the record has no `ph_value` and no `preparation_steps`. | Parse TOGO `comments` into `preparation_steps`, or curate the JCM GRMD 267 instructions directly into `data/normalized_yaml/bacterial/TOGO_M259_AN1_Medium.yaml`. |
| Major | `N2` is modeled as a variable-concentration ingredient instead of an atmospheric preparation condition. | The only source support for N2 is the JCM/TOGO preparation text: autoclave under N2 and assemble under an N2 stream. | Move `N2` from `ingredients` into preparation-step context or an atmosphere slot during TOGO normalization. |

## Recommended Edits

1. Correct Resazurin to `0.001 G_PER_L` in the authoritative TOGO M259 owner and importer, preserving the JCM `1.0 mg` source amount.
2. Stop converting the `Distilled water` final volume into an ingredient concentration; remove this ingredient or represent `1.0 L` as a final-volume field if available.
3. Add `ph_value: 7.3` and structured preparation steps for the JCM instructions, including the 10% thioglycolate solution, sulfur steaming schedule, N2 atmosphere, butyl-rubber stopper sealing, and anaerobic pre-inoculation thioglycolate addition.
4. Replace the defaulted `N2` variable-concentration ingredient with preparation atmosphere semantics.
5. Regenerate `data/merge_yaml/merged` with `just merge-recipes` after the normalized owner or TOGO importer is corrected.

## Follow-up Checks

1. Run focused open-schema, strict, term, and reference validation on `data/normalized_yaml/bacterial/TOGO_M259_AN1_Medium.yaml` and the regenerated `data/merge_yaml/merged/an1_medium__92dd8890.yaml`.
2. Run `just verify-merges` and `just audit-merge-freshness --fail-on-drift` after regeneration.
3. Re-fetch TOGO M259 via `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M259` and re-open JCM GRMD 267 to verify the corrected units and all preparation steps.
4. Run the concentration plausibility report and confirm the existing `INDICATOR_UNIT_SLIP` finding for Resazurin no longer appears for `CultureMech:009167`.

## Additional Notes

- The bounded identity search used `rg --no-ignore --hidden`; ignored paths were included for the searched roots.
- The source page for TOGO public `/medium/M259` is a SPA shell; the review used TOGO's `gmdb_medium_by_gmid` JSON API instead.
- The original JCM source page at GRMD 267 was fetched directly during this review.
