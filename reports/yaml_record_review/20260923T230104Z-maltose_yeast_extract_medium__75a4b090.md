# YAML Record Review: maltose_yeast_extract_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml
- Started UTC: 2026-09-23T23:01:04Z
- Finished UTC: 2026-09-23T23:01:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| CultureMech ID | CultureMech:008298 |
| Label | maltose_yeast_extract_medium |
| Original label | Maltose-Yeast Extract Medium |
| Category | bacterial |
| Source identity | TOGO M1736, originally NBRC Medium 945 |
| Merge fingerprint | 75a4b09059fd18036012a42fc1ef743b5329d53672064eb653ace8081a5976b0 |
| Merged from | `maltose_yeast_extract_medium` |
| Maintained owner | `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml` |
| Generated status | Generated by `merge_recipes.py` on 2026-08-06 |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml --out /private/tmp/maltose_yeast_extract_medium__75a4b090.strict.tsv --workers 1 --quiet` | Passed with 1 file, 0 errors; the TSV contained only the header row |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 1 file and 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded history | `just validate-history` | Not checked: this repository validator targets standalone `history/` records, not embedded `MediaRecipe.curation_history` entries |

## Identity and Grounding

- The generated record consistently denotes TOGO M1736, derived from NBRC Medium 945 Maltose-Yeast Extract Medium.
- NBRC 945 and TOGO M1736 support 20 g/L Maltose, 10 g/L Yeast extract, 2 g/L KH2PO4, 5 g/L NaCl, optional 15 g/L Agar, 1 L water, and pH 6.0.
- The same-slug JCM 606 record is not an exact duplicate because it uses K2HPO4 rather than KH2PO4 and lists agar as an ordinary required row.
- `Maltose` is grounded too narrowly as `CHEBI:18167` / alpha-maltose. NBRC and TOGO use the generic Maltose label, and the packaged MediaIngredientMech label index maps that exact label to `CHEBI:17306`.

## Evidence

| Claim | Support |
|---|---|
| NBRC 945 contains 20 g maltose, 10 g yeast extract, 2 g KH2PO4, 5 g NaCl, optional 15 g agar, and 1 L distilled water | Supported by the NBRC 945 page and TOGO M1736 |
| NBRC 945 has pH 6.0 | Supported by the NBRC 945 page and TOGO M1736 |
| NBRC 945 uses KH2PO4 rather than K2HPO4 | Supported by the NBRC 945 page |

## Completeness

- The generated record omits the source pH 6.0 despite TOGO M1736 carrying `ph: "6.0"` and the NBRC page listing pH 6.0.
- The water row uses `value: '1'` with `unit: G_PER_L`; NBRC and TOGO state 1 L distilled water.
- The generated `Agar (if needed)` row preserves the source condition but lacks an agar grounding.
- Empty optional slots for organism growth evidence, incubation temperature, salinity, atmosphere, storage, preparation, and variants were not treated as defects because the inspected NBRC and TOGO records do not state them.

## Findings

| Severity | Finding | Evidence | Maintained owner for a future fix |
|---|---|---|---|
| Major | The pH 6.0 source condition is missing. | NBRC 945 and TOGO M1736 both state pH 6.0, but the generated record has no `ph_value` or preparation note. | `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml` or the TOGO pH importer |
| Major | The water amount uses the wrong unit. | The source lists 1 L distilled water; the generated row stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml` or TOGO import unit normalization for water rows |
| Major | Maltose is grounded to alpha-maltose, over-specifying the source reagent. | The NBRC and TOGO labels are simply `Maltose`; the packaged MediaIngredientMech label index has an exact `Maltose` mapping to `CHEBI:17306`, while the generated row uses `CHEBI:18167` / alpha-maltose. | `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml` |
| Minor | Yeast extract and optional agar are missing available groundings. | The packaged MediaIngredientMech label index maps exact `Yeast extract` to `FOODON:03315426`, and the source `Agar (if needed)` row should still carry the exact agar identity while retaining its optional condition. | `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml` plus ingredient enrichment for complex non-CHEBI terms |

No blocker findings were found.

## Recommended Edits

1. Add pH 6.0 as a structured pH value on the normalized NBRC 945 owner.
2. Convert the source 1 L distilled-water row from `1 G_PER_L` to the repository's accepted 1000 `ML_PER_L` representation.
3. Re-ground `Maltose` from alpha-maltose to the exact generic maltose identity, mirroring the packaged label-index mapping.
4. Ground `Yeast extract` through the packaged MediaIngredientMech label index.
5. Ground `Agar (if needed)` to agar while preserving the `if needed` condition in a structured or note field.
6. Regenerate `data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml` and downstream media pages after the normalized owner is corrected.

## Follow-up Checks

- Re-run open schema, strict schema, term validation, and reference validation on `data/normalized_yaml/bacterial/maltose_yeast_extract_medium.yaml`.
- Re-run the same focused validators on regenerated `data/merge_yaml/merged/maltose_yeast_extract_medium__75a4b090.yaml`.
- Compare the regenerated record against NBRC 945 and TOGO M1736, checking pH 6.0, KH2PO4, optional agar, the 1 L water row, and generic Maltose grounding.
- Re-run the term validator and confirm `Maltose` no longer resolves to `CHEBI:18167`.

## Additional Notes

- The JCM 606 same-slug import was inspected only to disambiguate it from this NBRC 945 record.
