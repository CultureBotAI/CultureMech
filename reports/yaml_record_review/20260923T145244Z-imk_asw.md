# YAML Record Review: IMK (ASW)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/imk_asw.yaml
- Started UTC: 2026-09-23T14:49:05Z
- Finished UTC: 2026-09-23T14:52:48Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| ID | CultureMech:008569 |
| Name | imk_asw |
| Original name | IMK (ASW) |
| Class | MediaRecipe |
| Category | bacterial |
| Medium type | COMPLEX |
| Composition type | UNDEFINED |
| Physical state | SOLID_AGAR |
| pH | Not represented |
| Source identity | TOGO Medium M1986, derived from NBRC Medium 1272 |
| Generated path reviewed | data/merge_yaml/merged/imk_asw.yaml |
| Maintained owner | data/normalized_yaml/bacterial/imk_asw.yaml |

The reviewed file is generated from the maintained TOGO/NBRC owner above.
Future fixes should regenerate `data/merge_yaml/merged/` from that already
repaired normalized record.

## Validation

| Check | Result |
|---|---|
| Open LinkML validation, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/imk_asw.yaml` | Passed; no issues found. |
| Strict validation, `python scripts/validate_strict.py data/merge_yaml/merged/imk_asw.yaml --out /private/tmp/imk_asw.strict.tsv --workers 1 --quiet` | Passed; zero ERROR rows. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/imk_asw.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; zero reference checks were applicable. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/imk_asw.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` entry point validates standalone YAML under `history/`, not embedded MediaRecipe history arrays. |

The focused validators used Python 3.11 and the offline uv cache because the
project-level uv environment currently fails before validation while trying to
build `llvmlite==0.46.0` with Python 3.13.

## Identity and Grounding

- The stable ID, slug, `TOGO:M1986` media term, and source URL identify TOGO's
  NBRC Medium 1272 IMK (ASW) import.
- A gitignore-independent exact search for `CultureMech:008569` over bacterial
  normalized records, normalized indexes, and merged records found only this
  owner, its generated merged copy, and generated index entries.
- A gitignore-independent exact search for `TOGO:M1986` over the same paths
  found only this owner, its generated merged copy, and generated index
  entries.
- NBRC Medium 1272 and the TOGO M1986 API agree on the three source rows:
  Daigo's IMK Medium, optional agar, and Artificial Seawater.

## Evidence

- NBRC lists 0.253 g Daigo's IMK Medium from Wako Pure Chemical Industries,
  Ltd., product code 398-01333; 12 g agar if needed; and 1 L Artificial
  Seawater.
- TOGO M1986 preserves the same three rows, with Artificial Seawater expressed
  as 1 L and Daigo's IMK Medium as 0.253 g.
- The generated record reviewed here predates the maintained owner's Sep 7
  `repair_togo_exact_score25.py` curation event.
- The generated record still has `Daigo's IMK Medium*` as an empty
  `Unknown solution` at `0.253 G_PER_L`.
- The generated record still has Artificial Seawater as `1 G_PER_L`; the
  repaired owner has `1 L` with a source-specific note.
- The generated record lacks the repaired owner's NBRC reference, TOGO
  reference, preparation step, agar CHEBI grounding, and data-quality flags.

## Completeness

- The maintained normalized owner is source-faithful for this three-row NBRC
  formula.
- The generated copy is stale and should not be used until the Sep 7 normalized
  repair is propagated.
- Empty optional fields for synonyms, direct publication references, organism
  targets, discussions, variants, and quality flags are acceptable after the
  generated file is refreshed from the repaired owner.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated file is stale relative to the repaired normalized owner. | The reviewed merged file was generated on Aug 6, but the maintained owner was repaired on Sep 7 with NBRC-grounded rows, references, and a preparation step. | Regenerate from `data/normalized_yaml/bacterial/imk_asw.yaml`. |
| Major | Daigo's IMK Medium is still represented as an empty solution. | NBRC and TOGO list a 0.253 g Daigo's IMK Medium ingredient; the generated YAML has it in an `Unknown solution` with an empty `composition`. | Already fixed in `data/normalized_yaml/bacterial/imk_asw.yaml`; regenerate. |
| Major | Artificial Seawater uses the wrong unit in the generated file. | NBRC and TOGO list 1 L Artificial Seawater; the generated YAML records `1 G_PER_L`. | Already fixed in `data/normalized_yaml/bacterial/imk_asw.yaml`; regenerate. |
| Minor | The generated file is missing post-curation evidence metadata. | The maintained owner now has NBRC and TOGO `references`, a `preparation_steps` row, agar CHEBI grounding, and data-quality flags, none of which are in the generated copy. | Already fixed in `data/normalized_yaml/bacterial/imk_asw.yaml`; regenerate. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/imk_asw.yaml` from
   `data/normalized_yaml/bacterial/imk_asw.yaml`.
2. Confirm the regenerated file keeps Daigo's IMK Medium as a 0.253 g
   ingredient, Artificial Seawater as 1 L, agar as 12 g if needed, and the NBRC
   product-code note.
3. Confirm the regenerated file carries the Sep 7 curation history, NBRC and
   TOGO references, the MIX preparation step, and the data-quality flags.

## Follow-up Checks

1. Rerun open LinkML, strict, reference, and term validation after regenerating
   the merged YAML.
2. Diff regenerated `data/merge_yaml/merged/imk_asw.yaml` against
   `data/normalized_yaml/bacterial/imk_asw.yaml`; the only expected difference
   should be merge provenance appended to the generated file.

## Additional Notes

- The generated file has no target-organism or literature evidence entries, so
  no organism-grounding, snippet, or DOI checks were applicable.
- No additional formula curation is needed beyond propagating the repaired
  normalized owner.
