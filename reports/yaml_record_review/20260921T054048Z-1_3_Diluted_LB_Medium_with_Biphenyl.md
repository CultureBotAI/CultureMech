# YAML Record Review: 1/3 Diluted LB Medium with Biphenyl

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml`
- Started UTC: 2026-09-21T05:37:26Z
- Finished UTC: 2026-09-21T05:40:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008583` |
| Name | `1_3_diluted_lb_medium_with_biphenyl` |
| Original name | `1/3 Diluted LB Medium with Biphenyl` |
| Category | `bacterial` |
| Source term | `TOGO:M1999`, `1/3 Diluted LB Medium with Biphenyl` |
| Merge fingerprint | `8aedcc68324401a7593afd86acc7f4a4414aae2076ccf8600b5e026e37e1f93e` |
| Maintained owner | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The generated merge is a byte-for-byte representation of the maintained TOGO-derived owner plus the Aug 6 merge event. Future fixes belong in `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` or in the TOGO importer, followed by regeneration of `data/merge_yaml/merged/`; the generated merge itself is not the authoritative edit target.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml --out /private/tmp/1_3_Diluted_LB_Medium_with_Biphenyl.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record identity is correct but incomplete. The source term identifies TOGO Medium M1999 as `1/3 Diluted LB Medium with Biphenyl`; the live TOGO M1999 JSON payload resolves to `http://togomedium.org/medium/M1999`, names NBRC_M1286 as `original_media_id`, links the NBRC Medium 1286 URL as `src_url`, and reports `ph: "7.0"`. The live NBRC Medium 1286 page has Medium No. 1286 and the same title, so this record denotes the intended NBRC-derived 1/3 diluted LB agar with biphenyl rather than a sibling medium.

The generated record has three exact CHEBI-backed ingredient groundings:

- `Distilled water` -> `CHEBI:15377`, water.
- `NaCl` -> `CHEBI:26710`, sodium chloride.
- `Agar` -> `CHEBI:2509`, agar.

Two undefined commercial/natural ingredients are left ungrounded:

- `Yeast extract`
- `Bacto tryptone`

The separate direct NBRC owner, `data/normalized_yaml/bacterial/NBRC_1287.yaml`, has already been curated to the same NBRC Medium 1286 identity with `nbrc.medium:1286`, pH 7.0, the 1 L water row, an explicit variable biphenyl component, a direct NBRC reference, and additional groundings for Bacto tryptone, yeast extract, and biphenyl. This TOGO-derived owner remains a less complete duplicate of that same upstream NBRC formulation.

## Evidence

The inspected sources support these claims already present in the generated record:

- The title `1/3 Diluted LB Medium with Biphenyl` is supported by TOGO M1999 and NBRC Medium 1286.
- TOGO M1999 and NBRC Medium 1286 both support Bacto tryptone 3.3 g, yeast extract 1.6 g, NaCl 3.3 g, and agar 15 g in the source formulation.
- TOGO M1999 supports the imported GMO role/property annotations for water, yeast extract, NaCl, agar, and Bacto tryptone.
- `physical_state: SOLID_AGAR` is supported by the 15 g agar row.
- `composition_type: UNDEFINED` and the derived compatibility value `medium_type: COMPLEX` are supported by Bacto tryptone and yeast extract.

The inspected sources do not support the generated representation of water:

- TOGO M1999 reports `component_name: Distilled water`, `volume: 1`, and `unit: L`.
- NBRC Medium 1286 reports Distilled water as 1 L.
- The generated record instead has `value: '1'`, `unit: G_PER_L`.

The inspected sources contain two consequential source claims that are absent from the generated record:

- TOGO M1999 exposes `ph: "7.0"` and an explicit `pH 7.0` comment; NBRC Medium 1286 also lists pH 7.0.
- TOGO M1999 and NBRC Medium 1286 both preserve the instruction that biphenyl crystal is supplied on the petri-dish lid as a vapor phase.

## Completeness

Consequential gaps:

- `ph_value` is absent despite TOGO and NBRC both carrying pH 7.0.
- The vapor-phase biphenyl component/instruction is absent.
- Distilled water is present but has the wrong unit.
- `references` is absent, so the direct NBRC source URL in `notes` is not available to structured evidence validators.
- No ingredient row has a narrow `source` or source-specific evidence note.
- Bacto tryptone and yeast extract are ungrounded.
- The same NBRC Medium 1286 formulation is represented separately by the direct NBRC import `data/normalized_yaml/bacterial/NBRC_1287.yaml`.

The empty `target_organisms`, `growth_metrics`, and `variants` slots are not defects in this review. The TOGO and NBRC recipe sources verify the formulation and source identity; they do not by themselves support a specific organism-growth claim or variant relationship.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `TOGO:M1999`, `M1999`, `1_3_diluted_lb_medium_with_biphenyl`, this merge fingerprint, `NBRC_M1286`, `nbrc.medium:1286`, and `1/3 Diluted LB Medium with Biphenyl` found the generated merge, the normalized TOGO owner, the already-curated direct NBRC owner, the active ID registry/catalog rows, archived March validation reports for this CultureMech ID, one deep-research priority row, the Sep 11 NBRC repair script, and the prior review of `1287.yaml`.
- A gitignore-independent `find` over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` found only `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` and `data/merge_yaml/merged/1_3_Diluted_LB_Medium_with_Biphenyl.yaml` with this filename stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record drops the source-supported pH and biphenyl vapor-phase instruction. | TOGO M1999 exposes `ph: "7.0"` plus a biphenyl comment, and NBRC Medium 1286 lists the same pH and biphenyl handling. The generated record has no `ph_value`, no `Biphenyl crystal` component, and no preparation step for adding biphenyl as vapor phase. | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` or the TOGO importer |
| Major | Distilled water is dimensionally wrong. | TOGO M1999 and NBRC Medium 1286 both carry Distilled water as 1 L, but the generated record converts it to `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` or the TOGO importer |
| Major | Source evidence is stranded in free-text notes instead of structured references and row-level sources. | The record has a free-text TOGO URL and original NBRC URL but no `references` list, and none of the five ingredient rows has a `source` field or source-specific evidence note. The source recipe cannot be checked by the reference validator because it has no structured reference entries. | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` |
| Minor | Bacto tryptone and yeast extract remain ungrounded. | The two complex ingredient rows have no `term` or `mediaingredientmech_chebi_term`; the repaired direct NBRC owner has curated groundings for the same two rows. | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` |
| Minor | The TOGO-derived record duplicates the direct NBRC 1286 owner. | Exact gitignore-independent searches found `data/normalized_yaml/bacterial/NBRC_1287.yaml` and this TOGO-derived owner pointing at the same NBRC Medium 1286 source URL and formulation; the direct NBRC owner is already materially more complete. | `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml`, `data/normalized_yaml/bacterial/NBRC_1287.yaml`, and merge rules |

## Recommended Edits

1. Update `data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` from TOGO M1999 and NBRC Medium 1286: preserve Distilled water as `1 L`, add `ph_value: 7.0`, and add a `Biphenyl crystal` row with `CHEBI:17097`/`VARIABLE` concentration or another explicit representation that preserves the source instruction.
2. Add structured source provenance: `references` entries for TOGO M1999 and direct NBRC Medium 1286, row-level `source` values or evidence notes for all components, and a curation-history event that names the inspected sources and exact fields changed.
3. Ground `Bacto tryptone` and `Yeast extract` consistently with the already-curated direct NBRC owner or leave them explicitly unresolved with a concrete quality flag if the terms remain ambiguous after checking the packaged MIM label index.
4. Regenerate `data/merge_yaml/merged/` from normalized YAML and verify that the regenerated `1_3_Diluted_LB_Medium_with_Biphenyl.yaml` carries the corrected water unit, pH, biphenyl row, references, and new curation history.
5. Review duplicate handling with `data/normalized_yaml/bacterial/NBRC_1287.yaml`; once both records preserve the same NBRC Medium 1286 formulation, confirm whether the merge rules should emit one canonical generated artifact for the shared source formulation.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` after adding any Bacto tryptone, yeast extract, or biphenyl terms.
- `just validate-references data/normalized_yaml/bacterial/1_3_diluted_lb_medium_with_biphenyl.yaml` after adding structured references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating `data/merge_yaml/merged/`.
- Manual source comparison against live TOGO M1999 and NBRC Medium 1286 for all five quantitative rows, pH 7.0, and the vapor-phase biphenyl instruction.
- Manual duplicate check against `data/normalized_yaml/bacterial/NBRC_1287.yaml` and any regenerated merged artifact with `nbrc.medium:1286`.

## Additional Notes

- The TOGO `/medium/M1999` route rendered only a JavaScript app shell. The inspected source payload came from the same app's POST endpoint, `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid`, with body `gm_id=M1999`.
- TOGO M1999 preserves the NBRC rows and comments but does not expose a structured preparation sequence for autoclaving, sterilization, storage, or incubation. No missing sterilization finding was filed because the inspected source did not support a narrower assertion.
- The reference validator passed with 0 checks because this record has no structured `references` entries.
