# YAML Record Review: tcg_medium

- Repository: CultureMech
- Record: `data/merge_yaml/merged/tcg_medium__2e0f2453.yaml`
- Started UTC: 2026-09-25T09:05:32Z
- Finished UTC: 2026-09-25T09:07:32Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:010148` for `tcg_medium`, a TOGO M743 / JCM Medium 720 TCG Medium import merged from `TOGO_M743_TCG_Medium.yaml` with fingerprint `2e0f2453d03a22f3a5a1e334c852043360d74758e2729ddfcecaaa6a466bffcf`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed; 1 file scanned and 0 error rows written to `/private/tmp/tcg_medium__2e0f2453.strict.tsv`.
- Reference validation: Passed; 1 file validated, 0 external checks.
- Term validation: Passed.
- Embedded `curation_history`: Not checked; `just validate-history` validates standalone `history/`, not merged `MediaRecipe.curation_history`.

## Identity and Grounding

The generated `TOGO:M743` identity is the right source identity for the target. TOGO M743 reports the recipe name as `TCG Medium`, cites `JCM_M720` as the original medium, and links to JCM `GRMD=720`; the JCM page labels record 720 as `TCG MEDIUM` and lists the same solid TCG formulation.

MediaDive `J720` is the same JCM medium and therefore the source duplicate that should collapse with TOGO M743. The generated `kg_microbe_match: mediadive.medium:7` is unrelated: MediaDive numeric medium 7 is `ANCYLOBACTER - SPIROSOMA MEDIUM`, with glucose, peptone, yeast extract, agar, distilled water, and pH 7.0.

## Evidence

TOGO M743, JCM Medium 720, and MediaDive J720 agree on a 1 L medium containing:

- 3 g Tryptone, BD-Difco in JCM/TOGO/J720.
- 5 g Casitone, BD-Difco in JCM/TOGO/J720.
- 4 g glucose.
- 15 g agar.
- 1 L artificial seawater in TOGO/JCM, represented as 1000 mL artificial sea water by MediaDive J720.

Glucose and agar are valid simple ChEBI mappings. The BD-Difco tryptone product, BD-Difco Casitone product, and artificial seawater are source-disclosed complex materials and should stay unmapped instead of being forced to one ChEBI molecule.

## Completeness

The target contains the five expected ingredient identities, but it is stale relative to the repaired normalized record `data/normalized_yaml/bacterial/TOGO_M743_TCG_Medium.yaml`. The repaired normalized record already carries source-qualified ingredient notes, `references`, `data_quality_flags`, and a `SOURCE_DUPLICATE` parent link to `data/normalized_yaml/bacterial/JCM_J720_TCG_MEDIUM.yaml`.

No organism growth evidence is present in the TOGO or JCM recipe pages, so the empty `target_organisms` slot is not a defect in this generated recipe review.

## Findings

- The artificial seawater row is quantitatively wrong in the generated record: TOGO and JCM list 1 L artificial seawater, while `data/merge_yaml/merged/tcg_medium__2e0f2453.yaml` stores `Artificial seawater` as `1 G_PER_L`.
- The generated merge is stale relative to the September TCG seawater repair; the generated target lacks the corrected `1000 ML_PER_L` seawater row, source annotations, unmapped-complex notes, references, data quality flags, and source-duplicate relationship now present in `data/normalized_yaml/bacterial/TOGO_M743_TCG_Medium.yaml`.
- The source-duplicate merge missed JCM Medium 720. TOGO M743 and JCM J720 describe the same solid TCG formulation, but the generated output still has a standalone `tcg_medium__2e0f2453.yaml` record beside `data/merge_yaml/merged/TCG_MEDIUM.yaml`.
- The `kg_microbe_match` points to `mediadive.medium:7`, a different DSMZ recipe, rather than the JCM `J720` identity that matches the TOGO source.

## Recommended Edits

- Keep fixes in `data/normalized_yaml/bacterial/TOGO_M743_TCG_Medium.yaml` and the merge/import logic, then regenerate merged YAML; do not hand-edit `data/merge_yaml/merged/tcg_medium__2e0f2453.yaml`.
- Preserve the September repair that represents artificial seawater as `1000 ML_PER_L` with TOGO/JCM source notes.
- Drop or regenerate the erroneous `kg_microbe_match: mediadive.medium:7` assignment for this TOGO record.
- Rerun duplicate merging so TOGO M743 collapses into the JCM Medium 720 solid TCG record as a `SOURCE_DUPLICATE`, while DSMZ Medium 1009 / KOMODO 1009 remain the liquid TCG source-duplicate branch.

## Follow-up Checks

- Revalidate the regenerated TCG records with schema, strict, reference, and term validators.
- Search exact `TOGO:M743`, `mediadive.medium:J720`, `mediadive.medium:1009`, and `komodo.medium:1009` identities, including ignored files, to confirm the merged output preserves one solid JCM/TOGO duplicate set and one liquid DSMZ/KOMODO duplicate set.
- Search exact `kg_microbe_match: mediadive.medium:7`, including ignored files, to audit the same incorrect KG-Microbe match on other unrelated generated and normalized records.

## Additional Notes

Exact source-identity and `kg_microbe_match` searches used `rg --no-ignore --hidden`, so ignored files were included.
