# YAML Record Review: TOGO Medium M1980

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1980.yaml
- Started UTC: 2026-09-25T15:14:58Z
- Finished UTC: 2026-09-25T15:15:37Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/togo_medium_m1980.yaml`
- Class: `MediaRecipe`
- Stable ID: `CultureMech:008563`
- Label: `TOGO Medium M1980`
- Source accession: TOGO M1980 importing NBRC Medium 1264
- Generated status: derived merge artifact with `merged_from: togo_medium_m1980`; future source edits belong in `data/normalized_yaml/bacterial/togo_medium_m1980.yaml`, followed by rerunning `src/culturemech/merge/merge_recipes.py`

## Validation

| Check | Result |
|---|---|
| LinkML schema validation on the generated record with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` through Python 3.11 `uv --no-project --offline` | Passed; `No issues found` |
| Strict schema validation with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1980.yaml --out /private/tmp/m1980.strict.tsv --workers 1 --quiet` | Passed; 1-line TSV containing only the header and 0 error rows |
| Reference validation with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1980.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; exited 0 with no diagnostics |
| Term validation with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1980.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` `pkg_resources` deprecation warning |
| Embedded curation-history validation | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` blocks embedded in merged YAML |

## Identity and Grounding

The record identity is stable and source-aligned: the record has `CultureMech:008563`, `name: togo_medium_m1980`, a `TOGO:M1980` media term, and notes pointing to TOGO M1980 and NBRC Medium 1264. The inspected TOGO API payload for M1980 reports `original=NBRC_M1264` and pH 8.5, matching the NBRC 1264 page fetched for `NO=1264`.

Ingredient-level grounding is mixed. Water, NaCl, NH4Cl, K2HPO4, sodium pyruvate, and glucose have label-compatible CHEBI groundings. Yeast extract and optional gellan gum remain ungrounded mixtures, which is acceptable. The main identity defect is representational: `TAPS*` is treated as a stock solution even though the NBRC footnote only expands the TAPS abbreviation.

## Evidence

The main solutes and amounts in the TOGO/NBRC source are present: 940 ml distilled water, 7.5 g yeast extract, 3 g NaCl, 2.5 g NH4Cl, 2.5 g K2HPO4, 0.1 g sodium pyruvate, 60 ml 1.0 M Na2CO3 solution, 10 g optional gellan gum, 24 g TAPS, and 5 g glucose.

The closest source text does not support several generated details:

- The record encodes 940 ml distilled water as `940 G_PER_L` and 60 ml 1.0 M Na2CO3 solution as `60 G_PER_L`; both are source volumes, not mass-per-volume quantities.
- `TAPS*` is stored under `solutions` with an empty composition, but the NBRC note `N-Tris hydroxymethyl methyl-3-aminopropanesulfonic acid)` is a footnote for the reagent name, not a separate stock recipe.
- The record omits the source procedure to mix everything except the Na2CO3 stock, autoclave at 121 C for 20 min, aseptically add 1 M Na2CO3 afterward, and check a final pH of about 8.5.

## Completeness

The generated record is complete enough for source identity and the top-level TOGO/NBRC provenance. It is materially incomplete for preparation and pH because the TOGO/NBRC comments carry the post-autoclave carbonate addition and the final pH check.

No target-organism or growth evidence is asserted by the record, so the empty growth fields are not a defect.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `TAPS*` was migrated into `solutions` as an empty stock recipe. | TOGO/NBRC list 24 g TAPS in the main recipe and use the asterisk only to expand the reagent name; no TAPS stock composition is described. | `data/normalized_yaml/bacterial/togo_medium_m1980.yaml` |
| Major | Two source volumes are represented with `G_PER_L`. | TOGO/NBRC use 940 ml distilled water and 60 ml 1.0 M Na2CO3 solution; the generated record uses `940 G_PER_L` and `60 G_PER_L`. | `data/normalized_yaml/bacterial/togo_medium_m1980.yaml` |
| Major | Required pH and preparation claims are stranded in the source comments and absent from structured fields. | NBRC Medium 1264 instructs autoclaving at 121 C for 20 min, aseptic post-autoclave Na2CO3 addition, and checking the final pH at about 8.5. | `data/normalized_yaml/bacterial/togo_medium_m1980.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/togo_medium_m1980.yaml`, move `TAPS*` from `solutions` back to a normal ingredient while preserving 24 g/L and the abbreviation note.
2. Represent distilled water and Na2CO3 solution with volume units instead of `G_PER_L`; keep the Na2CO3 item as a true 60 ml/L addition of 1.0 M stock.
3. Add structured pH and preparation steps for the 121 C autoclave, post-autoclave Na2CO3 addition, and final pH 8.5 check.
4. Regenerate `data/merge_yaml/merged/togo_medium_m1980.yaml` through `src/culturemech/merge/merge_recipes.py`.

## Follow-up Checks

- Rerun schema, strict, reference, and term validation for `data/normalized_yaml/bacterial/togo_medium_m1980.yaml` and the regenerated `data/merge_yaml/merged/togo_medium_m1980.yaml`.
- Manually compare the regenerated M1980 solution list against TOGO M1980 / NBRC Medium 1264 to ensure only Na2CO3 remains a stock-solution addition.
- Check `git diff --check` before committing the normalized edit and regenerated merge artifact.

## Additional Notes

This review used TOGO M1980 and NBRC Medium 1264 as inspected source records. Searches used to resolve the normalized owner and the generated merge entry were exact-ID searches with `--no-ignore --hidden`, so ignored files were included.
