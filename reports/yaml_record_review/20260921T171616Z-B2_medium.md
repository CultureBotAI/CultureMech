# YAML Record Review: b2_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/B2_medium.yaml
- Started UTC: 2026-09-21T17:14:35Z
- Finished UTC: 2026-09-21T17:16:16Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:008775 |
| name | b2_medium |
| original_name | B2 medium |
| category | bacterial |
| media_term | TOGO Medium M2180, TOGO:M2180 |
| generated status | Generated merge artifact in data/merge_yaml/merged |
| maintained owner | data/normalized_yaml/bacterial/b2_medium.yaml |

The target is the generated merge for one TOGO source record, `b2_medium`, with
merge fingerprint `ea7e07631c4453ae057ada040a5007ad6cac693a7e5a0574a3fc3e0c74c382bf`.

## Validation

| Check | Result |
|---|---|
| Open schema | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/B2_medium.yaml` completed with no errors. |
| Strict validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/B2_medium.yaml --out /private/tmp/B2_medium.strict.tsv --workers 1 --quiet` completed with no errors. |
| Reference validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/B2_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` completed with 0 checks. |
| Term validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/B2_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` completed with no errors, aside from a non-fatal `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone `history/` files, not a focused embedded `MediaRecipe.curation_history` validator for one merge record. |
| Direct `just` validators | Not checked: the project-level `uv` environment currently fails before target-specific validation while building `llvmlite==0.46.0` under Python 3.13, with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project LinkML validator workaround above exercised the target schema, strict, reference, and term checks. |

## Identity and Grounding

- The record identity is supported. TOGO API data for `M2180` names `B2 medium`, and
  the generated record's `TOGO:M2180` grounding matches that source identity.
- `medium_type: COMPLEX` and `composition_type: UNDEFINED` are supported because TOGO
  classifies Yeast extract and Casein hydrolysate as undefined components.
- `physical_state: LIQUID` is reasonable for the inspected TOGO recipe because it has
  no agar or other gelling component.
- Distilled water, NaCl, K2HPO4, and Glucose are grounded to source-compatible CHEBI
  terms; Yeast extract and Casein hydrolysate are explicitly unresolved undefined
  components.

## Evidence

Supported by inspected source text:

- TOGO M2180 lists B2 medium with 93.4% Distilled water, 2.5% Yeast extract,
  2.5% NaCl, 0.1% K2HPO4, 0.5% Glucose, and 1% Casein hydrolysate.
- TOGO M2180 supplies GMO component metadata that supports the record's imported
  roles and component-property notes.
- TOGO M2180 records pH 7.5 in both `meta.ph` and a pH comment.

Unsupported or stale in the generated target:

- The pH 7.5 source value is missing from the generated record and its normalized owner.

## Completeness

- Source provenance is sufficient to recover the formulation through the structured
  TOGO `M2180` API payload.
- Empty organism and growth-metric slots are acceptable for this source recipe. The
  inspected TOGO payload establishes a formulation, not a growth claim for a specific
  strain.
- The generated record's standalone `references` slot is empty; the source pointer
  lives in `media_term` and `notes`.
- Exact gitignore-independent search covered `reports/yaml_record_review` for an
  existing `B2_medium.md` report and found no prior report for this exact generated
  record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The pH 7.5 value is missing. | TOGO M2180 has `meta.ph: 7.5` and an explicit `pH 7.5` comment; neither the generated merge nor `data/normalized_yaml/bacterial/b2_medium.yaml` has `ph_value` or a pH preparation step. | `data/normalized_yaml/bacterial/b2_medium.yaml` or the TOGO importer. |

No blocker or minor findings found.

## Recommended Edits

1. Add `ph_value: 7.5` to `data/normalized_yaml/bacterial/b2_medium.yaml`.
2. Regenerate `data/merge_yaml/merged/B2_medium.yaml` from the corrected normalized
   owner.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/b2_medium.yaml` after adding
  the missing pH.
- Manual comparison with TOGO M2180 to verify pH 7.5 and the six percentage rows.
- `just verify-merges` after regenerating the merge layer.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated record has
  no `references` block.
