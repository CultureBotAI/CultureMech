# YAML Record Review: JCM MEDIUM No. 188

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_188.yaml
- Started UTC: 2026-09-23T16:37:27Z
- Finished UTC: 2026-09-23T16:37:53Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_188.yaml`
- Stable ID: `CultureMech:002549`
- Name: `jcm_medium_no_188`
- Original name: `JCM MEDIUM No. 188`
- Category: `bacterial`
- Media term: `mediadive.medium:J188`
- Source: MediaDive import of JCM Medium 188, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=188`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_188`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_188.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_188.yaml --out /private/tmp/jcm_medium_no_188.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_188.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_188.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators check the stale generated YAML, not freshness relative to normalized curation.

## Identity and Grounding

The record identifies MediaDive `J188` for JCM Medium 188. The live JCM page, TogoMedium M181, and the maintained normalized `jcm_medium_no_188` owner agree that this is a peptone/yeast-extract/malt-extract/glucose/olive-oil agar adjusted to pH 6.0.

The generated record has the right identity, pH, and six non-water ingredient amounts. It is stale relative to `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml`, which was curated on 2026-09-12 to add distilled water, source notes, complex-ingredient groundings, and source-duplicate / pH-variant relationships.

## Evidence

Inspected evidence:

- The live JCM `GRMD=188` page lists 5 g peptone, 3 g yeast extract, 3 g malt extract, 10 g glucose, 10 g olive oil, 15 g agar, 1 L distilled water, and pH 6.0.
- TogoMedium M181 points to `JCM_M188` and `GRMD=188` and carries the same formula and pH.
- The regular MediaDive `medium/J188` page loads and its composition table includes peptone, yeast extract, malt extract, glucose, olive oil, agar, and distilled water. The MediaDive REST URL `/rest/medium/J188` returned an empty body during this review and was not used as evidence.
- `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml` already represents distilled water as 1000 ml/L, grounds peptone, yeast extract, malt extract, glucose, olive oil, water, and agar, and marks TOGO M181 as a source duplicate.
- `data/normalized_yaml/bacterial/togo_medium_m181.yaml` contains the same repaired ingredient set and links back to JCM 188 as its parent `SOURCE_DUPLICATE`.

Supported:

- The generated record's MediaDive ID, original name, category, physical state, pH, and six non-water ingredients are supported by JCM and by the maintained normalized owner.

Unsupported or incomplete:

- The generated record omits distilled water, which JCM lists and the normalized owner already contains.
- The generated record was produced before the 2026-09-12 repair, so it also omits the repaired source notes, complex extract groundings, `has_ontology_mappings` / `ingredients_curated` flags, and JCM 188 source-duplicate and pH-variant relations.
- The generated record has not merged the repaired `togo_medium_m181` source duplicate.

## Completeness

Consequential gaps:

- The generated artifact is stale and should be regenerated from `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml`.
- The generated artifact should also incorporate the already-repaired TOGO M181 duplicate after merge regeneration.

Empty slots that are acceptable:

- No stock solution appears in the JCM 188 source.
- No target-organism or growth-metric claim is represented.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=188`, exact `JCM_M188`, and exact `jcm_medium_no_188` IDs found only the direct MediaDive/JCM record and the TOGO M181 wrapper. No other normalized or merged copy of JCM 188 was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record is stale relative to the maintained normalized JCM 188 record. | `data/merge_yaml/merged/jcm_medium_no_188.yaml` has an August 2026 one-source `merge_recipes.py` event and no water row; `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml` has a September 12, 2026 curation event and a 1000 ml/L distilled-water row. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/jcm_medium_no_188.yaml`. |
| Major | The generated record has not merged its already-curated same-source duplicate. | The normalized JCM 188 owner has `togo_medium_m181` as a `SOURCE_DUPLICATE`, and the normalized TOGO M181 owner links back to JCM 188; the generated JCM 188 record still says it merged only `jcm_medium_no_188`. | Regenerate `data/merge_yaml/merged/` after the September normalized repairs. |

## Recommended Edits

1. Regenerate merged records from the maintained normalized JCM 188 and TOGO M181 inputs.
2. Verify that the regenerated JCM 188 generated record contains the 1000 ml/L distilled-water row.
3. Verify that regenerated merge provenance either collapses JCM 188 and TOGO M181 into one canonical generated recipe or explicitly preserves their source-duplicate relation.
4. Confirm the existing TOGO M1428 pH-variant relation remains a pH variant, not a source duplicate, after regeneration.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the regenerated JCM 188 generated record.
- Run `just verify-merges` and `just audit-merge-freshness` after regeneration.
- Compare the regenerated record against JCM `GRMD=188` for six dry rows, 1 L distilled water, and pH 6.0.
- Search with `rg --no-ignore --hidden 'GRMD=188([^0-9]|$)|JCM_M188([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm duplicate-source handling with ignored files included.

## Additional Notes

- This review found no new defect in the maintained normalized JCM 188 input. The problem is that the generated `data/merge_yaml/merged` layer lags the September normalized repair.
