# YAML Record Review: bl_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BL_Agar.yaml
- Started UTC: 2026-09-21T21:57:00Z
- Finished UTC: 2026-09-21T21:58:08Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/BL_Agar.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008201` |
| Name | `bl_agar` |
| Original name | `BL Agar` |
| Category | `bacterial` |
| Media term | `TOGO:M1645` / `BL Agar` |
| Source lineage | TOGO M1645 from NBRC medium 848 |
| Merge fingerprint | `2ba2e650a6bca51c104fad346564d233a8c13c23d5569d058ce72ebdf4c584dd` |
| Merged from | `bl_agar` |
| Generated status | Generated from `data/normalized_yaml/bacterial/bl_agar.yaml`; future fixes belong in that normalized owner or the TOGO importer and must then regenerate `data/merge_yaml/merged/`. |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BL_Agar.yaml` | Passed. |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BL_Agar.yaml --out /private/tmp/BL_Agar.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BL_Agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed structurally, but the stale generated file has no reference checks to perform: 1 file validated, 0 total checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BL_Agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed with only the known `eutils` / `pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone `history/` files, not a focused one-record check for embedded `MediaRecipe.curation_history` lists. |

The direct `just` targets were not rerun for this record because the project environment currently fails before target-specific validation while building `llvmlite==0.46.0` under Python 3.13; the commands above run the same validators in the cached Python 3.11 no-project environment used for this review batch.

## Identity and Grounding

- The reviewed merge is generated from one active owner, `data/normalized_yaml/bacterial/bl_agar.yaml`, and its ID `CultureMech:008201` is registered to that owner.
- The live TOGO M1645 API identifies the recipe as `BL Agar`, original media ID `NBRC_M848`, and source URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=848`. The live NBRC page also identifies medium 848 as `BL Agar`.
- NBRC 848 and TOGO M1645 agree on the three source ingredients: 58 g BL Agar product, 50 ml defibrinated horse blood, and 950 ml distilled water.
- The generated record is stale relative to its maintained owner. The owner has a 2026-09-12 `RESOLVED_TOGO_M1645_SCORE15` curation event from `repair_togo_m1645_bl_agar_score15.py`; the generated merge still reflects the pre-repair shape from its 2026-08-06 merge.
- An exact ignored-file-inclusive search over `data/normalized_yaml`, `data/merge_yaml`, `data/culturemech_id_registry.tsv`, `data/culturemech_recipe_catalog.tsv`, `reports`, `scripts`, and `history` for `CultureMech:008201`, exact `TOGO:M1645`, the exact merge fingerprint, `NBRC_M848`, `bl_agar.yaml`, and `BL_Agar` found the normalized owner, generated merge, expected indexes, the 2026-09-12 repair script, archived validation rows, and the separate TOGO M6 BL Agar (Glucose Blood Liver Agar) record.

## Evidence

### Source Claims That Are Supported

| Claim in normalized owner | Source check |
|---|---|
| NBRC medium 848 / TOGO M1645 is `BL Agar`. | Supported by both the live NBRC 848 page and TOGO M1645 API metadata. |
| The recipe uses 58 g BL Agar, 50 ml defibrinated horse blood, and 950 ml distilled water. | Supported by both NBRC and TOGO. |
| Horse blood is excluded from autoclaving, added aseptically at 5% final after the base is cooled to about 50 C, then the mixture is quickly dispensed into sterile test tubes or Petri dishes. | Supported by both NBRC and TOGO comments. |
| The asterisk on `BL Agar*` denotes Nissui Pharmaceutical Co. Ltd. | Supported by the NBRC footnote mirrored in TOGO M1645. |

### Unsupported or Misplaced Claims in the Generated Record

| Record claim | Source evidence | Assessment |
|---|---|---|
| `Distilled water` is 950 `G_PER_L`. | NBRC and TOGO say 950 ml. | Stale unsupported unit; the maintained owner now has 950 `ML_PER_L`. |
| `Defibrinated horse blood` is 50 `G_PER_L`. | NBRC and TOGO say 50 ml. | Stale unsupported unit; the maintained owner now has 50 `ML_PER_L`. |
| `BL Agar*` is an `Unknown solution` with an empty `composition`. | NBRC and TOGO list 58 g BL Agar product directly and footnote that the asterisk marks Nissui Pharmaceutical. | Stale importer artifact; the maintained owner now retains it as an opaque 58 g/L base/solidifying ingredient. |

## Completeness

- **Generated freshness:** missing. The generated record does not include the normalized owner's 2026-09-12 repair that removed the empty solution wrapper, corrected water and blood units, added NBRC preparation steps, and added TOGO/NBRC references.
- **Preparation:** stale/missing in the generated merge. NBRC and the normalized owner both specify autoclaving without horse blood, cooling to about 50 C, adding blood aseptically to 5% final, and quick dispensing; the generated merge has no `preparation_steps`.
- **References:** stale/missing in the generated merge. The normalized owner has TOGO and NBRC references; the generated merge has no `references`.
- **Growth claims:** no specific `target_organisms` or growth evidence are asserted in this record, so those optional slots are correctly empty for the inspected source recipe.
- **Report absence:** a `find` search over `reports/yaml_record_review` found no prior `*-BL_Agar.md` report before this file was written.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `data/merge_yaml/merged/BL_Agar.yaml` is stale after the September normalized repair. | The merge still stores only water and horse blood as mass ingredients plus an empty `Unknown solution` for `BL Agar*`. `data/normalized_yaml/bacterial/bl_agar.yaml` now stores all three source rows directly with the correct units, preparation steps, data-quality flags, and TOGO/NBRC references. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/bl_agar.yaml`; do not edit the generated file. |
| Major | Source volume units are wrong in the generated merge. | NBRC 848 and TOGO M1645 list 950 ml distilled water and 50 ml defibrinated horse blood, while the generated record has `G_PER_L` for both. | Already corrected in `data/normalized_yaml/bacterial/bl_agar.yaml`; regeneration should propagate the fix. |
| Major | The BL Agar product is modeled as an empty stock solution. | NBRC lists BL Agar at 58 g and says the product is from Nissui Pharmaceutical; it is not a named stock solution with an omitted composition. | Already corrected in `data/normalized_yaml/bacterial/bl_agar.yaml`; regeneration should propagate the fix. |
| Major | NBRC preparation and source references are missing from the generated merge. | The normalized owner includes four preparation steps and two source references added by the 2026-09-12 repair, but the generated merge still has neither. | Already corrected in `data/normalized_yaml/bacterial/bl_agar.yaml`; regeneration should propagate the fix. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/BL_Agar.yaml` from the current `data/normalized_yaml/bacterial/bl_agar.yaml`.
2. Run the merge freshness/audit checks that should catch a merge still carrying an August 2026 source projection after its normalized owner changed in September 2026.
3. Do not make recipe field edits directly in `data/merge_yaml/merged/BL_Agar.yaml`; the authoritative normalized owner already contains the source-supported unit, ingredient, preparation, and reference fixes.

## Follow-up Checks

- Run `just validate data/normalized_yaml/bacterial/bl_agar.yaml` to confirm the repaired owner still validates.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating the merge.
- Re-run focused schema, strict, term, and reference validators on the regenerated `data/merge_yaml/merged/BL_Agar.yaml`.
- Manually compare the regenerated merge against NBRC 848 or TOGO M1645 to confirm it contains three direct ingredients, four preparation steps, and source references, with no `Unknown solution`.

## Additional Notes

- This is one of the rare reviewed records where the maintained input already reflects the correct curation and the generated merge is simply stale.
- The exact ignored-file-inclusive search also found TOGO M6 `BL Agar (Glucose Blood Liver Agar)`, but that is a fuller NBRC medium 6 recipe and is not the same source identity as NBRC 848 / TOGO M1645 BL Agar.
