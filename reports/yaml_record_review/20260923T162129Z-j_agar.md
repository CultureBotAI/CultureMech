# YAML Record Review: j_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/j_agar.yaml`
- Started UTC: 2026-09-23T16:19:00Z
- Finished UTC: 2026-09-23T16:21:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:004666` |
| Label | `j_agar` |
| Source term | `komodo.medium:262`, `J - AGAR` |
| Generated target | `data/merge_yaml/merged/j_agar.yaml` |
| Merged owners | `data/normalized_yaml/bacterial/KOMODO_262_J_-_AGAR.yaml`; `data/normalized_yaml/bacterial/j_agar.yaml` |

This generated record merges the KOMODO Medium 262 wrapper with the direct DSMZ
Medium 262 import. The merge is identity-correct but incomplete: the direct
DSMZ owner contributes source preparation instructions that are missing from
the generated record.

## Validation

| Check | Result |
|---|---|
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/j_agar.yaml` | Passed. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/j_agar.yaml --out /private/tmp/j_agar.strict.tsv --workers 1 --quiet` | Passed; the TSV had only its header. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/j_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the configured reference pass performed 0 checks. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/j_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed with the known `pkg_resources` warning from `eutils`. |
| Embedded `curation_history` validator | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not the embedded `MediaRecipe.curation_history` block in generated YAML. |

## Identity and Grounding

KOMODO Medium 262, DSMZ/MediaDive Medium 262, and TOGO M2342 all point to the
same DSMZ Medium 262 PDF for J - AGAR. The generated target's
`komodo.medium:262` identity is therefore compatible with the direct
`mediadive.medium:262` parent it merged.

An ignored-file-inclusive exact `rg` for `komodo.medium:262`,
`mediadive.medium:262`, and `TOGO:M2342` under `data/normalized_yaml`,
`data/merge_yaml/merged`, and `reports/yaml_record_review` found the three
expected normalized owners, this generated KOMODO/DSMZ target, the separate
generated TOGO target `j_agar__3476a10f.yaml`, and generated JSON indexes.

## Evidence

The five dry ingredient rows and pH range are source-supported. DSMZ Medium
262 and MediaDive 262 specify 5 g tryptone, 15 g yeast extract, 2 g glucose,
3 g K2HPO4, 15 g agar, 1000 ml distilled water, and pH 7.3 to 7.5.
TOGO M2342 mirrors the same formula and source URL.

The generated target preserves the five non-water ingredients and the pH range,
but it omits DSMZ's 1000 ml distilled water row.

The generated target also omits the preparation instruction. DSMZ says to
adjust pH to 7.3 to 7.5, sterilize glucose separately, and add glucose after
sterilization; the maintained direct DSMZ owner carries that text in a
`preparation_steps` entry, but the generated merge does not.

## Completeness

The generated target is incomplete for a user trying to follow DSMZ Medium
262, because it lacks both the solvent row and the instruction to sterilize
glucose separately before aseptic addition to the basal medium.

The TOGO wrapper for the same DSMZ PDF remains a separate generated record.
TOGO M2342 preserves the 1000 ml water row but drops the preparation comment,
while the direct DSMZ owner preserves the preparation comment but drops water.
Both source views need normalization so all three wrappers collapse into one
complete generated J - AGAR record.

`target_organisms` and `growth_metrics` can remain empty: the inspected
MediaDive, DSMZ, and TOGO source records are medium-formula sources, not
strain-specific growth studies.

The exact `find reports/yaml_record_review -maxdepth 1 -name '*j_agar*.md'`
search included ignored files and found no previous J - AGAR report before
this report was created.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The generated KOMODO/DSMZ merge dropped the glucose sterilization and aseptic-addition instruction. | DSMZ Medium 262 and `data/normalized_yaml/bacterial/j_agar.yaml` state that glucose is sterilized separately and added after sterilization; `data/merge_yaml/merged/j_agar.yaml` has no `preparation_steps`. | Merge logic or the canonical KOMODO owner `data/normalized_yaml/bacterial/KOMODO_262_J_-_AGAR.yaml`. |
| Major | DSMZ's 1000 ml water row is missing from the generated target. | DSMZ 262, MediaDive 262, and TOGO M2342 all list 1000 ml distilled water. | `data/normalized_yaml/bacterial/j_agar.yaml`, `data/normalized_yaml/bacterial/KOMODO_262_J_-_AGAR.yaml`, or import/enrichment rules. |
| Major | The TOGO M2342 wrapper for the same DSMZ 262 source remains split. | Exact source-ID search found `j_agar__3476a10f.yaml` for TOGO M2342; it cites the same DSMZ Medium 262 PDF as the reviewed target. | `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`, the DSMZ/KOMODO owners, and merge reconciliation. |

## Recommended Edits

1. Preserve the DSMZ preparation step when KOMODO Medium 262 and direct DSMZ
   Medium 262 are merged.
2. Add the supported 1000 ml distilled-water row to the DSMZ and KOMODO
   normalized owners.
3. Add the same preparation step to `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`
   or the TOGO importer so the TOGO source is complete enough to merge with
   DSMZ and KOMODO.
4. Regenerate `data/merge_yaml/merged/` and verify that KOMODO 262, MediaDive
   262, and TOGO M2342 produce one J - AGAR generated recipe.

## Follow-up Checks

- Run `just validate-schema` and `just validate-strict` on the three J - AGAR
  normalized owners.
- Run `just validate-terms` after adding the water row.
- Run `just validate-references` after adding structured DSMZ, MediaDive,
  KOMODO, and TOGO source references.
- Regenerate merged YAML and rerun schema, strict, term, and reference
  validation on the single J - AGAR target.
- Manually compare the regenerated record with DSMZ Medium 262, MediaDive 262,
  and TOGO M2342 for water, pH, glucose sterilization, and ingredient order.

## Additional Notes

- No blocker findings were found.
- The source fetches used MediaDive REST `/rest/medium/262`, the DSMZ Medium
  262 PDF, and TOGO `/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2342`.
