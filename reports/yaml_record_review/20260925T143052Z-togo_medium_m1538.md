# YAML Record Review: togo_medium_m1538

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1538.yaml
- Started UTC: 2026-09-25T14:28:00Z
- Finished UTC: 2026-09-25T14:30:52Z
- Verdict: pass with minor issues

## Target

- Class: MediaRecipe
- ID: CultureMech:008085
- Name: togo_medium_m1538
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1538.yaml`.
- Source identity in the generated record: TOGO:M1538, original source NBRC_M329, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=329`.
- Merge lineage in the generated record: only `togo_medium_m1538`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1538.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1538.yaml --out /private/tmp/m1538.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1538.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it exited 0 with no diagnostics.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1538.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1538 resolves to NBRC_M329. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- NaCl is exactly grounded. Soluble starch is grounded only to the broader `starch` CHEBI term, which is a non-blocking near-parent fallback for the source label.

## Evidence

- TOGO M1538 and NBRC 329 support 2 g yeast extract, 10 g soluble starch, 50 g NaCl, 15 g agar if needed, and 1 L distilled water.
- NBRC 329 lists pH 7.3. The generated record has no pH field.
- The source scopes agar as "if needed"; the generated `physical_state: SOLID_AGAR` is acceptable only for the agar branch and does not expose the no-agar branch.

## Completeness

- Consequential gap: pH 7.3 is present in NBRC and TOGO but absent from the YAML.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1538` found no existing report for this record stem.

## Findings

- minor: `data/normalized_yaml/bacterial/togo_medium_m1538.yaml` omits the NBRC_M329 pH 7.3 value.
- minor: `Agar (if needed)` is represented as a solid physical state rather than as an optional branch.

## Recommended Edits

- Add `ph_value: 7.3` to `data/normalized_yaml/bacterial/togo_medium_m1538.yaml`.
- Preserve the optional agar scope either in preparation text or by splitting a solid variant if the corpus pattern supports it.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1538.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1538.yaml` and verify that pH 7.3 appears.

## Additional Notes

- Exact source inspection was bounded to TOGO M1538 and NBRC 329.
