# YAML Record Review: mc

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mc__f31d540f.yaml
- Started UTC: 2026-09-24T00:51:03Z
- Finished UTC: 2026-09-24T00:52:35Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000359 |
| Label | mc |
| Original label | MC |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mc__f31d540f.yaml |
| Maintained owner | data/normalized_yaml/bacterial/mc.yaml |
| Source accession | mediadive.medium:C49 |

The generated record is derived from `data/normalized_yaml/bacterial/mc.yaml`.
Its normalized owner has newer 2026-09-13 Casein and Yeast extract grounding, but
still carries the imported C49 concentration and serum-unit problems.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mc__f31d540f.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mc__f31d540f.yaml --out /private/tmp/mc__f31d540f.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mc__f31d540f.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mc__f31d540f.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The schema checks did not detect the final-volume and serum-unit mistakes.

## Identity and Grounding

- MediaDive C49 and the CCAP `MR_MC.pdf` identify this medium as `MC`
  / Modified Chang's Serum-Casein-Glucose-Yeast Extract Medium.
- Exact `find` and `rg --no-ignore --hidden` checks found two normalized
  `mc.yaml` files: the bacterial MediaDive C49 import, `CultureMech:000359`, and
  an algae CCAP MC record, `CultureMech:000086`, sourced to the same
  `MR_MC.pdf`. The filename-collision report already lists this cross-category
  `mc.yaml` pair as `DIFFERENT`.
- The algae owner was repaired on 2026-09-10 to the official CCAP PDF formula and
  was given structured CCAP source metadata on 2026-09-13.
- A gitignore-independent exact scan of the generated bacterial target and its
  normalized owner found no `references` or `target_organisms` sections.

## Evidence

### Supported

- The ingredient names Casein, Na2HPO4 x 7 H2O, KH2PO4, Yeast extract,
  D-Glucose, Liver digest, and Calf serum all come from MediaDive C49 and the
  CCAP MC PDF.
- pH 6.9, 5 x 180 ml aliquots, pressure cooking at 10 psi for 15 minutes,
  aseptic serum addition, and storage at 4 C are all supported by the CCAP PDF.
- The normalized algae sibling preserves more exact source labels for Casitone,
  Oxoid yeast extract, Oxoid liver digest, and gamma-irradiated sterile foetal
  calf serum.

### Unsupported or Over-scoped

- The first six ingredient concentrations are inflated by normalization against
  the pre-serum 900 ml working volume. The CCAP PDF labels the dry-component
  amounts `per litre`: 10 g casein digest, 2.5 g Na2HPO4.7H2O, 0.8 g KH2PO4,
  5 g yeast extract, 2.5 g D-glucose, and 2.5 g liver digest.
- The generated and normalized bacterial records omit the source water row even
  though the PDF says to add those first six constituents to 900 ml deionised
  water.
- `Calf serum` is not a 20 g/l ingredient. The source adds 5 x 20 ml to 5 x
  180 ml aliquots and states the final concentration as 10 percent, which the
  curated algae record represents as `100 ML_PER_L`.
- `Na2HPO4 x 7 H2O` is grounded to `CHEBI:34683` / `disodium hydrogenphosphate`,
  losing the source heptahydrate form.

## Completeness

- The bacterial C49 record is missing structured references. The PDF itself
  includes a De Jonckheere 1977 reference, but the YAML has no source or paper
  `references` block.
- `target_organisms` is empty and acceptable; the source says the recipe is for
  freshwater protozoa, but it does not assert a specific strain-level growth
  result for this CultureMech record.
- The generated target is stale relative to its normalized owner for the
  September 2026 Casein and Yeast extract groundings.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The bacterial MediaDive C49 record duplicates the same CCAP MC source already curated in the algae corpus. | `data/normalized_yaml/algae/mc.yaml` and `data/normalized_yaml/bacterial/mc.yaml` both cite `MR_MC.pdf`; the filename-collision report flags `mc.yaml` across bacterial/algae as `DIFFERENT`; the algae record already contains the official CCAP formula. | `data/normalized_yaml/bacterial/mc.yaml`, `data/normalized_yaml/algae/mc.yaml`, and merge/dedupe inputs. |
| major | Most dry ingredient concentrations are inflated by using 900 ml rather than the final liter. | The PDF states per-liter masses of 10, 2.5, 0.8, 5, 2.5, and 2.5 g, whereas MediaDive-derived rows store 11.1111, 2.77778, 0.888889, 5.55556, 2.77778, and 2.77778 G_PER_L. | `data/normalized_yaml/bacterial/mc.yaml` or the MediaDive importer. |
| major | The foetal calf serum addition has the wrong unit and amount. | CCAP adds 20 ml serum to each 180 ml aliquot for 10 percent final serum; the bacterial record stores `Calf serum` as `20 G_PER_L`. | `data/normalized_yaml/bacterial/mc.yaml` or the MediaDive importer. |
| major | The source water row is missing. | CCAP instructs adding the first six constituents to 900 ml deionised water; the bacterial record has no Deionized water ingredient. | `data/normalized_yaml/bacterial/mc.yaml`. |
| major | Na2HPO4 heptahydrate has an exact-form grounding error. | The source ingredient is `Na2HPO4.7H2O`, but the term is unsuffixed `disodium hydrogenphosphate`; hydrate state is identity-significant. | `data/normalized_yaml/bacterial/mc.yaml`. |
| minor | Preparation actions are poorly structured. | `To be added later:` is imported as a `MIX` step, the 5 x 180 ml dispensing and pressure-cooking instruction is labeled `POUR_PLATES`, and `Store at 4 C` is labeled `HEAT`. | `data/normalized_yaml/bacterial/mc.yaml` or the MediaDive importer. |

## Recommended Edits

1. Decide whether the bacterial C49 copy should collapse into the repaired algae
   MC record or remain as a source duplicate. If it remains, copy the official
   PDF formulation and source metadata into `data/normalized_yaml/bacterial/mc.yaml`.
2. Replace the 900 ml-normalized dry ingredient concentrations with the CCAP
   per-liter masses and add the 900 ml deionized-water row.
3. Represent sterile foetal calf serum as 100 ml/l, not 20 g/l.
4. Recheck exact ontology grounding for Na2HPO4.7H2O and leave it unresolved if
   no exact heptahydrate term is verified.
5. Split the preparation into dissolve, pH adjustment, aliquot, pressure-cook,
   aseptic serum-addition, and 4 C storage actions.
6. Add structured references for CCAP MC and the De Jonckheere 1977 paper if a
   stable PMID or DOI can be verified.

## Follow-up Checks

- Rerun focused open-schema, strict, term, and reference validation on the
  maintained owner and regenerated merged record.
- Recompare both bacterial and algae `mc.yaml` owners against the CCAP PDF to
  ensure there is only one chosen interpretation of the final liter.
- Run merge freshness and duplicate/collision checks so the same CCAP MC formula
  either collapses or remains explicitly linked as a duplicate.
- Repeat a gitignore-independent exact search for `CultureMech:000359`,
  `CultureMech:000086`, `mediadive.medium:C49`, and `MR_MC.pdf` after
  regeneration to confirm indexes and owners all reflect the chosen curation.

## Additional Notes

- The generated bacterial target lacks the 2026-09-13 Casein and Yeast extract
  exact groundings already present in `data/normalized_yaml/bacterial/mc.yaml`.
