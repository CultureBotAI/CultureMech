# YAML Record Review: nha_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/nha_medium__32dfcef0.yaml
- Started UTC: 2026-09-24T17:18:57Z
- Finished UTC: 2026-09-24T17:19:45Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/nha_medium__32dfcef0.yaml` as a generated `MediaRecipe` for TOGO M826, `CultureMech:010239`, label `nha_medium`, category `bacterial`, and physical state `SOLID_AGAR`.

The generated record was merged from `data/normalized_yaml/bacterial/TOGO_M826_NHA_Medium.yaml`. It is derived; future fixes belong in the normalized TOGO owner and in the relationship between M825, M826, and the direct JCM J793 import.

## Validation

| Check | Result |
|---|---|
| Open schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/nha_medium__32dfcef0.yaml` | Passed; `No issues found`. |
| Strict schema, `python scripts/validate_strict.py data/merge_yaml/merged/nha_medium__32dfcef0.yaml --out /private/tmp/nha_medium__32dfcef0.strict.tsv --workers 1 --quiet` | Passed; TSV had the header only, 1 line and 0 errors. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/nha_medium__32dfcef0.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were available. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/nha_medium__32dfcef0.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries inside a single merged YAML file. |

## Identity and Grounding

- The target denotes TOGO M826, NHA Medium, original source `JCM_M793-2`.
- Live JCM GRMD 793 returned `Nothing found`, so the current JCM page was unavailable for direct comparison.
- TOGO M826 was available and identifies M826 as the agar form of JCM 793, while TOGO M825 identifies the liquid form as original source `JCM_M793`.
- A gitignore-independent hidden-file search for `TOGO:M826`, `JCM_M793`, `GRMD=793`, `NHA Medium`, and `nha_medium` across `data/normalized_yaml` and `data/merge_yaml` found separate TOGO M825, TOGO M826, and direct JCM/MediaDive J793 owners plus separate generated outputs for those source records.

## Evidence

The inspected TOGO M826 API payload supports 1 L distilled water; 10 g MgSO4.7H2O; 240 g NaCl; 0.2 g CaCl2.2H2O; 5 g KCl; 3 g trisodium citrate; 5 g BD-Difco casamino acids; and 20 g/L agar. Its comments instruct adding the components to distilled water, bringing the volume to 1.0 L, adjusting the pH to 7.0-7.2, and adding 20.0 g/L agar for solid medium.

The generated record preserves the source masses and agar row, but it omits the pH/preparation comments and represents the 1 L final volume as `Distilled water` at `1 G_PER_L`.

## Completeness

- Consequential gap: pH 7.0-7.2 and the "bring volume to 1.0 L" preparation text are absent.
- Consequential error: 1 L distilled water is stored as `1 G_PER_L`.
- Consequential duplicate gap: the M826 agar form is unlinked from the M825 liquid form and the direct JCM/MediaDive J793 owner.
- Empty optional fields are not defects. The inspected TOGO source does not provide a target organism or incubation temperature.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | Preparation and pH were dropped. | TOGO M826 says to bring the recipe to 1.0 L and adjust pH to 7.0-7.2; the generated record has no `ph_value` and no `preparation_steps`. | Add the TOGO M826 pH and preparation comments to `data/normalized_yaml/bacterial/TOGO_M826_NHA_Medium.yaml`. |
| major | Distilled water has the wrong dimensionality. | TOGO M826 lists 1 L distilled water; the generated record stores `Distilled water` as `1 G_PER_L`. | Represent the water row as a 1 L final-volume solvent in the normalized owner. |
| major | The JCM 793 source family is split across three active records. | Exact hidden-file search found direct `mediadive.medium:J793`, TOGO M825 liquid, and TOGO M826 agar records, each with separate generated YAML. | Reconcile the direct JCM owner with TOGO M825 and link M826 as the agar variant. |

## Recommended Edits

1. Add pH 7.0-7.2 and the source volume-adjustment preparation to the M826 normalized owner.
2. Correct the distilled water row from `1 G_PER_L` to the 1 L final-volume semantics supported by TOGO.
3. Link TOGO M826 as the 20 g/L agar variant of TOGO M825 and reconcile both with the direct JCM/MediaDive J793 owner.
4. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the repaired M826 owner and regenerated merge.
- Re-run a gitignore-independent hidden-file search for `TOGO:M825`, `TOGO:M826`, `JCM_M793`, `JCM_M793-2`, and `GRMD=793` under `data/normalized_yaml` and `data/merge_yaml`.
- Manually compare the regenerated liquid and agar NHA records against TOGO M825 and M826; retry live JCM GRMD 793 in case it becomes available again.

## Additional Notes

The live JCM GRMD 793 URL returned `Nothing found` at review time. TOGO M825 and M826 still exposed the liquid and agar forms from that source family.
