# YAML Record Review: Modified 288 Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/modified_288_medium__c89a892d.yaml
- Started UTC: 2026-09-24T10:05:48Z
- Finished UTC: 2026-09-24T10:05:48Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Record | `data/merge_yaml/merged/modified_288_medium__c89a892d.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008535` |
| Name | `modified_288_medium` |
| Original name | `Modified 288 medium` |
| Category | `bacterial` |
| Medium source | TOGO `M1955`; original source NBRC `NBRC_M1233` |
| Maintained owner | `data/normalized_yaml/bacterial/modified_288_medium.yaml` |
| Generated status | Generated merge output from one normalized TOGO record |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema validation, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_288_medium__c89a892d.yaml` | Passed; exited 0 with "No issues found". |
| Strict validation, `python scripts/validate_strict.py data/merge_yaml/merged/modified_288_medium__c89a892d.yaml --out /private/tmp/modified_288_medium__c89a892d.strict.tsv --workers 1 --quiet` | Passed; 0 strict errors were reported and `/private/tmp/modified_288_medium__c89a892d.strict.tsv` contained only the header line. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/modified_288_medium__c89a892d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the focused run completed with 0 reference checks. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/modified_288_medium__c89a892d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not inline `MediaRecipe.curation_history` entries in a merged YAML record. |

## Identity and Grounding

The record identity is coherent: `CultureMech:008535`, `TOGO:M1955`, and `NBRC_M1233` all identify Modified 288 medium.

An exact `find data/normalized_yaml -name 'modified_288_medium.yaml'` search, which does not honor gitignore exclusions, found `data/normalized_yaml/bacterial/modified_288_medium.yaml` as the generated record's maintained owner.

All seven resolved ingredient groundings preserve the source chemical forms closely enough for this record: MgSO4 x 7H2O is grounded to magnesium sulfate heptahydrate, NH4Cl to ammonium chloride, K2HPO4 to dipotassium hydrogen phosphate, sodium lactate to sodium lactate, ferric citrate to iron(III) citrate, sodium thioglycolate to sodium thioglycolate, and distilled water to water. Bacto Yeast Extract is intentionally unresolved.

## Evidence

TOGO `M1955` and the live NBRC `NO=1233` page support the same formulation:

| Source claim | Record representation | Review |
| --- | --- | --- |
| Modified 288 medium is sourced from NBRC `NBRC_M1233` / NBRC page `NO=1233`. | The record cites TOGO `M1955`, original source `NBRC_M1233`, and the matching NBRC URL. | Supported. |
| The main formula contains 1 L distilled water plus 0.1 g MgSO4 x 7H2O, 0.5 g K2HPO4, 2.6 g sodium lactate, 0.1 g NH4Cl, 0.1 g Bacto Yeast Extract, 0.5 g sodium thioglycolate, and 0.006 g ferric citrate. | The record contains those eight ingredient labels and all seven non-water gram amounts as `G_PER_L`. | Supported for the gram-scale ingredients because the source formula is per 1 L; unsupported for the water row because 1 L final volume was imported as `1 G_PER_L`. |
| The pH is 6.8. | No pH is represented. | Incomplete. |
| Good growth can be obtained in semi-solid medium containing 1 g agar in 1 liter. | The generated record is only `LIQUID` and contains no agar variant or discussion of the semi-solid option. | Incomplete. |

## Completeness

Every visible base-medium ingredient in NBRC `NO=1233` is present with the right non-water amount, and the source gives no target organism or strain-specific growth table to capture.

The generated record is not complete enough to reproduce the NBRC formulation because it omits the pH 6.8 adjustment and drops NBRC's semi-solid agar option. The water row also remains a final-volume liter quantity encoded as a gram-per-liter mass concentration.

Empty target-organism and growth-evidence fields were not treated as defects. NBRC `NBRC_M1233` is a medium formulation page, not a growth-evidence page.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | Source final-volume water is represented as a mass concentration. | TOGO and NBRC say `Distilled water` is 1 L; the record stores it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/modified_288_medium.yaml`; TOGO unit normalization. |
| major | pH 6.8 is absent. | TOGO carries `ph: 6.8` and NBRC displays `pH 6.8`; the record has no pH field or pH preparation step. | `data/normalized_yaml/bacterial/modified_288_medium.yaml`; TOGO/NBRC comment import. |
| minor | The semi-solid agar option is absent. | NBRC and TOGO both carry the note that good growth can be obtained in semi-solid medium using 1 g agar in 1 liter; the record contains only the base liquid recipe. | `data/normalized_yaml/bacterial/modified_288_medium.yaml`; TOGO/NBRC comment import and variant modeling. |

## Recommended Edits

1. Re-curate `data/normalized_yaml/bacterial/modified_288_medium.yaml` so `Distilled water` is represented as the 1 L final volume, not `1 G_PER_L`.
2. Import pH 6.8 from TOGO `M1955` / NBRC `NBRC_M1233`.
3. Preserve the semi-solid option as a supported variant, preparation note, or discussion item scoped to the NBRC wording of 1 g agar in 1 liter.
4. Regenerate `data/merge_yaml/merged/modified_288_medium__c89a892d.yaml` after the normalized source is corrected.

## Follow-up Checks

1. Re-run focused open-schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/modified_288_medium.yaml` and the regenerated merge output.
2. Manually compare the regenerated ingredient list and pH against TOGO `M1955` and NBRC `NO=1233`.
3. Verify that the semi-solid agar option is either structured or explicitly preserved with the source wording, and that it does not incorrectly change the base recipe from liquid to solid or semi-solid.

## Additional Notes

The repository-level `just` validators were not used for this focused record review because the project currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused validators above were run with Python 3.11 and the offline uv cache.
