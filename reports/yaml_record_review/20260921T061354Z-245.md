# YAML Record Review: 245

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/245.yaml`
- Started UTC: 2026-09-21T06:11:26Z
- Finished UTC: 2026-09-21T06:13:54Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007480` |
| Name | `245` |
| Original name | `245` |
| Category | `bacterial` |
| Source term | Missing from the generated target; `data/normalized_yaml/bacterial/NBRC_245.yaml` now carries `nbrc.medium:244`, `NBRC Medium 244` |
| Source URL | Missing from structured `references`; the maintained owner now cites `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=244` |
| Merge fingerprint | `5d8b92533f1590222899f3b195d6cf4a33d6031e0a9e3ec4d899da1fc9c2c52c` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_245.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 244 record imported through `NBRC_245.yaml`. `data/merge_yaml/merged/245.yaml` is derived and stale: its maintained owner has since recovered the source composition, pH, source grounding, structured reference, and TOGO M1466 source-duplicate link.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/245.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/245.yaml --out /private/tmp/245.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/245.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/245.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The intended source identity is NBRC Medium 244:

- The legacy target name is `245`, but its import event says `Media No. 244`, and the maintained owner now grounds the record to `nbrc.medium:244`.
- The inspected live NBRC page for `NO=244` resolves and lists Medium No. 244 with the same yeast extract, malt extract, glucose, seawater, distilled water, optional agar, and pH 7.6 formulation now present in `data/normalized_yaml/bacterial/NBRC_245.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:007480`, `NBRC_245`, `NO=244`, `Media No. 244`, `nbrc.medium:244`, `M1466`, the merge fingerprint, and the concatenated legacy ingredient found the expected normalized owner, generated target, and linked TOGO M1466 source-duplicate records.

The generated record no longer matches that identity cleanly enough for publication. It lacks the explicit `media_term`, structured NBRC reference, individual ingredient rows, pH, and `variant_children` relation that are now in the authoritative normalized record.

Ingredient grounding in the generated target is unusable: all six source components plus pH are collapsed into one ungrounded `preferred_term`, and the second ingredient has an empty label with `value: Yeast extract` stored in the concentration value slot.

## Evidence

The inspected NBRC Medium 244 page supports this formulation:

- 4 g yeast extract.
- 10 g malt extract.
- 4 g glucose.
- 750 ml seawater.
- 250 ml distilled water.
- 20 g agar, if needed.
- pH 7.6.

Those same six components and pH are represented in `data/normalized_yaml/bacterial/NBRC_245.yaml`, but the generated target predates that recovery and still has the malformed pre-recovery rows. The target's generic `Dissolve ingredients in distilled water`, `Adjust pH if needed`, and `Sterilize by autoclaving` steps are also unsupported by the inspected NBRC table; the source page inspected for this review lists the formulation and final pH but not those procedural steps.

## Completeness

Consequential gaps:

- The six source components are not present as six structured ingredient rows in the generated target.
- `ph_value: 7.6` is absent from the generated target.
- NBRC Medium 244 provenance is absent from `media_term` and structured `references`.
- The source-duplicate relationship to `togo_medium_m1466` is absent from the generated target even though the maintained owner now has a `SOURCE_DUPLICATE` `variant_children` entry.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected NBRC source establishes a medium formulation, not an organism-specific growth outcome.

Bounded searches:

- The `rg --no-ignore --hidden` search described above included ignored files under the searched paths and did not find a second record using `CultureMech:007480` or the stale `245.yaml` merge fingerprint.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-245.md` report before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated target is stale and still stores the NBRC Medium 244 formulation as a malformed concatenated ingredient plus an empty-label artifact row. | The inspected NBRC page lists six separate rows and pH 7.6; `data/normalized_yaml/bacterial/NBRC_245.yaml` already carries those six ingredient rows; `data/merge_yaml/merged/245.yaml` instead has `Yeast extract4gMalt extract10gGlucose4gSeawater750mlDistilled water250mlAgar (if needed)20gpH 7.6` as one `VARIABLE` ingredient and a second ingredient with `preferred_term: ''`. | Generated `data/merge_yaml/merged/245.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_245.yaml` |
| Major | The generated target omits source identity, structured references, pH, and variant linkage that are now maintained upstream. | `NBRC_245.yaml` now has `media_term: nbrc.medium:244`, `ph_value: 7.6`, a structured NBRC reference, and a `SOURCE_DUPLICATE` child link to `data/normalized_yaml/bacterial/togo_medium_m1466.yaml`; the generated target has none of those fields and was last merged on 2026-08-06. | Generated `data/merge_yaml/merged/245.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_245.yaml` |
| Major | The generated target carries generic unsupported preparation steps. | The NBRC table inspected for Medium 244 lists the ingredient amounts and pH; it does not state the generated `DISSOLVE`, `ADJUST_PH`, or autoclave instructions. | `data/normalized_yaml/bacterial/NBRC_245.yaml` if source-supported preparation instructions are found later; otherwise regenerate so the empty maintained `preparation_steps` list is reflected in the merge |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/245.yaml` from `data/normalized_yaml/bacterial/NBRC_245.yaml` so the generated target carries the recovered six-row NBRC Medium 244 formulation, `ph_value: 7.6`, `media_term: nbrc.medium:244`, the structured NBRC reference, and the TOGO M1466 source-duplicate child link.
2. Regenerate or audit `data/merge_yaml/merged/togo_medium_m1466.yaml` at the same time. Its maintained owner has the reciprocal `SOURCE_DUPLICATE` link and corrected seawater/distilled-water units, while its generated merge is also a pre-curation artifact.
3. Leave `data/normalized_yaml/bacterial/NBRC_245.yaml` unchanged unless a later source inspection finds NBRC-supported preparation instructions. Its current empty `preparation_steps` list is preferable to the stale generated generic dissolve/pH/autoclave steps.

## Follow-up Checks

- `just verify-merges` after regenerating `data/merge_yaml/merged/`.
- `just audit-merge-freshness` after regenerating `data/merge_yaml/merged/`.
- `just validate-schema data/merge_yaml/merged/245.yaml` after regeneration.
- `just validate-strict data/merge_yaml/merged/245.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/245.yaml` after regeneration.
- `just validate-references data/merge_yaml/merged/245.yaml` after regeneration.
- Manual comparison of regenerated `245.yaml` against the NBRC Medium 244 table for six rows, 750 ml/L seawater, 250 ml/L distilled water, optional 20 g/L agar, and pH 7.6.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated target has no structured `references` entries.
- Fetching `https://togomedium.org/medium/M1466` returned only the TogoMedium single-page application shell in this run, so this review did not independently verify the TOGO M1466 source page.
