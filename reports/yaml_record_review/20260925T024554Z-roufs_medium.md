# YAML Record Review: roufs_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/roufs_medium.yaml
- Started UTC: 2026-09-25T02:42:55Z
- Finished UTC: 2026-09-25T02:45:54Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Generated file | `data/merge_yaml/merged/roufs_medium.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:003530` |
| Name | `roufs_medium` |
| Original name | `ROUF'S medium` |
| Category | `bacterial` |
| Media term | `KOMODO Medium 1019` / `komodo.medium:1019` |
| Immediate maintained input | `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml` |
| Merge fingerprint | `468f17f25add2552f2ae22fb7d1b14530e53f6ee84bc843213f390c3dfc09647` |
| Merged from | `KOMODO_1019_ROUF_S_medium` |

The generated file is the single-source merge of the KOMODO import for DSMZ Medium 1019, `ROUF'S MEDIUM`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/roufs_medium.yaml` exited 0 with no diagnostics. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/roufs_medium.yaml --out /private/tmp/roufs_medium.strict.tsv --workers 1 --quiet` scanned 1 file and reported 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/roufs_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file and reported 0 checks. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/roufs_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` blocks embedded in merged YAML. |

## Identity and Grounding

- The record identity is internally consistent for the KOMODO side of DSMZ 1019: `komodo.medium:1019`, original name `ROUF'S medium`, note `DSMZ Medium: 1019`, pH 7.1, and the first seven final-medium components all point at DSMZ/MediaDive 1019.
- The DSMZ PDF for Medium 1019 and the live MediaDive REST record for `medium/1019` both identify the formulation as `ROUF'S MEDIUM`; MediaDive reports the DSMZ source link as `DSMZ_Medium1019.pdf`.
- The direct MediaDive import of the same source remains split as `data/merge_yaml/merged/roufs_medium__4b99f695.yaml` from maintained source `data/normalized_yaml/bacterial/roufs_medium.yaml`, with stable ID `CultureMech:000440` and media term `mediadive.medium:1019`.
- Exact ignored-file searches for `KOMODO_1019_ROUF_S_medium`, `roufs_medium`, `CultureMech:003530`, `komodo.medium:1019`, `mediadive.medium:1019`, and `DSMZ Medium: 1019` found the KOMODO normalized input, the sibling MediaDive normalized input, this generated file, and the sibling generated merge; ignored files were included.

## Evidence

### Supported

- The DSMZ/MediaDive identity, pH 7.1, complex/solid agar classification, parent yeast extract 5 g, peptone 5 g, MgSO4 x 7 H2O 0.2 g, CaCl2 x 6 H2O 0.05 g, ferric ammonium citrate 0.015 g, MnSO4 x 4 H2O 0.05 g, FeCl3 x 4 H2O 0.01 g, and agar 17 g per liter are supported by DSMZ Medium 1019 and MediaDive `medium/1019`.
- The trace-elements stock component names and stock concentrations are supported by DSMZ Medium 1019 and MediaDive solution `2071`, but only as a `Trace elements solution` made to 1 L and added to the parent at 1 ml per 1 L.
- The vitamin component names and stock concentrations are supported by the `Wolin's vitamin solution` definition exposed by MediaDive solution `242` and by DSMZ Medium 141, but only as a 1 L stock referenced from DSMZ 1019 at 10 ml per 1 L.

### Unsupported Or Over-scoped

- The generated trace-only ingredients are recorded as final-medium `G_PER_L` values. DSMZ 1019 adds 1 ml of a 1 L trace stock per 1 L parent medium, so `Nitrilotriacetic acid` at `12.8 G_PER_L`, `CoCl2 x 6 H2O` at `0.17 G_PER_L`, and the other trace rows are the stock recipe, not the final-medium concentrations.
- The generated vitamin rows are also recorded as final-medium `G_PER_L` values. DSMZ 1019 adds 10 ml of a 1 L vitamin stock per 1 L parent medium, so every Wolin vitamin stock component is overstated by 100x if interpreted as a final concentration.
- The source preparation evidence is not represented in the KOMODO merged record: DSMZ and MediaDive preserve pH adjustment with NaOH if needed, the agar note, the KOH preparation for `Trace elements solution`, and the filtration instruction for `Wolin's vitamin solution`; this generated record has no `preparation_steps` at all.

## Completeness

- Consequential source structure is missing: the parent `10 ml` vitamin-stock addition, `1 ml` trace-stock addition, the three distilled-water rows that establish stock and parent volumes, and the stock boundaries are absent from the generated ingredient list.
- The generated row for `NaOH` is a variable pH-adjustment ingredient extracted from notes. DSMZ 1019 supports NaOH only as the reagent used if pH 7.1 needs adjustment, not as a fixed recipe component.
- `target_organisms`, growth metrics, strain-level assertions, citations, and literature evidence are empty. Those absences are not defects for this source-only generated recipe; DSMZ Medium 1019 itself does not provide a growth assertion.

## Findings

### Blocker

None found.

### Major

1. **Nested stocks are flattened into false final concentrations.** `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml` copied the `Trace elements solution` and `Wolin's vitamin solution` internal recipes into the parent ingredient list as `G_PER_L` rows. The immediate source adds only 1 ml trace stock and 10 ml vitamin stock to 1 L of `ROUF'S MEDIUM`, so generated rows such as `Nitrilotriacetic acid 12.8 G_PER_L`, `CoCl2 x 6 H2O 0.17 G_PER_L`, and `Biotin 0.002 G_PER_L` are stock concentrations, not final-medium concentrations.

2. **The KOMODO import lost preparation and sterilization instructions.** The generated record has no `preparation_steps`, while DSMZ/MediaDive 1019 carry the pH 7.1 NaOH note and agar note, MediaDive solution `2071` carries the trace-solution KOH instructions, and MediaDive solution `242` carries the 0.22 um filtration instruction for the Wolin vitamin stock. The direct MediaDive sibling kept some of these steps, so the loss is source-path specific to `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml`.

3. **DSMZ/MediaDive 1019 is published as two generated CultureMech records.** This generated record and `data/merge_yaml/merged/roufs_medium__4b99f695.yaml` both denote `ROUF'S MEDIUM` / DSMZ 1019. They remain split because the KOMODO-normalized source has the generated `NaOH` ingredient and no source `preparation_steps`, while the direct MediaDive source has a MediaDive term and preserved preparation text. Future curation should make the two source imports structurally comparable enough for `merge_recipes.py` to coalesce them instead of letting source-path artifacts mint two public records.

### Minor

1. **`Calcium D-(+)-pantothenate` lacks a mirrored MediaIngredientMech CHEBI link.** The row has a `term` of `CHEBI:31345` / `Calcium pantothenate`, while the other exact CHEBI-grounded rows also carry `mediaingredientmech_chebi_term`. This is a presentation/enrichment gap, not a source-formulation error.

## Recommended Edits

1. In `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml`, replace the flattened trace and vitamin stock rows with an explicit representation of the two stock additions, their 1 L internal recipes, and the parent 1 L distilled-water volume, or keep them as nested/source-owned structures until the merge layer can represent stocks without concentration inflation.

2. In the KOMODO/DSMZ enrichment path that produced `KOMODO_1019_ROUF_S_medium.yaml`, preserve DSMZ/MediaDive preparation text from the parent solution and child stock solutions, including the pH 7.1 note, agar note, trace-stock KOH steps, and Wolin-stock filtration step.

3. Compare `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml` with `data/normalized_yaml/bacterial/roufs_medium.yaml` and update the normalized inputs or `merge_recipes.py` fingerprinting so the KOMODO and MediaDive views of DSMZ 1019 merge into one generated record without treating a note-derived variable pH reagent as a different recipe.

4. Re-run MediaIngredientMech enrichment, or add a narrow backfill, so the `Calcium D-(+)-pantothenate` row receives the same `mediaingredientmech_chebi_term` mirror as the other exact CHEBI-grounded ingredients.

## Follow-up Checks

- Run `just verify-merges` and confirm the two DSMZ 1019 inputs produce one generated `roufs_medium` merge.
- Run `just audit-merge-freshness` to prove `data/merge_yaml/merged/roufs_medium.yaml` was regenerated from the repaired normalized inputs.
- Run the focused open-schema, strict, reference, and term validators on the repaired normalized YAML and on the regenerated merge.
- Manually re-check DSMZ Medium 1019, DSMZ Medium 141, and MediaDive `medium/1019` after the stock repair to confirm stock quantities, final quantities, water volumes, and pH/preparation notes were neither dropped nor double counted.

## Additional Notes

- The duplicate search used `rg --no-ignore --hidden` and `rg --files --no-ignore --hidden`; ignored files were included.
- `data/merge_yaml/merged/roufs_medium.yaml` should not be edited directly. Future source fixes belong in `data/normalized_yaml/bacterial/KOMODO_1019_ROUF_S_medium.yaml` or in the import and merge rules that regenerate it.
