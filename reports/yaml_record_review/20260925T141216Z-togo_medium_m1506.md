# YAML Record Review: togo_medium_m1506

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1506.yaml
- Started UTC: 2026-09-25T14:06:40Z
- Finished UTC: 2026-09-25T14:12:16Z
- Verdict: pass with minor issues

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008050 |
| Label | togo_medium_m1506 |
| Generated record | data/merge_yaml/merged/togo_medium_m1506.yaml |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m1506.yaml |
| Source | TOGO Medium M1506, NBRC M288 |

The reviewed file is a generated one-source merge of TOGO M1506. Future fixes
belong in the normalized owner or the TOGO/NBRC import path, not in the
generated merge.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/togo_medium_m1506.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; exited 0 with no diagnostics |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected `eutils/pkg_resources is
deprecated` warning before passing.

## Identity and Grounding

`CultureMech:008050`, `TOGO:M1506`, the `NBRC_M288` original source note, and
the NBRC medium 288 page all identify the same unnamed NBRC medium. TOGO and
NBRC agree on 0.1 g MgSO4 x 7 H2O, 0.1 g NH4Cl, 0.5 g K2HPO4, 1 g sodium
acetate, 0.006 g ferric citrate, 0.1 g Bacto Yeast Extract, 0.5 g sodium
thioglycolate, and 1 L distilled water.

The eight ingredient rows are present with the source amounts and no stock
solution has been flattened into the final recipe.

## Evidence

The generated ingredient formula is supported by the TOGO M1506 API payload
and the NBRC 288 page. pH 6.8 is explicit in both sources and absent from the
YAML. The NBRC/TOGO source also says good growth can be obtained in
semi-solid medium with 1 g agar in 1 L; the liquid parent recipe is still
represented correctly.

## Completeness

The absent `target_organisms`, growth evidence, references, and stock
solutions are not defects for this imported source record. The source pH and
semi-solid agar note are useful follow-up annotations but do not make the
captured liquid formula wrong.

Before this report was written, an exact `find` search under the ignored
`reports/yaml_record_review` directory found no pre-existing
`*-togo_medium_m1506.md` report.

## Findings

| Severity | Finding |
| --- | --- |
| minor | pH 6.8 is absent. Owner: `data/normalized_yaml/bacterial/togo_medium_m1506.yaml` or the TOGO/NBRC importer. |
| minor | The source note about the 1 g/L agar semi-solid option is absent. Owner: `data/normalized_yaml/bacterial/togo_medium_m1506.yaml`. |

## Recommended Edits

1. Add `ph_value: 6.8`.
2. Preserve the 1 g/L agar semi-solid instruction as a source note or a
   physical-state variant.
3. Regenerate `data/merge_yaml/merged/togo_medium_m1506.yaml`.

## Follow-up Checks

Run the focused schema, strict, reference, and term validators on the
regenerated record, then manually compare it with TOGO M1506 and NBRC 288 for
pH 6.8 and the 1 g/L agar semi-solid note.

## Additional Notes

The exact gitignore-independent source search for this batch covered
`data/import_tracking`, `data/normalized_yaml/bacterial`,
`data/merge_yaml/merged`, and ignored `reports/yaml_record_review`; it found
the expected normalized owner, generated merge, and import-tracking row for
`CultureMech:008050` / `TOGO:M1506`.
