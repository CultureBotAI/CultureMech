# YAML Record Review: mo_cfxi_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/mo_cfxi_medium.yaml
- Started UTC: 2026-09-24T09:47:24Z
- Finished UTC: 2026-09-24T09:48:15Z
- Verdict: needs curation

## Target

Generated bacterial recipe for `CultureMech:010301`, `mo_cfxi_medium`, from `TOGO:M882` / `JCM_M846`.

The generated file contains one source recipe, `TOGO_M882_MO-CFXI_Medium`.

## Validation

Open LinkML validation passed with `No issues found`.

Strict schema-layer validation passed with 0 error rows; `/private/tmp/mo_cfxi_medium.strict.tsv` contained only the header line.

Reference validation passed, but performed 0 checks.

Term validation passed. The run emitted the expected `eutils` / `pkg_resources` deprecation warning before `Validation passed`.

Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries inside generated YAML.

## Identity and Grounding

The top-level media identity is correct for TOGO `M882`, which imports JCM medium 846 as `MO-CFXI Medium`.

Hydrate-sensitive CHEBI groundings for calcium chloride dihydrate and magnesium chloride hexahydrate are consistent with the JCM Solution B strings.

`Solution A` and `Solution B` are incorrectly grounded to `mediadive.solution:5342` and `mediadive.solution:5343`. Those MediaDive solution records have unrelated recipes with yeast extract, casamino acids, citrate, sulfate, carbonate, and high NaCl rows; they are not the local JCM 846 Solution A and Solution B subrecipes.

The JCM stock cross-reference is source-local but resolvable: JCM 846 points trace vitamins, trace element solution, and Se/W solution to JCM medium 284, and TOGO `M278` is that `SI Medium` record with `original_media_id: JCM_M284`.

## Evidence

TOGO `M882` and JCM `GRMD=846` define a final medium from 900 ml Solution A, 100 ml Solution B, an N2-CO2 gas mixture, then 10 ml of 3% sodium sulfide nonahydrate solution and 10 ml of 3% L-cysteine hydrochloride monohydrate solution per liter.

JCM Solution A contains 900 ml distilled water, 25 g NaCl, 0.1 g `KH2PO4`, 0.535 g `NH4Cl`, 2 g `NaHCO3`, 1 g yeast extract, 1 g BD-Difco tryptone, 1.8 g glucose, 0.55 g sodium pyruvate, 1 mg resazurin, 2 ml trace vitamins solution, 1 ml trace element solution, and 1 ml Se/W solution.

JCM Solution B contains 100 ml distilled water, 1 g `CaCl2 x 2H2O`, and 4 g `MgCl2 x 6H2O`, and is autoclaved under N2.

JCM 846 carries several preparation instructions that are chemically significant: adjust Solution A to pH 7.0, autoclave Solution A under N2-CO2 and Solution B under N2, mix A and B aseptically and anaerobically under N2-CO2, distribute sealed culture vessels under the same gas mixture, and finally add the two sterile reducing solutions.

TOGO `M278` carries full nested formulas for the referenced trace vitamins solution, trace element solution, and Se/W solution.

## Completeness

The generated record has the main mass rows from Solution A and Solution B, but Solution A, Solution B, the M278 stock cross-references, and the two 3% reducing solutions are all represented as empty placeholders.

The 1 mg resazurin source row is present but has the wrong mass unit.

The source pH and anaerobic preparation instructions are absent.

No target organism, incubation temperature, or growth evidence is present in TOGO M882 or JCM 846, so those absences are not defects for this source-only import.

## Findings

1. Needs curation: the JCM solution hierarchy is flattened. `Solution A`, `Solution B`, trace vitamins, trace elements, Se/W, sodium sulfide, and cysteine all remain as empty solution entries, while their local components were either promoted to top-level ingredients or dropped.

2. Needs curation: the 1 mg resazurin row was stored as `1 G_PER_L`, a 1000-fold overstatement.

3. Needs curation: five source volume additions are stored as mass-per-liter concentrations: 2 ml trace vitamins, 1 ml trace element solution, 1 ml Se/W solution, 10 ml 3% sodium sulfide solution, and 10 ml 3% cysteine solution.

4. Needs curation: the M278 stock formulas are resolvable but absent.

5. Needs curation: `Solution A` and `Solution B` link to unrelated `mediadive.solution:5342` and `mediadive.solution:5343` records.

6. Needs curation: gas handling from distinct anaerobic preparation steps was collapsed into generic variable gas ingredients, including duplicate dinitrogen rows as `Nitrogen gas` and `N2`.

7. Minor: pH 7.0 and the required autoclave, overnight, anaerobic mixing, dispensing, sealing, and final reducing-solution addition instructions are absent from generated structured fields.

## Recommended Edits

Re-curate `data/normalized_yaml/bacterial/TOGO_M882_MO-CFXI_Medium.yaml`, or fix the TOGO importer and solution migrator, so the final recipe preserves 900 ml Solution A, 100 ml Solution B, and the two 10 ml 3% reducing additions as stock additions.

Keep resazurin at 1 mg in Solution A.

Resolve the M278 cross-references to local nested recipes for trace vitamins, trace elements, and Se/W solution, or link them to curated reusable stock records with the same formulas.

Remove `mediadive.solution:5342` and `mediadive.solution:5343` from this record unless a future grounding step can prove exact source-equivalent stock definitions.

Preserve pH 7.0 and the anaerobic autoclave, overnight standing, aseptic mixing, N2-CO2 distribution, bottle sealing, and final reducing-solution addition instructions.

## Follow-up Checks

Re-run open LinkML, strict, reference, and term validation after any normalized-record edits.

Re-fetch TOGO M882, TOGO M278, and JCM 846 while fixing the record so the stock cross-reference is followed through TOGO M278 / JCM 284 rather than treated as an unresolved empty composition.

Check other records with `Solution A` / `Solution B` generic MediaDive links for the same name-only grounding problem.

## Additional Notes

No GitHub issues or PR comments were created by this record review.

Exact source and stock lookups used `find` and `rg --no-ignore --hidden`, so ignored files were included.
