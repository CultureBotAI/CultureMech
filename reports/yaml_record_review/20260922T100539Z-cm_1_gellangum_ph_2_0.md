# YAML Record Review: CM+1%Gellangum (pH 2.0)

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cm_1_gellangum_ph_2_0.yaml`
- Started UTC: `2026-09-22T10:05:39Z`
- Finished UTC: `2026-09-22T10:06:31Z`
- Verdict: needs curation

## Target

Generated bacterial `MediaRecipe` record `CultureMech:008443` for TOGO Medium `M1869`, generated from `data/normalized_yaml/bacterial/cm_1_gellangum_ph_2_0.yaml` on merge fingerprint `ddec4a33bc44c0bb44cc11df6e0e5ae1b5f5318ed3b327f9225a2e826506af96`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/cm_1_gellangum_ph_2_0.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The source identity is correct. TOGO `M1869` imports NBRC Medium 1114, whose live NBRC page is titled `CM+1%Gellangum (pH 2.0)`.

The generated record is stale relative to its normalized input. `data/normalized_yaml/bacterial/cm_1_gellangum_ph_2_0.yaml` has a September 2026 repair that adds `ph_value: 2.0`, grounds `Gellan gum` to `CHEBI:85248`, records per-ingredient source notes, adds NBRC and TOGO references, and adds curation/data-quality metadata. The merged output was last generated in August 2026 and lacks those repairs.

The ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/import_tracking`, and `reports` found no sibling generated record for NBRC `NO=1114` or TOGO `M1869`. Ignored files were included.

## Evidence

The live NBRC Medium 1114 page supports all generated ingredients and pH: 2 g cornmeal extract, 1 L distilled water, 10 g Gellangum, and pH 2.0.

The TOGO `M1869` API reports the same original NBRC medium, `src_url`, and pH 2.0, and encodes the three source rows as distilled water, Gellangum, and cornmeal extract.

## Completeness

The generated record has the three source ingredients but is incomplete because the explicit `ph_value: 2.0` is missing from the merged YAML.

Curated source-level metadata from the normalized input is also absent from the generated output: the NBRC/TOGO `references`, data-quality flags, updated gellan gum label, ingredient source notes, and the `RESOLVED_TOGO_M1869_CM_GELLANGUM_SCORE15` curation-history event were not carried forward.

No target organism or growth evidence is present.

## Findings

- Major: the generated record lost pH 2.0 even though it appears in NBRC, TOGO, the title, and the repaired normalized YAML.
- Major: regenerated output is stale relative to the normalized YAML repaired on September 11, 2026.
- Minor: `Gellangum` is ungrounded in the generated record even though the normalized input already corrects it to `Gellan gum` / `CHEBI:85248`.
- Minor: curated `references`, ingredient source notes, and data-quality flags in the normalized input are absent from the merged output.

## Recommended Edits

- Regenerate this merged record from `data/normalized_yaml/bacterial/cm_1_gellangum_ph_2_0.yaml` so the September 2026 repair is reflected under `data/merge_yaml/merged`.
- Confirm the regenerated output keeps `ph_value: 2.0`, the gellan gum CHEBI grounding, and NBRC/TOGO references.
- If merge logic still drops source notes, references, or data-quality flags, fix that merge path instead of hand-editing the generated YAML.

## Follow-up Checks

- Validate the regenerated record with open schema, strict, reference, and term validators.
- Confirm the generated curation history includes `RESOLVED_TOGO_M1869_CM_GELLANGUM_SCORE15`.
- Confirm there is still only one generated NBRC 1114 / TOGO M1869 branch.

## Additional Notes

Empty optional fields are not defects. The source has no preparation comment beyond pH 2.0, so the missing `preparation_steps` field is not itself a defect here.
