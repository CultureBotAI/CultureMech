# YAML Record Review: 1401

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1401.yaml`
- Started UTC: 20260921T044255Z
- Finished UTC: 20260921T044348Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007477` |
| Label | `1401` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1401.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1401]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1401.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import whose import history points at NBRC Medium 1396. The maintained owner later recovered the six-row Modified Sour Dough Medium table, pH 5.6, and `nbrc.medium:1396`, but it still retains the stale numeric label `1401`.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1401.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1401.yaml --out /private/tmp/1401.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1401.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1401.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1401.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The target's source identity is NBRC Medium 1396:

- The generated import history says `Media No. 1396` and links the legacy NBRC `NO=1396` page.
- Live NBRC Medium 1396 is titled `Modified Sour Dough Medium`.
- The maintained owner now has `media_term: nbrc.medium:1396`, pH 5.6, and the six components listed by the NBRC table.

Two identity issues remain:

- The generated record is still labeled `1401`; live NBRC Medium 1401 is the unrelated `PEG basal medium`.
- The maintained owner also still has `name: '1401'` and `original_name: '1401'` rather than the official `Modified Sour Dough Medium` title.

## Evidence

Supported by inspected source text:

- NBRC Medium 1396 lists maltose 20 g, yeast extract 3 g, Tween 80 0.3 g, Trypticase peptone 6 g, distilled water 1 L, agar if needed 15 g, and pH 5.6.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all six NBRC ingredient rows plus the pH text into one `preferred_term`.
- The second ingredient row has an empty `preferred_term` and stores `Maltose` as a concentration value.
- The generated artifact has no separate rows for yeast extract, Tween 80, Trypticase peptone, distilled water, or optional agar.
- The generated file omits pH 5.6, `nbrc.medium:1396`, the NBRC reference, and all ingredient term mappings.
- The generated `ADJUST_PH` and autoclaving steps are not in the inspected NBRC 1396 table.

## Completeness

- The generated file omits the Aug 19 recovered NBRC composition, the Aug 20 MIM groundings, and the Sep 7 NBRC source-metadata repair in `data/normalized_yaml/bacterial/NBRC_1401.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRC_1401.yaml`, `NO=1396[^0-9]`, `NBRC Medium 1396`, `nbrc.medium:1396`, `Modified Sour Dough Medium`, and `CultureMech:007477` found the stale generated NBRC direct import, the partially repaired normalized owner, its ID/catalog entries, and two TOGO-derived `Modified Sour Dough Medium` copies.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRCMediumDetailServlet?NO=1401[^0-9]`, `nbrc.medium:1401`, and `PEG basal medium` found no NBRC direct import for live NBRC Medium 1401, but did find the separate TOGO-derived `peg_basal_medium` record.
- Empty preparation and growth-evidence slots are acceptable in the normalized NBRC 1396 source import because the NBRC table does not state a preparation sequence or an organism-specific growth observation.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated merge is stale and malformed relative to its repaired normalized owner. | The Aug 6 generated file still has the pre-recovery two-row parse, while `data/normalized_yaml/bacterial/NBRC_1401.yaml` now carries six NBRC 1396 ingredients, pH 5.6, `nbrc.medium:1396`, and the direct NBRC reference. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1401.yaml` after the title fix; fix merge source selection if the stale rows persist. |
| Blocker | The generated label `1401` is misleading for an NBRC Medium 1396 record. | The import event points to `NO=1396`, live NBRC 1396 is `Modified Sour Dough Medium`, and live NBRC 1401 is the distinct `PEG basal medium`. | `data/normalized_yaml/bacterial/NBRC_1401.yaml` plus merge regeneration; the normalized owner still needs the official title. |
| Major | The official medium title is still missing from both the generated and normalized NBRC direct records. | Live NBRC Medium 1396 is `Modified Sour Dough Medium`, but both the generated file and `data/normalized_yaml/bacterial/NBRC_1401.yaml` retain `name: '1401'`. | `data/normalized_yaml/bacterial/NBRC_1401.yaml`. |
| Major | The generated file omits source pH and all structured components except a corrupt Maltose row. | NBRC Medium 1396 lists six component rows and pH 5.6; the generated file has one concatenated formula blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1401.yaml` after the title fix. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 1396 page supplies the composition table and pH row, but it does not state the generated autoclaving step, and the maintained owner now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1401.yaml`. |
| Minor | TOGO-derived `Modified Sour Dough Medium` copies remain separate. | Gitignore-independent search found `TOGO_M2086_Modified_Sour_Dough_Medium` with the same NBRC 1396 source URL and `TOGO_M78_Modified_Sour_Dough_Medium` with the same title; the M2086 copy has `Distilled water` as `1 G_PER_L`. | Merge-fingerprint rules for NBRC/TOGO duplicate handling after the direct NBRC import receives its official title. |

## Recommended Edits

1. Curate `data/normalized_yaml/bacterial/NBRC_1401.yaml` to use `Modified Sour Dough Medium` as `name` and `original_name`, preserving `nbrc.medium:1396`.
2. Regenerate `data/merge_yaml/merged/` and confirm the generated NBRC Medium 1396 artifact carries the six recovered ingredient rows, pH 5.6, the NBRC 1396 reference, and the Sep 7 curation event.
3. If regeneration preserves the concatenated formula blob, fix the NBRC merge source path that is still using the pre-recovery import output.
4. Review duplicate handling between the NBRC direct import and the TOGO `Modified Sour Dough Medium` copies.
5. Review the `peg_basal_medium` TOGO record to decide whether live NBRC Medium 1401 needs an NBRC direct normalized owner or whether the existing TOGO-derived record is enough.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/NBRC_1401.yaml` after the normalized title edit.
- `just verify-merges` to prove generated outputs are fresh after regeneration.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1396>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1396>.yaml` for the grounded subset.
- Manual comparison to live NBRC Medium 1396 for all six ingredient rows and pH 5.6.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
