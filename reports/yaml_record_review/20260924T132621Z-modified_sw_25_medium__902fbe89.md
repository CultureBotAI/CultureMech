# YAML Record Review: modified_sw_25_medium__902fbe89
- Repository: CultureMech
- Record: data/merge_yaml/merged/modified_sw_25_medium__902fbe89.yaml
- Started UTC: 2026-09-24T13:25:20Z
- Finished UTC: 2026-09-24T13:26:21Z
- Verdict: needs curation

## Target
Generated merged YAML for `modified_sw_25_medium`, CultureMech ID `CultureMech:002289`, from one MediaDive/JCM source.

- Reviewed generated record: `data/merge_yaml/merged/modified_sw_25_medium__902fbe89.yaml`
- Reviewed normalized source: `data/normalized_yaml/bacterial/modified_sw_25_medium.yaml`
- Media term: `mediadive.medium:J1114`, `JCM Medium J1114`
- Source note: `JCM | Link: https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1114`
- Merge fingerprint: `902fbe893235e7a3a3ebae2fee28a997143e7b27c1fd689d946ed180c4725a13`
- `merged_from`: `modified_sw_25_medium`

## Validation
- Open LinkML validation: Passed, `No issues found`.
- Strict validation: Passed with 0 error rows across 1 file. The strict TSV had 1 line, the header only.
- LinkML reference validation: Passed; 1 file validated, 0 checks, all validations passed.
- LinkML term validation: Passed; emitted the known `eutils`/`pkg_resources` deprecation warning and exited 0.
- Embedded `curation_history`: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding
The JCM identity is coherent. The record is labeled as `MODIFIED SW-25 MEDIUM` from JCM `J1114`, and the TOGO payload for `M1191` reports the same recipe under `original_media_id` `JCM_M1114`.

Chemical grounding is mostly healthy:

- `NaCl`, `MgCl2 x 6 H2O`, `MgSO4 x 7 H2O`, `KCl`, `NaBr`, `NaHCO3`, and `CaCl2 x 2 H2O` are grounded to the matching CHEBI salt or hydrate terms.
- `Casamino acids` is intentionally ungrounded as a mixture.
- `Yeast extract` is ungrounded, which is acceptable for an undefined complex ingredient.

## Evidence
The live JCM URL for `GRMD=1114` currently resolves but returns `Nothing found`, so the direct JCM page is no longer enough to verify the formula.

The live TOGO API for `M1191` still exposes the original JCM recipe:

- Main solution: `Yeast extract`, 1 g; `Casamino acids (BD-Difco)`, 1 g; `SW--25 solution (see below)`, 1 L.
- Nested `SW--25 solution`: `Distilled water`, 1 L; `MgSO4 x 7H2O`, 51 g; `NaCl`, 195 g; `CaCl2 x 2H2O`, 0.8 g; `MgCl2 x 6H2O`, 32.5 g; `KCl`, 5 g; `NaHCO3`, 0.16 g; `NaBr`, 0.6 g.
- Comments: adjust pH to 7.5 and autoclave; add 20.0 g/L agar for solid medium; add SW-25 components to distilled water and bring volume to 1.0 L.

## Completeness
The final generated ingredient amounts match the SW-25 salt recipe plus 1 g/L yeast extract and 1 g/L casamino acids. The final pH and autoclave instruction are retained.

The generated recipe does not retain the source nesting: the SW-25 solution itself has been flattened away, while its preparation comment remains as a separate final-medium `MIX` step. This makes the generated procedure less complete than the source even though the flattened concentrations are chemically equivalent for a 1 L SW-25 base.

## Findings
1. Needs curation - the preparation order is not executable as written. The generated recipe places `Adjust pH to 7.5, and autoclave...` before `Add components to distilled water and bring volume to 1.0 L.`, but the latter instruction belongs to making the SW-25 base that is used in the final formula.

2. Needs curation - the nested `SW--25 solution (see below)` structure was flattened into final ingredients. The source distinguishes the final medium, with yeast extract, casamino acids, and 1 L SW-25 solution, from the SW-25 salt solution made up to 1 L; the generated YAML loses that intermediate solution and leaves no ingredient or solution carrying the SW-25 label.

3. Minor - the direct JCM provenance link in `notes` no longer resolves to the medium recipe. A live fetch of `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1114` returned `Nothing found`, so this branch depends on the MediaDive import and the TOGO mirror for formula verification.

4. Minor - this JCM branch is a duplicate of the adjacent TOGO `M1191` branch in `data/merge_yaml/merged/modified_sw_25_medium.yaml`. Both trace to `JCM_M1114`, but they differ structurally because the TOGO import kept an empty SW-25 solution with a false `1 G_PER_L` water row while this branch flattened the stock salts without water.

## Recommended Edits
- Repair the normalized JCM/MediaDive source, or the MediaDive import path, so SW-25 is represented as a named base solution and the final medium contains yeast extract, casamino acids, and that 1 L solution.
- Move the SW-25 make-up instruction before final pH adjustment and autoclaving.
- Preserve the 20.0 g/L agar instruction only as an optional solid-medium variant or source note; the reviewed record is explicitly liquid.
- Add an archival or alternate resolvable source note for JCM `1114`; the current JCM CGI URL is stale.
- Reconcile this record with the TOGO `M1191` branch after fixing that branch's water-unit and empty-solution defects.

## Follow-up Checks
- Regenerate `data/merge_yaml/merged` and confirm that the repaired JCM and TOGO branches either collapse onto one fingerprint or are explicitly cross-linked as equivalent JCM `1114` records.
- Revalidate the regenerated YAML with open LinkML, strict schema, reference, and term validators.
- Re-fetch the live TOGO `M1191` payload and confirm that all seven SW-25 salts and both final complex ingredients are still represented at the source amounts.

## Additional Notes
None found.
