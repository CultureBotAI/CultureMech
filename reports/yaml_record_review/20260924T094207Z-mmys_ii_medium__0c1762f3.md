# YAML Record Review: mmys_ii_medium__0c1762f3

- Repository: CultureMech
- Record: data/merge_yaml/merged/mmys_ii_medium__0c1762f3.yaml
- Started UTC: 2026-09-24T09:40:47Z
- Finished UTC: 2026-09-24T09:42:07Z
- Verdict: needs curation

## Target

Generated bacterial recipe for `CultureMech:008454`, `mmys_ii_medium`, from `TOGO:M187` / `JCM_M194`.

The generated file contains one source recipe, `TOGO_M187_Mmys-II_Medium`, and is identical to `data/normalized_yaml/bacterial/TOGO_M187_Mmys-II_Medium.yaml` apart from merge metadata.

## Validation

Open LinkML validation passed with `No issues found`.

Strict schema-layer validation passed with 0 error rows; `/private/tmp/mmys_ii_medium__0c1762f3.strict.tsv` contained only the header line.

Reference validation passed, but performed 0 checks.

Term validation passed. The run emitted the expected `eutils` / `pkg_resources` deprecation warning before `Validation passed`.

Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries inside generated YAML.

## Identity and Grounding

The top-level media identity is correct for TOGO `M187`: TOGO reports `Mmys-II Medium`, `original_media_id: JCM_M194`, and the JCM formula URL with `GRMD=194`.

Hydrate-sensitive top-level salt groundings for calcium chloride dihydrate, magnesium chloride hexahydrate, sodium thiosulfate pentahydrate, ammonium sulfate, sodium chloride, and disodium malate are consistent with the flattened source ingredient names.

`Solution I` and `Solution II` are incorrectly grounded to `mediadive.solution:5335` and `mediadive.solution:5336`. Those are unrelated MediaDive stock records whose compositions are `K2HPO4` plus water and `KH2PO4` / `(NH4)2SO4` / `NaCl` / `MgSO4 x 7 H2O` / `CaCl2 x 2 H2O` plus water, not the local JCM 194 solution blocks.

`high_metal: true` is plausible only through the SL8 cross-reference, because the generated record leaves the SL8 stock empty.

## Evidence

TOGO `M187` preserves the JCM recipe as three paragraphs: a final mixture of 500 ml `Solution I` plus 500 ml `Solution II`, a `Solution I` subrecipe in 500 ml distilled water, and a `Solution II` row with 500 ml of 20 mM potassium phosphate buffer at pH 7.8. The TOGO API also preserves the instruction to separately autoclave the two solutions, mix aseptically, and adjust final pH to 7.7-7.8.

JCM `GRMD=194` independently confirms the same MMYS-II Medium formula. Its `Solution I` table contains 1.0 g `(NH4)2SO4`, 8.2 g `NaCl`, 0.2 g `MgCl2 x 6H2O`, 0.05 g `CaCl2 x 2H2O`, 1.0 ml trace element solution SL8, 3.6 g sodium DL-malate, 1.0 g yeast extract, 0.5 g `Na2S2O3 x 5H2O`, and 500.0 ml distilled water. Its `Solution II` table contains 500.0 ml 20 mM potassium phosphate buffer at pH 7.8.

JCM 194 points its SL8 row to JCM medium 190. TOGO `M187` encodes that cross-reference as `M183`; TOGO `M183` is `LYS Medium` with `original_media_id: JCM_M190` and contains the actual `Trace element solution SL8` recipe.

MediaDive solution 5336 resolves to a named `Solution II` stock with 1000 ml volume and six recipe rows unrelated to this record. The local normalized `mediadive_5335_Solution_I.yaml` and `mediadive_5336_Solution_II.yaml` records likewise show unrelated generic solution recipes and incomplete-composition flags.

## Completeness

The JCM 194 / TOGO M187 formula cannot be reconstructed from the generated record because the local `Solution I` and `Solution II` blocks are placeholders with no composition.

The SL8 stock is also only a cross-reference with `composition: []`, even though JCM 190 / TOGO M183 give its EDTA, iron, zinc, manganese, boric acid, cobalt, copper, nickel, molybdate, and water rows.

The source final pH and separate-autoclave preparation instruction are absent from generated structured fields.

No target organism, incubation temperature, atmosphere, or growth evidence is present in TOGO M187 or JCM 194, so those absences are not defects for this source-only import.

## Findings

1. Needs curation: the record flattens a three-part JCM solution recipe. The final recipe should be 500 ml `Solution I` plus 500 ml `Solution II`; instead, the output promotes the `Solution I` constituents and the `Solution II` buffer to top-level ingredients, while keeping separate placeholder `Solution I` and `Solution II` entries in `solutions`.

2. Needs curation: source volume units are represented as mass-per-liter concentrations. The 500 ml potassium phosphate buffer row is stored as `500 G_PER_L`, and the 1 ml SL8 cross-reference is stored as `1 G_PER_L`.

3. Needs curation: the SL8 stock composition is dropped. The source recipe needs the JCM 190 / TOGO M183 trace-element stock rows or a resolvable local solution reference, not an empty `composition: []`.

4. Needs curation: generic solution-name grounding attached unrelated `mediadive.solution:5335` and `mediadive.solution:5336` identifiers to local JCM subrecipes that have different compositions.

5. Needs curation: final pH 7.7-7.8 and the instruction to separately autoclave `Solution I` and `Solution II` are present in TOGO/JCM but absent from the generated record.

6. Minor: `mmys_ii_medium__0c1762f3` appears to be the same NBRC/JCM formula as the adjacent `mmys_ii_medium` record from `TOGO:M1569` / `NBRC_M372`. The two records did not merge because the JCM variant kept SL8 as a cross-reference while the NBRC variant included SL8 as a nested subrecipe.

## Recommended Edits

Re-curate `data/normalized_yaml/bacterial/TOGO_M187_Mmys-II_Medium.yaml`, or fix the TOGO importer and solution migrator that generated it, so JCM paragraph structure is preserved as a final 1 L recipe made from two local 500 ml solutions.

Store `Solution I` with its eight ingredient rows, store `Solution II` as 500 ml of 20 mM potassium phosphate buffer at pH 7.8, and keep all ml rows as volumes rather than `G_PER_L`.

Resolve the SL8 cross-reference to JCM 190 / TOGO M183 and include the nested stock composition, or link to a curated reusable SL8 solution with the same formula.

Remove `mediadive.solution:5335` and `mediadive.solution:5336` from this record unless a future grounding step can prove exact source-equivalent stock definitions.

Preserve final pH 7.7-7.8 and the separate-autoclave / aseptic-mix preparation comment in structured fields or source notes.

After fixing the stock hierarchy, compare the fingerprint with `data/merge_yaml/merged/mmys_ii_medium.yaml` so the duplicate TOGO M187 and TOGO M1569 MMYS-II records can merge if the normalized formulas are equivalent.

## Follow-up Checks

Re-run open LinkML, strict, reference, and term validation after any normalized-record edits.

Re-fetch TOGO M187, TOGO M183, JCM 194, and JCM 190 while applying the fix to avoid propagating the TOGO M187 display-level `M183` vs JCM `190` cross-reference mismatch incorrectly.

Re-check for stale generic `Solution I` / `Solution II` MediaDive links on TOGO imports after the grounding fix, because the same `mediadive.solution:5335` and `mediadive.solution:5336` identifiers are attached to several unrelated records.

## Additional Notes

No GitHub issues or PR comments were created by this record review.

All searches used to support absence or exact-file checks included ignored files via `--no-ignore --hidden` or `find`.
