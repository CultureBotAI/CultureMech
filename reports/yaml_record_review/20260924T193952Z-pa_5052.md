# YAML Record Review: pa_5052

- Repository: CultureMech
- Record: data/merge_yaml/merged/pa_5052.yaml
- Started UTC: 2026-09-24T19:39:52Z
- Finished UTC: 2026-09-24T19:39:52Z
- Verdict: needs curation

## Target

Reviewed generated MediaRecipe `CultureMech:007079`, `pa_5052`, generated from `data/normalized_yaml/bacterial/pa_5052.yaml` for MediaDB Medium 189 / PA-5052.

## Validation

- Open LinkML validation: Passed; exited 0 with `No issues found`.
- Strict validation: Passed; scanned 1 file with 0 ERROR rows. `/private/tmp/pa_5052.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The generated record has the expected MediaDB Medium 189 identity for PA-5052. An ignored-inclusive exact search for `MEDIADB:189`, `PA-5052`, and `pa_5052` across `data/merge_yaml` and `data/normalized_yaml` found one normalized PA-5052 source record, its index entries, and this generated output.

## Evidence

The MediaDB text export for medium 189 lists 35 PA-5052 compounds. The generated record preserves all 35 concentrations but keeps a parser-corrupted iron label: MediaDB lists `Iron(III) chloride` at 0.01 mM with source ChEBI 30808, while generated YAML has `'''Iron(III` at 0.01 `MILLIMOLAR`.

The normalized source has already repaired that exact row. Its 2026-08-31 curation history records `REPAIRED_MEDIADB_TRUNCATED_NAME`, and its ingredient list contains `preferred_term: Iron(III) chloride`.

## Completeness

The generated record is complete at the row-count and concentration level, but it is not source faithful because a repaired defined ingredient name regressed to the parser-truncated form in generated YAML. Several source-supplied ChEBI identifiers are also absent from the corresponding generated ingredients.

## Findings

1. `Iron(III) chloride` is truncated to `'''Iron(III`.

   MediaDB 189 lists `Iron(III) chloride`, and `data/normalized_yaml/bacterial/pa_5052.yaml` has already restored that name. The generated record still emits the pre-repair parser artifact with three leading quotes and no closing parenthesis or chloride suffix.

2. Source ChEBI groundings are missing from several defined rows.

   MediaDB supplies ChEBI identifiers for L-Aspartate, beta-D-Glucose, and Iron(III) chloride, but those generated ingredients have no `term` or `mediaingredientmech_chebi_term`.

3. The preparation steps include generic, weakly sourced import text.

   The MediaDB text export lists ingredient concentrations and does not provide a pH value or sterilization instruction. Generated steps such as `Adjust pH if specified in original formulation` and filter sterilization at 0.22 um should either be backed by a MediaDB source field or omitted.

## Recommended Edits

- Regenerate PA-5052 from the current normalized record so the 2026-08-31 `Iron(III) chloride` repair appears in `data/merge_yaml/merged/pa_5052.yaml`.
- Fill exact ChEBI terms from MediaDB 189 where source ChEBI identifiers are available and unambiguous.
- Remove or source the generic pH-adjustment and filter-sterilization preparation steps in MediaDB imports.

## Follow-up Checks

- Re-run an ignored-inclusive exact search for `'''Iron(III` after regeneration and confirm the truncated artifact no longer exists in generated YAML.
- Re-fetch the MediaDB 189 text export and compare all 35 generated ingredient labels and concentrations against it.
- Re-run open, strict, reference, and term validation on the regenerated YAML.

## Additional Notes

None found.
