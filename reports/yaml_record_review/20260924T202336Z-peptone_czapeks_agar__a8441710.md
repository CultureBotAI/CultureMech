# YAML Record Review: peptone_czapeks_agar__a8441710

- Repository: CultureMech
- Record: data/merge_yaml/merged/peptone_czapeks_agar__a8441710.yaml
- Started UTC: 2026-09-24T20:23:36Z
- Finished UTC: 2026-09-24T20:23:36Z
- Verdict: needs curation

## Target

Generated fungal MediaRecipe `CultureMech:010544`, `peptone_czapeks_agar`, from the maintained direct MediaDive/JCM owner `data/normalized_yaml/fungal/peptone_czapeks_agar.yaml`.

The record represents MediaDive J64 / JCM GRMD 64, PEPTONE-CZAPEK'S AGAR. Its generated recipe contains 30 g/L sucrose, 5 g/L Bacto peptone, 1 g/L K2HPO4, 0.5 g/L KCl, 0.5 g/L MgSO4 x 7 H2O, 0.01 g/L FeSO4 x 7 H2O, and 15 g/L agar.

## Validation

- Open LinkML validation: Passed with no issues.
- Strict validator: Passed; `/private/tmp/peptone_czapeks_agar__a8441710.strict.tsv` was header-only, so there were 0 strict errors.
- Reference validation: Passed; 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked: `just validate-history` validates standalone `history/`, not embedded `MediaRecipe.curation_history`.

## Identity and Grounding

The direct MediaDive/JCM identity is internally consistent: `mediadive.medium:J64` names the source as JCM and links to JCM `GRMD=64`.

An exact ignored-inclusive search found a separate generated TOGO copy, `data/merge_yaml/merged/peptone_czapeks_agar.yaml`, from `data/normalized_yaml/bacterial/peptone_czapeks_agar.yaml`. That sibling is TOGO M56 and names the same JCM `JCM_M64` / `GRMD=64` source recipe, so it appears to be a duplicate representation of MediaDive J64 rather than a separate recipe.

## Evidence

MediaDive J64 supports the JCM source identity and all seven non-water rows in the generated record: sucrose, Bacto peptone, K2HPO4, KCl, MgSO4 x 7 H2O, FeSO4 x 7 H2O at 10 mg for 0.01 g/L, and agar.

MediaDive J64 also contains an eighth 1000 ml `Distilled water` row that is absent from this generated record and its maintained fungal owner. TOGO M56 mirrors the same JCM GRMD 64 recipe and also lists a 1 L distilled water row.

The ingredient grounding is appropriate. The six defined salts and carbohydrates map to their expected CHEBI terms, agar maps to `CHEBI:2509`, and commercial Bacto peptone is left ungrounded.

## Completeness

The direct generated record preserves source identity and the non-water MediaDive rows, and MediaDive reports final pH as not determined.

The generated record is incomplete because it omits the MediaDive/JCM 1000 ml distilled water row, and the same JCM 64 recipe is still split from the TOGO M56 generated sibling.

## Findings

1. Needs curation: `data/normalized_yaml/fungal/peptone_czapeks_agar.yaml` and this generated record omit the 1000 ml `Distilled water` component from MediaDive J64.
2. Needs curation: JCM `GRMD=64` appears twice in generated YAML. The TOGO M56 sibling carries the same JCM source recipe but has unit conversion errors for water and `FeSO4 x 7 H2O`, so the source-duplicate merge cannot happen cleanly yet.

## Recommended Edits

1. Add the 1000 ml distilled water row to `data/normalized_yaml/fungal/peptone_czapeks_agar.yaml`.
2. Repair `data/normalized_yaml/bacterial/peptone_czapeks_agar.yaml` so the TOGO M56 `Distilled water` and `FeSO4 x 7 H2O` source units are converted correctly.
3. Regenerate merged YAML and verify the direct MediaDive/JCM J64 and TOGO M56 owners merge as source duplicates.

## Follow-up Checks

1. Re-run open, strict, reference, and term validation on the repaired maintained owners and regenerated merged record.
2. Repeat an exact ignored-inclusive search for `mediadive.medium:J64`, `TOGO:M56`, and `GRMD=64` after regeneration to confirm the JCM 64 recipe is represented once in `data/merge_yaml/merged/`.

## Additional Notes

The exact ignored-inclusive duplicate search covered `data/normalized_yaml` and `data/merge_yaml/merged` for `TOGO:M56`, `JCM_M64`, `GRMD=64`, and `CultureMech:009965`.
