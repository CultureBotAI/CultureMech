# YAML Record Review: BHIS

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHIS__8d771a65.yaml
- Started UTC: 2026-09-21T21:22:38Z
- Finished UTC: 2026-09-21T21:26:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/BHIS__8d771a65.yaml` |
| ID | `CultureMech:015493` |
| Label | `BHIS` |
| Category | `specialized` |
| Maintained owner | `data/normalized_yaml/specialized/bhis.yaml` |
| Source | CultureBotHT `BHIS`, via FEBA media definitions |
| Merge fingerprint | `8d771a65a43d6c31145411569c780a3ddc9aff65859bd50e5b74b92246db87b9` |
| Merge inputs | `bhis` |

The reviewed file is a derived merge record. Future corrections belong in
`data/normalized_yaml/specialized/bhis.yaml`, in the CultureBotHT import or
repair path that owns this source family, or in merge generation if the
normalized owner is already correct.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHIS__8d771a65.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHIS__8d771a65.yaml --out /private/tmp/BHIS__8d771a65.strict.tsv --workers 1 --quiet` | Passed with 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHIS__8d771a65.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHIS__8d771a65.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | `just validate-history` | Not checked: the documented focused history validator targets standalone files under `history/`; no documented record-local embedded `curation_history` validator exists for one merge record |

Direct `just validate-schema`, `just validate-strict`, `just validate-terms`,
and `just validate-references` were not re-run because this checkout tries to
build `llvmlite==0.46.0` under Python 3.13 before reaching the requested file
and fails in setuptools with `TypeError: Popen.__init__() got an unexpected
keyword argument 'dry_run'`. The no-project Python 3.11 commands above run the
same narrow validators against this one generated MediaRecipe.

## Identity and Grounding

- `CultureMech:015493`, label `BHIS`, `category: specialized`, and
  `source_data.origin: CultureBotHT` agree on the FEBA/CultureBotHT brain-heart
  infusion salt broth record, not the TOGO-derived bacterial `BHIS` agar record
  in `data/merge_yaml/merged/BHIS.yaml`.
- The generated record is derived from exactly one source recipe, `bhis`, and
  points at the expected normalized owner
  `data/normalized_yaml/specialized/bhis.yaml`.
- The hidden-inclusive `find . -path ./.git -prune -o -iname '*bhis*' -print`
  search covered ignored files outside `.git` and found the specialized BHIS
  family, the separate bacterial BHIS records, the BHIS repair script/test, and
  this review family; it did not find a local raw CultureBotHT `BHIS.yaml` file
  or a second normalized owner for `CultureMech:015493`.
- `NCBITaxon:1042417` resolves locally as `Brachyspira pilosicoli P43/6/78`.
  NCBI Assembly ESummary for `GCA_000325665.1` resolves the corresponding
  GenBank/RefSeq synonyms `GCA_000325665.1` and `GCF_000325665.1` to taxid
  `1042417`, organism `Brachyspira pilosicoli P43/6/78`, culture collection
  `ATCC:51139`, and strain `P43/6/78`.

## Evidence

- The PubMed cache entry for `PMID:8573497` exactly supports the strain-specific
  growth claim that P43/6/78T cells grew in BHIS broth, reached a maximum cell
  density of `2 x 10(9)` cells/ml, and had a doubling time of 1 to 2 h at 37 to
  42 degrees C.
- Both copied `PMID:8573497` snippets in the variant and in
  `target_organisms[].growth_metrics[].evidence` are exact text from the cached
  abstract.
- The same source does not support the exact point estimate
  `doubling_time_minutes: 60.0` or the exact point estimate
  `temperature_celsius: 37.0`; it reports a doubling-time range of 1 to 2 h and
  a temperature range of 37 to 42 degrees C.
- The record's eight-ingredient formulation is consistent across the reviewed
  merge, its normalized owner, and the guarded
  `repair_culturebotht_bhis_score15.py` expectations, but I could not verify
  those exact amounts against an inspected upstream CultureBotHT source. The
  current `CultureBotAI/CultureBotHT` default-branch tree and GitHub code
  searches for `BHIS`, the expected `BHIS.yaml` filename, and exact imported
  ingredient amounts returned no current source file.

## Completeness

- `sources` and `references` are missing from this generated merge even though
  they are now present in `data/normalized_yaml/specialized/bhis.yaml`.
- `curation_history` has no event for the curator-added
  `Brachyspira pilosicoli` variant and growth evidence even though those blocks
  are not part of the original CultureBotHT import.
- No solution references are expected for this direct eight-ingredient broth
  record.
- Empty pH, preparation, storage, application, and discussion slots are not
  automatically defects here. The accessible CultureBotHT provenance does not
  include an inspected protocol that could support those fields.
- The hidden-inclusive
  `rg --no-ignore --hidden -n -F 'database_id: BHIS; url: https://github.com/CultureBotAI/CultureBotHT' . -g '!/.git/**'`
  search found only the normalized owner, this generated merge, and the BHIS
  score-15 repair test. The hidden-inclusive
  `rg --no-ignore --hidden -n -F 'Brain heart infusion salt broth' . -g '!/.git/**'`
  search found the local specialized BHIS family, BHI sibling, repair test, and
  prior BHI review; it did not find a local raw upstream source.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The structured growth metric collapses source ranges into exact lower-endpoint numbers. | `PMID:8573497` reports a 1 to 2 h doubling-time range and growth at 37 to 42 degrees C; `target_organisms[].growth_metrics[0]` records `doubling_time_minutes: 60.0` and `temperature_celsius: 37.0` as if those were exact observations. | `data/normalized_yaml/specialized/bhis.yaml` and, if this was produced by growth-evidence automation, the range parsing in `scripts/propose_growth_evidence.py` |
| Minor | The generated merge is stale relative to its normalized owner. | `data/normalized_yaml/specialized/bhis.yaml` contains the 2026-09-13 `NORMALIZED_CULTUREBOTHT_BHIS_SOURCE` event plus structured `sources` and `references`; `data/merge_yaml/merged/BHIS__8d771a65.yaml` stops at the older 2026-08-06 merge event and lacks the structured provenance slots. | Merge regeneration from `data/normalized_yaml/specialized/bhis.yaml` |
| Minor | The CultureBotHT recipe source is not recoverable from the stored provenance. | The record points only to the repository URL `https://github.com/CultureBotAI/CultureBotHT` and database id `BHIS`; current GitHub API tree/code searches did not find `BHIS.yaml`, `BHIS`, or the exact imported formula on the default branch, and hidden-inclusive local searches found no raw upstream copy. | `data/normalized_yaml/specialized/bhis.yaml` or its CultureBotHT importer |
| Minor | The Brachyspira variant and growth-evidence addition is not represented in `curation_history`. | The fields cite `PMID:8573497` and describe specialized-media auto-curation, but the only maintained events are import, ID assignment, `SET_ORGANISM_CULTURE_TYPE`, and the BHIS provenance repair. | `data/normalized_yaml/specialized/bhis.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/specialized/bhis.yaml`, replace the exact
   lower-endpoint growth fields with a representation that does not overstate
   the abstract. If the current schema cannot encode numeric ranges, keep the
   range in `measurement_conditions` and leave exact point fields empty rather
   than encoding `60.0` min and `37.0` degrees C as exact measurements.
2. Add curation/audit provenance for the `PMID:8573497` variant and target
   organism evidence in the normalized owner, without rewriting older history.
3. Preserve or add a retrievable CultureBotHT source path, commit, raw snapshot,
   or stronger `CultureBotHT:BHIS` reference in the maintained source metadata
   so the eight ingredient rows can be checked against the imported source
   after current CultureBotHT `main` no longer exposes `BHIS.yaml`.
4. Regenerate `data/merge_yaml/merged/BHIS__8d771a65.yaml` after the normalized
   owner is corrected so the derived record includes the structured
   `sources`/`references` slots and the corrected growth metric.

## Follow-up Checks

- Run `just validate data/normalized_yaml/specialized/bhis.yaml` or the
  equivalent focused schema, strict, term, and reference validators after
  changing the normalized owner.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  the merged record.
- Re-run the exact `PMID:8573497` snippet validator, if target-specific, or the
  narrowest documented growth-evidence validator that covers the corrected
  Brachyspira growth metric.
- Manually inspect a pinned CultureBotHT `BHIS` source, if it can be recovered,
  before asserting that the eight ingredient rows and their quantities are
  source-complete.

## Additional Notes

- `data/merge_yaml/merged/BHIS.yaml` is a separate `CultureMech:009362`
  TOGO M2814 agar record with hemin, menadione, and BHI aggregate components;
  it is not a duplicate owner for this FEBA/CultureBotHT liquid `BHIS` record.
- `has_unmapped_ingredients` is appropriate because Calf brains, Beef heart,
  and Proteose Peptone are intentionally retained as unresolved imported
  materials.
