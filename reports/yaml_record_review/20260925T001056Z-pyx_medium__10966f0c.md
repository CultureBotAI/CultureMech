# YAML Record Review: pyx_medium
- Repository: CultureMech
- Record: data/merge_yaml/merged/pyx_medium__10966f0c.yaml
- Started UTC: 2026-09-25T00:10:56Z
- Finished UTC: 2026-09-25T00:11:43Z
- Verdict: needs curation

## Target
Reviewed generated record `data/merge_yaml/merged/pyx_medium__10966f0c.yaml`.

Owner record:

- `data/normalized_yaml/bacterial/pyx_medium.yaml`
- `merge_fingerprint`: `10966f0cc1babd246c291b97a50d010ef617a1f58a72435f7a27dfe46f840ab7`
- `merged_from`: `pyx_medium`

The generated YAML differs from the normalized owner only by generated merge metadata.

## Validation
- Open LinkML validation against `MediaRecipe`: passed; no issues found.
- Strict validation: passed; `/private/tmp/pyx_medium__10966f0c.strict.tsv` was header-only with 0 data rows.
- LinkML reference validation: passed; 0 reference checks, all validations passed.
- LinkML term validation with labels: passed.
- Embedded history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding
- Identity is correct for JCM `1025`, PYX Medium.
- The exact ignored-file search for `mediadive.medium:J1025`, `JCM_J1025`, and `GRMD=1025` found this direct JCM owner and generated target, plus a TOGO M1087 sibling for the same JCM page.
- Resazurin is correctly carried as a small final gram-per-liter concentration, consistent with a 1 mg addition distributed over the finished medium volume.
- The salts from the referenced salt stock are grounded to hydrated or simple salts where applicable, but they are in the wrong structural location.

## Evidence
- Live JCM 1025 lists 5 g Trypticase peptone, 5 g Bacto peptone, 10 g yeast extract, 2 g glucose, 1 mg resazurin, 40 ml Salt solution from JCM 676, 0.5 g `L-Cysteine.HCl.H2O`, and 1 L distilled water.
- Live JCM 1025 says to boil the medium without glucose and cysteine under N2, cool it, add cysteine, adjust pH to 7.0, dispense under N2, autoclave, and then add glucose from a sterile anoxic stock prepared under N2.
- Live JCM 676 defines the referenced Salt solution as `CaCl2 x 2H2O` 0.25 g/L, `MgSO4 x 7H2O` 0.5 g/L, `K2HPO4` 1 g/L, `KH2PO4` 1 g/L, `NaHCO3` 10 g/L, `NaCl` 2 g/L, and distilled water to 1 L.
- The TOGO M1087 sibling has the same JCM 1025 source but keeps the salt solution as an empty 40 `G_PER_L` placeholder and lacks the JCM pH/procedure text.

## Completeness
The pH, resazurin final concentration, and full preparation text are present. The explicit distilled-water row and salt stock hierarchy are missing from the direct JCM owner.

## Findings
1. The direct JCM import omitted the explicit 1 L distilled-water row from the JCM 1025 main formula.
2. The 40 ml/L salt solution was flattened into top-level final ingredient rows at stock concentrations; `CaCl2 x 2H2O`, `MgSO4 x 7H2O`, `K2HPO4`, `KH2PO4`, `NaHCO3`, and `NaCl` should be nested under a salt stock rather than read as final grams per liter.
3. The same JCM 1025 recipe is stranded as a TOGO M1087 generated sibling with the opposite partial model: undiluted base rows, an empty salt placeholder, and missing pH/procedure metadata.

## Recommended Edits
1. Restore distilled water to `data/normalized_yaml/bacterial/pyx_medium.yaml`.
2. Replace the flattened salt rows with a 40 ml/L stock addition whose nested composition comes from JCM 676.
3. Deduplicate the TOGO M1087 sibling after the direct JCM and TOGO imports agree on salt-solution structure and procedure text.

## Follow-up Checks
- Re-run open LinkML, strict, reference, and term validators for the edited JCM owner.
- Regenerate `data/merge_yaml/merged/pyx_medium__10966f0c.yaml` and confirm the JCM 676 salt ingredients no longer appear as final top-level rows.
- Re-run exact ignored-file searches for `mediadive.medium:J1025`, `JCM_J1025`, and `GRMD=1025` to confirm the TOGO M1087 exact-source duplicate has been intentionally merged or retired.

## Additional Notes
None.
