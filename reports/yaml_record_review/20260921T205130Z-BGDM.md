# YAML Record Review: bgdm

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BGDM.yaml
- Started UTC: 2026-09-21T20:47:30Z
- Finished UTC: 2026-09-21T20:51:28Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:007420 |
| Name | bgdm |
| Original name | BGDM |
| Category | bacterial |
| Source identity | MEDIADB:72, MediaDB Medium 72, label BGDM |
| Reviewed artifact | `data/merge_yaml/merged/BGDM.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bgdm.yaml` |
| Generated state | Derived single-source merge from `merged_from: [bgdm]`, `merge_fingerprint: b42afd7beb2780aa6ab1b53554c632b1e4adc86a6b20d592a77cbbab2d15d0bb` |

`data/culturemech_id_registry.tsv` maps `CultureMech:007420` to
`data/normalized_yaml/bacterial/bgdm.yaml`; the reviewed file is the derived
merge product for that normalized owner.

MediaDB Medium 72 resolved to the "Bgdm" record, marked "Is minimal: Yes", with
the twelve compounds and mM amounts shown in the YAML. Its MediaDB Source 20
page identifies the formulation source as Seddon et al., Current Microbiology,
1988, titled "Chemically defined and minimal media for bacteroides gingivalis".

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BGDM.yaml` | Passed; `No issues found` |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BGDM.yaml --out /private/tmp/BGDM.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 ERROR rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BGDM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 reference checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BGDM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | Not checked | No focused embedded-`MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone records under `history/` |

The direct `just validate-schema`, `just validate-strict`,
`just validate-references`, and `just validate-terms` wrappers were not rerun
for this record because this checkout currently fails before target-specific
validation while building `llvmlite==0.46.0` under Python 3.13. The no-project
commands above exercise the same narrow validators under Python 3.11 from a
cache outside the project environment.

## Identity and Grounding

- **Record identity is coherent.** `CultureMech:007420`, `MEDIADB:72`, the
  registry row, and the MediaDB Medium 72 page all identify BGDM/Bgdm rather
  than a sibling medium.
- **The generated merge is stale.** The generated BGDM file was emitted on
  2026-08-06 from `bgdm.yaml`; the normalized owner has a later
  2026-08-20 `apply_mim_groundings.py` event that grounds Aspartate to
  `CHEBI:29995` (`aspartate(2-)`). The reviewed merge still leaves the
  Aspartate ingredient ungrounded.
- **Quantities agree with MediaDB Medium 72.** The MediaDB tab-delimited
  `media_text/72` export lists L-Glutamate 0.6797 mM, Alanine 1.1224 mM,
  Phenylalanine 0.6054 mM, Menadione 0.002904 mM, Calcium chloride anhydrous
  0.9011 mM, Potassium dibasic phosphate 1.1481 mM, Aspartate 0.7513 mM,
  Sodium chloride 1.7112 mM, Cysteine-HCl 0.4758 mM, Potassium dihydrogen
  phosphate 7.3483 mM, Magnesium sulfate 2.8401 mM, and Hemin 0.007669 mM,
  matching the reviewed record's twelve ingredient concentrations.
- **Specific ChEBI exactness remains only partly checked.** The term validator
  confirmed the current ID/label pairs carried by the generated merge, but the
  MediaDB tab-delimited export lists several different source ChEBI identifiers
  than the locally resolved ones. Alanine, Phenylalanine, Potassium dibasic
  phosphate, and Aspartate should be rechecked against the MediaDB compound
  pages or the archived SQL row before marking this record
  `ingredients_curated`.

## Evidence

| Claim | Review |
|---|---|
| `media_term: MEDIADB:72` / BGDM | Supported by the inspected MediaDB Medium 72 page. |
| Twelve listed mM ingredients | Supported by the inspected MediaDB `media_text/72` tab-delimited export. |
| Source article | Underrepresented. MediaDB Source 20 identifies Seddon et al. 1988, but the record only carries a generic MediaDB homepage note and an import-history note for Mazumdar et al. 2014. |
| `preparation_steps` | Unsupported. Neither the Medium 72 page nor `media_text/72` states to dissolve everything in distilled water, adjust pH conditionally, or filter sterilize through 0.22 um. |
| Growth context | Missing. The Medium 72 page links eight Porphyromonas gingivalis growth-data records; the YAML has no `target_organisms` or `growth_metrics`. |
| Generic `applications` | Too broad for the inspected BGDM sources. The MediaDB page establishes BGDM as a minimal chemically defined recipe with linked P. gingivalis records, not the broader "Metabolic modeling" or "Systems biology research" applications as record-specific assertions. |

The inspected MediaDB Growth Data 131 page reports Porphyromonas gingivalis W83
on Bgdm, Source 20, growth rate 0.067 1/h, temperature 37.0, pH `None`, and no
secretion/uptake compounds. The seven sibling growth records linked from the
Medium 72 page should be inspected before adding all organism claims.

## Completeness

- The record has all twelve ingredients and all mM values exposed by MediaDB
  Medium 72.
- The record has no pH. That is acceptable for the base formulation because
  the inspected Medium 72 and Growth Data 131 pages do not provide a pH.
- The record is missing the MediaDB Medium 72, `media_text/72`, Source 20, and
  growth-data URLs from `references`.
- The record is missing `organism_culture_type`, strain-scoped
  `target_organisms`, and `growth_metrics` for the eight linked MediaDB
  growth-data rows.
- The record has not been regenerated after the 2026-08-20 normalized-owner
  grounding event.
- A gitignore-independent search with `rg --no-ignore --hidden` over `data` and
  `reports/yaml_record_review` for `CultureMech:007420`, `MEDIADB:72`, and the
  merge fingerprint found the expected normalized owner, generated merge,
  generated indexes, and import-tracking references. A `find` search over the
  ignored `reports/yaml_record_review` directory found no pre-existing
  `*-BGDM.md` review report before this one was written.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The generated merge is stale and omits the normalized Aspartate grounding. | `data/normalized_yaml/bacterial/bgdm.yaml` has a 2026-08-20 MIM grounding event and carries `CHEBI:29995` on Aspartate; `data/merge_yaml/merged/BGDM.yaml` still has the 2026-08-06 merge event and an ungrounded Aspartate ingredient. | Regenerate `data/merge_yaml/merged/BGDM.yaml` from `data/normalized_yaml/bacterial/bgdm.yaml`, then regenerate generated pages. |
| major | The preparation protocol is unsupported MediaDB boilerplate. | The inspected Medium 72 page and tab-delimited export list compounds and mM amounts only. The three YAML preparation steps come from `src/culturemech/import/mediadb_importer.py`, not from Source 20 or Medium 72 evidence. | Remove unsupported BGDM `preparation_steps` from `data/normalized_yaml/bacterial/bgdm.yaml`; stop `src/culturemech/import/mediadb_importer.py` from inventing default dissolve/pH/filter steps for MediaDB imports. |
| major | The record omits the linked MediaDB growth evidence and medium-specific Seddon 1988 provenance. | Medium 72 links Source 20 plus growth-data records 131-138 for Porphyromonas gingivalis strains W83, W50, LH9, WPH15, WPH34, HG91, HG184, and Hg185. The YAML has no `target_organisms`, no `growth_metrics`, and no `references` block. | Curate `data/normalized_yaml/bacterial/bgdm.yaml` with MediaDB Medium 72, `media_text/72`, Source 20, and inspected growth-data pages. |
| minor | The inherited `applications` list overstates the record-specific evidence. | MediaDB Medium 72 supports a minimal BGDM formulation and linked P. gingivalis growth data; it does not explicitly assert "Metabolic modeling" or "Systems biology research" as BGDM applications. | Narrow or remove the BGDM `applications` values in `data/normalized_yaml/bacterial/bgdm.yaml`; consider changing `src/culturemech/import/mediadb_importer.py` so these are not emitted for every MediaDB import. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/bgdm.yaml`, remove the unsupported
   generic `preparation_steps` unless the Seddon 1988 paper is inspected and
   supplies a real preparation protocol.
2. Add BGDM-specific provenance to `references`: MediaDB Medium 72,
   `media_text/72`, Source 20, each inspected growth-data page that contributes
   organism/growth evidence, and the resolved Seddon article identifier if it
   can be recovered.
3. Add strain-scoped `target_organisms` and any supported `growth_metrics` for
   the eight MediaDB growth-data records. At minimum, record Growth Data 131 as
   Porphyromonas gingivalis W83 on Bgdm with 0.067 1/h at 37 C and no reported
   pH if that claim is curated.
4. Recheck the exact chemical identity of Alanine, Phenylalanine, Potassium
   dibasic phosphate, Aspartate, and Magnesium sulfate against MediaDB compound
   pages or `media_database.07Oct2015.sql`; leave any ambiguous stereochemistry
   or salt/hydrate as an explicit unresolved discussion rather than forcing a
   plausible ChEBI identifier.
5. Remove or narrow the generic MediaDB `applications` list for BGDM.
6. Add a normalized curation event describing the Seddon/MediaDB evidence and
   the exact changed sections, then regenerate merge YAML and page artifacts.
7. Repair `src/culturemech/import/mediadb_importer.py` in a separate
   importer-wide change so future MediaDB imports do not synthesize unsupported
   preparation steps or record-specific applications.

## Follow-up Checks

- Rerun `just validate-schema data/normalized_yaml/bacterial/bgdm.yaml` and
  `just validate-strict data/normalized_yaml/bacterial/bgdm.yaml`, or the
  no-project equivalents, after the normalized edit.
- Rerun `just validate-terms data/normalized_yaml/bacterial/bgdm.yaml` after
  any ingredient grounding changes.
- Rerun `just validate-references data/normalized_yaml/bacterial/bgdm.yaml`
  after adding MediaDB and article references.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  `data/merge_yaml/merged/BGDM.yaml`.
- Inspect all eight MediaDB growth-data pages, not only Growth Data 131, before
  treating the organism/growth block as complete.

## Additional Notes

- The old SpringerLink URL exposed by MediaDB Source 20,
  `http://www.springerlink.com/content/nq25r3743m452755/`, could not be
  resolved from this environment on 2026-09-21 (`curl: (6) Could not resolve
  host`). The source article DOI therefore remains unresolved in this review.
- The successful reference-validator run performed zero checks because this
  generated BGDM file has no `references` block or evidence references.
