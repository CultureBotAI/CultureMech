# YAML Record Review: jcm_medium_no_452

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_452.yaml
- Started UTC: 2026-09-23T17:08:10Z
- Finished UTC: 2026-09-23T17:09:17Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/jcm_medium_no_452.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002802` |
| Label | `jcm_medium_no_452` |
| Source term | `mediadive.medium:J452` |
| Upstream source | Legacy JCM MEDIUM No. 452, mirrored by MediaDive as JCM `J452` |
| Generated from | `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml` |

This is a generated merge artifact with `merge_fingerprint: 9476736853be3236adec09861e6c038f9e9c7cffe596ea20a19b0c5fb8e84609` and `merged_from: [jcm_medium_no_452]`. Future edits belong in the maintained MediaDive/JCM normalized owner, not in `data/merge_yaml/merged/`.

An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, `data/import_tracking`, `history`, and `data/culturemech_recipe_catalog.tsv` found the maintained JCM owner, the standalone `Main sol. J452` import, and TOGO M452 as an independently imported sibling for the same original JCM GRMD=452 formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_452.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_452.yaml --out /private/tmp/jcm452.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_452.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 0 reference checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_452.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. The only output before the pass line was the known `eutils` `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not `MediaRecipe.curation_history` embedded in merged YAML. |

The repository-level `just` validators were not used because this checkout's `uv` resolution currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails inside `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused Python 3.11 validators above exercise the relevant schema, strict, reference, and term gates for this one generated `MediaRecipe`.

## Identity and Grounding

The generated record denotes the intended MediaDive JCM import: `CultureMech:002802` is `jcm_medium_no_452`, carries source term `mediadive.medium:J452`, and was imported from MediaDive source `JCM, ID: J452`.

Live JCM GRMD=452 now returns `Nothing found`, so the current JCM page can no longer verify the historical source formula. MediaDive `/rest/medium/J452` still returns `JCM MEDIUM No. 452` with source `JCM`, pH 7.0, a 1000 ml main solution, and recipe rows for 10 g glucose, 5 g Bacto peptone with `attribute: BD-Difco`, 5 g beef extract with `attribute: BD-Difco`, 3 g NaCl, 15 g agar, and 1000 ml distilled water. TOGO M452 also preserves the same old JCM accession through `original_media_id: JCM_M452`.

The generated record preserves the non-water ingredient identities and amounts, pH 7.0, complex/undefined classification, and `SOLID_AGAR` state. It loses distilled water and both BD-Difco qualifiers.

## Evidence

Supported claims:

- MediaDive supports the J452 identity, complex-medium classification, pH 7.0, glucose 10 g/L, Bacto peptone 5 g/L, beef extract 5 g/L, NaCl 3 g/L, agar 15 g/L, and the pH-adjustment step.
- TOGO M452 independently supports the preserved legacy JCM formulation, including the two BD-Difco labels and 1 L distilled water.

Unsupported or incomplete claims:

- The generated recipe has no `Distilled water` ingredient and no preparation step that brings the medium to 1.0 L, although both MediaDive and TOGO M452 explicitly include 1000 ml or 1 L water.
- MediaDive carries `attribute: BD-Difco` on Bacto peptone and beef extract, and TOGO M452 includes those qualifiers in each preferred term. The generated JCM record drops them.
- The generated note still points readers to the live JCM GRMD=452 URL even though that page now returns `Nothing found`.

## Completeness

The generated record is chemically close to the preserved MediaDive legacy copy, but the missing water row removes the source's final 1 L basis and leaves the gram-per-liter rows without their explicit solvent. That should be repaired in the maintained JCM owner.

Empty optional evidence, target-organism, salinity, atmosphere, and storage fields are acceptable. The inspected MediaDive and TOGO records are source recipes and do not provide growth evidence or incubation/storage conditions.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The source water/final-volume row is missing. | MediaDive J452 row 6 is `Distilled water`, 1000 ml, and TOGO M452 has `Distilled water`, 1 L. The generated JCM record has neither water nor a bring-to-volume step. | `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml` and the MediaDive import handling for water rows. |
| Minor | BD-Difco qualifiers were dropped from two complex ingredients. | MediaDive has `attribute: BD-Difco` on Bacto peptone and beef extract; TOGO M452 keeps `(BD-Difco)` in both labels; the generated JCM record stores only `Bacto peptone` and `Beef extract`. | `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml` or the MediaDive import mapping for row attributes. |
| Minor | The JCM link is stale without a discussion flag. | The generated notes point to GRMD=452, but live JCM returns `Nothing found`. | `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml`. |

## Recommended Edits

1. Restore the 1000 ml distilled-water row in `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml`, using MediaDive J452 and TOGO M452 as preserved legacy evidence.
2. Preserve the `BD-Difco` qualifier on Bacto peptone and beef extract, either in the ingredient label or an attribute/notes field that survives generation.
3. Add a concrete discussion flag that JCM GRMD=452 is now unavailable while MediaDive and TOGO preserve matching legacy copies.

## Follow-up Checks

- Rerun the focused schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/jcm_medium_no_452.yaml` after the maintained owner is edited.
- Regenerate `data/merge_yaml/merged/jcm_medium_no_452.yaml` and confirm that the distilled-water row, both BD-Difco qualifiers, and the stale-JCM discussion survive into the merged artifact.
- Recheck JCM GRMD=452, MediaDive `/rest/medium/J452`, and TOGO M452 manually to ensure the source-availability note still reflects the current provider state.
- Run the merge freshness and product validators after regeneration.

## Additional Notes

- The standalone solution record `data/normalized_yaml/bacterial/mediadive_4199_Main_sol_J452.yaml` keeps `Distilled water` but encodes it as `1000 PERCENT_V_V`, drops the two `BD-Difco` attributes, and still carries an obsolete `incomplete_composition` data-quality flag despite having a complete composition.
