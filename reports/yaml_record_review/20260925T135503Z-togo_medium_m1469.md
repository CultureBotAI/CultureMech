# YAML Record Review: togo_medium_m1469

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1469.yaml`
- Started UTC: 2026-09-25T13:55:03Z
- Finished UTC: 2026-09-25T13:55:03Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1469, imported from NBRC medium 248.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1469.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1469`; the TOGO API identifies NBRC_M248 as its original medium ID and links NBRC medium 248.

The TOGO payload and NBRC page agree on 4 g yeast extract, 10 g malt extract, 4 g glucose, 10 g Na2CO3, 1 L distilled water, 20 g agar if needed, pH 9.0-10.0, and separate sterilization for Na2CO3.

## Evidence

The generated yeast extract, malt extract, glucose, and agar rows match the source amounts.

The generated record is stale relative to the repaired normalized source YAML. The normalized source has already converted 1 L distilled water to 1000 ml/L, moved Na2CO3 from an empty solution into the final formula, added pH 9.0-10.0, preserved the separate Na2CO3 sterilization note, and added source references.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated record does not preserve the source instruction that Na2CO3 is sterilized separately.

## Findings

- Major issue: `Distilled water` is represented as 1 g/L instead of 1 L.
- Major issue: `Na2CO3*` is modeled as an empty solution named `Unknown solution` instead of a separately sterilized 10 g/L ingredient.
- Minor issue: pH 9.0-10.0 is absent.
- Minor issue: the Na2CO3 separate-sterilization instruction is absent.

## Recommended Edits

- Regenerate the merged YAML from `data/normalized_yaml/bacterial/togo_medium_m1469.yaml` so the generated output picks up the existing NBRC medium 248 repair.
- Confirm Na2CO3 is retained as a final-medium ingredient with 10 g/L, a buffer role, and a preparation note indicating separate sterilization.
- Confirm pH 9.0-10.0 and the 1000 ml/L distilled-water final volume survive regeneration.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge.
- Add or run a regression check for starred source notes so a separate-sterilization footnote does not create an empty solution.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1469 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
