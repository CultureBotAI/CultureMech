# YAML Record Review: bb__9b6a7851

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bb__9b6a7851.yaml
- Started UTC: 2026-09-21T19:05:45Z
- Finished UTC: 2026-09-21T19:07:01Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bb__9b6a7851.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bb.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:000320` |
| Name | `bb` |
| Original name | `BB` |
| Source | MediaDive/CCAP Medium `C13`, CCAP `MR_BB.pdf` |
| Merge status | Generated one-source merge from `bb` with fingerprint suffix `9b6a7851` to avoid the algae `BB.yaml` filename collision |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated `bb__9b6a7851` merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this MediaDive import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated `bb__9b6a7851` merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bb__9b6a7851.yaml` is a generated merge for the MediaDive/CCAP `C13` copy of BB and resolves to `CultureMech:000320` from `data/normalized_yaml/bacterial/bb.yaml`.
- The same CCAP `MR_BB.pdf` source also appears as the algae `CultureMech:000053` record reviewed at `data/merge_yaml/merged/BB.yaml`; `data/import_tracking/reports/filename_collisions.tsv` records the `bb.yaml` bacterial/algae collision and marks the ingredient sets as different.
- The source identity is wrong at the category layer: CCAP BB is Bold's Basal Medium for freshwater algae, not a bacterial medium.
- Exact gitignore-independent searches for `CultureMech:000320`, `mediadive.medium:C13`, `bb__9b6a7851`, and `Source: CCAP, ID: C13` covered `data/raw`, `data/import_tracking`, `data/normalized_yaml`, and `data/merge_yaml`. They found the maintained normalized owner, generated merge, generated indexes, the filename-collision report, plausibility reports, and no local raw MediaDive or CCAP C13 dump.
- The source PDF supports most ingredient formulas, but the concentrations are not modeled at the source's stock-solution boundary and several imported amounts disagree with the PDF.

## Evidence

- The cited CCAP `MR_BB.pdf` supports a ten-stock formulation; the YAML flattens all 16 source chemical rows into one top-level ingredient list.
- CCAP lists `NaNO3`, `MgSO4.7H2O`, `NaCl`, `K2HPO4`, `KH2PO4`, and `CaCl2.2H2O` as the six stocks made to 400 ml and used at 10 ml per litre of final medium. The YAML stores `10`, `10`, `10`, `10`, `23.4`, and `10` g/L, respectively, which do not preserve the source masses or final diluted concentrations.
- CCAP lists `ZnSO4.7H2O`, `MnCl2.4H2O`, `MoO3`, `CuSO4.5H2O`, `Co(NO3)2.6H2O`, `H3BO3`, `EDTA`, `KOH`, `FeSO4.7H2O`, and concentrated `H2SO4` in stocks 7-10 used at 1 ml per litre. The YAML mostly keeps the stock-strength numeric values in top-level final-medium slots.
- The PDF lists concentrated `H2SO4` as 1.0 ml in stock 10; the YAML represents `H2SO4` as `1` g/L.
- The PDF instructs making the final medium to 1 litre with glass-distilled or deionized water; the YAML keeps that as its only preparation step but omits adding stocks 1-6 at 10 ml/L and stocks 7-10 at 1 ml/L.

## Completeness

- Consequential gaps:
  - The record is filed in `data/normalized_yaml/bacterial` and `category: bacterial` despite being a CCAP freshwater-algae recipe.
  - The record duplicates the same CCAP source identity as algae `CultureMech:000053` but has a divergent ingredient representation, preventing a clean merge.
  - The recipe has no stock-solution structure, even though every amount in `MR_BB.pdf` is scoped to one of ten stocks.
  - Macro-stock quantities for stock solutions 1-6 are wrong or unscoped, and stocks 7-10 remain at stock strength instead of final-medium strength.
  - The `H2SO4` amount has the wrong unit and missing concentrated-acid context.
  - The `NaNO3` ingredient still carries a legacy `mediaingredientmech_term` after the June 2026 MIM migration.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the inspected CCAP PDF gives only a recipe for freshwater algae and no strain-specific growth assay.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw MediaDive or CCAP C13 source dump under `data/raw` or `data/import_tracking`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The record is filed as bacterial even though the CCAP source recipe is for freshwater algae. | `MR_BB.pdf` titles the recipe Bold's Basal Medium (BB) for freshwater algae; the YAML owner is `data/normalized_yaml/bacterial/bb.yaml` with `category: bacterial`. | `data/normalized_yaml/bacterial/bb.yaml`, the category/indexes, and likely the MediaDive CCAP import mapping. |
| Major | The record duplicates the CCAP BB source identity already represented by `CultureMech:000053` but diverges from it. | Both records cite `MR_BB.pdf`; `filename_collisions.tsv` records `bb.yaml` in bacterial and algae categories with different ingredient-row sets. | Reconcile `data/normalized_yaml/bacterial/bb.yaml` against `data/normalized_yaml/algae/bb.yaml` and the merge/deduplication rules. |
| Major | Stock ingredients were flattened with wrong or unscoped amounts. | The PDF scopes every ingredient to a stock and then dilutes stocks into the final litre; the YAML stores all 16 rows as top-level `G_PER_L` final-medium ingredients, with stock 1-6 masses changed from the PDF values and stock 7-10 concentrations left at stock strength. | The normalized owner; broad repeats may require the MediaDive CCAP importer. |
| Major | `H2SO4` has the wrong quantity semantics. | The source row is 1.0 ml of concentrated `H2SO4` in stock 10, not 1 g/L sulfuric acid in the final medium. | The normalized owner. |
| Major | The record omits the actual stock-assembly instructions. | CCAP says to add stocks 1-6 at 10 ml/L and stocks 7-10 at 1 ml/L, but the YAML has only the final make-up-to-1-litre water step. | The normalized owner. |
| Minor | The first ingredient still uses a legacy MIM identifier. | `NaNO3` retains `mediaingredientmech_term: MediaIngredientMech:000171` while the other 15 rows were migrated to CHEBI-keyed `mediaingredientmech_chebi_term` links. | The normalized owner. |

## Recommended Edits

1. Move the authoritative CCAP BB record to the algae category and reconcile whether `CultureMech:000320` should merge into or be superseded by `CultureMech:000053`.
2. Replace the flat 16-row ingredient list with source-faithful stock solutions, including the six 400 ml stocks, the four 1 L stocks, and the 10 ml/L or 1 ml/L final-medium additions.
3. Restore the PDF's macro-stock masses and convert or scope all final-medium quantities so stock strengths are not presented as final grams per litre.
4. Represent concentrated `H2SO4` as a volume in stock 10 or leave the quantity unresolved if the current schema cannot safely model concentrated acid.
5. Add the missing stock-addition preparation steps from `MR_BB.pdf`.
6. Replace the legacy `NaNO3` MediaIngredientMech link with the CHEBI-keyed representation used by the rest of the record.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Re-extract `MR_BB.pdf` and manually compare every stock, chemical row, stock volume, final-volume instruction, and unit against the YAML.
- Regenerate the generated merge and run `just verify-merges` plus `just audit-merge-freshness`.
- Rebuild import/content manifests and confirm `bacterial/bb.yaml` no longer appears in `data/import_tracking/reports/concentration_plausibility.tsv` as `TRACE_SALT_AS_STOCK`.

## Additional Notes

- The generated merge is in sync with `data/normalized_yaml/bacterial/bb.yaml`; the scientific issues above are inherited from that maintained input.
- Exact gitignore-independent searches included ignored files where present.
