# YAML Record Review: MRS MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_medium__abcd760b.yaml
- Started UTC: 2026-09-24T14:45:14Z
- Finished UTC: 2026-09-24T14:47:44Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:000647` / `mrs_medium` at `data/merge_yaml/merged/mrs_medium__abcd760b.yaml`.

- Generated source: `data/normalized_yaml/bacterial/mrs_medium.yaml`
- Merge source: `mrs_medium`
- Merge fingerprint: `abcd760ba289f3ca344a6053365af73787e83132905d3d6feadf2b9aec132838`
- Category: `bacterial`
- Medium term: `mediadive.medium:11`, `DSMZ Medium 11`, label `MRS MEDIUM`
- Source note: `DSMZ`, linked to `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium11.pdf`

## Validation

| Check | Result |
|---|---|
| Open LinkML schema with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__abcd760b.yaml` | Passed. |
| Strict validator with `scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__abcd760b.yaml --out /private/tmp/mrs_medium__abcd760b.strict.rerun.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator with `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__abcd760b.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were applicable. |
| Term validator with `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__abcd760b.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml` records, not embedded `MediaRecipe.curation_history` events. |

## Identity and Grounding

The generated record denotes DSMZ / MediaDive medium 11, MRS MEDIUM. The `mediadive.medium:11` identifier, DSMZ source, pH range 6.2-6.5, and medium type agree with the inspected MediaDive REST record for medium 11.

The represented simple ingredients preserve MediaDive amounts and units:

| MediaDive row | YAML row | Review |
|---|---|---|
| Glucose, 20 g | `Glucose`, 20 `G_PER_L`, `CHEBI:17234` | Supported. |
| Tween 80, 1 g | `Tween 80`, 1 `G_PER_L`, `CHEBI:53426` | Supported. |
| K2HPO4, 2 g | `K2HPO4`, 2 `G_PER_L`, `CHEBI:131527` | Supported. |
| Na-acetate, 5 g | `Na-acetate`, 5 `G_PER_L`, `CHEBI:32954` | Supported. |
| (NH4)3 citrate, 2 g | `(NH4)3 citrate`, 2 `G_PER_L`, `CHEBI:63037` | Supported. |
| MgSO4 x 7 H2O, 0.2 g | `MgSO4 x 7 H2O`, 0.2 `G_PER_L`, `CHEBI:31795` | Supported. |
| MnSO4 x H2O, 0.05 g | `MnSO4 x H2O`, 0.05 `G_PER_L`, `CHEBI:86364` | Supported. |

The extract and peptone rows are intentionally unresolved to CHEBI in both normalized and merged YAML, which is acceptable for these complex undefined materials. The casein-peptone row loses MediaDive's `tryptic digest` attribute, but it still points at the right source row.

The exact `CultureMech:000647` search across `data/normalized_yaml` and `data/merge_yaml` used `rg --no-ignore --hidden`. It found one maintained YAML source, this generated merge, and generated indexes only. A `find data/normalized_yaml -name 'mrs_medium.yaml'` search also found only `data/normalized_yaml/bacterial/mrs_medium.yaml`.

## Evidence

The maintained normalized record's `media_term` and import history identify the source as DSMZ / MediaDive ID 11. MediaDive REST medium 11 returned `MRS MEDIUM`, source `DSMZ`, `min_pH` 6.2, `max_pH` 6.5, and main solution 11 with a 1000 ml volume.

MediaDive supports the ten represented solute rows at the recorded g/L values and the preparation step `Adjust pH to 6.2 - 6.5.` It also contains an eleventh required recipe row, `Distilled water`, 1000 ml, which is absent from `data/normalized_yaml/bacterial/mrs_medium.yaml` and therefore absent from the generated merge.

No growth, target-organism, or strain-specific claims are present in this generated record.

## Completeness

The recipe is incomplete because a source ingredient is absent:

- MediaDive row 11, `Distilled water`, 1000 ml, has no ingredient row in the maintained or generated YAML.

The pH range and pH-adjustment step are represented. No sterilization, incubation, atmosphere, target-organism, growth-metric, or publication-reference details are present in MediaDive medium 11, so the corresponding empty optional fields are not defects in this generated record.

## Findings

### Major

1. `data/normalized_yaml/bacterial/mrs_medium.yaml` omits the MediaDive water row.
   - Evidence: inspected MediaDive medium 11 lists `Distilled water`, amount 1000, unit `ml`, as recipe row 11 in `Main sol. 11`.
   - Impact: the generated recipe has every DSMZ solute but omits the source final-volume solvent, so the formulation is not a complete transcription of DSMZ Medium 11.
   - Owner: `data/normalized_yaml/bacterial/mrs_medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__abcd760b.yaml`.

### Minor

1. The casein-peptone source attribute is not preserved.
   - Evidence: inspected MediaDive medium 11 annotates row 1 as `Casein peptone` with attribute `tryptic digest`; both normalized and generated YAML keep only `Casein peptone`.
   - Impact: this does not change the row's quantity or broad identity, but it drops source wording that can distinguish DSMZ's intended peptone form from a generic casein peptone.
   - Owner: `data/normalized_yaml/bacterial/mrs_medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__abcd760b.yaml`.

### Blocker

None found.

## Recommended Edits

1. Add the missing `Distilled water`, 1000 ml ingredient to `data/normalized_yaml/bacterial/mrs_medium.yaml` using the repository's guarded YAML writer, and keep the amount as a volume rather than converting it to grams.
2. Preserve MediaDive's `tryptic digest` attribute on the casein-peptone row, either in a supported ingredient attribute slot or a narrow curation note if no structured slot exists.
3. Append a new `curation_history` event to the maintained normalized record describing the water restoration and attribute preservation.
4. Regenerate merged records so `data/merge_yaml/merged/mrs_medium__abcd760b.yaml` reflects the maintained fix.

## Follow-up Checks

After curation, run the same focused generated-record checks:

- `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__abcd760b.yaml`
- `python scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__abcd760b.yaml --workers 1 --quiet`
- `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__abcd760b.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__abcd760b.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`

Also rerun the merge freshness check that owns `data/merge_yaml/merged/` to confirm the generated record is synchronized with `data/normalized_yaml/bacterial/mrs_medium.yaml`.

## Additional Notes

- This record is a direct one-source merge. The only maintained owner found for `CultureMech:000647` was `data/normalized_yaml/bacterial/mrs_medium.yaml`; the exact ignored-file-inclusive search also hit generated indexes.
- Modified DSM media that cite DSMZ medium 11 as a base are distinct strain-specific variants, not direct duplicates of this record.
