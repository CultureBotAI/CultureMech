# YAML Record Review: 480g_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/480G_MEDIUM.yaml
- Started UTC: 2026-09-21T07:02:03Z
- Finished UTC: 2026-09-21T07:02:36Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/480G_MEDIUM.yaml`.
- Stable identifier: `CultureMech:003312`.
- Source identity asserted by the record: JCM / MediaDive `J964`, `480G MEDIUM`.
- The generated record was merged from one owner, `480g_medium.yaml`, on fingerprint `75581c2854b3ba30e38c9db9178f898c67e7a718aa8bedbe9b89fa745536e9ed`.
- Current authoritative owner: `data/normalized_yaml/bacterial/480g_medium.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/480G_MEDIUM.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/480G_MEDIUM.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/480G_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/480G_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The generated record is grounded to a coherent source identity: `mediadive.medium:J964`, original name `480G MEDIUM`, and the linked JCM URL all identify JCM medium 964.
- JCM's current `GRMD=964` page returns the same 480G formula represented in DSMZ/BacMedia's `text/j964` rendering.
- A gitignore-independent exact search for `mediadive.medium:J964` found only one active normalized owner and the generated merge for this source.
- A gitignore-independent exact search for `480G MEDIUM` found this active owner and generated record plus archival validation rows that referenced the record's former normalized path.

## Evidence

- JCM medium 964 lists a main litre containing 0.5 g `NaCl`, 0.33 g `NH4Cl`, 0.15 g `CaCl2 x 2H2O`, 0.35 g `MgCl2 x 6H2O`, 0.3 g `KCl`, 0.3 g `KH2PO4`, 1.0 g Casitone, 0.5 g yeast extract, 1.0 mg resazurin, 1 L distilled water, and an `A5 solution` addition.
- JCM instructs that the medium be autoclaved under a nitrogen atmosphere, then amended per litre with 5 ml of a 5% `Na2S x 9H2O` solution.
- The A5 stock solution contains 4.94 mg `Co(NO3)2 x 6H2O`, 7.9 mg `CuSO4 x 5H2O`, 0.286 g `H3BO3`, 0.181 g `MnCl2 x 4H2O`, 39.0 mg `Na2MoO4 x 2H2O`, and 22.2 mg `ZnSO4 x 7H2O` per litre of A5 stock.

## Completeness

- The record has 16 ingredient rows and captures the named salts, complex ingredients, resazurin, the sulfide reducer, and every A5 component.
- The normalized owner and generated target omit both distilled-water rows and omit an explicit `A5 solution` stock-addition row.
- The main base-medium rows appear to have been adjusted by 1/1.005 to account for 5 ml sulfide added after the 1 L base medium, but the 5% sulfide solution is recorded as `5 g/L`.
- The A5 stock components are recorded at stock concentrations, not final-medium concentrations.

## Findings

- BLOCKER: the `Na2S x 9 H2O` row stores `5 G_PER_L`, apparently from the 5 ml stock-solution volume. The source calls for 5 ml of a 5% sodium sulfide nonahydrate solution per litre after autoclaving, so this row is not a source-supported final concentration.
- BLOCKER: all six A5 trace components are flattened at their per-litre stock concentrations. They are not final-medium g/L values.
- MAJOR: the target loses the source-level nested-stock structure by omitting the `A5 solution` addition row and both distilled-water rows, leaving no way to distinguish main-litre ingredients from A5 stock ingredients.
- MINOR: the record's single preparation step contains only the post-autoclave sulfide-addition instruction; it omits JCM's general 121 C for 15 minutes autoclave condition.

## Recommended Edits

- Do not edit `data/merge_yaml/merged/480G_MEDIUM.yaml` directly; it is generated.
- Repair `data/normalized_yaml/bacterial/480g_medium.yaml` so the 5% `Na2S x 9 H2O` addition is represented as a stock addition or as its computed final sodium sulfide nonahydrate amount, not as `5 g/L`.
- Represent the A5 solution as a nested stock addition if the schema supports it. Otherwise, multiply the A5 stock concentrations by the source addition volume to store unambiguous final-medium concentrations, and document the stock recipe in notes.
- Preserve the main base-medium concentrations that have already been adjusted for the final 1.005 L volume if the model intentionally stores final concentrations after sulfide addition.
- Add a preparation step for JCM's default `121 C for 15 min` autoclave condition and keep the nitrogen-atmosphere requirement attached to both autoclaving and the stored sulfide solution.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation on the regenerated `data/merge_yaml/merged/480G_MEDIUM.yaml`.
- Confirm that the regenerated target no longer has stock solution volumes or stock solution per-litre amounts encoded as direct final `G_PER_L` solute rows.
- Reconcile row counts against the source so intentional omissions of distilled water and explicit `A5 solution` are traceable.

## Additional Notes

- The target is not a duplicate merge problem; the same stock-flattening issue exists in the single authoritative normalized owner.
