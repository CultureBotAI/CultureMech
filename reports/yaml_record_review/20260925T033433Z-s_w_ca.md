# YAML Record Review: s_w_ca

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s_w_ca.yaml`
- Started UTC: `2026-09-25T03:34:33Z`
- Finished UTC: `2026-09-25T03:34:33Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s_w_ca`, a single-source merge of `data/normalized_yaml/bacterial/s_w_ca.yaml`.

The record represents MediaDive/CCAP `C101`, `S/W + Ca`, a soil/water biphasic freshwater-algae medium with calcium carbonate. The normalized source has already been repaired against the CCAP PDF, but the generated merge YAML regressed the curated per-vessel ingredient structure.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to `mediadive.medium:C101` and the CCAP `MR_SW_Ca.pdf` source. The source title is `S/W + Ca (Soil/Water Biphasic Medium + calcium carbonate)`, so the generated `LIQUID` physical state is less precise than the curated normalized `BIPHASIC` state.

## Evidence

The CCAP PDF defines S/W + Ca as S/W medium prepared with approximately 0.01 g calcium carbonate placed into the base of the test tube before the soil and water.

The base S/W recipe places an approximately 1 cm deep layer of air-dried, sieved, good calcareous garden loam in a test tube or jar; carefully adds deionized water to a depth of 7 to 10 cm; plugs or covers the vessel; steams for one hour or autoclaves for 15 minutes at 15 psi on each of two consecutive days; and lets the medium stand for a further day before inoculation, when pH should be 7.0-8.0.

## Completeness

The generated record preserves the pH 7.0-8.0 range, calcium carbonate grounding, and the high-level CCAP prose about calcareous soil, deionized water, two-day sterilization, standing before inoculation, and calcium carbonate addition.

It loses or corrupts these curated details:

- The physical state regresses from `BIPHASIC` to `LIQUID`.
- `Air-dried sieved calcareous soil` is collapsed to a generic ungrounded `Soil` row.
- Deionized water is absent as an ingredient.
- The approximately 0.01 g per vessel calcium carbonate amount is converted to `0.01 G_PER_L`.
- Six ordered preparation steps collapse into two broad prose steps, losing the explicit standing step and the optional grain note.

## Findings

- `needs curation`: The generated `physical_state: LIQUID` contradicts the CCAP source and the repaired normalized record; S/W + Ca is a soil/water biphasic medium.
- `needs curation`: The 0.01 g calcium carbonate addition is a per-vessel amount placed in the base before adding soil and water, but the generated record converts it to `0.01 G_PER_L`.
- `needs curation`: The generated merge drops the deionized-water ingredient even though CCAP instructs users to add deionized water to a depth of 7 to 10 cm above the soil layer.
- `needs curation`: The generated record loses curated soil identity: `Air-dried sieved calcareous soil` becomes ungrounded `Soil`.
- `pass with minor issues`: The source URL, calcium carbonate CHEBI grounding, pH range, and core sterilization prose are otherwise consistent with the CCAP PDF.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s_w_ca.yaml`; fix merge generation so the repaired `data/normalized_yaml/bacterial/s_w_ca.yaml` graph survives regeneration.
- Preserve `physical_state: BIPHASIC`.
- Preserve calcium carbonate as a variable per-vessel 0.01 g amount, not as `G_PER_L`.
- Preserve air-dried sieved calcareous soil and deionized water as separate variable ingredients with their CCAP depth notes.
- Carry over the normalized ordered steps for adding calcium carbonate, adding the soil layer, optional grain addition, adding deionized water, two-day sterilization, and standing before inoculation.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/s_w_ca.yaml`.
- Regenerate `data/merge_yaml/merged/s_w_ca.yaml` and verify that its physical state remains `BIPHASIC`.
- Verify that calcium carbonate no longer appears as `0.01 G_PER_L`.
- Verify that deionized water, soil, and calcium carbonate all survive as ingredients.
- Run an exact ignored-file-inclusive search for `mediadive.medium:C101`, `MR_SW_Ca.pdf`, and `s_w_ca` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. This is one of the reviewed CCAP sparse media where unmapped bulk material identities are expected and should not be mistaken for missing ontology work.
