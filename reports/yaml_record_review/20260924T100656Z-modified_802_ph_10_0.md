# YAML Record Review: Modified 802 (pH 10.0)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/modified_802_ph_10_0.yaml
- Started UTC: 2026-09-24T10:06:56Z
- Finished UTC: 2026-09-24T10:06:56Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Record | `data/merge_yaml/merged/modified_802_ph_10_0.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008297` |
| Name | `modified_802_ph_10_0` |
| Original name | `Modified 802 (pH 10.0)` |
| Category | `bacterial` |
| Medium source | TOGO `M1733`; original source NBRC `NBRC_M942` |
| Maintained owner | `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml` |
| Generated status | Stale generated merge output from one normalized TOGO record |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema validation, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_802_ph_10_0.yaml` | Passed; exited 0 with "No issues found". |
| Strict validation, `python scripts/validate_strict.py data/merge_yaml/merged/modified_802_ph_10_0.yaml --out /private/tmp/modified_802_ph_10_0.strict.tsv --workers 1 --quiet` | Passed; 0 strict errors were reported and `/private/tmp/modified_802_ph_10_0.strict.tsv` contained only the header line. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/modified_802_ph_10_0.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the focused run completed with 0 reference checks. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/modified_802_ph_10_0.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not inline `MediaRecipe.curation_history` entries in a merged YAML record. |

## Identity and Grounding

The record identity is coherent: `CultureMech:008297`, `TOGO:M1733`, and `NBRC_M942` all identify Modified 802 (pH 10.0).

An exact `find data/normalized_yaml -name 'modified_802_ph_10_0.yaml'` search, which does not honor gitignore exclusions, found `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml` as the generated record's maintained owner.

Several generated claims are stale relative to a 2026-09-12 repair in the normalized owner. The maintained record now stores `Na2CO3 solution` and `Distilled water` as milliliter-per-liter additions, restores `Hipolypepton*` as an ingredient, adds `ph_value: 10.0`, removes the generated empty solution stubs, and grounds yeast extract, agar, and Hipolypepton.

## Evidence

TOGO `M1733` and the live NBRC `NO=942` page support the same formulation:

| Source claim | Record representation | Review |
| --- | --- | --- |
| Modified 802 (pH 10.0) is sourced from NBRC `NBRC_M942` / NBRC page `NO=942`. | The record cites TOGO `M1733`, original source `NBRC_M942`, and the matching NBRC URL. | Supported. |
| Add 100 ml of a 10% Na2CO3 solution after autoclaving. | `Na2CO3 solution` is an empty solution stub at `100 G_PER_L`. | Unsupported unit, missing stock strength, and missing post-autoclave handling in the generated merge output. |
| Add 900 ml distilled water. | The generated record stores `Distilled water` as `900 G_PER_L`. | Unsupported volume-to-mass-concentration import. |
| Add 1 g MgSO4 x 7H2O, 2 g yeast extract, 1 g K2HPO4, and 10 g Hipolypepton. | The generated record correctly stores the three gram-scale non-Hipolypepton rows as `G_PER_L`, but moves `Hipolypepton*` to an empty solution stub at `10 G_PER_L`. | Hipolypepton is in the wrong record compartment. |
| Agar is 15 g if needed. | `Agar (if needed)` is a `15 G_PER_L` ingredient, but the whole recipe is classified as `SOLID_AGAR`. | The optional agar amount is present; the physical state overstates it as unconditional solid agar. |
| Check the final pH to be about 10.0. | The name mentions pH 10.0, but no `ph_value` or preparation step is represented. | Incomplete in the generated merge output. |

## Completeness

Every visible NBRC ingredient label is present somewhere in the generated record, but `Na2CO3 solution` and `Hipolypepton*` are represented as empty stock solutions instead of source-supported main-medium additions. The generated output also has stale units and missing pH relative to both the source and the normalized owner's 2026-09-12 repair.

The asterisked Hipolypepton vendor note is not a consequential scientific gap after the September repair because the normalized owner preserves it in the Hipolypepton notes. Empty target-organism and growth-evidence fields were not treated as defects. NBRC `NBRC_M942` is a medium formulation page, not a growth-evidence page.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The generated record is stale relative to the 2026-09-12 normalized repair. | The generated merge still has `100 G_PER_L` `Na2CO3 solution`, `900 G_PER_L` water, no structured pH, and empty `Na2CO3 solution` / `Hipolypepton*` solution stubs; the normalized owner has already corrected those imported values. | Regenerate from `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml`. |
| major | The post-autoclave 10% Na2CO3 addition is unsupported in generated output. | NBRC says to add 100 ml of 10% Na2CO3 solution aseptically after autoclaving; the generated record has only an empty solution stub with `100 G_PER_L`. | Already repaired in `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml`; regenerate merge output. |
| major | Hipolypepton is represented in the wrong compartment in generated output. | NBRC lists `Hipolypepton*` as a 10 g main-medium ingredient; the generated record stores it under `solutions` with an empty composition. | Already repaired in `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml`; regenerate merge output. |
| minor | Optional agar is modeled as unconditional `SOLID_AGAR`. | NBRC says `Agar (if needed)` at 15 g; the record classifies the whole recipe as `SOLID_AGAR` even though agar is conditional. | `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml`; physical-state/variant curation. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/modified_802_ph_10_0.yaml` so the 2026-09-12 repairs in `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml` propagate to generated output.
2. Re-curate `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml` so the `Agar (if needed)` row does not make the whole base recipe unconditionally `SOLID_AGAR`.
3. After regeneration, verify that `Na2CO3 solution` is no longer `100 G_PER_L`, `Distilled water` is no longer `900 G_PER_L`, `Hipolypepton*` is no longer an empty solution, and `ph_value: 10.0` is present.

## Follow-up Checks

1. Re-run focused open-schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/modified_802_ph_10_0.yaml` and the regenerated merge output.
2. Manually compare the regenerated record against TOGO `M1733` and NBRC `NO=942`, checking the 100 ml 10% Na2CO3 post-autoclave addition, the 900 ml water volume, the 10 g Hipolypepton row, and pH 10.0.
3. Verify that optional agar remains scoped to "if needed" rather than turning the source recipe into an unconditional solid-agar medium.

## Additional Notes

The repository-level `just` validators were not used for this focused record review because the project currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused validators above were run with Python 3.11 and the offline uv cache.
