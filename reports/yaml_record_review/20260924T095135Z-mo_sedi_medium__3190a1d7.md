# YAML Record Review: mo_sedi_medium__3190a1d7

- Repository: CultureMech
- Record: data/merge_yaml/merged/mo_sedi_medium__3190a1d7.yaml
- Started UTC: 2026-09-24T09:50:50Z
- Finished UTC: 2026-09-24T09:51:35Z
- Verdict: needs curation

## Target

Generated bacterial recipe for `CultureMech:010309`, `mo_sedi_medium`, from `TOGO:M891` / `JCM_M855`.

The generated file contains one source recipe, `TOGO_M891_MO-SEDI_Medium`.

## Validation

Open LinkML validation passed with `No issues found`.

Strict schema-layer validation passed with 0 error rows; `/private/tmp/mo_sedi_medium__3190a1d7.strict.tsv` contained only the header line.

Reference validation passed, but performed 0 checks.

Term validation passed. The run emitted the expected `eutils` / `pkg_resources` deprecation warning before `Validation passed`.

Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries inside generated YAML.

## Identity and Grounding

The top-level identity is correct for TOGO `M891`, which imports JCM medium 855 as `MO-SEDI Medium`.

Unlike the sibling MediaDive `J855` record, this TOGO import correctly applies the defining JCM 855 delta: glucose and tryptone are absent, and sodium pyruvate is set to 2.75 g/L.

`Solution A` and `Solution B` are incorrectly grounded to unrelated `mediadive.solution:5342` and `mediadive.solution:5343` records. These are the same name-only generic-solution links seen in the MO-CFXI parent, not the local JCM subrecipes.

JCM 855 uses JCM 846 as its base. JCM 846 links the trace vitamins, trace element solution, and Se/W solution to JCM medium 284; TOGO encodes those same reusable stocks through `M278`, which is SI Medium with `original_media_id: JCM_M284`.

## Evidence

JCM `GRMD=855` defines MO-SEDI as a delta from Medium 846: use Medium 846, omit glucose and tryptone, supplement with final 2.75 g/L sodium pyruvate, and otherwise prepare as Medium 846.

TOGO `M891` expands that instruction into a recipe that omits glucose and tryptone, keeps the Solution A / Solution B structure from MO-CFXI, and changes the Solution A sodium pyruvate row to 2.75 g/L.

TOGO `M891` also preserves the 1 mg resazurin row, the 2 ml trace vitamins solution, 1 ml trace element solution, 1 ml Se/W solution, 10 ml 3% sodium sulfide nonahydrate solution, 10 ml 3% L-cysteine hydrochloride monohydrate solution, and the anaerobic preparation comments inherited from JCM 846.

TOGO `M278` carries full nested formulas for the referenced trace vitamins solution, trace element solution, and Se/W solution.

## Completeness

The generated record preserves the defining glucose and tryptone omissions and the 2.75 g/L sodium pyruvate row.

The generated record does not preserve the M278 stock formulas, the 3% reducing-solution compositions, pH 7.0, or the anaerobic Solution A / Solution B preparation instructions.

No target organism, incubation temperature, or growth evidence is present in TOGO M891 or JCM 855, so those absences are not defects for this source-only import.

## Findings

1. Needs curation: the JCM solution hierarchy is flattened. `Solution A`, `Solution B`, trace vitamins, trace elements, Se/W, sodium sulfide, and cysteine all remain as empty solution entries, while their local components were either promoted to top-level ingredients or dropped.

2. Needs curation: the 1 mg resazurin row was stored as `1 G_PER_L`, a 1000-fold overstatement.

3. Needs curation: five source volume additions are stored as mass-per-liter concentrations: 2 ml trace vitamins, 1 ml trace element solution, 1 ml Se/W solution, 10 ml 3% sodium sulfide solution, and 10 ml 3% cysteine solution.

4. Needs curation: the M278 stock formulas are resolvable but absent.

5. Needs curation: `Solution A` and `Solution B` link to unrelated `mediadive.solution:5342` and `mediadive.solution:5343` records.

6. Minor: gas handling from distinct anaerobic preparation steps was collapsed into generic variable gas ingredients, including duplicate dinitrogen rows as `Nitrogen gas` and `N2`.

7. Minor: pH 7.0 and the anaerobic autoclave, overnight, Solution A/B mixing, culture-vessel distribution, stopper sealing, and final reducing-solution addition instructions are absent from generated structured fields.

## Recommended Edits

Re-curate `data/normalized_yaml/bacterial/TOGO_M891_MO-SEDI_Medium.yaml`, or fix the TOGO importer and solution migrator, so it keeps the correctly expanded JCM 855 carbon-source delta while preserving the Solution A / Solution B and final reducing-solution hierarchy from JCM 846.

Keep glucose and tryptone absent, keep final sodium pyruvate at 2.75 g/L, and keep resazurin at 1 mg in Solution A.

Resolve the M278 cross-references to local nested recipes for trace vitamins, trace elements, and Se/W solution, or link them to curated reusable stock records with the same formulas.

Remove `mediadive.solution:5342` and `mediadive.solution:5343` from this record unless a future grounding step can prove exact source-equivalent stock definitions.

After fixing both MO-SEDI imports, merge the TOGO M891 and MediaDive J855 records with each other, not with their MO-CFXI parent.

## Follow-up Checks

Re-run open LinkML, strict, reference, and term validation after any normalized-record edits.

After regeneration, assert that no glucose or tryptone row is introduced and that sodium pyruvate remains 2.75 g/L.

Re-fetch JCM 855, JCM 846, TOGO M891, and TOGO M278 while applying the fix so the base recipe and stock cross-references are followed explicitly.

## Additional Notes

No GitHub issues or PR comments were created by this record review.

Exact source-record lookups used `find` and `rg --no-ignore --hidden`, so ignored files were included.
