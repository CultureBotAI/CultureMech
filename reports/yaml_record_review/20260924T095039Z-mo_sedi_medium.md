# YAML Record Review: mo_sedi_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/mo_sedi_medium.yaml
- Started UTC: 2026-09-24T09:49:46Z
- Finished UTC: 2026-09-24T09:50:39Z
- Verdict: needs curation

## Target

Generated bacterial recipe for `CultureMech:003201`, `mo_sedi_medium`, from MediaDive / JCM medium `J855`.

The generated record merged `mo_sedi_medium` with `JCM_J846_MO-CFXI_MEDIUM`, the referenced parent used to expand JCM 855.

## Validation

Open LinkML validation passed with `No issues found`.

Strict schema-layer validation passed with 0 error rows; `/private/tmp/mo_sedi_medium.strict.tsv` contained only the header line.

Reference validation passed, but performed 0 checks.

Term validation passed. The run emitted the expected `eutils` / `pkg_resources` deprecation warning before `Validation passed`.

Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries inside generated YAML.

## Identity and Grounding

The top-level identity is correct for JCM `J855`: MediaDive and JCM both name the source `MO-SEDI MEDIUM` and link to JCM `GRMD=855`.

The parent relationship is wrong. JCM 855 is a recipe variant of JCM 846, not a source duplicate, because it instructs curators to omit glucose and tryptone and use final 2.75 g/L sodium pyruvate.

Most copied hydrate-sensitive groundings match the copied JCM 846 source rows, but the copied formula itself is not the final MO-SEDI formula.

## Evidence

JCM `GRMD=855` defines MO-SEDI as a delta from Medium 846: use Medium 846, omit glucose and tryptone, supplement with final 2.75 g/L sodium pyruvate, and otherwise prepare as Medium 846.

MediaDive REST for `J855` encodes the same instruction as the only `Main sol. J855` step; it does not publish an independent ingredient table for MO-SEDI.

The normalized `data/normalized_yaml/bacterial/mo_sedi_medium.yaml` was repaired by copying 34 ingredient rows from `CultureMech:003191`, the JCM 846 MO-CFXI parent, but those copied rows still include glucose at 1.99115 g/L, include tryptone at 1.10619 g/L, and retain sodium pyruvate at 0.608407 g/L.

The generated merge then fingerprinted the copied MO-SEDI composition as a duplicate of `JCM_J846_MO-CFXI_MEDIUM` instead of preserving a distinct variant.

## Completeness

The generated record does not represent the defining MO-SEDI modification at all: glucose and tryptone should be absent, and sodium pyruvate should be 2.75 g/L final.

Because the wrong parent formula was copied wholesale, the record also carries all of JCM 846's pre-existing stock-flattening and duplicate-calcium defects.

No target organism, incubation temperature, atmosphere, or growth evidence is present in JCM 855 or MediaDive J855, so those absences are not defects for this source-only import.

## Findings

1. Needs curation: the MO-SEDI variant instruction was not applied. The generated record still contains glucose and tryptone even though JCM 855 explicitly omits both, and it stores 0.608407 g/L sodium pyruvate instead of the required final 2.75 g/L.

2. Needs curation: the merge collapsed MO-SEDI with its MO-CFXI parent as `SOURCE_DUPLICATE`. These records need a real variant relationship, not a duplicate merge, because the final carbon-source recipe differs.

3. Needs curation: the copied JCM 846 composition carries inherited quantitative defects, including a calcium chloride row summed from local Solution B plus a trace-element stock row, and stock vitamin / trace element concentrations that are not scaled by their addition volumes.

4. Minor: `data_quality_flags` still contains `resolved_reference`, but the reference was only partially resolved because the variant delta was never applied.

## Recommended Edits

Re-curate `data/normalized_yaml/bacterial/mo_sedi_medium.yaml` so it derives from JCM 846 after applying the JCM 855 delta: remove glucose, remove tryptone, and set final sodium pyruvate to 2.75 g/L.

Change the relationship to the JCM 846 parent from `SOURCE_DUPLICATE` to a variant relationship that captures those carbon-source modifications.

Regenerate merged YAML after the normalized fix so MO-SEDI and JCM 846 no longer collapse under one fingerprint.

Carry over fixes for the referenced JCM 846 stock hierarchy when reconstructing MO-SEDI, otherwise the variant will inherit the same flattened trace-element and vitamin stock concentrations.

## Follow-up Checks

Re-run open LinkML, strict, reference, and term validation after any normalized-record edits.

After regeneration, assert that MO-SEDI contains no glucose or tryptone rows and that sodium pyruvate is exactly 2.75 g/L final.

Review `data/merge_yaml/merged/mo_sedi_medium__3190a1d7.yaml` separately; it is the sibling MO-SEDI generated record and may carry an independent TOGO/JCM expansion of the same source.

## Additional Notes

No GitHub issues or PR comments were created by this record review.

Exact generated-record searches used `find` and `rg --no-ignore --hidden`, so ignored files were included.
