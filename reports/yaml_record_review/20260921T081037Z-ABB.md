# YAML Record Review: ABB

- Repository: CultureMech
- Record: data/merge_yaml/merged/ABB.yaml
- Started UTC: 2026-09-21T08:08:54Z
- Finished UTC: 2026-09-21T08:10:37Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/ABB.yaml`.
- Stable identifier: `CultureMech:015482`.
- Source identity asserted by the record: CultureBotHT `ABB`, `Anaerobic basal broth`.
- The generated record was merged from one owner, `abb`, on fingerprint `ce1f621d542b10a8527a9823c6597895e253bf398869b513995a14d8aab9b53b`.
- Current authoritative owner: `data/normalized_yaml/specialized/abb.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ABB.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/ABB.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/ABB.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/ABB.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The local identity is coherent: `sources.database` is `CultureBotHT`, `sources.database_id` is `ABB`, and the curation history says the importer read `ABB.yaml`.
- A gitignore-independent exact search for `database_id: ABB` found only one normalized owner and its generated merge.
- The source provenance is not reproducible from the record. The only URL is the private CultureBotHT repository root, not a file URL or commit-pinned blob.
- Authenticated GitHub tree lookup of `CultureBotAI/CultureBotHT` on `main` found no path matching `ABB.yaml` or `abb`, so the source file named in curation history is no longer discoverable at that name on the default branch.
- The ChEBI groundings for sodium chloride, starch, D-glucose, sodium pyruvate, L-arginine, sodium succinate, sodium bicarbonate, L-cysteine hydrochloride, ferric pyrophosphate, hemin, menadione, dithiothreitol, and sodium thioglycolate are plausible label matches.

## Evidence

- The CultureMech normalized owner lists an anaerobic basal broth with peptic digest of animal tissue, yeast extract, sodium chloride, starch, D-glucose, sodium pyruvate, L-arginine, sodium succinate, sodium bicarbonate, L-cysteine HCl, ferric pyrophosphate, hemin, menadione, dithiothreitol, and sodium thioglycolate.
- The record says the formula was imported from FEBA media definitions through CultureBotHT.
- No original FEBA page, raw CultureBotHT file path, or source commit is retained in the YAML.

## Completeness

- The generated merge is complete relative to `data/normalized_yaml/specialized/abb.yaml`.
- Source-level completeness cannot be confirmed because the upstream `ABB.yaml` used by the importer is not recoverable from the recorded metadata.
- The record has no preparation steps, pH, temperature, or anaerobic handling steps beyond the short `Anaerobic basal broth` description.

## Findings

- BLOCKER: source provenance is incomplete. The record preserves a repository root and historical filename but not the CultureBotHT file path, commit SHA, raw URL, or FEBA primary source needed to reproduce ABB.
- MAJOR: evidence behind the CultureBotHT `ABB` formula cannot be independently verified from the current CultureMech record.
- MINOR: the anaerobic nature is only in free text; no structured preparation step describes anaerobic preparation or reducing-agent handling.

## Recommended Edits

- Recover the exact CultureBotHT source file and add a commit-pinned URL or archived copy to `data/normalized_yaml/specialized/abb.yaml`.
- Add the original FEBA citation if CultureBotHT copied ABB from a FEBA media definition.
- Add structured preparation information if the recovered source specifies anaerobic setup, sterilization, hemin/menadione addition, or reducing-agent timing.
- Regenerate `data/merge_yaml/merged/ABB.yaml` after provenance repair.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after provenance repair.
- Re-run an exact ignored-file-inclusive search for `database_id: ABB`; the CultureBotHT source ID should remain uniquely represented.
- Re-run an authenticated CultureBotHT lookup for the recorded source file path and commit before closing the provenance issue.

## Additional Notes

- The current ingredient table may be chemically plausible for anaerobic basal broth, but the review should not treat it as evidence-backed until the source file or FEBA definition is recovered.
