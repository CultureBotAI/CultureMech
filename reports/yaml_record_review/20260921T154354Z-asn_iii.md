# YAML Record Review: ASN-III

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asn_iii.yaml
- Started UTC: 2026-09-21T15:41:50Z
- Finished UTC: 2026-09-21T15:43:55Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asn_iii.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:000317` |
| Label | `ASN-III` |
| Normalized owner | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Source | CCAP/MediaDive `mediadive.medium:C110` |
| Generated status | Generated one-input merge of `asn_iii.yaml`; future fixes belong in the normalized owner and should be propagated by regenerating merges/pages. |

## Validation

| Check | Result |
|---|---|
| Open-schema LinkML validation | Passed: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asn_iii.yaml` exited with status 0 and no validation errors. |
| Strict validation | Passed: the no-project Python 3.11 invocation of `scripts/validate_strict.py data/merge_yaml/merged/asn_iii.yaml --out /private/tmp/asn_iii.strict.tsv --workers 1 --quiet` scanned 1 file and wrote 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/asn_iii.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 snippet checks, and reported all validations passed. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/asn_iii.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` reported validation passed after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone records under `history/`, not a focused embedded-`MediaRecipe.curation_history` validator for one generated merge record. |

The documented `just` entry points remain blocked by the project-level Python
3.13 `llvmlite==0.46.0` build failure, so the focused no-project Python 3.11
validator invocations above were used for this one record.

## Identity and Grounding

The record identity is coherent: `CultureMech:000317` is registered to
`data/normalized_yaml/bacterial/asn_iii.yaml`, the generated target has the
same stable ID, and both carry `mediadive.medium:C110` with label `ASN-III`.
MediaDive page `C110` is also `ASN-III` and exposes a CCAP-branded medium
record.

Most hydrate-specific groundings match the source labels. One phosphate
grounding still needs review: `K2HPO4 x 3 H2O` is grounded to `CHEBI:131527`
with label `dipotassium hydrogen phosphate`, which is internally valid but does
not encode the trihydrate form named by ATCC/MediaDive. `NaNO3` also still uses
the deprecated legacy `mediaingredientmech_term` slot rather than the
CHEBI-keyed MIM slot used on most other ingredients.

## Evidence

The generated record has no structured `references` or snippet-level
`evidence`, but the source URL in `notes` resolves to an ATCC ASN-III PDF and
the MediaDive C110 page exposes the same medium and A-5 Trace Metals stock.

Supported source-level claims:

| Claim | Source check |
|---|---|
| Main ASN-III salts and agar match the source quantities. | The ATCC PDF lists 25 g NaCl, 3.5 g MgSO4 x 7H2O, 2 g MgCl2 x 6H2O, 0.5 g CaCl2 x 2H2O, 0.5 g KCl, 3 mg citric acid, 3 mg ferric ammonium citrate, 0.5 mg EDTA, 0.75 g NaNO3, 0.75 g K2HPO4 . 3H2O, 0.02 g Na2CO3, and 10 g Agar Noble for 1000 ml. The record has the corresponding g/L values. |
| pH adjustment and filter sterilization are source-supported. | The ATCC PDF and MediaDive page both state to adjust pH to 7.3, filter-sterilize, and dispense into the appropriate vessel. |
| A-5 Trace Metals is a 1 L stock solution. | The ATCC PDF and MediaDive C110 page list H3BO3, MnCl2 x 4H2O, ZnSO4 x 7H2O, Na2MoO4 x 2H2O, CuSO4 x 5H2O, Co(NO3)2 x 6H2O, and 1000 ml water under a separate A-5 Trace Metals heading. |

Unsupported or incomplete assertions:

| Record assertion | Problem |
|---|---|
| H3BO3, MnCl2 x 4H2O, ZnSO4 x 7H2O, Na2MoO4 x 2H2O, CuSO4 x 5H2O, and Co(NO3)2 x 6H2O are top-level final-medium ingredients. | The source uses these as A-5 Trace Metals stock ingredients and adds 1.0 ml of that stock to the 1 L main medium. |
| `Vitamin B12` is `0.01 G_PER_L`. | The source says 10.0 mcg per 1000 ml, so the record is 1000-fold too concentrated. |
| The final medium omits water. | ATCC lists DI Water, 1000 ml, in the main ASN-III recipe. |

## Completeness

- The local standalone stock `data/normalized_yaml/bacterial/mediadive_6248_A-5_Trace_Metals.yaml`
  already models the stock identity and composition needed by this medium, but
  `asn_iii.yaml` never references it through `solutions`.
- Empty `target_organisms` and growth-evidence slots are not defects here; the
  inspected ATCC PDF and MediaDive medium page define the formulation rather
  than a narrow growth claim for one strain.
- A gitignore-independent exact search for
  `CultureMech:000317|mediadive.medium:C110|CCAP Medium C110|ASN-III` under
  `data`, `scripts`, `history`, `src`, `reports`, and `references_cache` found
  the normalized owner, the generated target, old generated copies/indexes, and
  reports; it did not find any second current normalized `CultureMech:000317`
  owner.
- `find data/normalized_yaml -name 'asn_iii.yaml'` found exactly one normalized
  owner, `data/normalized_yaml/bacterial/asn_iii.yaml`.
- `find reports/yaml_record_review -maxdepth 1 -name '*asn_iii.md'` found no
  prior report for this generated record.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | A-5 Trace Metals was flattened into final-medium ingredients. | ATCC and MediaDive add 1.0 ml of A-5 Trace Metals to the main ASN-III liter, but `asn_iii.yaml` lists the six A-5 salts as top-level ingredients at their stock g/L values and has no `solutions` entry for `mediadive.solution:6248`. | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Major | Vitamin B12 has a microgram-to-gram unit error. | ATCC specifies `Vitamin B12` as 10.0 mcg in 1000 ml; the record asserts `0.01 G_PER_L`, which represents 10 mg/L rather than 10 mcg/L. | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Major | The main 1000 ml DI water row is missing. | ATCC lists DI Water 1000 ml in the main recipe. The record omits the water row entirely. | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Major | `K2HPO4 x 3 H2O` is grounded to a non-hydrate CHEBI label. | The source and preferred term specify the trihydrate; the record's `term` is `CHEBI:131527`, label `dipotassium hydrogen phosphate`. | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Minor | Source pH is only present in prose. | ATCC and MediaDive define pH 7.3; the record preserves that value in a `preparation_steps` description but leaves top-level `ph_value` empty. | `data/normalized_yaml/bacterial/asn_iii.yaml` |
| Minor | One ingredient still uses the legacy MediaIngredientMech identifier slot. | `NaNO3` retains `mediaingredientmech_term: MediaIngredientMech:000171` after the CHEBI-keyed MIM migration. | `data/normalized_yaml/bacterial/asn_iii.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/asn_iii.yaml`, replace the flattened A-5
   trace salts with a `solutions` entry for A-5 Trace Metals:
   `mediadive.solution:6248`, 1.0 ml per 1000 ml main medium.
2. Add the main `DI Water` 1000 ml row from the source formulation.
3. Convert the Vitamin B12 quantity from 10 mcg per liter to the corresponding
   supported value and unit instead of leaving it at `0.01 G_PER_L`.
4. Ground `K2HPO4 x 3 H2O` to an exact trihydrate term or leave it unresolved;
   do not point the hydrate label at an anhydrous/generic phosphate term.
5. Promote pH 7.3 to `ph_value` while keeping the source preparation step that
   scopes the pH adjustment and filter sterilization.
6. Refresh the legacy MIM link on `NaNO3` through the packaged MIM resolver.

## Follow-up Checks

- Re-run focused schema, strict, term, and reference validation on
  `data/normalized_yaml/bacterial/asn_iii.yaml`.
- Re-run `just verify-merges` and `just audit-merge-freshness` after
  regenerating `data/merge_yaml/merged/asn_iii.yaml`.
- Re-read the ATCC ASN-III PDF and MediaDive C110 page to confirm that only
  main-solution ingredients remain top-level and that A-5 Trace Metals resolves
  to the local `mediadive_6248_A-5_Trace_Metals.yaml` stock.
- Re-run the concentration plausibility report or an equivalent focused check
  to verify that the stock-strength borate/manganese rows and B12 unit slip no
  longer appear on `CultureMech:000317`.

## Additional Notes

- The MediaDive JSON download endpoint returned `Error: wrong ID given: C110`
  for this alphanumeric CCAP identifier. The HTML page still embeds structured
  JSON-LD for the main solution and A-5 Trace Metals blocks, and the linked
  ATCC PDF provided an independent source check.
