# YAML Record Review: BG11 TES no bicarb

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BG11_TES_no_bicarb.yaml
- Started UTC: 2026-09-21T20:42:41Z
- Finished UTC: 2026-09-21T20:42:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:015453 |
| Name | BG11 TES no bicarb |
| Source | CultureBotHT FEBA media definitions |
| Generated path reviewed | data/merge_yaml/merged/BG11_TES_no_bicarb.yaml |
| Maintained owner | data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml |

The reviewed file is a generated canonical merge of one normalized source:
`data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml`. Future scientific fixes
belong in that normalized owner or in the CultureBotHT import/repair scripts,
then `data/merge_yaml/merged/` should be regenerated.

## Validation

| Check | Command | Result |
|---|---|---|
| Open-schema LinkML | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BG11_TES_no_bicarb.yaml` | Passed; no issues found |
| Strict focused validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BG11_TES_no_bicarb.yaml --out /private/tmp/BG11_TES_no_bicarb.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 ERROR rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BG11_TES_no_bicarb.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BG11_TES_no_bicarb.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` `pkg_resources` deprecation warning |
| CultureBotHT source fetch | Authenticated `gh api` for `data/manuscript_handoffs/20260914_artifact_followup/inputs/FEBA_media_definitions - Medias.tsv` and `FEBA_media_definitions - Mixes.tsv` in `CultureBotAI/CultureBotHT` | Passed; the fetched tables contain the exact `BG11_TES_no_bicarb` and `Trace metal mix A5` blocks |
| Embedded curation history | Focused single-record validator | Not checked: the repository exposes `just validate-history` for standalone records under `history/`, but no documented focused validator for embedded `MediaRecipe.curation_history` |

Direct `just validate-schema`, `just validate-strict`, and `just validate-terms`
were not rerun because the project `uv` environment currently tries to build
`llvmlite==0.46.0` under Python 3.13 and fails before reaching the target file;
the no-project Python 3.11 commands above exercise the same LinkML and semantic
entry points at record scope.

## Identity and Grounding

The generated record's ID, label, `CultureBotHT` source tuple, and algae
category identify the FEBA `BG11_TES_no_bicarb` formula. The current CultureBotHT
FEBA Medias TSV defines that source block as a minimal BG11-family medium with
no sodium bicarbonate and with TES buffer.

The component set matches the expected no-bicarbonate + TES variant: it contains
sodium nitrate, dibasic potassium phosphate, magnesium sulfate heptahydrate,
calcium chloride dihydrate, citric acid, ferric ammonium citrate, disodium EDTA,
the 1X `Trace metal mix A5` contribution, and 20 mM TES. Its absence of sodium
carbonate and sodium bicarbonate is supported by the source row and by the
`repair_culturebotht_bg11_score15.py` target for `CultureMech:015453`.

`Citric Acid` is grounded to `CHEBI:53258`, sodium citrate, even though the
source says `Citric Acid`, the YAML stores CAS `77-92-9`, and sodium citrate is
a different salt. All other present ingredient terms are aligned with the
source labels inspected in FEBA.

## Evidence

The generated concentrations are numerically supported after expanding the 1X
`Trace metal mix A5` row through its 1000X source definition:

| Source component | Source amount | Generated representation |
|---|---:|---|
| Sodium nitrate | 17.6 mM | 17.6 mM |
| Potassium phosphate dibasic | 0.2 mM | 0.2 mM |
| Magnesium sulfate heptahydrate | 0.3 mM | 0.3 mM |
| Calcium chloride dihydrate | 0.24 mM | 0.24 mM |
| Citric Acid | 28.5 uM | 28.5 uM |
| Ferric ammonium citrate | 0.006 g/l | 0.006 g/l |
| EDTA (disodium salt) | 2.4 uM | 2.4 uM |
| Trace metal mix A5 | 1 X | expanded into direct trace salts |
| TES | 20 mM | 20 mM |

The CultureBotHT FEBA Mixes table defines `Trace metal mix A5` as a 1000X trace
metal solution for `Synechococcus elongatus PCC 7942`. The generated direct
trace-salt values are the listed stock values divided by 1000, so the arithmetic
is correct for the final medium.

The generated merge is stale relative to its maintained normalized owner.
`data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` now has a `media_term`,
2026-09-10 CultureBotHT BG11 curation event, `ingredients_curated`, and
references to `CultureBotHT:BG11 TES no bicarb` plus the CultureBotHT
repository URL; the reviewed generated record has none of those fields because
it predates that normalized repair.

## Completeness

The record has no target organisms, pH, growth metrics, incubation conditions,
or preparation steps. Those are not treated as defects because the inspected
FEBA block for this formula provides a composition and description only, and
`repair_culturebotht_bg11_score15.py` intentionally assigns `ph_value` to the
base `BG11`, `BG11 noNitrogen`, and `BG11R` targets only.

The record is complete enough for the FEBA direct formula after the generated
merge is refreshed, the citric acid grounding is corrected, and the A5 stock
boundary is either restored or explicitly accepted as a derived 1X expansion.

A gitignore-independent `find` under `reports/yaml_record_review` found no
pre-existing `*-BG11_TES_no_bicarb.md` report. A gitignore-independent exact
`rg` for `BG11_TES_no_bicarb`, `BG11 TES no bicarb`, `CultureMech:015453`, and
the merge fingerprint across `data` and `reports/yaml_record_review` found the
generated merge, the normalized owner, generated indices, and import-priority
reports; it did not reveal a different maintained owner.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale relative to the normalized owner. | The normalized owner has `media_term`, `ingredients_curated`, `has_ontology_mappings`, references, and a 2026-09-10 CultureBotHT BG11 curation event that are absent from `data/merge_yaml/merged/BG11_TES_no_bicarb.yaml`. | Regenerate `data/merge_yaml/merged/BG11_TES_no_bicarb.yaml` from `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` |
| Major | `Citric Acid` is grounded to sodium citrate. | The source row is `Citric Acid`, 28.5 uM, and the CAS in the YAML is `77-92-9`; the YAML term is `CHEBI:53258` / sodium citrate instead of citric acid. | `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` |
| Minor | The source-defined `Trace metal mix A5` boundary is flattened away. | CultureBotHT lists `Trace metal mix A5` as a 1X component and defines that stock in FEBA Mixes; the YAML stores only the direct final trace-salt amounts. | `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` or a documented CultureBotHT import rule |
| Minor | `has_unmapped_ingredients` is stale. | Every ingredient in the reviewed generated file has a `term`, while the normalized owner also carries `has_ontology_mappings` and `ingredients_curated`. | `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` |

## Recommended Edits

1. Change the `Citric Acid` row in
   `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` from sodium citrate to
   an exact citric acid grounding while preserving the source concentration and
   CultureBotHT CAS/MW notes.
2. Decide whether CultureBotHT 1X mix imports should preserve a nested
   `Trace metal mix A5` solution or document that the importer intentionally
   expands 1X stocks to final direct ingredients. If nesting is preferred,
   restore the A5 stock boundary in the normalized owner.
3. Remove the stale `has_unmapped_ingredients` flag from the normalized owner
   when all ingredients remain grounded.
4. Regenerate `data/merge_yaml/merged/BG11_TES_no_bicarb.yaml` from the
   normalized owner so the canonical merge contains the September curation
   event, references, `media_term`, and data-quality flags.

## Follow-up Checks

1. Run schema, strict, term, and reference validation on
   `data/normalized_yaml/algae/bg11_tes_no_bicarb.yaml` after the citric-acid
   and flag edits.
2. Run the CultureBotHT BG11 repair coverage check or re-run
   `scripts/repair_culturebotht_bg11_score15.py` in dry-run mode, if a dry-run
   mode is added, to verify `CultureMech:015453` is still a score-15 target.
3. Regenerate merges and run `just verify-merges` and `just audit-merge-freshness`
   to prove the reviewed canonical record matches the repaired normalized owner.
4. Re-run term validation on the generated merge after regeneration so the
   corrected citric acid term is checked in the publication source layer.

## Additional Notes

This is the TES-buffered sibling of `BG11_no_bicarb`: the same citric-acid
grounding and Trace metal mix A5 representation issues recur, with one added
20 mM TES component that is source-supported.
