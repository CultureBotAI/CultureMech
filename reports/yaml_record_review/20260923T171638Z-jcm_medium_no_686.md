# YAML Record Review: jcm_medium_no_686

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_686.yaml
- Started UTC: 2026-09-23T17:15:40Z
- Finished UTC: 2026-09-23T17:16:38Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/jcm_medium_no_686.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:003031` |
| Label | `jcm_medium_no_686` |
| Source term | `mediadive.medium:J686` |
| Upstream source | JCM MEDIUM No. 686, mirrored by MediaDive as JCM `J686` |
| Generated from | `data/normalized_yaml/bacterial/jcm_medium_no_686.yaml` |

This is a generated merge artifact with `merge_fingerprint: 675db2300f03e8e730314906b43d6118adf9ec4d6ffa9595383be02134b46eed` and `merged_from: [jcm_medium_no_686]`. Future edits belong in the maintained MediaDive/JCM normalized owner, not in `data/merge_yaml/merged/`.

An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, `data/import_tracking`, `history`, and `data/culturemech_recipe_catalog.tsv` found the maintained JCM owner, the standalone `Main sol. J686` import, and TOGO M706 as an independently imported sibling for the same original JCM GRMD=686 formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_686.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_686.yaml --out /private/tmp/jcm686.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_686.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 0 reference checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_686.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. The only output before the pass line was the known `eutils` `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not `MediaRecipe.curation_history` embedded in merged YAML. |

The repository-level `just` validators were not used because this checkout's `uv` resolution currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails inside `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused Python 3.11 validators above exercise the relevant schema, strict, reference, and term gates for this one generated `MediaRecipe`.

## Identity and Grounding

The record denotes the intended JCM medium. `CultureMech:003031` is `jcm_medium_no_686`, is grounded to `mediadive.medium:J686`, and links to live JCM GRMD=686. JCM, MediaDive, and TOGO all identify the same pH 7.0 agar medium.

The main non-water ingredient list is supported: JCM and MediaDive agree on 1 g sodium succinate, 10 g Bacto peptone, 1 g ammonium sulfate, 1 g MgSO4 x 7 H2O, 2 mg MnSO4 x n H2O, 2 mg FeCl3 x 6 H2O, and 15 g agar per 1 L. The generated record loses the 1 L distilled-water row and the BD-Difco qualifier on Bacto peptone.

## Evidence

Supported claims:

- JCM and MediaDive support the J686 identity, complex/agar classification, pH 7.0, all generated non-water amounts, and adjustment to pH 7.0 with KOH.
- TOGO M706 independently supports the same original JCM accession and ingredient list.

Unsupported or incomplete generated claims:

- JCM, MediaDive, and TOGO all include 1 L or 1000 ml distilled water, but the generated JCM record has no water row.
- JCM lists `Bacto peptone (BD-Difco)` and MediaDive carries `attribute: BD-Difco`; the generated row is only `Bacto peptone`.
- JCM's default 121 C, 15 min autoclaving instruction is not retained.

## Completeness

The record is close to source but omits the solvent/final-volume row that establishes the one-liter recipe basis. Empty optional evidence, target-organism, salinity, atmosphere, and storage fields are acceptable: the inspected JCM, MediaDive, and TOGO records are source recipes and do not provide growth evidence or incubation/storage conditions.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The source water/final-volume row is missing. | JCM and TOGO list `Distilled water` as 1.0 L; MediaDive lists `Distilled water`, 1000 ml; the generated JCM record has no water ingredient. | `data/normalized_yaml/bacterial/jcm_medium_no_686.yaml` and the MediaDive import handling for water rows. |
| Minor | The Bacto peptone vendor qualifier was dropped. | JCM and TOGO list `Bacto peptone (BD-Difco)` and MediaDive has `attribute: BD-Difco`; the generated JCM record stores only `Bacto peptone`. | `data/normalized_yaml/bacterial/jcm_medium_no_686.yaml` or the MediaDive import mapping for row attributes. |
| Minor | JCM default autoclaving is absent. | The JCM GRMD=686 page states the default 121 C, 15 min autoclave rule before the table. No sterilization step is present in the generated record. | JCM/MediaDive normalized import. |

## Recommended Edits

1. Restore the 1000 ml distilled-water row in `data/normalized_yaml/bacterial/jcm_medium_no_686.yaml`.
2. Preserve the BD-Difco qualifier on Bacto peptone.
3. Preserve the JCM default 121 C, 15 min autoclaving instruction when no medium-specific exception overrides it.

## Follow-up Checks

- Rerun the focused schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/jcm_medium_no_686.yaml` after the maintained owner is edited.
- Regenerate `data/merge_yaml/merged/jcm_medium_no_686.yaml` and confirm the distilled-water row, Bacto peptone qualifier, and default sterilization text survive into the merged artifact.
- Compare the regenerated record against live JCM GRMD=686, MediaDive `/rest/medium/J686`, and TOGO M706.
- Run the merge freshness and product validators after regeneration.

## Additional Notes

- TOGO M706 is stale in a separate way: it imports KOH as a variable-concentration ingredient from the pH-adjustment instruction and has the same missing-water unit bug seen in other TOGO records, storing `1 L` water as `1 G_PER_L`.
- The standalone solution record `data/normalized_yaml/bacterial/mediadive_4589_Main_sol_J686.yaml` keeps `Distilled water` but encodes it as `1000 PERCENT_V_V`, drops the BD-Difco attribute, and still carries an obsolete `incomplete_composition` data-quality flag despite having a complete composition.
