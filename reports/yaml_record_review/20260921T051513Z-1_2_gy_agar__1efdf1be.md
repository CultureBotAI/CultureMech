# YAML Record Review: 1_2_gy_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml`
- Started UTC: 20260921T051400Z
- Finished UTC: 20260921T051513Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:007664` |
| Slug | `1_2_gy_agar` |
| Original name | `1/2 GY Agar` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |
| Source accession | `TOGO:M1141`, original JCM Medium 1072 |
| Merge fingerprint | `1efdf1be8f4d9fb4c57c534982f73b5018736f81ae55f96cb65622cd3217bcc2` |

Reviewed the generated TOGO import of JCM Medium 1072. The exact gitignore-independent ID/accession/fingerprint/JCM-source search covered the normalized YAML, generated YAML, raw/import/reference trees, and recipe catalogs; it found this target, its maintained TOGO owner, their expected catalog/index rows, the `deep_research_priority.json` entry, and a separate MediaDive/JCM 1072 record that points to the same JCM source.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml --out /private/tmp/1_2_gy_agar__1efdf1be.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validator wrappers remain blocked by the project `llvmlite==0.46.0` build under Python 3.13; the no-project `uv` commands above were used as focused equivalents.

## Identity and Grounding

- The generated record, TOGO M1141, and JCM 1072 all denote `1/2 GY Agar`.
- TOGO M1141 and JCM 1072 support 1 L distilled water, 0.5 g KH2PO4, 10 g glucose, 15 g agar, and 1 g yeast extract (BD-Difco).
- The non-water gram amounts are correctly normalized because the JCM recipe is on a 1 L basis.
- Distilled water is incorrectly stored as `1 G_PER_L`; the source unit is 1 L.
- JCM 1072 uses the default JCM sterilization condition, 121 C for 15 min unless the page states otherwise; no override is present on JCM 1072.
- `Yeast extract (BD-Difco)` is ungrounded even though the local label index maps that exact source label to `FOODON:03315426`.
- The same JCM URL is also owned by `data/normalized_yaml/bacterial/1_2_gy_agar.yaml`; that MediaDive-derived record omits distilled water, so the two JCM 1072 imports currently split into two generated artifacts instead of collapsing as source duplicates.

## Evidence

- The live TOGO M1141 API response was inspected and preserves JCM Medium 1072 as `original_media_id: JCM_M1072` with the JCM `GRMD=1072` URL.
- The JCM 1072 HTML page was inspected during the paired JCM 1072 review and supports the same component list and JCM default autoclave condition.
- No pH, incubation, salinity, or target organism is specified by the inspected JCM page; those optional fields are correctly empty.

## Completeness

- Consequential gap: `Distilled water` uses the wrong unit.
- Consequential gap: the JCM default autoclave step is missing.
- Consequential duplicate gap: this TOGO/JCM import and the MediaDive/JCM import of the same source disagree only because of the bad or absent water row.
- Non-blocking grounding gap: yeast extract is not grounded to its exact FOODON term.
- Non-blocking provenance gap: TOGO and JCM URLs are only embedded in `notes`; no structured `references` rows exist.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water has a dimensionally wrong concentration of `1 G_PER_L`. | TOGO M1141 and JCM 1072 both list 1 L distilled water. | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |
| Major | The JCM default autoclave step is omitted. | JCM 1072 has no sterilization override, so JCM's page-level 121 C, 15 min default applies. | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |
| Major | The same JCM 1072 source exists as a separate MediaDive/JCM generated record. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` points to the same JCM page and contains the same solutes, but omitted water; this TOGO owner has distorted water. | Both JCM 1072 normalized owners plus merge generation |
| Minor | `Yeast extract (BD-Difco)` is an exact ontology miss. | The local MediaIngredientMech label index maps that exact label to `FOODON:03315426`. | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |
| Minor | TOGO/JCM source URLs are not structured references. | The URLs appear only in top-level `notes`, so reference validation has 0 checks. | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |

## Recommended Edits

1. Change distilled water to `1000 ML_PER_L` in `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml`.
2. Add the JCM default autoclave step.
3. Add exact `FOODON:03315426` grounding to `Yeast extract (BD-Difco)`.
4. Add structured references for TOGO M1141 and JCM 1072.
5. Reconcile this owner with `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` so the two JCM 1072 imports merge after water normalization.

## Follow-up Checks

- Re-run strict, term, and reference validation for the TOGO owner.
- Re-run the same focused validators for the MediaDive JCM 1072 owner after both sides are repaired.
- Re-run merge generation and verify there is one JCM 1072 generated output instead of separate `1_2_GY_AGAR.yaml` and `1_2_gy_agar__1efdf1be.yaml` artifacts.

## Additional Notes

- KH2PO4, glucose, agar, and the yeast-extract amount match both inspected sources.
- This report intentionally mirrors the JCM 1072 duplicate finding from `1_2_GY_AGAR` because each source-owned normalized file has a different water defect that contributes to the same merge split.
