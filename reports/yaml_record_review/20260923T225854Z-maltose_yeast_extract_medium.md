# YAML Record Review: maltose_yeast_extract_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/maltose_yeast_extract_medium.yaml
- Started UTC: 2026-09-23T22:58:54Z
- Finished UTC: 2026-09-23T22:59:27Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| CultureMech ID | CultureMech:010013 |
| Label | maltose_yeast_extract_medium |
| Original label | Maltose-Yeast Extract Medium |
| Category | bacterial |
| Source identity | TOGO M613, originally JCM Medium J606 |
| Merge fingerprint | d3a8b47a30a75125a43e894ee9d2bd47c1ec3cb600fc0b5569ab009caafcff98 |
| Merged from | `TOGO_M613_Maltose-Yeast_Extract_Medium` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml` |
| Generated status | Generated by `merge_recipes.py` on 2026-08-06 |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/maltose_yeast_extract_medium.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/maltose_yeast_extract_medium.yaml --out /private/tmp/maltose_yeast_extract_medium.strict.tsv --workers 1 --quiet` | Passed with 1 file, 0 errors; the TSV contained only the header row |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/maltose_yeast_extract_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 1 file and 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/maltose_yeast_extract_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded history | `just validate-history` | Not checked: this repository validator targets standalone `history/` records, not embedded `MediaRecipe.curation_history` entries |

## Identity and Grounding

- The generated record consistently denotes TOGO M613, derived from JCM Medium J606 MALTOSE-YEAST EXTRACT MEDIUM.
- JCM GRMD 606, MediaDive `J606`, and the TOGO M613 API agree on the represented JCM formula: 20 g maltose, 10 g yeast extract, 2 g K2HPO4, 5 g NaCl, 15 g agar, and 1 L water at pH 6.0.
- The same-slug NBRC 945 record is not an exact duplicate because it uses KH2PO4 rather than K2HPO4 and marks agar optional.
- `Maltose` is grounded too narrowly as `CHEBI:18167` / alpha-maltose. The JCM and TOGO labels are generic Maltose, and the packaged MediaIngredientMech label index maps that exact label to `CHEBI:17306`.

## Evidence

| Claim | Support |
|---|---|
| JCM 606 contains 20 g maltose, 10 g yeast extract, 2 g K2HPO4, 5 g NaCl, 15 g agar, and 1 L distilled water | Supported by the live JCM GRMD 606 page and mirrored by MediaDive `J606` and TOGO M613 |
| JCM 606 is adjusted to pH 6.0 | Supported by the live JCM GRMD 606 preparation note, MediaDive `J606`, and TOGO M613 |
| JCM 606 uses K2HPO4 rather than KH2PO4 | Supported by the live JCM GRMD 606 page |

## Completeness

- The generated record omits the source pH 6.0 despite TOGO M613 carrying `ph: "6.0"` and JCM GRMD 606 stating the pH adjustment.
- The generated water row uses `value: '1'` with `unit: G_PER_L`; the inspected JCM, MediaDive, and TOGO records all state 1 L or 1000 ml distilled water.
- The generated record omits JCM's default 121 C for 15 min autoclave instruction.
- Empty optional slots for organism growth evidence, incubation temperature, salinity, atmosphere, storage, and variants were not treated as defects because the inspected JCM and MediaDive records do not state them.

## Findings

| Severity | Finding | Evidence | Maintained owner for a future fix |
|---|---|---|---|
| Major | The pH 6.0 source condition is missing. | JCM GRMD 606, MediaDive `J606`, and TOGO M613 state pH 6.0, but the generated record has no `ph_value` or `ADJUST_PH` step. | `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml` or the TOGO pH importer |
| Major | The water amount uses the wrong unit. | The inspected sources list 1 L or 1000 ml distilled water; the generated row stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml` or TOGO import unit normalization for water rows |
| Major | Maltose is grounded to alpha-maltose, over-specifying the source reagent. | The JCM and TOGO labels are simply `Maltose`; the packaged MediaIngredientMech label index has an exact `Maltose` mapping to `CHEBI:17306`, while the generated row uses `CHEBI:18167` / alpha-maltose. | `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml` |
| Minor | Yeast extract is ungrounded. | The packaged MediaIngredientMech label index maps exact `Yeast extract` to `FOODON:03315426`, but the generated row has no ontology term. | `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml` plus ingredient enrichment for complex non-CHEBI terms |
| Minor | The generated record omits JCM's default sterilization. | The live JCM GRMD 606 page says to autoclave media at 121 C for 15 min unless otherwise stated. | The normalized TOGO M613 owner or JCM import normalization |

No blocker findings were found.

## Recommended Edits

1. Add pH 6.0 as a structured pH value and `ADJUST_PH` step on the normalized TOGO M613 owner.
2. Convert the source 1 L distilled-water row from `1 G_PER_L` to the repository's accepted 1000 `ML_PER_L` representation.
3. Re-ground `Maltose` from alpha-maltose to the exact generic maltose identity, mirroring the packaged label-index mapping.
4. Ground `Yeast extract` through the packaged MediaIngredientMech label index.
5. Add the JCM default autoclave instruction as a source-level sterilization step.
6. Regenerate `data/merge_yaml/merged/maltose_yeast_extract_medium.yaml` and downstream media pages after the normalized owner is corrected.

## Follow-up Checks

- Re-run open schema, strict schema, term validation, and reference validation on `data/normalized_yaml/bacterial/TOGO_M613_Maltose-Yeast_Extract_Medium.yaml`.
- Re-run the same focused validators on regenerated `data/merge_yaml/merged/maltose_yeast_extract_medium.yaml`.
- Compare the regenerated record against JCM GRMD 606 and MediaDive `J606`, checking pH 6.0, K2HPO4, 1 L water, 15 g/L agar, and generic Maltose grounding.
- Re-run the term validator and confirm `Maltose` no longer resolves to `CHEBI:18167`.

## Additional Notes

- The NBRC 945 same-slug record was inspected only to disambiguate it from this JCM 606 record; its KH2PO4 and optional agar formulation should remain a separate source record unless a future manual review proves equivalence.
