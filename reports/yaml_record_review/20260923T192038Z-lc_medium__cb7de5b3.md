# YAML Record Review: LC medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/lc_medium__cb7de5b3.yaml
- Started UTC: 2026-09-23T19:19:33Z
- Finished UTC: 2026-09-23T19:20:38Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:009623 |
| Name | lc_medium |
| Original name | LC medium |
| Category | bacterial |
| Physical state | LIQUID |
| Media grounding | TOGO:M3166 |
| Source provenance | Togo Medium M3166 |
| Generated file | data/merge_yaml/merged/lc_medium__cb7de5b3.yaml |
| Maintained owner | data/normalized_yaml/bacterial/lc_medium.yaml |
| Merge fingerprint | cb7de5b3a7c72c31b7a6abf6bb29e4db1cc3829c55bda725881123aada1971f8 |

The reviewed file is a generated merge from one maintained Togo M3166 owner. Future fixes belong in `data/normalized_yaml/bacterial/lc_medium.yaml`, followed by regeneration of `data/merge_yaml/merged/` and downstream products.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema validation | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/lc_medium__cb7de5b3.yaml`. |
| Strict validator | Passed with zero error rows in `/private/tmp/lc_medium__cb7de5b3.strict.tsv`. |
| LinkML reference validator | Passed; the reference validator completed with zero checks because this generated record has no `references` collection. |
| LinkML term validator | Passed. |
| Embedded curation history | Not checked: the repository history validator validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

Togo M3166 identifies a liquid `LC medium` record extracted from a sentence that states the medium was adapted from LC medium and lists phosphate, sulfate, urea, magnesium, calcium, iron, cysteine, vitamins, and trace metals per liter. The generated record preserves the Togo identity and the intended ingredient set, but its concentration units and several ontology mappings are not reliable.

An ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive` for `CultureMech:009623`, `TOGO:M3166`, `TOGO_M3166`, `lc_medium.yaml`, and the merge fingerprint found this generated record, its maintained owner, source indexes, and archived validation rows. No original source URL or paper citation for the LC medium sentence was present in those searched paths.

## Evidence

Supported in the inspected Togo M3166 source:

- The `TOGO:M3166` identity, `LC medium` label, and liquid state are supported.
- The gram-per-liter macro-components are supported at these amounts: 2 g/L KH2PO4, 3 g/L K2HPO4, 0.1 g/L Na2SO4, 0.5 g/L urea, 0.2 g/L MgCl2.6H2O, 0.05 g/L CaCl2.2H2O, 0.0035 g/L Fe salt, 0.025 g/L FeCl2.4H2O, and 1.0 g/L L-cysteine HCl.H2O.
- The milligram-per-liter trace and vitamin components are supported as quantities, but not as gram-per-liter concentrations: 20 mg/L pyridoxamine dihydrochloride, 4 mg/L PABA, 2 mg/L d-biotin, 2 mg/L B12, 6 mg/L MnCl2.4H2O, 2.5 mg/L ZnCl2, 0.6 mg/L CoCl2.6H2O, 0.6 mg/L NiCl2.6H2O, 0.6 mg/L CuSO4.5H2O, 0.6 mg/L H3BO3, and 0.6 mg/L Na2MoO4.2H2O.

Unsupported or incomplete in the generated record:

- Water is encoded as `1 G_PER_L`, while Togo M3166 lists 1 L distilled water.
- Every source value expressed in mg/L is encoded as the same numeric value in `G_PER_L`, causing 1000-fold concentration errors.
- The record grounds `CoCl2.6H2O` to CHEBI:35696 / cobalt dichloride and `NiCl2.6H2O` to CHEBI:34887 / nickel dichloride, labels that do not preserve the hexahydrate salts named in the source.
- B12 is left unmapped even though the Togo component label resolves it to cobalamin.
- The Togo parsed component is `FeSO4.7H2O`, but the extracted comment says `FeSO2.7H2O`; the original LC medium source should be inspected before deciding whether this is a Togo transcription error or a record error.

## Completeness

The record is complete enough for the LC medium identity and the list of ingredient names in the Togo parse. It is not complete enough for use as a recipe because the solvent volume and eleven mg/L trace or vitamin components are off by 1000-fold, and because the iron sulfate source conflict is unresolved.

Empty optional organism, pH, incubation, and storage fields are acceptable for now; the inspected Togo source does not provide those values.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The maintained owner and generated record convert eleven mg/L quantities to g/L. | Togo M3166 lists pyridoxamine, PABA, d-biotin, B12, MnCl2.4H2O, ZnCl2, CoCl2.6H2O, NiCl2.6H2O, CuSO4.5H2O, H3BO3, and Na2MoO4.2H2O in mg/L, but the YAML stores the same values as `G_PER_L`. | `data/normalized_yaml/bacterial/lc_medium.yaml` or the Togo importer |
| Major | Distilled water is converted from 1 L to `1 G_PER_L`. | Togo M3166 lists 1 L Distilled water. | `data/normalized_yaml/bacterial/lc_medium.yaml` or the Togo importer |
| Major | Hydrated cobalt and nickel salts have under-scoped CHEBI mappings. | The source components are `CoCl2.6H2O` and `NiCl2.6H2O`, while the YAML maps them to anhydrous cobalt dichloride and nickel dichloride labels. | `data/normalized_yaml/bacterial/lc_medium.yaml` |
| Major | The iron sulfate identity is unresolved. | Togo's parsed component is `FeSO4.7H2O`, but the same source's extracted comment says `FeSO2.7H2O`; no primary LC medium paper was available locally to resolve the discrepancy. | `data/normalized_yaml/bacterial/lc_medium.yaml` |
| Minor | B12 lacks an ontology grounding and the record has no structured references. | Togo labels B12 as cobalamin, while the YAML leaves it unmapped and reference validation performed zero checks. | `data/normalized_yaml/bacterial/lc_medium.yaml` |

## Recommended Edits

1. Convert the eleven trace and vitamin entries from mg/L to g/L or introduce an mg/L unit representation that preserves the source dimensions exactly.
2. Convert distilled water to 1000 ml/L.
3. Inspect the original LC medium reference behind the Togo extraction to resolve the FeSO2 versus FeSO4 discrepancy and add a structured reference if the paper can be recovered.
4. Correct hydrated-salt ontology mappings for CoCl2.6H2O and NiCl2.6H2O, and ground B12 to cobalamin if the schema's ingredient grounding rules allow that broad term.
5. Regenerate `data/merge_yaml/merged/` and downstream pages.

## Follow-up Checks

- Rerun open schema validation, the strict validator, the reference validator, and the term validator on `data/merge_yaml/merged/lc_medium__cb7de5b3.yaml` after regeneration.
- Manually compare the regenerated values against Togo M3166 and confirm that all mg/L source quantities have been scaled or represented without 1000-fold inflation.
- Re-run an ignored-inclusive search for `TOGO:M3166` and `cb7de5b3a7c72c31b7a6abf6bb29e4db1cc3829c55bda725881123aada1971f8` to confirm that only the intended LC medium source owns the regenerated record.

## Additional Notes

The source sentence in Togo says the medium was adapted from LC medium and cites `[49]`, but Togo does not expose the paper URL or title in the M3166 API response. This report did not identify that paper in the local normalized, merged, or archived validation paths.
