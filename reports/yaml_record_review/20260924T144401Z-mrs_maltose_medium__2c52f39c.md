# YAML Record Review: MRS MALTOSE MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml
- Started UTC: 2026-09-24T14:42:35Z
- Finished UTC: 2026-09-24T14:44:01Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:010518 |
| Name | mrs_maltose_medium |
| Original name | MRS MALTOSE MEDIUM |
| Category | fungal |
| Media term | mediadive.medium:J293 |
| Source | MediaDive import of JCM Medium 293 |
| Reviewed path | data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml |
| Maintained source | data/normalized_yaml/fungal/mrs_maltose_medium.yaml |
| Generated status | Generated by merge_recipes.py on 2026-08-06; stale relative to the maintained 2026-09-13 JCM repair |

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml` exited 0 with no diagnostics. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml --out /private/tmp/mrs_maltose_medium__2c52f39c.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The generated record is structurally valid but stale relative to the repaired
JCM 293 source.

## Identity and Grounding

- The reviewed record has `media_term.id: mediadive.medium:J293` and
  `media_term.label: MRS MALTOSE MEDIUM`; the live JCM GRMD 293 page has the
  same formula and title.
- The generated record preserves JCM 293's 55 g Lactobacilli MRS broth, 10 g
  maltose, and pH 6.5.
- The generated record omits JCM 293's 1 L distilled-water row, which the
  maintained record now stores as `1000.0 ML_PER_L`.
- `Lactobacilli MRS broth (BD-Difco)` is an opaque commercial component;
  leaving it ungrounded is consistent with the maintained record's
  `has_unmapped_ingredients` flag.
- TOGO M287 is a TOGO copy of the same JCM_M293 page and remains as a separate
  generated `MRS_Maltose_Medium.yaml` record instead of being merged with this
  JCM/MediaDive output.

## Evidence

JCM GRMD 293 supports:

| Ingredient or instruction | Source value | Generated YAML | Maintained YAML |
| --- | --- | --- | --- |
| Lactobacilli MRS broth, BD-Difco | 55.0 g | 55 G_PER_L | 55 G_PER_L |
| Maltose | 10.0 g | 10 G_PER_L | 10 G_PER_L |
| Distilled water | 1.0 L | missing | 1000.0 ML_PER_L |
| pH adjustment | pH 6.5 | ADJUST_PH step and `ph_value: 6.5` | ADJUST_PH step and `ph_value: 6.5` |

JCM states that media should be autoclaved at 121 C for 15 min unless otherwise
specified. Neither the generated nor the maintained MediaDive/JCM record
preserves that default autoclave instruction.

## Completeness

- The generated record omits the source distilled-water row.
- The generated record omits maintained source-scoped ingredient notes, the
  `ingredients_curated`, `has_ontology_mappings`, and
  `has_unmapped_ingredients` quality flags, and the JCM/MediaDive references.
- The generated and maintained JCM/MediaDive records omit JCM's default
  autoclave instruction.
- A direct duplicate exists at `data/merge_yaml/merged/MRS_Maltose_Medium.yaml`:
  TOGO M287 imports JCM_M293 and carries the same Lactobacilli MRS broth,
  maltose, distilled-water, and pH formula.
- A `find` search, which included ignored files, found two maintained files
  named `mrs_maltose_medium.yaml`: the repaired MediaDive/JCM source under
  `data/normalized_yaml/fungal` and the TOGO M287 copy under
  `data/normalized_yaml/bacterial`.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `mediadive.medium:J293`, `JCM_M293`, `GRMD=293`,
  `CultureMech:010518`, and `mrs_maltose_medium` found the maintained
  MediaDive/JCM source, the JCM 293 solution artifact, this generated record,
  and the direct TOGO M287 copy of the same JCM source.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The generated record is missing the JCM water row. | JCM GRMD 293 lists 1.0 L distilled water, and the 2026-09-13 maintained repair stores `1000.0 ML_PER_L`; the generated YAML has no water ingredient. | Regenerate `data/merge_yaml/merged/mrs_maltose_medium__2c52f39c.yaml` from `data/normalized_yaml/fungal/mrs_maltose_medium.yaml` |
| major | The generated record lost repaired JCM source metadata. | The maintained YAML has source-scoped ingredient notes, curated quality flags, and JCM/MediaDive references; the generated YAML predates those fields. | Regeneration from `data/normalized_yaml/fungal/mrs_maltose_medium.yaml` |
| major | The same JCM 293 formula remains as two generated records. | `mrs_maltose_medium__2c52f39c.yaml` imports JCM J293 via MediaDive, while `MRS_Maltose_Medium.yaml` imports TOGO M287 whose original source is JCM_M293; both denote the same JCM page. | Cross-provider duplicate detection for JCM-backed TOGO and MediaDive/JCM imports |
| major | The maintained JCM/MediaDive record still lacks the JCM default autoclave step. | JCM GRMD 293 says to autoclave at 121 C for 15 min unless otherwise stated; `data/normalized_yaml/fungal/mrs_maltose_medium.yaml` has only the pH adjustment step. | `data/normalized_yaml/fungal/mrs_maltose_medium.yaml` |

## Recommended Edits

1. Regenerate the JCM/MediaDive MRS Maltose Medium output from
   `data/normalized_yaml/fungal/mrs_maltose_medium.yaml`.
2. Confirm the regenerated record includes `Distilled water` at
   `1000.0 ML_PER_L`, curated quality flags, source-scoped ingredient notes,
   and the JCM/MediaDive references.
3. Add the JCM default AUTOCLAVE preparation step and `sterilization` block to
   the maintained MediaDive/JCM record before regeneration.
4. Add or repair cross-provider duplicate handling so JCM J293 and TOGO M287
   merge into one generated record rather than remaining as
   `mrs_maltose_medium__2c52f39c.yaml` and `MRS_Maltose_Medium.yaml`.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated JCM/MediaDive output.
- Search with `rg --no-ignore --hidden` for `mediadive.medium:J293`,
  `TOGO:M287`, and `JCM_M293` across `data/normalized_yaml` and
  `data/merge_yaml` to confirm the JCM and TOGO copies are either deliberately
  linked or merged.
- Manually compare the regenerated record with JCM GRMD 293.

## Additional Notes

None found.
