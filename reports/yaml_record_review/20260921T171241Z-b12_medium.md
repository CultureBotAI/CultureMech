# YAML Record Review: b12_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/b12_medium.yaml
- Started UTC: 2026-09-21T17:11:08Z
- Finished UTC: 2026-09-21T17:12:41Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:001338 |
| name | b12_medium |
| original_name | B12 - MEDIUM |
| category | bacterial |
| media_term | DSMZ Medium 236, mediadive.medium:236 |
| generated status | Generated merge artifact in data/merge_yaml/merged |
| maintained owner | data/normalized_yaml/bacterial/b12_medium.yaml |

The target is the generated merge for one DSMZ/MediaDive source record,
`b12_medium`, with merge fingerprint
`83618cce5da2a18c231e649ce0cf27fc0048e8175dc829a75cd2a4e97778c345`.

## Validation

| Check | Result |
|---|---|
| Open schema | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/b12_medium.yaml` completed with no errors. |
| Strict validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/b12_medium.yaml --out /private/tmp/b12_medium.strict.tsv --workers 1 --quiet` completed with no errors. |
| Reference validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/b12_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` completed with 0 checks. |
| Term validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/b12_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` completed with no errors, aside from a non-fatal `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone `history/` files, not a focused embedded `MediaRecipe.curation_history` validator for one merge record. |
| Direct `just` validators | Not checked: the project-level `uv` environment currently fails before target-specific validation while building `llvmlite==0.46.0` under Python 3.13, with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project LinkML validator workaround above exercised the target schema, strict, reference, and term checks. |

## Identity and Grounding

- The record identity is supported. DSMZ Medium 236 is titled `B12 - MEDIUM`, and
  the generated record's `mediadive.medium:236` grounding agrees with the maintained
  DSMZ owner and cited PDF URL.
- The solid agar state, pH 7.0, `COMPLEX` medium type, and `UNDEFINED` composition
  classification are supported: DSMZ lists 15 g agar, tells curators to adjust pH to
  7.0, and includes undefined Casein hydrolysate.
- The K2HPO4, MgSO4 x 7 H2O, glycerol, asparagine, vitamin B12, and agar identities
  are grounded to terms that match the source labels closely.

## Evidence

Supported by inspected source text:

- DSMZ Medium 236 lists Casein hydrolysate 6.00 g, K2HPO4 0.20 g,
  MgSO4 x 7 H2O 0.20 g, FeSO4 x 7 H2O at trace amount, Glycerol 2.00 ml,
  Asparagine 0.15 g, Vitamin B12 40.00 micrograms, Agar 15.00 g, and
  Distilled water 1000.00 ml.
- DSMZ Medium 236 specifies pH 7.0, and the generated record preserves it both as
  `ph_value: 7.0` and as one preparation step.
- The generated target correctly represents Vitamin B12 at `4e-05 G_PER_L`, equivalent
  to the source 40 micrograms per liter.

Unsupported or stale in the generated target:

- Glycerol is stored as `2 G_PER_L`, but DSMZ lists 2.00 ml.
- FeSO4 x 7 H2O is stored as `0 G_PER_L`, but DSMZ lists a trace amount, not a zero
  amount.
- Distilled water is absent, but DSMZ lists 1000.00 ml.

## Completeness

- Source provenance is sufficient to recover the source formulation through the
  DSMZ Medium 236 PDF URL in the maintained owner.
- Empty organism and growth-metric slots are acceptable for this source recipe. DSMZ
  Medium 236 establishes a formulation, not a growth claim for a specific strain.
- The generated record's standalone `references` slot is empty; the source pointer
  lives in `media_term` and `notes`.
- Exact gitignore-independent search covered `reports/yaml_record_review` for an
  existing `b12_medium.md` report and found no prior report for this exact generated
  record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Glycerol has the wrong unit dimension. | DSMZ Medium 236 lists 2.00 ml Glycerol; the generated target stores `2 G_PER_L`. | `data/normalized_yaml/bacterial/b12_medium.yaml` or the MediaDive/DSMZ importer. |
| Major | The trace iron row is flattened to zero concentration. | DSMZ Medium 236 lists FeSO4 x 7 H2O as trace; the generated target stores `0 G_PER_L`, which states absence rather than trace addition. | `data/normalized_yaml/bacterial/b12_medium.yaml` or the MediaDive/DSMZ importer. |
| Major | The 1000 ml water row is missing. | DSMZ Medium 236 lists 1000.00 ml Distilled water, but neither the normalized owner nor the generated target has a water ingredient row. | `data/normalized_yaml/bacterial/b12_medium.yaml` or the MediaDive/DSMZ importer. |

No blocker or minor findings found.

## Recommended Edits

1. Correct Glycerol in `data/normalized_yaml/bacterial/b12_medium.yaml` to a supported
   2.00 ml/L volume representation instead of `2 G_PER_L`.
2. Replace the FeSO4 x 7 H2O `0 G_PER_L` row with an explicit trace representation
   that does not assert zero concentration.
3. Add the missing 1000.00 ml Distilled water row.
4. Regenerate `data/merge_yaml/merged/b12_medium.yaml` from the corrected normalized
   owner.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/b12_medium.yaml` after the
  maintained owner is repaired.
- `just validate-terms data/normalized_yaml/bacterial/b12_medium.yaml` after adding
  and grounding Distilled water.
- Manual comparison with DSMZ Medium 236 to verify glycerol, FeSO4 x 7 H2O, Vitamin
  B12, Distilled water, and pH 7.0.
- `just verify-merges` after regenerating the merge layer.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated record has
  no `references` block.
