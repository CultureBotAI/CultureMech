# YAML Record Review: pc

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pc__c50558b8.yaml
- Started UTC: 2026-09-24T19:53:45Z
- Finished UTC: 2026-09-24T19:53:45Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000386 |
| Name | pc |
| Original name | PC |
| Source identity | CCAP Medium C73 / PC |
| Source path | data/normalized_yaml/bacterial/pc.yaml |
| Generated path | data/merge_yaml/merged/pc__c50558b8.yaml |
| Merge fingerprint | c50558b8c109f5da18144867240e434075bc8f58b5bf47b5744775a55c988617 |

The target is a generated merged record. Future edits should be made in the
maintained CCAP input and MediaDive stock-solution import logic, not directly
in `data/merge_yaml/merged/pc__c50558b8.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pc__c50558b8.yaml` exited 0 and reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/pc__c50558b8.yaml --out /private/tmp/pc__c50558b8.strict.tsv --workers 1 --quiet` exited 0 and wrote only the TSV header. |
| Reference validator | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/pc__c50558b8.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 checks. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/pc__c50558b8.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

## Identity and Grounding

The `mediadive.medium:C73` grounding resolves to CCAP medium PC and the
inspected CCAP `MR_PC.pdf`. The PDF identifies PC as Prescott's and Carrier's
Solution for freshwater protozoa.

The generated CHEBI groundings are syntactically valid, but the concentrations
are stock-level values, not final-medium values. CCAP prepares two stock
solutions and adds 1 ml of Stock solution 1 plus 10 ml of Stock solution 2 per
liter of final PC:

| Ingredient | Generated value | Expected flattened final value |
| --- | --- | --- |
| MgSO4 x 7 H2O | 0.2 `G_PER_L` | 0.002 `G_PER_L` |
| KCl | 0.5 `G_PER_L` | 0.005 `G_PER_L` |
| CaCl2 | 1 `G_PER_L` | 0.01 `G_PER_L` |
| NaCl | 1 `G_PER_L` | 0.01 `G_PER_L` |
| CaHPO4 | 0.36 `G_PER_L` | 0.0036 `G_PER_L` |

The record category is `bacterial`, but the inspected CCAP source is a Culture
Collection of Algae and Protozoa recipe for freshwater protozoa; the source
does not support bacterial categorization.

## Evidence

The CCAP PDF supports the PC identity, its two stock-solution formulation, the
1 ml/L and 10 ml/L final stock additions, final make-up to 1 liter with sterile
deionised water, both stock autoclave steps, and the final decanting or
0.2-micron filtration guidance.

MediaDive C73 preserves the stock structure but represents Stock solution 1 as
a 10x dilution added at 10 ml/L. That is equivalent to the CCAP PDF's 1 ml/L
addition of the original Stock solution 1, but the generated target does not
apply either dilution and stores the stock rows as final concentrations.

An ignored-inclusive PCRE2 search for `mediadive.medium:C73`, `Source: CCAP,
ID: C73`, `MR_PC.pdf`, and `original_name: PC` found two maintained PC inputs:

- `data/normalized_yaml/bacterial/pc.yaml`, the MediaDive/CCAP C73 projection
  that generated this target.
- `data/normalized_yaml/algae/pc.yaml`, an earlier direct CCAP curation with
  only three malformed stock-level ingredient rows and legacy quality flags.

The direct CCAP and MediaDive imports also emit separate generated files,
`data/merge_yaml/merged/PC.yaml` and
`data/merge_yaml/merged/pc__c50558b8.yaml`, even though both describe CCAP PC.

## Completeness

The generated target is materially incomplete because it omits the final
sterile deionised-water row and does not represent the two final stock-solution
additions.

No `target_organisms` are expected on this provider recipe. That optional slot
is correctly empty.

## Findings

### Major

- `data/normalized_yaml/bacterial/pc.yaml` flattens CCAP stock-solution
  concentrations as if they were final-medium concentrations. If the recipe is
  flattened, all five solute rows should be 0.01x the generated values.
- `data/normalized_yaml/bacterial/pc.yaml` and the generated target omit the
  final sterile deionised-water row from the CCAP PC recipe.
- CCAP PC is split into two generated records, `pc__c50558b8.yaml` from
  MediaDive C73 and `PC.yaml` from the direct CCAP input, instead of being
  reconciled as one maintained recipe.
- The generated target keeps a `bacterial` category, but the inspected CCAP
  source describes a freshwater protozoa solution and does not support a
  bacterial classification.

## Recommended Edits

- Fix the MediaDive CCAP import or maintained `data/normalized_yaml/bacterial/pc.yaml`
  so PC preserves its two stock recipes or stores correctly diluted final
  concentrations.
- Add the final sterile deionised-water row to the maintained PC representation.
- Reconcile `data/normalized_yaml/bacterial/pc.yaml` with
  `data/normalized_yaml/algae/pc.yaml` so the two CCAP PC imports do not emit
  separate generated records for the same recipe.
- Reclassify PC into a source-supported category or document why it belongs in
  the bacterial collection despite the CCAP protozoa-source identity.

## Follow-up Checks

- Rerun the open schema, strict, reference, and term validators on the
  regenerated PC record.
- Compare the regenerated PC ingredient list against MediaDive C73 and the
  CCAP PDF to confirm that stock concentrations have not been used as final
  concentrations.
- Re-run an ignored-inclusive exact search for `mediadive.medium:C73`,
  `MR_PC.pdf`, and `CCAP:PC` to confirm only one maintained CCAP PC recipe is
  active or that a deliberate category split is explicitly documented.

## Additional Notes

None found.
