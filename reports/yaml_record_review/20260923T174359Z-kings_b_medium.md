# YAML Record Review: kings_b_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/kings_b_medium.yaml
- Started UTC: 2026-09-23T17:43:24Z
- Finished UTC: 2026-09-23T17:43:59Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:008788`, the generated `kings_b_medium` record merged from `data/normalized_yaml/bacterial/kings_b_medium.yaml`. The record represents TOGO `M2195`, "King's B medium", as a bacterial, complex, undefined, liquid medium.

## Validation

- LinkML open schema validation passed for `data/merge_yaml/merged/kings_b_medium.yaml`.
- Strict validation passed with 0 error rows in `/private/tmp/kings_b_medium.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` entries were not checked: the available history validator validates standalone files under `history/`, not embedded generated YAML history.

## Identity and Grounding

An ignored-inclusive exact search for `CultureMech:008788` and `TOGO:M2195` found only the active TOGO owner, index rows, and this generated record for the exact TOGO source. No exact local duplicate was found for the TOGO source identifier.

The TOGO record has no `src_url`, but the TOGO payload identifies `M2195` as "King's B medium" and carries the same five-ingredient formula used by the normalized record.

## Evidence

TOGO M2195 lists, per 1 L distilled water, 1.5 g magnesium sulphate heptahydrate, 1.5 g dipotassium hydrogen phosphate, 15 ml glycerol, and 20 g Proteose peptone. The TOGO comments report a final pH of 7.2 +/- 0.2 at 25 C.

## Completeness

The five component rows are present, but the generated record has no `ph_value`, imports two volume measurements as mass concentrations, and carries the source Proteose peptone label with a trailing space.

## Findings

- `glycerol` is represented as `15 G_PER_L`, but TOGO specifies 15 ml in the 1 L recipe. This is a unit-preservation error for a liquid carbon source.
- `Distilled water` is represented as `1 G_PER_L`, but TOGO specifies 1 L solvent.
- The source pH, 7.2 +/- 0.2 at 25 C, is absent.
- `Proteose peptone ` keeps a trailing space in `preferred_term`, which should be normalized to `Proteose peptone`.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/kings_b_medium.yaml`, then regenerate the merged record.
- Preserve glycerol as a 15 ml liquid addition or convert it deliberately with density-aware evidence instead of treating ml as g.
- Represent distilled water as the 1 L solvent.
- Add the source final pH range if the schema can represent it; otherwise carry the exact `7.2 +/- 0.2 at 25 C` statement in notes.
- Trim the Proteose peptone label.

## Follow-up Checks

- Re-run the open schema, strict, reference, and term validators against regenerated King's B.
- Re-run the exact `TOGO:M2195` search, including ignored and hidden files, if deduplicating this record.

## Additional Notes

The exact search included ignored files and hidden files.
