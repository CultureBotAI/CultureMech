# YAML Record Review: MRS FRUCTOSE MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml
- Started UTC: 2026-09-24T14:40:59Z
- Finished UTC: 2026-09-24T14:42:35Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:003199 |
| Name | mrs_fructose_medium |
| Original name | MRS FRUCTOSE MEDIUM |
| Category | bacterial |
| Media term | mediadive.medium:J853 |
| Source | MediaDive import of JCM Medium 853 |
| Reviewed path | data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml |
| Maintained source | data/normalized_yaml/bacterial/mrs_fructose_medium.yaml |
| Generated status | Generated by merge_recipes.py on 2026-08-06; stale relative to the maintained 2026-09-10 JCM repair |

The reviewed file is a generated single-source output for JCM medium 853. It is
distinct from DSMZ/MediaDive numeric medium 853; MediaDive's provider-local JCM
identifier `J853` resolves to the JCM `MRS FRUCTOSE MEDIUM` page.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml` exited 0 and printed `No issues found`. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml --out /private/tmp/mrs_fructose_medium__3d9e8ee8.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The generated record is structurally valid but incomplete.

## Identity and Grounding

- The reviewed record has `media_term.id: mediadive.medium:J853` and
  `media_term.label: MRS FRUCTOSE MEDIUM`; the live JCM GRMD 853 page has the
  same formula and title.
- JCM GRMD 853 lists only Lactobacilli MRS broth from BD-Difco, fructose, and
  distilled water. The generated record preserves the first two rows and omits
  water.
- The generated fructose grounding to CHEBI:28757 is consistent with the
  generic JCM ingredient label `Fructose`.
- `Lactobacilli MRS broth (BD-Difco)` is an opaque commercial component;
  leaving it ungrounded is consistent with the maintained record's
  `has_unmapped_ingredients` flag.
- TOGO M889 is a TOGO copy of the same JCM_M853 source page and remains as a
  separate generated `MRS_FRUCTOSE_MEDIUM.yaml` record instead of being merged
  with this JCM/MediaDive output.

## Evidence

JCM GRMD 853 supports:

| Ingredient | Source amount | Generated YAML | Maintained YAML |
| --- | --- | --- | --- |
| Lactobacilli MRS broth, BD-Difco | 55.0 g | 55 G_PER_L | 55.0 G_PER_L |
| Fructose | 10.0 g | 10 G_PER_L | 10.0 G_PER_L |
| Distilled water | 1.0 L | missing | 1000 ML_PER_L |

JCM states that media should be autoclaved at 121 C for 15 min unless otherwise
specified. The maintained YAML preserves that default as an AUTOCLAVE step and
`sterilization.method: AUTOCLAVE`; the generated YAML predates those fields.

## Completeness

- The generated record omits the source distilled-water row.
- The generated record omits the maintained JCM default autoclave step and
  `sterilization` block.
- The generated record omits maintained source-scoped ingredient notes, the
  `ingredients_curated`, `has_ontology_mappings`, and
  `has_unmapped_ingredients` quality flags, and the JCM reference.
- A direct duplicate exists at
  `data/merge_yaml/merged/MRS_FRUCTOSE_MEDIUM.yaml`: TOGO M889 also imports JCM
  Medium 853 and carries the same Lactobacilli MRS broth, fructose, and water
  formula.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `mediadive.medium:J853`, `JCM_M853`, `GRMD=853`,
  `CultureMech:003199`, and `mrs_fructose_medium` found the maintained
  JCM/MediaDive source, the JCM 853 solution artifact, this generated record,
  and the direct TOGO M889 copy of the same JCM source.
- A `find` search, which included ignored files, found no normalized file named
  `mrs_fructose_medium__3d9e8ee8.yaml`; the maintained source filename is
  `mrs_fructose_medium.yaml`.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The generated record is missing the JCM water row. | JCM GRMD 853 lists 1.0 L distilled water, and the 2026-09-10 maintained repair stores `1000 ML_PER_L`; the generated YAML has no water ingredient. | Regenerate `data/merge_yaml/merged/mrs_fructose_medium__3d9e8ee8.yaml` from `data/normalized_yaml/bacterial/mrs_fructose_medium.yaml` |
| major | The generated record lost JCM preparation and curation metadata. | The maintained YAML has an AUTOCLAVE step, `sterilization`, source-scoped ingredient notes, curated quality flags, and a JCM reference; the generated YAML predates those fields. | Regeneration from `data/normalized_yaml/bacterial/mrs_fructose_medium.yaml` |
| major | The same JCM 853 formula remains as two generated records. | `mrs_fructose_medium__3d9e8ee8.yaml` imports JCM J853 via MediaDive, while `MRS_FRUCTOSE_MEDIUM.yaml` imports TOGO M889 whose original source is JCM_M853; both denote the same three-row JCM formula. | Cross-provider duplicate detection for JCM-backed TOGO and MediaDive/JCM imports |

## Recommended Edits

1. Regenerate the JCM/MediaDive MRS Fructose Medium output from
   `data/normalized_yaml/bacterial/mrs_fructose_medium.yaml`.
2. Confirm the regenerated record includes `Distilled water` at
   `1000 ML_PER_L`, the AUTOCLAVE step, `sterilization.method: AUTOCLAVE`,
   curated quality flags, source-scoped ingredient notes, and the JCM
   reference.
3. Add or repair cross-provider duplicate handling so JCM J853 and TOGO M889
   merge into one generated record rather than remaining as
   `mrs_fructose_medium__3d9e8ee8.yaml` and `MRS_FRUCTOSE_MEDIUM.yaml`.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated JCM/MediaDive output.
- Search with `rg --no-ignore --hidden` for `mediadive.medium:J853`,
  `TOGO:M889`, and `JCM_M853` across `data/normalized_yaml` and
  `data/merge_yaml` to confirm the JCM and TOGO copies are either deliberately
  linked or merged.
- Manually compare the regenerated record with JCM GRMD 853.

## Additional Notes

MediaDive `/rest/medium/853` resolves DSMZ medium 853, `FUSIBACTER MEDIUM`, not
JCM provider-local medium J853; it was inspected and discarded as non-evidence
for this JCM record.
