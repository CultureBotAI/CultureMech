# YAML Record Review: nha_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/nha_medium__b6eea1a7.yaml
- Started UTC: 2026-09-24T17:20:13Z
- Finished UTC: 2026-09-24T17:20:52Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/nha_medium__b6eea1a7.yaml` as a generated `MediaRecipe` for direct JCM/MediaDive J793, `CultureMech:003137`, label `nha_medium`, category `bacterial`, and physical state `LIQUID`.

The generated record was merged from `data/normalized_yaml/bacterial/nha_medium.yaml`. It is derived; the direct owner should be reconciled with the TOGO M825 liquid owner and TOGO M826 agar variant.

## Validation

| Check | Result |
|---|---|
| Open schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/nha_medium__b6eea1a7.yaml` | Passed; `No issues found`. |
| Strict schema, `python scripts/validate_strict.py data/merge_yaml/merged/nha_medium__b6eea1a7.yaml --out /private/tmp/nha_medium__b6eea1a7.strict.tsv --workers 1 --quiet` | Passed; TSV had the header only, 1 line and 0 errors. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/nha_medium__b6eea1a7.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were available. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/nha_medium__b6eea1a7.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries inside a single merged YAML file. |

## Identity and Grounding

- The target denotes direct JCM/MediaDive `J793`, NHA Medium.
- Live JCM GRMD 793 returned `Nothing found`, so the current JCM page was unavailable for direct comparison.
- MediaDive REST `J793` and TOGO M825 both support the same six-ingredient NHA liquid formulation, the pH 7.0-7.2 preparation text, and a note that 20 g/L agar can be added for solid medium.
- A gitignore-independent hidden-file search for `TOGO:M826`, `JCM_M793`, `GRMD=793`, `NHA Medium`, and `nha_medium` across `data/normalized_yaml` and `data/merge_yaml` found separate direct JCM, TOGO M825 liquid, and TOGO M826 agar owners plus their generated outputs.

## Evidence

MediaDive `J793` supports 240 g NaCl, 10 g MgSO4 x 7 H2O, 5 g KCl, 3 g trisodium citrate, 0.2 g CaCl2 x 2 H2O, and 5 g BD-Difco casamino acids for the main J793 solution. Its steps say to add components to distilled water, bring the volume to 1.0 L, adjust pH to 7.0-7.2, and add 20.0 g/L agar when preparing solid medium.

The generated direct record preserves those ingredients and the two preparation notes. The main unresolved problem is identity hygiene: TOGO M825 independently represents the same liquid formulation, and TOGO M826 represents the agar variant, but none of the three owners are linked or merged.

## Completeness

- Consequential duplicate gap: the same JCM 793 liquid formulation is active as both the direct JCM/MediaDive owner and TOGO M825.
- Consequential variant gap: TOGO M826 is the 20 g/L agar form of the same source family but is not linked to the direct JCM liquid record.
- Empty optional fields are not defects. The inspected MediaDive and TOGO records do not provide target organisms or incubation temperature.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The JCM 793 source family is split across three unlinked active records. | Exact hidden-file search found `data/normalized_yaml/bacterial/nha_medium.yaml` for direct `mediadive.medium:J793`, `TOGO_M825_NHA_Medium.yaml` for `JCM_M793`, and `TOGO_M826_NHA_Medium.yaml` for `JCM_M793-2`, with separate generated YAML for each. | Merge or link the direct J793 owner with TOGO M825 as one liquid recipe and model TOGO M826 as the agar variant. |

## Recommended Edits

1. Reconcile `data/normalized_yaml/bacterial/nha_medium.yaml` with `data/normalized_yaml/bacterial/TOGO_M825_NHA_Medium.yaml`; both represent the liquid JCM 793 formula.
2. Link `data/normalized_yaml/bacterial/TOGO_M826_NHA_Medium.yaml` as the 20 g/L agar variant of the liquid NHA medium.
3. Regenerate `data/merge_yaml/merged/` after source-family relationships are corrected.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the reconciled normalized records and regenerated merges.
- Re-run a gitignore-independent hidden-file search for `JCM_M793`, `JCM_M793-2`, `mediadive.medium:J793`, `TOGO:M825`, and `TOGO:M826` under `data/normalized_yaml` and `data/merge_yaml`.
- Manually compare the regenerated source family against MediaDive REST J793 and the TOGO M825/M826 payloads; retry live JCM GRMD 793 in case it becomes available again.

## Additional Notes

No source-side ingredient conflict was found between MediaDive J793 and TOGO M825. The generated direct record uses `ph_value: 7.1`, while its preparation step preserves the wider pH 7.0-7.2 text.
