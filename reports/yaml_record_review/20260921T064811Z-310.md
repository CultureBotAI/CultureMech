# YAML Record Review: 310

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/310.yaml`
- Started UTC: 2026-09-21T06:47:23Z
- Finished UTC: 2026-09-21T06:48:11Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007486` |
| Name | `310` |
| Original name | `310` |
| Category | `bacterial` |
| Source | NBRC Medium 309 |
| Source URL | `http://www.nbrc.nite.go.jp/NBRC2/NBRCMediumDetailServlet?NO=309` |
| Merge fingerprint | `23814f315bd38ae6efa8a49bfec663151188efe95a693e43f144e6ee5db1332f` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_310.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC 309 import. Future corrections generally belong in `data/normalized_yaml/bacterial/NBRC_310.yaml`; for this record, that owner already carries later 2026-08 and 2026-09 repairs, so the generated merge should be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/310.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/310.yaml --out /private/tmp/310.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/310.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/310.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated target is stale relative to the maintained owner. `data/normalized_yaml/bacterial/NBRC_310.yaml` keeps the same `CultureMech:007486` identity, renames the recipe to NBRC's official `Rhamnose Salt Medium`, adds `media_term.term.id: nbrc.medium:309`, adds `ph_value: 7.0`, and replaces the generated target's two malformed ingredient rows with ten NBRC Medium 309 rows.

The September 2026 repair grounds the defined chemical rows from the recovered NBRC formula. Potassium phosphate, ammonium chloride, magnesium sulfate heptahydrate, sodium chloride, calcium chloride dihydrate, iron(2+) sulfate heptahydrate, rhamnose, and water all have CHEBI groundings in the normalized owner; yeast extract is correctly left without a CHEBI `term` because it is a complex extract.

The old NBRC URL recorded in the import history now redirects to `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=309` with HTTP 200.

## Evidence

The live NBRC Medium 309 page identifies the medium as `Rhamnose Salt Medium` and contains a composition table with:

- 2.9 g K2HPO4.
- 2.1 g KH2PO4.
- 2 g NH4Cl.
- 0.4 g MgSO4.7H2O.
- 30 mg NaCl.
- 3 mg CaCl2.2H2O.
- 1 mg FeSO4.7H2O.
- 3 g yeast extract.
- 10 g Rhamnose.
- 1 L distilled water.
- pH 7.0.

The maintained owner matches those amounts and final pH. The generated target instead has one row whose preferred term is the whole concatenated NBRC formula plus a second row with an empty `preferred_term` and `concentration.value: K2HPO4`.

## Completeness

Consequential gaps:

- The generated target is missing the maintained owner's recovered ten-row composition.
- The generated target is missing the official NBRC title, NBRC source term, final pH, structured NBRC reference, and ontology-grounding flags.
- The generated target still carries a malformed row with an empty ingredient label and a nonnumeric `K2HPO4` concentration value.

Empty optional slots:

- `solutions` is correctly absent. NBRC 309 is a flat final-volume recipe in the live NBRC table.
- `target_organisms` and `growth_metrics` are correctly absent. The NBRC medium page supports the recipe composition and final pH, but it does not make a strain-specific growth claim.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-310.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, `data/raw`, and `scripts` for `CultureMech:007486`, `NBRC_310`, and `NBRC, Media No. 309` found the normalized owner, generated merge, indexes/manifests, archived validation rows, media variant proposals, and `scripts/repair_nbrc_310_score15.py`.
- A gitignore-independent `find` under `data/raw` for `*309*` found no cached NBRC 309 raw source file. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still carries the broken pre-recovery NBRC import. | The generated target has one collapsed full-formula ingredient and a second empty-label row whose value is `K2HPO4`; the maintained owner has already recovered NBRC Medium 309 as ten ingredient rows, added `ph_value: 7.0`, added `nbrc.medium:309`, added the redirected NBRC reference, grounded the chemical rows, and recorded `RESOLVED_NBRC_310_SCORE15`. | `data/normalized_yaml/bacterial/NBRC_310.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml` so `310.yaml` picks up the existing `NBRC_310.yaml` repairs.
2. Verify that the regenerated `data/merge_yaml/merged/310.yaml` has ten ingredient rows, `media_term.term.id: nbrc.medium:309`, `ph_value: 7.0`, and the structured NBRC reference.

## Follow-up Checks

- `just verify-merges` after regenerating generated records.
- `just audit-merge-freshness` after regenerating generated records.
- `just validate-schema data/merge_yaml/merged/310.yaml` after regeneration.
- `just validate-strict data/merge_yaml/merged/310.yaml` after regeneration.
- `just validate-references data/merge_yaml/merged/310.yaml` after regeneration.
- Manual comparison against NBRC Medium 309 for the ten component amounts and pH 7.0.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
- `scripts/repair_nbrc_310_score15.py` enforces the ten-row recovered ingredient signature before writing the NBRC source term, final pH, official title, groundings, and reference to the normalized owner.
