# YAML Record Review: rv5_medium__aac0ea47

- Repository: CultureMech
- Record: `data/merge_yaml/merged/rv5_medium__aac0ea47.yaml`
- Started UTC: `2026-09-25T03:20:10Z`
- Finished UTC: `2026-09-25T03:20:10Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `rv5_medium__aac0ea47`, a single-source merge of `data/normalized_yaml/bacterial/rv5_medium.yaml`.

The record represents DSMZ/MediaDive Medium 1147, `RV5 MEDIUM`. It was generated from the normalized MediaDive import and has no duplicate-source ambiguity, but its trace-elements stock was flattened into the parent medium.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to `mediadive.medium:1147` and to DSMZ Medium 1147. No false merge was found: the file is generated from one normalized source, `rv5_medium`.

## Evidence

The DSMZ Medium 1147 PDF and MediaDive REST record list the 1 L parent as 10 mg EDTA, 200 mg `MgSO4 x 7 H2O`, 75 mg `CaCl2 x 2 H2O`, 15 g `NaCl`, 0.52 g `NH4Cl`, 4 g DL-malic acid, 1 g `K2HPO4`, 10 mM BICINE buffer, 1 ml Trace elements, and 1000 ml distilled water.

The source then says to filter-sterilize and add 1 mg Thiamine, 20 ug Vitamin B12, and 30 ug Biotin after autoclaving. It adjusts the medium to pH 9.5 with NaOH, autoclaves it, adds 0.1% (w/v) `NaHCO3` from a 5% (w/v) filter-sterilized stock solution before inoculation, and uses medium RV5 plus 0.05% (w/v) Difco yeast extract for routine culture.

The Trace elements stock itself is a separate 1 L solution: 5.2 g EDTA, 190 mg `CoCl2 x 6 H2O`, 0.1 g `MnCl2 x 4 H2O`, 1.5 g `FeCl2 x 4 H2O`, 6 mg `H3BO3`, 17 mg `CuCl2 x 2 H2O`, 188 mg `Na2MoO4 x 2 H2O`, 25 mg `NiCl2 x 6 H2O`, 70 mg `ZnCl2`, 30 mg `VOSO4 x 2 H2O`, 2 mg `Na2WO4 x 2 H2O`, 2 mg `NaHSeO3`, and 1000 ml deionized water.

## Completeness

The generated record preserves pH 9.5, the main gram-per-liter salts and carbon source, the 10 mM BICINE conversion to 1.6317 g/L, and the post-autoclave vitamin amounts.

It loses or corrupts these details:

- The 1 ml/L Trace elements aliquot is missing.
- Every Trace elements stock component is flattened into the parent at full 1 L stock concentration.
- The 10 mg/L parent EDTA row and 5.2 g/L stock EDTA row are merged into one `5.21 G_PER_L` parent row.
- The 1000 ml parent distilled water and 1000 ml Trace elements deionized water rows are absent.
- The required 0.1% (w/v) `NaHCO3` supplement is only present in a preparation-step description.
- The routine 0.05% (w/v) Difco yeast extract supplement is only present in a preparation-step description.

## Findings

- `needs curation`: The Trace elements stock was flattened into the parent recipe. DSMZ calls for only 1 ml Trace elements per 1 L RV5, but the generated file places stock-strength `CoCl2 x 6 H2O`, `MnCl2 x 4 H2O`, `FeCl2 x 4 H2O`, `H3BO3`, `CuCl2 x 2 H2O`, `Na2MoO4 x 2 H2O`, `NiCl2 x 6 H2O`, `ZnCl2`, `VOSO4 x 2 H2O`, `Na2WO4 x 2 H2O`, and `NaHSeO3` directly in the parent.
- `needs curation`: EDTA from two different scopes was merged into one parent value. The source has 10 mg EDTA in the main solution and 5.2 g EDTA in the 1 L Trace elements stock; the generated file sums them as `5.21 G_PER_L`.
- `needs curation`: The parent 1 ml/L Trace elements aliquot is absent, so the generated record has no way to recover the intended 1000-fold dilution.
- `needs curation`: The source adds 0.1% (w/v) `NaHCO3` before inoculation from a 5% (w/v) filter-sterilized stock, but the generated record only keeps this as prose in `preparation_steps`.
- `needs curation`: The source says routine culture uses medium RV5 supplemented with 0.05% (w/v) Difco yeast extract, but the generated record does not model that supplement structurally.
- `pass with minor issues`: `NiCl2 x 6 H2O` is grounded to CHEBI:34887, `nickel dichloride`; that identifier is too broad for a hexahydrate-labeled row and should either use a hydrate-specific term if one exists or be left ungrounded.
- `pass with minor issues`: The water rows are not represented: both the parent distilled water and Trace elements deionized water are absent rather than scoped to their source solutions.
- `pass with minor issues`: The second preparation step is typed as `FILTER_STERILIZE`, even though the text describes pH adjustment, autoclaving, `NaHCO3` supplementation, and routine yeast-extract supplementation.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/rv5_medium__aac0ea47.yaml`; fix `data/normalized_yaml/bacterial/rv5_medium.yaml`, then rerun the merge.
- Restore Trace elements as a nested stock or structured solution with a 1 ml parent aliquot.
- Keep EDTA as separate 10 mg/L parent and 5.2 g/L Trace elements stock rows instead of merging them.
- Scope `CoCl2 x 6 H2O`, `MnCl2 x 4 H2O`, `FeCl2 x 4 H2O`, `H3BO3`, `CuCl2 x 2 H2O`, `Na2MoO4 x 2 H2O`, `NiCl2 x 6 H2O`, `ZnCl2`, `VOSO4 x 2 H2O`, `Na2WO4 x 2 H2O`, and `NaHSeO3` under Trace elements.
- Restore the main distilled water row and the Trace elements deionized water row in their source scopes.
- Model the 0.1% (w/v) `NaHCO3` addition as a supplement where the schema permits.
- Decide whether the routine 0.05% (w/v) Difco yeast extract condition is a variant or optional supplement, then model it explicitly rather than leaving it only in prose.
- Correct or remove the `NiCl2 x 6 H2O` grounding to anhydrous nickel dichloride.
- Reclassify the second preparation step with a non-filter action, or split it into distinct pH-adjustment, autoclaving, and supplementation steps.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/rv5_medium.yaml`.
- Regenerate `data/merge_yaml/merged/rv5_medium__aac0ea47.yaml` and verify that the trace-metal rows no longer appear as direct parent ingredients.
- Verify that EDTA exists separately as 10 mg/L in the parent and 5.2 g/L in the Trace elements stock.
- Verify that the regenerated parent includes 1 ml/L Trace elements and the 0.1% (w/v) `NaHCO3` supplement.
- Run an exact ignored-file-inclusive search for `rv5_medium` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The vitamin amounts are very small, but the generated `0.001`, `2e-05`, and `3e-05` g/L values match the DSMZ 1 mg Thiamine, 20 ug Vitamin B12, and 30 ug Biotin amounts.
