# YAML Record Review: kd_1_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/kd_1_medium.yaml
- Started UTC: 2026-09-23T17:38:47Z
- Finished UTC: 2026-09-23T17:40:10Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:007531`, the generated `kd_1_medium` record merged from `data/normalized_yaml/bacterial/TOGO_M1017_KD-1_Medium.yaml`. The record represents TOGO `M1017`, JCM `M967`, "KD-1 Medium", as a bacterial, complex, undefined, liquid medium.

## Validation

- LinkML open schema validation passed for `data/merge_yaml/merged/kd_1_medium.yaml`.
- Strict validation passed with 0 error rows in `/private/tmp/kd_1_medium.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` entries were not checked: the available history validator validates standalone files under `history/`, not embedded generated YAML history.

## Identity and Grounding

An ignored-inclusive exact search for `CultureMech:007531`, `TOGO:M1017`, `TOGO_M1017_KD-1_Medium`, `JCM_M967`, and `GRMD=967` found the active TOGO owner, this generated record, and a second normalized/generated JCM duplicate at `data/normalized_yaml/bacterial/kd_1_medium.yaml` and `data/merge_yaml/merged/kd_1_medium__10323f04.yaml`.

The two records are grounded to the same JCM 967 formula and should be reconciled. The TOGO-derived owner keeps the 10 ml trace solution references only as empty placeholder `solutions`; the direct JCM duplicate tries to flatten JCM 197 trace vitamins and JCM 741 trace elements into the final record and should not be merged mechanically because its stock ingredients are not all scaled into 10 ml additions.

## Evidence

JCM 967 lists, per 1 L distilled water, 10 g Bacto peptone, 2 g urea, 2.5 g K2HPO4, 3 g KH2PO4, 3 g NaCl, 0.2 g MgSO4.7H2O, 10 ml trace vitamins from JCM 197, 10 ml trace element solution from JCM 741, and 1 mg resazurin. The JCM preparation text instructs curators to mix the components, adjust pH to 8.0, distribute under a N2-CO2 9:1 gas mixture, seal with butyl rubber stoppers, and autoclave. The TOGO M1017 JSON mirrors the same component values, reports pH 8.0, and preserves the same preparation sentence.

## Completeness

The major salts, Bacto peptone, urea, resazurin, and gases are present, but several key source semantics are missing or distorted:

- `ph_value: 8.0` is absent.
- The N2-CO2 9:1 atmosphere, butyl-rubber sealing, and autoclave step are absent from `preparation_steps`.
- The 10 ml trace vitamins and 10 ml trace element additions are no longer volume additions to referenced stock media; they are empty `Unknown solution` records with `10 G_PER_L`.
- `Distilled water` is represented as `1 G_PER_L` rather than a 1 L solvent volume.

## Findings

- `Resazurin` is 1000x too high. JCM and TOGO both specify 1 mg in the 1 L recipe, but the generated record stores `1 G_PER_L`. `data/import_tracking/reports/concentration_plausibility.tsv` independently flags this owner as an `INDICATOR_UNIT_SLIP`.
- The two stock additions were migrated from ingredients to `solutions` without usable solution structure. Both records have `name: Unknown solution`, empty `composition`, and gram-per-liter concentrations even though the source lists 10 ml of cross-referenced trace vitamin and trace element media.
- The final pH and anaerobic distribution/autoclave instructions were lost during import. This is behaviorally material for KD-1 because the JCM formula explicitly sets pH 8.0 and the N2-CO2 9:1 headspace before sterilization.
- The duplicate JCM record `CultureMech:003315` should be deduplicated with this TOGO import. It is grounded to the same JCM 967 URL and name but currently appears as `kd_1_medium__10323f04`.
- The duplicate JCM record contains a different stock-flattening artifact: it expands trace vitamins and trace elements at stock strength, merges trace-stock NaCl and MgSO4 into the base-salt rows, and rescales the main formula as if the 10 ml plus 10 ml stock additions increase the final volume to 1.02 L.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/TOGO_M1017_KD-1_Medium.yaml`, then regenerate `data/merge_yaml/merged/kd_1_medium.yaml`.
- Convert resazurin to the source amount, equivalent to `0.001 G_PER_L` for a 1 mg per 1 L final recipe.
- Represent distilled water as the 1 L solvent rather than `1 G_PER_L`.
- Restore `ph_value: 8.0`.
- Restore a preparation step that captures pH adjustment, N2-CO2 9:1 distribution, butyl-rubber sealing, and autoclaving.
- Replace the two empty solution placeholders with structured or explicitly referenced 10 ml additions for the JCM trace vitamin and trace element media.
- Merge the TOGO and direct JCM owners once the direct JCM trace-stock expansion is corrected or suppressed.

## Follow-up Checks

- Re-run the open schema, strict, reference, and term validators against regenerated KD-1.
- Re-run concentration plausibility and confirm the `INDICATOR_UNIT_SLIP` row for `TOGO_M1017_KD-1_Medium.yaml` disappears.
- Confirm only one generated `kd_1_medium*.yaml` record remains after TOGO/JCM deduplication.
- Confirm the repaired recipe has exactly one base NaCl row and one base MgSO4.7H2O row unless trace-stock compositions are represented as nested stock records.

## Additional Notes

The exact search included ignored files and hidden files.
