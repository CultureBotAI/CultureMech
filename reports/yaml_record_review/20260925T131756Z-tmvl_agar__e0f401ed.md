# YAML Record Review: tmvl_agar__e0f401ed

- Repository: CultureMech
- Record: `data/merge_yaml/merged/tmvl_agar__e0f401ed.yaml`
- Started UTC: 2026-09-25T13:17:56Z
- Finished UTC: 2026-09-25T13:17:56Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for JCM medium 633, `TMVL AGAR`.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/tmvl_agar.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/` records, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to JCM medium 633 by `media_term.term.id: mediadive.medium:J633` and the JCM link in `notes`.

The JCM source lists parent components with 1 L distilled water, including 10 ml of 0.07% hemin solution and pH 7.0.

## Evidence

Most parent ingredients are present and were normalized over an apparent 1.01 L final volume: 20 g glucose became 19.802 g/L, 10 g Trypticase peptone became 9.90099 g/L, and 15 g agar became 14.8515 g/L.

The hemin row is not source-backed as a g/L ingredient. The source has 10 ml of a 0.07% hemin solution, not 10 g hemin.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated record does not preserve the hemin stock solution identity or the final distilled water volume from the JCM source.

## Findings

- Major issue: the `Hemin` concentration was imported from the 10 ml source volume and emitted as `10 G_PER_L`. JCM specifies 10 ml of 0.07% hemin solution, which is a stock addition.
- Minor issue: water is omitted from the generated record. This is common in existing merged YAML, but the normalized formulation should retain enough source context to explain the 1 L parent water plus hemin solution addition.
- Minor issue: the generated `preparation_steps` capture only pH adjustment and omit the source's default JCM autoclaving rule.

## Recommended Edits

- Model `0.07% Hemin solution, 10 ml` as a solution addition or convert it to a final hemin concentration with the stock percentage and final volume applied.
- Add or preserve the 1 L distilled water source row in the normalized source if final-volume normalization is expected.
- Include the JCM default autoclaving condition if it applies to medium 633.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Verify whether TMVL agar should be normalized over 1 L or 1.01 L final volume before changing glucose, agar, and other direct parent amounts.

## Additional Notes

Exact local source search found only the expected normalized JCM source for this merge. Ignored files were included for duplicate-report checks with `find`, and no pre-existing ignored report for this generated record was found.
