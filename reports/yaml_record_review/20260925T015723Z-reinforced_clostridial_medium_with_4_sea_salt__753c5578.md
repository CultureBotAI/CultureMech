# YAML Record Review: reinforced_clostridial_medium_with_4_sea_salt__753c5578

- Repository: CultureMech
- Record: `data/merge_yaml/merged/reinforced_clostridial_medium_with_4_sea_salt__753c5578.yaml`
- Started UTC: 2026-09-25T01:56:05Z
- Finished UTC: 2026-09-25T01:57:23Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:002832` for JCM Medium J482 / `mediadive.medium:J482`, generated by an August merge of the direct JCM 3% and 4% sea-salt MediaDive owners.

## Validation

- Open schema validation: passed with `No issues found`.
- Strict validation: passed; `scripts/validate_strict.py` reported 0 errors and wrote only the TSV header.
- Reference validation: passed; 0 checks.
- Term validation: passed with the known `eutils` / `pkg_resources` deprecation warning.
- Embedded history: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

Exact ignored-inclusive searches found `mediadive.medium:J481` on the direct JCM 3% owner, the generated stale synonym, and the manifest row, and found `mediadive.medium:J482` on the direct JCM 4% owner, this generated primary media term, and the manifest row. MediaDive J481 maps to JCM Medium 481 / 3% sea salt; MediaDive J482 maps to JCM Medium 482 / 4% sea salt.

## Evidence

MediaDive J481 and JCM 481 list 38 g Reinforced clostridial medium (BD-Difco), 30 g Sea Salt (Sigma), 15 g agar, and 1000 ml distilled water. MediaDive J482 and JCM 482 list the same base recipe except Sea Salt (Sigma) is 40 g. The maintained normalized owners have already been repaired as separate salinity variants with source notes, water, default JCM autoclaving, and the correct 30.0 g/L versus 40.0 g/L sea-salt rows.

## Completeness

The generated target is internally contradictory: it has the direct JCM 482 identity and salinity-variant metadata for the 4% child, but it still emits the 3% parent amount and synonym.

## Findings

- The generated target falsely merged JCM 481 and JCM 482 under one `merge_fingerprint`. These are not duplicate recipes; the source formula changes Sea Salt from 30 g/L to 40 g/L.
- The generated target uses `mediadive.medium:J482` and a 4% label but its `Sea Salt` row is 30 g/L from the 3% parent. The direct MediaDive J482 and JCM 482 sources both require 40 g/L.
- The generated target is stale relative to both September-repaired direct JCM normalized owners: it lacks the BD-Difco and Sigma qualifiers, distilled water, source-scoped owner notes, JCM default autoclaving, and direct JCM references.
- The generated target still carries `kg_microbe_match: mediadive.medium:12`, which points at DSMZ Soil Extract Medium rather than JCM Medium J482.

## Recommended Edits

- Regenerate `data/merge_yaml/merged` from the repaired direct JCM owners so J481 and J482 remain separate salinity variants.
- Ensure the regenerated J482 target carries 40.0 g/L Sea Salt (Sigma), 38.0 g/L Reinforced clostridial medium (BD-Difco), 15.0 g/L agar, and 1000 ml/L distilled water.
- Remove or correct the false `kg_microbe_match: mediadive.medium:12` before regeneration.

## Follow-up Checks

- Confirm direct JCM 481 and JCM 482 emit with distinct merge fingerprints.
- Confirm the JCM 482 target has 40 g/L sea salt and the JCM 481 target has 30 g/L sea salt.
- Re-run open schema, strict, reference, and term validation for both regenerated targets.

## Additional Notes

The normalized owners already contain the intended September salinity-variant repair; this generated file should disappear in favor of regenerated, source-faithful targets.
