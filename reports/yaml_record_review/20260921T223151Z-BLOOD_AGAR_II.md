# YAML Record Review: blood_agar_ii

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/BLOOD_AGAR_II.yaml`
- Started UTC: 2026-09-21T22:31:52Z
- Finished UTC: 2026-09-21T22:33:08Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Generated record ID | `CultureMech:004616` |
| Generated label | `blood_agar_ii` |
| Original name | `BLOOD AGAR II` |
| Canonical source term | `komodo.medium:245` / `BLOOD AGAR II` |
| Maintained owner | `data/normalized_yaml/bacterial/KOMODO_245_BLOOD_AGAR_II.yaml` |
| Other merge input | `data/normalized_yaml/bacterial/blood_agar_ii.yaml` |
| Generated status | Derived from two active `data/normalized_yaml/bacterial/*.yaml` inputs by `merge_recipes.py`; future fixes belong in normalized YAML or import/repair rules, followed by merge regeneration |

The ignored-file-inclusive accession search for full IDs and source accessions
`CultureMech:004616`, `CultureMech:001345`, `komodo.medium:245`, and
`mediadive.medium:245` found the two active normalized owners above plus
generated normalized-index and merged-record copies. It did not find another
maintained normalized YAML owner for the same stable IDs or source accessions.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open-schema LinkML | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BLOOD_AGAR_II.yaml` | Passed with no diagnostics |
| Strict closed-schema validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BLOOD_AGAR_II.yaml --out /private/tmp/BLOOD_AGAR_II.strict.tsv --workers 1 --quiet` | Passed, 1 file scanned, 0 error rows |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BLOOD_AGAR_II.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed, 0 checks, all validations passed |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BLOOD_AGAR_II.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils`/`pkg_resources` warning |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merged record; `just validate-history` targets standalone files under `history/` |

The documented `just validate-schema`, `just validate-strict`, and
`just validate-terms` entrypoints were not used directly because this checkout's
project `uv` environment currently tries to build `llvmlite==0.46.0` under
Python 3.13 and fails inside `setuptools` before a target-specific check runs.
The no-project Python 3.11 commands above exercise the same target validators.

## Identity and Grounding

- The source identity is correct for the two duplicate owners: KOMODO `245` is
  `BLOOD AGAR II`, DSMZ Medium 245 is `BLOOD AGAR II`, and the two normalized
  recipes represent a KOMODO import and its DSMZ source duplicate.
- The flattened casein peptone, soy peptone, NaCl, agar, and pH 7.3 values are
  supported only through the DSMZ 245 dependency on DSMZ Medium 220,
  `CASO AGAR (Merck 105458)`.
- The generated record is stale relative to the Sept 2026 normalized owner
  repairs: both normalized inputs ground `Casein peptone` and `Soy peptone` to
  FOODON terms, but the generated `BLOOD_AGAR_II.yaml` does not include those
  `term` nodes yet.
- `kg_microbe_match: mediadive.medium:101` is not aligned with this record's
  identity. Local `mediadive.medium:101` is DSMZ Medium 101,
  `NUTRIENT AGAR or BROTH WITH NaCl`, and an ignored-file-inclusive search for
  `kg_microbe_match: mediadive.medium:245` under `data/normalized_yaml`,
  `data/merge_yaml`, `src`, `scripts`, and `conf` found nothing.

## Evidence

Inspected sources:

| Source | Scope checked |
| --- | --- |
| `/private/tmp/DSMZ_Medium245.pdf` | DSMZ Medium 245 name and definition as Medium 220 with sheep blood |
| `/private/tmp/DSMZ_Medium220.pdf` | DSMZ Medium 220 base CASO Agar ingredients and pH |
| `/private/tmp/komodo-media-245.html` | KOMODO source ID `245`, name `BLOOD AGAR II`, DSMZ instructions link, pH/aerobic flags, and metabolite table |
| `data/normalized_yaml/bacterial/nutrient_agar_or_broth_with_nacl.yaml` | Local target of the incorrect `mediadive.medium:101` match |

Supported:

- The base and duplicate source identities are supported by DSMZ Medium 245 and
  KOMODO ID `245`.
- The base CASO Agar rows are indirectly supported: DSMZ 245 says the recipe is
  Medium 220 with `5% defibrinated sheep blood`, and DSMZ 220 lists 15 g
  casein peptone, 5 g soy peptone, 5 g NaCl, and 15 g agar per liter.
- `ph_value: 7.3` is supported as the pH adjustment for DSMZ Medium 220, the
  base recipe for Blood Agar II.
- The KOMODO page supports the same source accession and reports a metabolite
  table equivalent to CASO Agar plus defibrinated sheep blood.

Unsupported or over-scoped:

- The source phrase `5% defibrinated sheep blood` does not support the schema
  assertion `unit: PERCENT_W_V`; the record currently turns a liquid blood
  addition into a mass-per-volume percentage without evidence.
- The copied DSMZ 220 preparation sentence is present in the DSMZ owner as a
  Blood Agar II recipe step, but its second sentence, "Medium is identical with
  Tryptone Soya Agar", is an identity note about the base CASO Agar recipe, not
  a preparation action for the blood-supplemented medium.
- `kg_microbe_match: mediadive.medium:101` maps both maintained Blood Agar II
  owners to the wrong DSMZ medium.

## Completeness

- Consequentially incomplete: the flattened ingredient rows do not record that
  Casein peptone, Soy peptone, NaCl, agar, and pH 7.3 were expanded from DSMZ
  Medium 220 rather than listed directly in DSMZ Medium 245.
- Consequentially incomplete: the blood supplement amount needs a reviewed unit
  choice. `PERCENT_V_V` exists in the schema, but the future edit should verify
  the repository's DSMZ import convention for liquid supplement percentages
  before rewriting the unit.
- Acceptably empty: there are no target-organism or growth claims to add from
  the checked DSMZ/KOMODO recipe pages.
- Bounded search: ignored files were included when searching for the correct
  `kg_microbe_match: mediadive.medium:245` value, and no such value was found
  under `data/normalized_yaml`, `data/merge_yaml`, `src`, `scripts`, or `conf`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | Both maintained Blood Agar II owners have `kg_microbe_match: mediadive.medium:101`, which points at DSMZ Medium 101 rather than DSMZ Medium 245. The generated merge inherits that wrong cross-reference. | DSMZ/KOMODO 245 identities; local `data/normalized_yaml/bacterial/nutrient_agar_or_broth_with_nacl.yaml`; ignored-file-inclusive absence of `kg_microbe_match: mediadive.medium:245`. | `data/normalized_yaml/bacterial/blood_agar_ii.yaml`; `data/normalized_yaml/bacterial/KOMODO_245_BLOOD_AGAR_II.yaml` |
| major | The defibrinated sheep blood amount is over-specified as `5 PERCENT_W_V`. DSMZ states an unqualified 5 percent liquid supplement, and KOMODO derives a 50 g/L value, but neither inspected source establishes weight-per-volume blood. | DSMZ Medium 245 PDF; KOMODO `245` source page. | `data/normalized_yaml/bacterial/blood_agar_ii.yaml`; `data/normalized_yaml/bacterial/KOMODO_245_BLOOD_AGAR_II.yaml` |
| minor | DSMZ 245's dependency on DSMZ 220 is flattened without explicit local provenance, and the copied DSMZ 220 identity sentence is stored as part of a Blood Agar II preparation action. This is traceable but makes the source boundary unclear. | DSMZ Medium 245 and 220 PDFs; `data/normalized_yaml/bacterial/blood_agar_ii.yaml` preparation step 1. | `data/normalized_yaml/bacterial/blood_agar_ii.yaml`; `data/normalized_yaml/bacterial/KOMODO_245_BLOOD_AGAR_II.yaml` |
| minor | The generated merge is stale after Sept 2026 ingredient grounding in both normalized owners; the generated rows for `Casein peptone` and `Soy peptone` lack the FOODON term IDs already present upstream. | Direct comparison of `data/merge_yaml/merged/BLOOD_AGAR_II.yaml` against its two normalized inputs. | Regenerate `data/merge_yaml/merged/BLOOD_AGAR_II.yaml` after repairing normalized YAML; do not patch the generated merge directly. |

## Recommended Edits

1. Replace `kg_microbe_match: mediadive.medium:101` in both normalized Blood
   Agar II owners with the reviewed exact match for DSMZ Medium 245, or remove
   the slot until an exact KG-Microbe match is verified.
2. Review the `Defibrinated sheep blood` unit in both normalized owners and
   replace `PERCENT_W_V` with the source-faithful representation for a 5
   percent liquid supplement.
3. Make the DSMZ Medium 220 dependency explicit in the Blood Agar II records:
   preserve the Medium 220-derived base rows and pH, but distinguish the base
   recipe identity note from actual Blood Agar II preparation.
4. Rerun merge generation so the derived `BLOOD_AGAR_II.yaml` picks up the
   Sept 2026 FOODON ingredient groundings and any corrected `kg_microbe_match`
   or blood-unit edits.

## Follow-up Checks

- Run the focused schema, strict, term, and reference validators on both
  repaired normalized owners.
- Run the same validators on `data/merge_yaml/merged/BLOOD_AGAR_II.yaml` after
  merge regeneration.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  merge outputs.
- Re-open DSMZ Medium 245, DSMZ Medium 220, and KOMODO `245` to confirm the
  base Medium 220 expansion, sheep-blood unit, pH scope, and generated FOODON
  term propagation.

## Additional Notes

- `find reports/yaml_record_review -name '*BLOOD_AGAR_II.md'` returned no path
  before this report was created.
- This review intentionally wrote only this Markdown report. The generated
  merge record and both normalized owner YAML files were left unchanged.
