# YAML Record Review: bl_with_0_5_maltose

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml
- Started UTC: 2026-09-21T22:12:43Z
- Finished UTC: 2026-09-21T22:15:07Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:010526` |
| Name | `bl_with_0_5_maltose` |
| Original name | `BL WITH 0.5% MALTOSE` |
| Source identity | `mediadive.medium:J418`, JCM Medium 418 |
| Category | `fungal` |
| Physical state | `SOLID_AGAR` |
| Merge fingerprint | `d1461a3ec181ac7c37e9c2129cf4f34ec32517086dfa217ec666c6931c1a1080` |
| Merged from | `bl_with_0_5_maltose` |
| Maintained owner | `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml` |
| Generated status | Derived merge product; future fixes belong in `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml`, related duplicate-linkage inputs, or merge regeneration, not in this generated YAML. |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml` | Passed, `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml --out /private/tmp/bl_with_0_5_maltose__d1461a3e.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with exit 0 and no reference diagnostics. The record has no structured `references` to check. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after a non-fatal `eutils` `pkg_resources` warning. |
| Embedded curation history | Not checked: no focused embedded `MediaRecipe.curation_history` validator is documented for one generated merge record; `just validate-history` targets standalone files under `history/`. |

`just validate-schema`, `just validate-strict`, and `just validate-terms` were not used directly because the project `uv` environment currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation starts. The no-project Python 3.11 commands above are the same focused checks run without installing the project dependency set.

## Identity and Grounding

- `CultureMech:010526`, `name: bl_with_0_5_maltose`, `original_name: BL WITH 0.5% MALTOSE`, `media_term.term.id: mediadive.medium:J418`, `category: fungal`, `medium_type: COMPLEX`, and `physical_state: SOLID_AGAR` all match the direct JCM Medium 418 source identity.
- The live JCM Medium 418 source identifies the recipe as `BL WITH 0.5% MALTOSE` and lists exactly `1.0 L` BL Agar from Medium 13 plus `5.0 g` Maltose.
- The live JCM Medium 13 source identifies the parent as `BL AGAR (GLUCOSE BLOOD LIVER AGAR)`, which agrees with the repaired normalized `parent_media` link to `CultureMech:002499`.
- The generated record is stale. It still stores `BL Agar` as a top-level `1000 G_PER_L` ingredient grounded to `mediadive.compound:1757` / `BL Agar` and `CHEBI:2509` / `agar`; the authoritative normalized owner now stores `BL Agar` as a `1000 ML_PER_L` solution linked to `CultureMech:002499`.
- Exact ignored-file-inclusive searches over `data`, `.claude`, `src`, `scripts`, and `reports/yaml_record_review` for `CultureMech:010526`, exact `mediadive.medium:J418`, exact `GRMD=418`, and `bl_with_0_5_maltose` found the repaired direct JCM owner, the active repaired TOGO M416 owner, both stale generated BL With 0.5% Maltose merge files, the active registry/catalog rows, and the September repair scripts.

## Evidence

| Claim | Review |
|---|---|
| JCM Medium 418 identity | Supported by the live JCM `GRMD=418` page, which labels record 418 as `BL WITH 0.5% MALTOSE`. |
| JCM Medium 418 formulation | Not faithfully represented by the generated record. The live source says to use `1.0 L` of BL Agar from Medium 13 plus `5.0 g` Maltose; the generated record instead treats BL Agar as a raw `1000 G_PER_L` top-level ingredient. |
| Parent BL Agar identity | Supported by the live JCM `GRMD=13` page, which labels record 13 as `BL AGAR (GLUCOSE BLOOD LIVER AGAR)`. |
| `mediadive.medium:J418` provenance | Partially recoverable from the `media_term`, import history, and free-text `notes`, but the generated record has no structured `references` to the JCM 418 or JCM 13 source pages. The maintained normalized owner now has both. |
| Ingredient ontology | Stale in the generated record. The normalized owner now narrows the maltose row to `CHEBI:18167` / `alpha-maltose` and moves BL Agar out of the direct `ingredients` array into a `solutions` entry. |

## Completeness

- Consequentially missing fields relative to the repaired maintained owner: `solutions`, `preparation_steps`, `parent_media`, `variant_relationship`, `variant_modifications`, `data_quality_flags`, and structured `references`.
- The generated merge has no structured `references`, so the reference validator has no JCM URLs to dereference.
- The direct JCM source does not state target organisms, growth metrics, temperature, incubation atmosphere, storage conditions, or a pH override for JCM Medium 418 itself. Those empty optional slots are not defects in this generated wrapper.
- The exact ignored-file-inclusive search found the active TOGO M416 sibling in `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`; the direct JCM and TOGO wrappers are not merged with each other or linked in generated output.
- No existing `*-bl_with_0_5_maltose__d1461a3e.md` report was found under ignored `reports/yaml_record_review/` before this report was written.

## Findings

| Severity | Finding | Evidence | Maintained owner for a fix |
|---|---|---|---|
| Major | The generated merge is stale and materially misrepresents BL Agar as a raw mass ingredient. | The generated file stores BL Agar at `1000 G_PER_L` with an agar grounding. JCM 418 says the recipe uses `1.0 L` of the JCM 13 BL Agar medium, and `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml` now stores that row as a `1000 ML_PER_L` solution linked to `CultureMech:002499`. | Already corrected in `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml`; regenerate `data/merge_yaml/merged/`. |
| Major | Source-supported variant structure and references are missing from the generated merge. | The authoritative normalized record now has a parent-media link to `CultureMech:002499`, `variant_relationship: SUPPLEMENTED_VARIANT`, a preparation step, `variant_modifications`, `data_quality_flags`, and JCM 418/JCM 13 references. The generated merge has none of those fields. | Already corrected in `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml`; regenerate `data/merge_yaml/merged/`. |
| Major | The active TOGO M416 duplicate is neither merged nor linked with this generated direct JCM output. | The exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`, whose TOGO M416 wrapper points to the same JCM 418 formula. The generated direct JCM file contains only `merged_from: [bl_with_0_5_maltose]` and has no source-duplicate relationship to the TOGO owner. | Add explicit source-duplicate linkage between `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml` and `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`, or adjust merge generation to co-merge the TOGO M416 and direct JCM J418 wrappers. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `data/merge_yaml/merged/bl_with_0_5_maltose__d1461a3e.yaml` picks up the repaired `1000 ML_PER_L` BL Agar solution, `CultureMech:002499` parent link, `SUPPLEMENTED_VARIANT` relationship, preparation step, references, and quality flags.
2. Add explicit source-duplicate linkage between the direct JCM J418 owner and `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`, or make merge generation combine the TOGO M416 and direct JCM J418 wrappers for JCM Medium 418.

## Follow-up Checks

- Rerun `just verify-merges` after regenerating to prove the generated merge is fresh.
- Rerun focused schema, strict, reference, and term validation for the regenerated BL With 0.5% Maltose merge file.
- Rerun an ignored-file-inclusive exact search for `mediadive.medium:J418`, `TOGO:M416`, and `GRMD=418` to confirm the two active JCM 418 wrappers either intentionally stay separate with an explicit relationship or co-merge into one generated recipe.
- Inspect the regenerated YAML and rendered `pages/media` page to confirm BL Agar is rendered as a solution/parent medium and not as an agar ingredient at `1000 G_PER_L`.

## Additional Notes

- This was a read-only review. I did not edit normalized YAML, generated merge YAML, generated pages, GitHub issues, or PR state.
- This review intentionally judged the generated direct JCM sibling, not the TOGO M416 generated sibling reviewed in `reports/yaml_record_review/20260921T221141Z-bl_with_0_5_maltose.md`.
