# YAML Record Review: eg_jm

- Repository: CultureMech
- Record: data/merge_yaml/merged/eg_jm__2b49a2ab.yaml
- Started UTC: 2026-09-22T23:24:19Z
- Finished UTC: 2026-09-22T23:25:57Z
- Verdict: needs curation

## Target

Generated bacterial CCAP/MediaDive C27 record `CultureMech:000335`, named `eg_jm`.

## Validation

- Open LinkML validation against `MediaRecipe`: passed.
- Strict validation against `scripts/validate_strict.py`: passed with 0 error rows.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded `curation_history`: Not checked: the focused history validator targets standalone files under `history/`, not inline `MediaRecipe.curation_history` entries.

## Identity and Grounding

The generated C27 identity points to CCAP EG:JM. The category is `bacterial`, but the current CCAP PDF defines EG:JM for freshwater algae and protozoa, and an algae EG:JM generated record already exists.

Some stock components are grounded correctly, but sodium acetate trihydrate is grounded to anhydrous sodium acetate, `FeNa-EDTA` and ammonium molybdate tetrahydrate need more exact terms, and the source-spelling artifacts `Cyanocobalamine` and `Biotine` should be normalized.

## Evidence

MediaDive C27 encodes the target as 500 ml EG plus 500 ml JM. Its EG subrecipe includes a millilitre CaCl2 stock addition. Its JM subrecipe has nine separate 1 ml stock additions, and three of those point to nested 200 ml solutions for Fe/EDTA, borate/manganese/molybdate, and vitamins.

The current CCAP PDF is equivalent at the recipe-design level: EG:JM is a 1:1 mixture of separate EG and JM recipes; EG has a CaCl2 stock; JM is made with stocks 1 through 9 at 1 ml each per litre.

## Completeness

The generated record captures most leaf ingredient labels from both EG and JM, but every stock boundary and the final 1:1 mixture volume are lost. It is impossible to tell from the YAML which ingredients belong to EG, which belong to JM, which were stock ingredients, and which concentrations are component-medium-local rather than final EG:JM concentrations.

## Findings

- `CaCl2` is recorded as `10 G_PER_L` instead of a millilitre stock addition to EG.
- JM stock ingredients are promoted to gram-per-litre rows in the final medium even though each stock is dosed at 1 ml/L into JM before the final 1:1 EG:JM mixture.
- `Agar` is doubled to `30.0 G_PER_L` by summing two optional 15 g/L agar rows.
- Sodium acetate trihydrate is grounded to anhydrous sodium acetate.
- Optional agar and component-preparation instructions are repeated as flat final autoclave steps rather than represented under EG and JM subrecipes.
- The generated record is not reconciled with the algae EG:JM extraction from the same CCAP PDF.

## Recommended Edits

- Recurate EG:JM as 500 ml EG plus 500 ml JM, with EG, JM, and all JM stocks modeled explicitly.
- Convert CaCl2 and JM stock rows from stock attributes into millilitre stock additions or calculated final contributions.
- Replace the summed `30.0 G_PER_L` agar row with one optional 15 g/L agar variant if the solid formulation is retained.
- Normalize source labels and ground sodium acetate trihydrate, FeNa-EDTA, ammonium molybdate tetrahydrate, cyanocobalamin, and biotin correctly.
- Reconcile the bacterial and algae generated copies under one CCAP EG:JM canonical record.

## Follow-up Checks

- Re-run open, strict, reference, and term validators after rebuilding.
- Compare the rebuilt record against MediaDive C27 and both pages of the current CCAP `MR_EG_JM.pdf`.
- Search with ignored files included for stale EG:JM rows such as `Agar 30.0 G_PER_L`, `CaCl2 10 G_PER_L`, and `Cyanocobalamine 0.04 G_PER_L` after regeneration.

## Additional Notes

The generated record has no explicit organism growth data to review.
