# YAML Record Review: JCM MEDIUM No. 1046

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_1046.yaml
- Started UTC: 2026-09-23T16:26:26Z
- Finished UTC: 2026-09-23T16:27:20Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_1046.yaml`
- Stable ID: `CultureMech:002230`
- Name: `jcm_medium_no_1046`
- Original name: `JCM MEDIUM No. 1046`
- Category: `bacterial`
- Media term: `mediadive.medium:J1046`
- Source: MediaDive import of JCM Medium 1046, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1046`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_1046`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_1046.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_1046.yaml --out /private/tmp/jcm_medium_no_1046.strict.tsv --workers 1 --quiet` | Passed. The TSV had one line, the header only. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_1046.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_1046.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The focused validators establish YAML shape, reference resolvability for represented references, and ontology label correspondence. They do not check that stock-solution concentrations are scoped to stocks instead of final media.

## Identity and Grounding

The generated record identifies the MediaDive record `J1046` for JCM Medium 1046 and points to the JCM `GRMD=1046` source page. Those identifiers resolve to the same formulation: a basal solid medium with 1 ml of a 100 ml trace element solution.

The medium is a bacterial complex solid agar recipe, consistent with the 5 g yeast extract and 15 g agar rows in the JCM and MediaDive sources.

The ingredient identities for the basal dry rows are mostly consistent with the source strings and CHEBI groundings:

- `NaCl`, `MgSO4 x 7 H2O`, `CaCl2 x 2 H2O`, `NaHCO3`, `NH4Cl`, `K2HPO4`, `FeSO4 x 7 H2O`, `Nitrilotriacetic acid`, `Glucose`, and `Agar` match the JCM and MediaDive basal rows.
- `Yeast extract` keeps the 5 g amount but loses the JCM and MediaDive `BD-Difco` qualifier.
- `MnSO4 x n H2O`, `CoCl2 x 6 H2O`, `ZnSO4 x 7 H2O`, `CuSO4 x 5 H2O`, `AlK(SO4)2 x 12 H2O`, `H3BO3`, `Na2MoO4 x 2 H2O`, `Pyridoxine hydrochloride`, and `Thiamine HCl` are trace-element stock components, not direct final-medium rows.

## Evidence

Inspected evidence:

- MediaDive REST `medium/J1046` reports `JCM MEDIUM No. 1046`, source `JCM`, link `GRMD=1046`, and two solutions:
  - `Main sol. J1046`, volume 1001, with 80 g NaCl, 0.12 g MgSO4 x 7 H2O, 0.061 g CaCl2 x 2 H2O, 4.2 g NaHCO3, 0.85 g NH4Cl, 0.48 g K2HPO4, 0.021 g FeSO4 x 7 H2O, 0.015 g nitrilotriacetic acid, 5 g `Yeast extract` with attribute `BD-Difco`, 2 g glucose, 1 ml `Trace element solution`, 15 g agar, and 1000 ml distilled water.
  - `Trace element solution`, volume 100, with 500 mg MnSO4 x n H2O, 100 mg CoCl2 x 6 H2O, 100 mg ZnSO4 x 7 H2O, 10 mg CuSO4 x 5 H2O, 10 mg AlK(SO4)2 x 12 H2O, 10 mg H3BO3, 10 mg Na2MoO4 x 2 H2O, 10 mg pyridoxine hydrochloride, 5 mg thiamine HCl, and 100 ml distilled water.
- The live JCM `GRMD=1046` page lists the same basal rows, the same 1 ml trace element addition, 15 g agar, 1 L distilled water, and the same 100 ml trace element solution.
- TogoMedium M1111 points to the same original JCM medium, `JCM_M1046`, and source URL `GRMD=1046`.

Supported:

- The record label, MediaDive source ID, source link, complex/undefined/solid-agar classification, and basal dry amounts are supported.
- The generated basal `G_PER_L` concentrations are the MediaDive `g_l` values for the 1001 ml main solution. They are lower than the JCM table values by 1000/1001 because MediaDive treats the 1 ml trace solution as part of the main solution volume.
- The nested trace-stock ingredient concentrations that were still present in the normalized owner at `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml` were legitimate stock-strength values.

Unsupported or mis-scoped:

- The generated rows for trace-stock components assert 5, 1, 0.1, and 0.05 `G_PER_L` concentrations in the final medium. The source only supports those as `g_l` values in the 100 ml trace stock.
- The JCM and MediaDive sources include basal distilled water and the record under review omits it.
- The source supports `Yeast extract (BD-Difco)` rather than generic `Yeast extract`.

## Completeness

Consequential missing data:

- The 1000 ml basal distilled-water row is missing.
- The generated record has no `solutions` entry for the 1 ml/L trace element solution. The normalized owner already nests four stock-strength rows in `solutions`, but the generated merge re-flattened them and five stock components remain top-level in the normalized owner.
- The JCM source's default sterilization instruction, autoclaving at 121 C for 15 min unless otherwise stated, is not represented.

Empty slots that are acceptable:

- No pH row was found on the live JCM page, in the MediaDive `medium/J1046` response, or in the TogoMedium M1111 response.
- No target-organism or growth-metric claim is present, so there is no over-scoped growth evidence in this record.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=1046`, `JCM_M1046`, `jcm_medium_no_1046`, `togo_medium_m1111`, and the exact MediaDive/TOGO IDs found only the direct MediaDive/JCM record and the TOGO M1111 wrapper. No other normalized or merged copy of this JCM source was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated recipe conflates trace-stock concentrations with final-medium ingredient concentrations. The generated rows for MnSO4 x n H2O, CoCl2 x 6 H2O, ZnSO4 x 7 H2O, CuSO4 x 5 H2O, AlK(SO4)2 x 12 H2O, H3BO3, Na2MoO4 x 2 H2O, pyridoxine hydrochloride, and thiamine HCl are stock components, not direct final-medium rows. | JCM lists those nine compounds under `Trace element solution`; MediaDive puts them in solution `5107`, whose 100 ml stock is used at 1 ml in solution `5106`. | `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml` for the five still-flat trace components, and merge logic if `data/merge_yaml/merged` continues flattening normalized `solutions` entries. |
| Major | The record omits the source's 1 L distilled-water basal row. | JCM lists `Distilled water` at 1 L in the basal recipe, and MediaDive records 1000 ml `Distilled water` as recipe order 13 of `Main sol. J1046`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml`. |
| Major | The same JCM source is represented as two unmerged generated MediaRecipes. | The ignored-file-inclusive exact search found `data/normalized_yaml/bacterial/togo_medium_m1111.yaml` and `data/merge_yaml/merged/togo_medium_m1111.yaml` pointing to `JCM_M1046` and `GRMD=1046`. That sibling record is not merged with `jcm_medium_no_1046`. | Both normalized owners, plus `scripts/merge_recipes.py` after the two imports are corrected to the same formulation. |
| Minor | The yeast-extract supplier qualifier is lost. | JCM lists `Yeast extract (BD-Difco)` and MediaDive keeps `compound: "Yeast extract"` with `attribute: "BD-Difco"`, while the generated row is generic `Yeast extract`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml`. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/jcm_medium_no_1046.yaml` or the MediaDive cocktail importer, move the remaining stock-only CuSO4 x 5 H2O, AlK(SO4)2 x 12 H2O, H3BO3, Na2MoO4 x 2 H2O, and thiamine HCl rows into the `Trace element solution` composition, preserving their 100 ml stock concentrations.
2. Keep the already-nested MnSO4 x n H2O, CoCl2 x 6 H2O, ZnSO4 x 7 H2O, and pyridoxine hydrochloride rows inside the trace solution when regenerating `data/merge_yaml/merged`.
3. Add the basal `Distilled water`, 1000 ml, to the normalized MediaDive/JCM source or repair the importer so it preserves MediaDive order 13.
4. Preserve the `BD-Difco` qualifier on yeast extract, either as the preferred term or structured notes.
5. Add the source-supported default sterilization step from JCM as a preparation step, scoped as JCM's general default for this page.
6. Repair `data/normalized_yaml/bacterial/togo_medium_m1111.yaml`, which points to the same JCM source but currently scales `mg` stock rows as `G_PER_L` and merges 1 L plus 100 ml water into `101.0 G_PER_L`.
7. Regenerate merged recipes and verify that `jcm_medium_no_1046` and `togo_medium_m1111` either collapse to one canonical recipe or remain explicitly tied by provenance if the project intentionally preserves both source accessions.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Compare the regenerated JCM 1046 ingredient list against the live JCM page and MediaDive `medium/J1046`: the final medium should have one 1 ml/L trace-solution addition, not nine final-medium trace rows.
- Inspect the regenerated `data/merge_yaml/merged/jcm_medium_no_1046.yaml` to verify that nested `solutions` entries were not flattened during merging.
- Search with `rg --no-ignore --hidden` for `GRMD=1046` and `JCM_M1046` after the TogoMedium repair to confirm that the duplicate-source state is intentional and consistently represented.

## Additional Notes

- The TOGO M1111 sibling has a different, more severe unit bug: values like 500 mg and 100 mg in the 100 ml trace stock became 500 and 100 `G_PER_L`, and 1 L plus 100 ml distilled-water rows were merged as `101.0 G_PER_L`. Those errors are outside this target record but explain why the exact same JCM source did not merge with the direct MediaDive import.
- The generated direct MediaDive record's basal dry rows are internally explainable as MediaDive `g_l` values over a 1001 ml main solution; this report does not require changing those values to the raw JCM gram amounts.
