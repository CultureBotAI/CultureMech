# YAML Record Review: mrs_0_05_cysteine

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_0_05_cysteine.yaml
- Started UTC: 2026-09-24T14:25:50Z
- Finished UTC: 2026-09-24T14:26:59Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mrs_0_05_cysteine.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:008733`
- Label: `mrs_0_05_cysteine`
- Original label: `MRS + 0.05% Cysteine`
- Category: `bacterial`
- Media source: `TOGO:M2139`, original `NBRC_M1480`
- Generated status: generated merge output from one normalized source on fingerprint `6046db59a6f523dbf362b77ac6a0ba3063c0a3b793ff6e00b27524c0db544603`
- Maintained owner: `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_0_05_cysteine.yaml`
  - Result: passed; exited 0 with no diagnostics.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mrs_0_05_cysteine.yaml --out /private/tmp/mrs_0_05_cysteine.strict.tsv --workers 1 --quiet`
  - Result: passed; exited 0 after its startup line and wrote a TSV with only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mrs_0_05_cysteine.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 0 reference checks were evaluated.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mrs_0_05_cysteine.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record correctly denotes NBRC medium 1480 via TOGO `M2139`, `MRS + 0.05% Cysteine`.

An exact gitignore-independent search of `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:008733`, `TOGO:M2139`, `NBRC_M1480`, `NO=1480`, and the slug found only `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml` and this generated output. Ignored files were included.

Hydrate-aware groundings are correct for `MgSO4 x 7H2O`; the remaining exact simple-chemical groundings match the source labels closely enough for this review. The complex ingredients `Yeast extract`, `Meat extract`, and `Peptone` are intentionally ungrounded.

## Evidence

TOGO `M2139` and the live NBRC medium 1480 page agree on all formula rows:

| Ingredient | Source amount | CultureMech amount | Status |
| --- | ---: | ---: | --- |
| `Peptone` | 10 g | 10 g/L | supported |
| `Meat extract` | 10 g | 10 g/L | supported |
| `Yeast extract` | 5 g | 5 g/L | supported |
| `Glucose` | 20 g | 20 g/L | supported |
| `Tween 80` | 1 g | 1 g/L | supported |
| `K2HPO4` | 2 g | 2 g/L | supported |
| `Sodium acetate` | 5 g | 5 g/L | supported |
| `Diammonium hydrogen citrate` | 2 g | 2 g/L | supported |
| `MgSO4 x 7H2O` | 0.2 g | 0.2 g/L | supported |
| `MnSO4 x nH2O` | 0.05 g | 0.05 g/L | supported |
| `Cysteine` | 0.5 g | 0.5 g/L | supported |
| `Distilled water` | 1 L | `1 G_PER_L` | wrong unit |
| `Agar (if needed)` | 15 g | 15 g/L | amount supported; optionality flattened |

Unsupported or unresolved:

- The pH 6.0-6.5 source comment is absent from the record.
- `physical_state: SOLID_AGAR` overstates an optional agar row; NBRC says `Agar (if needed)`, so the recipe can be prepared without agar.

## Completeness

The formula ingredients are complete, but pH and agar optionality are not.

Empty optional fields are not defects in this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The pH range is missing. | TOGO `M2139` reports `ph: 6.0 - 6.5`, and the NBRC 1480 composition table prints the same pH comment; the YAML has no `ph_value` or `ph_range`. | `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml` |
| Major | Optional agar was promoted to required solid-agar state. | The source row is `Agar (if needed)`, but the record has `physical_state: SOLID_AGAR` and a normal top-level `15 G_PER_L` agar row. | `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml` |
| Minor | The distilled-water row has the wrong unit. | NBRC and TOGO list `1 L`; CultureMech stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml` |
| Minor | The optional agar row is ungrounded. | TOGO identifies the row as agar and the record already flags its solidifying role, but no CHEBI or MediaIngredientMech term is present. | `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/mrs_0_05_cysteine.yaml`, add `ph_range` 6.0-6.5 from NBRC 1480 / TOGO `M2139`.
2. Represent `Agar (if needed)` as optional and avoid marking the base recipe as unconditionally `SOLID_AGAR`; if the schema cannot express optional solidification, add a note preserving the conditional source wording.
3. Convert `Distilled water` from `1 G_PER_L` to a liter solvent row or remove it if one-liter water is represented structurally elsewhere.
4. Ground `Agar (if needed)` to `CHEBI:2509` / agar if the optional row is retained.
5. Regenerate `data/merge_yaml/merged/mrs_0_05_cysteine.yaml` from the maintained normalized record.

## Follow-up Checks

- Re-run open schema validation on the regenerated merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm any new agar grounding resolves.
- Re-fetch TOGO `M2139` and NBRC `NO=1480` and verify all 13 source rows plus the pH 6.0-6.5 comment are represented.
- Repeat the exact ignored-inclusive search for `TOGO:M2139`, `NBRC_M1480`, and `NO=1480` to verify no duplicate NBRC 1480 recipe was introduced.

## Additional Notes

- The 0.05% cysteine label is consistent with 0.5 g cysteine per liter, so the source name and ingredient amount agree.
- `MnSO4 x nH2O` lacks a `mediaingredientmech_chebi_term` mirror even though the primary CHEBI term is present.
