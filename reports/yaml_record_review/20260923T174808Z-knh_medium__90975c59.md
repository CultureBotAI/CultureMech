# YAML Record Review: knh_medium__90975c59

- Repository: CultureMech
- Record: data/merge_yaml/merged/knh_medium__90975c59.yaml
- Started UTC: 2026-09-23T17:47:16Z
- Finished UTC: 2026-09-23T17:48:08Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:010343`, the generated `knh_medium__90975c59` record merged from `data/normalized_yaml/bacterial/TOGO_M922_KNH_Medium.yaml`. The record represents TOGO `M922`, JCM `M881`, "KNH Medium", as a bacterial, complex, undefined, liquid medium.

## Validation

- LinkML open schema validation passed for `data/merge_yaml/merged/knh_medium__90975c59.yaml`.
- Strict validation passed with 0 error rows in `/private/tmp/knh_medium__90975c59.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` entries were not checked: the available history validator validates standalone files under `history/`, not embedded generated YAML history.

## Identity and Grounding

An ignored-inclusive exact search for `CultureMech:010343`, `TOGO:M922`, `TOGO_M922_KNH_Medium`, `JCM_M881`, and `GRMD=881` found this TOGO owner plus a direct JCM duplicate at `data/normalized_yaml/bacterial/JCM_J881_KNH_MEDIUM.yaml` and `data/merge_yaml/merged/KNH_MEDIUM.yaml`.

The TOGO and direct JCM owners are grounded to the same historical JCM 881 formula and should be reconciled. The current JCM lookup for `GRMD=881` returned "Nothing found", so the available source evidence for this review is the TOGO M922 API payload, which preserves the original JCM URL and formula text already imported into both local owners.

## Evidence

TOGO M922 lists, per 1 L distilled water, base salts, 1 mg resazurin, 1 ml trace element solution referencing M235, 1 ml Se/W solution referencing M278, and 1 ml trace vitamins referencing M190. The TOGO comment instructs curators to mix everything except sodium thioglycolate and ascorbic acid, deoxygenate with N2-CO2 80:20, dispense into vials sparged with H2-CO2 80:20, cap with butyl rubber stoppers, autoclave, add filter-sterilized 1% sodium thioglycolate and ascorbic acid reducing-agent solution at 0.1 ml per 10 ml medium, inoculate with 5-10% fully grown culture, and fill with H2-CO2 80:20 at 1.5 atm.

## Completeness

The base salts, reducing agents, and gas labels are present. The generated record loses the source water semantics, source stock references, resazurin unit, and nearly all of the anaerobic preparation sequence.

## Findings

- `Resazurin` is 1000x too high. The TOGO source specifies 1 mg per 1 L, but the generated record stores `1 G_PER_L`; `data/import_tracking/reports/concentration_plausibility.tsv` independently flags this as an `INDICATOR_UNIT_SLIP`.
- The three 1 ml stock additions were migrated to empty `Unknown solution` records with `1 G_PER_L` concentrations. They should remain 1 ml volume additions to trace element, Se/W, and trace vitamin stocks.
- `Distilled water` is represented as `1 G_PER_L` rather than as the 1 L solvent.
- No preparation step captures the two defined gas atmospheres, autoclaving before reducing-agent addition, filter sterilization of the reducing agents, 5-10% inoculum, or final 1.5 atm H2-CO2 fill.
- Carbon dioxide, nitrogen, and hydrogen are present only as independent `VARIABLE` gas ingredients, which loses the N2-CO2 80:20 and H2-CO2 80:20 mixture semantics.
- The direct JCM duplicate should be merged only after repair. Its stock solution ingredients are currently flattened into the final medium at stock strength and some stock-only pH steps are promoted to final KNH preparation.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/TOGO_M922_KNH_Medium.yaml`, then regenerate this merged record.
- Convert resazurin to the source amount, equivalent to `0.001 G_PER_L` for 1 mg in the 1 L recipe.
- Represent the three cross-referenced trace stocks as structured or explicitly linked 1 ml additions, not empty gram-per-liter `Unknown solution` rows.
- Restore the source anaerobic preparation text with N2-CO2 80:20, H2-CO2 80:20, butyl stoppers, autoclaving, post-autoclave filter-sterilized reducing agents, inoculum, and the final 1.5 atm gas fill.
- Reconcile the TOGO and direct JCM owners once both preserve stock boundaries.

## Follow-up Checks

- Re-run the open schema, strict, reference, and term validators against regenerated KNH.
- Re-run concentration plausibility and confirm the resazurin `INDICATOR_UNIT_SLIP` row for `TOGO_M922_KNH_Medium.yaml` disappears.
- Confirm only one generated KNH record remains for JCM 881 after deduplication.

## Additional Notes

The exact search included ignored files and hidden files.
