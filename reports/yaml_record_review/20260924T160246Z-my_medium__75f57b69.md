# YAML Record Review: my_medium__75f57b69

- Repository: CultureMech
- Record: data/merge_yaml/merged/my_medium__75f57b69.yaml
- Started UTC: 2026-09-24T16:00:23Z
- Finished UTC: 2026-09-24T16:02:46Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/my_medium__75f57b69.yaml`.

The generated record represents the TOGO M759 import of JCM Medium 735:

- canonical `id`: `CultureMech:010165`
- canonical `media_term`: `TOGO:M759`
- canonical `name`: `my_medium`
- canonical `original_name`: `MY Medium`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `SOLID_AGAR`
- `ingredients`: 5
- `merged_from`: `TOGO_M759_MY_Medium`

## Validation

- Open schema validation: Passed with `No issues found`.
- Strict schema validation: Passed with 0 errors; the TSV contained only the
  header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked; `just validate-history` validates
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The source identity is a real JCM 735 / MY Medium import, but the related JCM
735 records are split into multiple unlinked owners. An exact
ignored-file-inclusive search for `CultureMech:010165`, `TOGO:M759`,
`GRMD=735`, `mediadive.medium:J735`, `CultureMech:003078`,
`CultureMech:010167`, and `TOGO:M760` found three active normalized records
for the JCM 735 page:

- `CultureMech:010165`:
  `data/normalized_yaml/bacterial/TOGO_M759_MY_Medium.yaml`
- `CultureMech:003078`: `data/normalized_yaml/bacterial/JCM_J735_MY_MEDIUM.yaml`
- `CultureMech:010167`:
  `data/normalized_yaml/bacterial/TOGO_M760_MY_Medium.yaml`

TOGO M759 imports the base JCM 735 recipe. TOGO M760 imports the same base plus
the tryptophan addition for tryptophan-requiring strains. The direct JCM J735
owner is active but currently appears downstream only as a synonym of the
separate `yeast_extract_malt_extract_agar_ph_9_0` merge group, which should be
reviewed independently when that generated record sorts into scope.

## Evidence

The current JCM 735 page lists 10.0 g Malt extract, 4.0 g Yeast extract
`(BD-Difco)`, 4.0 g Glucose, 20.0 g Agar, and 1.0 L Distilled water. The page
also notes that tryptophan-requiring strains should receive 100 mg/L
tryptophan.

The current TOGO M759 API points to `JCM_M735` and carries the five base JCM
components. The current TOGO M760 API points to `JCM_M735-2` and carries the
same five components plus 100 mg/L tryptophan.

## Completeness

The generated record preserves the four dry base components, but `Distilled
water` is incorrect. TOGO and JCM both specify 1 L water in the base recipe;
the generated record represents that as 1 g/L.

The generated record has no preparation step. Unless otherwise stated, JCM
instructs sterilization by autoclaving at 121 C for 15 min.

## Findings

- `Distilled water` is represented as 1 g/L instead of 1 L.
- The TOGO M759 base owner is not linked to the direct JCM J735 owner for the
  same base recipe.
- The TOGO M760 tryptophan-containing variant is not linked to the JCM 735 base
  recipe.
- The direct JCM J735 owner is entangled with pH-adjusted yeast
  extract-malt extract agar records rather than the TOGO M759/M760 records.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/TOGO_M759_MY_Medium.yaml` so the
  water is modeled as 1000 ml/L or 1 L/L rather than 1 g/L.
- Link TOGO M759 to `data/normalized_yaml/bacterial/JCM_J735_MY_MEDIUM.yaml`
  as a `SOURCE_DUPLICATE` of the base JCM recipe.
- Link `data/normalized_yaml/bacterial/TOGO_M760_MY_Medium.yaml` as a
  tryptophan-added variant of the JCM 735 base.
- Preserve JCM's default 121 C for 15 min autoclave instruction if no
  source-specific sterilization overrides it.
- Revisit the `JCM_J735_MY_MEDIUM` membership in the
  `yeast_extract_malt_extract_agar_ph_9_0` merge group when that generated
  record is reviewed.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the
  regenerated MY Medium YAML.
- Confirm the regenerated base MY Medium has distilled water as a volume, not
  1 g/L.
- Confirm the regenerated tryptophan variant retains 100 mg/L tryptophan as the
  only intentional delta from base JCM 735.
- Confirm an ignored-file-inclusive exact search for `TOGO:M759`,
  `TOGO:M760`, and `mediadive.medium:J735` shows the three owners linked into
  one base-plus-variant family.

## Additional Notes

TOGO M760 generated as `my_medium__9c0144c9.yaml`, immediately adjacent to this
base TOGO M759 record in sorted order.
