# YAML Record Review: ml_15_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ml_15_medium.yaml
- Started UTC: 2026-09-24T08:05:37Z
- Finished UTC: 2026-09-24T08:05:37Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/ml_15_medium.yaml`
- Class: `MediaRecipe`
- Record ID: `CultureMech:007628`
- Label: `ml_15_medium`
- Original label: `ML-15 Medium`
- Source grounding: `TOGO:M1109`, labelled `ML-15 Medium`
- Category: `bacterial`
- Generated status: generated merged record with `merge_fingerprint: 3caadce36cc2eb2f67418fb1bb3e860ce5fa2d95ae326bd27d8f93e17d475658`
- Maintained owner for future edits: `data/normalized_yaml/bacterial/TOGO_M1109_ML-15_Medium.yaml`

## Validation

- Open schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ml_15_medium.yaml`
  - Result: passed with `No issues found`.
- Strict schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ml_15_medium.yaml --out /private/tmp/ml_15_medium.strict.tsv --workers 1 --quiet`
  - Result: passed; 1 file scanned, 0 files with errors, 0 total error rows. `/private/tmp/ml_15_medium.strict.tsv` has 1 line, the header only.
- LinkML reference validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ml_15_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 1 file validated, 0 reference checks, all validations passed.
- Term validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ml_15_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed.
- Embedded history validation:
  - Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in generated merged YAML.

## Identity and Grounding

- The generated record identity matches TOGO `M1109`: the inspected TOGO API names `gm: http://togomedium.org/medium/M1109`, `name: ML-15 Medium`, `original_media_id: JCM_M1044`, and the original JCM medium 1044 URL.
- The record preserves the direct salts from TOGO `M1109`: 70 g NaCl, 6.8 g NaHCO3, 1 g ammonium sulfate, 32 g Na2CO3, 3.8 g Na2B4O7 x 10 H2O, and 1 L distilled water.
- TOGO `M1109` represents `10% Yeast extract solution` as a second subcomponent with `volume: 20`, `unit: ml`, `conc_value: 10`, and `conc_unit: %`. The generated record migrates that row to `solutions` but leaves it as an empty `Unknown solution` with a `20 G_PER_L` concentration.
- An exact gitignore-independent search with `rg --no-ignore --hidden` over `data/normalized_yaml` and this generated target found the maintained TOGO source at `data/normalized_yaml/bacterial/TOGO_M1109_ML-15_Medium.yaml`; the same label also occurs in `data/normalized_yaml/bacterial/ml_15_medium.yaml`.

## Evidence

- The source identity, original JCM accession, ML-15 label, main formula, 20 ml yeast-extract subcomponent, and preparation comment are supported by the inspected TOGO `M1109` API payload.
- The preserved JCM medium 1044 URL was inspected and currently returns a `Nothing found` page, so it did not provide recoverable corroborating formula text.
- The source says the 10% Yeast extract solution is a 20 ml subcomponent. The generated solution row has `composition: []`, `name: Unknown solution`, and `20 G_PER_L`, so it loses the 10% concentration, the milliliter unit, and the yeast-extract identity except for the display label.
- TOGO `M1109` has a preparation comment that says to mix thoroughly and autoclave, then add the autoclaved solution after cooling. An exact `rg --no-ignore --hidden` search for `preparation_steps:`, `step_number`, `Adjust pH`, `autoclave`, `filter`, and `sparg` across `data/merge_yaml/merged/ml_15_medium.yaml` and `data/normalized_yaml/bacterial/TOGO_M1109_ML-15_Medium.yaml` returned no matches, with ignored files included by the search.

## Completeness

- The record is complete for the direct formula rows exposed in TOGO M1109.
- It is materially incomplete for the 10% Yeast extract solution because the stock is an empty solution stub with the wrong amount unit.
- It is materially incomplete for preparation because the source comment's autoclaving, cooling, and post-cooling stock-addition instruction is absent.
- Empty optional organism, pH, source-data, and discussion fields are not defects for this generated import. The inspected TOGO entry does not expose those claims for ML-15 Medium.

## Findings

1. **major - the 20 ml 10% Yeast extract solution was migrated to an empty mass-concentration stub**
   - Evidence: TOGO `M1109` represents `10% Yeast extract solution` as `volume: 20`, `unit: ml`, `conc_value: 10`, and `conc_unit: %`. The generated `solutions` entry has no composition, is named `Unknown solution`, and says `20 G_PER_L`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M1109_ML-15_Medium.yaml` and the solution migration that moved the source item into `solutions`.
2. **major - source preparation instructions are missing**
   - Evidence: TOGO `M1109` preserves the preparation comment to mix thoroughly, autoclave, cool, and add the autoclaved yeast-extract solution. The generated record and normalized owner both lack `preparation_steps`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M1109_ML-15_Medium.yaml`, via the TOGO importer or a source-specific overlay.

## Recommended Edits

1. Replace the empty `10% Yeast extract solution` stub with a structured 20 ml stock addition that keeps the 10% concentration and yeast-extract identity.
2. Add the TOGO M1109 preparation comment as ordered preparation steps: mix thoroughly, autoclave, cool, autoclave the yeast-extract solution, and add it after cooling.
3. Regenerate `data/merge_yaml/merged/ml_15_medium.yaml` after the normalized source and any importer fixes land; do not patch the generated merged file directly.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the regenerated `data/merge_yaml/merged/ml_15_medium.yaml`.
- Inspect the regenerated YAML for a non-empty 20 ml, 10% yeast-extract stock solution.
- Re-run exact ignored-inclusive `rg --no-ignore --hidden` checks for `preparation_steps`, `10% Yeast extract solution`, and `TOGO:M1109` against the maintained normalized owner and regenerated merge output.

## Additional Notes

- JCM medium 1044 no longer served the ML-15 formula during this review; the bounded source check used the TOGO `M1109` API as the recoverable source text for the imported record.
