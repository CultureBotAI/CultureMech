# YAML Record Review: mp_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mp_agar__3414ec19.yaml
- Started UTC: 2026-09-24T14:12:03Z
- Finished UTC: 2026-09-24T14:13:35Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mp_agar__3414ec19.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:003182`
- Label: `mp_agar`
- Original label: `MP AGAR`
- Category: `bacterial`
- Media source: MediaDive/JCM `J838`
- Generated status: generated merge output from one normalized source on fingerprint `3414ec19c475c05dec9d410c9ac361286f38fa66de07d7bddbc7ebe2b26bd89a`
- Maintained owner: `data/normalized_yaml/bacterial/mp_agar.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mp_agar__3414ec19.yaml`
  - Result: passed; exited 0 with no diagnostics.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mp_agar__3414ec19.yaml --out /private/tmp/mp_agar__3414ec19.strict.tsv --workers 1 --quiet`
  - Result: passed with 0 errors; `/private/tmp/mp_agar__3414ec19.strict.tsv` contained only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mp_agar__3414ec19.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 0 reference checks were evaluated.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mp_agar__3414ec19.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record correctly denotes JCM medium 838, `MP AGAR`.

The direct JCM GRMD 838 page, the live MediaDive `J838` JSON, and TOGO `M874` all resolve to the same medium identity:

| Source | ID | Label | pH |
| --- | --- | --- | --- |
| JCM | `838` | `MP AGAR` | `7.0` |
| MediaDive | `J838` | `MP AGAR` | `7.0` |
| TOGO | `M874`, original `JCM_M838` | `MP Agar` | `7.0` |

An exact gitignore-independent search of `data/normalized_yaml` and `data/merge_yaml/merged` found `GRMD=838` in this MediaDive-derived record and in `data/normalized_yaml/bacterial/TOGO_M874_MP_Agar.yaml` plus its generated `data/merge_yaml/merged/MP_AGAR.yaml` output. Ignored files were included. Those TOGO and MediaDive records are direct source duplicates and have not been merged.

The CHEBI groundings on melibiose, dipotassium hydrogen phosphate, magnesium sulfate heptahydrate, potassium chloride, and agar are narrow for the recorded ingredients. `Polypeptone` is intentionally ungrounded because it is undefined.

## Evidence

The reviewed MediaDive-derived record matches the direct JCM ingredient masses for the non-water rows:

| Ingredient | JCM GRMD 838 | MediaDive `J838` | CultureMech | Status |
| --- | ---: | ---: | ---: | --- |
| `Melibiose` | 10 g | 10 g/L | 10 g/L | supported |
| `Polypepton (Nihon Pharm. Co.)` | 10 g | 10 g/L | 10 g/L | amount supported; manufacturer attribute lost |
| `K2HPO4` | 0.5 g | 0.5 g/L | 0.5 g/L | supported |
| `MgSO4 x 7 H2O` | 0.1 g | 0.1 g/L | 0.1 g/L | supported |
| `KCl` | 0.1 g | 0.1 g/L | 0.1 g/L | supported |
| `Agar` | 15 g | 15 g/L | 15 g/L | supported |

The JCM page lists `Distilled water` as `1.0 L`, and the reviewed record captures the liter basis by expressing the other ingredient rows as grams per liter.

The JCM page also states that, unless otherwise stated, media should be sterilized by autoclaving at 121 C for 15 min. The reviewed record only captures the MP-specific pH adjustment and omits that default autoclave instruction.

## Completeness

The record is complete for ingredient masses but incomplete for preparation and de-duplication:

- The applicable JCM default autoclave instruction is missing.
- The direct TOGO import of the same JCM GRMD 838 medium remains as `MP_AGAR.yaml`.
- The MediaDive import reduced `Polypepton (Nihon Pharm. Co.)` to `Polypeptone`, dropping the manufacturer attribute retained by MediaDive and TOGO.

Empty optional fields are not defects in this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | A direct JCM duplicate is still present as a separate TOGO-derived generated record. | This record is MediaDive `J838`; `data/normalized_yaml/bacterial/TOGO_M874_MP_Agar.yaml` is TOGO `M874`, whose `original_media_id` is `JCM_M838` and whose `src_url` is the same GRMD 838 URL. | `data/normalized_yaml/bacterial/mp_agar.yaml`; `data/normalized_yaml/bacterial/TOGO_M874_MP_Agar.yaml`; merge de-duplication logic |
| Major | The JCM default autoclave instruction is omitted. | GRMD 838 inherits JCM's page-level instruction to autoclave at 121 C for 15 min unless otherwise stated; the record only stores `Adjust pH to 7.0.` | `data/normalized_yaml/bacterial/mp_agar.yaml` |
| Minor | The exact complex ingredient label lost the supplier attribute. | JCM uses `Polypepton (Nihon Pharm. Co.)`; MediaDive preserves `Nihon Pharm. Co.` as an attribute; the reviewed record stores only `Polypeptone`. | `data/normalized_yaml/bacterial/mp_agar.yaml` |
| Minor | The `media_term` CURIE uses a MediaDive-local identifier for a JCM source. | The direct provider is JCM GRMD 838; `mediadive.medium:J838` is a MediaDive ID for that JCM page rather than the JCM identifier itself. | `data/normalized_yaml/bacterial/mp_agar.yaml` |

## Recommended Edits

1. Add a cross-source duplicate mapping so MediaDive `J838` and TOGO `M874` / `JCM_M838` collapse to one maintained JCM GRMD 838 recipe.
2. Preserve the direct JCM GRMD 838 identity and either keep both MediaDive and TOGO as provenance for the canonical record or choose one source as the maintained owner and record the other as a duplicate.
3. In `data/normalized_yaml/bacterial/mp_agar.yaml`, add the inherited JCM autoclave instruction after pH adjustment.
4. Restore the exact `Polypepton (Nihon Pharm. Co.)` source detail, either in `preferred_term` or in a note on the `Polypeptone` row.
5. Regenerate `data/merge_yaml/merged/mp_agar__3414ec19.yaml` and remove the separate generated `MP_AGAR.yaml` duplicate through the normal merge pipeline.

## Follow-up Checks

- Re-run open schema validation on the regenerated canonical JCM GRMD 838 merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm every retained CHEBI and MediaIngredientMech grounding still resolves.
- Re-fetch JCM `GRMD=838`, MediaDive `J838`, and TOGO `M874`; verify that the canonical generated record has all six non-water formula rows, `pH 7.0`, the inherited autoclave step, and a recorded TOGO duplicate.
- Repeat the exact ignored-inclusive search for `GRMD=838`, `JCM_M838`, and `TOGO:M874` to verify the direct generated duplicate has been removed or explicitly linked.

## Additional Notes

- `MP_AGAR.yaml` has its own defects, including a water row imported as `1 G_PER_L` from TOGO's `1 L` amount. Those should be fixed while consolidating the duplicate, but they are owned by `data/normalized_yaml/bacterial/TOGO_M874_MP_Agar.yaml`, not by this reviewed MediaDive owner alone.
