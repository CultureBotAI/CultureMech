# YAML Record Review: togo_medium_m1606

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1606.yaml
- Started UTC: 2026-09-25T14:41:00Z
- Finished UTC: 2026-09-25T14:44:24Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008159
- Name: togo_medium_m1606
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1606.yaml`.
- Source identity in the generated record: TOGO:M1606, original source NBRC_M805, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=805`.
- Merge lineage in the generated record: only `togo_medium_m1606`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1606.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1606.yaml --out /private/tmp/m1606.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1606.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it reported zero checks.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1606.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1606 resolves to NBRC_M805. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- The grounded simple chemicals match the ingredient labels inspected in the NBRC and TOGO source records.

## Evidence

- TOGO M1606 and NBRC 805 support 1 g yeast extract, 5 g mannitol, 0.7 g K2HPO4, 0.1 g KH2PO4, 1 g MgSO4 x 7 H2O, 1 L distilled water, 15 g agar if needed, and pH 7.0-7.2.
- The generated record preserves the ingredient set and per-liter gram quantities other than the final water volume.
- The generated record encodes the 1 L distilled-water row as 1 `G_PER_L`, which is a source unit error.
- The source pH range is absent.

## Completeness

- Consequential missing or wrong fields: 1 L water as final volume, pH 7.0-7.2, and optional agar scope.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1606` found no existing report for this record stem.

## Findings

- major: `data/normalized_yaml/bacterial/togo_medium_m1606.yaml` stores the 1 L distilled-water source row as 1 g/L.
- minor: the normalized owner omits the NBRC_M805 pH 7.0-7.2 value and treats optional agar as an unconditional solid physical state.

## Recommended Edits

- Convert the distilled-water row to a final-volume representation matching 1 L.
- Add pH 7.0-7.2 from NBRC_M805.
- Preserve the conditional agar scope instead of treating the whole medium as unconditionally solid.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1606.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1606.yaml` and verify water, pH, and optional agar scope.

## Additional Notes

- Exact source inspection was bounded to TOGO M1606 and NBRC 805.
