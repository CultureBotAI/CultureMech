# YAML Record Review: CM3 MEDIUM

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cm3_medium__1c7ff4f6.yaml`
- Started UTC: `2026-09-22T10:03:17Z`
- Finished UTC: `2026-09-22T10:04:17Z`
- Verdict: pass with minor issues

## Target

Generated bacterial `MediaRecipe` record `CultureMech:002302` for MediaDive/JCM Medium `J112`, generated from `data/normalized_yaml/bacterial/cm3_medium.yaml` on merge fingerprint `1c7ff4f636f6aa2ce66964446d3e8ceab5e5f8afd071102d080e9a82f7b604c6`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/cm3_medium__1c7ff4f6.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The record is the direct JCM import for JCM Medium 112, `CM3 MEDIUM`.

Nine of ten ingredients are grounded to exact CHEBI terms. The remaining ungrounded row, `Cellobiose or Cellulose MN 300`, is ambiguous in the source because JCM lists cellobiose and cellulose MN 300 as an alternative carbon source on the same 10 g row.

The ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/import_tracking`, and `reports` found a separate TOGO `M104` branch for the same JCM `GRMD=112` URL. Ignored files were included.

## Evidence

The live JCM Medium 112 page supports the generated final formula: 4.4 g K2HPO4, 1.5 g urea, 0.4 g `(NH4)2SO4`, 0.5 g `MgSO4 x 7 H2O`, 0.05 g `CaCl2 x 2 H2O`, 1.0 mg `FeSO4 x 7 H2O`, 1.0 g `L-Cysteine HCl x H2O`, 6.0 g yeast extract, 20.0 g MOPS, 10.0 g `Cellobiose or Cellulose MN 300`, and 1.0 L distilled water.

The same JCM page says to adjust pH to 7.1; the generated record has `ph_value: 7.1` and an `ADJUST_PH` preparation step with that instruction.

## Completeness

The generated direct branch is complete for the JCM source formula and pH note.

The ambiguous carbon-source row is faithful to the source text, but the alternatives would be more computable as source variants or structured alternatives than as one ungrounded ingredient name.

No target organism or growth evidence is present.

## Findings

- Minor: `Cellobiose or Cellulose MN 300` is ungrounded and combines two alternative carbon sources in one ingredient.
- Minor: the TOGO `M104` branch for the same JCM source is unmerged because that import still has the JCM 1.0 mg FeSO4 row as `1 G_PER_L`.

## Recommended Edits

- Represent cellobiose and Cellulose MN 300 as grounded source alternatives, if the schema can express the source `or` relationship.
- Regenerate TOGO `M104` with `FeSO4 x 7 H2O` converted from 1 mg to `0.001 G_PER_L`, then merge it with the direct JCM branch.

## Follow-up Checks

- Validate the regenerated direct and TOGO branches with open schema, strict, reference, and term validators.
- Confirm FeSO4 remains at 0.001 g/L.
- Confirm pH 7.1 and the 10 g/L alternative cellobiose/cellulose row survive any merge.

## Additional Notes

No formula-level issues were found in this direct JCM branch. Empty optional fields are not defects.
