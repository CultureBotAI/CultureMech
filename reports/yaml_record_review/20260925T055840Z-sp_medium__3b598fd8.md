# YAML Record Review: sp_medium__3b598fd8

- Repository: CultureMech
- Record: data/merge_yaml/merged/sp_medium__3b598fd8.yaml
- Started UTC: 2026-09-25T05:56:45Z
- Finished UTC: 2026-09-25T05:58:39Z
- Verdict: needs curation

## Target

Generated merged YAML for TOGO M1636 / NBRC 839, SP Medium. The generated target is a one-source merge from `data/normalized_yaml/bacterial/TOGO_M1636_SP_Medium.yaml`.

## Validation

- LinkML open validation: Passed; `linkml-validate` reported no issues.
- Strict validation: Passed; `/private/tmp/sp_medium__3b598fd8.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks and reported no failures.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in merged YAML.

## Identity and Grounding

The source identity is correct: TOGO M1636 maps to NBRC medium 839, SP Medium. This is separate from DSMZ 222 SP - MEDIUM despite the shared normalized `sp_medium` name.

Groundings are mostly acceptable for the generated record. The MgSO4 x 7H2O row is grounded to magnesium sulfate heptahydrate, K2HPO4 is grounded to dipotassium hydrogen phosphate, and the sugar rows are grounded to CHEBI.

## Evidence

The TOGO M1636 payload lists 1 L distilled water, 0.5 g MgSO4 x 7H2O, 0.25 g K2HPO4, 1 g sucrose, 1 g galactose, 1 g raffinose, 5 g soluble starch, 15 g agar if needed, and 2.5 g Casitone. The NBRC 839 page lists the same composition and gives pH 7.4.

## Completeness

All non-water ingredient masses from the TOGO and NBRC sources are present. The generated record omits the pH 7.4 value and imports `Distilled water` as 1 g/L instead of 1 L/L.

## Findings

- Critical: `Distilled water` is modeled as `1` `G_PER_L`; both TOGO and NBRC specify 1 L of distilled water.
- Major: source pH 7.4 is missing from the generated record.
- Minor: `Agar (if needed)` is represented in a record whose `physical_state` is `SOLID_AGAR`; keep the optional nature explicit if a liquid agar-free derivative is ever split from the NBRC source.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/TOGO_M1636_SP_Medium.yaml` so `Distilled water` is represented as 1000 ml/L or another volume-equivalent water amount, not 1 g/L.
- Add pH 7.4 from NBRC 839.
- Regenerate `data/merge_yaml/merged/sp_medium__3b598fd8.yaml` from the repaired normalized record.

## Follow-up Checks

- Re-run open, strict, reference, and term validation on the regenerated merged record.
- Confirm `Distilled water` no longer appears as `1` `G_PER_L`.
- Confirm the regenerated record carries pH 7.4 and still keeps TOGO M1636 / NBRC 839 identity separate from DSMZ 222.

## Additional Notes

None found.
