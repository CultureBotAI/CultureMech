# YAML Record Review: asw_300_barley

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asw_300_barley.yaml
- Started UTC: 2026-09-21T16:05:59Z
- Finished UTC: 2026-09-21T16:06:13Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asw_300_barley.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000049` |
| Label | `asw_300_barley` |
| Category | `algae` |
| Original name | `ASW 300 + barley` |
| Source identity | `CCAP:ASW_300`; `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW_300.pdf` |
| Generated status | Generated by `recipe-merger` on 2026-08-06 from `data/normalized_yaml/algae/CCAP_ASW 300 + barley  + barley grain_).yaml` and `data/normalized_yaml/algae/asw_300_barley.yaml` |

`data/merge_yaml/merged/` is derived output. Future curation must update the
normalized algae owners or the merge process, then regenerate this merge; the
generated merge itself should not be hand-edited.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asw_300_barley.yaml` | Pass; `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/asw_300_barley.yaml --out /private/tmp/asw_300_barley.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/asw_300_barley.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/asw_300_barley.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass. The only emitted message was the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked | No focused validator for embedded `MediaRecipe.curation_history` is documented for one merged recipe. `just validate-history` targets standalone records under `history/`. |

Direct `just` validation was not used because this checkout currently reaches a
project `uv` build of `llvmlite==0.46.0` under Python 3.13 before target-specific
validation and fails in `setuptools` with `TypeError: Popen.__init__() got an
unexpected keyword argument 'dry_run'`.

## Identity and Grounding

- The generated record still denotes the algae CCAP `ASW_300` source and keeps
  the permanent `CultureMech:000049` ID assigned to
  `data/normalized_yaml/algae/CCAP_ASW 300 + barley  + barley grain_).yaml`.
- The current CCAP PDF at `MR_ASW_300.pdf`, refetched for this review, is a
  one-page recipe headed `ASW 300`, approved and reviewed on 2026-05-29. It
  supports the five-salt artificial-seawater recipe and the 1 L, pH, and
  autoclave instructions.
- The inspected 2026 CCAP PDF does not mention barley. The retained
  `original_name: ASW 300 + barley` and `asw_300_barley` slug therefore need a
  versioned-source check: either preserve the older CCAP or MediaDive C8 source
  that asserted barley, or scope the current `MR_ASW_300.pdf` evidence to the
  salt-only ASW 300 base.
- The five salts are grounded to the expected ChEBI concepts for sodium
  chloride, potassium chloride, magnesium dichloride hexahydrate, magnesium
  sulfate heptahydrate, and calcium chloride dihydrate.
- `MgSO .7H O` is a malformed ingredient label for the source formula
  `MgSO4.7H2O`; the ChEBI grounding itself is for magnesium sulfate
  heptahydrate.

## Evidence

Supported by the inspected current CCAP PDF:

| Claim | Review |
|---|---|
| `NaCl`, 243.2 g/L | Supported exactly by the `ASW 300` recipe. |
| `KCl`, 7.6 g/L | Supported exactly by the `ASW 300` recipe. |
| `MgCl2 x 6 H2O`, 54.36 g/L | Supported exactly; the source writes the hydrate formula with a dot. |
| `MgSO .7H O`, 59.4 g/L | Amount and heptahydrate identity are supported; the preferred label is mangled. |
| `CaCl2 x 2 H2O`, 1.3 g/L | Supported exactly; the source writes the hydrate formula with a dot. |

Unsupported, stale, or over-scoped in the generated merge:

- `Make up to 1 litre with deionised water. Adjust pH to 7.6-8 with` is
  preparation text, not a molar ingredient. Current normalized owner records
  have already moved that row into ordered `preparation_steps`, but this
  generated merge still carries it with `concentration.value: '1'` and
  `unit: MOLAR`.
- The generated merge lacks the current owner records' ordered preparation
  steps for final volume, pH adjustment with 1 M NaOH or 1 M HCl before
  autoclaving, and autoclaving at 15 psi for 15 minutes.
- The generic `light_intensity`, `light_cycle`, and `temperature_range` values
  are not supported by the inspected `ASW 300` PDF.
- The current PDF does not support the `+ barley` part of the identity, so
  barley should remain unresolved unless an older PDF revision or the bacterial
  MediaDive C8 source can be preserved as evidence.

## Completeness

- No stock `solutions` are expected for this source recipe; the CCAP PDF is a
  direct five-salt formulation.
- No `target_organisms`, `growth_metrics`, or organism-specific growth evidence
  are required for this source-formulation record. The CCAP recipe includes a
  bibliographic reference to Park et al. 2006, but the reviewed record does not
  make a narrow strain-growth claim that requires it.
- `find reports/yaml_record_review -maxdepth 1 -name '*asw_300_barley.md'`
  searched the ignored report directory independently of `.gitignore` and found
  no prior review report for this exact generated record stem before this report
  was written.
- An ignored-file-inclusive exact search for `CultureMech:000049`,
  `CCAP:ASW_300`, `MR_ASW_300`, `wp-content/uploads/MR_ASW_300`, `ASW 300`,
  and `asw_300_barley` found the normalized owners, the generated merge,
  adjacent bacterial and duplicate ASW 300 records, ASW salinity-grid review
  artifacts, indexes, and generated outputs. It did not change the finding that
  the inspected current CCAP `MR_ASW_300.pdf` omits barley.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | The generated merge is stale relative to its maintained algae owners. It still has a sixth pseudo-ingredient for final-volume and pH text and lacks the structured CCAP source plus three `preparation_steps` added upstream on 2026-08-25 and 2026-09-13. | The reviewed merge still lists `Make up to 1 litre...` at `1 MOLAR`; both algae owners now list only the five salts and ordered make-up, pH, and autoclave steps. | `data/normalized_yaml/algae/CCAP_ASW 300 + barley  + barley grain_).yaml`, `data/normalized_yaml/algae/asw_300_barley.yaml`, and the merge regeneration path. |
| major | The current CCAP `MR_ASW_300.pdf` supports ASW 300 but not the retained `+ barley` source identity. | The refetched 2026-05-29 CCAP PDF is titled `ASW 300` and lists only the five salts plus pH/autoclave instructions. | The same normalized algae owners, or the MediaDive/CCAP C8 bacterial owner if that is where the barley-grain instruction originated. |
| minor | The magnesium sulfate label lost the digit `4` and the second hydrate digit. | The target says `MgSO .7H O`; the source formula is `MgSO4.7H2O`, and the record is grounded to `CHEBI:31795` magnesium sulfate heptahydrate. | The normalized algae owners or the CCAP importer/repair rule that preserves formula labels. |
| minor | Generic culture conditions are over-scoped to a recipe PDF that does not state them. | The inspected CCAP PDF has no light-intensity, light-cycle, or incubation-temperature prescription. | The normalized algae owners or the CCAP import defaulting rule that emits those algae defaults. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/asw_300_barley.yaml` from the current
   normalized owners so the pseudo-ingredient is removed, structured `sources`
   are retained, and `preparation_steps` are present in the generated record.
2. Resolve the `+ barley` provenance. If CCAP or MediaDive has an older C8
   source that says to add barley grains to ASW 300, preserve that source and
   cite it narrowly; otherwise rename the algae CCAP ASW 300 record so the
   current `MR_ASW_300.pdf` is not presented as evidence for barley.
3. Correct the magnesium sulfate preferred label to preserve the source formula
   as `MgSO4.7H2O` or the local preferred hydrate spelling used elsewhere in
   CultureMech.
4. Remove the generic light and temperature assertions unless a checked CCAP or
   organism-specific source supports them.

## Follow-up Checks

- Run the merge freshness and merge verification checks that cover
  `asw_300_barley.yaml`, then rerun open-schema, strict, term, and reference
  validation on the regenerated merge.
- Manually re-open the archived or source-specific document used to justify
  `+ barley`; prove the retained original name with inspected source text or
  make the record salt-only.
- Re-run ingredient term validation after the magnesium sulfate label is
  corrected.
- Re-run an exact ignored-file-inclusive search for `CultureMech:000049`,
  `CCAP:ASW_300`, and `asw_300_barley` to make sure no stale ASW 300 generated
  page or index still exposes the old pseudo-ingredient.

## Additional Notes

- `data/normalized_yaml/bacterial/asw_300_barley.yaml` is a distinct
  MediaDive-derived bacterial record with `CultureMech:000404`; it also points
  at CCAP `MR_ASW_300.pdf` and includes a T-25 flask preparation step that adds
  20 ml of autoclaved ASW 300 and four autoclaved barley grains.
- The bacterial owner rounds the magnesium chloride hexahydrate amount to
  54.4 g/L, while the inspected CCAP PDF and the algae owners use 54.36 g/L.
  That rounding difference is outside the reviewed generated algae target but
  should be kept in mind if the ASW 300 records are reconciled as salinity
  variants.
