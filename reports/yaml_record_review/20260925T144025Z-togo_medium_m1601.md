# YAML Record Review: togo_medium_m1601

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1601.yaml
- Started UTC: 2026-09-25T14:36:00Z
- Finished UTC: 2026-09-25T14:40:25Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008154
- Name: togo_medium_m1601
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1601.yaml`.
- Source identity in the generated record: TOGO:M1601, original source NBRC_M707, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=707`.
- Merge lineage in the generated record: only `togo_medium_m1601`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1601.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1601.yaml --out /private/tmp/m1601.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1601.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it reported zero checks.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1601.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1601 resolves to NBRC_M707. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- MgSO4 x 7 H2O is exactly grounded to CHEBI:31795.

## Evidence

- TOGO M1601 and NBRC 707 support Peptone 5 g, Yeast extract 3 g, MgSO4 x 7 H2O 1 g, Distilled water 1 L, and pH 7.0.
- The generated record preserves the non-water ingredient set and quantities.
- The generated record encodes the 1 L distilled-water row as 1 `G_PER_L`, which is a source unit error.
- The source pH 7.0 is absent.

## Completeness

- Consequential missing or wrong fields: 1 L water as final volume and pH 7.0.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1601` found no existing report for this record stem.

## Findings

- major: `data/normalized_yaml/bacterial/togo_medium_m1601.yaml` stores the 1 L distilled-water source row as 1 g/L.
- minor: the normalized owner omits the NBRC_M707 pH 7.0 value.

## Recommended Edits

- Convert the distilled-water row to a final-volume representation matching 1 L.
- Add pH 7.0 from NBRC_M707.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1601.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1601.yaml` and verify water and pH.

## Additional Notes

- Exact source inspection was bounded to TOGO M1601 and NBRC 707.
