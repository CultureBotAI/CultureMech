# YAML Record Review: 1135

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1135.yaml`
- Started UTC: 20260921T043009Z
- Finished UTC: 20260921T043139Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007458` |
| Label | `1135` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1135.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1135]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1135.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import for NBRC Medium 1132. The maintained owner has since been recovered and curated to `Malt Extract Ebios Agar (MEBA)` with separate malt extract, Ebios tablet, water, and agar rows.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1135.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1135.yaml --out /private/tmp/culturemech-review-1135.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-1135.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1135.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1135.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

The passing schema checks do not catch the stale parsed-composition loss.

## Identity and Grounding

The intended identity is NBRC Medium 1132:

- The import history says `Media No. 1132` and links the NBRC `NO=1132` page.
- Live NBRC Medium 1132 is titled `Malt Extract Ebios Agar (MEBA)`.
- The maintained owner now carries `media_term: nbrc.medium:1132`, the official title, and the NBRC HTTPS reference.

The generated file no longer agrees with that identity:

- `name` and `original_name` are plain `1135`, but live NBRC Medium 1135 is a different yeast/casamino-acids/salts recipe at pH 9.0-9.5.
- The generated file has no `media_term` or `references`.
- Its `merged_from: [NBRC_1135]` pointer is the only source-specific handle, and the maintained `NBRC_1135.yaml` input now differs materially from this generated artifact.

## Evidence

Supported by inspected source text:

- NBRC Medium 1132 lists `Malt Extract Ebios Agar (MEBA)`.
- NBRC Medium 1132 lists malt extract 20 g, Ebios tablets 1 g, distilled water 1 L, and agar 15 g.
- NBRC Medium 1132 notes that the Ebios tablets were manufactured by Asahi Breweries in Tokyo and distributed by Tanabe Seiyaku in Osaka.

Unsupported or malformed in the generated record:

- One ingredient row concatenates the full NBRC composition and the Ebios footnote into a single `preferred_term` with `VARIABLE` concentration.
- The second ingredient row has `preferred_term: ''` and `concentration.value: Malt extract`, which is dimensionally invalid.
- No generated row separately represents malt extract 20 g/L, Ebios tablets 1 g/L, distilled water 1 L, or agar 15 g/L.
- `preparation_steps[3].action` is `DISSOLVE` while its description is `Sterilize by autoclaving`.

## Completeness

- The generated file is one regeneration behind the repaired normalized NBRC source and omits the Sep 11 `RESOLVED_NBRC_1135_SCORE15` repair, the Aug 19 recovered composition, and the Aug 20 MIM groundings.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_1135`, `NO=1132`, `NBRC Medium 1132`, and `nbrc.medium:1132` found this stale generated NBRC direct import, the repaired normalized owner, normalized index entries, and a separate TOGO-derived `malt_extract_ebios_agar_meba` record that cites original NBRC Medium 1132.
- The TOGO-derived `malt_extract_ebios_agar_meba` record should be considered during future deduplication, but it is not the owner of this stale generated `NBRC_1135` artifact.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading numeric identity for NBRC Medium 1132. | The file is named and labeled `1135`, while its import event points to NBRC Medium 1132; live NBRC Medium 1135 is a different alkaline yeast/casamino-acids/salts medium. | `data/normalized_yaml/bacterial/NBRC_1135.yaml` is repaired; regenerate `data/merge_yaml/merged/` and clean up the stale artifact. |
| Blocker | The NBRC composition is collapsed into malformed ingredient rows. | NBRC Medium 1132 has four source rows, but the generated file has one concatenated formula/footnote blob plus one empty ingredient whose concentration value is `Malt extract`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1135.yaml`; if the malformed rows persist, fix the NBRC importer or merge source selection. |
| Major | The generated file lacks the curated source grounding and reference. | The normalized owner now has `nbrc.medium:1132`, the official NBRC title, an HTTPS reference, and source-scoped ingredient notes; the generated merge has none of them. | Regenerate the merge corpus from the repaired normalized record. |
| Major | The autoclave step is typed as dissolution. | The generated third step says `Sterilize by autoclaving` but has `action: DISSOLVE`; the maintained owner now uses `action: AUTOCLAVE`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1135.yaml`. |
| Minor | A TOGO-derived copy of NBRC Medium 1132 remains separate. | Gitignore-independent search found `malt_extract_ebios_agar_meba` as a TOGO record with the same original NBRC 1132 URL. | Merge-fingerprint rules for NBRC/TOGO duplicate handling. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the repaired NBRC normalized corpus and confirm `CultureMech:007458` carries `Malt Extract Ebios Agar (MEBA)` rather than `1135`.
2. Confirm the regenerated NBRC Medium 1132 artifact has separate malt extract, Ebios tablet, distilled water, and agar ingredients; the `nbrc.medium:1132` media term; `AUTOCLAVE` sterilization; `ingredients_curated`; and the NBRC 1132 HTTPS reference.
3. If regeneration leaves the malformed blob intact, fix the NBRC merge source path that is still selecting the pre-Aug 19 parsed record.
4. Decide whether the repaired NBRC direct import should merge with the TOGO `malt_extract_ebios_agar_meba` copy, or remain separate with explicit source rationale.

## Follow-up Checks

- `just verify-merges` to prove generated outputs are fresh relative to `data/normalized_yaml/bacterial/NBRC_1135.yaml`.
- `just audit-merge-freshness` to catch sibling stale NBRC direct-import artifacts.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1132>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1132>.yaml` to verify the FoodOn/CHEBI groundings.
- Manual comparison to live NBRC Medium 1132 for all four ingredients and the Ebios manufacturer/distributor footnote.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
