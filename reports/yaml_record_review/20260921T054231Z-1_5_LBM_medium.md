# YAML Record Review: 1/5 LBM medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_5_LBM_medium.yaml`
- Started UTC: 2026-09-21T05:40:50Z
- Finished UTC: 2026-09-21T05:42:32Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008247` |
| Name | `1_5_lbm_medium` |
| Original name | `1/5 LBM medium` |
| Category | `bacterial` |
| Source term | `TOGO:M1688`, `1/5 LBM medium` |
| Merge fingerprint | `69631d21d42d34d8418c188d403100fe837b3f5506b92260785b7be1ca1b554e` |
| Maintained owner | `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The generated file is a singleton TOGO-derived merge. It is not current with its maintained owner: `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` was repaired on 2026-09-11 with pH 7.0, ingredient source notes, a direct NBRC reference, the TOGO reference, data-quality flags, and an agar grounding, but this generated merge predates those edits.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_5_LBM_medium.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_5_LBM_medium.yaml --out /private/tmp/1_5_LBM_medium.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_5_LBM_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_5_LBM_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record correctly identifies TOGO Medium M1688 and NBRC Medium 893:

- Live TOGO M1688 reports `name: 1/5 LBM medium`, `original_media_id: NBRC_M893`, `src_url` ending in `NO=893`, and `ph: "7.0"`.
- Live NBRC Medium 893 reports Medium No. 893 and the same title.
- The generated `media_term.term.id` is `TOGO:M1688`, with the right TOGO label.

Grounding is incomplete:

- The generated agar row is ungrounded, but the maintained owner has already grounded it to `CHEBI:2509`.
- `Bacto Yeast  Extract (Difco)` and `Bacto Tryptone (Difco)` intentionally remain ungrounded in the maintained owner because these rows are named Difco products, not single ChEBI molecules.
- The source seawater row is incorrectly modeled as `mediadive.solution:1801`, `Seawater`, with `1 G_PER_L`. TOGO M1688 and NBRC Medium 893 both say `Seawater*`, 1 L, and the NBRC/TOGO footnote specifically allows filtered aged seawater or Daigo's Artificial Seawater SP.

## Evidence

The inspected TOGO and NBRC sources support:

- `1/5 LBM medium` as the record label.
- Bacto Tryptone (Difco), 2 g.
- Bacto Yeast Extract (Difco), 1 g.
- Agar (if needed), 15 g.
- Seawater*, 1 L.
- pH 7.0.
- The seawater footnote permitting filtered aged seawater or Daigo's Artificial Seawater SP.

The generated file omits several source-supported or curated facts:

- It has no `ph_value`.
- It has no structured `references`.
- It has no source notes on the three `ingredients` rows.
- It has no agar grounding.
- It does not preserve the TOGO/NBRC seawater footnote.

The generated and maintained records both carry one unsupported transformation: `Seawater*` was migrated from a 1 L source component into a `solutions` entry as `mediadive.solution:1801` at `1 G_PER_L`. The linked solution is a generic MediaDive solution record for 32 g sea salt in 1000 ml distilled water; it is not NBRC's filtered aged seawater / Daigo's Artificial Seawater SP option and the final-medium amount is not grams per liter.

## Completeness

Consequential gaps:

- The generated merge is stale relative to the maintained owner repaired on Sep 11, 2026.
- Both the generated merge and maintained owner misrepresent the 1 L source seawater row as a 1 g/L generic stock-solution addition.
- Neither layer records the NBRC/TOGO seawater footnote.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected TOGO and NBRC recipe pages support the medium formulation but do not verify a particular organism-growth claim.
- `preparation_steps` is absent. This is incomplete with respect to pH adjustment, but the inspected TOGO/NBRC sources do not state sterilization, storage, or ordering details.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `CultureMech:008247`, `TOGO:M1688`, the merge fingerprint, `NBRC_M893`, and `NO=893` found the normalized owner, the generated merge, active registry/index rows, historical March validation rows, one deep-research priority row, and `scripts/repair_sparse_top_score15.py`.
- A gitignore-independent `find` over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` found only `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` and `data/merge_yaml/merged/1_5_LBM_medium.yaml` with this filename stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale relative to its repaired normalized owner. | The generated file lacks pH 7.0, structured TOGO/NBRC references, row-level source notes, `data_quality_flags`, and the `CHEBI:2509` agar grounding added to `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` on 2026-09-11. | Regenerate from `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml`; fix merge freshness if the stale output persists |
| Major | The source seawater component is represented as the wrong thing with the wrong unit. | TOGO M1688 and NBRC Medium 893 list `Seawater*` as 1 L and define the footnote as filtered aged seawater or Daigo's Artificial Seawater SP. The generated record and its maintained owner instead link `mediadive.solution:1801` and assign the final-medium concentration `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` or the solution-migration rule |
| Minor | The seawater source footnote is missing. | TOGO M1688 and NBRC Medium 893 both carry the filtered-aged-seawater / Daigo's Artificial Seawater SP note and the manufacturer footnote; neither the generated merge nor the repaired normalized owner preserves it. | `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/1_5_lbm_medium.yaml`, replace the migrated `solutions` entry with a main-recipe `Seawater*` component at `1 L` and remove the unsupported `mediadive.solution:1801` final-medium reference.
2. Preserve the source footnote in a row note or recipe note: filtered aged seawater and Daigo's Artificial Seawater SP are allowed alternatives for the 1 L seawater row.
3. Append a curation-history event naming TOGO M1688 and NBRC Medium 893 and describing the seawater/footnote correction.
4. Regenerate `data/merge_yaml/merged/` and confirm the generated `1_5_LBM_medium.yaml` carries the Sep 11 pH/reference/grounding repair plus the seawater correction.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` after curation.
- `just validate-references data/normalized_yaml/bacterial/1_5_lbm_medium.yaml` after any reference edits.
- `just verify-merges` and `just audit-merge-freshness` after regenerating the generated merges.
- Manual TOGO M1688 and NBRC Medium 893 comparison for pH 7.0, the three dry ingredient rows, 1 L seawater, and the seawater footnotes.

## Additional Notes

- The term validator passed against the generated record because it cannot know that `1 G_PER_L` is dimensionally wrong for a source row that was 1 L.
- `mediadive.solution:1801` resolves to `data/normalized_yaml/bacterial/mediadive_1801_Seawater.yaml`, but that solution record itself has `data_quality_flags: [incomplete_composition]` and contains a placeholder `See source for composition` ingredient.
- The reference validator passed with 0 checks because this stale generated merge predates the structured references now present in the normalized owner.
