# YAML Record Review: 2x_yeast_extract_tryptone_2xyt

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml`
- Started UTC: 2026-09-21T06:45:46Z
- Finished UTC: 2026-09-21T06:46:54Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009454` |
| Name | `2x_yeast_extract_tryptone_2xyt` |
| Original name | `2x Yeast Extract Tryptone (2xYT)` |
| Category | `bacterial` |
| Source term | `TOGO:M2919`, `2x Yeast Extract Tryptone (2xYT)` |
| Source URL | `https://togomedium.org/medium/M2919` |
| Merge fingerprint | `69b8ae2d5ae1aa7966693ad313b2e2d767b26c1fc95c1f52c6f1abe5cf9c9a9e` |
| Maintained owner | `data/normalized_yaml/bacterial/2x_yeast_extract_tryptone_2xyt.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO M2919 import. Future corrections generally belong in `data/normalized_yaml/bacterial/2x_yeast_extract_tryptone_2xyt.yaml`; for this record, that owner already carries a later 2026-09-08 curation, so the generated merge should be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml --out /private/tmp/2x_yeast_extract_tryptone_2xyt.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The TOGO identity is internally consistent: the target cites `TOGO:M2919`, the live TOGO API payload identifies M2919 as `2x Yeast Extract Tryptone (2xYT)`, and the normalized owner has the same stable CultureMech ID.

The sole component is deliberately opaque. The live TOGO M2919 payload lists one component named `2x Yeast Extract Tryptone (2xYT)` with GMO `GMO_002249` and marks it as both `Undefined component` and `Complex component`; it does not disclose internal tryptone, yeast extract, or sodium chloride rows.

The generated merge is stale relative to the maintained owner. `data/normalized_yaml/bacterial/2x_yeast_extract_tryptone_2xyt.yaml` carries a 2026-09-08 `RESOLVED_TOGO_MRS_2XYT_SCORE40` repair, represents the opaque one-liter component as `1000 ML_PER_L`, adds a `MIX` preparation step, adds `https://togomedium.org/medium/M2919` to `references`, and preserves `has_unmapped_ingredients` for the ungrounded opaque component. The generated merge predates that repair and still stores the same source volume as `1 G_PER_L`.

## Evidence

The inspected live TOGO M2919 API payload supports the curated normalized owner:

- M2919 name: `2x Yeast Extract Tryptone (2xYT)`.
- Component: `2x Yeast Extract Tryptone (2xYT)`.
- Amount: `1 L`.
- GMO ID: `GMO_002249`.
- Properties: `Undefined component`, `Complex component`.
- Comment: a CB9615 colony seeded a 20 mL overnight culture in 2x Yeast Extract Tryptone.
- `src_url`: empty.

Fetching `https://togomedium.org/medium/M2919` returned HTTP 200, but the source comparison used the TOGO REST API route used by the repository's own `src/culturemech/fetch/togo_fetcher.py`.

## Completeness

Consequential gaps:

- The generated target is missing the maintained owner's September 2026 repair.
- The generated target has no structured `references`, so the TOGO M2919 source URL is not validator-visible.
- The generated target has no `preparation_steps`, while the maintained owner has a source-faithful one-step `MIX` instruction.
- The generated target stores TOGO's 1 L row as `1 G_PER_L`; the maintained owner already corrects the source volume to `1000 ML_PER_L`.

Empty optional slots:

- `solutions` is correctly absent. The live TOGO payload is a one-row opaque product/medium entry with no named stock solutions.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected TOGO payload mentions a CB9615 overnight culture in a free-text comment but has no resolvable taxon, strain identity, or growth measurement.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2x_yeast_extract_tryptone_2xyt.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, and `data/raw` for `2x_yeast_extract_tryptone_2xyt` found the normalized owner, generated merge, generated indexes/manifests, and a review manifest row.
- A gitignore-independent `rg --no-ignore --hidden` search over `scripts`, `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, and `data/raw` for `M2919` found the normalized owner, generated merge, indexes/manifests, and archived validation reports.
- A gitignore-independent `find` under `data/raw` for `*2919*` found no cached raw source files. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still carries the pre-repair TOGO M2919 import. | The generated target stores TOGO's 1 L opaque 2xYT component as `1 G_PER_L`, has no `references`, no `preparation_steps`, and no data-quality flag for its intentionally ungrounded opaque component. The maintained owner already stores `1000 ML_PER_L`, the TOGO reference, a source-faithful `MIX` step, and the September 2026 `RESOLVED_TOGO_MRS_2XYT_SCORE40` curation event. | `data/normalized_yaml/bacterial/2x_yeast_extract_tryptone_2xyt.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml` so `2x_yeast_extract_tryptone_2xyt.yaml` picks up the existing owner repair.
2. Verify that the regenerated `data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml` stores the opaque component as `1000 ML_PER_L`, includes the `MIX` preparation step, includes the TOGO reference, and keeps `has_unmapped_ingredients`.

## Follow-up Checks

- `just verify-merges` after regenerating generated records.
- `just audit-merge-freshness` after regenerating generated records.
- `just validate-schema data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml` after regeneration.
- `just validate-strict data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml` after regeneration.
- `just validate-references data/merge_yaml/merged/2x_yeast_extract_tryptone_2xyt.yaml` after regeneration.
- Manual comparison against TOGO M2919 for the 1 L opaque `2x Yeast Extract Tryptone (2xYT)` component.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
- The September 2026 curation script named in the normalized owner's history, `repair_togo_mrs_2xyt_score40.py`, is not present in the current `scripts/` directory; a gitignore-independent `rg --no-ignore --hidden` search for that exact filename found only curation-history entries in normalized YAML.
