# YAML Record Review: 2_5_nacl_bpg_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_5_nacl_bpg_agar__bf5a0a80.yaml`
- Started UTC: 2026-09-21T06:24:10Z
- Finished UTC: 2026-09-21T06:25:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008272` |
| Name | `2_5_nacl_bpg_agar` |
| Original name | `2.5% NaCl-BPG Agar` |
| Category | `bacterial` |
| Source term | `TOGO:M170`, `2.5% NaCl-BPG Agar` |
| Source URL | `https://togomedium.org/medium/M170`; notes also name `JCM_M177` |
| Merge fingerprint | `bf5a0a80b2694650c246c899e6cec18ca7b76d8e266e47fa65bf35b0008dec8e` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO import for TOGO M170, which its own notes describe as sourced from JCM Medium 177. Future corrections belong in `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_5_nacl_bpg_agar__bf5a0a80.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_5_nacl_bpg_agar__bf5a0a80.yaml --out /private/tmp/2_5_nacl_bpg_agar__bf5a0a80.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_5_nacl_bpg_agar__bf5a0a80.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_5_nacl_bpg_agar__bf5a0a80.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M170`, the label is `2.5% NaCl-BPG Agar`, and the notes identify TOGO M170 as imported from `JCM_M177`. The inspected JCM Medium 177 page resolves to `2.5% NaCl-BPG AGAR`, so this TOGO record should be treated as a source duplicate of `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml`, the MediaDive/JCM import for the same JCM recipe.

Ingredient grounding is mostly sound for the inspected JCM page: glucose, agar, NaCl, MgSO4 x 7 H2O, and KCl carry chemically matching terms, while Bacto peptone and fish meat extract are correctly ungrounded. Distilled water carries the correct water term but the wrong concentration dimension.

Fetching `https://togomedium.org/medium/M170` returned only the TogoMedium single-page application shell in this run, so the inspected primary formulation was the JCM Medium 177 page named in the TOGO import notes rather than the live rendered TOGO M170 table.

## Evidence

The inspected JCM Medium 177 page supports this formulation for a 1 L distilled-water basis:

- 5 g Bacto peptone (BD-Difco).
- 5 g fish meat extract.
- 1 g glucose.
- 15 g agar.
- 25 g NaCl.
- 2.5 g MgSO4 x 7 H2O.
- 1 g KCl.
- 1 L distilled water.
- pH 7.2.

The generated target preserves all seven dry components and the Bacto peptone product qualifier. Its distilled-water row is materially wrong: the source lists a final 1 L solvent volume, but the generated target records `value: 1` with `unit: G_PER_L`. The target also omits pH 7.2.

The JCM page states a source-wide default to sterilize media by autoclaving at 121 C for 15 minutes unless otherwise stated. The generated target and its maintained normalized owner omit that instruction.

## Completeness

Consequential gaps:

- The distilled-water amount and unit are wrong.
- `ph_value: 7.2` is absent.
- The record has no structured `references`, so neither the TOGO M170 nor JCM Medium 177 URL is validator-visible.
- The record has no `SOURCE_DUPLICATE` parent link to `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml`.
- The record has no preparation step for the JCM default autoclave instruction.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM recipe supports a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_5_nacl_bpg_agar__bf5a0a80.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:002537`, `CultureMech:008272`, `mediadive.medium:J177`, `TOGO_M170_2.5_NaCl-BPG_Agar`, the JCM GRMD URL, and both merge fingerprints found the expected MediaDive/JCM and TOGO M170 normalized/generated pairs. Ignored files were included under the searched paths. The short `TOGO:M170` token also matched unrelated longer TOGO IDs, so only exact JCM 177 and TOGO M170 hits were used.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is represented with the wrong dimension. | The inspected JCM Medium 177 source lists 1 L distilled water; the generated and normalized TOGO M170 records store `Distilled water` as `1 G_PER_L`, which turns a final solvent volume into a 1 g/L mass concentration. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` |
| Major | Final pH 7.2 is missing. | JCM Medium 177 instructs pH adjustment to 7.2; the TOGO M170 generated and normalized records have no `ph_value` or pH adjustment step. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` |
| Major | The JCM default autoclaving instruction is missing. | JCM states that media should be autoclaved at 121 C for 15 min unless otherwise stated; neither the generated target nor its maintained normalized owner represents that step. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` |
| Major | TOGO M170 duplicates JCM Medium 177 but is not linked to the MediaDive/JCM owner. | The TOGO record notes `Original source: JCM - JCM_M177`; `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` already represents the same JCM formulation, but neither record has a reciprocal `SOURCE_DUPLICATE` link. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` and `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO and JCM URLs only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` |

## Recommended Edits

1. Correct `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` so distilled water is represented as the 1 L solvent row supported by JCM Medium 177 rather than `1 G_PER_L`.
2. Add `ph_value: 7.2`.
3. Add a JCM-supported preparation step for autoclaving at 121 C for 15 minutes.
4. Add structured references for both `https://togomedium.org/medium/M170` and `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=177`.
5. Add reciprocal `SOURCE_DUPLICATE` links between `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` and `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml`.
6. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` after repairing the distilled-water row.
- `just validate-references data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` after adding structured TOGO and JCM references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 177 for 5 g/L Bacto peptone (BD-Difco), 5 g/L fish meat extract, 1 g/L glucose, 15 g/L agar, 25 g/L NaCl, 2.5 g/L MgSO4 x 7 H2O, 1 g/L KCl, 1 L distilled water, pH 7.2, and default autoclaving.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
