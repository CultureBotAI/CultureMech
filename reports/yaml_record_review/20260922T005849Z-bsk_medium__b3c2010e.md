# YAML Record Review: BSK-MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bsk_medium__b3c2010e.yaml
- Started UTC: 2026-09-22T00:58:49Z
- Finished UTC: 2026-09-22T00:58:49Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:001513 |
| Label | bsk_medium |
| Original label | BSK-MEDIUM |
| Category | bacterial |
| Source accession | mediadive.medium:403 |
| Generated path | data/merge_yaml/merged/bsk_medium__b3c2010e.yaml |
| Maintained owner | data/normalized_yaml/bacterial/bsk_medium.yaml |
| Merge fingerprint | b3c2010e592dbbb7d9b7fcb63b0de3cfca05bdbb2d2573a3c80682f84b221c70 |

The reviewed target is a generated merge under `data/merge_yaml/merged`,
derived from the single normalized owner `bsk_medium`. Future fixes belong in
that normalized file, the MediaDive importer that flattened DSMZ 403's
solutions, or the LB-commercial-product enrichment path that added unsupported
LB rows.

## Validation

| Check | Result |
| --- | --- |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bsk_medium__b3c2010e.yaml` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bsk_medium__b3c2010e.yaml --out /private/tmp/bsk_medium__b3c2010e.strict.tsv --workers 1 --quiet` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bsk_medium__b3c2010e.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 structured URL checks |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bsk_medium__b3c2010e.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| `just validate-history data/merge_yaml/merged/bsk_medium__b3c2010e.yaml` | Not checked: `validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries |

Direct `just validate-schema`, `just validate-strict`, `just validate-terms`,
and `just validate-references` were not rerun because the project uv environment
tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with
`TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The
equivalent no-project Python 3.11 validators above passed.

## Identity and Grounding

The direct DSMZ/MediaDive identity is coherent: the record uses
`mediadive.medium:403`, and the inspected MediaDive page for medium 403 is
`403: BSK-MEDIUM`. The original DSMZ PDF URL in `notes` resolves through the
MediaDive 403 page's modern representation.

This direct DSMZ owner is not reconciled with the KOMODO 403 wrapper, whose
`notes` say it copied its ingredients from DSMZ Medium 403, or the TOGO M2366
wrapper, whose source URL also points at the DSMZ Medium 403 PDF. TOGO M2366
has already been repaired to preserve the source stock-solution boundaries; the
direct DSMZ owner still has the older flattened import.

The CHEBI mappings for HEPES, sodium citrate, glucose, sodium pyruvate,
N-acetylglucosamine, magnesium dichloride hexahydrate, L-glutamine, water, and
gelatin are chemically plausible for the imported labels. CMRL 1066, Proteose
peptone no. 2, Tryptone peptone, Proteose yeastolate, and Rabbit serum are
complex materials that are appropriately unresolved until their exact MediaDive
product labels can be represented.

## Evidence

Supported:

| Claim | Source support |
| --- | --- |
| MediaDive medium 403 denotes DSMZ BSK-MEDIUM | The inspected MediaDive page title is `403: BSK-MEDIUM`. |
| Solution A component labels and gram amounts | MediaDive 403 lists CMRL 1066 100 ml, Proteose peptone no. 2 5 g, Tryptone peptone 1 g, Proteose yeastolate 1 g, HEPES 6 g, Sodium citrate 0.7 g, Glucose 3 g, Na-pyruvate 0.8 g, N-Acetylglucosamine 0.4 g, MgCl2 x 6 H2O 0.3 g, L-Glutamine 0.1 g, and 900 ml double-distilled water in Solution A. |
| Preparation step text | The five imported preparation steps match the main-medium, Solution A, Solution B, and Solution C steps on the MediaDive page. |

Unsupported or incomplete:

| Generated claim | Problem |
| --- | --- |
| Flat final ingredient list | DSMZ Medium 403 is a stock-based formulation: the main medium combines 1000 ml Solution A, 200 ml Solution B, 143 ml Solution C, and 86 ml Solution D. The generated row list mixes stock concentrations and solution volumes without those boundaries. |
| `Double distilled water`, `1243.0 G_PER_L` | This is the sum of the 900 ml, 200 ml, and 143 ml water rows from Solutions A, B, and C, converted into a mass unit and detached from the stock recipes that require those separate water volumes. |
| `CMRL 1066`, `100 G_PER_L` and `Rabbit serum`, `86 G_PER_L` | MediaDive 403 gives these as milliliter quantities inside Solution A and Solution D, not as mass concentrations. |
| `Gelatine`, `70 G_PER_L` as a final top-level ingredient | The 70 g/L concentration applies inside 200 ml Solution B; the main medium uses 200 ml Solution B in a 1429 ml source batch. |
| No structured Bovine serum albumin row | Solution C requires 50.05 g Bovine serum albumin, Sigma A9647, in 143 ml water; the BSA preparation step is present, but the ingredient itself is absent. |
| Top-level LB Miller notes and `Tryptone 10.0 G_PER_L`, extra `Yeast extract 5.0 G_PER_L`, `Sodium chloride 10.0 G_PER_L` | MediaDive 403 does not cite LB Miller broth, Sigma L3522, BD 244520, laboratorynotes.com, or these three LB rows. |
| No `ph_value: 7.6` and no structured solution references | The pH appears only in Solution A prose, and the record has no Solution A-D structures to carry stock-specific pH, sterilization, and composition. |
| No structured DSMZ/MediaDive reference | The only source support is free text in `notes`; there is no `source_data` or reference object for MediaDive 403. |

## Completeness

The direct DSMZ owner lacks the structured stock-solution boundary that defines
DSMZ Medium 403 and omits the Bovine serum albumin ingredient from Solution C.
It also flattens solution volumes into `G_PER_L` final ingredients, lacks a
top-level pH even though Solution A is adjusted to pH 7.6, and carries the
unsupported LB Miller enrichment block already seen in other BSK-family records.

A gitignore-independent `rg --no-ignore --hidden` search of
`data/normalized_yaml`, `data/merge_yaml/merged`, the ID registry, the recipe
catalog, `reports/yaml_record_review`, `reports/media_content_review_manifest`,
and `data/import_tracking/reports` for `CultureMech:001513`,
`mediadive.medium:403`, `bsk_medium.yaml`, and merge fingerprint `b3c2010e...`
found the active direct DSMZ 403 owner, this generated merge, expected generated
indexes and reports, and no existing YAML review report for this record. A
second exact search for `DSMZ_Medium403`, `mediadive.medium:403`,
`komodo.medium:403`, and `TOGO:M2366` found the direct DSMZ owner, the KOMODO
403 wrapper, and the TOGO M2366 wrapper around the same DSMZ source. `find
reports/yaml_record_review -maxdepth 1 -type f -name
'*bsk_medium__b3c2010e.md'` found no pre-existing report, including ignored
files in the report directory.

Empty target-organism and growth-evidence slots are not defects for this
source-only recipe.

## Findings

| Severity | Finding | Maintained owner for a future fix |
| --- | --- | --- |
| Major | DSMZ Medium 403's Solution A/B/C/D boundaries are lost. The record stores 900, 200, and 143 ml water as a single `1243.0 G_PER_L` ingredient, keeps the 70 g/L Gelatine stock concentration as a final component, and records 100 ml CMRL 1066 and 86 ml Rabbit serum as mass concentrations. | `data/normalized_yaml/bacterial/bsk_medium.yaml` and the MediaDive import path |
| Major | Solution C's 50.05 g Bovine serum albumin is absent from structured ingredients, even though the Solution C preparation step mentions it in prose. | `data/normalized_yaml/bacterial/bsk_medium.yaml` |
| Major | Three LB Miller ingredients and all LB Miller commercial-product metadata are unsupported in DSMZ Medium 403. They add 10 g/L tryptone, 5 g/L yeast extract, and 10 g/L sodium chloride to BSK-MEDIUM even though the source formula has none of those LB rows. | `data/normalized_yaml/bacterial/bsk_medium.yaml` and the LB commercial-product enrichment rule |
| Major | The direct DSMZ 403 owner is an unreconciled duplicate of the KOMODO 403 and TOGO M2366 wrappers around the same DSMZ source. | `data/normalized_yaml/bacterial/bsk_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_403_BSK-MEDIUM.yaml`, `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml`, and source duplicate handling |
| Minor | The record has no structured MediaDive source reference, so the reference validator had no source URL to check. | `data/normalized_yaml/bacterial/bsk_medium.yaml` |

No blocker findings found.

## Recommended Edits

1. Rebuild `data/normalized_yaml/bacterial/bsk_medium.yaml` from MediaDive
   Medium 403 with explicit Solution A, B, C, and D boundaries.
2. Restore Solution C's 50.05 g Bovine serum albumin row and its Sigma A9647 /
   A7409 product note.
3. Preserve source volumes for 100 ml CMRL 1066, 900/200/143 ml water, and 86 ml
   Rabbit serum instead of encoding them as `G_PER_L` final concentrations.
4. Move Solution A pH adjustment to stock-specific preparation context and add
   `ph_value: 7.6`.
5. Remove the unsupported LB Miller top-level note block and the extra Tryptone,
   LB Yeast extract, and Sodium chloride rows from the DSMZ owner.
6. Add structured MediaDive 403 provenance.
7. Reconcile the direct DSMZ 403, KOMODO 403, and TOGO M2366 owners after the
   direct DSMZ and KOMODO owners both preserve stock boundaries.
8. Regenerate `data/merge_yaml/merged/bsk_medium__b3c2010e.yaml`.

## Follow-up Checks

- Rerun focused schema, strict, term, and reference validation on
  `data/normalized_yaml/bacterial/bsk_medium.yaml`.
- Rerun `just verify-merges` after regeneration and inspect the generated BSK
  diff to confirm DSMZ 403 stock boundaries, Bovine serum albumin, pH, source
  volume units, and LB-row removal.
- Recheck `mediadive.medium:403`, `komodo.medium:403`, `TOGO:M2366`, and
  `DSMZ_Medium403` after duplicate reconciliation.

## Additional Notes

- `data/import_tracking/reports/merged_duplicates.tsv` already flags the
  `Double distilled water` row as `DIFFERING_PARTS` because `1243.0` was built
  by summing 900, 200, and 143 ml from different water rows.
