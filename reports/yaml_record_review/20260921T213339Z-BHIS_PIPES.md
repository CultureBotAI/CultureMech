# YAML Record Review: BHIS_PIPES

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHIS_PIPES.yaml
- Started UTC: 2026-09-21T21:32:39Z
- Finished UTC: 2026-09-21T21:33:40Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/BHIS_PIPES.yaml` |
| ID | `CultureMech:015498` |
| Label | `BHIS_PIPES` |
| Category | `specialized` |
| Maintained owner | `data/normalized_yaml/specialized/bhis_pipes.yaml` |
| Source | CultureBotHT `BHIS_PIPES`, via FEBA media definitions |
| Merge fingerprint | `73b19f99838061b8887e808db14bc04020d39fb612ac7d357b70421f942ebef8` |
| Merge inputs | `bhis_pipes` |

The reviewed file is a derived merge record generated from
`data/normalized_yaml/specialized/bhis_pipes.yaml`.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHIS_PIPES.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHIS_PIPES.yaml --out /private/tmp/BHIS_PIPES.strict.tsv --workers 1 --quiet` | Passed with 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHIS_PIPES.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHIS_PIPES.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | `just validate-history` | Not checked: the documented focused history validator targets standalone files under `history/`; no documented record-local embedded `curation_history` validator exists for one merge record |

Direct `just` validation remains blocked by the project environment's Python
3.13 build of `llvmlite==0.46.0` before those recipes reach this file; the
no-project Python 3.11 commands above exercised the same focused validators.

## Identity and Grounding

- `CultureMech:015498`, label `BHIS_PIPES`, and
  `sources[0].database_id: BHIS_PIPES` consistently identify the
  FEBA/CultureBotHT BHIS + 30 mM PIPES variant.
- The reviewed merge has one source, `bhis_pipes`, and the generated
  ingredient block exactly matches `data/normalized_yaml/specialized/bhis_pipes.yaml`.
- The ninth ingredient keeps the imported label `PIPES sesquisodium salt`, CAS
  `100037-69-2`, and molecular weight `335.34`, but the ontology `term` is
  `CHEBI:63055` / `disodium PIPES`. The local ChEBI structure index identifies
  `CHEBI:63055` as disodium PIPES with formula `C8H16N2O6S2.2Na` and molecular
  weight `346.338`, while the packaged MediaIngredientMech label index maps
  `PIPES sesquisodium salt` only to `cas:100037-69-2`.
- The hidden-inclusive
  `rg --no-ignore --hidden -n 'database_id: BHIS_PIPES$' . -g '!/.git/**'`
  search found only the normalized owner and this generated merge. The
  hidden-inclusive `find . -path ./.git -prune -o -iname '*bhis_pipes*' -print`
  search found the same two files.

## Evidence

- The record contains no target-organism, variant, growth-metric, or literature
  evidence blocks.
- The nine-ingredient formulation is internally consistent between the
  maintained owner and the derived merge.
- I could not verify the exact ingredient amounts against an inspected upstream
  CultureBotHT source. The current `CultureBotAI/CultureBotHT` default-branch
  tree did not include a `BHIS_PIPES` path, GitHub code search for `BHIS_PIPES`
  returned 0 matches, and the local hidden-inclusive exact search found only
  the maintained and generated CultureMech records.

## Completeness

- No solution references are expected for this direct nine-ingredient broth
  record.
- Empty pH, preparation, organism, growth-metric, discussion, and application
  slots are not defects here because the inspected local provenance does not
  contain a protocol, organism context, or empirical growth claim.
- `has_unmapped_ingredients` is appropriate because Calf brains, Beef heart,
  and Proteose Peptone remain ungrounded imported materials.
- The description says only `Brain heart infusion salt broth` even though the
  record name and composition identify a PIPES variant. That is non-blocking
  because the ingredient row preserves the 30 mM PIPES claim.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `PIPES sesquisodium salt` is grounded to the wrong salt form. | The ingredient label, CAS `100037-69-2`, and MW `335.34` denote PIPES sesquisodium salt; `CHEBI:63055` is cached locally as disodium PIPES with MW `346.338`, and MediaIngredientMech maps the sesquisodium label to its CAS accession rather than to `CHEBI:63055`. | `data/normalized_yaml/specialized/bhis_pipes.yaml` and the legacy/MIM migration rule that promoted this CAS-bearing row |
| Minor | The CultureBotHT formula source is not recoverable from the stored provenance. | `sources` records only `database: CultureBotHT`, `database_id: BHIS_PIPES`, and the repository URL; current GitHub API tree/code searches did not find a live upstream `BHIS_PIPES` source, and hidden-inclusive local searches found no raw upstream copy. | `data/normalized_yaml/specialized/bhis_pipes.yaml` or its CultureBotHT importer |
| Minor | The description is under-specific for this buffered variant. | `name` and the ninth ingredient row identify `BHIS_PIPES`, but `description` is the parent-like text `Brain heart infusion salt broth`. | `data/normalized_yaml/specialized/bhis_pipes.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/specialized/bhis_pipes.yaml`, remove
   `term: CHEBI:63055` from the `PIPES sesquisodium salt` row or replace it
   with an exact identifier for the sesquisodium salt if one is available from
   a verified resolver.
2. Add a retrievable CultureBotHT file path, commit, source snapshot, or
   stronger `CultureBotHT:BHIS_PIPES` reference in the maintained source
   metadata so the nine ingredient rows can be audited against the imported
   source after current CultureBotHT `main` stopped exposing this accession.
3. Update the maintained description to include the 30 mM PIPES supplement once
   the pinned source is available.

## Follow-up Checks

- Rerun the focused schema, strict, term, and reference validators on
  `data/normalized_yaml/specialized/bhis_pipes.yaml` after correcting the PIPES
  salt grounding.
- Re-run the ingredient grounding consistency check that catches exact-form
  salt mismatches, not only the schema-bound term-label validator.
- Regenerate merge products and run `just verify-merges` plus
  `just audit-merge-freshness`.
- Manually inspect the pinned CultureBotHT `BHIS_PIPES` source before declaring
  the ingredient quantities fully source-verified.

## Additional Notes

- The focused LinkML term validator passed because ingredient chemistry terms
  are not bound for canonical OAK label checks; it does not prove exact salt
  identity.
