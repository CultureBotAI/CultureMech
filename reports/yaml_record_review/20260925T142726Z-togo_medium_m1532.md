# YAML Record Review: togo_medium_m1532

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1532.yaml
- Started UTC: 2026-09-25T14:23:40Z
- Finished UTC: 2026-09-25T14:27:26Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008079
- Name: togo_medium_m1532
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1532.yaml`, `data/normalized_yaml/bacterial/medium_for_alicyclobacillus_acidiphilus.yaml`, source-specific SolutionRecipe records, or the merge pipeline.
- Source identity in the generated record: TOGO:M1532, original source NBRC_M323, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=323`.
- Merge lineage in the generated record: `medium_for_alicyclobacillus_acidiphilus` and `togo_medium_m1532`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1532.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1532.yaml --out /private/tmp/m1532.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1532.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it reported zero checks.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1532.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1532 resolves to NBRC_M323 and contains 500 ml Solution A plus 500 ml Solution B.
- TOGO M1660 resolves to NBRC_M864, named Medium for Alicyclobacillus acidiphilus, and contains the same named components and amounts but different solution pH ranges from M1532.
- The generated M1532 record links Solution A to `mediadive.solution:5342` and Solution B to `mediadive.solution:5343`. The local `mediadive_5342_Solution_A.yaml` and `mediadive_5343_Solution_B.yaml` compositions are not the NBRC_M323 solutions.
- The normalized owner has already repaired a duplicated water concentration from 1000.0 back to 500.0 g/L, but the generated record still has the stale summed 1000.0 value.

## Evidence

- NBRC 323 and the TOGO M1532 payload support this split preparation: Solution A is 500 ml with 0.5 g MgSO4 x 7 H2O, 1 g yeast extract, 0.25 g CaCl2 x 2 H2O, 0.6 g KH2PO4, and 0.2 g (NH4)2SO4; Solution B is 500 ml with 1 g glucose and optional 20 g agar.
- NBRC 323 specifies pH 3.0 to 4.0 for Solution A and says Solutions A and B are autoclaved separately and mixed. Those claims are not in the generated record.
- NBRC 864 differs from NBRC 323 by specifying Solution A pH 2.5 to 3.0 and Solution B pH 5.5 to 6.0. The generated `SOURCE_DUPLICATE` relationship loses that difference.
- The inspected `mediadive_5342_Solution_A.yaml` contains unrelated ingredients including Casamino acids, Na glutamate, and 100 g/L NaCl; `mediadive_5343_Solution_B.yaml` contains Na2CO3 and NaCl. Neither is the NBRC_M323 stock composition.

## Completeness

- Consequential missing or wrong fields: source-specific Solution A/B identities, the Solution A pH range, separate autoclaving/mixing, optional agar semantics, the repaired 500 ml water value, and the non-identical pH scope of M1660.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1532` found no existing report for this record stem.

## Findings

- major: the generated record is stale relative to the September repair in `data/normalized_yaml/bacterial/togo_medium_m1532.yaml`; it still has distilled water at 1000.0 g/L from summing two 500 ml duplicate rows.
- major: `data/normalized_yaml/bacterial/togo_medium_m1532.yaml` and `data/normalized_yaml/bacterial/medium_for_alicyclobacillus_acidiphilus.yaml` link the NBRC Solution A/B labels to `mediadive.solution:5342` and `mediadive.solution:5343`, whose inspected compositions do not match the NBRC stock solutions.
- major: the generated merge treats M1532 and M1660 as duplicates even though the two NBRC source pages specify different Solution A/B pH ranges.
- major: the NBRC split-stock preparation is incomplete: the source pH range for M1532 Solution A and the separate autoclaving/mixing step are absent.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/` so the generated M1532 record receives the repaired 500.0 water value.
- Replace the wrong MediaDive Solution A/B links in the M1532 and M1660 normalized owners with source-specific SolutionRecipe records or inline stock definitions that match NBRC 323 and NBRC 864.
- Preserve the NBRC solution pH ranges and separate-autoclave preparation notes.
- Adjust duplicate detection so the pH-range difference prevents M1532 and M1660 from being flattened into one canonical record unless that conflict is explicitly represented.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation on the edited M1532 and M1660 normalized owners and any new SolutionRecipe records.
- Run merge freshness and merge verification after regeneration.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1532.yaml` and verify that water is 500.0, Solution A/B no longer point at MediaDive 5342/5343, and M1660 is not silently collapsed into M1532.

## Additional Notes

- Exact source inspection was bounded to TOGO M1532/M1660, NBRC pages 323/864, and the exact local `mediadive_5342_Solution_A.yaml` and `mediadive_5343_Solution_B.yaml` stock records.
- The source-specific pH differences explain why an ingredient-only fingerprint is too weak for this pair.
