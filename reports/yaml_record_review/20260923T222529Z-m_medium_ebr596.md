# YAML Record Review: m_medium_ebr596

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/m_medium_ebr596.yaml`
- Started UTC: `2026-09-23T22:25:29Z`
- Finished UTC: `2026-09-23T22:25:30Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:008322` |
| Record name | `m_medium_ebr596` |
| Original name | `M medium (EBR596)` |
| Source accession | `TOGO:M1758` |
| Source label | `M medium (EBR596)` |
| Original source | `NBRC_M970` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` |
| Generated record | `data/merge_yaml/merged/m_medium_ebr596.yaml` |

`data/merge_yaml/merged/m_medium_ebr596.yaml` is a generated merge from
`data/normalized_yaml/bacterial/m_medium_ebr596.yaml` with fingerprint
`7e6ea9a785bef0b3edfaf2bd4944259544e01db53980d8b5e90c393fd90b518e`. Future
fixes should be made in the normalized input, the TOGO/NBRC importer or
solution-migration path that populated it, and then regenerated into
`data/merge_yaml/merged/`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_medium_ebr596.yaml` | Passed. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_medium_ebr596.yaml --out /private/tmp/m_medium_ebr596.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows emitted to `/private/tmp/m_medium_ebr596.strict.tsv`. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_medium_ebr596.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_medium_ebr596.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The record identity is correct at the medium level: `CultureMech:008322`,
  `name: m_medium_ebr596`, `original_name: M medium (EBR596)`,
  `media_term.term.id: TOGO:M1758`, TOGO's `original_media_id: NBRC_M970`,
  and NBRC `NO=970` all denote the same recipe.
- An ignored-file-inclusive exact search for `CultureMech:008322`,
  `m_medium_ebr596`, and `M medium (EBR596)` across
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`
  found one live maintained input at
  `data/normalized_yaml/bacterial/m_medium_ebr596.yaml`; the older
  `TOGO_M1758_M_medium_EBR596.yaml` name appeared only in archived validation
  reports.
- TOGO M1758 supports the record as a source-distinct sibling of the preceding
  M-media: it has 2 g yeast extract, 5 g cellobiose, 10 g main-medium `NaCl`,
  and pH 6.5.
- The two solution CURIEs are unsupported. TOGO M1758 defines local
  `Vitamin solution*` and `Mineral solution**` formulas on the NBRC M970 page,
  but the YAML links to generic `mediadive.solution:6241` and
  `mediadive.solution:5281` records whose formulas do not match.
- Several ingredient groundings are broader than the source chemicals:
  `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, and `NiCl2 x 6 H2O` are grounded to
  labels that omit the hydration state, and `Ca-pantothenate` is grounded to
  pantothenate rather than an inspected calcium salt.

## Evidence

- TOGO M1758 and NBRC `NO=970` support the main-medium formula, including 1 L
  distilled water; 0.4 g `KH2PO4`; 0.4 g `K2HPO4 x 3 H2O`; 1 g `NH4Cl`;
  0.1 g `MgCl2 x 6 H2O`; 2 g yeast extract; 6 g `NaHCO3`; 0.5 g
  `Cysteine-HCl`; 0.25 g `Na2S x 9 H2O`; 1 mg resazurin; 5 g cellobiose;
  10 g `NaCl`; 10 ml `Vitamin solution*`; 10 ml `Mineral solution**`; and
  pH 6.5.
- The generated record stores `Resazurin` as `1 G_PER_L`; TOGO and NBRC state
  1 mg in the one-liter main medium.
- The generated record stores `NaCl` as `11.0 G_PER_L` after merging the
  10 g main-medium row with the 1 g row from the separate mineral stock. The
  stock is added at 10 ml/L, so its `NaCl` row is not another 1 g/L direct
  final-medium row.
- The generated record stores `Vitamin solution*` and `Mineral solution**` as
  `10 G_PER_L`. TOGO and NBRC state 10 ml additions of each stock per liter.
- The local M1758 vitamin stock has 1 L water plus ten mg-scale vitamin or
  growth-factor rows. The generated record flattens those ten stock rows into
  direct top-level final-medium `G_PER_L` rows without applying the 10 ml/L
  dilution or preserving the mg units.
- The local M1758 mineral stock has 1 L water plus eleven gram-scale mineral
  rows. The generated record flattens those eleven stock rows into direct
  top-level final-medium rows without applying the 10 ml/L dilution.
- TOGO and NBRC specify a nitrogen-gas preparation: omit `NaHCO3`,
  cysteine-HCl, sodium sulfide, and vitamin solution at first, distribute the
  medium under nitrogen gas, seal with butyl rubber stoppers, autoclave at
  121 C for 15 minutes, aseptically inject the held-back ingredients, and
  adjust pH to 6.5 with sterile 0.6 N sulfuric acid. The generated record has
  variable rows for nitrogen and sulfuric acid but no `preparation_steps` and
  no `ph_value`.
- The generated merge still stores `Distilled water` as `3.0 G_PER_L`, while
  the maintained normalized input has been repaired to `1.0 G_PER_L`.

## Completeness

- Consequentially incomplete: local `Vitamin solution*` and
  `Mineral solution**` are not represented as nested M1758 stock formulas with
  10 ml/L addition volumes.
- Consequentially incomplete: the record does not retain pH 6.5.
- Consequentially incomplete: the record does not retain the nitrogen-gas,
  sealed-vessel, autoclave, post-autoclave injection, and sulfuric-acid pH
  adjustment instructions.
- Empty optional target-organism and growth-evidence fields were not treated
  as defects; TOGO and NBRC M970 are recipe sources, not primary growth
  evidence for a named target organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Resazurin` is off by a factor of 1000 because a 1 mg source row became `1 G_PER_L`. | TOGO M1758 and NBRC `NO=970` list resazurin as 1 mg in the one-liter main medium; the generated YAML stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` or the TOGO/NBRC importer. |
| Major | A mineral-stock `NaCl` row was summed into the direct final-medium `NaCl` row. | The source has 10 g `NaCl` in the main medium and 1 g `NaCl` inside the local mineral stock; the YAML stores one `NaCl` row at `11.0 G_PER_L`. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` and the duplicate-merge or stock-flattening logic that ignored solution scope. |
| Major | Local M1758 vitamin and mineral stocks were flattened into final top-level ingredients without the 10 ml/L dilution. | The source adds 10 ml of each one-liter stock to the main medium; generated vitamin and mineral rows retain stock-strength values as final `G_PER_L` ingredients. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` and the TOGO/NBRC solution migration or flattening logic. |
| Major | The two stock additions have wrong identities and wrong units. | `Vitamin solution*` and `Mineral solution**` are local M1758 stocks added at 10 ml/L; the generated `solutions` rows use `10 G_PER_L` and point to generic `mediadive.solution:6241` and `mediadive.solution:5281` records with different formulas. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml`; remove unsupported generic MediaDive solution CURIEs unless equivalence is source-verified. |
| Major | The source pH and preparation protocol are mostly absent. | TOGO/NBRC specify pH 6.5, nitrogen gassing, butyl rubber stoppers, autoclaving at 121 C for 15 minutes, aseptic injection of NaHCO3, cysteine-HCl, sodium sulfide, and vitamin solution, and pH adjustment with sterile 0.6 N sulfuric acid. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` or the TOGO/NBRC importer fields for preparation comments. |
| Major | Several hydrated or salt-specific ingredient identities are grounded too broadly. | Source labels `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and `Ca-pantothenate` do not match the broader CHEBI labels stored for those rows. | `data/normalized_yaml/bacterial/m_medium_ebr596.yaml`; re-resolve these labels with exact hydrate and salt constraints. |
| Major | The generated merge is stale for the water duplicate repair. | The generated record still has `Distilled water` 3.0 with a merged-duplicates note; `data/normalized_yaml/bacterial/m_medium_ebr596.yaml` has already repaired that row to 1.0. | Regenerate `data/merge_yaml/merged/m_medium_ebr596.yaml` from the normalized input after source-level fixes. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/m_medium_ebr596.yaml`, or in the TOGO/NBRC
   import transform that owns it, correct the main-medium amounts: keep
   resazurin as 1 mg per source liter, keep the main-medium `NaCl` row at 10 g
   rather than adding the mineral-stock `NaCl`, keep each stock addition as
   10 ml/L, and add `ph_value: 6.5`.
2. Model the local M1758 `Vitamin solution*` and `Mineral solution**` formulas
   as local stock recipes instead of final direct ingredients. If any stock
   members are intentionally flattened, apply both the 10 ml/L dilution and the
   source mg-to-g conversion.
3. Remove the unsupported `mediadive.solution:6241` and
   `mediadive.solution:5281` links, or replace them only with identifiers for
   the exact local M1758 stock formulas.
4. Add preparation steps that preserve the source order: mix all rows except
   `NaHCO3`, cysteine-HCl, sodium sulfide, and vitamin stock; distribute under
   nitrogen gas; seal with butyl rubber stoppers; autoclave at 121 C for
   15 minutes; aseptically inject the held-back ingredients; adjust to pH 6.5
   with sterile 0.6 N sulfuric acid.
5. Re-ground `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and
   `Ca-pantothenate` against exact hydrated or salt forms, leaving unresolved
   rows explicit if exact ontology terms are unavailable.
6. Regenerate `data/merge_yaml/merged/` so the generated `m_medium_ebr596`
   record reflects the current normalized record and no longer carries the
   stale `3.0` water sum.

## Follow-up Checks

- Run open-schema validation for the corrected normalized record and the
  regenerated merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and regenerated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference validation after removing or replacing the two generic
  MediaDive solution CURIEs:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data <record> --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`.
- Run term validation after re-grounding hydrate and salt rows:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data <record> -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`.
- Manually compare the regenerated record against TOGO M1758 or NBRC `NO=970`
  and verify that the main formula, both stock formulas, 10 ml/L stock
  additions, pH 6.5, and nitrogen preparation instructions are preserved
  without flattening stock formulas at stock strength.

## Additional Notes

- The focused validators pass because the stock-scope NaCl merge, wrong stock
  references, stale water sum, and stock-strength flattened ingredients are
  valid YAML and valid LinkML.
