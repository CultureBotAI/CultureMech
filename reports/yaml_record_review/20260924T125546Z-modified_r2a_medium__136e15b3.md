# YAML Record Review: modified_r2a_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/modified_r2a_medium__136e15b3.yaml
- Started UTC: 2026-09-24T12:55:46Z
- Finished UTC: 2026-09-24T12:56:26Z
- Verdict: needs curation

## Target

- Reviewed merged record `CultureMech:003110`, `modified_r2a_medium`, generated from `data/normalized_yaml/bacterial/modified_r2a_medium.yaml`.
- The record represents MediaDive `J769`, sourced from JCM `GRMD=769`, named `MODIFIED R2A MEDIUM`.
- The generated MediaDive record was compared with MediaDive `J769`, JCM `GRMD=769`, and the parallel TOGO `M796` import of the same JCM source.

## Validation

- LinkML open-schema validation: passed; `linkml-validate` reported no issues.
- Strict validation: passed; `scripts/validate_strict.py` reported 1 file scanned, 0 files with errors, and 0 total error rows.
- Reference validation: passed; `linkml-reference-validator` checked 1 file and reported 0 reference checks and no failures.
- Term validation: passed; `linkml-term-validator` exited 0 and printed `Validation passed`.
- Embedded `curation_history` entries were not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

- MediaDive `J769`, TOGO `M796`, and JCM `GRMD=769` all identify the same Modified R2A Medium recipe.
- A gitignore-independent exact search for the JCM/MediaDive identifiers found a parallel TOGO maintained record, `data/normalized_yaml/bacterial/TOGO_M796_Modified_R2A_Medium.yaml`, and a separate generated record, `data/merge_yaml/merged/MODIFIED_R2A_MEDIUM.yaml`, for the same JCM 769 source.
- JCM `GRMD=769` is also used by TOGO `M797` with original source `JCM_M769-2`; that branch appears to be a distinct variant and was not treated as a duplicate of this record.
- `Yeast extract`, `Proteose peptone no. 3`, and `Casamino acids` are ungrounded.
- No inspected source payload identified a target organism for this medium.

## Evidence

- JCM lists 0.5 g Yeast extract, 0.5 g Proteose peptone No. 3, 0.5 g Casamino acids, 0.5 g Glucose, 0.5 g Sodium pyruvate, 3.0 g Trisodium citrate, 0.3 g K2HPO4, 20 g MgSO4 x 7 H2O, and 230 g NaCl.
- JCM instructs adding components to distilled water and bringing the volume to 1.0 L, then adjusting pH to 7.0-7.2.
- JCM notes that solid medium should be prepared by adding 18.0 g/L washed agar.

## Completeness

- All nine listed ingredients are present at the JCM numeric values.
- The pH range is represented as `ph_value: 7.1` and preserved in the preparation text.
- The optional 18.0 g/L washed agar variant is preserved in preparation text.
- The recipe duplicates the parallel TOGO `M796` generated record for the same JCM source.

## Findings

- Major: the same JCM 769 medium is maintained and generated twice, once through MediaDive `J769` and once through TOGO `M796`.
- Minor: the undefined BD-Difco ingredients are ungrounded in the MediaDive branch.

## Recommended Edits

- Merge or explicitly cross-link the TOGO `M796` and MediaDive `J769` maintained records before regeneration so JCM `GRMD=769` has one CultureMech identity.
- Add suitable ontology or product grounding for the Yeast extract, Proteose peptone No. 3, and Casamino acids rows where stable terms exist.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validation after duplicate consolidation.
- Recompare the regenerated record against JCM `GRMD=769`, especially the intentionally high 20 g/L MgSO4 x 7 H2O and 230 g/L NaCl values.
- Confirm that generated YAML no longer contains both `MODIFIED_R2A_MEDIUM.yaml` and `modified_r2a_medium__136e15b3.yaml` as separate records for the same JCM recipe.

## Additional Notes

None found.
