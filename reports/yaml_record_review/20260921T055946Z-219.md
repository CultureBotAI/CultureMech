# YAML Record Review: 219

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/219.yaml`
- Started UTC: 2026-09-21T05:58:45Z
- Finished UTC: 2026-09-21T05:59:46Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007478` |
| Name | `219` |
| Original name | `219` |
| Category | `bacterial` |
| Source term | Missing in generated artifact; maintained owner now uses `nbrc.medium:214` |
| Merge fingerprint | `6ececfc21ab8a8f4ff941c4f32ed07897b50d8dfdd938113b23dad258240d87a` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_219.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 214 skim-milk record. The maintained owner was repaired on 2026-08-19 and 2026-09-07, so this generated artifact is stale relative to the source record that owns future fixes.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/219.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/219.yaml --out /private/tmp/219.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/219.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/219.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The free-text description and import history identify the source as NBRC Medium 214, but the generated record lacks a structured `media_term`. The live NBRC Medium 214 page resolves and supports the two-row skim-milk formulation.

Grounding is stale-incomplete. The generated artifact has no water row and no grounded ingredients. The maintained owner now restores `Distilled water` with `CHEBI:15377` and intentionally leaves `Skim milk` ungrounded because it is a food mixture.

## Evidence

The inspected NBRC Medium 214 page supports exactly two ingredients:

- 100 g Skim milk.
- 1 L Distilled water.

The generated artifact has neither row correctly represented. It has one concatenated `Skim milk100gDistilled water1L` variable row and a second row with an empty preferred term and `value: Skim milk`. It also carries generic pH and autoclave preparation steps that NBRC Medium 214 does not state.

## Completeness

Consequential gaps:

- The two source ingredient rows are not represented as `Skim milk` at 100 g and `Distilled water` at 1 L.
- The second generated ingredient row has an empty label and a nonnumeric concentration value.
- The generated record lacks the maintained owner's structured `nbrc.medium:214` media term, source reference, `ingredients_curated` flag, and recovered composition history.
- The generated preparation steps are placeholders unsupported by NBRC Medium 214.

Empty optional slots:

- `target_organisms`, `growth_metrics`, `variants`, pH, and source-specific sterilization are correctly absent in the maintained owner. The inspected NBRC page verifies only the recipe rows.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` for `CultureMech:007478`, `NBRC_219`, `NO=214`, the `6ececfc2` merge fingerprint, and `Skim milk` found the expected normalized owner, expected generated merge, source index rows, this NBRC Medium 214 URL reused by other records, and unrelated skim-milk media.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact still contains the original malformed NBRC composition parse. | NBRC Medium 214 lists `Skim milk` at 100 g and `Distilled water` at 1 L; the generated merge stores `Skim milk100gDistilled water1L` as one variable ingredient plus a second empty-label row with `value: Skim milk`. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_219.yaml` |
| Major | The generated artifact carries unsupported placeholder preparation. | NBRC Medium 214 does not state pH adjustment or autoclaving, while the generated merge has generic `Adjust pH if needed` and `Sterilize by autoclaving` steps. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_219.yaml` |
| Minor | Structured source provenance is stale-missing. | The maintained owner now has `media_term: nbrc.medium:214`, a structured NBRC reference, and curation flags; the generated merge still has only the old free-text import note, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/NBRC_219.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `219.yaml` receives the maintained owner's recovered `Skim milk` and `Distilled water` rows, NBRC Medium 214 source term, structured reference, curated flags, and removal of unsupported generic preparation steps.
2. After regeneration, re-run focused schema, strict, term, and reference validation on `data/merge_yaml/merged/219.yaml`.

## Follow-up Checks

- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- `just validate-strict` and `just validate-terms` on the regenerated `219.yaml`.
- `just validate-references` on the regenerated merge to confirm the structured NBRC reference resolves.
- Manual comparison against NBRC Medium 214 for exactly 100 g skim milk and 1 L distilled water, with no pH or sterilization statements added.

## Additional Notes

- The maintained normalized owner is already repaired; no additional normalized edit was identified for NBRC Medium 214 in this review.
- `linkml-reference-validator` performed zero checks because this generated record has no structured `references` entries.
