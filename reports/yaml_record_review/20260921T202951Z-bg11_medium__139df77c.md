# YAML Record Review: bg11_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bg11_medium__139df77c.yaml
- Started UTC: 2026-09-21T20:28:19Z
- Finished UTC: 2026-09-21T20:29:53Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/bg11_medium__139df77c.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bg11_medium.yaml` |
| ID | `CultureMech:009170` |
| name / original_name | `bg11_medium` / `BG11 medium` |
| source identity | `TOGO Medium M2601`, `TOGO:M2601` |
| source URL in record | `https://togomedium.org/medium/M2601` |
| merge fingerprint | `139df77c13acb668dfa116bf275ba8f622c4ef959347a19b6e76fd5c61ec0831` |
| merged_from | `bg11_medium` |

The reviewed file is a generated one-source merge of
`data/normalized_yaml/bacterial/bg11_medium.yaml`. A `find` search under
`reports/yaml_record_review` found no existing `*-bg11_medium__139df77c.md`
report; `find` includes ignored files.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bg11_medium__139df77c.yaml` | Passed |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bg11_medium__139df77c.yaml --out /private/tmp/bg11_medium__139df77c.strict.tsv --workers 1 --quiet` | Passed |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bg11_medium__139df77c.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with no diagnostics |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bg11_medium__139df77c.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| TOGO source fetch | `curl -L -o /private/tmp/togo_M2601.json 'https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2601'` | Passed and returned `gm = http://togomedium.org/medium/M2601`, `name = BG11 medium`, and `ph = 7.4` |
| Embedded `curation_history` validation | Not checked: this repository documents `just validate-history` for standalone `history/` records, not for embedded `MediaRecipe.curation_history` entries in one generated merge |

## Identity and Grounding

The generated record consistently denotes TOGO M2601. `data/culturemech_id_registry.tsv`
maps `CultureMech:009170` to `data/normalized_yaml/bacterial/bg11_medium.yaml`,
and the generated source indexes also list `source_id: TOGO:M2601` for that
record.

An exact gitignore-independent search for `CultureMech:009170`, `TOGO:M2601`,
and the merge fingerprint across `data` and `reports/yaml_record_review` found
the maintained owner, generated merge, generated indexes, and import
diagnostics for this same record. It found no competing normalized record with
this CultureMech ID.

Several grounded ingredient labels agree with TOGO's GMO labels, including
magnesium sulfate heptahydrate, calcium chloride dihydrate, sodium nitrate,
citric acid, sodium molybdate dihydrate, manganese chloride tetrahydrate, zinc
sulfate heptahydrate, copper sulfate pentahydrate, and cobalt nitrate
hexahydrate. Two identity problems remain:

- TOGO reports `K2HPO4.3H2O` as `GMO_001125`, label "Potassium phosphate
  dibasic trihydrate"; the YAML grounds it to `CHEBI:131527`, dipotassium
  hydrogen phosphate, without preserving the trihydrate identity.
- TOGO reports `EDTA (disodium magnesium salt)` as `GMO_001958`, label "EDTA
  disodium magnesium salt"; the YAML leaves it ungrounded.

## Evidence

The TOGO API represents M2601 in two formulation blocks:

| TOGO block | Source content |
|---|---|
| main medium | `MgSO4.7H2O` 0.075 g, `CaCl2. 2H2O` 0.036 g, `Na2CO3` 0.02 g, `NaNO3` 1.5 g, `K2HPO4.3H2O` 0.04 g, ferric ammonium citrate 0.006 g, deionized water 1000 ml, citric acid 0.006 g, `EDTA (disodium magnesium salt)` 0.001 g, and `Trace metal mix A5 + Co` 1 ml |
| `Trace metal mix A5 + Co` | `Na2MoO4. 2H2O` 0.39 g, `H3BO3` 2.86 g, `MnCl2.4H2O` 1.81 g, `ZnSO4.7H2O` 0.222 g, `CuSO4 .5H2O` 0.079 g, `Co(NO3)2 . 6H2O` 0.0494 g, and deionized water 1000 ml |

The YAML preserves the main-medium gram amounts for magnesium sulfate, calcium
chloride, sodium carbonate, sodium nitrate, ferric ammonium citrate, citric
acid, and EDTA. It does not preserve the stock boundary for `Trace metal mix
A5 + Co`: TOGO adds that stock at 1 ml in the main medium, but the YAML stores
`Trace metal mix A5 + Co` as a `1 G_PER_L` root ingredient and also stores the
trace-stock salts at their stock gram amounts as root final-medium ingredients.

TOGO stores `ph: 7.4`, and the source comment further scopes that value as pH
after autoclaving and cooling. The YAML has no `ph_value`.

There is no literature `evidence` block or target-organism claim to inspect.

## Completeness

The generated merge is stale. Its maintained owner contains a 2026-09-02
`REPAIRED_SUMMED_DUPLICATE_MERGE` curation event that collapsed `Deionized
water` from a duplicated `2000.0` back to `1000.0`; the generated merge still
contains `2000.0 G_PER_L` and lacks the 2026-09-02 event.

The maintained owner is still incomplete after that repair:

- 1000 ml of main-medium water is represented as `1000.0 G_PER_L`.
- The 1 ml `Trace metal mix A5 + Co` stock addition is represented as
  `1 G_PER_L`.
- The trace stock lacks a solution boundary, so its components are inflated to
  stock-strength root ingredients.
- `ph_value: 7.4` is absent.
- `EDTA (disodium magnesium salt)` remains ungrounded.

Empty growth-evidence, target-organism, storage, and shelf-life slots are
acceptable for the inspected TOGO formulation source.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The generated merge is stale relative to its normalized owner. | The normalized owner has a 2026-09-02 duplicate-water repair and `Deionized water: 1000.0`; the generated merge still has the old `2000.0` value and no repair event. | Regenerate `data/merge_yaml/merged/bg11_medium__139df77c.yaml` |
| major | The A5 + Co trace-metal stock is flattened into the final medium. | TOGO M2601 adds 1 ml of `Trace metal mix A5 + Co`; the YAML records `Trace metal mix A5 + Co` as `1 G_PER_L` and also records `Na2MoO4`, `H3BO3`, `MnCl2`, `ZnSO4`, `CuSO4`, and `Co(NO3)2` stock amounts as root ingredients. | `data/normalized_yaml/bacterial/bg11_medium.yaml` or the TOGO importer |
| major | The source pH is dropped. | The fetched TOGO M2601 payload has `meta.ph = 7.4` and a comment saying the pH after autoclaving and cooling is 7.4; the YAML has no `ph_value`. | `data/normalized_yaml/bacterial/bg11_medium.yaml` or the TOGO importer |
| major | `K2HPO4.3H2O` is grounded to an anhydrous phosphate term. | TOGO identifies the component as potassium phosphate dibasic trihydrate; the YAML uses `CHEBI:131527` for dipotassium hydrogen phosphate. | `data/normalized_yaml/bacterial/bg11_medium.yaml` |
| minor | `EDTA (disodium magnesium salt)` is ungrounded despite a precise TOGO GMO identity. | TOGO provides `GMO_001958`, label "EDTA disodium magnesium salt"; the imported row has no `term` or MIM link. | `data/normalized_yaml/bacterial/bg11_medium.yaml` |
| minor | BG11 is classified as `COMPLEX` / `UNDEFINED` even though TOGO lists only defined chemical components and water. | Both fetched M2601 formulation blocks are defined salts, small molecules, or water. | `data/normalized_yaml/bacterial/bg11_medium.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/bg11_medium__139df77c.yaml` so it picks up
   the 2026-09-02 normalized repair before assessing merge-fingerprint behavior.

2. Repair `data/normalized_yaml/bacterial/bg11_medium.yaml` or the TOGO importer
   so `Trace metal mix A5 + Co` is a stock solution with a 1 ml final-medium
   addition and a populated trace-salt composition.

3. Move the six trace salts out of root `ingredients`; retain them in the A5 +
   Co stock composition or explicitly calculate their 1 ml/L direct-equivalent
   final concentrations.

4. Convert the two deionized-water rows from concentrations into main-medium
   and stock make-up volumes.

5. Add `ph_value: 7.4` with a note that TOGO scopes it to the post-autoclave,
   post-cooling medium.

6. Reground `K2HPO4.3H2O` to the exact trihydrate, and preserve
   `EDTA (disodium magnesium salt)` as unresolved only if no exact chemical
   grounding is available.

7. Revisit `medium_type` and `composition_type` after the trace mix is modeled
   as a defined stock solution.

## Follow-up Checks

- Rerun open-schema, strict, term, and reference validation on the repaired
  normalized owner and on regenerated `data/merge_yaml/merged/bg11_medium__139df77c.yaml`.
- Run `just verify-merges` and `just audit-merge-freshness` to prove the
  generated merge includes the 2026-09-02 owner repair and any new curation
  event.
- Fetch TOGO M2601 and compare the ten main-medium rows plus the seven A5 + Co
  rows against the normalized YAML.
- Re-run exact gitignore-independent searches for `CultureMech:009170` and
  `TOGO:M2601` after regeneration to verify indexes still map the ID and source
  to `data/normalized_yaml/bacterial/bg11_medium.yaml`.

## Additional Notes

The gitignore-independent exact search for `M2601` and `TOGO:M2601` covered
`data/raw`, `data/normalized_yaml`, `data/import_tracking`, and
`data/merge_yaml`. It found the maintained owner, generated merge, generated
indexes, and import diagnostics for M2601; no raw TOGO M2601 payload was checked
in under `data/raw`.

The current source URL in the YAML is a browser route that returns a shell to
non-browser fetches. The inspected source data came from TOGO's JSON endpoint:
`https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2601`.
