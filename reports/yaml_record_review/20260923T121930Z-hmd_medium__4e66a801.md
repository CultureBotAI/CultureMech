# YAML Record Review: HMD MEDIUM
- Repository: CultureMech
- Record: data/merge_yaml/merged/hmd_medium__4e66a801.yaml
- Started UTC: 2026-09-23T12:18:45Z
- Finished UTC: 2026-09-23T12:19:30Z
- Verdict: needs curation

## Target

Reviewed the generated direct DSMZ branch for DSMZ Medium 1356, `HMD MEDIUM`, at `data/merge_yaml/merged/hmd_medium__4e66a801.yaml`. The maintained source is `data/normalized_yaml/bacterial/hmd_medium.yaml`.

## Validation

- Open LinkML validation: passed for `MediaRecipe`.
- Strict validation: passed with zero error rows in `/private/tmp/hmd_medium__4e66a801.strict.tsv`.
- Reference validation: passed with zero checks.
- Term validation: passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone YAML files under `history/`, not embedded `MediaRecipe.curation_history` entries in generated merge artifacts.

## Identity and Grounding

The record is grounded to `mediadive.medium:1356`, DSMZ Medium 1356 `HMD MEDIUM`, with the correct pH of 7.5.

## Evidence

DSMZ 1356 defines HMD MEDIUM as a 1000 ml Mineral Salts Solution with yeast extract, casamino acids, CaCl2 x 2 H2O, MgCl2 x 6 H2O, K2SO4, NH4Cl, KH2PO4, NaCl, and distilled water. It then instructs curators to adjust to pH 7.5 and autoclave, and states that the medium may be solidified by adding 12 g/L agar.

The generated record keeps all non-water Mineral Salts Solution amounts and the pH/preparation text. MediaDive exposes agar with `condition: for solid medium`, but the generated record sets `physical_state: SOLID_AGAR` and places 12 G_PER_L agar in the ordinary top-level ingredient list.

## Completeness

The only composition row missing from the source solution is the 1000 ml distilled-water row. The conditional nature of agar is preserved only in a free-text note on an unconditional agar ingredient and in the preparation prose.

## Findings

- DSMZ treats agar as an optional solidification addition, but the record makes the whole medium `SOLID_AGAR`.
- The 12 G_PER_L agar row is unconditional despite the source condition `for solid medium`.
- The 1000 ml distilled-water row is missing.

## Recommended Edits

- Change `data/normalized_yaml/bacterial/hmd_medium.yaml` so the basal Mineral Salts Solution is not forced to `SOLID_AGAR`.
- Represent agar as a conditional solidification option rather than an unconditional ingredient, or split the liquid and solid variants explicitly.
- Restore the 1000 ml distilled-water row.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation after regeneration.
- Re-open DSMZ 1356 and confirm the regenerated recipe distinguishes the basal mineral salts solution from the optional 12 g/L agar solidification step.

## Additional Notes

None.
