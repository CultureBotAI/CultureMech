# YAML Record Review: Luria Broth (LB)

- Repository: CultureMech
- Record: `data/merge_yaml/merged/luria_broth_lb.yaml`
- Started UTC: `2026-09-23T20:36:05Z`
- Finished UTC: `2026-09-23T20:37:40Z`
- Verdict: needs curation

## Target

- `id`: `CultureMech:009651`
- `name`: `luria_broth_lb`
- `original_name`: `Luria Broth (LB)`
- `category`: `bacterial`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `LIQUID`
- `media_term`: `TOGO:M3210`
- `merge_fingerprint`: `55bc28fe50d05390223761810d25455f79ada3525d5b0aac2dd8bff18757da3d`
- `merged_from`: `luria_broth_lb`

## Validation

- Open schema validation passed with `linkml-validate`.
- Strict schema validation passed with 0 error rows written to `/private/tmp/luria_broth_lb.strict.tsv`.
- LinkML reference validation passed with 0 checks.
- LinkML term validation passed.
- Embedded `curation_history` objects were not checked: the available history validator checks standalone `history/` files, not merged `MediaRecipe.curation_history` entries.

## Identity and Grounding

- An ignored-inclusive exact search for `CultureMech:009651`, `TOGO:M3210`, `M3210`, `luria_broth_lb`, and the merge fingerprint found the maintained TOGO M3210 owner and this generated record.
- TOGO M3210 identifies the source as `Luria Broth (LB)`.
- TOGO M3210 lists 5 g yeast extract, 10 g triptone, 5 g NaCl, and 1 L deionized water.
- The TOGO comment says pH was set to 7.0 with NaOH and HCl for cultivation of `C. necator`.

## Evidence

- The generated record stores 5 `G_PER_L` yeast extract, 10 `G_PER_L` `Triptone`, and 5 `G_PER_L` NaCl, matching the TOGO masses per liter.
- The generated record stores TOGO's 1 L deionized-water row as 1 `G_PER_L`.
- The generated HCl and NaOH rows have `variable` / `VARIABLE` concentrations under `ingredients`.
- The source pH 7.0 endpoint is absent from the generated record.

## Completeness

- The three nutrient and salt ingredient amounts match the TOGO source.
- The pH adjustment reagents are present but are not linked to an adjustment step or to pH 7.0.
- The source final-volume water is present with the wrong dimension.
- The TOGO `Triptone` row remains ungrounded.
- The generated record has no structured `references`.

## Findings

1. The pH adjustment is not modeled.
   - Evidence: TOGO M3210 says pH was set to 7.0 with NaOH and HCl, but the generated record has no `ph_value`; HCl and NaOH are represented only as variable-concentration ingredient rows.
   - Impact: the final pH and the purpose of the acid/base solutions are lost.

2. The source solvent row has the wrong unit.
   - Evidence: TOGO lists deionized water as 1 L, while the generated record stores `Deionized water` with `value: '1'` and `unit: G_PER_L`.
   - Impact: the final recipe volume is represented as an impossible water mass concentration.

3. The source triptone row is not ontology-grounded.
   - Evidence: `Triptone` is present at the right amount but has no `term` or `mediaingredientmech_chebi_term`.
   - Impact: the row cannot be queried with the rest of the tryptone-grounded LB records.

4. Structured source references are absent.
   - Evidence: TOGO M3210 is stored only in `media_term` and a free-text `notes` URL.
   - Impact: reference validation has no source URL to check.

## Recommended Edits

1. Preserve pH 7.0 as `ph_value` and move HCl/NaOH into an `ADJUST_PH` preparation step.
2. Represent the 1 L deionized-water source row as final volume, not 1 `G_PER_L`.
3. Ground `Triptone` to the same tryptone term used by other LB records, while preserving the source spelling if needed.
4. Add a structured reference for TOGO M3210.
5. Preserve the `C. necator` cultivation context if the target-organism schema can represent it.

## Follow-up Checks

- Re-fetch TOGO M3210 and confirm the regenerated record keeps 5 g/L yeast extract, 10 g/L tryptone, 5 g/L NaCl, and pH 7.0.
- Re-run open schema, strict schema, reference, and term validation on the rebuilt record.
- Run an ignored-inclusive exact search for `TOGO:M3210` and `luria_broth_lb` to confirm no duplicate TOGO M3210 owner was introduced.

## Additional Notes

- Exact duplicate checks included ignored and hidden files via `rg --no-ignore --hidden` and were scoped to `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive`.
