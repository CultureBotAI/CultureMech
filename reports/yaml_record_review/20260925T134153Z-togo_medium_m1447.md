# YAML Record Review: togo_medium_m1447

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1447.yaml`
- Started UTC: 2026-09-25T13:41:53Z
- Finished UTC: 2026-09-25T13:41:53Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1447, imported from NBRC medium 219.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1447.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1447`; the TOGO API identifies NBRC_M219 as its original medium ID and links NBRC medium 219.

The TOGO payload and NBRC page agree on 20 g Bacto Casitone, 1 g MgSO4 x 7 H2O, 1 L potassium phosphate buffer at 0.01 M and pH 7.2, and 15 g agar if needed.

The normalized source YAML already contains the September 2026 NBRC 219 repair, but the generated merged YAML was last rebuilt from an older source snapshot.

## Evidence

The generated Bacto Casitone, MgSO4 x 7 H2O, and agar rows match the source amounts.

The generated merge still represents the source 1 L potassium phosphate buffer row as 1 g/L and still carries the false `mediadive.medium:790` match that the normalized source removed.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated merge omits the source references and preparation steps present in the repaired normalized source.

## Findings

- Major issue: the generated merge is stale relative to `data/normalized_yaml/bacterial/togo_medium_m1447.yaml` and does not include the September 2026 NBRC 219 repair.
- Major issue: 1 L potassium phosphate buffer is represented as 1 g/L.
- Major issue: `kg_microbe_match: mediadive.medium:790` remains in the generated merge despite being removed from the repaired normalized source.
- Minor issue: preparation and source-reference fields present in the normalized source are missing from the generated merge.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/togo_medium_m1447.yaml` from the current normalized TOGO M1447 source.
- Confirm the regenerated record preserves the 1000 ml/L potassium phosphate buffer row, drops the false MediaDive match, and keeps the source references and preparation steps.
- Do not manually patch the generated merge; keep any remaining formulation curation in `data/normalized_yaml/bacterial/togo_medium_m1447.yaml`.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge.
- Confirm the regenerated fingerprint and `MERGED_RECIPES` history entry correspond to the repaired normalized source, not the stale August 2026 merge.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1447 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
