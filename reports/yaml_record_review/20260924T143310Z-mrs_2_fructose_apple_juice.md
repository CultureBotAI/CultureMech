# YAML Record Review: MRS + 2% Fructose + Apple Juice

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml
- Started UTC: 2026-09-24T14:31:30Z
- Finished UTC: 2026-09-24T14:33:10Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009564 |
| Name | mrs_2_fructose_apple_juice |
| Original name | MRS + 2% Fructose + Apple Juice |
| Category | bacterial |
| Media term | TOGO:M3050 |
| Source | NBRC_M1600, NBRC medium 1600 |
| Reviewed path | data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml |
| Maintained source | data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml |
| Generated status | Generated by merge_recipes.py on 2026-08-06 |

This is a generated single-source merge output. `merged_from` contains only
`mrs_2_fructose_apple_juice`, and the `merge_fingerprint` does not collapse it
with the base 1%, 2%, or 10% fructose variants.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml` exited 0 with no diagnostics under Python 3.11. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml --out /private/tmp/mrs_2_fructose_apple_juice.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The record passes schema, strict, reference, and term validation despite the
source-translation issues below.

## Identity and Grounding

- `media_term.id` is `TOGO:M3050`, `media_term.label` is
  `MRS + 2% Fructose + Apple Juice`, and the notes point to NBRC medium 1600.
  TOGO M3050 reports the same name, `original_media_id: NBRC_M1600`, and the
  same NBRC URL; NBRC medium 1600 is the same medium.
- The generated record has the correct MRS fructose amount for the named
  medium: TOGO M3050 and NBRC 1600 both list `Fructose` at 20 g.
- The generated record preserves the extra `L-Cysteine` row at 1 g and the
  `Apple Juice` row value at 200, but it changes the apple-juice unit from ml
  to `G_PER_L`.
- The exact `L-Cysteine` source row is grounded to CHEBI:17561
  `L-cysteine`; that is consistent with the label.

## Evidence

TOGO M3050 and NBRC 1600 agree on these source rows:

| Ingredient | Source amount | YAML amount |
| --- | --- | --- |
| Peptone | 10 g | 10 G_PER_L |
| Meat extract | 10 g | 10 G_PER_L |
| Yeast extract | 5 g | 5 G_PER_L |
| Glucose | 20 g | 20 G_PER_L |
| Fructose | 20 g | 20 G_PER_L |
| Tween 80 | 1 g | 1 G_PER_L |
| K2HPO4 | 2 g | 2 G_PER_L |
| Sodium acetate | 5 g | 5 G_PER_L |
| Diammonium hydrogen citrate | 2 g | 2 G_PER_L |
| MgSO4 x 7H2O | 0.2 g | 0.2 G_PER_L |
| MnSO4 x nH2O | 0.05 g | 0.05 G_PER_L |
| Apple Juice | 200 ml | 200 G_PER_L |
| L-Cysteine | 1 g | 1 G_PER_L |
| Distilled water | 1 L | 1 G_PER_L |
| Agar, if needed | 15 g | 15 G_PER_L |

The gram rows were normalized consistently. The apple-juice and water rows were
volumetric source rows and need a representation that preserves volume rather
than mass concentration.

## Completeness

- The source pH, `6.0`, is absent from the YAML.
- The NBRC comment requiring sterile filtration to prevent apple-juice
  caramelization is absent from the YAML.
- The source marks agar as conditional with `if needed`; the generated record
  stores the medium as `physical_state: SOLID_AGAR` and makes agar an ordinary
  ingredient.
- `Agar (if needed)` has no `term` or `mediaingredientmech_chebi_term`
  grounding.
- Empty optional fields are not defects.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `TOGO:M3050`, `NBRC_M1600`, `CultureMech:009564`, and
  `mrs_2_fructose_apple_juice` found one maintained source record, this
  generated record, and generated index entries only.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The apple-juice volume was cast to a mass concentration. | TOGO M3050 and NBRC 1600 specify `Apple Juice` as 200 ml; the YAML stores `200 G_PER_L`. | TOGO/NBRC import normalization for `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |
| major | The imported water amount uses the wrong unit. | The source row is `Distilled water` 1 L; the YAML stores `Distilled water` as `1 G_PER_L`. | TOGO/NBRC import normalization for `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |
| major | The record drops the source pH. | TOGO M3050 reports pH `6.0`, and NBRC 1600 lists pH 6.0 in the composition table; the YAML has no pH field or note. | `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |
| major | The sterile-filtration instruction is missing. | NBRC 1600 states that the medium must be sterile filtered to prevent caramelization of apple juice, and TOGO M3050 preserves that comment. The YAML has no preparation note. | `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |
| major | Conditional agar is promoted to a mandatory solid formulation. | NBRC and TOGO phrase the agar row as `Agar (if needed)`; the YAML sets `physical_state: SOLID_AGAR` and stores agar as an ordinary ingredient. | `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |
| major | The agar component is ungrounded. | `Agar (if needed)` has a solidifying role note but no `term` or `mediaingredientmech_chebi_term`. | CHEBI enrichment for `data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` |

## Recommended Edits

1. Preserve `Apple Juice` as a 200 ml volume addition, or model it in another
   explicit non-`G_PER_L` representation supported by the schema.
2. Preserve `Distilled water` as 1 L, or omit solvent rows from normalized
   concentration rows instead of casting liters to grams per liter.
3. Preserve pH 6.0 on the maintained normalized source record.
4. Preserve the apple-juice sterile-filtering instruction as preparation
   evidence or a preparation note on the maintained normalized source record.
5. Rework `Agar (if needed)` so the optional solidifying additive stays
   conditional and does not force the base medium to `SOLID_AGAR`.
6. Add agar ontology grounding during enrichment when a conditional agar row is
   retained.
7. Regenerate the merged YAML from the maintained normalized record.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on
  `data/merge_yaml/merged/mrs_2_fructose_apple_juice.yaml` after regeneration.
- Manually compare the regenerated record against TOGO M3050 and NBRC 1600 to
  confirm apple juice remains a volume row, water remains a volume row or is
  omitted, pH 6.0 is present, and the sterile-filtering instruction is present.
- Search with `rg --no-ignore --hidden` for `TOGO:M3050`, `NBRC_M1600`,
  `CultureMech:009564`, and `mrs_2_fructose_apple_juice` across
  `data/normalized_yaml` and `data/merge_yaml` to confirm regeneration does not
  create an unintended duplicate or merge synonym.

## Additional Notes

The generated record is not stale relative to
`data/normalized_yaml/bacterial/mrs_2_fructose_apple_juice.yaml` for the
inspected fields. The source-unit and missing-comment defects are already
present in the maintained normalized YAML.
