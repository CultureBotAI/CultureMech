# YAML Record Review: marine_srb_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/marine_srb_medium__21305457.yaml
- Started UTC: 2026-09-24T00:08:28Z
- Finished UTC: 2026-09-24T00:09:17Z
- Verdict: needs curation

## Target

- Reviewed generated record: `data/merge_yaml/merged/marine_srb_medium__21305457.yaml`
- Maintained upstream owner: `data/normalized_yaml/bacterial/marine_srb_medium.yaml`
- Class: `MediaRecipe`
- Stable ID: `CultureMech:007665`
- Source identity: `TOGO:M1142`
- Original source in record notes: `JCM_M1073`
- Label: `Marine SRB Medium`
- Generated status: single-source merge of `marine_srb_medium.yaml` with
  fingerprint `21305457d69f89b3458e0f2952a05478f4a40f9209f1247add12cc1258e766b5`.

## Validation

| Check | Result |
|---|---|
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/marine_srb_medium__21305457.yaml` | Passed; printed `No issues found`. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/marine_srb_medium__21305457.yaml --out /private/tmp/marine_srb_medium__21305457.strict.tsv --workers 1 --quiet` | Passed; summary reported 1 scanned file, 0 files with errors, and 0 total error rows. The TSV had one header line only. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/marine_srb_medium__21305457.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 total reference checks. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/marine_srb_medium__21305457.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after a non-fatal `eutils`/`pkg_resources` deprecation warning. |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone files under `history/`, not embedded recipe events. |

## Identity and Grounding

- `TOGO:M1142` resolves to `Marine SRB Medium`, with `original_media_id:
  JCM_M1073` and source URL `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1073`.
- The live JCM URL for `GRMD=1073` now returns no medium record, so JCM identity
  was verified through TOGO `M1142` and MediaDive `J1073`, both of which preserve
  the same original JCM formula.
- MediaDive REST `J1073` identifies the same `MARINE SRB MEDIUM`, source `JCM`,
  and JCM URL as the sibling
  `data/normalized_yaml/specialized/marine_srb_medium.yaml`
  (`CultureMech:015379`; `mediadive.medium:J1073`).
- The top-level salts, water, resazurin, carbon dioxide, and nitrogen CURIEs all
  pass OBO term validation.

## Evidence

- Supported:
  - The `TOGO:M1142` record identity, JCM provenance, and main component list
    are supported by TOGO's `gmdb_medium_by_gmid?gm_id=M1142` API response.
  - The same formula is preserved by MediaDive REST medium `J1073`.
  - TOGO supports 950 ml distilled water, 0.5 g yeast extract, 13.5 g NaCl,
    0.15 g CaCl2 x 2 H2O, 0.2 g KH2PO4, 0.3 g NH4Cl, 1 mg resazurin, 2.2 g
    MgCl2 x 6 H2O, 0.5 g KCl, 3 g Na2SO4, and 3 ug Na2SeO3 in the initial
    solution.
- Unsupported or misrepresented:
  - `Resazurin` and `Na2SeO3` were inflated during import: the source has 1 mg
    resazurin and 3 ug Na2SeO3, while the record stores both numeric values as
    `G_PER_L`.
  - The six `solutions` entries have empty `composition` arrays and use
    `G_PER_L` for source additions measured in ml. TOGO and MediaDive both
    represent these as 1 ml FeCl2 solution, 1 ml trace element solution, 10 ml
    trace vitamins, 30 ml of 8% NaHCO3 solution, 20 ml of 10% L-sodium lactate,
    and 6 ml of 5% Na2S x 9 H2O solution.
  - N2 and CO2 are represented as variable-concentration ingredients, but the
    source uses them to describe the N2-CO2 atmosphere for autoclaving and
    dispensing.
  - TOGO preserves two preparation comments covering N2-CO2 atmosphere,
    post-autoclave stock additions, butyl rubber stoppers, and pre-inoculation
    Na2S x 9 H2O addition; the reviewed record has no `preparation_steps`.
  - The record is unlinked from the MediaDive `J1073` sibling despite sharing
    the same JCM source formula.

## Completeness

- The top-level ingredient list is structurally incomplete because the three
  cross-referenced stock solutions from JCM M180 and M190 are present only as
  empty placeholders.
- Concentration completeness is compromised by the mg/ug scaling errors and by
  treating ml stock additions as grams per liter.
- Procedure coverage is incomplete: the required anaerobic preparation and stock
  addition order are absent.
- Duplicate/variant coverage is incomplete: no parent, child, or synonym edge
  connects the TOGO and MediaDive JCM 1073 records.
- A gitignore-independent exact scan of the reviewed normalized and merged YAML
  files found no `preparation_steps`, `sources`, `source_data`, `references`,
  `target_organisms`, `growth_metrics`, `parent_media`, `variant_children`, or
  `variant_relationship` fields in the current target.
- Empty target-organism and growth-metric slots are acceptable for this source
  recipe because the inspected TOGO and MediaDive source records are medium
  formulas, not primary growth experiments.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | Two mass quantities were scaled into grams per liter. | TOGO lists 1 mg resazurin and 3 ug Na2SeO3; the record stores `1 G_PER_L` and `3 G_PER_L`. | `data/normalized_yaml/bacterial/marine_srb_medium.yaml`; if recurring, fix the TOGO unit conversion in the importer or normalization path. |
| major | Six stock-solution additions are empty placeholders with volume units rewritten as mass units. | The record has empty `composition: []` for FeCl2 solution, Trace element solution, Trace vitamins, 8% NaHCO3 solution, 10% L-Sodium lactate solution, and 5% Na2S x 9 H2O solution, and each ml addition is represented as `G_PER_L`. TOGO and MediaDive preserve these as stock-solution volumes. | `data/normalized_yaml/bacterial/marine_srb_medium.yaml`; use the JCM/MediaDive J1073 representation and the referenced JCM M180/M190 stocks to repair structure. |
| major | The anaerobic preparation protocol is absent. | TOGO and MediaDive both carry the N2-CO2 atmosphere, post-autoclave stock addition boundary, culture-vessel dispensing, rubber stoppers, and pre-inoculation Na2S x 9 H2O addition; the reviewed record has no `preparation_steps`. | `data/normalized_yaml/bacterial/marine_srb_medium.yaml`; if recurring, fix TOGO comment import. |
| major | The TOGO and MediaDive copies of JCM Medium 1073 are not represented as source duplicates. | `TOGO:M1142` and `mediadive.medium:J1073` both preserve JCM GRMD 1073 for Marine SRB Medium, but `CultureMech:007665` and `CultureMech:015379` are independent normalized records with no variant edge. | `data/normalized_yaml/bacterial/marine_srb_medium.yaml`, `data/normalized_yaml/specialized/marine_srb_medium.yaml`, and the merge inputs regenerated from them. |
| minor | The record classifies the medium as fully `UNDEFINED`. | The source contains yeast extract plus otherwise defined salts and stock solutions. The MediaDive sibling uses `SEMI_DEFINED`, which better matches the formulation. | `data/normalized_yaml/bacterial/marine_srb_medium.yaml`. |

## Recommended Edits

1. Reconcile `data/normalized_yaml/bacterial/marine_srb_medium.yaml` with
   JCM/MediaDive `J1073`, keeping source units or normalized final
   concentrations consistently and correcting the resazurin and Na2SeO3 scale.
2. Replace the six empty stock-solution placeholders with explicit ml/l solution
   references or nested stock compositions, including the JCM M180 FeCl2 and
   trace element solutions and the JCM M190 trace vitamins.
3. Move N2 and CO2 out of root nutritional ingredients unless they are modeled
   as atmosphere conditions, and add the two source preparation steps.
4. Link the TOGO and MediaDive JCM 1073 records as source duplicates so the
   merged corpus does not publish two uncoordinated `marine_srb_medium` records.
5. Change `composition_type` from `UNDEFINED` to `SEMI_DEFINED` after the stock
   solution structure is repaired.
6. Add a curation-history event covering the source-backed unit, stock-solution,
   preparation, classification, and duplicate-link changes.

## Follow-up Checks

- Re-run the focused LinkML, strict, term, and reference validators on the
  edited bacterial record.
- Run `just validate-media-variant-links` after adding the duplicate relation.
- Regenerate merged records and verify that the TOGO and MediaDive JCM 1073
  records are connected or merged as intended.
- Manually compare the edited recipe against TOGO `M1142` and MediaDive `J1073`
  for every mass, volume, stock concentration, and preparation step.

## Additional Notes

- The original JCM `GRMD=1073` page was checked directly and returned "Nothing
  found"; TOGO and MediaDive were therefore used as preserved source records for
  the JCM formula.
- Some TOGO source strings contain non-ASCII chemical punctuation and a Japanese
  GMO gas label. The reviewed YAML preserves those, but this report normalizes
  formulas such as `MgCl2 x 6 H2O` and `Na2S x 9 H2O` to ASCII.
