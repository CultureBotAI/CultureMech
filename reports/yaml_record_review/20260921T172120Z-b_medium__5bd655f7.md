# YAML Record Review: b_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/b_medium__5bd655f7.yaml
- Started UTC: 2026-09-21T17:20:18Z
- Finished UTC: 2026-09-21T17:21:20Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:002807 |
| name | b_medium |
| original_name | B MEDIUM |
| category | bacterial |
| media_term | JCM Medium J458, mediadive.medium:J458 |
| generated status | Generated merge artifact in data/merge_yaml/merged |
| maintained owner | data/normalized_yaml/bacterial/b_medium.yaml |

The target is a stale generated single-source merge for the MediaDive/JCM `b_medium`
record, with old merge fingerprint
`5bd655f7040baf7c163b1d4e762eaf3a8a363d23fd4eef6fa70799a75aab643b`. It should no
longer be a distinct formulation from TOGO M458 after the maintained owner repair.

## Validation

| Check | Result |
|---|---|
| Open schema | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/b_medium__5bd655f7.yaml` completed with no errors. |
| Strict validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/b_medium__5bd655f7.yaml --out /private/tmp/b_medium__5bd655f7.strict.tsv --workers 1 --quiet` completed with no errors. |
| Reference validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/b_medium__5bd655f7.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` completed with 0 checks. |
| Term validation | Pass. `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/b_medium__5bd655f7.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` completed with no errors, aside from a non-fatal `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone `history/` files, not a focused embedded `MediaRecipe.curation_history` validator for one merge record. |
| Direct `just` validators | Not checked: the project-level `uv` environment currently fails before target-specific validation while building `llvmlite==0.46.0` under Python 3.13, with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project LinkML validator workaround above exercised the target schema, strict, reference, and term checks. |

## Identity and Grounding

- The record identity is supported as MediaDive/JCM J458 B MEDIUM.
- MediaDive J458 and TOGO M458 preserve the same JCM Medium 458 B Medium formulation:
  500 ml distilled water, 500 ml seawater, 1 g glucose, 0.2 g KH2PO4,
  0.05 g MgSO4 x 7H2O, 15 g Bacto agar, and 2 g each of Bacto peptone,
  Casamino acids, and yeast extract.
- The normalized owner has already repaired the JCM/MediaDive import by adding the
  missing Distilled water row, correcting Seawater to a volume, adding source URLs,
  and adding a reciprocal source-duplicate link to TOGO M458. The generated target
  has not been regenerated from that repaired owner.

## Evidence

Supported by inspected source text:

- MediaDive J458 lists a 1000 ml main solution with 500 ml Sea water, 500 ml
  Distilled water, and the same seven solid ingredients listed by TOGO M458.
- MediaDive and TOGO both preserve the JCM pH-adjustment step as 7.5-7.8.

Unsupported or stale in the generated target:

- Distilled water is absent from the generated target but present in MediaDive, TOGO,
  and the repaired normalized owner.
- Seawater is still `500 G_PER_L`; the source says 500 ml, and the normalized owner
  already has `500.0 ML_PER_L`.
- The generated target still has `ph_value: 7.7` instead of the source range
  `ph_range: 7.5-7.8`.
- The generated target lacks the curated source-specific row notes, `references`,
  `data_quality_flags`, reciprocal `variant_children`, and 2026-09-11 repair curation
  event now present in the normalized owner.

## Completeness

- Source provenance is sufficient to recover the formulation through MediaDive J458 and
  TOGO M458, even though the current JCM GRMD page no longer exposes the recipe table.
- Empty organism and growth-metric slots are acceptable for this source recipe. The
  inspected source records establish a formulation, not a growth claim for a specific
  strain.
- Exact gitignore-independent search covered `reports/yaml_record_review` for an
  existing `b_medium__5bd655f7.md` report and found no prior report for this exact
  generated record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale relative to the repaired normalized owner. | `data/normalized_yaml/bacterial/b_medium.yaml` already has the missing 500 ml/L distilled water, 500 ml/L seawater, pH 7.5-7.8, exact source notes, references, quality flags, a TOGO source-duplicate child, and the 2026-09-11 repair curation event; `data/merge_yaml/merged/b_medium__5bd655f7.yaml` still has no water row, mass-like seawater, scalar pH, no references, no quality flags, and no duplicate linkage. | Regenerate `data/merge_yaml/merged/` from the repaired normalized layer; this stale fingerprint-suffixed artifact should merge with TOGO M458 after regeneration. |

No blocker or minor findings found.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the already-repaired normalized owners.
2. Re-run the merge freshness audit to confirm that MediaDive J458 and TOGO M458 now
   collapse into one source-duplicate group and the obsolete `b_medium__5bd655f7.yaml`
   artifact disappears.

## Follow-up Checks

- `just validate-strict data/merge_yaml/merged/B_MEDIUM.yaml` after regeneration.
- `just validate-references data/normalized_yaml/bacterial/b_medium.yaml` because the
  repaired owner now has explicit source URLs.
- `just verify-merges` and `just audit-merge-freshness` after regenerating the merge
  layer.
- Manual comparison with TOGO M458 and MediaDive J458 to verify the 500 ml distilled
  water, 500 ml seawater, pH range, and seven source solid ingredients.

## Additional Notes

- `linkml-reference-validator` performed zero checks on the stale generated record
  because it has no `references` block.
