# YAML Record Review: mnyp_medium__27c614d7

- Repository: CultureMech
- Record: data/merge_yaml/merged/mnyp_medium__27c614d7.yaml
- Started UTC: 2026-09-24T09:46:12Z
- Finished UTC: 2026-09-24T09:47:08Z
- Verdict: needs curation

## Target

Generated bacterial recipe for `CultureMech:009700`, `mnyp_medium`, from `TOGO:M3286` / `NBRC_M1647-1`.

The generated file contains one source recipe, `mnyp_medium`, merged from `data/normalized_yaml/bacterial/mnyp_medium.yaml`.

## Validation

Open LinkML validation passed with `No issues found`.

Strict schema-layer validation passed with 0 error rows; `/private/tmp/mnyp_medium__27c614d7.strict.tsv` contained only the header line.

Reference validation passed, but performed 0 checks.

Term validation passed. The run emitted the expected `eutils` / `pkg_resources` deprecation warning before `Validation passed`.

Embedded `curation_history` was not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries inside generated YAML.

## Identity and Grounding

The medium identity is correct: TOGO `M3286` imports NBRC medium 1647 variant 1 as `MNYP medium`.

The hydrate-sensitive CHEBI groundings for magnesium sulfate heptahydrate, iron(II) sulfate heptahydrate, and manganese(II) sulfate tetrahydrate match the salts in NBRC and TOGO.

`Salts solution***` is incorrectly grounded to `mediadive.solution:4429`. MediaDive solution 4429 contains calcium chloride, phosphate salts, sodium bicarbonate, sodium chloride, and 1 L water; the NBRC local salts solution contains magnesium sulfate, manganese sulfate, ferrous sulfate, sodium chloride, 1 ml water, and a 12 N HCl acidification instruction.

## Evidence

TOGO `M3286` and NBRC medium 1647 list the main MNYP medium as 1 L distilled water with 10 g mannitol, 10 g Bacto Yeast Extract, 5 g Hipolypepton, 2 g sodium acetate, 10 ml Tween 80 solution, 5 ml Salts solution, 15 g agar if needed, and pH 6.8.

TOGO and NBRC define `Tween 80 solution` locally as 50 mg Tween 80 in 1 ml distilled water.

TOGO and NBRC define `Salts solution` locally as 40 mg `MgSO4 x 7H2O`, 2 mg `MnSO4 x 4H2O`, 2 mg `FeSO4 x 7H2O`, 2 mg `NaCl`, 1 ml distilled water, and the note to add a drop of 12 N HCl to 500 ml of salts solution.

The normalized owner has already repaired one stale merge problem: on 2026-09-02 it collapsed three identical 1 L water rows back to `1.0` instead of summing them to `3.0`.

## Completeness

The generated record cannot reconstruct the source recipe hierarchy. `Hipolypepton*`, `Tween 80 solution**`, and `Salts solution***` were all moved to empty `solutions` entries, while the two solution formulas were also flattened into top-level ingredients.

The source pH 6.8 is absent from generated structured fields.

No target organism, incubation temperature, atmosphere, or growth evidence is present in TOGO M3286 or NBRC medium 1647, so those absences are not defects for this source-only import.

## Findings

1. Needs curation: the generated record drops `Hipolypepton*` from top-level ingredients by misclassifying it as a solution. NBRC lists it as a 5 g main-medium row with a manufacturer footnote, not as a stock added at 5 g/L.

2. Needs curation: both local stock solutions are flattened into the final ingredient list while remaining as empty `solutions` placeholders. `Tween 80` and the salts rows need to stay under the 10 ml Tween 80 stock and 5 ml Salts solution additions, or be explicitly scaled from those stock additions if flattened.

3. Needs curation: milligram stock rows were promoted to grams per liter. TOGO/NBRC list 50 mg Tween 80 and 40 mg / 2 mg / 2 mg / 2 mg salts rows in local stocks, but the generated final recipe stores `50 G_PER_L`, `40 G_PER_L`, and `2 G_PER_L` values.

4. Needs curation: the 12 N HCl used to acidify 500 ml of Salts solution is represented as a top-level variable-concentration `HCl` ingredient, losing its role as a stock-preparation reagent.

5. Needs curation: the generated record links `Salts solution***` to unrelated `mediadive.solution:4429`.

6. Minor: the generated file is stale after the normalized 2026-09-02 duplicate-water repair, so it still has `3.0 G_PER_L` distilled water from summing three independent water rows.

7. Minor: source pH 6.8 is missing.

## Recommended Edits

Re-curate `data/normalized_yaml/bacterial/mnyp_medium.yaml` so `Hipolypepton*` remains a main-medium ingredient with its manufacturer footnote and the two local stock recipes remain nested.

Represent the final recipe as 1 L base medium plus 10 ml Tween 80 solution and 5 ml Salts solution, with 15 g agar as an optional solidifying row.

Preserve the 12 N HCl note as a Salts solution preparation instruction, not as a final-medium variable ingredient.

Remove `mediadive.solution:4429` from this record unless a future grounding step can prove an exact source-equivalent salts solution.

Preserve source pH 6.8.

Regenerate the merged YAML after the normalized repair so the `1.0` distilled-water duplicate fix is carried into `data/merge_yaml/merged`.

## Follow-up Checks

Re-run open LinkML, strict, reference, and term validation after any normalized-record edits.

Recompare the corrected YAML with the NBRC medium 1647 table to verify that `Hipolypepton*` is not migrated to `solutions` and that stock rows retain milligram units.

Check the sibling TOGO `M3287` MNYP medium import for the same Hipolypepton and stock-solution defects.

## Additional Notes

No GitHub issues or PR comments were created by this record review.

Exact source-record searches used `find` and `rg --no-ignore --hidden`, so ignored files were included.
