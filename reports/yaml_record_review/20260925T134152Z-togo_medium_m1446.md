# YAML Record Review: togo_medium_m1446

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1446.yaml`
- Started UTC: 2026-09-25T13:41:52Z
- Finished UTC: 2026-09-25T13:41:52Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1446, imported from NBRC medium 214.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1446.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1446`; the TOGO API identifies NBRC_M214 as its original medium ID and links NBRC medium 214.

The TOGO payload and NBRC page agree on 100 g skim milk and 1 L distilled water.

The normalized source YAML already contains the September 2026 NBRC 214 repair, but the generated merged YAML was last rebuilt from an older source snapshot.

## Evidence

The generated skim milk row matches the source amount.

The generated merge still represents the source 1 L distilled water row as 1 g/L and omits the source references and simple preparation step that are present in the repaired normalized source.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The source has no pH or preparation comments beyond mixing 100 g skim milk in 1 L distilled water.

## Findings

- Major issue: the generated merge is stale relative to `data/normalized_yaml/bacterial/togo_medium_m1446.yaml` and does not include the September 2026 NBRC 214 repair.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.
- Minor issue: preparation and source-reference fields present in the normalized source are missing from the generated merge.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/togo_medium_m1446.yaml` from the current normalized TOGO M1446 source.
- Confirm the regenerated record preserves the 1000 ml/L distilled water row, references, data-quality flags, and simple MIX preparation step.
- Do not manually patch the generated merge; keep any remaining formulation curation in `data/normalized_yaml/bacterial/togo_medium_m1446.yaml`.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge.
- Confirm the regenerated fingerprint and `MERGED_RECIPES` history entry correspond to the repaired normalized source, not the stale August 2026 merge.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1446 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
