# YAML Record Review: 1210

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1210.yaml`
- Started UTC: 20260921T043605Z
- Finished UTC: 20260921T043724Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007464` |
| Label | `1210` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1210.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1210]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1210.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import for NBRC Medium 1209. The maintained owner later recovered the 10-row NBRC table and source pH range, but it still retains the stale plain numeric label.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1210.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1210.yaml --out /private/tmp/culturemech-review-1210.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-1210.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1210.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1210.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The intended source identity is NBRC Medium 1209:

- The import history says `Media No. 1209` and links the NBRC `NO=1209` page.
- Live NBRC Medium 1209 is titled `5xR2A Medium`.
- The normalized owner now has `media_term: nbrc.medium:1209`, a recovered ingredient list, a pH range of 7.0-7.4, and a direct NBRC 1209 reference.

Two identity issues remain:

- The generated record is still labeled `1210`; live NBRC Medium 1210 is `A3`, a different mineral medium with Fe and A5 stock solutions.
- The maintained owner also still has `name: '1210'` and `original_name: '1210'` rather than the official `5xR2A Medium` title.

## Evidence

Supported by inspected source text:

- NBRC Medium 1209 lists Bacto Yeast Extract (Difco) 2.5 g, Bacto Proteose Peptone No. 3 (Difco) 2.5 g, Bacto Casamino Acids (Difco) 2.5 g, glucose 2.5 g, soluble starch 2.5 g, sodium pyruvate 1.5 g, K2HPO4 1.5 g, MgSO4.7H2O 0.25 g, optional agar 15 g, and distilled water 1 L.
- NBRC Medium 1209 lists pH 7.2 +/- 0.2.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all ten source rows plus the pH text into a single `preferred_term`.
- The second ingredient row has an empty `preferred_term` and stores `Bacto Yeast Extract (Difco)` as a concentration value.
- The generated artifact has no ingredient groundings, no separate 1.5 g/L sodium pyruvate/K2HPO4 rows, no 0.25 g/L magnesium sulfate heptahydrate row, no 1 L water row, no pH range, no source term, and no reference.
- The generated `ADJUST_PH` and autoclaving steps are not in the inspected NBRC 1209 table.

## Completeness

- The generated file omits the Aug 19 recovered NBRC composition, the Aug 20 MIM groundings, and the Sep 7 partial NBRC source-metadata repair.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_1210`, `NO=1209`, `NBRC Medium 1209`, and `nbrc.medium:1209` found this stale generated NBRC direct import, the partially repaired normalized owner, normalized index entries, and a separate TOGO-derived `5_r2a_medium` copy of NBRC Medium 1209.
- Empty preparation and growth-evidence slots are acceptable in the normalized source import because the NBRC table does not state a preparation sequence or organism-specific growth observation.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading numeric identity for NBRC Medium 1209. | The generated file is labeled `1210`, but its import event points to NBRC Medium 1209; live NBRC Medium 1210 is the distinct A3 formulation. | `data/normalized_yaml/bacterial/NBRC_1210.yaml` plus merge regeneration; the normalized owner still needs the official title. |
| Blocker | The 10-row NBRC table is collapsed into two malformed rows. | NBRC Medium 1209 has ten component rows and pH text; the generated file has one concatenated formula/pH blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1210.yaml`; fix NBRC import or merge source selection if the malformed rows persist. |
| Major | The official medium title is still missing from both the generated and normalized NBRC direct records. | Live NBRC Medium 1209 is `5xR2A Medium`, but both the generated file and `data/normalized_yaml/bacterial/NBRC_1210.yaml` retain `name: '1210'`. | `data/normalized_yaml/bacterial/NBRC_1210.yaml`. |
| Major | The generated file omits pH 7.2 +/- 0.2. | NBRC Medium 1209 states pH 7.2 +/- 0.2, and the normalized owner represents it as `ph_range` 7.0-7.4; the generated file has no pH field. | Regenerate from the normalized owner after the title fix. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 1209 page supplies the composition table and pH row; the stale generated record invented `ADJUST_PH` and autoclave text, while the maintained owner now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1210.yaml`. |
| Minor | A TOGO-derived copy of NBRC Medium 1209 remains separate. | Gitignore-independent search found `5_r2a_medium` with the same NBRC 1209 source URL. | Merge-fingerprint rules for NBRC/TOGO duplicate handling after the direct NBRC import receives its official title. |

## Recommended Edits

1. Curate `data/normalized_yaml/bacterial/NBRC_1210.yaml` to use `5xR2A Medium` as `name` and `original_name`, preserving `nbrc.medium:1209`.
2. Regenerate `data/merge_yaml/merged/` and confirm the generated artifact carries the ten recovered ingredient rows, pH 7.0-7.4, and the NBRC 1209 reference.
3. If regeneration preserves the concatenated blob, fix the NBRC merge source path that is still using the pre-recovery import output.
4. Review duplicate handling between the NBRC direct import and the TOGO `5_r2a_medium` copy.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/NBRC_1210.yaml` after the normalized title edit.
- `just verify-merges` to prove generated outputs are fresh after regeneration.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1209>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1209>.yaml` for the grounded subset.
- Manual comparison to live NBRC Medium 1209 for all ten rows and pH 7.2 +/- 0.2.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
