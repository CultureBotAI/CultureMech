# YAML Record Review: sporulation_medium__5840b198
- Repository: CultureMech
- Record: data/merge_yaml/merged/sporulation_medium__5840b198.yaml
- Started UTC: 2026-09-25T07:06:00Z
- Finished UTC: 2026-09-25T07:08:05Z
- Verdict: needs curation

## Target
Reviewed the generated merged record `data/merge_yaml/merged/sporulation_medium__5840b198.yaml`, a single-source TOGO import from `data/normalized_yaml/bacterial/TOGO_M1868_Sporulation_Medium.yaml`.

The record is identified as TOGO Medium M1868, `Sporulation Medium`, with original NBRC source `NBRC_M1113`.

## Validation
- LinkML schema validation: Passed with `No issues found`.
- Strict validation: Passed; scanned 1 file and reported 0 `ERROR` rows. The strict TSV contained the header only.
- Reference validation: Passed; 1 file was validated and 0 reference checks were available.
- Term validation: Passed after the known `eutils` `pkg_resources` deprecation warning.
- Embedded `curation_history`: Not checked. The available `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` inside merged YAML.

## Identity and Grounding
The media identity is grounded to a consistent TOGO/NBRC source pair. TOGO M1868 reports `Sporulation Medium`, `original_media_id` `NBRC_M1113`, and NBRC source URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=1113`; the generated record carries the same source and URL in `notes`.

An exact source/id search for `TOGO:M1868`, `NBRC_M1113`, `CultureMech:008442`, and `Sporulation Medium` included ignored files and found only the expected normalized TOGO file, generated merged file, and index references under `data/normalized_yaml` and `data/merge_yaml/merged`.

## Evidence
TOGO and NBRC both describe three subsolutions:

- Solution A: Nutrient broth 8 g, FeSO4.7H2O 0.278 mg, MgSO4.7H2O 0.25 g, KCl 1 g, agar 15 g if needed, and distilled water 800 ml.
- Solution B: Ca(NO3)2.4H2O 236.15 mg in 100 ml distilled water.
- Solution C: MnCl2.4H2O 1.979 mg in 100 ml distilled water.

TOGO/NBRC instruct curators to dissolve each reagent in distilled water, sterilize the solutions separately, cool them to about 60 C, and mix the solutions together. All three subsolutions are marked `pH unadjusted`.

## Completeness
No target organisms are present in the generated record; no source evidence in the reviewed TOGO/NBRC material names organism growth for this recipe, so that is not a defect.

The generated record drops the three-subsolution preparation topology and `pH unadjusted` comments. That loss compounds the unit error because the output no longer distinguishes the 800 ml Solution A water from the two 100 ml stock-like subsolution waters.

## Findings
- High: The importer stored all three source `mg` quantities as the same numeric values in `G_PER_L`. `FeSO4.7H2O` should be 0.000278 g/L, not 0.278 g/L; `Ca(NO3)2.4H2O` should be 0.23615 g/L, not 236.15 g/L; and `MnCl2.4H2O` should be 0.001979 g/L, not 1.979 g/L.
- Medium: The generated final-medium formula collapsed the three solution waters into one `Distilled water` row and does not preserve the source instruction to sterilize each solution individually before cooling and mixing. That makes a serial recreation of the NBRC recipe impossible from the generated record alone.
- Low: `high_metal: true` appears to be a secondary artifact of the 1000x unit inflation on the calcium and manganese salts rather than a true source assertion.

## Recommended Edits
- Fix TOGO/NBRC import or the normalized TOGO M1868 record so `mg` rows are converted to grams before the generated `G_PER_L` values are stored:
  - `FeSO4.7H2O`: 0.000278 g/L
  - `Ca(NO3)2.4H2O`: 0.23615 g/L
  - `MnCl2.4H2O`: 0.001979 g/L
- Preserve the source preparation text: Solution A in 800 ml water, Solution B in 100 ml water, Solution C in 100 ml water, each solution sterilized individually, cooled to about 60 C, and then mixed.
- Preserve the three `pH unadjusted` annotations where the schema allows.
- Regenerate merged YAML after editing the normalized source; do not patch `data/merge_yaml/merged/sporulation_medium__5840b198.yaml` by hand.
- Recompute `high_metal` after the unit fix.

## Follow-up Checks
- After regeneration, verify that the TOGO M1868 generated recipe no longer reports calcium nitrate tetrahydrate at 236.15 g/L.
- Run schema, strict, reference, and term validators on the regenerated merged record.

## Additional Notes
The NBRC page spelling says `regent`; this appears in source preparation prose and should be normalized as `reagent` if the instruction is carried into `preparation_steps`.
