# YAML Record Review: GYE medium

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gye_medium__b6ab294a.yaml`
- Started UTC: 2026-09-23T08:04:22Z
- Finished UTC: 2026-09-23T08:05:22Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:001259`, `gye_medium`, imported from DSMZ / MediaDive medium 1830.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gye_medium_b6ab294a.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The generated record is the expected single-source merge from `data/normalized_yaml/bacterial/gye_medium.yaml` and carries `mediadive.medium:1830`. Exact ignored-file-inclusive search found only that parent/generated pair for MediaDive 1830; other `gye_medium` files refer to distinct DSMZ/KOMODO 572 records.

Glucose, calcium carbonate, agar, and ethanol are grounded. Yeast extract is ungrounded, but it is a complex component and not a schema failure.

## Evidence

The MediaDive 1830 REST payload names `GYE medium`, has pH 6.8-7.0, and lists one 1000 ml main solution with 20 g/l Glucose, 8 g/l Yeast extract, optional 7 g/l CaCO3 with the source attribute `add only for agar plates`, optional 12 g/l Agar, and 5 ml/l Ethanol with `non-methylated` and `sterile` annotations. The three preparation steps in the generated record match MediaDive: adjust pH to 6.8-7.0, autoclave at 121 C for 15 min, then add sterile autoclaved non-methylated ethanol after autoclaving.

## Completeness

The generated record preserves the core ingredients, pH range, optional flags as free-text notes, and preparation steps. It drops the MediaDive distinction between `g/l` and `ml/l` for ethanol, and it flattens a medium that is solid only when optional agar is added into an unconditional `SOLID_AGAR` record.

## Findings

- Ethanol is misunitized. MediaDive reports Ethanol as 5 `ml/l`, while the generated row records `value: '5'`, `unit: G_PER_L`.
- Optional solid-medium semantics are underspecified. MediaDive marks both CaCO3 and Agar optional and says CaCO3 should be added only for agar plates, but the generated recipe only writes `Optional ingredient` notes and classifies the entire record as `SOLID_AGAR`.
- Yeast extract remains ungrounded. This is minor because it is a complex ingredient, but it should be checked against available mappings.

## Recommended Edits

- Preserve MediaDive volume concentration units for ethanol instead of coercing 5 ml/l to 5 g/l.
- Represent the liquid-vs-agar-plate branch explicitly, or at least carry the `add only for agar plates` condition on CaCO3 and the optional status of Agar into curation notes.
- Attempt a complex-component grounding for Yeast extract if one is available.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Recompare ingredient values, ethanol units, pH, and preparation steps against MediaDive 1830.

## Additional Notes

None found.
