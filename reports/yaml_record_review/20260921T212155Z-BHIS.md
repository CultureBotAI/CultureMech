# YAML Record Review: bhis

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHIS.yaml
- Started UTC: 2026-09-21T21:21:16Z
- Finished UTC: 2026-09-21T21:21:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:009362 |
| Name | bhis |
| Original name | BHIS |
| Category | bacterial |
| Source identity | TOGO Medium M2814 |
| Reviewed artifact | `data/merge_yaml/merged/BHIS.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bhis.yaml` |
| Generated state | Derived single-source merge from `merged_from: [bhis]`, `merge_fingerprint: 66e1b01c930c98f4f254b1fd7ae7eadc098eb2034abf49255b4fe7e9de8411e1` |

`data/culturemech_id_registry.tsv` maps `CultureMech:009362` to
`data/normalized_yaml/bacterial/bhis.yaml`.

The inspected TOGO `M2814` API payload identifies BHIS as one liter of Brain
heart infusion agar supplemented with 50 ug/ml hemin and 0.5 ug/ml menadione.
TOGO's source comment says Bacteroides fragilis strains were grown
anaerobically on those BHIS plates.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHIS.yaml` | Passed; no issues emitted |
| Strict closed-schema gate | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHIS.yaml --out /private/tmp/BHIS.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 ERROR rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHIS.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 reference checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHIS.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | Not checked | No focused embedded-`MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone records under `history/` |

The direct `just validate-*` wrappers were not rerun for this record because
this checkout currently fails before target-specific validation while building
`llvmlite==0.46.0` under Python 3.13. The no-project commands above exercise
the same narrow validators under Python 3.11 from a cache outside the project
environment.

## Identity and Grounding

- **Record identity is coherent.** `TOGO:M2814`, the label BHIS, and the TOGO
  API payload all identify this bacterial BHI agar plus hemin/menadione
  formulation.
- **The physical state is supported.** TOGO identifies the base as Brain heart
  infusion agar and the comment describes BHIS plates.
- **Hemin and menadione have 1000-fold unit slips.** The source lists 50
  ug/ml hemin and 0.5 ug/ml menadione, which are 0.05 g/L and 0.0005 g/L;
  the generated record stores 50 and 0.5 as `G_PER_L`.
- **Brain heart infusion agar is over-expanded.** TOGO lists one liter of a
  Brain heart infusion agar aggregate. The generated record replaces it with
  six full-strength BHI broth constituents from a secondary MicrobeNotes page
  and has no explicit agar component.
- **Growth context is only partially represented.** The source comment names
  anaerobic growth of B. fragilis strains on BHIS plates; the generated record
  has no target organism or anaerobic culture context.

## Evidence

| Claim | Review |
|---|---|
| TOGO M2814 / BHIS identity | Supported by TOGO M2814. |
| SOLID_AGAR physical state | Supported by the TOGO Brain heart infusion agar component and by the source comment's `plates` wording. |
| Hemin at 50 g/L and Menadione at 0.5 g/L | Unsupported; TOGO lists 50 ug/ml hemin and 0.5 ug/ml menadione. |
| Six expanded BHI constituents | Unsupported. TOGO lists 1 L Brain heart infusion agar. |
| B. fragilis strains grown anaerobically on BHIS | Present in TOGO's retained source comment but not modeled in the generated record. |

## Completeness

- Empty preparation fields are acceptable because the inspected TOGO payload
  does not describe sterilization or handling steps.
- The reviewed merge has only the public TOGO URL in `notes`; it has no
  structured `references` entry for the public TOGO page or TOGO API payload.
- The related `data/normalized_yaml/specialized/bhis.yaml` record is a
  distinct Brachyspira pilosicoli BHIS formulation that correctly remains in a
  separate generated file, `data/merge_yaml/merged/BHIS__8d771a65.yaml`.
- A gitignore-independent search with `rg --no-ignore --hidden` over `data`
  and `reports/yaml_record_review` for `CultureMech:009362`, `TOGO:M2814`,
  the exact `BHIS`/`bhis` labels, and the merge fingerprint found the expected
  normalized owner, generated merge, generated indexes, and the distinct
  specialized BHIS record. A `find` search over the ignored
  `reports/yaml_record_review` directory found no pre-existing `*-BHIS.md`
  report before this one was written.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | Hemin and menadione are 1000-fold too concentrated. | TOGO M2814 lists 50 ug/ml hemin and 0.5 ug/ml menadione, while the generated record stores those same numeric values as g/L. | `data/normalized_yaml/bacterial/bhis.yaml` |
| major | The one-liter Brain heart infusion agar base is replaced by unsupported BHI broth subcomponents and no agar row. | TOGO M2814 lists `Brain heart infusion (BHI) agar` as a 1 L aggregate. | `data/normalized_yaml/bacterial/bhis.yaml` |
| minor | Structured source references are absent. | The TOGO URL is present only in the free-text `notes`, and the TOGO API URL is not recorded. | `data/normalized_yaml/bacterial/bhis.yaml` |
| minor | Anaerobic B. fragilis plate-growth context is not modeled. | TOGO's retained source comment states that B. fragilis strains were grown anaerobically on BHIS plates, but the record has no growth evidence or anaerobic condition. | `data/normalized_yaml/bacterial/bhis.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/bhis.yaml`, change Hemin to 0.05 g/L
   and Menadione to 0.0005 g/L.
2. Replace the six BHI decomposition rows with one 1 L Brain heart infusion
   agar aggregate, or otherwise model the exact BHI agar commercial product
   without inventing a BHI broth decomposition.
3. Add structured references for the public TOGO M2814 page and TOGO API
   payload.
4. After recovering the source publication behind the TOGO comment, model the
   B. fragilis anaerobic BHIS plate-growth evidence at strain-level precision.
5. Regenerate `data/merge_yaml/merged/BHIS.yaml` and generated media pages from
   the corrected normalized owner.

## Follow-up Checks

- Rerun schema, strict, term, and reference validation on the corrected
  normalized owner and on the regenerated merge.
- Run `just verify-merges` and `just audit-merge-freshness` after regeneration.
- Manually compare the regenerated merge against TOGO `M2814` and confirm the
  hemin, menadione, and BHI agar rows match the source.

## Additional Notes

None found.
