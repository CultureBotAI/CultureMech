# YAML Record Review: 1_10_r2a_broth

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_R2A_BROTH.yaml`
- Started UTC: 20260921T045430Z
- Finished UTC: 20260921T045556Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003150` |
| Label | `1_10_r2a_broth` |
| Original label | `1/10 R2A BROTH` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/1_10_r2a_broth.yaml`, `data/normalized_yaml/bacterial/TOGO_M840_1_10_R2A_Broth.yaml`, or merge regeneration |
| Merge owner | `merged_from: [1_10_r2a_broth]` |
| Maintained owner | `data/normalized_yaml/bacterial/1_10_r2a_broth.yaml` |

This generated singleton is the pre-repair MediaDive/JCM import of JCM Medium 805.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_R2A_BROTH.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_R2A_BROTH.yaml --out /private/tmp/1_10_R2A_BROTH.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_R2A_BROTH.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_R2A_BROTH.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_R2A_BROTH.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly denotes JCM Medium 805, `1/10 R2A BROTH`, but it predates a 2026-09-06 repair that brought the JCM owner into agreement with the inspected JCM page.

The maintained `data/normalized_yaml/bacterial/1_10_r2a_broth.yaml` owner now has the source-supported 0.32 g/L R2A broth DAIGO row, the 1 L distilled-water row, the JCM autoclave default, structured references, a `CONCENTRATION_VARIANT` parent link to `r2a_broth`, and a `SOURCE_DUPLICATE` child link to `data/normalized_yaml/bacterial/TOGO_M840_1_10_R2A_Broth.yaml`.

## Evidence

Supported by inspected source text:

- JCM 805 is `1/10 R2A BROTH`.
- JCM 805 lists 0.32 g R2A broth (DAIGO) (Nihon Pharm. Co.) and 1.0 L distilled water.
- JCM's page-level default says to autoclave media at 121 degrees C for 15 min unless otherwise stated.

Unsupported, missing, or stale in the generated record:

- The generated artifact shortens `R2A broth (DAIGO) (Nihon Pharm. Co.)` to `R2A Broth`.
- The generated artifact omits distilled water and the autoclave step.
- The generated artifact has no structured references, parent-medium link, or TOGO M840 `SOURCE_DUPLICATE` child.
- TOGO M840 still appears as `data/merge_yaml/merged/1_10_r2a_broth__effd6b5a.yaml`, a separate Aug 6 singleton, even though the two normalized owners now explicitly mark it as the same JCM 805 formulation.

## Completeness

- No pH row is missing; the JCM 805 page does not state pH.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the JCM page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:003150`, `mediadive.medium:J805`, `TOGO:M840`, `TOGO_M840_1_10_R2A_Broth`, and the JCM merge fingerprint found only the direct JCM owner, the linked TOGO M840 owner, the two stale generated singleton records, and ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record is stale and omits source-supported water and sterilization. | The JCM page lists 1.0 L distilled water and its standard 121 degrees C / 15 min autoclave default; the Sep 6 normalized owner has both, but the Aug 6 generated artifact has neither. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_10_r2a_broth.yaml`. |
| Major | The stale JCM and TOGO M840 generated artifacts remain split despite repaired source-duplicate links. | The normalized owners now mark TOGO M840 as the same JCM 805 formulation, but generated `1_10_R2A_BROTH.yaml` and `1_10_r2a_broth__effd6b5a.yaml` still have separate fingerprints. | Regenerate `data/merge_yaml/merged/` from the Sep 6 normalized R2A owners. |
| Minor | Structured references are absent from the generated artifact. | The maintained owner has JCM 805 and JCM 839 references, but the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from the normalized owner. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `1_10_R2A_BROTH.yaml` receives the Sep 6 normalized owner fields: exact R2A broth DAIGO label, 1 L water, autoclave step, sterilization block, structured references, parent-medium relationship, and TOGO M840 source-duplicate child.
2. Confirm that generated `1_10_r2a_broth__effd6b5a.yaml` disappears or is folded into the regenerated JCM/TOGO canonical.

## Follow-up Checks

- `just verify-merges` to prove the JCM and TOGO normalized records now coalesce.
- `just validate-strict data/normalized_yaml/bacterial/1_10_r2a_broth.yaml data/normalized_yaml/bacterial/TOGO_M840_1_10_R2A_Broth.yaml` if either normalized owner is touched.
- `just validate-strict` and `just validate-terms` on the regenerated `1_10_R2A_BROTH.yaml` merge artifact.
- `just validate-references` on the regenerated merge artifact after structured references are carried forward.
- Manual comparison to JCM Medium 805 for the R2A broth DAIGO label, 0.32 g/L amount, 1 L water row, and autoclave default.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
