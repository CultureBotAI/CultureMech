# YAML Record Review: JCM MEDIUM No. 189

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_189.yaml
- Started UTC: 2026-09-23T16:38:29Z
- Finished UTC: 2026-09-23T16:38:50Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_189.yaml`
- Stable ID: `CultureMech:002550`
- Name: `jcm_medium_no_189`
- Original name: `JCM MEDIUM No. 189`
- Category: `bacterial`
- Media term: `mediadive.medium:J189`
- Source: MediaDive import of JCM Medium 189, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=189`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_189.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_189`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_189.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_189.yaml --out /private/tmp/jcm_medium_no_189.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_189.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_189.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

## Identity and Grounding

The record identifies MediaDive `J189` for JCM Medium 189. MediaDive, live JCM `GRMD=189`, and TogoMedium M182 agree on a solid high-salt agar adjusted to pH 7.0.

The direct record preserves the eight source amounts and the procedural 1.0 L distilled-water volume. Its broad `Starch` row is less exact than the source `Soluble starch`, and the two undefined nitrogen rows lost their supplier/exact-name qualifiers.

## Evidence

Inspected evidence:

- The live JCM `GRMD=189` page lists 125 g NaCl, 160 g MgCl2 x 6 H2O, 0.13 g CaCl2 x 2 H2O, 5 g K2SO4, 1 g Polypepton (Nihon Pharm. Co.), 1 g yeast extract (BD-Difco), 2 g soluble starch, and 20 g agar.
- JCM also instructs the curator to add the components to distilled water, bring volume to 1.0 L, and adjust pH to 7.0.
- MediaDive REST `medium/J189` reports the same eight rows and two preparation steps. MediaDive preserves `Nihon Pharm. Co.`, `BD-Difco`, and `soluble` as row attributes.
- TogoMedium M182 points to `JCM_M189` and `GRMD=189` and carries the same formulation.

Supported:

- The MediaDive ID, original name, JCM link, category, complex/undefined/solid-agar classification, pH value, eight ingredient amounts, and two preparation steps are supported.
- The source expresses distilled water as a bring-to-1.0-L instruction, and the direct generated record correctly keeps it procedural rather than as a `G_PER_L` ingredient row.

Unsupported or incomplete:

- `Polypeptone` loses the source spelling and `Nihon Pharm. Co.` supplier.
- `Yeast extract` loses the `BD-Difco` supplier.
- `Starch` loses the source's `Soluble starch` qualifier.

## Completeness

Consequential gap:

- Exact undefined ingredient names and supplier qualifiers need restoration for the direct MediaDive import.

Empty slots that are acceptable:

- No stock solution appears in the JCM 189 source.
- No target-organism or growth-metric claim is represented.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=189`, exact `JCM_M189`, and exact `jcm_medium_no_189` IDs found only the direct MediaDive/JCM record and the TOGO M182 wrapper. No other normalized or merged copy of JCM 189 was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/togo_medium_m182.yaml` and `data/merge_yaml/merged/togo_medium_m182.yaml` pointing to `JCM_M189` and `GRMD=189`. The TOGO wrapper adds a `distilled water`, 1 `G_PER_L` row for JCM's 1 L make-up instruction. | Both normalized owners, plus `scripts/merge_recipes.py` after source repair. |
| Minor | Polypepton, yeast extract, and soluble starch are not represented at full source specificity. | JCM lists `Polypepton (Nihon Pharm. Co.)`, `Yeast extract (BD-Difco)`, and `Soluble starch`; MediaDive keeps those distinctions as row attributes, while the direct generated record has `Polypeptone`, generic `Yeast extract`, and generic `Starch`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_189.yaml`. |

## Recommended Edits

1. Preserve `Polypepton (Nihon Pharm. Co.)`, `Yeast extract (BD-Difco)`, and `Soluble starch` in `data/normalized_yaml/bacterial/jcm_medium_no_189.yaml`.
2. Repair `data/normalized_yaml/bacterial/togo_medium_m182.yaml` so the JCM instruction to bring the recipe to 1 L with distilled water is not represented as `distilled water`, 1 `G_PER_L`.
3. Regenerate `data/merge_yaml/merged/` and verify whether `jcm_medium_no_189` and `togo_medium_m182` collapse to one canonical generated recipe.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Compare the regenerated record against live JCM `GRMD=189` and MediaDive `medium/J189`, including exact qualifiers on Polypepton, yeast extract, and soluble starch.
- Search with `rg --no-ignore --hidden 'GRMD=189([^0-9]|$)|JCM_M189([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to verify duplicate-source handling with ignored files included.

## Additional Notes

- No pH, core concentration, or water-volume defect was found in the direct MediaDive record under review.
