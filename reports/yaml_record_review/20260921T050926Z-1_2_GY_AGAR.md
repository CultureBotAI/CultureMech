# YAML Record Review: 1_2_gy_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2_GY_AGAR.yaml`
- Started UTC: 20260921T050820Z
- Finished UTC: 20260921T050926Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:002253` |
| Slug | `1_2_gy_agar` |
| Original name | `1/2 GY AGAR` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owner | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |
| Source accession | `mediadive.medium:J1072`, JCM Medium 1072 |
| Merge fingerprint | `55c16297b81a4297af2632f3aa870a60a099983aa682f6d0efc50e30ed936774` |

Reviewed the singleton generated merge for the MediaDive/JCM import of JCM Medium 1072. A gitignore-independent exact search for the CultureMech ID, MediaDive accession, merge fingerprint, and JCM URL found this record in `data/merge_yaml/merged/1_2_GY_AGAR.yaml`, its owner in `data/normalized_yaml/bacterial/1_2_gy_agar.yaml`, the expected active recipe/index rows, and a separate TOGO-derived `1/2 GY Agar` record that points to the same JCM 1072 page.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2_GY_AGAR.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2_GY_AGAR.yaml --out /private/tmp/1_2_GY_AGAR.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2_GY_AGAR.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2_GY_AGAR.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validator wrappers remain blocked by the project `llvmlite==0.46.0` build under Python 3.13; the no-project `uv` commands above were used as focused equivalents.

## Identity and Grounding

- The record identity matches the JCM page: JCM Medium 1072 is named `1/2 GY AGAR` and lists glucose, KH2PO4, yeast extract, agar, and distilled water.
- `physical_state: SOLID_AGAR` is supported by the source agar row.
- Glucose, KH2PO4, and agar are correctly grounded to exact CHEBI terms and carry the source amounts of 10.0 g, 0.5 g, and 15.0 g per 1 L.
- The yeast-extract row has the correct 1.0 g amount, but the JCM source specifies `Yeast extract (BD-Difco)` and the current MediaDive-derived record drops the vendor qualifier.
- The recipe is incomplete because the JCM source's 1 L distilled-water row is absent.
- The exact JCM URL also appears in `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` and `data/merge_yaml/merged/1_2_gy_agar__1efdf1be.yaml`. That sibling imports the same source through TOGO Medium M1141, preserves the yeast-extract vendor qualifier, and includes distilled water as `1 G_PER_L`; the water omission in this record and the wrong water unit in the TOGO sibling prevent those equivalent JCM records from merging.

## Evidence

- The inspected JCM 1072 HTML page lists exactly: glucose 10.0 g, KH2PO4 0.5 g, yeast extract (BD-Difco) 1.0 g, agar 15.0 g, and distilled water 1.0 L.
- The JCM page header says to autoclave media at 121 C for 15 min unless a medium states otherwise; JCM 1072 does not state another sterilization.
- No pH, target organism, incubation temperature, salinity, or atmosphere is specified on the JCM 1072 page; those empty fields are not defects.

## Completeness

- Consequential gap: the 1 L distilled-water row is missing.
- Consequential gap: JCM's default autoclave instruction is missing.
- Non-blocking gap: the source's `Yeast extract (BD-Difco)` qualifier is not preserved.
- Non-blocking provenance gap: the JCM source URL is only free text in `notes`; no structured `references` entry exists.
- Non-blocking grounding gap: `Yeast extract` is ungrounded even though `src/culturemech/data/mediaingredientmech/label_index.csv` maps both `Yeast extract` and `Yeast extract (BD-Difco)` to `FOODON:03315426`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is absent from the MediaDive/JCM record. | JCM 1072 lists 1.0 L distilled water after the four gram-based ingredients; `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` has no water row. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |
| Major | The JCM default autoclave step is not represented. | The JCM 1072 page instructs autoclaving at 121 C for 15 min unless otherwise stated, and the medium itself does not override that default. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |
| Major | The same JCM 1072 recipe exists as a separate TOGO-derived CultureMech record instead of one merged recipe. | `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` points to the same JCM `GRMD=1072` source. It has the same solute amounts plus distorted `1 G_PER_L` water, while this MediaDive owner omits water entirely. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` and `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml` |
| Minor | The yeast-extract vendor qualifier from JCM is lost. | JCM specifies `Yeast extract (BD-Difco)`; the MediaDive-derived owner records only `Yeast extract`. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |
| Minor | The exact yeast-extract ontology mapping is missing. | The local MediaIngredientMech label index maps both source yeast-extract labels to `FOODON:03315426`, but the row has no `term`. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |
| Minor | The JCM source is not represented as a structured reference. | The URL is present only in the top-level `notes` string, so reference validation has 0 checks. | `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` |

## Recommended Edits

1. Add JCM's 1.0 L distilled-water component to `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` as `1000 ML_PER_L`.
2. Add the JCM default autoclave instruction to the maintained owner.
3. Reconcile `data/normalized_yaml/bacterial/1_2_gy_agar.yaml` with `data/normalized_yaml/bacterial/TOGO_M1141_1_2_GY_Agar.yaml`: both should preserve the same JCM formulation and should collapse during merge generation after the missing/wrong water representation is fixed.
4. Preserve `Yeast extract (BD-Difco)` as the source ingredient label or in a source note, and add the exact `FOODON:03315426` grounding from the local label index.
5. Add a structured reference for `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1072`.

## Follow-up Checks

- Re-run strict, term, and reference validation for both normalized JCM 1072 owners after curation.
- Re-run merge generation and verify that `1_2_GY_AGAR.yaml` and `1_2_gy_agar__1efdf1be.yaml` are replaced by one merged JCM 1072 output or are explicitly linked as duplicates.
- Re-run duplicate-signature checks over JCM/TOGO imports so other MediaDive-vs-TOGO JCM pairs are not split only by `1 G_PER_L` water artifacts.

## Additional Notes

- The source page uses a one-liter basis, so the gram amounts in the current generated record are already numerically correct as `G_PER_L`.
- Empty pH and target-organism fields are acceptable for this record; the inspected source does not specify those values.
