# YAML Record Review: PYG Agar (H)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_agar_h.yaml
- Started UTC: 2026-09-24T22:24:16Z
- Finished UTC: 2026-09-24T22:25:12Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009677 |
| Name | pyg_agar_h |
| Original name | PYG Agar (H) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_agar_h.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml |
| Merge source | TOGO_M323_PYG_Agar_H |
| Source grounding | TOGO:M323, JCM_M328 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO normalization or in the TOGO import path that
converted the 1 L final-volume water row into the current `1 G_PER_L` row.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_agar_h.yaml` | Passed; exited 0 with no diagnostics. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_agar_h.yaml --out /private/tmp/pyg_agar_h.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_agar_h.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_agar_h.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_agar_h.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_agar_h.yaml` is the generated TOGO M323 branch
  for CultureMech:009677 and `PYG Agar (H)`.
- The TOGO M323 API reports original medium `JCM_M328`, pH 7.0, the same JCM
  GRMD 328 URL embedded in the reviewed record, and the PYG Agar (H)
  component list.
- The live JCM GRMD 328 URL no longer returns a recipe; it returned a `Nothing
  found` page for medium 328 during review. The reviewed record's exact JCM
  accession therefore could be checked only through TOGO and local historical
  imports, not against the current JCM page.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M323`, TOGO M323
  URLs, `JCM_M328`, `GRMD=328`, and `TOGO_M323_PYG_Agar_H` found the reviewed
  TOGO branch, its maintained owner, and a direct JCM normalized file for the
  same GRMD page.
- The direct JCM duplicate,
  `data/normalized_yaml/bacterial/pyg_agar_h.yaml`, is already linked into the
  generated `data/merge_yaml/merged/zymomonas_medium.yaml` duplicate cluster;
  that cluster was not reviewed here.

## Evidence

- TOGO M323 supports the PYG Agar (H) identity, pH 7.0, 1 L distilled water, 5
  g glucose, 15 g agar, 10 g yeast extract, and 5 g Bacto peptone.
- The reviewed record preserves the listed solute masses for the one-liter
  solid medium, including 15 g/L agar and the `SOLID_AGAR` physical state.
- The reviewed record encodes the 1.0 L distilled water row as 1 `G_PER_L`,
  which is not a supported final volume or amount.
- The reviewed record omits pH 7.0.
- The current JCM URL does not independently support any recipe claim because
  it no longer serves GRMD 328.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- `ph_value` is missing even though TOGO provides pH 7.0.
- The original JCM source link is stale at the cited URL, so a future curator
  should either refresh the original source URL or document TOGO as the only
  surviving inspected source for M323/JCM_M328.
- Exact ignored-file search included ignored files before assessing the exact
  TOGO/JCM source scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The source 1 L water row is misencoded as `1 G_PER_L`. | TOGO M323 describes a one-liter recipe. The YAML stores the final water row as 1 g/L, which is not the source volume and prevents this TOGO branch from matching the equivalent direct JCM import. | data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml; TOGO import |
| Major | The exact local JCM duplicate is not reconciled with this TOGO branch. | Exact ignored-file search found `data/normalized_yaml/bacterial/pyg_agar_h.yaml` for the same GRMD 328 URL, and that direct JCM file is linked into a different generated merge cluster. | data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml; merge_recipes.py |
| Minor | pH is absent. | TOGO M323 reports pH 7.0, but the generated record has no `ph_value`. | data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml |
| Minor | The original JCM URL no longer resolves to a recipe. | Fetching `GRMD=328` from JCM returned a `Nothing found` page during review, while the YAML still lists that URL as the original source. | data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml |

## Recommended Edits

1. Fix the water conversion in
   `data/normalized_yaml/bacterial/TOGO_M323_PYG_Agar_H.yaml` so the source 1 L
   row is represented consistently with other one-liter JCM imports instead of
   as 1 g/L.
2. Add pH 7.0 from TOGO M323.
3. Resolve the stale JCM GRMD 328 URL: update it if JCM has a replacement page
   or document that the recipe is currently grounded through TOGO's archived
   `JCM_M328` copy.
4. Regenerate merged records and verify the TOGO M323 copy is reconciled with
   the direct JCM GRMD 328 normalized file.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_agar_h.yaml`.
- Manually compare the regenerated TOGO M323 branch against
  `data/normalized_yaml/bacterial/pyg_agar_h.yaml` to ensure both JCM GRMD 328
  imports have equivalent pH, composition, and final-volume handling.
- Fetch the original JCM URL or its replacement and confirm the original source
  provenance no longer returns `Nothing found`.
- Run an exact ignored-file search for `TOGO:M323`, `JCM_M328`, and `GRMD=328`
  to confirm no stale duplicate remains after regeneration.

## Additional Notes

- The local source fetches for TOGO and JCM initially failed with sandbox DNS
  errors. Both official URLs were then fetched successfully with the approved
  escalated `curl -L` rule.
