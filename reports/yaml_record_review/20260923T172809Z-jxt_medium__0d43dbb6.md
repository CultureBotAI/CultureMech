# YAML Record Review: jxt_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml
- Started UTC: 2026-09-23T17:27:26Z
- Finished UTC: 2026-09-23T17:28:10Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008774` |
| Label | `jxt_medium` |
| Original label | `JXT Medium` |
| Source identity | `TOGO:M217`, JCM medium 224 |
| Category | `bacterial` |
| Generated from | `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml` by `merge_recipes.py` |

An ignored-inclusive exact search for `CultureMech:008774`, `TOGO:M217`,
`TOGO_M217_JXT_Medium`, `JCM_M224`, `GRMD=224`, and
`mediadive.medium:1090` across `data/normalized_yaml`,
`data/merge_yaml/merged`, `data/import_tracking`, `history`, and
`data/culturemech_recipe_catalog.tsv` found this TOGO M217 owner, a separate
JCM GRMD 224 import at `data/normalized_yaml/bacterial/jxt_medium.yaml`, and
MediaDive 1090 references in Pyrobaculum Calidifontis Medium records.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml` | Passed; `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml --out /private/tmp/jxt_medium__0d43dbb6.strict.tsv --workers 1 --quiet` | Passed; 0 ERROR rows and a header-only TSV. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were available because the stale generated record has no structured `references`. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not run | Not checked: the documented history validator targets standalone files under `history/`, not embedded `MediaRecipe.curation_history` lists in merged YAML. |

## Identity and Grounding

The primary TOGO/JCM identity is correct. TOGO M217 names `JXT Medium`, records
`JCM_M224` as the original source, links to JCM `GRMD=224`, and carries the pH
range 7.0-7.2. The live JCM 224 HTML is headed `224 JXT MEDIUM` and contains
the same four recipe rows.

The `kg_microbe_match: mediadive.medium:1090` field is wrong. Live MediaDive
1090 is DSMZ `PYROBACULUM CALIDIFONTIS MEDIUM`, with tryptone, yeast extract,
sodium thiosulfate pentahydrate, and distilled water. It is not JCM 224 / JXT
Medium, whose solvent is seawater and whose nitrogen source is Trypticase
peptone.

The generated file is stale relative to its maintained normalized owner. The
owner at `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml` has a
September 11 repair that restores 1 L seawater, the JCM pH range, the default
JCM autoclave step, source-specific notes, and structured references; the
reviewed generated file was last merged on August 6 and still has the older
`1 G_PER_L` seawater row and no pH or autoclave step.

## Evidence

TOGO M217 and JCM 224 support exactly four ingredients:

- 1 g yeast extract, with the source-qualified `BD-Difco` vendor retained in
  TOGO.
- 1 g Trypticase peptone, with the source-qualified `BD-BBL` vendor retained in
  TOGO.
- 1 g `Na2S2O3.5H2O`.
- 1 L seawater.

The stale generated record correctly names the four components but gives
seawater as `1 G_PER_L`, which changes a liter solvent row into one gram per
liter of an undefined component. It also omits JCM's pH 7.0-7.2 instruction and
JCM's site-wide default autoclaving statement.

The current normalized owner fixes those formulation and preparation details,
but it still carries the unsupported `kg_microbe_match` to MediaDive 1090. The
separate normalized JCM import `data/normalized_yaml/bacterial/jxt_medium.yaml`
represents the same JCM `GRMD=224` source under a second stable ID,
`CultureMech:002586`, and its generated merge still has seawater at
`1000 G_PER_L`.

## Completeness

Once regenerated from the repaired normalized TOGO owner, this record will
recover the source-stated pH, default autoclaving, 1 L seawater volume, and
structured source references.

No temperature, atmosphere, storage, or strain-specific growth claims are made.
Those omissions are acceptable for the inspected TOGO and JCM recipe records.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The generated merge is stale relative to its normalized TOGO owner. | `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml` has the September 11 seawater, pH, autoclave, and reference repair; the August generated file does not. | Regenerate `data/merge_yaml/merged/jxt_medium__0d43dbb6.yaml` from `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml` |
| Major | `kg_microbe_match` points to the wrong MediaDive record. | Live MediaDive 1090 is DSMZ Pyrobaculum Calidifontis Medium, not JCM 224 / TOGO M217 JXT Medium. | `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml` |
| Major | The same JCM 224 medium is duplicated under a second stable ID. | The ignored-inclusive exact search found `data/normalized_yaml/bacterial/jxt_medium.yaml`, a JCM GRMD 224 import for `CultureMech:002586`, alongside the TOGO M217 owner for `CultureMech:008774`. | `data/normalized_yaml/bacterial/jxt_medium.yaml`; recipe ID lifecycle catalog |
| Major | The visible generated seawater amount is dimensionally wrong. | JCM 224 and TOGO M217 list 1 L seawater; the generated file lists `Seawater` as `1 G_PER_L`. | Already fixed in `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml`; regenerate merged output |

## Recommended Edits

- Regenerate `data/merge_yaml/merged` so `jxt_medium__0d43dbb6.yaml` picks up
  the September 11 normalized repair.
- Remove or correct `kg_microbe_match: mediadive.medium:1090` on
  `data/normalized_yaml/bacterial/TOGO_M217_JXT_Medium.yaml`; do not point JXT
  Medium at a DSMZ Pyrobaculum medium.
- Resolve the duplicate JCM 224 representation in
  `data/normalized_yaml/bacterial/jxt_medium.yaml`, using the recipe ID
  lifecycle workflow if `CultureMech:002586` should be retired or folded into
  the TOGO M217 record.
- Regenerate again after the cross-match and duplicate-owner corrections.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation on the regenerated
  TOGO M217 merged output.
- Run `just verify-merges` and `just audit-merge-freshness` to confirm the
  September 11 normalized repair is represented in `data/merge_yaml/merged`.
- Run ID lifecycle and catalog checks if the JCM GRMD 224 duplicate is merged
  or retired.
- Manually compare the regenerated record to live TOGO M217 and JCM GRMD 224,
  confirming 1 L seawater, pH 7.0-7.2, and default JCM autoclaving.

## Additional Notes

None found.
