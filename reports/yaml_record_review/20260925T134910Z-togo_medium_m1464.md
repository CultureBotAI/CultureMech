# YAML Record Review: togo_medium_m1464

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1464.yaml`
- Started UTC: 2026-09-25T13:49:10Z
- Finished UTC: 2026-09-25T13:49:10Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1464, imported from NBRC medium 240.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1464.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed; exited 0 with no diagnostics.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1464`; the TOGO API identifies NBRC_M240 as its original medium ID and links NBRC medium 240.

The TOGO payload and NBRC page agree on 0.5 g ammonium sulfate, 0.3 g NaCl, 1 g K2HPO4, 0.3 g MgSO4 x 7 H2O, 0.03 g FeSO4 x 7 H2O, 7.5 g CaCO3, 1 L distilled water, pH 8.0, and a CaCO3 insolubility footnote.

## Evidence

The generated non-water ingredient amounts match the TOGO and NBRC amounts.

The generated record moves CaCO3 into an empty solution because the source marks that row with an insolubility footnote.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The source CaCO3 footnote is absent, and pH 8.0 is absent.

## Findings

- Major issue: `CaCO3*` is modeled as an empty solution named `Unknown solution` instead of an ingredient with an insolubility footnote.
- Minor issue: pH 8.0 is absent.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.

## Recommended Edits

- Move CaCO3 out of the empty solution wrapper and preserve the NBRC insolubility footnote as a note.
- Add `ph_value: 8.0`.
- Correct or omit the water row according to the repo's final-volume convention.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Add a targeted regression check for starred TOGO solubility notes so they are not emitted as empty solutions.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1464 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
