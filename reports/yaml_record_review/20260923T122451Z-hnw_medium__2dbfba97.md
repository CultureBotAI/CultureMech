# YAML Record Review: HNW Medium
- Repository: CultureMech
- Record: data/merge_yaml/merged/hnw_medium__2dbfba97.yaml
- Started UTC: 2026-09-23T12:24:11Z
- Finished UTC: 2026-09-23T12:24:51Z
- Verdict: needs curation

## Target

Reviewed the generated TOGO branch for TOGO M339, `HNW Medium`, at `data/merge_yaml/merged/hnw_medium__2dbfba97.yaml`. The maintained source is `data/normalized_yaml/bacterial/TOGO_M339_HNW_Medium.yaml`.

## Validation

- Open LinkML validation: passed for `MediaRecipe`.
- Strict validation: passed with zero error rows in `/private/tmp/hnw_medium__2dbfba97.strict.tsv`.
- Reference validation: passed with zero checks.
- Term validation: passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone YAML files under `history/`, not embedded `MediaRecipe.curation_history` entries in generated merge artifacts.

## Identity and Grounding

The record is grounded to TOGO `M339`, which names `HNW Medium` and points to JCM Medium 344 as the original provider source. The generated record is the right medium identity but an incomplete import of the JCM formulation.

## Evidence

JCM 344 lists 670 ml MJ(-N) synthetic seawater, 0.25 g NH4Cl, 1 g NaNO3, 0.1 mg Na2WO4 x 2 H2O, and 330 ml distilled water in the pre-autoclave main medium. After pH adjustment to 7.0 with NaOH and autoclaving, it adds 12.5 ml 8% NaHCO3 and 10 ml Trace vitamins as filter-sterilized solutions, then distributes the medium under H2-CO2 gas and pressurizes inoculated bottles to 200 kPa H2-CO2.

The TOGO M339 API preserves the 0.1 mg sodium tungstate amount. The generated YAML has that row as `0.1` G_PER_L. The generated YAML also represents MJ(-N) synthetic seawater, 8% NaHCO3 solution, and Trace vitamins as empty `solutions` with `G_PER_L` concentrations that are actually source milliliter addition volumes.

## Completeness

The main distilled-water, NH4Cl, and NaNO3 rows are present. The MJ(-N) synthetic seawater and Trace vitamins compositions are missing behind cross-reference placeholders, the 8% NaHCO3 stock is not structured, NaOH is represented as a variable ingredient instead of a pH adjustment reagent, and H2 and CO2 are represented as variable ingredients instead of bottle gas atmosphere. The source pH and ordered anaerobic preparation workflow are absent.

## Findings

- The 0.1 mg Na2WO4 x 2 H2O row is imported as 0.1 G_PER_L.
- MJ(-N) synthetic seawater is an empty solution with concentration `670` G_PER_L instead of a 670 ml addition with structured composition.
- 8% NaHCO3 is an empty solution with concentration `12.5` G_PER_L instead of a 12.5 ml post-autoclave stock addition.
- Trace vitamins is an empty solution with concentration `10` G_PER_L instead of a 10 ml post-autoclave stock addition with structured composition.
- The pH 7.0 adjustment, autoclave step, filter-sterilized late additions, H2-CO2 gas atmosphere, and 200 kPa pressurization are missing.

## Recommended Edits

- Fix the TOGO unit conversion in `data/normalized_yaml/bacterial/TOGO_M339_HNW_Medium.yaml` so the sodium tungstate 0.1 mg amount is not treated as grams per liter.
- Preserve the MJ(-N) synthetic seawater and Trace vitamins cross-references as nested stock recipes, resolving them from the linked JCM/TOGO media where available.
- Represent 8% NaHCO3 as a 12.5 ml late stock addition or convert it to the equivalent final NaHCO3 concentration.
- Move NaOH, hydrogen, and carbon dioxide out of final `ingredients` and into pH/aeration or preparation semantics.
- Restore the JCM pH and anaerobic preparation workflow.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation after regeneration.
- Re-open JCM 344 and TOGO M339 to confirm sodium tungstate is 0.1 mg and the three solution additions retain milliliter units.
- Confirm regenerated HNW has no empty MJ(-N) synthetic seawater or Trace vitamins placeholders.

## Additional Notes

None.
