# YAML Record Review: mga_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mga_agar__8a4444c2.yaml
- Started UTC: 2026-09-24T06:05:37Z
- Finished UTC: 2026-09-24T06:06:13Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/mga_agar__8a4444c2.yaml`
- Class: `MediaRecipe`
- Stable ID: `CultureMech:002888`
- Label: `mga_agar`
- Generated status: generated by `merge_recipes.py` from one normalized MediaDive import on fingerprint `8a4444c29a24f2f9b5d7fe368c4ded1034bb09341dd88af52eb468d2b438b93d`
- Maintained owner for future fixes: the normalized owner `data/normalized_yaml/bacterial/mga_agar.yaml` is already corrected; regenerate `data/merge_yaml/merged/mga_agar__8a4444c2.yaml` from it and verify merge freshness
- Source identity: MediaDive/JCM medium `J53`, `MGA AGAR`, linked by MediaDive to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=53`

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mga_agar__8a4444c2.yaml` | Passed; `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mga_agar__8a4444c2.yaml --out /private/tmp/mga_agar__8a4444c2.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows, and the emitted TSV had 1 header-only line. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mga_agar__8a4444c2.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 total checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mga_agar__8a4444c2.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked | `just validate-history` validates standalone `history/*.yaml` records, not embedded `MediaRecipe.curation_history` arrays. |

## Identity and Grounding

- The generated record correctly identifies `CultureMech:002888`, `mga_agar`, `mediadive.medium:J53`, source name `MGA AGAR`, pH 7.4, and solid agar state.
- The inspected MediaDive REST record and live JCM `GRMD=53` page support the same medium identity and list `Trace salts solution` as a 1 ml addition to the 1001 ml main solution.
- The exact normalized owner search used `find data/normalized_yaml -name 'mga_agar.yaml' -print`; this included ignored paths and found only `data/normalized_yaml/bacterial/mga_agar.yaml`.

## Evidence

- JCM and MediaDive support the five main-medium ingredients in the generated record: 2 g glucose, 1 g L-asparagine, 0.5 g `K2HPO4`, 0.5 g `MgSO4 x 7 H2O`, 15 g agar, 1 L distilled water, and pH adjustment to 7.4.
- JCM and MediaDive support a separate 100 ml `Trace salts solution` containing 1 g `FeSO4 x 7 H2O`, 0.1 g `CuSO4 x 5 H2O`, and 0.1 g `ZnSO4 x 7 H2O`, added to the main solution at 1 ml.
- The normalized record now represents that stock correctly in `solutions` with stock concentrations of `10`, `1`, and `1 G_PER_L` plus a `1 ML_PER_L` addition and an `apply_cocktail_nesting.py` history event from 2026-08-13.
- The reviewed generated merge is stale: it still lists `FeSO4 x 7 H2O` at `10 G_PER_L`, `CuSO4 x 5 H2O` at `1 G_PER_L`, and `ZnSO4 x 7 H2O` at `1 G_PER_L` as final-medium ingredients.

## Completeness

- The generated merge is missing the `Trace salts solution` boundary now present in the normalized record.
- The generated merge's curation history ends with an Aug 6 `merge_recipes.py` event, while the normalized owner has an Aug 13 `NESTED_FLATTENED_COCKTAIL` event that moved the three stock-strength components into a solution. That places the current defect in stale generated output rather than unresolved normalized curation.
- Empty target-organism and growth-metric fields are acceptable here. The inspected JCM/MediaDive recipe establishes a formula, not a strain-specific growth outcome.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and publishes stock-strength trace salts as final-medium ingredients. | JCM and MediaDive add 1 ml of the 100 ml `Trace salts solution`; the generated record has `10 G_PER_L` Fe stock, `1 G_PER_L` Cu stock, and `1 G_PER_L` Zn stock directly in `ingredients`. | Regenerate `data/merge_yaml/merged/mga_agar__8a4444c2.yaml` from `data/normalized_yaml/bacterial/mga_agar.yaml`. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/mga_agar__8a4444c2.yaml` from `data/normalized_yaml/bacterial/mga_agar.yaml` so the generated record includes `Trace salts solution` at `1 ML_PER_L` instead of three final stock-strength ingredient rows.
2. Re-run the merge freshness check that should have caught the Aug 13 normalized update being absent from the Aug 6 generated merge.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validation on the regenerated merged record.
- Re-run the merge freshness check that covers `mga_agar`.
- Manually compare regenerated `mga_agar__8a4444c2.yaml` with MediaDive REST `J53` or JCM `GRMD=53` and confirm the trace-salts stock remains nested.
- Re-render the medium page and confirm it displays the trace-salts stock as a stock addition, not three stock-strength final ingredients.

## Additional Notes

- No GitHub issues, PR comments, or YAML curation edits were made during this read-only review.
- No additional normalized YAML correction is recommended by this review; the stale generated merge is the defect.
