# YAML Record Review: PYG Medium (I)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_medium_i.yaml
- Started UTC: 2026-09-24T22:48:05Z
- Finished UTC: 2026-09-24T22:49:31Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:007741 |
| Name | pyg_medium_i |
| Original name | PYG Medium (I) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_medium_i.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml |
| Merge source | TOGO_M1213_PYG_Medium_I |
| Source grounding | TOGO:M1213, JCM_M1133 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO normalization that lost pH and preparation context
for this otherwise compact JCM recipe.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_medium_i.yaml` | Passed with `No issues found`. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_medium_i.yaml --out /private/tmp/pyg_medium_i.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_medium_i.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_medium_i.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_medium_i.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_medium_i.yaml` is the generated TOGO M1213 branch
  for CultureMech:007741 and JCM_M1133 `PYG Medium (I)`.
- The TOGO M1213 API reports the same label, original medium `JCM_M1133`, pH
  7.0, and the same JCM GRMD 1133 URL embedded in the YAML.
- The live JCM GRMD 1133 URL currently returns `Nothing found`, so TOGO is the
  available live source that preserves the upstream JCM-derived recipe.
- The equivalent direct JCM import locally confirms the same ingredient set and
  carries pH 7.0 plus the anaerobic dispensing and autoclave instruction.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M1213`, TOGO
  M1213 URLs, `JCM_M1133`, `GRMD=1133`, and
  `TOGO_M1213_PYG_Medium_I` found the reviewed TOGO branch, its maintained
  owner, a metal-analysis index entry, and the equivalent direct JCM branch.

## Evidence

- TOGO M1213 defines PYG Medium I as 1 L distilled water, 1 g yeast extract,
  1.5 g KH2PO4, 0.2 g MgCl2.6H2O, 0.1 g FeSO4.7H2O, 10 g glucose, 0.5 g
  L-cysteine.HCl.H2O, and 4 g peptone, adjusted to pH 7.0 and dispensed under
  an N2 atmosphere before sealing and autoclaving.
- The reviewed record preserves the TOGO M1213 identity and every dry
  ingredient amount.
- The reviewed record stores 1 L distilled water as 1 `G_PER_L`.
- The reviewed record carries N2 as a variable concentration ingredient but has
  no pH 7.0, no dispensing under N2, no butyl-rubber sealing instruction, and
  no autoclave preparation step.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- `ph_value` is missing even though TOGO gives pH 7.0.
- The anaerobic dispensing, sealing, and autoclave instructions are missing.
- The equivalent direct JCM branch remains separate and needs reconciliation
  once the TOGO branch preserves the preparation context.
- Exact ignored-file search included ignored files before assessing exact
  TOGO/JCM duplicate scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | pH 7.0 and the source preparation sequence are absent. | TOGO M1213 instructs curators to adjust pH to 7.0, dispense under N2 into culture vessels, seal with butyl rubber stoppers, and autoclave. The reviewed YAML has no `ph_value` or `preparation_steps`. | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml |
| Minor | Source water was converted to the wrong unit family. | TOGO M1213 encodes 1 L distilled water, but the YAML stores `Distilled water` as 1 `G_PER_L`. | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml |
| Minor | The N2 atmosphere is not modeled as preparation context. | The source instructs dispensing under N2 gas; the YAML only stores N2 as a variable-concentration gas ingredient. | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml |
| Minor | Hydrated MgCl2 and FeSO4 rows lack MediaIngredientMech CHEBI back-links. | The rows have primary CHEBI terms for magnesium dichloride hexahydrate and iron(2+) sulfate heptahydrate but no `mediaingredientmech_chebi_term` entries, unlike the equivalent direct JCM branch. | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml; MediaIngredientMech enrichment |
| Minor | The equivalent direct JCM branch remains a separate generated record. | Exact ignored-file search found `data/merge_yaml/merged/pyg_medium_i__8850a9b4.yaml` for the same JCM 1133 recipe. | data/normalized_yaml/bacterial/TOGO_M1213_PYG_Medium_I.yaml; merge_recipes.py |

## Recommended Edits

1. Add pH 7.0 and the source instruction to dispense under N2, seal with butyl
   rubber stoppers, and autoclave.
2. Store the 1 L water row in source units or omit it consistently rather than
   converting it to 1 g/L.
3. Move the N2 atmosphere out of ordinary variable ingredients into an explicit
   preparation or gas-atmosphere representation supported by the schema.
4. Refresh MediaIngredientMech CHEBI links for MgCl2.6H2O and FeSO4.7H2O.
5. Regenerate the merged output and verify this TOGO M1213 branch converges
   with the equivalent direct JCM branch.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_medium_i.yaml`.
- Manually confirm the regenerated record includes pH 7.0 and the anaerobic
  dispensing/autoclave preparation sequence.
- Manually compare the regenerated TOGO M1213 branch against
  `data/merge_yaml/merged/pyg_medium_i__8850a9b4.yaml` after the direct branch
  is fixed.
- Run an exact ignored-file search for `TOGO:M1213`, `JCM_M1133`, and
  `GRMD=1133` to verify no stale duplicate remains after regeneration.

## Additional Notes

- The local TOGO fetch initially failed with a sandbox DNS error. The official
  URL was then fetched successfully with the approved escalated `curl -L` rule.
- The original JCM GRMD 1133 page linked from TOGO and the YAML now returns
  `Nothing found`.
