# YAML Record Review: medium_163_modified_for_dsm_10520

- Repository: CultureMech
- Record: data/merge_yaml/merged/medium_163_modified_for_dsm_10520.yaml
- Started UTC: 2026-09-24T01:08:15Z
- Finished UTC: 2026-09-24T01:09:05Z
- Verdict: needs curation

## Target

Reviewed generated merged record `data/merge_yaml/merged/medium_163_modified_for_dsm_10520.yaml` for KOMODO medium `163_10520`, which cites DSMZ/MediaDive medium 163. Exact `find` over `data/normalized_yaml` with ignored files included found one owner: `data/normalized_yaml/bacterial/medium_163_modified_for_dsm_10520.yaml`.

Exact `komodo.medium:163_10520` scans over the generated record, the owner, and normalized indexes included ignored files and found only this record family and the matching bacterial/recipe index entries.

## Validation

- Open LinkML validation against `src/culturemech/schema/culturemech.yaml` as `MediaRecipe`: passed with `No issues found`.
- Strict validation with `scripts/validate_strict.py`: passed with 0 error rows; the strict TSV had a header only.
- LinkML reference validation: passed; 0 reference checks were emitted for this file.
- LinkML term validation with labels and `conf/oak_config.yaml`: passed.
- Embedded `curation_history`: Not checked. The repository `just validate-history` target validates standalone `history/` files rather than embedded generated-record history blocks.

## Identity and Grounding

`media_term.term.id` is `komodo.medium:163_10520`, and the record notes identify the DSMZ base medium as `mediadive.medium:163`. The KOMODO variant name, pH 7.5, and sodium-bicarbonate buffer note are not present in base DSMZ 163, whose live MediaDive record is `HALODESULFOVIBRIO (POSTGATE) MEDIUM` with a pH range of 6.8-7.0.

The generated file is a one-source merge of the normalized KOMODO owner.

## Evidence

MediaDive 163 defines a 1000 ml main solution made from 980 ml Solution A, 10 ml Solution B, and 10 ml Solution C.

The target stores all rows using subsolution g/L values rather than final 1000 ml concentrations:

- Solution A rows are based on a 980 ml stock. `NaCl`, `K2HPO4`, `NH4Cl`, `Na2SO4`, `CaCl2 x 2 H2O`, `MgSO4 x 7 H2O`, `Na-DL-lactate`, and `Yeast extract` are therefore about 2.04% too high in the flattened record.
- Solution B contains 0.5 g `FeSO4 x 7 H2O` in 10 ml and all 10 ml are added to the final liter. The target records the 50 g/L stock strength instead of the 0.5 g/L final strength.
- Solution C contains 0.1 g `Na-thioglycolate` and 0.1 g `Ascorbic acid` in 10 ml, and all 10 ml are added to the final liter. The target records the 10 g/L stock strengths instead of the 0.1 g/L final strengths.

The final `sodium` row is not a source ingredient. It was extracted from the KOMODO note `pH buffer: sodium bicarbonate`, reduced to bare sodium, and defaulted to a variable concentration.

## Completeness

No non-water DSMZ 163 row is absent, and the KOMODO-specific pH 7.5 value is represented. The formula is still not quantitatively complete because the three DSMZ subsolutions were flattened without their final-volume factors and the sodium-bicarbonate variant was not represented as a grounded bicarbonate ingredient.

`Yeast extract` lacks ontology grounding, and `Sodium resazurin` has a CHEBI term but no migrated `mediaingredientmech_chebi_term`.

## Findings

1. `needs curation` - Solution B is 100x too concentrated: `FeSO4 x 7 H2O` should be 0.5 g/L in the final liter, not 50 g/L.
2. `needs curation` - Solution C is 100x too concentrated: `Na-thioglycolate` and `Ascorbic acid` should be 0.1 g/L in the final liter, not 10 g/L each.
3. `needs curation` - Solution A rows use 980 ml stock concentrations directly, leaving the main salts, lactate, and yeast extract about 2.04% too high.
4. `needs curation` - The KOMODO sodium-bicarbonate buffer note became an unsupported variable `sodium` ingredient.
5. `minor` - `Yeast extract` and `Sodium resazurin` need enrichment cleanup after the formula repair.

## Recommended Edits

Recompute final concentrations from the DSMZ 163 solution volumes before applying the DSM 10520 modifications: multiply Solution A stock values by 0.98, Solution B stock values by 0.01, and Solution C stock values by 0.01. Replace the variable `sodium` ingredient with a source-backed sodium bicarbonate row only if the KOMODO variant provides a concentration; otherwise keep the buffer as a note instead of a pseudo-ingredient.

Then rerun ontology enrichment for `Yeast extract` and `Sodium resazurin`.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validators on the regenerated merged record.
- Confirm the regenerated record has FeSO4 x 7 H2O at 0.5 g/L and Na-thioglycolate/Ascorbic acid at 0.1 g/L.
- Confirm no bare variable `sodium` ingredient remains.
- Re-check `komodo.medium:163_10520` in normalized source indexes with ignored files included.

## Additional Notes

The live MediaDive 163 REST and rendered pages were inspected directly. The sibling `mediadive.medium:163*` index entries that exist elsewhere in CultureMech were ignored after an exact source-ID scan for this KOMODO variant.
