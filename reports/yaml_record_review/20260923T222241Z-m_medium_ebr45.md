# YAML Record Review: m_medium_ebr45

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/m_medium_ebr45.yaml`
- Started UTC: `2026-09-23T22:22:41Z`
- Finished UTC: `2026-09-23T22:22:48Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:008259` |
| Record name | `m_medium_ebr45` |
| Original name | `M medium (EBR45)` |
| Source accession | `TOGO:M1699` |
| Source label | `M medium (EBR45)` |
| Original source | `NBRC_M905` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml` |
| Generated record | `data/merge_yaml/merged/m_medium_ebr45.yaml` |

`data/merge_yaml/merged/m_medium_ebr45.yaml` is a generated merge from
`data/normalized_yaml/bacterial/m_medium_ebr45.yaml` with fingerprint
`b6097025efa42f10aa510bdcd715bcec02dca2e9475b39a9c4749fa2071f2e03`. Future
fixes should be made in the normalized input, its TOGO/NBRC import or solution
migration logic, and the related local-stock modeling before regenerating
`data/merge_yaml/merged/`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_medium_ebr45.yaml` | Passed. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_medium_ebr45.yaml --out /private/tmp/m_medium_ebr45.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows emitted to `/private/tmp/m_medium_ebr45.strict.tsv`. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_medium_ebr45.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_medium_ebr45.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The record identity is correct at the medium level: `CultureMech:008259`,
  `name: m_medium_ebr45`, `original_name: M medium (EBR45)`,
  `media_term.term.id: TOGO:M1699`, the TOGO metadata's `original_media_id:
  NBRC_M905`, and NBRC `NO=905` all denote the same `M medium (EBR45)`.
- An ignored-file-inclusive exact search for `CultureMech:008259`,
  `m_medium_ebr45`, and `M medium (EBR45)` across `data/normalized_yaml`,
  `data/merge_yaml`, `reports`, `src`, `tests`, `conf`, `scripts`,
  `.claude`, `pyproject.toml`, `justfile`, and `README.md` found one live
  maintained input at `data/normalized_yaml/bacterial/m_medium_ebr45.yaml`;
  the older `TOGO_M1699_M_medium_EBR45.yaml` name appeared only in archived
  validation reports.
- The ingredient groundings are mixed. Many root rows resolve to the intended
  salts, but several hydrate or salt forms are grounded too broadly: source
  `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, and `NiCl2 x 6 H2O` are grounded to terms
  whose labels drop the hydration state, and source `Ca-pantothenate` is
  grounded to pantothenate rather than an inspected calcium salt.
- The two solution identities are not supported. TOGO M1699 marks
  `Vitamin solution*` and `Mineral solution**` as local stocks belonging to
  the same M1699 page. The YAML points them to generic
  `mediadive.solution:6241` and `mediadive.solution:5281` records whose
  formulas differ materially from the local M1699 stock formulas.

## Evidence

- TOGO M1699 and NBRC `NO=905` support the main-medium formula, including
  1 L distilled water; 0.2 g yeast extract; 0.4 g `KH2PO4`; 0.4 g
  `K2HPO4 x 3 H2O`; 1 g `NH4Cl`; 0.1 g `MgCl2 x 6 H2O`; 6 g `NaHCO3`;
  0.5 g `Cysteine-HCl`; 0.25 g `Na2S x 9 H2O`; 1 mg resazurin; 10 g
  cellulose or cellobiose; 10 ml `Vitamin solution*`; 10 ml
  `Mineral solution**`; and pH 7.5.
- The generated record stores `Resazurin` as `1 G_PER_L`; TOGO and NBRC state
  1 mg in the one-liter main medium.
- The generated record stores `Vitamin solution*` and `Mineral solution**` as
  `10 G_PER_L`. TOGO and NBRC state 10 ml additions of each stock per liter.
- The local M1699 vitamin stock has 1 L water plus ten mg-scale vitamin or
  growth-factor rows. The generated record flattens those ten stock rows into
  direct top-level final-medium `G_PER_L` rows without applying the 10 ml/L
  dilution or preserving the mg units.
- The local M1699 mineral stock has 1 L water plus eleven gram-scale mineral
  rows. The generated record flattens those eleven stock rows into direct
  top-level final-medium rows without applying the 10 ml/L dilution.
- The source preparation says to mix all ingredients except cysteine-HCl,
  sodium sulfide, and vitamin solution, distribute under an `N2/CO2` 80/20 gas
  stream or `N2`, seal with butyl rubber stoppers, autoclave at 121 C for
  15 minutes, aseptically inject the remaining ingredients, and adjust to pH
  7.5 with sterile 0.6 N sulfuric acid. The generated record has gas
  ingredients and a variable sulfuric-acid row, but no `preparation_steps` and
  no pH value.
- The generated merge stores `Distilled water` as `3.0 G_PER_L` with a
  duplicate-merge note. The maintained normalized input has already been
  repaired to `1.0 G_PER_L`, so this generated record is stale relative to its
  source for the water sum.

## Completeness

- Consequentially incomplete: local `Vitamin solution*` and
  `Mineral solution**` are not represented as local nested stock formulas with
  10 ml/L addition volumes.
- Consequentially incomplete: the record does not preserve the preparation
  protocol, including gas phase, butyl-stopper sealing, autoclaving, aseptic
  post-autoclave additions, and sulfuric-acid pH adjustment.
- Consequentially incomplete: `ph_value` is absent despite TOGO and NBRC both
  recording pH 7.5.
- Empty optional target-organism and growth-evidence fields were not treated
  as defects; TOGO and NBRC M905 are recipe sources, not primary growth
  evidence for a named target organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Resazurin` is off by a factor of 1000 because a 1 mg source row became `1 G_PER_L`. | TOGO M1699 and NBRC `NO=905` list resazurin as 1 mg in the one-liter main medium; the generated YAML stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml` or the TOGO/NBRC importer. |
| Major | Local M1699 vitamin and mineral stocks were flattened into final top-level ingredients without the 10 ml/L dilution. | The source adds 10 ml of each one-liter stock to the main medium; generated vitamin and mineral rows retain stock-strength values as final `G_PER_L` ingredients. | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml` and the TOGO/NBRC solution migration or flattening logic. |
| Major | The two stock additions have wrong identities and wrong units. | `Vitamin solution*` and `Mineral solution**` are local M1699 stocks added at 10 ml/L; the generated `solutions` rows use `10 G_PER_L` and point to generic `mediadive.solution:6241` and `mediadive.solution:5281` records with different formulas. | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml`; remove unsupported generic MediaDive solution CURIEs unless equivalence is source-verified. |
| Major | The source preparation protocol and pH are mostly absent. | TOGO/NBRC specify pH 7.5, N2/CO2 80/20 or N2 gassing, butyl rubber stoppers, autoclaving at 121 C for 15 minutes, aseptic injection of cysteine-HCl, sodium sulfide, and vitamin solution, and pH adjustment with sterile 0.6 N sulfuric acid. | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml` or the TOGO/NBRC importer fields for preparation comments. |
| Major | Several hydrated or salt-specific ingredient identities are grounded too broadly. | Source labels `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and `Ca-pantothenate` do not match the broader CHEBI labels stored for those rows. | `data/normalized_yaml/bacterial/m_medium_ebr45.yaml`; re-resolve these labels with exact hydrate and salt constraints. |
| Major | The generated merge is stale for the water duplicate repair. | The generated record still has `Distilled water` 3.0 with a merged-duplicates note; `data/normalized_yaml/bacterial/m_medium_ebr45.yaml` has already repaired that row to 1.0. | Regenerate `data/merge_yaml/merged/m_medium_ebr45.yaml` from the normalized input after the source-level fixes above. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/m_medium_ebr45.yaml`, or in the
   TOGO/NBRC import transform that owns it, correct the main-medium amounts:
   keep resazurin as 1 mg per source liter, keep each stock addition as
   10 ml/L, and add `ph_value: 7.5`.
2. Model the local M1699 `Vitamin solution*` and `Mineral solution**` formulas
   as local stock recipes instead of final direct ingredients. If any stock
   members are intentionally flattened, apply both the 10 ml/L dilution and the
   source mg-to-g conversion.
3. Remove the unsupported `mediadive.solution:6241` and
   `mediadive.solution:5281` links, or replace them only with identifiers for
   the exact local M1699 stock formulas.
4. Add preparation steps that preserve the source order: mix all rows except
   cysteine-HCl, sodium sulfide, and vitamin stock; distribute under
   `N2/CO2` 80/20 or `N2`; seal with butyl rubber stoppers; autoclave at
   121 C for 15 minutes; aseptically inject the held-back ingredients; adjust
   to pH 7.5 with sterile 0.6 N sulfuric acid.
5. Re-ground `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and
   `Ca-pantothenate` against exact hydrated or salt forms, leaving unresolved
   rows explicit if exact ontology terms are unavailable.
6. Regenerate `data/merge_yaml/merged/` so
   `data/merge_yaml/merged/m_medium_ebr45.yaml` reflects the current normalized
   record and no longer carries the stale `3.0` water sum.

## Follow-up Checks

- Run open-schema validation for the corrected normalized record and the
  regenerated merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and regenerated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference validation after removing or replacing the two generic
  MediaDive solution CURIEs:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data <record> --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`.
- Run term validation after re-grounding hydrate and salt rows:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data <record> -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`.
- Manually compare the regenerated merged record against TOGO M1699 or NBRC
  `NO=905` and verify that the main formula, both stock formulas, 10 ml/L
  stock additions, pH 7.5, and anaerobic preparation instructions are
  preserved without flattening stock formulas at stock strength.

## Additional Notes

- The focused validators pass because the wrong stock references and
  stock-strength ingredient rows are syntactically valid.
- The exact local search above included ignored and hidden files. A later
  exploratory search for the common `mediadive.solution:6241` vitamin stock ID
  was too broad and was not used as a completeness claim.
