# YAML Record Review: JCM MEDIUM No. 223

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_223.yaml
- Started UTC: 2026-09-23T16:46:38Z
- Finished UTC: 2026-09-23T16:47:07Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_223.yaml`
- Stable ID: `CultureMech:002585`
- Name: `jcm_medium_no_223`
- Original name: `JCM MEDIUM No. 223`
- Category: `bacterial`
- Media term: `mediadive.medium:J223`
- Source: MediaDive import of JCM Medium 223, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=223`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_223.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_223`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_223.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_223.yaml --out /private/tmp/jcm_medium_no_223.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_223.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_223.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators do not detect the loss of a liquid indicator-solution row or missing final water.

## Identity and Grounding

The record identifies MediaDive `J223` for JCM Medium 223. The live JCM `GRMD=223` page now returns `Nothing found`, but MediaDive J223 and TogoMedium M216 both point to the same historical JCM URL and agree that the recipe is a pH 7.5 solid medium with KH2PO4, Na2HPO4 x 12 H2O, MgCl2 x 6 H2O, yeast extract, L-cysteine HCl x H2O, skim milk, 1 ml of 1% resazurin solution, agar, and water.

The generated record has the correct MediaDive ID and main dry ingredient quantities normalized to the 1001 ml MediaDive volume, but it drops the water row and misrepresents the 1 ml 1% resazurin solution as 1 g/L resazurin.

## Evidence

Inspected evidence:

- Live JCM `GRMD=223` was inspected and currently returns no medium table.
- MediaDive REST `medium/J223` lists a 1001 ml `Main sol. J223` with 1.5 g KH2PO4, 4.2 g Na2HPO4 x 12 H2O, 0.38 g MgCl2 x 6 H2O, 1 g yeast extract, 0.1 g L-cysteine HCl x H2O, 10 g skim milk, 1 ml 1% resazurin, 25 g agar, 1000 ml distilled water, and pH 7.5.
- TogoMedium M216 points to `JCM_M223` and the same `GRMD=223` URL, and preserves the 1 ml 1% resazurin solution as a solution row.

Supported:

- The MediaDive ID, JCM link, category, pH 7.5, solid agar state, dry ingredient identities, and normalized dry-ingredient amounts are supported by MediaDive.

Unsupported or incomplete:

- The generated record omits the 1000 ml water row.
- The generated record converts 1 ml of 1% resazurin solution to `1 G_PER_L`.
- `Na2HPO4 x 12 H2O` is grounded to generic disodium hydrogenphosphate rather than a dodecahydrate-specific term.

## Completeness

Consequential gaps:

- The resazurin indicator needs to be represented as a 1 ml addition of a 1% solution.
- The main distilled-water row needs to be restored.

Empty slots that are acceptable:

- No target-organism or growth-metric claim is represented.

Bounded negative search:

- An ignored-file-inclusive owner search with `rg --no-ignore --hidden` across `data/normalized_yaml` and `data/merge_yaml/merged` for `Source: JCM, ID: J223`, `Original source: JCM - JCM_M223`, and exact `jcm_medium_no_223` found only the direct MediaDive/JCM record and the TOGO M216 wrapper as source owners for JCM 223.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | A 1 ml 1% resazurin solution is modeled as 1 g/L resazurin. | MediaDive stores this row with `amount: 1`, `unit: ml`, and `attribute: 1%`; TogoMedium M216 also records `1% Resazurin solution` with `volume: 1`, `unit: ml`, and `conc_value: 1`, `conc_unit: %`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_223.yaml`. |
| Major | The 1000 ml distilled-water row is missing. | MediaDive J223 includes 1000 ml distilled water in the main solution, and TogoMedium M216 mirrors it as 1 L. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_223.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive owner search found `data/normalized_yaml/bacterial/togo_medium_m216.yaml` and `data/merge_yaml/merged/togo_medium_m216.yaml` pointing to `JCM_M223` and `GRMD=223`. The TOGO wrapper moves 1% resazurin to an empty `Unknown solution` stub and records it with `G_PER_L`, while the direct MediaDive record keeps it as a direct 1 g/L ingredient. | Both normalized owners, plus merge regeneration after source repair. |
| Minor | One hydrate-specific grounding is missing. | The source specifies `Na2HPO4 x 12 H2O`, but the target grounds it to generic disodium hydrogenphosphate. | MediaDive import/normalization and CHEBI enrichment for `data/normalized_yaml/bacterial/jcm_medium_no_223.yaml`. |

## Recommended Edits

1. Represent resazurin as a 1 ml addition of 1% resazurin solution instead of `1 G_PER_L`.
2. Add the 1000 ml distilled-water row.
3. Replace the Na2HPO4 x 12 H2O grounding with a verified dodecahydrate-specific term or leave the exact source hydrate explicit and unresolved.
4. Repair `data/normalized_yaml/bacterial/togo_medium_m216.yaml` in parallel so the resazurin solution row is not an empty `G_PER_L` solution stub, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on repaired normalized records and regenerated merged records.
- Compare the regenerated JCM 223 against MediaDive `medium/J223` and confirm all nine main rows are present.
- Manually verify that 1% resazurin remains a volume of solution rather than a mass concentration.
- Search with `rg --no-ignore --hidden 'Source: JCM, ID: J223|Original source: JCM - JCM_M223' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm same-source duplicate handling with ignored files included.

## Additional Notes

- JCM 223 itself was not available on the live JCM site during this review; MediaDive and TogoMedium were sufficient for a bounded review because both still name the original JCM accession and agree on the consequential resazurin and water rows.
