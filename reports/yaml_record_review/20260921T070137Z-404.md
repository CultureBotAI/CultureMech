# YAML Record Review: 404

- Repository: CultureMech
- Record: data/merge_yaml/merged/404.yaml
- Started UTC: 2026-09-21T07:01:02Z
- Finished UTC: 2026-09-21T07:01:37Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/404.yaml`.
- Stable identifier: `CultureMech:007493`.
- Source identity asserted by the record: NBRC medium 403.
- The generated record was last merged from `NBRC_404.yaml` on fingerprint `67a3b7e56b9352f682e3dcf986438cf055a98c1117299efff5ac3c2f90b87e5b`.
- Current authoritative owner: `data/normalized_yaml/bacterial/NBRC_404.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/404.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/404.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/404.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/404.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The record identity is internally coherent: `CultureMech:007493` is registered to `data/normalized_yaml/bacterial/NBRC_404.yaml`, and the NBRC import history points at medium 403.
- A gitignore-independent exact search for `NBRC_404` found the normalized owner, the generated merge, the ID registry, and repair/report metadata; no second normalized owner was found.
- A gitignore-independent exact search for the NBRC medium 403 servlet URL found `data/normalized_yaml/bacterial/NBRC_404.yaml`, `data/normalized_yaml/bacterial/togo_medium_m1594.yaml`, and their generated merges. The Togo record preserves the same NBRC URL as an original URL for a separate `TOGO:M1594` import and is not a duplicate owner for this NBRC record.
- A `find` over `data/raw` and `data/normalized_yaml` found no hidden or ignored raw NBRC source file for this record; only normalized NBRC YAML files are present in the search scope.

## Evidence

- NITE's current NBRC medium 403 page lists ten composition rows: `K2HPO4`, `KH2PO4`, `MgSO4 x 7H2O`, `NaCl`, `CaCO3`, yeast extract, arabinose, sodium glutamate, optional agar, and distilled water.
- NBRC lists those rows at 0.5 g, 0.6 g, 0.2 g, 0.1 g, 3 g, 0.6 g, 4 g, 6 g, 15 g, and 1 L, respectively, with pH 6.7+/-0.2.
- The current normalized owner matches that composition after its 2026-08-19 `recover_nbrc_composition.py` event and 2026-09-11 `repair_nbrc_404_score15.py` event.

## Completeness

- The generated target is not complete. It has one concatenated ingredient blob plus a second ingredient row with an empty `preferred_term` and the string `K2HPO4` in `concentration.value`.
- The current normalized owner is count-complete against NBRC medium 403 with all ten composition rows restored.
- The generated target lacks the normalized owner's NBRC `media_term`, pH range, data-quality flags, and structured NITE reference.

## Findings

- BLOCKER: `data/merge_yaml/merged/404.yaml` is stale relative to `data/normalized_yaml/bacterial/NBRC_404.yaml` and still contains the pre-recovery parse failure. Its ingredient table is not usable.
- BLOCKER: the second generated ingredient row has `preferred_term: ''`, `concentration.value: K2HPO4`, and `unit: G_PER_L`, so a chemical label is being stored as a numeric concentration.
- MAJOR: the generated record lacks all nine grounded NBRC ingredients present in the repaired owner, plus the distilled-water row and the source pH range.

## Recommended Edits

- Do not edit `data/merge_yaml/merged/404.yaml` directly; it is generated.
- Regenerate merged YAML from the repaired `data/normalized_yaml/bacterial/NBRC_404.yaml` owner so the generated record inherits the ten recovered ingredient rows, `nbrc.medium:403` source term, pH range, reference, and curated data-quality flags.
- Confirm that the regenerated record no longer has any empty `preferred_term`, any prose-concatenated formula blob, or any non-numeric `concentration.value`.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation on the regenerated `data/merge_yaml/merged/404.yaml`.
- Search all generated records for the stale fingerprint `67a3b7e56b9352f682e3dcf986438cf055a98c1117299efff5ac3c2f90b87e5b`; after regeneration, it should not remain attached to the malformed two-row parse.
- Verify that the generated row count for `404.yaml` matches the normalized owner count of ten ingredients.

## Additional Notes

- The repair script for this owner enforces the recovered NBRC medium 403 signature before it mutates the record, which makes the current owner a guarded repair of the old importer output rather than an untracked manual rewrite.
