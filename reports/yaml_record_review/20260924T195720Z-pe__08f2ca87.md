# YAML Record Review: pe

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pe__08f2ca87.yaml
- Started UTC: 2026-09-24T19:57:20Z
- Finished UTC: 2026-09-24T19:57:20Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000387 |
| Name | pe |
| Original name | PE |
| Source identity | CCAP Medium C74 / PE |
| Source path | data/normalized_yaml/bacterial/pe.yaml |
| Generated path | data/merge_yaml/merged/pe__08f2ca87.yaml |
| Merge fingerprint | 08f2ca87afc1631ef96ba173f10c5a29114290a1b88cd3ea91ea57eeaba40bf6 |

The target is a generated merged record. Future edits should be made in the
maintained CCAP input and MediaDive stock-solution import logic, not directly
in `data/merge_yaml/merged/pe__08f2ca87.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pe__08f2ca87.yaml` exited 0 and reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/pe__08f2ca87.yaml --out /private/tmp/pe__08f2ca87.strict.tsv --workers 1 --quiet` exited 0 and wrote only the TSV header. |
| Reference validator | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/pe__08f2ca87.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 checks. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/pe__08f2ca87.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

## Identity and Grounding

The `mediadive.medium:C74` grounding resolves to CCAP medium PE and the
inspected CCAP `MR_PE.pdf`. The PDF identifies PE as Plymouth Erdshreiber
medium.

The generated target does not represent the final PE medium correctly. CCAP
defines the final 1 L medium as 950 ml filtered 95% natural seawater, 50 ml SE1
soil extract, and 1 ml salt solution. The salt solution contains 200 g/L NaNO3
and 20 g/L Na2HPO4.12H2O, so the final flattened salt concentrations are 0.2
g/L NaNO3 and 0.02 g/L Na2HPO4.12H2O.

| Source component | Generated representation |
| --- | --- |
| 950 ml filtered 95% natural seawater | `Natural sea water`, 950 `G_PER_L` |
| 50 ml SE1 | `Soil`, `VARIABLE` |
| 1 ml salt solution containing 200 g/L NaNO3 | `NaNO3`, 200 `G_PER_L` |
| 1 ml salt solution containing 20 g/L Na2HPO4.12H2O | `Na2HPO4`, 2 `G_PER_L` |

The record category is `bacterial`, but the inspected CCAP source is a PE
recipe used for marine algae; the source does not support bacterial
categorization.

## Evidence

The CCAP PDF supports the PE identity, the salt stock, the 950 ml natural
seawater row, the 50 ml SE1 addition, the 1 ml salt-stock addition, and the
soil-extract preparation guidance. MediaDive C74 preserves the main solution,
the SE1 solution, and the salt solution as separate solution records, but the
generated target flattens SE1 and the salt stock as if they were final
ingredients.

The older manual CCAP curation at `data/normalized_yaml/algae/pe.yaml` is a
separate maintained input for the same CCAP PE source. It has the right final
NaNO3 concentration but still uses an incorrect CHEBI term for disodium
hydrogen phosphate dodecahydrate.

An ignored-inclusive PCRE2 search for `mediadive.medium:C74`, `Source: CCAP,
ID: C74`, `MR_PE.pdf`, and `original_name: PE` found two maintained PE inputs:

- `data/normalized_yaml/bacterial/pe.yaml`, the MediaDive/CCAP C74 projection
  that generated this target.
- `data/normalized_yaml/algae/pe.yaml`, the older manual direct-CCAP curation.

The direct CCAP and MediaDive imports also emit separate generated files,
`data/merge_yaml/merged/PE.yaml` and
`data/merge_yaml/merged/pe__08f2ca87.yaml`, even though both describe CCAP PE.

## Completeness

The generated target is materially incomplete because it omits the final 50
ml/L SE1 row, the final 1 ml/L salt-stock row, and the corrected final
salt-stock concentrations. It also omits the 50 ml/L glass-distilled-water
dilution implicit in reducing natural seawater to 95%.

No `target_organisms` are expected on this provider recipe. That optional slot
is correctly empty.

## Findings

### Major

- `data/normalized_yaml/bacterial/pe.yaml` stores the 200 g/L NaNO3 salt-stock
  concentration as a final-medium concentration. The final value should be 0.2
  `G_PER_L` if the stock is flattened.
- The same input stores the phosphate stock as 2 `G_PER_L` `Na2HPO4`, but CCAP
  lists 20 g/L `Na2HPO4.12H2O` in a stock added at 1 ml/L.
- The target stores 950 ml filtered natural seawater as 950 `G_PER_L`.
- The target stores SE1 internals as a variable `Soil` final ingredient instead
  of representing the final 50 ml/L SE1 addition and the nested soil-extract
  preparation.
- CCAP PE is split into two generated records, `pe__08f2ca87.yaml` from
  MediaDive C74 and `PE.yaml` from the direct CCAP input, instead of being
  reconciled as one maintained recipe.
- The generated target keeps a `bacterial` category, but the inspected CCAP
  source describes a marine-algae PE medium and does not support a bacterial
  classification.

### Minor

- The `NaNO3` ingredient still has legacy `mediaingredientmech_term` instead of
  `mediaingredientmech_chebi_term`.

## Recommended Edits

- Fix the MediaDive CCAP import or maintained `data/normalized_yaml/bacterial/pe.yaml`
  so PE preserves its seawater, SE1, and salt-solution additions rather than
  flattening stocks at stock strength.
- Restore `Na2HPO4.12H2O` and the 20 g/L salt-stock amount before calculating
  any final flattened phosphate value.
- Reconcile `data/normalized_yaml/bacterial/pe.yaml` with
  `data/normalized_yaml/algae/pe.yaml` so the two CCAP PE imports do not emit
  separate generated records for the same recipe.
- Reclassify PE into a source-supported category or document why it belongs in
  the bacterial collection despite the marine-algae CCAP identity.
- Refresh the NaNO3 MediaIngredientMech CHEBI link so it uses
  `mediaingredientmech_chebi_term`.

## Follow-up Checks

- Rerun the open schema, strict, reference, and term validators on the
  regenerated PE record.
- Compare the regenerated PE ingredient list against MediaDive C74 and the CCAP
  PDF to confirm that the salt stock and SE1 are correctly scoped.
- Re-run an ignored-inclusive exact search for `mediadive.medium:C74`,
  `MR_PE.pdf`, and `CCAP:PE` to confirm only one maintained CCAP PE recipe is
  active or that a deliberate category split is explicitly documented.

## Additional Notes

None found.
