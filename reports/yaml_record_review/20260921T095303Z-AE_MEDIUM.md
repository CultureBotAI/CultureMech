# YAML Record Review: AE medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/AE_MEDIUM.yaml
- Started UTC: 2026-09-21T09:51:00Z
- Finished UTC: 2026-09-21T09:53:03Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/AE_MEDIUM.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:006656`
- Label: `ae_medium`
- Original name: `AE medium`
- Category: `bacterial`
- Media term: `komodo.medium:849`
- Generated status: generated merge of `data/normalized_yaml/bacterial/KOMODO_849_AE_medium.yaml` and `data/normalized_yaml/bacterial/ae_medium.yaml`

## Validation

All narrow record-level validators passed for the generated YAML shape:

- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/AE_MEDIUM.yaml`
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/AE_MEDIUM.yaml --out /private/tmp/ae_medium.strict.tsv --workers 1 --quiet`
  - Summary: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/AE_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Summary: 1 file validated, 0 total checks, all validations passed.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/AE_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Summary: term validation passed; the command also printed the expected `eutils` / `pkg_resources` deprecation warning.
- Not checked: embedded `MediaRecipe.curation_history` semantics. The repository documents `just validate-history` for standalone records under `history/`, not as a focused one-file validator for generated embedded history.

The documented `just validate-schema`, `just validate-strict`, and `just validate-terms` entrypoints still fail before target-specific validation because this project resolves `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`; the no-project Python 3.11 invocations above exercise the same validators against the target file.

## Identity and Grounding

The generated identity is the KOMODO 849 copy of DSMZ Medium 849, merged with the direct DSMZ/MediaDive 849 import. That source-duplicate relationship is correct: `data/normalized_yaml/bacterial/KOMODO_849_AE_medium.yaml` and `data/normalized_yaml/bacterial/ae_medium.yaml` now carry the same curated DSMZ 849 ingredient set, source notes, preparation steps, and structured DSMZ PDF reference.

The generated target is stale. Its latest merge history is from 2026-08-06, while both normalized parents were repaired on 2026-09-11 to preserve the DSMZ 849 milliliter rows, distilled water, preparation steps, source fields, data-quality flags, and structured reference. The generated YAML still shows the old six-ingredient form without distilled water, with ethanol and acetic acid as `G_PER_L`, and with no DSMZ reference.

## Evidence

Supported:

- The generated `komodo.medium:849` identity, `mediadive.medium:849` provenance in notes, and source-duplicate relationship to `data/normalized_yaml/bacterial/ae_medium.yaml` match the current normalized sources.
- Glucose 5 g/L, yeast extract 3 g/L, peptone 4 g/L, and agar 9 g/L agree with the live DSMZ 849 PDF.
- The current normalized KOMODO and DSMZ parents both correctly represent absolute ethanol 30 ml/L, glacial acetic acid 30 ml/L, and distilled water 940 ml/L using `ML_PER_L`.

Unsupported or stale:

- The generated target stores absolute ethanol and glacial acetic acid as `30 G_PER_L`; DSMZ lists both as 30.0 ml.
- The generated target omits distilled water 940.0 ml entirely.
- The generated target omits DSMZ's preparation requirements: autoclave the medium without glacial acetic acid and absolute ethanol; sterilize those two components separately using Teflon filtration or a closed screw-cap glass bottle sealed with a Teflon-coated septum; then add them sterile and incubate plates in sealed containers at high humidity.
- The generated target omits the `source`/source-specific `notes` fields on every ingredient that are present in both repaired normalized parents.
- The generated target omits `sterilization`, `data_quality_flags`, and the structured DSMZ PDF `references` entry that are present in the normalized parents.

## Completeness

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-AE_MEDIUM.md' -print` searched the ignored timestamped-report directory and found no pre-existing exact AE_MEDIUM report.
- Exact `find data/merge_yaml/merged -maxdepth 1 -type f -name 'AE_MEDIUM.yaml' -print` searched ignored files and found only `data/merge_yaml/merged/AE_MEDIUM.yaml` for this generated filename.
- Exact `rg --no-ignore --hidden` searches for `KOMODO_849_AE_medium`, `komodo.medium:849\b`, `mediadive.medium:849\b`, `^name: ae_medium$`, and `DSMZ Medium: 849` covered tracked and ignored files. They found only the generated target, the normalized KOMODO 849 parent, the normalized DSMZ 849 parent, and index references.
- The DSMZ 849 PDF was fetched live and extracted locally with `mutool`; its ingredient and preparation text agree with the September-repaired normalized source YAML, not with the stale generated YAML.

## Findings

### blocker: generated AE_MEDIUM is stale relative to authoritative normalized YAML

Both normalized parents were repaired on 2026-09-11, but `data/merge_yaml/merged/AE_MEDIUM.yaml` is still an August 6 merge artifact. It lacks the repaired distilled-water row, repaired milliliter units, preparation steps, structured references, ingredient source notes, `sterilization`, and data-quality flags.

### major: ethanol and acetic acid have the wrong units in the generated target

DSMZ Medium 849 lists 30.0 ml absolute ethanol and 30.0 ml glacial acetic acid. The target's old `30 G_PER_L` values make those liquid additions look like mass concentrations.

### major: distilled water and preparation semantics were dropped

DSMZ Medium 849 lists 940.0 ml distilled water and gives special handling for volatile/corrosive liquid additions: autoclave without glacial acetic acid and absolute ethanol, sterilize those additions separately through Teflon filters or sealed glass bottles, then add them sterile. Those source facts are already present upstream and only need to survive merge regeneration.

### minor: generated references are absent

The source YAML now has the DSMZ 849 PDF in `references`, but the generated target has no `references`, so `linkml-reference-validator` performed zero checks.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/AE_MEDIUM.yaml` from the current `data/normalized_yaml/bacterial/KOMODO_849_AE_medium.yaml` and `data/normalized_yaml/bacterial/ae_medium.yaml`.
2. Confirm regeneration carries forward the DSMZ 849 `Distilled water` row and the `ML_PER_L` units for water, absolute ethanol, and glacial acetic acid.
3. Confirm regeneration carries forward the DSMZ preparation steps, `sterilization`, `data_quality_flags`, ingredient source notes, and structured `references`.
4. If regeneration still drops those fields, fix the merge generator to preserve them from duplicate normalized parents.

## Follow-up Checks

- Rerun the four one-file validators above against regenerated `data/merge_yaml/merged/AE_MEDIUM.yaml`.
- Fetch the DSMZ Medium 849 PDF again and compare the regenerated target against glucose 5.0 g, yeast extract 3.0 g, peptone 4.0 g, absolute ethanol 30.0 ml, glacial acetic acid 30.0 ml, agar 9.0 g, and distilled water 940.0 ml.
- Search with `rg --no-ignore --hidden 'Absolute ethanol|Glacial acetic acid|Distilled water|ML_PER_L' data/merge_yaml/merged/AE_MEDIUM.yaml` and confirm all three liquid rows are present with `ML_PER_L`.
- Search with `rg --no-ignore --hidden 'Teflon|sealed containers|DSMZ_Medium849' data/merge_yaml/merged/AE_MEDIUM.yaml` and confirm the preparation text and DSMZ reference survived.

## Additional Notes

- Optional organism, pH, and growth metric fields are absent from the live DSMZ 849 evidence and were not treated as defects.
