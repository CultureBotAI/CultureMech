# YAML Record Review: MODIFIED HM MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_hm_medium__454ef2cd.yaml`
- Started UTC: 2026-09-24T11:22:28Z
- Finished UTC: 2026-09-24T11:23:31Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003309` |
| Name | `modified_hm_medium` |
| Original name | MODIFIED HM MEDIUM |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_hm_medium__454ef2cd.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/modified_hm_medium.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `modified_hm_medium` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_hm_medium__454ef2cd.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_hm_medium__454ef2cd.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_hm_medium__454ef2cd.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record identifies MediaDive J961, MODIFIED HM MEDIUM, and links to
the direct JCM page for medium 961. MediaDive REST also identifies J961 as a
JCM-sourced MODIFIED HM MEDIUM at pH 7.0, so the source identity is coherent.

The grounded inorganic ingredients and glucose resolve to the intended ChEBI
terms. Bacto peptone and BD-Difco yeast extract are left ungrounded, which is
acceptable for complex product or extract ingredients without a schema-safe
provider-specific term.

## Evidence

MediaDive REST and JCM 961 support the generated 100 g/L NaCl, 1 g/L MgSO4 x
7 H2O, 2 g/L KCl, 0.36 g/L CaCl2 x 2 H2O, 0.06 g/L NaHCO3, 0.23 g/L NaBr,
0.001 g/L FeCl3 x 6 H2O, 0.5 g/L Bacto peptone, 0.1 g/L glucose, 1 g/L yeast
extract, pH 7.0, and optional 20 g/L agar for solid medium.

The source record includes 1 L distilled water. The generated record omits water
as a separate ingredient, but all ingredient quantities are expressed as
provider-supplied g/L concentrations over a 1000 ml MediaDive main solution.

JCM medium pages state that media should be sterilized by autoclaving at 121 C
for 15 min unless otherwise stated. JCM 961 gives no alternate sterilization
instruction, and MediaDive does not carry the default autoclave step into REST;
the generated record currently retains only the pH adjustment and optional agar
steps.

## Completeness

The generated record is complete for ingredient identity, concentration, pH, and
the optional solid-medium agar addition. It is incomplete for preparation
because it omits the JCM default 121 C, 15 min autoclave sterilization.

Optional `target_organisms` and `discussion` are empty. Those omissions are not
defects because JCM 961 and MediaDive J961 provide a medium recipe, not
strain-specific growth curation for a target organism.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/modified_hm_medium.yaml` omits the JCM
   default sterilization instruction. The direct JCM 961 source falls under the
   page-level default to autoclave media at 121 C for 15 min, but the generated
   record only says to adjust pH and optionally add agar for solid medium.

### Minor

None found.

## Recommended Edits

1. Add a supported autoclave preparation step to
   `data/normalized_yaml/bacterial/modified_hm_medium.yaml`, scoped to the JCM
   page-level default of 121 C for 15 min.

2. Regenerate merged recipes and downstream media pages/browser artifacts from
   the repaired normalized source.

## Follow-up Checks

- Run the focused open schema, strict schema, term, and reference validators on
  the regenerated `data/merge_yaml/merged/modified_hm_medium__454ef2cd.yaml`.
- Re-fetch MediaDive J961 and JCM 961 and verify the regenerated record still
  preserves all ten listed component concentrations, pH 7.0, the optional
  20 g/L agar step, and the default 121 C for 15 min autoclave step.

## Additional Notes

- The generated suffix `454ef2cd` is the prefix of the record's merge
  fingerprint.
