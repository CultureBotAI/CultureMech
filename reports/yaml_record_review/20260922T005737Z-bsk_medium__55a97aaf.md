# YAML Record Review: BSK-Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bsk_medium__55a97aaf.yaml
- Started UTC: 2026-09-22T00:57:37Z
- Finished UTC: 2026-09-22T00:57:37Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008950 |
| Label | bsk_medium |
| Original label | BSK-Medium |
| Category | bacterial |
| Source accession | TOGO:M2366 |
| Generated path | data/merge_yaml/merged/bsk_medium__55a97aaf.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml |
| Merge fingerprint | 55a97aaf4d78c85ff9efa94832c930c7e30b464ce63b4e1d44690ed2da5b20ee |

The reviewed target is a generated merge under `data/merge_yaml/merged`,
derived from the single normalized owner `TOGO_M2366_BSK-Medium`. The
maintained owner has already been repaired after this generated merge was last
written, so the immediate future fix is to regenerate the stale merge output.

## Validation

| Check | Result |
| --- | --- |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bsk_medium__55a97aaf.yaml` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bsk_medium__55a97aaf.yaml --out /private/tmp/bsk_medium__55a97aaf.strict.tsv --workers 1 --quiet` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bsk_medium__55a97aaf.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bsk_medium__55a97aaf.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| `just validate-history data/merge_yaml/merged/bsk_medium__55a97aaf.yaml` | Not checked: `validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries |

Direct `just validate-schema`, `just validate-strict`, `just validate-terms`,
and `just validate-references` were not rerun because the project uv environment
tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with
`TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The
equivalent no-project Python 3.11 validators above passed.

## Identity and Grounding

The source identity is internally consistent: live TOGO M2366 resolves to
`BSK-Medium`, reports DSMZ Medium 403 as `src_url`, and the generated record
uses `TOGO:M2366`. This is a TOGO wrapper around DSMZ Medium 403 and should
remain distinct from the direct KOMODO 403 and direct DSMZ 403 owners until that
source-duplicate group is reconciled deliberately.

The generated merge is stale against its maintained owner. Its embedded merge
event is dated 2026-08-06, while `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml`
has a 2026-08-25 `RESTORED_TOGO_STOCK_SOLUTION_BOUNDARIES` curation event that
rebuilt Solutions A-D from the TOGO payload, restored bovine serum albumin and
source units, and removed the unrelated LB constituents.

The repaired owner now represents TOGO's stock boundaries: Solution A through D
are populated inline, the source 1000, 200, 143, and 86 ml batch volumes are
normalized to ml/L, `ph_value: 7.6` is present, and the record-level
sterilization method is `FILTER`.

## Evidence

Supported:

| Claim | Source support |
| --- | --- |
| TOGO M2366 denotes BSK-Medium and wraps DSMZ Medium 403 | The inspected TOGO M2366 API payload has `name: BSK-Medium` and `src_url` pointing to `DSMZ_Medium403.pdf`. |
| The medium is built from Solutions A, B, C, and D | TOGO M2366 lists top-level 1000, 200, 143, and 86 ml rows for those four solutions. |
| Stock contents and preparation comments in the repaired owner | The live TOGO payload lists Solution A, B, C, and D components and the pH-adjustment, gelatine, bovine-serum-albumin, and warm-combination comments now present in the normalized owner. |

Unsupported or incomplete in the generated merge:

| Generated claim | Problem |
| --- | --- |
| Flat final ingredient list | The current normalized owner has no top-level `ingredients` and instead stores all BSK chemistry under `solutions`. The generated merge still has 19 stale top-level ingredient rows. |
| `Solution A:` through `Solution D:` with empty `composition: []` and `G_PER_L` concentration units | The current normalized owner has populated Solution A-D recipes with milliliter-per-liter source-batch additions, and TOGO lists these as solution volumes in milliliters. |
| `bidest. Water`, `343.0 G_PER_L` | The stale merge sums 200 and 143 ml water rows from Solutions B and C; the repaired owner preserves separate stock water components. |
| No bovine serum albumin | The live TOGO payload and current owner both include Solution C's 50.05 g bovine serum albumin fraction V. |
| Top-level LB Miller notes and `Tryptone 10.0 G_PER_L`, extra `Yeast extract 5.0 G_PER_L`, `Sodium chloride 10.0 G_PER_L` | TOGO M2366 does not cite LB Miller broth, Sigma L3522, BD 244520, laboratorynotes.com, or these three LB rows; the current owner has removed them. |
| `NaOH`, `variable VARIABLE` in the final ingredient list | TOGO M2366 includes 5 M NaOH as part of the Solution A pH-adjustment comment; the current owner now keeps it in `preparation_notes`. |

## Completeness

The generated merge is substantially less complete than the live TOGO payload and
than the normalized owner that already repaired this accession. It has no
populated stock-solution recipes, no Bovine serum albumin, no preserved source
units for Solution A-D additions, no preparation notes, no `ph_value`, and the
old unsupported LB Miller enrichment rows.

A gitignore-independent `rg --no-ignore --hidden` search of
`data/normalized_yaml`, `data/merge_yaml/merged`, the ID registry, the recipe
catalog, `reports/yaml_record_review`, `reports/media_content_review_manifest`,
and `data/import_tracking/reports` for `CultureMech:008950`,
`TOGO_M2366_BSK-Medium`, `TOGO:M2366`, `M2366`, and merge fingerprint
`55a97aaf...` found the active TOGO M2366 owner, this generated merge, expected
generated indexes and reports, and no existing YAML review report for this
record. `find reports/yaml_record_review -maxdepth 1 -type f -name
'*bsk_medium__55a97aaf.md'` likewise found no pre-existing report, including
ignored files in the report directory.

Empty target-organism and growth-evidence slots are not defects for this
source-only recipe.

## Findings

| Severity | Finding | Maintained owner for a future fix |
| --- | --- | --- |
| Major | `data/merge_yaml/merged/bsk_medium__55a97aaf.yaml` is stale: it still reflects the pre-repair flattened TOGO M2366 record even though the normalized owner restored stock boundaries, bovine serum albumin, pH, filtration, source units, and preparation notes on 2026-08-25. | Regenerate the merge from `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` |
| Major | The stale generated merge has empty Solution A-D stubs with `G_PER_L` solution volumes and carries the old flat ingredient list that lost stock boundaries. | Regenerate the merge from `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` |
| Major | The stale generated merge is missing Solution C's bovine serum albumin and keeps unsupported LB Miller top-level metadata plus three unsupported LB ingredient rows. | Regenerate the merge from `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` |
| Major | The stale generated merge still models 5 M NaOH as a `VARIABLE` final ingredient even though the repaired owner keeps NaOH scoped to Solution A's pH-adjustment preparation note. | Regenerate the merge from `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` |

No blocker or minor findings found.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/bsk_medium__55a97aaf.yaml` from
   `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` so the merge
   reflects the repaired Solution A-D structure.
2. Inspect the regenerated merge to confirm it has no top-level LB Miller note
   block or LB Tryptone, Yeast extract, Sodium chloride, or variable NaOH rows.
3. Keep future source-level changes in
   `data/normalized_yaml/bacterial/TOGO_M2366_BSK-Medium.yaml` or the TOGO import
   repair code that produced the 2026-08-25 curation event.

## Follow-up Checks

- Rerun `just verify-merges` to prove the generated merge is fresh.
- Rerun focused schema, strict, term, and reference validation on the regenerated
  `data/merge_yaml/merged/bsk_medium__55a97aaf.yaml`.
- Manually compare the regenerated merge against the live TOGO M2366 payload to
  confirm Solution A-D batch volumes and preparation comments still agree.

## Additional Notes

- `reports/media_content_review_manifest.tsv` now marks the maintained
  `TOGO_M2366_BSK-Medium.yaml` owner `PASS`; the generated merge predates that
  repaired normalized state.
