# YAML Record Review: 1_10_sabourauds_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml`
- Started UTC: 20260921T050259Z
- Finished UTC: 20260921T050354Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:010521` |
| Label | `1_10_sabourauds_agar` |
| Original label | `1/10 SABOURAUD'S AGAR` |
| Category | `fungal` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml`, `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml`, their import normalization, or merge regeneration |
| Merge owner | `merged_from: [1_10_sabourauds_agar]` |
| Maintained owner | `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` |

This generated singleton is the direct MediaDive/JCM import of JCM Medium 35.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml --out /private/tmp/1_10_sabourauds_agar__6d784de4.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_sabourauds_agar__6d784de4.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly identifies JCM Medium 35, `1/10 SABOURAUD'S AGAR`. The same JCM Medium 35 source is also represented as the TOGO Medium M28 generated singleton at `data/merge_yaml/merged/1_10_sabourauds_agar.yaml`; both should be one source-duplicate cluster after normalization.

## Evidence

Supported by inspected source text:

- JCM 35 supports the Glucose, Peptone, KH2PO4, MgSO4 x 7 H2O, NaNO3, Agar, and Distilled water rows.
- JCM 35 supports pH 6.8-7.0.
- JCM 35 supports `physical_state: SOLID_AGAR` because the source lists 20 g agar.

Unsupported or incomplete in the generated record:

- JCM lists distilled water as 1.0 L, but the direct JCM record has no water row.
- JCM's default instruction to autoclave media at 121 degrees C for 15 min unless otherwise stated is not represented.
- The JCM URL is present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The pH adjustment is present.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the JCM page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:009437`, `CultureMech:010521`, exact `TOGO:M28`, exact `mediadive.medium:J35`, `JCM_M35`, and both Sabouraud merge fingerprints found only the TOGO owner, this direct JCM owner, their generated singletons, and ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The direct JCM 35 and TOGO M28 imports of the same source recipe failed to merge as source duplicates. | Both generated records trace to the JCM 35 URL, but they have distinct merge fingerprints because the direct JCM import omits water while the TOGO import includes a mis-unit water row and lacks pH. | `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` and `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml`, followed by merge regeneration. |
| Major | The JCM water row is missing. | JCM 35 lists distilled water as 1.0 L; the direct JCM generated record has no water row. | `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` or the JCM importer. |
| Major | JCM's default sterilization condition is missing. | The JCM page says media should be autoclaved at 121 degrees C for 15 min unless otherwise stated; the generated record has only the pH adjustment step. | `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` or the JCM importer. |
| Minor | Structured references are absent. | The JCM URL is present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` or the JCM import mapping. |

## Recommended Edits

1. Normalize the direct JCM 35 and TOGO M28 owners so both represent JCM 35 water, pH, autoclaving, and source accessions consistently enough to collapse into one generated source-duplicate cluster.
2. Add the 1 L distilled-water row to the direct JCM owner.
3. Add the JCM default autoclave instruction.
4. Add a structured reference for JCM Medium 35.
5. Regenerate `data/merge_yaml/merged/` and confirm the TOGO and JCM copies merge as source duplicates.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` after adding a structured reference.
- `just verify-merges` to prove the JCM and TOGO generated outputs coalesce after regeneration.
- `just validate-strict` and `just validate-terms` on the regenerated 1/10 Sabouraud's Agar merge record.
- Manual comparison to JCM Medium 35 for all ingredients, pH 6.8-7.0, 1 L water, and the default autoclave instruction.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
