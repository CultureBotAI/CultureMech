# YAML Record Review: GYA(0.5% NaCl)

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gya_0_5_nacl.yaml`
- Started UTC: 2026-09-23T08:02:26Z
- Finished UTC: 2026-09-23T08:03:27Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:008467`, `gya_0_5_nacl`, imported from Togo Medium `M1892` / `NBRC_M1139`.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gya_0_5_nacl.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The direct Togo identity is coherent: the normalized parent and generated record both carry `TOGO:M1892`, `GYA(0.5% NaCl)`, and `NBRC_M1139`. Exact ignored-file-inclusive search across `data/normalized_yaml/bacterial` and `data/merge_yaml/merged` found one Togo parent and one generated Togo record for this identity.

The `kg_microbe_match: mediadive.medium:53` link is suspect. MediaDive medium 53 is DSMZ `CORYNEBACTERIUM AGAR`; it has casein peptone, different yeast extract and glucose amounts, and pH 7.2-7.4, so it is not ingredient-equivalent to the Togo GYA recipe.

## Evidence

The Togo `M1892` API payload contains one main solution with 1 L `DW`, 2.5 g Yeast extract, 5 g NaCl, 10 g Glucose, and 15 g Agar. Those ingredient names and mass values are reproduced in the generated record.

MediaDive medium 53 has 10 g Casein peptone, 5 g Yeast extract, 5 g Glucose, 5 g NaCl, 15 g Agar, 1000 ml Distilled water, and an instruction to adjust pH to 7.2-7.4.

## Completeness

The Togo recipe is small and has no nested solutions or preparation comments in the Togo payload. The generated record still misrepresents the 1 L `DW` solvent volume as a 1 g/L mass concentration and leaves `DW` ungrounded.

## Findings

- `DW` is misunitized. Togo reports a 1 L water volume, but the generated record stores it as `value: '1'`, `unit: G_PER_L`.
- `DW` is not grounded to water (`CHEBI:15377`), despite equivalent distilled-water rows being groundable elsewhere in CultureMech.
- `kg_microbe_match: mediadive.medium:53` appears incorrect. DSMZ 53 / MediaDive 53 describes `CORYNEBACTERIUM AGAR`, contains casein peptone, doubles the yeast extract, halves the glucose, and specifies pH 7.2-7.4; the Togo `M1892` GYA recipe does not.
- `Yeast extract` is ungrounded. This is not a simple chemical term, but the row should be checked against available complex-component mappings during recuration.

## Recommended Edits

- Correct Togo liter solvent import so `DW` is not emitted as `1 G_PER_L`.
- Ground `DW` to `CHEBI:15377` water.
- Re-evaluate and remove or replace `kg_microbe_match: mediadive.medium:53`.
- Attempt a complex-component grounding for Yeast extract if a suitable mapping is available.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Recompare the corrected record against Togo `M1892`.
- Verify that the record is no longer linked to MediaDive 53 unless a curator intentionally records that relationship as non-equivalent.

## Additional Notes

The no-ignore identity search also found separate KOMODO records sourced from DSMZ/MediaDive 53; those support the conclusion that `mediadive.medium:53` belongs to Corynebacterium Agar rather than to this Togo GYA recipe.
