# YAML Record Review: 225M1 MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/225M1_MEDIUM.yaml`
- Started UTC: 2026-09-21T06:01:39Z
- Finished UTC: 2026-09-21T06:03:34Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003254` |
| Name | `225m1_medium` |
| Original name | `225M1 MEDIUM` |
| Category | `bacterial` |
| Source term | `mediadive.medium:J905`, `225M1 MEDIUM` |
| Source URL | `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=905` |
| Merge fingerprint | `69d8f8e9e5e34c0548b22575f9a10a4eac37b697d4d0f0a6b7e604eb0439d7eb` |
| Maintained owner | `data/normalized_yaml/bacterial/225m1_medium.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated MediaDive/JCM import for JCM Medium 905, 225M1 MEDIUM. The generated artifact and its maintained owner currently match for the scientific fields reviewed here, so future corrections belong in `data/normalized_yaml/bacterial/225m1_medium.yaml` followed by merge regeneration.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/225M1_MEDIUM.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/225M1_MEDIUM.yaml --out /private/tmp/225M1_MEDIUM.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/225M1_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/225M1_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes MediaDive `J905` / JCM Medium 905, 225M1 MEDIUM:

- The YAML carries `media_term.term.id: mediadive.medium:J905` and `media_term.term.label: 225M1 MEDIUM`.
- Its notes point to the JCM GRMD 905 URL.
- The inspected MediaDive REST record resolves `J905` as a JCM-sourced medium named `225M1 MEDIUM`, links to the same JCM `GRMD=905` page, and reports pH 6.8.
- The inspected JCM Medium 905 page resolves and lists `225M1 MEDIUM` with the same basal formulation.

Ingredient grounding is largely sound. The exact salts match their hydrate states: `MgCl2 x 6 H2O` to magnesium dichloride hexahydrate, `CaCl2 x 2 H2O` to calcium chloride dihydrate, `NaCl` to sodium chloride, `K2SO4` to potassium sulfate, and sodium pyruvate to sodium pyruvate. `Casamino acids` and `Yeast extract` are correctly left ungrounded because each is a mixture rather than one CHEBI molecule. `Starch` is grounded to the broader starch term; the JCM and MediaDive sources specifically call it soluble starch / soluble.

## Evidence

The inspected JCM Medium 905 page and MediaDive REST `J905` payload support the eight imported main-solution rows:

- 1 g/L Casamino acids, BD-Difco.
- 1 g/L Yeast extract, BD-Difco.
- 2 g/L soluble starch.
- 20 g/L NaCl.
- 300 g/L MgCl2 x 6 H2O.
- 0.13 g/L CaCl2 x 2 H2O.
- 5 g/L K2SO4.
- 0.13 g/L sodium pyruvate.

They also support pH 6.8, final volume to 1 L with distilled water, adjustment with 25% KOH, and an optional solid-medium variant made by adding 23 g/L Bacto agar (BD-Difco). The current record preserves the pH and basal ingredient amounts, but drops the BD-Difco and soluble qualifiers from structured ingredient labels, keeps distilled water and KOH only inside the preparation sentence, and does not expose the JCM URL as a structured reference.

## Completeness

Consequential gaps:

- Distilled water to 1 L and variable 25% KOH are procedural requirements in the source but absent from `ingredients`.
- Source ingredient qualifiers are partially lost: MediaDive and JCM preserve BD-Difco for Casamino acids, BD-Difco for Yeast extract, and soluble for Starch.
- The structured `references` list is absent; the JCM source URL is only in free-text `notes`, so `linkml-reference-validator` had no source reference to inspect.
- MediaDive/JCM `J905` and TOGO `M947` both represent the same JCM Medium 905 liquid recipe, while TOGO `M948` represents the documented 23 g/L Bacto agar solid variant. These imports remain three generated records because the MediaDive record omits explicit water and KOH rows that the TOGO liquid record models.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected JCM and MediaDive source records verify the recipe but do not support a target-organism assertion.
- `physical_state: LIQUID` is correct for this basal MediaDive `J905` record. The JCM agar sentence is optional, and the TOGO import models it separately as `TOGO_M948_225M1_Medium.yaml` / generated `225m1_medium__936a42af.yaml`.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, and `data/raw` for `CultureMech:003254`, `mediadive.medium:J905`, `GRMD=905`, the `69d8f8e9` merge fingerprint, `225m1_medium`, `225M1 MEDIUM`, `TOGO_M947`, `TOGO_M948`, and related JCM aliases found the expected MediaDive owner, expected generated MediaDive merge, two expected TOGO sibling records and merges, source index rows, and no additional `J905` MediaRecipe records.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water and KOH are not represented as structured ingredients. | JCM instructs adding components to distilled water and bringing the volume to 1 L, then adjusting pH to 6.8 with 25% KOH; the generated and normalized MediaDive records keep that sentence but have no `Distilled water` or variable `KOH` ingredient rows. | `data/normalized_yaml/bacterial/225m1_medium.yaml` |
| Major | The source-duplicate imports for JCM Medium 905 are not reconciled. | The inspected MediaDive `J905` record and JCM `GRMD=905` page identify the reviewed record as the same 225M1 liquid recipe that TOGO imports as `TOGO:M947`; the two records have distinct CultureMech IDs and generated merges because the TOGO import includes explicit water and KOH rows while the MediaDive import omits them. | `data/normalized_yaml/bacterial/225m1_medium.yaml`, `data/normalized_yaml/bacterial/TOGO_M947_225M1_Medium.yaml`, and the merge-equivalence logic |
| Minor | Product and material qualifiers are present in the source but missing from structured ingredient names. | JCM and MediaDive state BD-Difco Casamino acids, BD-Difco Yeast extract, and soluble starch; the MediaDive YAML rows use only `Casamino acids`, `Yeast extract`, and `Starch`. | `data/normalized_yaml/bacterial/225m1_medium.yaml` |
| Minor | Structured source provenance is absent. | The record cites MediaDive/JCM only in `notes`, so the reference validator performed zero checks. | `data/normalized_yaml/bacterial/225m1_medium.yaml` |

## Recommended Edits

1. Add explicit `Distilled water` at 1 L final volume and variable `KOH` rows to `data/normalized_yaml/bacterial/225m1_medium.yaml`, matching the treatment of JCM Medium 905 in `data/normalized_yaml/bacterial/TOGO_M947_225M1_Medium.yaml` while preserving the unknown KOH amount.
2. Preserve the source material qualifiers by changing the ingredient labels to `Casamino acids (BD-Difco)`, `Yeast extract (BD-Difco)`, and `Soluble starch`.
3. Add a structured source reference for MediaDive `J905` / the JCM `GRMD=905` page so the URL is validator-visible instead of only free text.
4. Review merge identity between the corrected MediaDive `J905` record and TOGO `M947`; after their basal recipes are harmonized, regenerate `data/merge_yaml/merged/` and confirm the JCM Medium 905 liquid import no longer appears as two independent generated recipes unless a curated merge rule explicitly keeps MediaDive and TOGO source accessions separate.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/225m1_medium.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/225m1_medium.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/225m1_medium.yaml` after any ingredient-label or KOH grounding edit.
- `just validate-references data/normalized_yaml/bacterial/225m1_medium.yaml` after adding structured MediaDive/JCM provenance.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 905 and MediaDive REST `J905` for the eight basal ingredients, final 1 L distilled-water volume, pH 6.8, 25% KOH adjustment, and optional 23 g/L Bacto agar solid variant.

## Additional Notes

- The optional 23 g/L Bacto agar sentence is already represented by the separate TOGO solid-agar import (`TOGO:M948` / `225m1_medium__936a42af.yaml`); it should not be added to this liquid MediaDive record as a required basal ingredient.
- `data/normalized_yaml/bacterial/mediadive_4906_Main_sol_J905.yaml` is the imported MediaDive main solution for this medium. It carries the same eight component rows plus the JCM preparation note, but it also contains a stale `See source for composition` placeholder ingredient and `incomplete_composition` flag even though its `composition` list is populated.
- `linkml-reference-validator` performed zero checks because this record has no structured `references` entries.
