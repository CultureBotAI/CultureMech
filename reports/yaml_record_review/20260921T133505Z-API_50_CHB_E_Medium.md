# YAML Record Review: API 50 CHB/E Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/API_50_CHB_E_Medium.yaml
- Started UTC: 2026-09-21T13:34:15Z
- Finished UTC: 2026-09-21T13:35:06Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| Stable ID | CultureMech:009376 |
| Name | api_50_chb_e_medium |
| Original name | API 50 CHB/E Medium |
| Category | bacterial |
| Source | TOGO:M2832 |
| Reviewed artifact | data/merge_yaml/merged/API_50_CHB_E_Medium.yaml |
| Maintained owner | data/normalized_yaml/bacterial/api_50_chb_e_medium.yaml, with generated merge output under data/merge_yaml/merged/ |

This review targeted the generated canonical merge. Its `merged_from` list contains only `api_50_chb_e_medium`.

An ignored-file-inclusive exact search for `CultureMech:009376`, `TOGO:M2832`, `api_50_chb_e_medium`, and `M2832` covered `data/normalized_yaml`, `data/merge_yaml`, `data/culturemech_id_registry.tsv`, `data/culturemech_recipe_catalog.tsv`, `reports/media_content_review_manifest.tsv`, `data/import_tracking/reports/concentration_plausibility.tsv`, and `data/import_tracking/reports/merged_duplicates.tsv`. It found this generated merge, the normalized owner, generated indexes, registry/catalog rows, and the media content manifest row for the same CultureMech ID.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed with `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/API_50_CHB_E_Medium.yaml`; no issues found. |
| Strict schema | Passed with `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/API_50_CHB_E_Medium.yaml --out /private/tmp/API_50_CHB_E_Medium.strict.tsv --workers 1 --quiet`; 1 file scanned, 0 error rows. |
| Reference validator | Passed with `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/API_50_CHB_E_Medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; 1 file validated, 0 total active checks. |
| Term validator | Passed with `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/API_50_CHB_E_Medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; only the upstream `eutils`/`pkg_resources` deprecation warning was emitted. |
| Embedded curation history | Not checked: this repository exposes `just validate-history` for standalone `history/` files, not a focused `MediaRecipe.curation_history` check for one merged recipe. |

The stale generated record passes shape validation; its defect is that the generated artifact has not caught up to the maintained normalized repair.

## Identity and Grounding

The target identity is correct. Togo `M2832` names the recipe `API 50 CHB/E Medium`; the generated record carries `TOGO:M2832` and the same source label.

This Togo record is an opaque product recipe, not an internally disclosed formulation. The live Togo API lists a single `API 50 CHB/E Medium` component with `volume: 1`, `unit: L`, GMO `GMO_002128`, and label `API &reg; 50 CHB/E medium`. It does not list sugars, salts, nitrogen sources, vitamins, or preparation subcomponents to curate as separate ingredients.

## Evidence

| Claim | Source support |
| --- | --- |
| The record denotes API 50 CHB/E Medium from TOGO M2832. | Supported by the live Togo M2832 payload. |
| The formulation is one liter of API 50 CHB/E Medium as a product placeholder. | Supported: Togo lists only one component, `API 50 CHB/E Medium`, at 1 L. |
| Incubation condition context is 28 C for 24-48 h under aerobic conditions. | Supported by Togo's comment describing UCMB5113 resuspended in API 50 CHB/E Medium for API fermentation strips. |
| Internal product formula is unknown. | Supported by absence from the inspected Togo payload; the source contains no rows beyond the one-liter product. |

The maintained normalized record reflects that interpretation: it stores `API 50 CHB/E Medium` as `1000 ML_PER_L`, cites `TOGO M2832` on the ingredient, adds a note that the source does not disclose the product's internal formulation, stores `temperature_value: 28.0`, flags `ingredients_curated` and `has_unmapped_ingredients`, and retains the Togo reference. The generated merge still has the pre-repair `1 G_PER_L` placeholder and none of the 2026-09-07 source repair details.

## Completeness

The source-level incompleteness is correctly bounded in the normalized owner: Togo provides a commercial product volume and incubation context but no internal formulation. Empty stock-solution, pH, atmosphere, organism, growth-evidence, and variant fields are not direct defects on this source-only opaque product record.

The generated merge is incomplete only because it is stale relative to the maintained owner. It lacks the normalized repair event, Togo reference, data quality flags, ingredient source/note, `1000 ML_PER_L` product amount, and `temperature_value: 28.0`.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The generated merge has stale product quantity semantics. | The live Togo M2832 payload lists one liter of API 50 CHB/E Medium. `data/normalized_yaml/bacterial/api_50_chb_e_medium.yaml` records that as `1000 ML_PER_L` and marks the source repair on 2026-09-07; `data/merge_yaml/merged/API_50_CHB_E_Medium.yaml` still encodes the opaque product as `1 G_PER_L`. | Regenerate `data/merge_yaml/merged/API_50_CHB_E_Medium.yaml` from `data/normalized_yaml/bacterial/api_50_chb_e_medium.yaml`; do not hand-edit the generated merge. |
| minor | The generated merge omits the normalized source context and retained Togo evidence. | The normalized owner has ingredient/source notes, the Togo reference, `temperature_value: 28.0`, and `ingredients_curated` / `has_unmapped_ingredients` flags explaining why the one-liter product placeholder is intentional. Those reviewed fields have not propagated to the generated merge. | Regenerate the generated merge and confirm the merge renderer preserves scalar condition fields, references, and data quality flags where applicable. |

No blocker findings: the record denotes the correct TOGO recipe and all YAML/reference/term validators pass.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/API_50_CHB_E_Medium.yaml` from `data/normalized_yaml/bacterial/api_50_chb_e_medium.yaml` so the product volume, temperature, reference, flags, and 2026-09-07 repair history propagate.
2. After regeneration, confirm the merged ingredient remains an opaque `API 50 CHB/E Medium` product at `1000 ML_PER_L`; do not invent an internal composition that Togo M2832 does not disclose.
3. If the merge step drops `temperature_value`, references, or data quality flags despite fresh regeneration, fix the merge-preservation rule rather than duplicating a manual edit in `data/merge_yaml/merged/`.

## Follow-up Checks

- Run the focused open-schema, strict, reference, and term validators on `data/merge_yaml/merged/API_50_CHB_E_Medium.yaml` after regeneration.
- Run `just verify-merges` and `just audit-merge-freshness` to prove the generated record no longer lags the normalized owner.
- Recompare the regenerated record with the live Togo `M2832` API payload and the normalized owner: the source should still contain only one liter of opaque API 50 CHB/E Medium plus the 28 C, 24-48 h incubation comment.

## Additional Notes

The Togo public `/medium/M2832` route was not needed; the structured payload for this review was `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2832`.
