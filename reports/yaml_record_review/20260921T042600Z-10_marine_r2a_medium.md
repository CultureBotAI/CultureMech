# YAML Record Review: 10_marine_r2a_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/10_marine_r2a_medium.yaml`
- Started UTC: 20260921T042409Z
- Finished UTC: 20260921T042600Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008637` |
| Label | `10_marine_r2a_medium` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml` or the merge pipeline |
| Merge owner | `merged_from: [10_marine_r2a_medium]` |
| Maintained owner | `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml` |

This generated record was last merged on Aug 6. The maintained normalized source has since been repaired on Sep 7 with NBRC Medium 1347 source support, a 1 L aged-seawater amount, source-scoped ingredient notes, a preparation step, references, and data-quality flags.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/10_marine_r2a_medium.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/10_marine_r2a_medium.yaml --out /private/tmp/culturemech-review-10-marine-r2a.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-10-marine-r2a.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/10_marine_r2a_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/10_marine_r2a_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

The passing validators do not detect the stale generated gram-per-liter seawater amount.

## Identity and Grounding

The generated identity is mostly coherent:

- TOGO M2047 resolves to `10% marine R2A medium`.
- TOGO M2047 records `original_media_id: NBRC_M1347` and the NBRC Medium 1347 source URL.
- Live NBRC Medium 1347 is also titled `10% marine R2A medium`.

The artifact is still stale relative to the maintained record:

- `Aged seawater` is `1 G_PER_L` in the generated merge but `1 L` in both TOGO/NBRC and the maintained normalized record.
- `Agar (if needed)` is still ungrounded in the generated merge; the maintained owner normalizes it to `Agar`, grounds it to `CHEBI:2509`, and keeps the optional status in the source note and preparation text.
- The generated merge has no `references`, no `data_quality_flags`, no source-scoped ingredient support, and no preparation step.

## Evidence

Supported by inspected source text:

- TOGO M2047 and NBRC Medium 1347 both identify the medium as `10% marine R2A medium`.
- TOGO M2047 and NBRC Medium 1347 both list aged seawater 1 L, R2A Broth (DAIGO) 0.32 g, and optional agar 15 g.

Unsupported or incomplete in the generated record:

- `Aged seawater` has `concentration.value: '1'` and `unit: G_PER_L`, which changes NBRC and TOGO's 1 L solvent into a one-gram-per-liter ingredient.
- The generated record has only generic GMO-derived notes, not citations tying each amount to NBRC Medium 1347 or TOGO M2047.
- The generated record omits the maintained `MIX` step that says to dissolve R2A Broth and optional agar in 1 L aged seawater.

## Completeness

- This generated record is one regeneration behind its authoritative source: it still reflects the Jan-Mar import shape plus the Aug 6 merge event and does not include the Sep 7 `RESOLVED_SCORE25_CROSS_REFERENCE` normalized repair.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `M2047`, `10_marine_r2a_medium`, `10% marine R2A`, `NBRC_M1347`, and `NO=1347` found only this generated merge, its maintained normalized owner, and normalized index entries.
- Empty optional organism and growth-evidence slots are not defects for this compact source-import record; TOGO/NBRC support the medium formulation, not a tested target organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated aged-seawater quantity has the wrong unit. | NBRC Medium 1347 and TOGO M2047 both list 1 L aged seawater; the generated merge says `1 G_PER_L`. | `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml` already has `1 L`; regenerate `data/merge_yaml/merged/`. |
| Major | The generated merge is missing the Sep 7 normalized curation that added direct source support and preparation detail. | The maintained record now contains source-scoped ingredient notes, a NBRC/TOGO summary note, a `MIX` preparation step, references, and `ingredients_curated` / `has_ontology_mappings` flags; the generated merge has none of them. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml`. |
| Minor | The optional agar label is less clean than the maintained owner. | TOGO and NBRC say `Agar (if needed)`; the maintained record represents the ingredient as `Agar`, grounds it to `CHEBI:2509`, and preserves optionality in the evidence notes. | Regenerate from the maintained normalized record. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/10_marine_r2a_medium.yaml` from `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml`.
2. Confirm the regenerated artifact carries `Aged seawater` as `1 L`, `Agar` as 15 g/L with `CHEBI:2509`, R2A Broth (DAIGO) as 0.32 g/L, the NBRC/TOGO references, and the Sep 7 normalized curation event.
3. If regeneration still emits `1 G_PER_L` for aged seawater, fix the TOGO volume-unit conversion path for solvent rows before regenerating again.

## Follow-up Checks

- `just verify-merges` to prove the merge artifact is fresh relative to `data/normalized_yaml/bacterial/10_marine_r2a_medium.yaml`.
- `just validate-strict data/merge_yaml/merged/10_marine_r2a_medium.yaml` to recheck the closed schema after regeneration.
- `just validate-terms data/merge_yaml/merged/10_marine_r2a_medium.yaml` to verify the regenerated agar grounding.
- Manual source check against TOGO M2047 and NBRC Medium 1347 to verify that the seawater volume has not been re-coerced into a mass concentration.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
