# YAML Record Review: CM + YE medium

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cm_ye_medium.yaml`
- Started UTC: `2026-09-22T10:11:05Z`
- Finished UTC: `2026-09-22T10:11:30Z`
- Verdict: needs curation

## Target

Generated bacterial `MediaRecipe` record `CultureMech:005996` for KOMODO Medium `549`, enriched from DSMZ Medium `549`, generated from `data/normalized_yaml/bacterial/KOMODO_549_CM_YE_medium.yaml` on merge fingerprint `48e2ee5b753634439620f91415f3711a54817fef20249bf5725d67e8f15acaf8`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/cm_ye_medium.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The identity is partially indirect but coherent: this is KOMODO Medium 549 enriched by copying DSMZ Medium 549, `CM + YE MEDIUM`.

The base salt and agar groundings are appropriate, and Casamino acids is intentionally left without a single CHEBI term. `FeSO4 x 7 H2O` and `HCl` are grounded, but they are internals of the DSMZ Fe stock rather than final top-level ingredients.

The ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/import_tracking`, and `reports` found a separate direct DSMZ `cm_ye_medium__86792d50.yaml` branch. That sibling also merged two KOMODO 549 DSM-specific aliases. Ignored files were included.

## Evidence

The DSMZ Medium 549 PDF supports the base formula: 7.5 g vitamin-free Casamino acids, 10.0 g yeast extract, 20.0 g `MgSO4 x 7 H2O`, 3.0 g sodium citrate, 2.0 g KCl, 200.0 g NaCl, 15.0 g agar, 1000.0 ml distilled water, and pH 7.4 adjusted with NaOH.

The DSMZ table does not list 10 g/L final `FeSO4 x 7 H2O`. It lists `Fe*` as 10.0 ppm and defines the asterisk as 4.98% `FeSO4 x 7 H2O` stock in 0.001 M HCl, with 1 ml stock added per liter of medium.

## Completeness

The generated record is complete for the base formula, agar, high NaCl, and NaOH pH adjustment.

The Fe stock is not represented accurately. Its FeSO4 and HCl contents were flattened into final top-level ingredient rows, and the generated record therefore loses the 1 ml/L stock relationship to DSMZ `Fe*`.

No target organism or growth evidence is present.

## Findings

- Severe: `FeSO4 x 7 H2O` is stored as `10 G_PER_L`, but DSMZ specifies 10 ppm Fe supplied by a 1 ml/L FeSO4 stock.
- Major: `HCl` from the 0.001 M Fe stock is stored as a final top-level `0.03646 G_PER_L` row.
- Major: the direct DSMZ Medium 549 branch and its two KOMODO alias children are unmerged with this KOMODO-only branch even though they represent the same DSMZ formula.

## Recommended Edits

- Replace the flattened FeSO4/HCl rows with a structured 1 ml/L Fe stock addition or a correctly diluted final FeSO4 concentration with the 4.98% FeSO4 / 0.001 M HCl stock retained as source context.
- Preserve the NaOH pH 7.4 adjustment as preparation context, not a final ingredient.
- Regenerate and merge the KOMODO 549 branch with the direct DSMZ 549 branch after Fe-stock handling is fixed.

## Follow-up Checks

- Validate the regenerated record with open schema, strict, reference, and term validators.
- Confirm no final FeSO4 row remains at `10 G_PER_L`.
- Confirm no final HCl row remains from the Fe stock.
- Confirm `cm_ye_medium.yaml` and `cm_ye_medium__86792d50.yaml` merge or are intentionally linked as source duplicates.

## Additional Notes

Empty optional fields are not defects.
