# YAML Record Review: CaMM noCarbon

- Repository: CultureMech
- Record: data/merge_yaml/merged/CaMM_noCarbon.yaml
- Started UTC: 2026-09-22T03:20:32Z
- Finished UTC: 2026-09-22T03:20:32Z
- Verdict: needs curation

## Target

- Generated record: `CultureMech:015501`, `CaMM noCarbon`, class `MediaRecipe`.
- Merge lineage: one source recipe, `camm_nocarbon`, on fingerprint `0a045d7583781cfb2ff4c4b0cff5ac4232c4bd79cbff9eaba3f4fde9427bf03e`.
- Authoritative owner: `data/normalized_yaml/specialized/camm_nocarbon.yaml`.
- Claimed source identity: CultureBotHT medium `CaMM noCarbon`, imported from `CaMM_noCarbon.yaml`, source note `Source: FEBA media definitions`.

## Validation

- Open LinkML schema validation passed for the generated merge.
- Strict CultureMech validation passed for the generated merge.
- LinkML reference validation passed for the generated merge with 0 reference checks.
- LinkML term validation passed for the generated merge.
- Embedded `curation_history` was not separately validated because `just validate-history` checks standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in a merged record.

## Identity and Grounding

- The generated record and normalized owner agree on ID `CultureMech:015501`, label `CaMM noCarbon`, source database `CultureBotHT`, source accession `CaMM noCarbon`, and the complete 13-row ingredient list. The generated merge only adds its `MERGED_RECIPES` event and fingerprint.
- The record denotes a CultureBotHT FEBA carbon-omission variant distinct from MediaDB `CAMM`; a gitignore-independent exact search found no second `CultureMech:015501`, `CaMM_noCarbon`, `camm_nocarbon`, or `CaMM noCarbon` owner.
- The specialized category is the CultureBotHT importer's default when a name does not match a bacterial, algae, or fungal name pattern; that fallback is internally consistent with the imported record.
- Every ingredient row has a structured `term.id` and LinkML term validation accepts those identifiers and labels. `Ammonium heptamolybdate tetrahydrate` preserves the exact hydrate CAS and molecular weight in notes while using the broader-looking `CHEBI:91249` / `ammonium molybdate` label already used across the corpus for molybdate salts.

## Evidence

- The only structured source is the CultureBotHT repository root plus the import event naming `CaMM_noCarbon.yaml`; no pinned CultureBotHT commit, blob URL, local raw source file, `source_data` block, or primary FEBA reference is attached.
- The stored CultureBotHT repository URL currently fails at the GitHub repository API, and the guessed live raw paths for `media_yaml/CaMM_noCarbon.yaml` on both `main` and `master` return 404. A bounded `find .. -maxdepth 4` search included ignored and hidden entries and found no sibling `CultureBotHT` checkout or local `CaMM_noCarbon.yaml` source file under the neighboring Mechs tree.
- Because the source YAML was not recoverable, the review could verify local lineage and importer behavior but could not compare the stated phosphate salts, ammonium sulfate, calcium chloride, magnesium sulfate, trace metal salts, or `Used for growing Xanthomonas` description against the authoritative CultureBotHT input.

## Completeness

- Optional preparation, pH, incubation, sterilization, application, `target_organisms`, `variants`, and `references` fields are empty. The inspected local record has no source text for those details, so their absence is not independently a defect.
- The description's genus-level `Xanthomonas` wording is not normalized to a target organism or strain-specific growth evidence; that is acceptable until a recovered CultureBotHT snapshot or primary source makes the organism and medium relationship explicit.
- The `has_unmapped_ingredients` flag is stale: all 13 current ingredient rows have a `term.id`, and the generated record passes LinkML term validation.

## Findings

- Major: CultureBotHT `CaMM_noCarbon.yaml` is not reproducible from the record. `data/normalized_yaml/specialized/camm_nocarbon.yaml` stores only the repository root and a filename note; the live source location no longer resolves from the stored URL or common raw paths, leaving every source-derived ingredient and concentration unsupported by a recoverable citation.
- Minor: `data_quality_flags` still includes `has_unmapped_ingredients` even though every ingredient in the active owner has a structured ontology term.
- Minor: the CultureBotHT source identity is encoded only in `sources` and the import event. There is no `source_data` object, pinned upstream commit, or blob/path reference that would let a future curator distinguish the imported FEBA content from later curator additions.

## Recommended Edits

- Curate `data/normalized_yaml/specialized/camm_nocarbon.yaml`, not the generated merge.
- Recover the original CultureBotHT `media_yaml/CaMM_noCarbon.yaml` from a pinned historical commit or another durable import snapshot, then add stable source provenance for that exact input.
- After recovering the upstream file, compare every ingredient identity, amount, unit, and the `Used for growing Xanthomonas` description against the source before marking the record curated.
- Remove `has_unmapped_ingredients` once all ingredient rows remain grounded after source verification.
- Add a structured reference or `source_data` block for the FEBA/CultureBotHT source path and commit so future merges can be reproduced.

## Follow-up Checks

- Re-run open schema, strict validation, reference validation, and term validation against `data/normalized_yaml/specialized/camm_nocarbon.yaml` after the provenance repair.
- Re-run `just verify-merges` and `just audit-merge-freshness` after regenerating `data/merge_yaml/merged/CaMM_noCarbon.yaml`.
- Re-check CultureBotHT at the recovered commit and at the stored repository URL so the report can distinguish historical upstream provenance from any live repository changes.
- Confirm that `data_quality_flags` drops `has_unmapped_ingredients` only if no ingredient remains ungrounded after the provenance repair.

## Additional Notes

- `find . -iname '*camm*'` included ignored and hidden files and found only the generated `CAMM.yaml`, generated `CaMM_noCarbon.yaml`, their two normalized owners, and the prior `CAMM` review report; no local CultureBotHT raw `CaMM_noCarbon.yaml` capture is present under this checkout.
- A narrower gitignore-independent search for `CultureMech:015501`, `CaMM_noCarbon`, `camm_nocarbon`, and `CaMM noCarbon` across source and data paths found only the active owner, generated merge, current registry/index references, and generated import-prioritization metadata.
