# YAML Record Review: 2_5_nacl_gypfk_agar_broth

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_5_nacl_gypfk_agar_broth.yaml`
- Started UTC: 2026-09-21T06:25:15Z
- Finished UTC: 2026-09-21T06:27:52Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008125` |
| Name | `2_5_nacl_gypfk_agar_broth` |
| Original name | `2.5% NaCl GYPFK Agar/Broth` |
| Category | `bacterial` |
| Source term | `TOGO:M1576`, `2.5% NaCl GYPFK Agar/Broth` |
| Source URL | `https://togomedium.org/medium/M1576`; notes also name `NBRC_M383` and `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=383` |
| Merge fingerprint | `6aeef759c15828ffc7a69b5f00b54549f61b7f63a85ad6af85f846dfec0a069f` |
| Maintained owner | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO import for TOGO M1576, which its own notes describe as sourced from NBRC Medium 383. Future corrections belong in `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_5_nacl_gypfk_agar_broth.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_5_nacl_gypfk_agar_broth.yaml --out /private/tmp/2_5_nacl_gypfk_agar_broth.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_5_nacl_gypfk_agar_broth.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_5_nacl_gypfk_agar_broth.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M1576`, the label is `2.5% NaCl GYPFK Agar/Broth`, and the notes identify TOGO M1576 as imported from `NBRC_M383` with NBRC Medium Detail `NO=383`. The inspected NBRC 383 page resolves to `2.5% NaCl GYPFK Agar/Broth`.

Glucose, yeast extract, NaCl, K2HPO4, Na2CO3, MgSO4-7H2O, FeSO4-7H2O, MnSO4-4H2O, distilled water, and agar all preserve recognizable source labels or formulae. The chemical groundings for water, sodium chloride, dipotassium hydrogen phosphate, glucose, sodium carbonate, magnesium sulfate heptahydrate, iron(2+) sulfate heptahydrate, and manganese(II) sulfate tetrahydrate are chemically compatible with the labels that remain on the generated target.

Fetching `https://togomedium.org/medium/M1576` was not required for a definitive formulation in this run because the TOGO record itself names NBRC Medium 383 and the live NBRC page supplied the complete recipe table.

## Evidence

The inspected NBRC Medium 383 page supports this staged formulation:

- Solution A contains 10 g glucose, 5 g Hipolypepton, 5 g yeast extract, 5 g Extract Bonito, 25 g NaCl, 10 g K2HPO4, 5 ml Salt solution, 15 g agar if needed, and 900 ml distilled water; its pH is unadjusted.
- Solution B contains 3 g Na2CO3 and 100 ml distilled water; its pH is unadjusted.
- Solutions A and B are separately autoclaved and mixed aseptically.
- Final pH is around 8.5.
- Hipolypepton and Extract Bonito carry the same Wako Pure Chemical Industries footnote.
- Salt solution is a 1 L stock containing 40 g MgSO4-7H2O, 2 g MnSO4-4H2O, and 2 g FeSO4-7H2O.

The generated target preserves many literal component names but loses the source boundaries that make the amounts meaningful. It flattens the salt-stock components into the final ingredient list at 40, 2, and 2 g/L, even though NBRC adds only 5 ml of that stock to Solution A. It similarly moves Na2CO3 out of Solution B into the final ingredient list, moves Hipolypepton and Extract Bonito out of ingredients into empty `solutions` entries, records `Solution A` and `Solution B` as 900 and 100 `G_PER_L`, records `Salt solution**` as 5 `G_PER_L`, and collapses 900 ml, 100 ml, and 1 L water rows into `Distilled water` at `1001.0 G_PER_L`.

The generated record omits the NBRC instructions that Solutions A and B are separately autoclaved and aseptically mixed, that both solution pH values are unadjusted, and that the final pH is around 8.5.

## Completeness

Consequential gaps:

- The final formulation is numerically corrupt because stock-solution concentrations and final-medium additions are conflated.
- Solution A, Solution B, and Salt solution nesting is not represented.
- Hipolypepton and Extract Bonito are no longer final ingredients.
- The water rows are merged across incompatible final-solution and stock-solution scopes.
- NBRC preparation and pH instructions are absent.
- `physical_state: SOLID_AGAR` does not capture the source title's `Agar/Broth` scope or the source's conditional `Agar (if needed)` row.
- The record has no structured `references`, so neither the TOGO M1576 nor NBRC 383 URL is validator-visible.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected NBRC recipe supports a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_5_nacl_gypfk_agar_broth.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:008125`, `TOGO:M1576`, `NBRC_M383`, `NO=383`, the merge fingerprint, and `2.5% NaCl GYPFK` found only the expected live normalized owner, its generated merged copy, derived indexes and manifests, and historical report/archive rows. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Stock-solution concentrations are flattened into the final ingredient list. | NBRC Medium 383 adds only 5 ml of a 1 L Salt solution stock to Solution A, with the stock containing 40 g/L MgSO4-7H2O, 2 g/L MnSO4-4H2O, and 2 g/L FeSO4-7H2O. The generated and normalized records place those three salts in top-level `ingredients` at 40, 2, and 2 `G_PER_L`, overstating their final amounts and erasing the stock boundary. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Major | Solution rows are represented with wrong units and incomplete composition. | NBRC lists Solution A as the 900 ml organic/salt/agar portion, Solution B as 3 g Na2CO3 in 100 ml water, and Salt solution as a 5 ml stock addition to Solution A. The record stores Solution A and B as 900 and 100 `G_PER_L`, stores Salt solution as 5 `G_PER_L`, and leaves the actual nested composition out of those entries. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Major | Hipolypepton and Extract Bonito are misclassified as empty solutions. | NBRC lists Hipolypepton and Extract Bonito as 5 g components of Solution A, both with a Wako supplier footnote. The generated and normalized records omit them from `ingredients` and store each one as a solution with `composition: []`. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Major | Distilled water is merged across incompatible recipe scopes. | NBRC has 900 ml distilled water in Solution A, 100 ml distilled water in Solution B, and 1 L distilled water in the Salt solution stock. The record collapses all three rows into one `Distilled water` ingredient at `1001.0 G_PER_L` with the note `Merged 3 duplicates: 900.0, 100.0, 1.0`. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Major | pH and sterilization instructions are missing. | NBRC says Solution A and Solution B have unadjusted pH, should be separately autoclaved and aseptically mixed, and produce a final pH around 8.5. The generated and normalized records have no pH value and no preparation steps. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO and NBRC URLs only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` |

## Recommended Edits

1. Rebuild `data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` so Solution A, Solution B, and Salt solution preserve the NBRC component boundaries and use volume units for 900 ml, 100 ml, and 5 ml additions instead of `G_PER_L`.
2. Represent Hipolypepton, yeast extract, Extract Bonito, NaCl, K2HPO4, glucose, optional agar, and Salt solution as Solution A components, not as orphaned final ingredients plus empty solutions.
3. Represent Na2CO3 only inside Solution B and represent MgSO4-7H2O, MnSO4-4H2O, FeSO4-7H2O, and the 1 L water row only inside the Salt solution stock.
4. Remove the merged `Distilled water 1001.0 G_PER_L` top-level ingredient and keep the three NBRC water quantities scoped to their owning Solution A, Solution B, or Salt solution recipe.
5. Add preparation text for separate autoclaving of Solutions A and B and aseptic mixing.
6. Add pH information for unadjusted Solutions A/B and final pH around 8.5 in the narrowest slots the schema supports.
7. Reassess `physical_state` against the source's `Agar/Broth` title and conditional `Agar (if needed)` component.
8. Add structured references for `https://togomedium.org/medium/M1576` and `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=383`.
9. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` after rebuilding the nested solutions.
- `just validate-references data/normalized_yaml/bacterial/2_5_nacl_gypfk_agar_broth.yaml` after adding structured TOGO and NBRC references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 383 for Solution A, Solution B, Salt solution, separate autoclaving, aseptic mixing, unadjusted solution pH, final pH around 8.5, and the optional agar row.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
