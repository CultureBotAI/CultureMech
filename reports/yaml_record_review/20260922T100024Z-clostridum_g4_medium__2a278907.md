# YAML Record Review: CLOSTRIDUM G4 MEDIUM

- Repository: CultureMech
- Record: `data/merge_yaml/merged/clostridum_g4_medium__2a278907.yaml`
- Started UTC: `2026-09-22T10:00:24Z`
- Finished UTC: `2026-09-22T10:01:19Z`
- Verdict: pass with minor issues

## Target

Generated bacterial `MediaRecipe` record `CultureMech:002341` for MediaDive/JCM Medium `J1169`, generated from `data/normalized_yaml/bacterial/clostridum_g4_medium.yaml` on merge fingerprint `2a278907ba8081b7171028a885e39b9422ef6832d918fd69e928942590faf52b`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/clostridum_g4_medium__2a278907.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The record is the direct JCM import for JCM Medium 1169, `CLOSTRIDUM G4 MEDIUM`. The source name itself uses `Clostridum`, and the generated normalized name preserves that source spelling.

All seven generated formula ingredients have CHEBI terms and MediaIngredientMech back-links. The record does not keep `KOH` or `N2` as variable final ingredients; it correctly records them in the preparation step as the reagent used to adjust pH to 7.0 and the atmosphere used while dispensing into Hungate tubes.

The ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/import_tracking`, and `reports` found a separate TOGO `M1251` branch for the same JCM `GRMD=1169` URL. Ignored files were included.

## Evidence

TOGO `M1251` records the same JCM `GRMD=1169` source and confirms the generated final formula: 0.1 g/L `MgSO4 x 7H2O`, 5 g/L NaCl, 0.7 g/L KH2PO4, 0.5 g/L NH4Cl, 0.7 g/L K2HPO4, 2 g/L glucose, 1 g/L sodium ascorbate, and pH 7.0.

TOGO also preserves the single handling note: mix the components, adjust to pH 7.0 with KOH, distribute the medium under N2 in sealed Hungate vessels, and autoclave. The generated `preparation_steps` entry captures those facts.

The live JCM URL for `GRMD=1169` currently returns `Nothing found`, so the formula was cross-checked against the TOGO snapshot for that JCM source rather than against a live JCM page.

## Completeness

The generated direct branch is complete for the final formula and the available preparation note.

No target organism or growth evidence is present.

## Findings

- Minor: the TOGO `M1251` branch for the same JCM source is not merged with this direct JCM branch.
- Minor: the recorded JCM URL no longer serves JCM 1169, so future curation needs either the TOGO snapshot or an archived/replacement JCM source.

## Recommended Edits

- Regenerate TOGO `M1251` with KOH and N2 retained as preparation context rather than variable final ingredients, then merge it with the direct JCM branch.
- Record an archived or replacement source for JCM 1169 if one is available.

## Follow-up Checks

- Validate the regenerated direct and TOGO branches with open schema, strict, reference, and term validators.
- Confirm the merged formula still has exactly the seven final ingredient rows from this direct branch.
- Confirm the pH 7.0 / KOH / N2 Hungate preparation note survives the merge.

## Additional Notes

No formula-level issues were found in this direct JCM branch. Empty optional fields are not defects.
