# YAML Record Review: 2_glucose_yeast_extract_peptone_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar__32ac0304.yaml`
- Started UTC: 2026-09-21T06:32:24Z
- Finished UTC: 2026-09-21T06:32:56Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:010522` |
| Name | `2_glucose_yeast_extract_peptone_agar` |
| Original name | `2% GLUCOSE-YEAST EXTRACT-PEPTONE AGAR` |
| Category | `fungal` |
| Source term | `mediadive.medium:J37`, `2% GLUCOSE-YEAST EXTRACT-PEPTONE AGAR` |
| Source URL | `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=37` |
| Merge fingerprint | `32ac0304a6305137d518b51d0fcc6d6b4f819ced4716ea46b2e146ecf21d9b75` |
| Maintained owner | `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated MediaDive/JCM J37 import. The same filename stem also exists under `data/normalized_yaml/bacterial/`; the stable ID, source term, category, and merge fingerprint resolve this generated target to `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml`. Future corrections belong in that normalized fungal file; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar__32ac0304.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar__32ac0304.yaml --out /private/tmp/2_glucose_yeast_extract_peptone_agar__32ac0304.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar__32ac0304.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar__32ac0304.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct JCM identity is internally consistent: `media_term.term.id` is `mediadive.medium:J37`, the label is `2% GLUCOSE-YEAST EXTRACT-PEPTONE AGAR`, and the inspected JCM page resolves GRMD 37 to the same title.

Ingredient identity is sound for the eight dry components present in the target. Peptone, yeast extract, glucose, K2HPO4, MgSO4 x 7 H2O, FeSO4 x 7 H2O, CaCO3, and agar all appear on the JCM page with the same or equivalent concentration, and the CHEBI groundings for glucose, dipotassium hydrogen phosphate, magnesium sulfate heptahydrate, iron(2+) sulfate heptahydrate, calcium carbonate, and agar are chemically compatible with those labels.

## Evidence

The inspected JCM Medium 37 page supports this formulation:

- 5 g peptone.
- 1 g yeast extract.
- 20 g glucose.
- 0.5 g K2HPO4.
- 0.5 g MgSO4-7H2O.
- 50 mg FeSO4-7H2O.
- 1 g CaCO3.
- 15 g agar.
- 1 L distilled water.
- Unadjusted pH 6.8.
- A source-wide default to sterilize by autoclaving at 121 C for 15 minutes unless otherwise stated.

The generated target preserves all eight dry components and represents the 50 mg FeSO4-7H2O row as 0.05 g/L. It preserves the unadjusted pH 6.8 comment as a `preparation_steps` entry. It omits the 1 L distilled-water row and the JCM default autoclaving instruction.

## Completeness

Consequential gaps:

- The 1 L distilled-water row is missing.
- The JCM default autoclaving instruction is missing.
- The JCM J37 import is a source duplicate of `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml`, the TOGO M29 import from the same JCM original source, but neither record has a reciprocal `SOURCE_DUPLICATE` link.
- The record has no structured `references`, so the JCM 37 URL is not validator-visible.

Empty optional slots:

- `solutions` is correctly absent. The inspected JCM recipe is a flat one-part formulation.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM source supports a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_glucose_yeast_extract_peptone_agar__32ac0304.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml/fungal`, `data/merge_yaml/merged`, `history`, `reports`, and `data/import_tracking` for `CultureMech:010522`, exact JCM J37 source tokens, the generated fingerprint suffix, and the JCM title found the expected fungal MediaDive/JCM J37 normalized/generated records, the bacterial TOGO M29 source duplicate, and derived indexes, manifests, and archive report rows. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is missing. | JCM Medium 37 lists 1 L distilled water. The generated and normalized JCM J37 records include only the eight dry components and do not represent the water row. | `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` |
| Major | The JCM default autoclaving instruction is missing. | JCM states that media should be autoclaved at 121 C for 15 min unless otherwise stated; the generated and normalized JCM J37 records have only an unadjusted-pH preparation step. | `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` |
| Major | JCM J37 duplicates TOGO M29 but is not linked to the TOGO owner. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` is TOGO M29, and TOGO M29 names `JCM_M37` and GRMD 37 as its original source. Neither record has a reciprocal `SOURCE_DUPLICATE` link. | `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` and `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |
| Minor | Structured source provenance is absent. | The target cites the JCM URL only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` |

## Recommended Edits

1. Add the 1 L distilled-water row from JCM Medium 37 to `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml`.
2. Add a JCM-supported preparation step for autoclaving at 121 C for 15 minutes.
3. Add a structured reference for `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=37`.
4. Add reciprocal `SOURCE_DUPLICATE` links between `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` and `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml`.
5. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` after curation.
- `just validate-terms data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` after adding distilled water.
- `just validate-references data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` after adding a structured JCM reference.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 37 for 5 g/L peptone, 1 g/L yeast extract, 20 g/L glucose, 0.5 g/L K2HPO4, 0.5 g/L MgSO4-7H2O, 50 mg/L FeSO4-7H2O, 1 g/L CaCO3, 15 g/L agar, 1 L distilled water, unadjusted pH 6.8, and default autoclaving.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
