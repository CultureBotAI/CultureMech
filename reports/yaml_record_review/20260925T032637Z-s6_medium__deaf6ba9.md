# YAML Record Review: s6_medium__deaf6ba9

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s6_medium__deaf6ba9.yaml`
- Started UTC: `2026-09-25T03:26:37Z`
- Finished UTC: `2026-09-25T03:26:37Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s6_medium__deaf6ba9`, a single-source merge of `data/normalized_yaml/bacterial/s6_medium.yaml`.

The record represents the direct MediaDive/JCM `J90` import for JCM Medium 90, `S6 MEDIUM`. It imports the milligram-scale mineral salts correctly, unlike the TOGO copies, but still flattens the 10% sodium thiosulfate stock and drops the 900 ml water row.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to `mediadive.medium:J90`, JCM Medium 90 `S6 MEDIUM`. No false duplicate merge was found: this is a direct JCM copy separate from the TOGO `M81` and `M82` imports.

## Evidence

The MediaDive REST record for `J90` and the live JCM 90 page list 1.8 g `KH2PO4`, 1.2 g `Na2HPO4`, 0.1 g `(NH4)2SO4`, 0.1 g `MgSO4 x 7H2O`, 30 mg `FeCl3 x 6H2O`, 30 mg `MnSO4 x nH2O`, 40 mg `CaCl2 x 2H2O`, 100 ml 10% `Na2S2O3` solution, and 900 ml distilled water.

The source pH is 7.0. The source also says to adjust pH to 7.0, separately sterilize the `Na2S2O3` solution by filtration, aseptically add sterile `Na2S2O3` solution to the medium, and add 15 g/L agar for solid medium.

## Completeness

The generated record preserves pH 7.0, all direct gram-scale salts, the 30 mg iron and manganese salts, the 40 mg calcium salt, and the source preparation text.

It loses or corrupts these details:

- The 100 ml 10% `Na2S2O3` solution is represented as direct `Na2S2O3` at `100 G_PER_L`.
- The 900 ml distilled water row is absent.
- The thiosulfate row still carries the legacy `mediaingredientmech_term` slot instead of a CHEBI-keyed `mediaingredientmech_chebi_term`.
- The separate thiosulfate filtration and solid-agar note are typed as generic `MIX` actions.

## Findings

- `needs curation`: The 100 ml 10% `Na2S2O3` stock aliquot was flattened into a direct `Na2S2O3` ingredient at the 100 g/L stock concentration. JCM adds 100 ml of that stock to the final medium, so the generated parent concentration is not the final concentration and the stock volume is unrecoverable.
- `needs curation`: The generated record omits the 900 ml distilled water row that balances the 100 ml thiosulfate aliquot to a 1 L medium.
- `pass with minor issues`: The direct gram and milligram salt amounts, including `FeCl3 x 6 H2O` at 0.03 g/L, `MnSO4 x n H2O` at 0.03 g/L, and `CaCl2 x 2 H2O` at 0.04 g/L, match the source.
- `pass with minor issues`: The `Na2S2O3` row still uses a legacy `mediaingredientmech_term` entry despite later migration history saying legacy MediaIngredientMech links were refreshed to CHEBI keying.
- `pass with minor issues`: The thiosulfate filtration step and solid-agar note are typed as generic `MIX` actions rather than filter and solidification-related actions.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s6_medium__deaf6ba9.yaml`; fix `data/normalized_yaml/bacterial/s6_medium.yaml`, then rerun the merge.
- Model 10% `Na2S2O3` solution as a filtered stock with a 100 ml parent aliquot instead of a direct 100 g/L parent ingredient.
- Restore the 900 ml distilled water row.
- Migrate or remove the legacy `MediaIngredientMech:000321` link on `Na2S2O3`.
- Reclassify the thiosulfate sterilization step away from generic `MIX`, and keep the 15 g/L agar instruction scoped to the solid formulation.
- Preserve the corrected 0.03 g/L and 0.04 g/L values for the milligram-scale Fe, Mn, and Ca salts.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/s6_medium.yaml`.
- Regenerate `data/merge_yaml/merged/s6_medium__deaf6ba9.yaml` and verify that `Na2S2O3` no longer appears as a direct `100 G_PER_L` parent ingredient.
- Verify that 900 ml distilled water and the 100 ml 10% `Na2S2O3` solution aliquot survive regeneration.
- Verify that the Fe, Mn, and Ca rows remain at 0.03, 0.03, and 0.04 g/L.
- Run an exact ignored-file-inclusive search for `mediadive.medium:J90`, `TOGO:M81`, and `TOGO:M82` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The direct JCM import is the best of the three generated S6 artifacts because it correctly carries pH 7.0, preparation text, and milligram-scale salt conversions.
