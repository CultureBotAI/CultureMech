# YAML Record Review: s200_medium__32f136ae

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s200_medium__32f136ae.yaml`
- Started UTC: `2026-09-25T03:23:36Z`
- Finished UTC: `2026-09-25T03:23:36Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s200_medium__32f136ae`, a two-source merge of:

- `data/normalized_yaml/bacterial/halanaerobacter_jeridensis_medium.yaml`
- `data/normalized_yaml/bacterial/s200_medium.yaml`

The generated record is canonicalized to JCM Medium `J791`, `S200 MEDIUM`, and also folds in JCM Medium `J818`, `HALANAEROBACTER JERIDENSIS MEDIUM`.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

JCM Medium 791 and JCM Medium 818 are related, but they are not source duplicates. The JCM 818 page says to use JCM Medium 791 with 5.0 g/L `MgSO4 x 7H2O` instead of `CaCl2 x 2H2O` and to adjust pH to 7.5. Merging it into the pH 7.0 JCM 791 S200 record erases those `HALANAEROBACTER JERIDENSIS MEDIUM` modifications.

The JCM 791 identity itself is grounded correctly to `mediadive.medium:J791`, but its formula needs nested stocks: it references 10 ml/L Trace minerals from JCM Medium 151 and adds three anaerobic stock solutions after cooling.

## Evidence

JCM 791 lists the main S200 recipe as 1 g `NH4Cl`, 0.5 g `KH2PO4`, 0.2 g `CaCl2 x 2H2O`, 150 g `NaCl`, 10 g `MgCl2 x 6H2O`, 2 g `KCl`, 0.5 g Yeast extract, 10 ml Trace minerals from JCM Medium 151, 0.5 g `L-Cysteine HCl x H2O`, and 1 mg Resazurin brought to 1 L with distilled water.

The same page instructs the user to adjust pH to 7.0 with KOH, distribute under `N2-CO2` at 4:1, autoclave, and after cooling add 25 ml 8% `NaHCO3` solution, 20 ml 1 M Glucose solution, and 8 ml 5% `Na2S x 9H2O` solution per liter from anaerobic stocks. JCM Medium 151 defines the referenced Trace minerals stock as a separate 1 L stock with nitrilotriacetic acid, `MgSO4 x 7H2O`, `MnSO4 x n H2O`, `NaCl`, `FeSO4 x 7H2O`, `CoSO4 x 7H2O`, `CaCl2 x 2H2O`, `ZnSO4 x 7H2O`, `CuSO4 x 5H2O`, `AlK(SO4)2`, `H3BO3`, and `Na2MoO4 x 2H2O`.

JCM 818 contains only a variant instruction: use JCM 791 with 5.0 g/L `MgSO4 x 7H2O` instead of `CaCl2 x 2H2O`, and adjust pH to 7.5.

## Completeness

The generated record preserves the JCM 791 main gram amounts for `NH4Cl`, `KH2PO4`, `CaCl2 x 2H2O`, `NaCl`, `MgCl2 x 6H2O`, `KCl`, Yeast extract, and `L-Cysteine HCl x H2O`. It also computes plausible final gram-per-liter masses for the bicarbonate, glucose, and sulfide post-autoclave aliquots.

It loses or corrupts these source details:

- JCM 818 is merged as a source duplicate even though it is a pH 7.5 variant with a calcium-to-magnesium substitution.
- The 10 ml/L Trace minerals aliquot is absent.
- Every Trace minerals stock component is flattened into the parent at full stock strength.
- Main-recipe `NaCl` and stock `NaCl` are summed into `151.0 G_PER_L`.
- Main-recipe `CaCl2 x 2H2O` and stock `CaCl2 x 2H2O` are summed into `0.30000000000000004 G_PER_L`.
- Distilled water from JCM 791 and JCM Medium 151 Trace minerals is absent.
- The 25 ml, 20 ml, and 8 ml anaerobic stock aliquots are represented only as final mass rows.

## Findings

- `needs curation`: JCM 818 was falsely merged into JCM 791 as a `SOURCE_DUPLICATE`. The source says JCM 818 changes pH from 7.0 to 7.5 and uses 5.0 g/L `MgSO4 x 7H2O` instead of `CaCl2 x 2H2O`, so it must be represented as a variant rather than collapsed into S200.
- `needs curation`: The JCM Medium 151 Trace minerals stock was flattened into the S200 parent. JCM 791 calls for 10 ml/L Trace minerals, but the generated parent contains all trace-mineral ingredients at their 1 L stock concentrations.
- `needs curation`: Ingredients from different scopes were merged. `NaCl` combines 150 g/L parent `NaCl` with 1 g/L Trace minerals `NaCl`, and `CaCl2 x 2H2O` combines 0.2 g/L parent calcium chloride with 0.1 g/L Trace minerals calcium chloride.
- `needs curation`: The stock aliquot rows are missing. The generated record lacks 10 ml/L Trace minerals, 25 ml/L 8% `NaHCO3`, 20 ml/L 1 M Glucose, and 8 ml/L 5% `Na2S x 9H2O` entries, so it cannot reconstruct the post-autoclave stock workflow.
- `needs curation`: Water is missing from both the parent S200 scope and the JCM Medium 151 Trace minerals stock scope.
- `pass with minor issues`: The direct main JCM 791 gram amounts, pH 7.0, and final computed masses for the post-autoclave bicarbonate, glucose, and sulfide additions are otherwise plausible for S200 itself.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s200_medium__32f136ae.yaml`; fix the normalized JCM 791 and JCM 818 records and the merge grouping, then rerun the merge.
- Split `halanaerobacter_jeridensis_medium` out of the S200 `SOURCE_DUPLICATE` group and model it as a pH 7.5 variant with 5.0 g/L `MgSO4 x 7H2O` replacing the parent `CaCl2 x 2H2O` row.
- Preserve JCM Medium 151 Trace minerals as a nested stock with a 10 ml/L parent aliquot.
- Keep the parent and Trace minerals `NaCl` and `CaCl2 x 2H2O` rows scoped separately rather than summing them.
- Restore parent distilled water and Trace minerals distilled water in their source scopes.
- Preserve the 25 ml 8% `NaHCO3`, 20 ml 1 M Glucose, and 8 ml 5% `Na2S x 9H2O` stock-solution additions as post-autoclave aliquots rather than only flattened final masses.
- Keep the anaerobic `N2-CO2` 4:1 atmosphere, Hungate-tube distribution, butyl-stopper sealing, autoclaving, cooling, and anaerobic-stock addition sequence attached to the parent S200 preparation.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on the edited normalized JCM 791 and JCM 818 source files.
- Regenerate `data/merge_yaml/merged/s200_medium__32f136ae.yaml` and verify that no generated S200 parent ingredient has `NaCl` at `151.0 G_PER_L` or `CaCl2 x 2H2O` at `0.30000000000000004 G_PER_L`.
- Verify that Trace minerals is retained as a 10 ml/L aliquot with its own nested stock.
- Verify that `halanaerobacter_jeridensis_medium` is no longer emitted as a source duplicate of pH 7.0 S200.
- Run an exact ignored-file-inclusive search for `halanaerobacter_jeridensis_medium`, `mediadive.medium:J818`, `s200_medium`, and `mediadive.medium:J791` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. JCM Medium 151 is evidence only for the Trace minerals stock referenced by S200; its `PYROCOCCUS MEDIUM` parent recipe is not an ingredient of S200.
