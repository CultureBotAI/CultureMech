# YAML Record Review: defined_medium

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/defined_medium__19dd9e65.yaml`
- Started UTC: 2026-09-22T16:32:23Z
- Finished UTC: 2026-09-22T16:33:11Z
- Verdict: needs curation

## Target

- Reviewed generated record: `data/merge_yaml/merged/defined_medium__19dd9e65.yaml`
- Class: `MediaRecipe`
- ID: `CultureMech:009665`
- Name / original name: `defined_medium` / `Defined medium`
- Source term: `TOGO:M3225`, label `Defined medium`
- Maintained owner: `data/normalized_yaml/bacterial/defined_medium.yaml`
- Merge provenance: `merge_recipes.py` merged one source, `defined_medium.yaml`, on fingerprint `19dd9e65ec16f5be0278015ad5acb29f62cf528b21f84f4783f1930a328fa20e`.
- Exact ignored-file-inclusive searches for `CultureMech:009665`, `TOGO:M3225`, `M3225`, `defined_medium.yaml`, and the merge fingerprint across `data/normalized_yaml`, `data/merge_yaml/merged`, `scripts`, `data`, `src`, `reports`, and `.claude` resolved this target to the maintained normalized owner and the reviewed generated merge. The same search also found another same-name TOGO record, `data/normalized_yaml/bacterial/TOGO_M3228_Defined_medium.yaml`, but its source ID is different and the exact `TOGO:M3225` ID disambiguates this review target.

## Validation

| Check | Result |
|---|---|
| Open schema, equivalent to `just validate-schema data/merge_yaml/merged/defined_medium__19dd9e65.yaml` | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`. |
| Strict closed schema | Passed with `scripts/validate_strict.py`: one file scanned, zero files with errors, zero error rows. |
| Reference integrity | Passed with `linkml-reference-validator validate data ... --target-class MediaRecipe`; the validator reported zero checks. |
| Term labels | Passed with `linkml-term-validator validate-data ... --labels -c conf/oak_config.yaml`. |
| Embedded `curation_history` | Not checked separately: `just validate-history` targets standalone `history/*.yaml` records, not `MediaRecipe.curation_history` entries embedded in a medium YAML. The embedded event shape was still covered by schema and strict validation. |

## Identity and Grounding

- The generated file denotes TOGO Medium M3225: the record ID, `media_term`, top-level note, initial `togo-import` event, merge source, normalized owner, ID registry, and recipe catalog all agree on `CultureMech:009665` / `TOGO:M3225`.
- The generated record is stale relative to its owner. The maintained normalized owner has a `2026-09-02T07:38:01.272389+00:00` `repair_merged_duplicates.py` event that collapsed the water row back to `1.0 G_PER_L`; this generated merge lacks that event and still has `4.0 G_PER_L`.
- `medium_type: COMPLEX` and `composition_type: UNDEFINED` are not supported by the inspected TOGO M3225 API payload. The final medium and its inline trace, Stock A, and phosphate-buffer recipes contain defined water, salts, glycerol, EDTA, and FeNa.EDTA rows, and the TOGO component metadata marks the parsed chemical rows as defined components.
- Two hydrated salt rows have wrong ChEBI groundings: source `CoCl2.6H2O` is grounded to `CHEBI:35696` / `cobalt dichloride`, and source `NiCl2.6H2O` is grounded to `CHEBI:34887` / `nickel dichloride`. The packaged MIM label index shows exact hydrated entries for `cobalt chloride hexahydrate` and `nickel chloride hexahydrate` under distinct CHEBI IDs, so the current rows collapse the hydrate identity.
- The source `FeNa.EDTA` row is left ungrounded even though the packaged MIM label index has a unique `FeNa.EDTA` match, `CHEBI:78292` / `Sodium ferric EDTA`.

## Evidence

- The TOGO M3225 API supports the source identity and formulation exposed by the record: final medium per liter has `1 L` distilled water, `16 g` glycerol, `1.0 g` `(NH4)2SO4`, `1.0 g` `MgSO4.7H2O`, `0.015 g` `CaCl2.2H2O`, `1.0 ml` Trace element solution, `1.0 ml` Stock A solution, and `35.2 ml` `1 M Phosphate buffer`.
- The same API nests three local stock recipes beneath M3225:
  - Trace element solution, per liter of water: `0.015 g` `H3BO3`, `0.5 g` `FeSO4.7H2O`, `0.05 g` `CoCl2.6H2O`, `0.01 g` `NiCl2.6H2O`, `0.4 g` `ZnSO4.7H2O`, `0.005 g` `CuCl2.2H2O`, `0.02 g` `MnSO4.H2O`, and `0.25 g` EDTA.
  - Stock A solution, per liter of water: `2.0 g` `NaMoO4.2H2O` and `5.0 g` `FeNa.EDTA`.
  - 1 M Phosphate buffer, per liter of water: `47 g` `KH2PO4` and `113 g` `K2HPO4`.
- The generated record flattens the local trace, Stock A, and phosphate-buffer solutes into final-medium `ingredients` at their stock concentrations. Those quantities are supported only inside the stock recipes; they are not final-medium grams per liter.
- The generated `solutions` rows do not preserve the source dimensions. TOGO says the final medium receives `1.0 ml` Trace element solution, `1.0 ml` Stock A solution, and `35.2 ml` 1 M Phosphate buffer; the YAML instead stores two solution rows at `1 G_PER_L`, omits a nested composition for Stock A, leaves the 35.2 ml phosphate buffer as a top-level ingredient, and has no local `SolutionRecipe` records for M3225 discovered by exact ignored-file-inclusive `M3225` search under `data/normalized_yaml` and `data/merge_yaml/merged`.
- `data/normalized_yaml/bacterial/mediadive_6187_Trace_element_solution.yaml` is an unrelated MediaDive stock. It contains Na2-EDTA, FeCl3, MnCl2, ZnCl2, CoCl2, and Na2MoO4 rows, while M3225 defines a different H3BO3, FeSO4, CoCl2, NiCl2, ZnSO4, CuCl2, MnSO4, and EDTA trace stock.
- The only inspected TOGO payload had an empty `src_url`. It preserved formula comments from an upstream article, including a cross-reference to `[66]`, but it did not identify a DOI, PMID, JCM, DSMZ, ATCC, or other primary recipe accession that can be independently verified from this record.

## Completeness

- Consequential gaps:
  - The three inline stock recipes are not represented as source-local solutions with the correct `ml/L` additions and stock compositions.
  - The generated recipe cannot be followed to reproduce M3225 because trace-stock, Stock A, and phosphate-buffer ingredient quantities are stored as final medium amounts.
  - The generated recipe lacks the September 2026 normalized-owner repair that fixed the duplicate water row from `4.0` to `1.0`.
  - Several exact ingredient identities are either wrong or ungrounded despite resolvable MIM index entries.
  - Provenance is TOGO-only; the original source article behind the TOGO snippets remains unresolved because the M3225 API reported an empty `src_url`.
- Empty optional slots correctly left empty:
  - No `target_organisms`, pH, temperature, atmosphere, sterilization step, growth claim, or growth evidence should be filled from the inspected M3225 API payload.
  - No `references` entry should be invented from TOGO's unresolved `[66]` cross-reference.
- Bounded searches:
  - Ignored-file-inclusive exact search for `defined_medium__19dd9e65`, `CultureMech:009665`, and `TOGO:M3225` under `reports/yaml_record_review` found no existing review report for this generated file.
  - Ignored-file-inclusive exact search for `M3225` under `data/normalized_yaml` and `data/merge_yaml/merged` found no M3225-local standalone `SolutionRecipe` for the trace, Stock A, or phosphate-buffer recipes.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale relative to the normalized owner. | Generated `Distilled water` is `4.0 G_PER_L` with `Merged 4 duplicates`; the normalized owner has `1.0 G_PER_L` and a 2026-09-02 `REPAIRED_SUMMED_DUPLICATE_MERGE` event. | Regenerate `data/merge_yaml/merged/defined_medium__19dd9e65.yaml` from `data/normalized_yaml/bacterial/defined_medium.yaml`. |
| Major | Local stock recipes are flattened into the final medium at stock concentrations. | TOGO M3225 places H3BO3, FeSO4, CoCl2, NiCl2, ZnSO4, CuCl2, MnSO4, EDTA, NaMoO4, FeNa.EDTA, KH2PO4, and K2HPO4 inside three local stock recipes, but the YAML stores all of them as final-medium `ingredients` with the stock recipe grams per liter. | `data/normalized_yaml/bacterial/defined_medium.yaml` and the TOGO stock-solution import/migration rule. |
| Major | Final stock additions have wrong units, missing nesting, or wrong identities. | TOGO M3225 adds Trace element solution and Stock A solution at 1 ml/L each and 1 M Phosphate buffer at 35.2 ml/L. The YAML stores two `solutions` at `1 G_PER_L`, omits Stock A composition, leaves phosphate buffer as `35.2 G_PER_L`, and links the M3225-local trace stock to unrelated `mediadive.solution:6187`. | `data/normalized_yaml/bacterial/defined_medium.yaml` and the TOGO stock-solution import/migration rule. |
| Major | The medium is classified as complex/undefined although the inspected source composition is defined. | TOGO M3225 exposes water, glycerol, salts, EDTA, FeNa.EDTA, and three fully printed stock recipes; the API component properties mark the parsed chemical ingredients as `Defined component`. | `data/normalized_yaml/bacterial/defined_medium.yaml`. |
| Major | Exact chemical forms are missing or misgrounded. | The current record grounds `CoCl2.6H2O` and `NiCl2.6H2O` to non-hydrate ChEBI terms and leaves `FeNa.EDTA` ungrounded even though the packaged MIM index contains exact hydrated cobalt/nickel and FeNa.EDTA entries. | `data/normalized_yaml/bacterial/defined_medium.yaml`; if automatic MIM matching is reused, fix the hydrate synonym conflicts in the packaged MIM snapshot or resolver. |
| Minor | Source provenance stops at a TOGO API record with no original URL. | The TOGO M3225 API has `src_url: ""` and formula comments that cite `[66]`, so the record preserves the imported formula but not the upstream article that TOGO extracted. | `data/normalized_yaml/bacterial/defined_medium.yaml`; unresolved until a curator locates the primary source for TOGO M3225. |

## Recommended Edits

1. Regenerate the merged corpus so `data/merge_yaml/merged/defined_medium__19dd9e65.yaml` incorporates the 2026-09-02 water-row repair already present in `data/normalized_yaml/bacterial/defined_medium.yaml`.
2. Repair `data/normalized_yaml/bacterial/defined_medium.yaml` with three source-local solution descriptors for M3225's Trace element solution, Stock A solution, and 1 M Phosphate buffer; keep their stock compositions nested and keep only the five true final-medium chemicals plus the three `ml/L` stock additions at top level.
3. Remove the unsupported `mediadive.solution:6187` link and do not replace it with another global stock unless an inspected source proves exact equivalence to the M3225-local trace recipe.
4. Reclassify the medium as defined rather than complex/undefined.
5. Reground `CoCl2.6H2O`, `NiCl2.6H2O`, and `FeNa.EDTA` to exact ChEBI/MIM entries, and add exact terms for the still-ungrounded `MgSO4.7H2O` and `CaCl2.2H2O` rows if the resolver can do so without collapsing hydrate state.
6. Add a concrete discussion or provenance note recording that the original article behind TOGO's `[66]` cross-reference was not identified by the M3225 API, then resolve or retain that gap after a bounded manual source search.

## Follow-up Checks

- Run `just validate-schema data/normalized_yaml/bacterial/defined_medium.yaml`, `just validate-strict data/normalized_yaml/bacterial/defined_medium.yaml`, `just validate-terms data/normalized_yaml/bacterial/defined_medium.yaml`, and `just validate-references data/normalized_yaml/bacterial/defined_medium.yaml`.
- Run the merge freshness check after regenerating to prove `data/merge_yaml/merged/defined_medium__19dd9e65.yaml` was rebuilt from `data/normalized_yaml/bacterial/defined_medium.yaml`.
- Manually compare the regenerated YAML against the TOGO M3225 API: five final chemical ingredients, three final stock additions, three nested stock compositions, no `mediadive.solution:6187`, and no stock recipe grams per liter as final-medium ingredient amounts.
- Re-run term validation and inspect the exact label-index matches for hydrated salts and FeNa.EDTA so hydrate-specific rows are not collapsed to anhydrous or generic ChEBI IDs.

## Additional Notes

- The generated merge carries no `target_organisms`; that is appropriate for the inspected source because the M3225 API formula payload did not provide strain- or organism-scoped growth evidence.
- `applications: Microbial cultivation` is broad but not contradicted by TOGO's formula payload.
- This report did not edit any YAML record or append a curation event.
