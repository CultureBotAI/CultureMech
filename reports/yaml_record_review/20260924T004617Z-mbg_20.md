# YAML Record Review: mbg_20

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mbg_20.yaml
- Started UTC: 2026-09-24T00:45:01Z
- Finished UTC: 2026-09-24T00:46:46Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:010085 |
| Label | mbg_20 |
| Original label | MBG-20 |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mbg_20.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml |
| Source accession | TOGO:M680 |

The reviewed YAML is generated from `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`,
but it is stale: it was generated on 2026-08-06 and does not include the
2026-09-11 `repair_togo_m680_score15.py` corrections already present in the
normalized owner.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mbg_20.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mbg_20.yaml --out /private/tmp/mbg_20.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mbg_20.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mbg_20.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The validators accept the stale generated YAML because it remains schema-valid.

## Identity and Grounding

- TOGO M680 identifies `MBG-20`, `original_media_id: JCM_M662`, source URL
  `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=662`, and `ph: 7.0`.
  JCM GRMD 662 and MediaDive J662 identify the same `MBG-20` source recipe.
- Exact `find`/`rg --no-ignore --hidden` checks resolved `TOGO_M680_MBG-20.yaml`
  as the normalized owner of `CultureMech:010085` and found the same TOGO M680
  owner in the normalized TOGO, recipe, bacterial, and ID-registry indexes.
- The normalized TOGO owner now records `parent_media` pointing to the duplicate
  MediaDive J662 record, `CultureMech:003008`; the MediaDive owner reciprocates
  with `variant_children`.
- The generated `data/merge_yaml/merged/mbg_20.yaml` does not include the
  normalized owner's `parent_media`, `variant_relationship`, references, pH, or
  preparation steps.

## Evidence

### Supported

- TOGO M680 and JCM 662 support the direct ingredients: 16 g peptone, 4 g yeast
  extract, 5 g glucose, 40 g Sea salts (Sigma), and 1 L distilled water.
- The maintained normalized owner correctly retains Sea salts (Sigma) as an
  opaque ungrounded commercial mixture rather than replacing it with a generic
  salt ontology term.
- The maintained normalized owner correctly restored pH 7.0 and split the JCM
  preparation into pH adjustment, Sea-salts filtration, separate autoclaving of
  peptone/yeast/glucose solutions, mixing, and repeated argon degassing/charging.

### Unsupported or Over-scoped

- The generated reviewed record says `Distilled water` is `1 G_PER_L`; the JCM
  and TOGO source amount is `1.0 L`.
- The generated record has no `ph_value`, although TOGO and JCM both state pH
  7.0 and the normalized owner already carries `ph_value: 7.0`.
- The generated record has no preparation instructions. Its source requires
  filter-sterilizing the Sea-salts solution, separately autoclaving peptone,
  yeast extract, and glucose solutions, and making the liquid medium anaerobic
  by three degassing/argon cycles.
- The generated record has no source-duplicate metadata, so a browser consuming
  only `data/merge_yaml/merged/` would not know that TOGO M680 and MediaDive J662
  are the same JCM Medium 662 formulation.

## Completeness

- A gitignore-independent exact scan of the generated target and normalized
  owner found `preparation_steps` and `references` only in the owner, not in the
  generated review target.
- `target_organisms` is empty in both the generated target and normalized owner.
  The inspected source recipe does not assert a strain-specific growth result,
  so that empty optional slot is acceptable.
- The exact `mbg_20` generated-file scan found both `data/merge_yaml/merged/mbg_20.yaml`
  and `data/merge_yaml/merged/mbg_20__9b6d5adf.yaml`; both are older than the
  September duplicate-link repair.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The reviewed generated record is stale relative to its normalized owner. | `data/merge_yaml/merged/mbg_20.yaml` stops at the 2026-08-06 `MERGED_RECIPES` event; `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml` has a 2026-09-11 repair that corrected the water unit, pH, preparation, references, and source-duplicate metadata. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`. |
| major | The generated record changes 1 L distilled water into `1 G_PER_L`. | JCM 662 and TOGO M680 both state 1.0 L distilled water; the normalized TOGO owner has `unit: L`, but the generated reviewed record has `unit: G_PER_L`. | Already fixed in `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`; regenerate and verify. |
| major | The generated record omits required preparation and pH context. | The source instructs pH 7.0 adjustment, Sea-salts filtration, separate autoclaving of peptone/yeast/glucose solutions, sterile mixing, and three degassing/argon cycles; none of that appears in the generated reviewed YAML. | Already fixed in `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`; regenerate and verify. |
| minor | The generated record omits structured references and source-duplicate links. | The normalized owner has three `references`, `parent_media` to `CultureMech:003008`, `variant_relationship: SOURCE_DUPLICATE`, and an explanatory `variant_modifications` entry; all are absent from the reviewed generated file. | Already fixed in `data/normalized_yaml/bacterial/TOGO_M680_MBG-20.yaml`; regenerate and verify merge support for these fields. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the current normalized corpus so
   the TOGO M680 artifact picks up the 2026-09-11 repair.
2. Verify after regeneration that `mbg_20.yaml` carries `unit: L` for distilled
   water, `ph_value: 7.0`, the six preparation steps, the three source
   references, and the `SOURCE_DUPLICATE` relationship to `CultureMech:003008`.
3. Rebuild or repair merge deduplication if TOGO M680 and MediaDive J662 are
   supposed to collapse into one generated MBG-20 record instead of two
   source-duplicate records.

## Follow-up Checks

- Rerun the focused open-schema, strict, term, and reference validators on the
  regenerated `data/merge_yaml/merged/mbg_20.yaml`.
- Run merge-freshness and duplicate checks over the two `mbg_20` source records.
- Repeat a gitignore-independent exact search for `CultureMech:010085`,
  `CultureMech:003008`, `TOGO:M680`, and `mediadive.medium:J662` after
  regeneration to confirm the generated records and indexes preserve the
  duplicate relationship.

## Additional Notes

- The normalized TOGO and MediaDive MBG-20 owners look scientifically aligned
  with JCM 662. This review found defects in the generated stale TOGO artifact,
  not in the September normalized repair.
