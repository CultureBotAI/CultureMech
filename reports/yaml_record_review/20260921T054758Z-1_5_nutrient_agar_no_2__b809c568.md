# YAML Record Review: 1/5 Nutrient Agar NO. 2

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml`
- Started UTC: 2026-09-21T05:47:06Z
- Finished UTC: 2026-09-21T05:47:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009840` |
| Name | `1_5_nutrient_agar_no_2` |
| Original name | `1/5 Nutrient Agar NO. 2` |
| Category | `bacterial` |
| Source term | `TOGO:M453`, `1/5 Nutrient Agar NO. 2` |
| Original source | `JCM_M453` |
| Merge fingerprint | `b809c56802f11de5b8b0ed55314bebebf74840602ac9bd8d8a8ae7ba274b86f3` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO Medium M453 mirror of JCM Medium 453. The generated record differs from its maintained normalized owner only by the merge event, `merge_fingerprint`, and `merged_from` fields.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml --out /private/tmp/1_5_nutrient_agar_no_2__b809c568.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes TOGO Medium M453:

- The YAML carries `media_term.term.id: TOGO:M453` and `media_term.term.label: 1/5 Nutrient Agar NO. 2`.
- Its free-text notes preserve the TOGO URL, `JCM_M453` original source accession, and JCM `GRMD=453` URL.
- The inspected TOGO M453 payload reports the same TOGO accession, `original_media_id: JCM_M453`, JCM source URL, medium name, five ingredient rows, and pH range.

The current JCM `GRMD=453` URL returned `Nothing found`, so the live JCM page could not independently verify the historical import. It also did not contradict the TOGO mirror.

Grounding is mostly complete. Water, sodium chloride, and agar are grounded to CHEBI IDs with matching labels, and the two undefined BD-Difco commercial ingredients remain explicit rather than being forced to broader or inexact chemical ontology terms.

## Evidence

The inspected TOGO M453 payload supports:

- `Distilled water`, 1 L.
- `NaCl`, 1 g.
- `Agar`, 15 g.
- `Bacto peptone (BD-Difco)`, 2 g.
- `Beef extract (BD-Difco)`, 2 g.
- pH adjustment to 7.0-7.2.

The generated TOGO record preserves all five source component labels, including the two BD-Difco qualifiers that the direct MediaDive/JCM import drops. It does not fully preserve source quantities or conditions: the TOGO 1 L distilled-water row became `1 G_PER_L`, and the pH 7.0-7.2 adjustment is absent from both the generated merge and its normalized owner.

## Completeness

Consequential gaps:

- The 1 L solvent row is present but assigned the wrong unit.
- The source pH range and pH-adjustment instruction are missing.
- The source URLs remain only in free-text `notes`; there are no structured `references`.
- The same upstream JCM Medium 453 recipe is duplicated by the direct MediaDive/JCM record at `data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml`.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected TOGO mirror verifies the recipe but does not support a target-organism or variant assertion.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` for `CultureMech:009840`, `TOGO:M453`, `JCM_M453`, `GRMD=453`, and the `b809c568` merge fingerprint found the expected TOGO normalized owner, the expected generated TOGO merge, TOGO index rows, the direct JCM sibling via the shared JCM URL, and the neighboring review report.
- A second gitignore-independent `rg --no-ignore --hidden` search over the same paths for `CultureMech:002803`, `mediadive.medium:J453`, and the direct JCM merge fingerprint found the direct normalized owner, the direct generated merge, MediaDive index rows, and the neighboring review report.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The TOGO importer encoded 1 L distilled water as grams per liter. | The TOGO M453 payload lists `Distilled water` with `volume: 1` and `unit: L`; both the generated merge and normalized owner store the same row as `value: '1'`, `unit: G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` or the TOGO volume import mapping |
| Major | The source pH range and pH-adjustment instruction are absent. | The TOGO M453 payload reports `ph: 7.0-7.2` and the adjustment comment; neither `ph_value` nor `preparation_steps` appears in the generated merge or normalized owner. | `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` or the TOGO pH/comment import mapping |
| Minor | Structured references are absent. | The record cites TOGO and JCM only in free-text `notes`, so `linkml-reference-validator` had no structured references to check. | `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` |
| Minor | The TOGO and direct JCM imports remain separate generated records for the same upstream source. | Exact hidden/ignored-inclusive searches found `CultureMech:009840` and `CultureMech:002803`, both named `1_5_nutrient_agar_no_2` and both pointing at `GRMD=453`; they do not merge because the direct copy omits water and strips two commercial qualifiers while the TOGO copy imports water as `1 G_PER_L` and omits pH. | `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml`, `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml`, and merge rules |

## Recommended Edits

1. Correct `Distilled water` in `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` from `1 G_PER_L` to the source 1 L amount.
2. Add the source pH range, with `ph_value: 7.1` as the midpoint and a preparation step for adjusting pH to 7.0-7.2, to `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml`.
3. Add structured TOGO and JCM references to `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml`; keep the JCM reference note explicit that the live URL currently returns `Nothing found`.
4. Append a curation-history event to the TOGO normalized owner describing the water-unit and pH additions.
5. After repairing both JCM Medium 453 owners, regenerate `data/merge_yaml/merged/` and confirm whether `1_5_nutrient_agar_no_2.yaml` and `1_5_nutrient_agar_no_2__b809c568.yaml` collapse into one canonical merge.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` after any grounding or label edits.
- `just validate-references data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml` after adding structured references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against TOGO M453 or an archived JCM Medium 453 source for all five component rows and the pH range.

## Additional Notes

- The current JCM URL returned `Nothing found` for `GRMD=453` on 2026-09-21, so TOGO M453 was the only inspected JCM_M453 source payload available for this report.
- The direct MediaDive/JCM sibling already preserves the pH midpoint and pH-adjustment step but needs the missing water row and exact BD-Difco labels before merge regeneration can test canonicalization of the two JCM Medium 453 records.
- `linkml-reference-validator` performed zero checks because this record has no structured `references` entries.
