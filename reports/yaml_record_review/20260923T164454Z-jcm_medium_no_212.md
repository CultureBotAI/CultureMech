# YAML Record Review: JCM MEDIUM No. 212

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_212.yaml
- Started UTC: 2026-09-23T16:44:22Z
- Finished UTC: 2026-09-23T16:44:54Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_212.yaml`
- Stable ID: `CultureMech:002574`
- Name: `jcm_medium_no_212`
- Original name: `JCM MEDIUM No. 212`
- Category: `bacterial`
- Media term: `mediadive.medium:J212`
- Source: MediaDive import of JCM Medium 212, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=212`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_212.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_212`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_212.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_212.yaml --out /private/tmp/jcm_medium_no_212.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_212.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_212.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators do not detect missing implicit JCM sterilization steps, lost supplier qualifiers, or same-source duplicates that differ only because one wrapper models procedural water as a direct ingredient.

## Identity and Grounding

The record identifies MediaDive `J212` for JCM Medium 212. Live JCM, MediaDive, and TogoMedium M205 all point to `GRMD=212`/`JCM_M212` and agree that the recipe is a pH 7.0 solid hypersaline agar medium.

The generated record has the correct JCM accession, state, pH, salt rows, glucose, and agar. It loses only the BD-Difco qualifier on yeast extract and the default JCM autoclaving instruction from the source page.

## Evidence

Inspected evidence:

- Live JCM `GRMD=212` lists 200 g NaCl, 13 g MgCl2 x 6 H2O, 20 g MgSO4 x 7 H2O, 4 g KCl, 1 g CaCl2 x 2 H2O, 0.2 g NaHCO3, 0.5 g KBr, 5 g Yeast extract (BD-Difco), 1 g glucose, and 20 g agar, then says to add components to distilled water, bring volume to 1 L, and adjust pH to 7.0.
- The same JCM page carries the default sterilization rule to autoclave at 121 C for 15 min unless otherwise stated.
- MediaDive REST `medium/J212` preserves the ten source ingredient rows and the two explicit preparation steps.
- TogoMedium M205 points to `JCM_M212` and the same JCM `GRMD=212` page.

Supported:

- The MediaDive ID, JCM link, category, solid agar state, pH 7.0, ingredient identities, and ingredient amounts are supported.
- Leaving distilled water out of the main ingredient list is acceptable because JCM 212 uses water procedurally to bring the recipe to 1 L rather than as a table row.

Unsupported or incomplete:

- The record omits the JCM page's default autoclaving instruction.
- The generated `Yeast extract` row drops the `BD-Difco` supplier qualifier.

## Completeness

Consequential gaps:

- The default JCM autoclaving step is missing from the generated preparation.

Empty slots that are acceptable:

- No target-organism or growth-metric claim is represented.
- Distilled water can remain procedural in this record.

Bounded negative search:

- An ignored-file-inclusive owner search with `rg --no-ignore --hidden` across `data/normalized_yaml` and `data/merge_yaml/merged` for `Source: JCM, ID: J212`, `Original source: JCM - JCM_M212`, and exact `jcm_medium_no_212` found only the direct MediaDive/JCM record and the TOGO M205 wrapper as source owners for JCM 212.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The JCM default autoclaving step is missing. | The source page says media should be autoclaved at 121 C for 15 min unless otherwise stated; no alternate sterilization is stated for JCM 212. The generated record includes the bring-to-volume and pH adjustment steps only. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_212.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive owner search found `data/normalized_yaml/bacterial/togo_medium_m205.yaml` and `data/merge_yaml/merged/togo_medium_m205.yaml` pointing to `JCM_M212` and `GRMD=212`. The TOGO wrapper adds distilled water as `1 G_PER_L`, preventing equivalence with the otherwise close direct MediaDive import. | Both normalized owners, plus merge regeneration after source repair. |
| Minor | The yeast-extract supplier qualifier is missing. | JCM, MediaDive, and TogoMedium preserve `BD-Difco`; the target stores only `Yeast extract`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_212.yaml`. |

## Recommended Edits

1. Add the JCM default autoclaving step to the direct normalized JCM 212 owner.
2. Preserve `BD-Difco` on yeast extract.
3. Repair `data/normalized_yaml/bacterial/togo_medium_m205.yaml` so distilled water is procedural rather than a 1 g/L ingredient, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on repaired normalized records and regenerated merged records.
- Compare the regenerated JCM 212 against MediaDive `medium/J212` and the live JCM page to confirm the ten source table rows, pH 7.0 adjustment, and default autoclaving step are all represented.
- Search with `rg --no-ignore --hidden 'Source: JCM, ID: J212|Original source: JCM - JCM_M212' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm same-source duplicate handling with ignored files included.

## Additional Notes

- The generated `high_metal: true` flag is plausible for a medium with 200 g/L NaCl and was not treated as a defect.
