# YAML Record Review: 1197

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1197.yaml`
- Started UTC: 20260921T043430Z
- Finished UTC: 20260921T043605Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007463` |
| Label | `1197` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1197.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1197]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1197.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import for NBRC Medium 1196. The maintained owner recovered the 16-row source table on Aug 19 and now has a `nbrc.medium:1196` source term, but it still needs its official `Modified W-medium` display name before regeneration.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1197.yaml` | Pass; `No issues found` |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1197.yaml --out /private/tmp/culturemech-review-1197.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-1197.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1197.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1197.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The intended source identity is NBRC Medium 1196:

- The import history says `Media No. 1196` and links the NBRC `NO=1196` page.
- Live NBRC Medium 1196 is titled `Modified W-medium`.
- The normalized owner now has `media_term: nbrc.medium:1196` and a direct NBRC 1196 reference.

Two identity issues remain:

- The generated file is still labeled `1197`, while live NBRC Medium 1197 is `Plate Count Agar (PCA)`.
- The maintained owner has not yet adopted the official `Modified W-medium` title; it also remains `name: '1197'`.

## Evidence

Supported by inspected source text:

- NBRC Medium 1196 lists `Substrates*` 10 mM with a footnote naming vanillic acid, syringic acid, and similar substrates.
- NBRC Medium 1196 lists KH2PO4 0.85 g, Na2HPO4.7H2O 4.9 g, (NH4)2SO4 0.5 g, MgSO4.7H2O 0.1 g, FeSO4.7H2O 9.5 mg, MgO 10.75 mg, CaCO3 2 mg, ZnSO4.7H2O 1.44 mg, MnSO4.4H2O 1.12 mg, CuSO4.5H2O 0.25 mg, CoSO4.7H2O 0.28 mg, H3BO4 0.06 mg, concentrated HCl 51.3 microliters, distilled water 1 L, and optional agar 15 g.

Unsupported or malformed in the generated record:

- One ingredient row concatenates all 16 source rows and the substrate footnote into a single `preferred_term`.
- The second ingredient row has `preferred_term: ''` and `concentration.value: Substrates*` with `unit: G_PER_L`.
- The generated artifact has no milligram trace-salt rows, no HCl volume row, no source term, and no reference.
- The generated `ADJUST_PH` and autoclaving steps are not in the inspected NBRC 1196 table.

## Completeness

- The generated file omits the Aug 19 recovered NBRC composition, the Aug 20 MIM groundings, and the Sep 7 partial NBRC source-metadata repair.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_1197`, `NO=1196`, `NBRC Medium 1196`, and `nbrc.medium:1196` found this stale generated NBRC direct import, the partially repaired normalized owner, normalized index entries, and a separate TOGO-derived `modified_w_medium` copy of NBRC Medium 1196.
- The broad `Substrates*` row is a real NBRC source row; the record should preserve it as a broad 10 mM substrate slot until a curator resolves exact source variants, rather than replacing it with one selected compound.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading numeric identity for NBRC Medium 1196. | The generated file is labeled `1197`, but its import event points to NBRC Medium 1196; live NBRC Medium 1197 is Plate Count Agar. | `data/normalized_yaml/bacterial/NBRC_1197.yaml` plus merge regeneration; the normalized owner still needs the official title. |
| Blocker | The 16-row NBRC table is collapsed into two malformed rows. | NBRC Medium 1196 has 16 composition rows, including milligram salts and a 51.3 microliter HCl row; the generated file has one concatenated formula/footnote blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1197.yaml`; fix NBRC import or merge source selection if the malformed rows persist. |
| Major | The official medium title is still missing from both the generated and normalized NBRC direct records. | Live NBRC Medium 1196 is `Modified W-medium`, but both `data/merge_yaml/merged/1197.yaml` and `data/normalized_yaml/bacterial/NBRC_1197.yaml` retain `name: '1197'`. | `data/normalized_yaml/bacterial/NBRC_1197.yaml`. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 1196 page supplies the composition table and substrate footnote; the stale generated record invented `ADJUST_PH` and autoclave text, while the maintained owner now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1197.yaml`. |
| Minor | A TOGO-derived copy of NBRC Medium 1196 remains separate. | Gitignore-independent search found `modified_w_medium` with the same NBRC 1196 source URL. | Merge-fingerprint rules for NBRC/TOGO duplicate handling after the direct NBRC import receives its official title. |

## Recommended Edits

1. Curate `data/normalized_yaml/bacterial/NBRC_1197.yaml` to use `Modified W-medium` as `name` and `original_name`, preserving `nbrc.medium:1196`.
2. Regenerate `data/merge_yaml/merged/` and confirm the generated artifact carries the 16 recovered ingredient rows, milligram and microliter units, 1 L water, optional 15 g/L agar, and the NBRC 1196 reference.
3. If regeneration preserves the concatenated blob, fix the NBRC merge source path that is still using the pre-recovery import output.
4. Review duplicate handling between the NBRC direct import and the TOGO `modified_w_medium` copy.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/NBRC_1197.yaml` after the normalized title edit.
- `just verify-merges` to prove generated outputs are fresh after regeneration.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1196>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1196>.yaml` for the grounded subset.
- Manual comparison to live NBRC Medium 1196 for all 16 rows and the `Substrates*` footnote.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
