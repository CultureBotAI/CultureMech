# YAML Record Review: bcye_2216e

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bcye_2216e.yaml
- Started UTC: 2026-09-21T19:14:46Z
- Finished UTC: 2026-09-21T19:16:48Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bcye_2216e.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bcye_2216e.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:009559` |
| Name | `bcye_2216e` |
| Original name | `BCYEalpha-2216E` |
| Source | Togo Medium `M3046`; NBRC `NBRC_M1596` |
| Merge status | Generated one-source merge from `bcye_2216e` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated Togo M3046 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this Togo import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated Togo M3046 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bcye_2216e.yaml` denotes Togo Medium `M3046`, `BCYEalpha-2216E`, originally from NBRC `NBRC_M1596`.
- The generated merge is in sync with `data/normalized_yaml/bacterial/bcye_2216e.yaml`; fixes belong in that normalized owner.
- The Togo M3046 API supports the ingredient labels and gram or litre amounts represented in the YAML, but it reports medium pH `7.5` while the YAML has `ph_value: 6.9`.
- Exact gitignore-independent searches for `CultureMech:009559`, `TOGO:M3046`, `NBRC_M1596`, `bcye_2216e`, and `BCYEalpha-2216E` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, generated merge, generated indexes, and ingredient-grounding reports, but no local raw Togo or NBRC dump.

## Evidence

- Togo M3046 lists one `main solution 1` containing 1 L distilled water, 1 g L-cysteine, 10 g glucose, 10 g ACES Buffer, 2.8 g KOH, 0.25 g ferric pyrophosphate, 1 g alpha-ketoglutarate, 37.4 g Marine Broth 2216, and 25 g Legionella CYE Agar Base.
- The YAML records the seven simple components as direct ingredients with the same amounts, except 1 L distilled water is modeled as `1 G_PER_L` rather than a volume or final make-up solvent.
- The YAML moved the two complex powder components, `Marine Broth 2216**` and `Legionella CYE Agar Base*`, into `solutions` with empty `composition: []` and `name: Unknown solution`. The Togo API lists them as ordinary components in the same main solution, not as stock solutions.
- The Togo API labels `ACES Buffer` as ACES and `alpha-Ketoglutarate` as alpha-ketoglutaric acid; both remain ungrounded in the YAML.

## Completeness

- Consequential gaps:
  - `ph_value` is wrong: the source record says pH 7.5, not pH 6.9.
  - The water row has the wrong unit and semantics.
  - The commercial Marine Broth and Legionella CYE Agar Base rows are modeled as anonymous empty-composition solutions instead of source components.
  - ACES and alpha-ketoglutarate need exact ontology grounding or an explicit unresolved note.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the inspected Togo API record gives a medium formulation and no strain-specific growth assay.
  - Marine Broth 2216 and Legionella CYE Agar Base are complex commercial components that should not be forced to narrow CHEBI small-molecule terms.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw Togo M3046 or NBRC_M1596 source dump under `data/raw` or `data/import_tracking`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The imported pH is wrong. | Togo M3046 has pH `7.5`; the generated and normalized YAML have `ph_value: 6.9`. | `data/normalized_yaml/bacterial/bcye_2216e.yaml`. |
| Major | Distilled water is represented as a mass concentration. | The source lists 1 L distilled water; the YAML stores `Distilled water` as `1 G_PER_L`. | The normalized owner; broad repeats may require the Togo importer water mapping. |
| Major | Two commercial powders are over-modeled as anonymous stock solutions. | Togo lists `Marine Broth 2216**` 37.4 g and `Legionella CYE Agar Base*` 25 g as items in `main solution 1`; the March migration moved them to empty `solutions` and assigned `name: Unknown solution`. | The normalized owner; broad repeats may require revisiting `solution-migrator-v1.0`. |
| Major | Two defined ingredients are ungrounded despite source labels. | Togo labels `ACES Buffer` as ACES and `alpha-Ketoglutarate` as alpha-ketoglutaric acid; the YAML has no `term` for either row. | The normalized owner. |

## Recommended Edits

1. Correct `ph_value` to the source pH 7.5.
2. Represent the distilled water row as 1 L final-volume solvent or move it into preparation so it is not a `G_PER_L` ingredient.
3. Move `Marine Broth 2216**` and `Legionella CYE Agar Base*` back to complex component ingredients unless the schema has a more specific commercial-component slot; remove `name: Unknown solution`.
4. Ground ACES and alpha-ketoglutarate to exact terms after verifying the intended protonation and salt forms, or document them as unresolved.
5. Regenerate `data/merge_yaml/merged/bcye_2216e.yaml` from the repaired normalized owner.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Re-fetch `gmdb_medium_by_gmid?gm_id=M3046` and manually compare pH, all nine main-solution rows, and complex component boundaries against the YAML.
- Regenerate the generated merge and run `just verify-merges` plus `just audit-merge-freshness`.
- Confirm `Marine Broth 2216**` and `Legionella CYE Agar Base*` no longer appear as `Unknown solution` entries.

## Additional Notes

- The Togo public `/medium/M3046` route was not used; the inspected source was the Togo SPARQList API response for `gm_id=M3046`.
- Exact gitignore-independent searches included ignored files where present.
