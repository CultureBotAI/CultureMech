# YAML Record Review: 15_mh_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml`
- Started UTC: 20260921T044931Z
- Finished UTC: 20260921T045021Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009790` |
| Label | `15_mh_medium` |
| Original label | `15% MH Medium` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml`, the direct JCM owner, their import normalization, or merge regeneration |
| Merge owner | `merged_from: [TOGO_M406_15_MH_Medium]` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` |

This generated singleton is the TOGO Medium M406 import of a JCM 408 `15% MH MEDIUM` copy.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml --out /private/tmp/15_mh_medium__cef1d1f5.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/15_mh_medium__cef1d1f5.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated target correctly carries the TOGO Medium M406 accession and traces back to JCM 408, but it has not been merged with the direct JCM 408 import:

- The local TOGO import says `Original source: JCM - JCM_M408` and names the JCM 408 URL.
- The JCM 408 source page lists the same solid `15% MH MEDIUM`, agar, hydrate salts, BD-Difco peptone and yeast extract, glucose, distilled water, and final pH 7.5 represented by the TOGO record, apart from TOGO's water unit conversion and missing pH field.
- `data/merge_yaml/merged/15_mh_medium__b586cb85.yaml` is the direct JCM import of the same JCM 408 page. It should become a source duplicate of this record after source accession and water handling are normalized.

The record should remain separate from the DSMZ Medium 582 liquid recipe because DSMZ 582 contains NaBr and no top-level agar row.

## Evidence

Supported by inspected source text:

- JCM 408 supports the TOGO record's hydrate-specific MgCl2, MgSO4, and CaCl2 labels; NaCl, KCl, NaHCO3, glucose, 20 g/L agar, and BD-Difco complex ingredients.
- JCM 408 supports the record's `physical_state: SOLID_AGAR`.

Unsupported, incomplete, or dimensionally wrong in the generated record:

- JCM lists distilled water as 1.0 L, not as `1 G_PER_L`.
- JCM says to adjust pH to 7.5; the TOGO record has no `ph_value` and no pH preparation step.
- The global JCM instruction to autoclave media at 121 degrees C for 15 min unless otherwise stated is not represented.
- The JCM URL and TOGO URL are present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The solidifying component and the hydrate identities are more complete than the direct JCM singleton.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the JCM source page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:009790`, `CultureMech:002764`, `TOGO:M406`, `JCM_M408`, the TOGO/JCM source slugs, and their merge fingerprints found only this TOGO singleton, the direct JCM singleton, and their ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The water row has the wrong dimension. | JCM 408 lists distilled water as `1.0 L`; the TOGO record stores it as `value: '1'`, `unit: G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` or the TOGO import mapping for solvent rows. |
| Major | JCM's pH 7.5 is missing. | The JCM source says to adjust pH to 7.5, while the TOGO record has no `ph_value` and no preparation step. | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` or the TOGO import mapping. |
| Major | The JCM default sterilization condition is missing. | The JCM page says media should be autoclaved at 121 degrees C for 15 min unless otherwise stated; the generated record has no preparation steps. | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` or the TOGO import mapping. |
| Major | The TOGO M406 and direct JCM 408 imports of the same source recipe failed to merge as source duplicates. | This target and `data/merge_yaml/merged/15_mh_medium__b586cb85.yaml` both trace to the JCM 408 URL, but they have distinct merge fingerprints because the TOGO import stores water as `1 G_PER_L` and uses different preferred labels. | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` and `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml`, followed by merge regeneration. |
| Minor | Structured references are absent. | The TOGO and JCM URLs are present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` or the TOGO import mapping. |

## Recommended Edits

1. Correct the TOGO solvent conversion so `Distilled water` is represented as the JCM source's 1 L final-volume row or an equivalent final-volume convention, not as `1 G_PER_L`.
2. Add `ph_value: 7.5` and a pH adjustment preparation step from the JCM 408 source.
3. Add the JCM default autoclave instruction.
4. Normalize this TOGO import against `data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` so both source copies merge as a `SOURCE_DUPLICATE` cluster after regeneration.
5. Add structured references for TOGO Medium M406 and the original JCM 408 source page.
6. Regenerate `data/merge_yaml/merged/` and confirm the TOGO and JCM copies merge while DSMZ Medium 582 remains separate.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml data/normalized_yaml/bacterial/JCM_J408_15_MH_MEDIUM.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/TOGO_M406_15_MH_Medium.yaml` after adding structured references.
- `just verify-merges` to prove the TOGO and JCM generated outputs coalesce after regeneration.
- `just validate-strict` and `just validate-terms` on the regenerated 15% MH JCM/TOGO merge record.
- Manual comparison to the JCM 408 page for water amount, pH, autoclave conditions, agar, BD-Difco qualifiers, and source-duplicate equivalence with the direct JCM import.

## Additional Notes

- The direct TOGO Medium M406 fetch returned the TogoMedium JavaScript shell rather than embedded recipe rows; the local TOGO record points back to the JCM 408 source page, and that source page resolved.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
