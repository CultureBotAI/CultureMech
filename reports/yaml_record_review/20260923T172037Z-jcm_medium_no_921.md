# YAML Record Review: jcm_medium_no_921

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_921.yaml
- Started UTC: 2026-09-23T17:19:41Z
- Finished UTC: 2026-09-23T17:20:37Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/jcm_medium_no_921.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:003269` |
| Label | `jcm_medium_no_921` |
| Source term | `mediadive.medium:J921` |
| Upstream source | Legacy JCM MEDIUM No. 921, mirrored by MediaDive as JCM `J921` |
| Generated from | `data/normalized_yaml/bacterial/jcm_medium_no_921.yaml` |

This is a generated merge artifact with `merge_fingerprint: 4dc558846f456eb7bdcb49bfeb1dea917c0fb097f0bae486493943649c02c974` and `merged_from: [jcm_medium_no_921]`. Future edits belong in the maintained MediaDive/JCM normalized owner, not in `data/merge_yaml/merged/`.

An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, `data/import_tracking`, `history`, and `data/culturemech_recipe_catalog.tsv` found the maintained JCM owner, the standalone `Main sol. J921` import, and TOGO M967 as an independently imported sibling for the same original JCM GRMD=921 formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_921.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_921.yaml --out /private/tmp/jcm921.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_921.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 0 reference checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_921.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. The only output before the pass line was the known `eutils` `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not `MediaRecipe.curation_history` embedded in merged YAML. |

The repository-level `just` validators were not used because this checkout's `uv` resolution currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails inside `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused Python 3.11 validators above exercise the relevant schema, strict, reference, and term gates for this one generated `MediaRecipe`.

## Identity and Grounding

The record denotes the intended MediaDive JCM import: `CultureMech:003269` is `jcm_medium_no_921`, carries source term `mediadive.medium:J921`, and has MediaDive import provenance for JCM `J921`.

Live JCM GRMD=921 now returns `Nothing found`, and the TOGO M967 API now returns an empty `components` array plus a `Not yet` comment. The legacy formulation is still recoverable from MediaDive J921 and the repaired TOGO M967 normalized record: 5 g/L Marine broth 2216, 2 g/L starch, 5 g/L NaCl, and pH 7.0.

The generated JCM record preserves the three ingredient amounts, pH 7.0, and liquid/complex classification. It loses the `BD-Difco` attribute on Marine broth 2216 and the `soluble` attribute on starch.

## Evidence

Supported claims:

- MediaDive J921 supports the generated identity, 5 g Marine broth 2216, 2 g starch, 5 g NaCl, and pH adjustment to 7.0.
- TOGO M967 no longer exposes the ingredients in its live API, but its maintained normalized record was repaired from the same JCM 921 source and agrees with MediaDive on the three rows.

Unsupported or incomplete generated claims:

- MediaDive attaches `attribute: BD-Difco` to Marine broth 2216; the generated record stores only `Marine broth 2216`.
- MediaDive attaches `attribute: soluble` to starch; the generated record stores only `Starch`.
- The generated note points to live JCM GRMD=921 even though that page now returns `Nothing found`.

## Completeness

The generated record is complete enough to capture the preserved three-row legacy recipe. Empty optional water, evidence, target-organism, salinity, atmosphere, and storage fields are acceptable: the inspected MediaDive payload provides no water, growth evidence, or incubation/storage conditions, and TOGO's live payload is an empty placeholder.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Minor | Two MediaDive row attributes were dropped. | MediaDive J921 carries `BD-Difco` on Marine broth 2216 and `soluble` on Starch; the generated record omits both. | `data/normalized_yaml/bacterial/jcm_medium_no_921.yaml` or the MediaDive import mapping for row attributes. |
| Minor | The JCM source URL is stale without a discussion flag. | The generated notes point to GRMD=921, but live JCM returns `Nothing found`; TOGO M967 also now has no structured components. | `data/normalized_yaml/bacterial/jcm_medium_no_921.yaml`. |

## Recommended Edits

1. Preserve the BD-Difco and soluble row attributes in `data/normalized_yaml/bacterial/jcm_medium_no_921.yaml`.
2. Add a concrete discussion flag that live JCM GRMD=921 is unavailable and live TOGO M967 is a placeholder while MediaDive preserves the legacy formula.

## Follow-up Checks

- Rerun the focused schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/jcm_medium_no_921.yaml` after the maintained owner is edited.
- Regenerate `data/merge_yaml/merged/jcm_medium_no_921.yaml` and confirm the Marine broth 2216 and starch qualifiers survive into the merged artifact.
- Recheck JCM GRMD=921, MediaDive `/rest/medium/J921`, and TOGO M967 manually to ensure the source-availability note still reflects the current provider state.
- Run the merge freshness and product validators after regeneration.

## Additional Notes

- The standalone solution record `data/normalized_yaml/bacterial/mediadive_4926_Main_sol_J921.yaml` has the same two missing row attributes and still carries an obsolete `incomplete_composition` data-quality flag despite having a complete three-row composition.
