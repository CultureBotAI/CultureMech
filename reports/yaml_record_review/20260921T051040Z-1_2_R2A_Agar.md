# YAML Record Review: 1_2_r2a_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2_R2A_Agar.yaml`
- Started UTC: 20260921T050926Z
- Finished UTC: 20260921T051040Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:010300` |
| Slug | `1_2_r2a_agar` |
| Original name | `1/2 R2A Agar` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owners | `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml`, `data/normalized_yaml/bacterial/TOGO_M839_1_10_R2A_Agar.yaml` |
| Source accessions | parent `TOGO:M881` / JCM 845; child `TOGO:M839` / JCM 804 |
| Merge fingerprint | `91d917ee07ef5180979b02c394725670252532c117232acfba08f13ea782dabc` |

Reviewed the generated merge that collapsed TOGO M881 `1/2 R2A Agar` and its TOGO M839 `1/10 R2A Agar` concentration variant. The exact ID/accession/fingerprint/JCM-source search included ignored and hidden files under the normalized YAML, merged YAML, recipe catalogs, raw data, import tracking, and references trees; it found the two TOGO owners, the generated target, their expected catalog/index rows, the `deep_research_priority.json` inventory rows, and a separate direct MediaDive/JCM 845 owner that already repairs the 1/2 R2A formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2_R2A_Agar.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2_R2A_Agar.yaml --out /private/tmp/1_2_R2A_Agar.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2_R2A_Agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2_R2A_Agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validator wrappers remain blocked by the project `llvmlite==0.46.0` build under Python 3.13; the no-project `uv` commands above were used as focused equivalents.

## Identity and Grounding

- The generated artifact is internally inconsistent. Its stable ID, `media_term`, top-level name, and `notes` denote TOGO M881 / JCM 845 `1/2 R2A Agar`, but its `Agar` and `R2A agar (BD-Difco)` amounts are copied from the TOGO M839 / JCM 804 `1/10 R2A Agar` child.
- JCM 845 and TOGO M881 support the maintained parent values: 9.1 g R2A agar (BD-Difco), 7.5 g added agar, and 1.0 L distilled water.
- JCM 804 and TOGO M839 support the maintained child values: 1.82 g R2A agar (BD-Difco), 15.0 g added agar, and 1.0 L distilled water.
- The two normalized TOGO owners correctly encode their different R2A and added-agar amounts, but both still encode distilled water as `1 G_PER_L`.
- `data/normalized_yaml/bacterial/1_2_r2a_agar.yaml` is a direct JCM/MediaDive owner for the same 1/2 R2A JCM 845 source and was repaired on Sep 8 with the correct JCM 845 values, the JCM default autoclave step, structured JCM references, and a `culturemech_term` link from the `R2A agar (BD-Difco)` wrapper to `CultureMech:002706` / `R2A Agar`.

## Evidence

- The live TOGO M881 response and the live JCM 845 HTML page both identify the parent as `1/2 R2A Agar` and list 9.1 g R2A agar powder plus 7.5 g agar in 1 L distilled water.
- The live TOGO M839 response and the live JCM 804 HTML page both identify the child as `1/10 R2A Agar` and list 1.82 g R2A agar powder plus 15 g agar in 1 L distilled water.
- Both JCM pages carry the default instruction to autoclave at 121 C for 15 min unless stated otherwise; neither R2A page overrides that default.
- Neither JCM page specifies pH, incubation temperature, salinity, atmosphere, or target organisms.

## Completeness

- Blocker: the generated `1/2 R2A Agar` record publishes the 1/10 child formulation.
- Consequential gap: both TOGO owners use `1 G_PER_L` for a source row that is 1 L distilled water.
- Consequential gap: the JCM default autoclave step is missing from both TOGO owners.
- Consequential gap: the TOGO parents/children should be generated as separate concentration variants, not a single merged recipe with one set of ingredients.
- Non-blocking gap: `R2A agar (BD-Difco)` is an exact wrapper for the maintained `R2A Agar` medium but the TOGO rows do not link it to `CultureMech:002706`.
- Non-blocking provenance gap: the TOGO and JCM URLs are only free text in the TOGO owners; they have no structured `references` entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record's identity and formulation disagree. | `data/merge_yaml/merged/1_2_R2A_Agar.yaml` denotes `TOGO:M881` / `1/2 R2A Agar` but contains `1.82 G_PER_L` R2A agar and `15 G_PER_L` added agar, which are the inspected TOGO M839 / JCM 804 child values. | Merge generation and the TOGO parent/child merge rule; never patch the generated file directly. |
| Major | Distilled water has the wrong dimension in both TOGO normalized owners. | JCM 845 and JCM 804 list 1.0 L distilled water; `TOGO_M881_1_2_R2A_Agar.yaml` and `TOGO_M839_1_10_R2A_Agar.yaml` both store `1 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml`, `data/normalized_yaml/bacterial/TOGO_M839_1_10_R2A_Agar.yaml` |
| Major | Both TOGO normalized owners omit the JCM default autoclave step. | The JCM 845 and 804 pages both use the default 121 C, 15 min autoclave instruction. | `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml`, `data/normalized_yaml/bacterial/TOGO_M839_1_10_R2A_Agar.yaml` |
| Major | A curated direct JCM 845 owner already exists separately from the TOGO 1/2 owner. | `data/normalized_yaml/bacterial/1_2_r2a_agar.yaml` was repaired against JCM 845 on Sep 8; the generated target only lists the two TOGO files under `merged_from` and has not reconciled the direct JCM record. | Duplicate reconciliation across `data/normalized_yaml/bacterial/1_2_r2a_agar.yaml` and `TOGO_M881_1_2_R2A_Agar.yaml` |
| Minor | `R2A agar (BD-Difco)` lacks the available wrapper link in both TOGO owners. | The repaired direct JCM owner links the row to `culturemech_term: CultureMech:002706`; the two TOGO owners leave the same commercial powder ungrounded. | `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml`, `data/normalized_yaml/bacterial/TOGO_M839_1_10_R2A_Agar.yaml` |
| Minor | TOGO/JCM source URLs are only free text. | The URLs appear in top-level `notes`, and focused reference validation reports 0 checks. | `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml`, `data/normalized_yaml/bacterial/TOGO_M839_1_10_R2A_Agar.yaml` |

## Recommended Edits

1. Fix merge generation so `CONCENTRATION_VARIANT` parent/child pairs keep distinct ingredient lists and do not collapse into one generated recipe.
2. Repair both TOGO normalized owners: convert `1 G_PER_L` distilled water to `1000 ML_PER_L`, add the JCM default autoclave step, add structured TOGO/JCM references, and link `R2A agar (BD-Difco)` to the exact `CultureMech:002706` wrapper.
3. Reconcile `data/normalized_yaml/bacterial/TOGO_M881_1_2_R2A_Agar.yaml` with the already repaired direct JCM owner `data/normalized_yaml/bacterial/1_2_r2a_agar.yaml` so the same JCM 845 formulation does not remain duplicated under separate source paths.
4. Regenerate the merge layer and verify that 1/2 R2A and 1/10 R2A are generated as separate linked variants with the correct 9.1/7.5 and 1.82/15 values respectively.

## Follow-up Checks

- Run strict, term, and reference validators for `TOGO_M881_1_2_R2A_Agar.yaml`, `TOGO_M839_1_10_R2A_Agar.yaml`, and `1_2_r2a_agar.yaml` after curation.
- Re-run merge generation and inspect the generated 1/2 and 1/10 R2A outputs for correct concentration values and variant links.
- Add or run a regression check that rejects generated records whose canonical `media_term` accession carries child-variant ingredient values.

## Additional Notes

- The unrelated JCM 846 page was checked during source resolution because `GRMD=846` appeared near the exact JCM 845 search hits; it is MO-CFXI Medium and is irrelevant to this R2A record.
- Empty pH, incubation, salinity, and organism slots are appropriate for the inspected JCM 804/845 source pages.
