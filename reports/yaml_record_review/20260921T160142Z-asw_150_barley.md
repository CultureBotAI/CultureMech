# YAML Record Review: ASW 150 + barley

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asw_150_barley.yaml
- Started UTC: 2026-09-21T15:59:23Z
- Finished UTC: 2026-09-21T16:01:42Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asw_150_barley.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:000045` |
| Label | `ASW 150 + barley` |
| Normalized owners | `data/normalized_yaml/algae/CCAP_ASW 150 + barley  + barley grain_).yaml`, `data/normalized_yaml/algae/asw_150_barley.yaml` |
| Source | CCAP `ASW_150`, `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW_150.pdf` |
| Generated status | Generated merge of two algae CCAP `ASW_150` records; future fixes belong in the normalized owners or the CCAP importer/repair rules and should be propagated by regenerating merges/pages. |

## Validation

| Check | Result |
|---|---|
| Open-schema LinkML validation | Passed: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asw_150_barley.yaml` reported `No issues found`. |
| Strict validation | Passed: the no-project Python 3.11 invocation of `scripts/validate_strict.py data/merge_yaml/merged/asw_150_barley.yaml --out /private/tmp/asw_150_barley.strict.tsv --workers 1 --quiet` scanned 1 file and wrote 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/asw_150_barley.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 snippet checks, and reported all validations passed. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/asw_150_barley.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` reported validation passed after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone records under `history/`, not a focused embedded-`MediaRecipe.curation_history` validator for one generated merge record. |

The documented `just` entry points remain blocked by the project-level Python
3.13 `llvmlite==0.46.0` build failure, so the focused no-project Python 3.11
validator invocations above were used for this one record.

## Identity and Grounding

The generated target is a merged CCAP `ASW_150` algae record with two
authoritative normalized inputs. The current CCAP PDF at the recorded
`MR_ASW_150.pdf` URL is titled `ASW 150`, lists five artificial-seawater salts,
and gives pH/autoclaving instructions.

The generated merge is stale relative to both normalized owners. It was
generated on 2026-08-06 and still carries the old instruction row
`Make up to 1 litre with deionised water. Adjust pH to 7-8 with` as a
`1 MOLAR` ingredient. Both normalized algae owners have 2026-08-25 and
2026-09-13 curation events that removed that pseudo-ingredient, added
preparation steps, and normalized structured CCAP provenance.

The `+ barley` identity needs a source-drift check. The bacterial MediaDive
copy for CCAP `C6` has an old preparation instruction to add 20 ml ASW 150 and
four autoclaved barley grains to a tissue-culture flask, but the current CCAP
`MR_ASW_150.pdf` at the algae records' source URL has no barley instruction.

## Evidence

The CCAP `MR_ASW_150.pdf` text was downloaded and extracted with `pypdf`. It
supports the five salt rows and pH/autoclave instructions, but not the stale
sixth generated ingredient.

Supported claims:

- The current CCAP source URL serves a recipe titled `ASW 150`.
- The source lists NaCl 136 g/L, KCl 3.8 g/L, MgCl2.6H2O 8.9 g/L,
  MgSO4.7H2O 0.9 g/L, and CaCl2.2H2O 0.65 g/L.
- The source instructs the preparer to make the medium up to 1 litre with
  deionized water, adjust pH to 7-8 with 1 M NaOH or 1 M HCl before
  autoclaving, and autoclave at 15 psi for 15 minutes.

Unsupported or over-scoped assertions:

| Record assertion | Problem |
|---|---|
| `Make up to 1 litre with deionised water. Adjust pH to 7-8 with` as a `1 MOLAR` ingredient | This is a truncated instruction, not an ingredient. Both current normalized owners already moved it to `preparation_steps`. |
| `MgSO .7H O` | The source formula is `MgSO4.7H2O`; the generated label dropped both `4` and `2`. |
| `ASW 150 + barley` | The current inspected CCAP PDF is labeled `ASW 150` and does not mention barley grains. |
| Generic light, temperature, and salinity prose | The inspected ASW 150 PDF gives formulation, pH, and autoclaving instructions but no culture light cycle, photon flux, or incubation temperature range. |

## Completeness

- The current normalized algae owners contain five salt ingredients and three
  preparation steps; the generated record has not been regenerated to pick up
  that August repair.
- `find data/normalized_yaml -name 'asw_150_barley.yaml' -o -name '*ASW*150*barley*' -o -name '*asw*150*barley*'`
  found the two algae owners and one bacterial MediaDive/CCAP copy.
- `find reports/yaml_record_review -maxdepth 1 -name '*asw_150_barley.md'`
  found no prior report for this generated record.
- A gitignore-independent exact search for
  `CultureMech:000045|CCAP:ASW_150|MR_ASW_150|wp-content/uploads/MR_ASW_150|ASW 150|asw_150_barley`
  under `data`, `scripts`, `history`, `src`, `reports`, and
  `references_cache` found the target owners, the generated target, the
  bacterial CCAP C6 owner, ASW salinity-variant proposals, historical
  validation references to older filenames, and the August CCAP
  instruction-row repair script.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The generated merge is stale and still represents a preparation sentence as a molar ingredient. | The generated file contains a sixth `Make up to...` row at `1 MOLAR`; both normalized owners removed that row on 2026-08-25 and now store make-up, pH-adjustment, and autoclave instructions as preparation steps. | Regenerate `data/merge_yaml/merged/asw_150_barley.yaml` from the normalized owners. |
| Major | The retained `ASW 150 + barley` identity is not supported by the current CCAP source at `MR_ASW_150.pdf`. | The current PDF is titled `ASW 150` and lists only the five salts plus pH/autoclave instructions. The only inspected barley wording was in the separate bacterial MediaDive import. | Both algae `ASW_150` normalized owners, or the CCAP import metadata if an older PDF revision carried barley. |
| Minor | `MgSO4.7H2O` is mangled in the generated ingredient label. | The source says `MgSO4.7H2O`; the generated label is `MgSO .7H O`. The CHEBI grounding is already magnesium sulfate heptahydrate. | Both algae `ASW_150` normalized owners still carry the mangled label. |
| Minor | Generic light and temperature notes are unsupported by the inspected ASW 150 recipe. | The CCAP PDF does not state the record's generic light intensity, light cycle, or 15-30 C temperature range. | Both algae `ASW_150` normalized owners. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/asw_150_barley.yaml` so the generated
   record contains the repaired five-ingredient, three-step normalized form.
2. Verify whether CCAP `ASW_150` previously denoted a barley-grain recipe. If
   not, rename the algae owners to `ASW 150`; if yes, preserve the old source
   revision or scope the current `MR_ASW_150.pdf` reference to only the five
   salts.
3. Correct the magnesium sulfate heptahydrate display label to
   `MgSO4 x 7 H2O` or another local formula style that preserves the `4` and
   `2`.
4. Remove or evidence-scope the generic light-intensity, light-cycle, salinity,
   and temperature prose.

## Follow-up Checks

- Re-run focused schema, strict, term, and reference validation on the two
  normalized algae owners.
- Re-run `just verify-merges` and `just audit-merge-freshness` after
  regenerating `data/merge_yaml/merged/asw_150_barley.yaml`.
- Re-fetch `MR_ASW_150.pdf` and compare all five salt rows and pH/autoclaving
  instructions against the regenerated YAML.
- Re-run a gitignore-independent exact search for `CultureMech:000045`,
  `CultureMech:000044`, and `MR_ASW_150.pdf` to confirm there is no stale
  generated ASW 150 row left.

## Additional Notes

- The bacterial `data/normalized_yaml/bacterial/asw_150_barley.yaml` record has
  the same five-salt signature and may be the right place to preserve the
  20-ml-plus-barley-grain preparation if that instruction came from a
  MediaDive/CCAP page rather than the current PDF.
