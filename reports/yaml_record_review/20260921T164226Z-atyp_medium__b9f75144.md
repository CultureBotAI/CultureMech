# YAML Record Review: atyp_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/atyp_medium__b9f75144.yaml
- Started UTC: 2026-09-21T16:40:42Z
- Finished UTC: 2026-09-21T16:42:26Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/atyp_medium__b9f75144.yaml`, a generated
`MediaRecipe` with stable ID `CultureMech:002717`, normalized name
`atyp_medium`, original name `ATYP MEDIUM`, category `bacterial`,
`medium_type: COMPLEX`, `composition_type: UNDEFINED`, `physical_state:
LIQUID`, `ph_value: 6.9`, media term `mediadive.medium:J358`, and merge
fingerprint
`b9f75144f6a32612fdf4c71f9a921f0f975f39b9f5610b90c4ee26dbffbded2f`.

The merge was generated from exactly one maintained source,
`data/normalized_yaml/bacterial/atyp_medium.yaml`. Future curation must change
that normalized owner, the related MediaDive solution import, or the merge
outputs regenerated from them; the generated merge file should not be edited
directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/atyp_medium__b9f75144.yaml` | Pass, `No issues found`. |
| Strict schema layer | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/atyp_medium__b9f75144.yaml --out /private/tmp/atyp_medium__b9f75144.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned, 0 files with errors, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/atyp_medium__b9f75144.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated, 0 reference checks emitted. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/atyp_medium__b9f75144.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass; the validator also emitted only the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone `history/` files, not a focused validator for `MediaRecipe.curation_history` in one generated merge record. |

The direct `just validate-schema`, `just validate-strict`, `just
validate-references`, and `just validate-terms` entrypoints were not used for
this target because this checkout currently reaches a project `uv` build of
`llvmlite==0.46.0` under Python 3.13 before target-specific validation and
crashes in setuptools with `TypeError: Popen.__init__() got an unexpected
keyword argument 'dry_run'`.

## Identity and Grounding

The generated record identity is internally consistent as a MediaDive-derived
JCM J358 import: the generated target and its normalized owner use
`CultureMech:002717`, `JCM Medium J358`, `mediadive.medium:J358`, and the JCM
GRMD 358 URL. The live JCM GRMD 358 page resolved during this review as
`ATYP MEDIUM`, and the Togo M352 API copy identifies the same JCM medium as
`ATYP Medium` with pH 6.9.

One stock ingredient grounding is too broad for the source label:
`NiCl2 x 6 H2O` is grounded to `CHEBI:34887` nickel dichloride even though
JCM and Togo both specify nickel chloride hexahydrate.

A gitignore-independent exact search covered `data`, `reports`, `history`, and
`.claude` for `CultureMech:002717`, `mediadive.medium:J358`, `J358`,
`GRMD=358`, `ATYP MEDIUM`, and `atyp_medium__b9f75144`. It found the generated
target, the maintained MediaDive/JCM owner, the related
`data/normalized_yaml/bacterial/mediadive_4077_Main_sol_J358.yaml` solution,
the same-source TOGO M352 import, indexes, and archived pre-rename paths.

## Evidence

JCM GRMD 358 lists the base medium as one liter of water with 1 g each of
NaCl, KH2PO4, NH4Cl, sodium acetate, and sodium succinate; 0.5 g MgCl2
hexahydrate; 0.1 g CaCl2 dihydrate; 0.5 g Yeast extract; 0.3 g Bacto peptone;
and 1 ml additions of Trace element solution SLA and Vitamin solution VA. The
MediaDive `Main sol. J358` import instead carries an `Original volume: 1002
mL` note and stores those base ingredients as divided values such as
0.998004 g/L rather than the source's 1 g/L.

Trace element solution SLA is a 1 L stock with nine trace salts. The generated
record keeps FeCl2 tetrahydrate, CoCl2 hexahydrate, NiCl2 hexahydrate, CuCl2
dihydrate, MnCl2 tetrahydrate, ZnCl2, H3BO3, Na2MoO4 dihydrate, and Na2SeO3
pentahydrate as final-medium `ingredients`; the current normalized owner has
already nested FeCl2 into `Trace element solution SLA` but still leaves the
other eight trace-stock components flat in the final medium.

Vitamin solution VA is a 100 ml stock with Biotin, Niacin, Thiamine HCl,
p-Aminobenzoic acid, Pyridoxine HCl, Calcium pantothenate, and Vitamin B12.
The generated record keeps all seven vitamins as direct `ingredients`; the
current normalized owner has already nested Biotin, Thiamine HCl,
p-Aminobenzoic acid, and Pyridoxine HCl into `Vitamin solution VA` but still
leaves Niacin, Calcium pantothenate, and Vitamin B12 flat in the final medium.

The generated target is stale relative to that partial repair. Its
`curation_history` stops with an August 6, 2026 merge event, while the
normalized owner has an August 13, 2026 `apply_cocktail_nesting.py` event that
moved five stock-strength components under two solution additions.

The preparation text is incomplete but not absent. It retains the final pH
adjustment to 6.9, separate filtration of Vitamin solution VA, and pH 2 to 3
adjustment with HCl, but the last pH instruction is detached from Trace
element solution SLA because the trace stock itself is not represented in the
generated record.

## Completeness

The missing target organism and growth-evidence fields are not findings in
this imported JCM recipe: neither the inspected JCM recipe nor the Togo API
payload asserts a strain growth outcome.

The generated target is consequentially incomplete as a projection of the
current normalized owner because it lacks both repaired `solutions` rows.
The current normalized owner is also still incomplete relative to JCM GRMD 358
because it leaves eight Trace element solution SLA components and three
Vitamin solution VA components flat in the medium. Before writing this report,
`find reports/yaml_record_review -maxdepth 1 -name '*atyp_medium__b9f75144.md' -print`
covered ignored and unignored files in the review-report directory and found no
prior exact report for this generated stem.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and omits the maintained record's two repaired solution additions. | `data/normalized_yaml/bacterial/atyp_medium.yaml` nested five stock components under `Trace element solution SLA` and `Vitamin solution VA` on 2026-08-13; `data/merge_yaml/merged/atyp_medium__b9f75144.yaml` was generated on 2026-08-06 and still has all stock components flat. | Regenerate `data/merge_yaml/merged/` from the current normalized owner. |
| Major | The current MediaDive/JCM owner only partially repaired Trace element solution SLA. | JCM has nine salts in the trace stock; only FeCl2 tetrahydrate is nested in `atyp_medium.yaml`, leaving CoCl2, NiCl2, CuCl2, MnCl2, ZnCl2, H3BO3, Na2MoO4, and Na2SeO3 as direct final-medium ingredients. | `data/normalized_yaml/bacterial/atyp_medium.yaml`. |
| Major | The current MediaDive/JCM owner only partially repaired Vitamin solution VA. | JCM has seven vitamins in the 100 ml stock; only Biotin, Thiamine HCl, p-Aminobenzoic acid, and Pyridoxine HCl are nested, leaving Niacin, Calcium pantothenate, and Vitamin B12 as direct final-medium ingredients. | `data/normalized_yaml/bacterial/atyp_medium.yaml`. |
| Major | Main-solution masses have been divided by a synthetic 1002 ml volume. | JCM gives 1 g/L values for KH2PO4, NH4Cl, NaCl, sodium acetate, and sodium succinate; the MediaDive path stores each as `0.998004 G_PER_L`, consistent with division by 1.002 L. | `data/normalized_yaml/bacterial/atyp_medium.yaml` and `data/normalized_yaml/bacterial/mediadive_4077_Main_sol_J358.yaml`. |
| Major | Nickel chloride hexahydrate is grounded to an anhydrous nickel dichloride term. | The source labels `NiCl2 x 6 H2O`; the target grounds that row to `CHEBI:34887` nickel dichloride. | `data/normalized_yaml/bacterial/atyp_medium.yaml` and the CHEBI enrichment path. |
| Major | The same JCM GRMD 358 medium is split between two normalized owners and two generated merge records. | This target points at `GRMD=358` as `mediadive.medium:J358`; `data/normalized_yaml/bacterial/TOGO_M352_ATYP_Medium.yaml` points at the same JCM URL as TOGO `M352`, and generates `data/merge_yaml/merged/ATYP_MEDIUM.yaml`. | Cross-source duplicate reconciliation between the MediaDive and Togo normalized owners plus merge rules. |

No blockers or minor findings were found beyond those major curation issues.

## Recommended Edits

1. Finish `data/normalized_yaml/bacterial/atyp_medium.yaml` stock nesting:
   move all remaining Trace element solution SLA and Vitamin solution VA
   components under their solution rows with stock-scoped concentrations and
   keep only the 1 ml/L solution additions in the final medium.
2. Preserve the source base masses instead of the MediaDive `1002 mL`
   normalization unless a directly inspected MediaDive source proves those
   divided values are intentional.
3. Re-ground `NiCl2 x 6 H2O` to the exact CHEBI hydrate after verifying the ID
   and label with OAK or another trusted CHEBI lookup.
4. Regenerate the merge layer so the generated `atyp_medium__b9f75144` target
   includes the August 2026 nesting repair and any additional ATYP fixes.
5. Reconcile the MediaDive/JCM and Togo M352 imports for GRMD 358 against the
   live JCM page so the canonical merge does not keep two same-source records.

## Follow-up Checks

- Re-run open-schema LinkML, `scripts/validate_strict.py`, the reference
  validator, and the term validator against the edited normalized MediaDive
  owner and the regenerated `data/merge_yaml/merged/atyp_medium__b9f75144.yaml`.
- Manually compare `atyp_medium.yaml`, `TOGO_M352_ATYP_Medium.yaml`, and the
  regenerated merge against JCM GRMD 358 to confirm every stock and
  final-medium value is scoped correctly.
- Validate `data/normalized_yaml/bacterial/mediadive_4077_Main_sol_J358.yaml`
  if it remains the maintained source for the MediaDive main solution.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  generated merges.

## Additional Notes

The Togo-owned ATYP record has a worse unit slip, because its M352 importer
promoted stock milligram values directly to `G_PER_L`. The MediaDive/JCM owner
has stock concentrations that are closer to the JCM stock recipes, but it still
needs complete stock nesting and cross-source duplicate reconciliation.
