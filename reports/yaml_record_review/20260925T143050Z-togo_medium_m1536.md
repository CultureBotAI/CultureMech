# YAML Record Review: togo_medium_m1536

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1536.yaml
- Started UTC: 2026-09-25T14:28:00Z
- Finished UTC: 2026-09-25T14:30:50Z
- Verdict: pass with minor issues

## Target

- Class: MediaRecipe
- ID: CultureMech:008083
- Name: togo_medium_m1536
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1536.yaml`.
- Source identity in the generated record: TOGO:M1536, original source NBRC_M326, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=326`.
- Merge lineage in the generated record: only `togo_medium_m1536`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1536.yaml`; it exited 0 with no diagnostics.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1536.yaml --out /private/tmp/m1536.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1536.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it exited 0 after the startup line.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1536.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1536 resolves to NBRC_M326. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- The grounded salts match the source labels. Peptone, Yeast extract, and optional agar are intentionally not forced to narrow chemical terms.

## Evidence

- TOGO M1536 and NBRC 326 support 10 g peptone, 2 g yeast extract, 1 g MgSO4 x 7 H2O, 30 g NaCl, 15 g agar if needed, and 1 L distilled water.
- NBRC 326 lists pH 7.0. The generated record has no pH field.
- The source scopes agar as "if needed"; the generated `physical_state: SOLID_AGAR` is acceptable only for the agar branch and does not expose the no-agar branch.

## Completeness

- Consequential gap: pH 7.0 is present in NBRC and TOGO but absent from the YAML.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1536` found no existing report for this record stem.

## Findings

- minor: `data/normalized_yaml/bacterial/togo_medium_m1536.yaml` omits the NBRC_M326 pH 7.0 value.
- minor: `Agar (if needed)` is represented as a solid physical state rather than as an optional branch.

## Recommended Edits

- Add `ph_value: 7.0` to `data/normalized_yaml/bacterial/togo_medium_m1536.yaml`.
- Preserve the optional agar scope either in preparation text or by splitting a solid variant if the corpus pattern supports it.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1536.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1536.yaml` and verify that pH 7.0 appears.

## Additional Notes

- Exact source inspection was bounded to TOGO M1536 and NBRC 326.
