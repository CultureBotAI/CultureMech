# YAML Record Review: 804

- Repository: CultureMech
- Record: data/merge_yaml/merged/804.yaml
- Started UTC: 2026-09-21T07:40:24Z
- Finished UTC: 2026-09-21T07:41:16Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/804.yaml`.
- Stable identifier: `CultureMech:007494`.
- Source identity asserted by the record: NBRC medium 803, imported as `NBRC_804.yaml`.
- The generated record was merged from one owner, `NBRC_804.yaml`, on fingerprint `875c7f11cdb6d5b4d255fecb66ff731ad05a39104c872be2b4267ec087651c70`.
- Current authoritative owner: `data/normalized_yaml/bacterial/NBRC_804.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/804.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/804.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/804.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/804.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The intended NBRC identity is recoverable from curation history, but the generated record is stale relative to `data/normalized_yaml/bacterial/NBRC_804.yaml`.
- The current normalized owner now identifies `nbrc.medium:803`, carries a structured NBRC medium 803 reference, and stores the repaired pH range of 6.5 to 6.8.
- A gitignore-independent exact search for `NO=803` and `Media No. 803` found this NBRC owner and a separate Togo M1604 import that points at the same NBRC medium 803 URL.
- A gitignore-independent exact search with a digit boundary for `TOGO:M1604` found only the Togo normalized owner and its generated merge, `data/merge_yaml/merged/togo_medium_m1604.yaml`.

## Evidence

- The current NBRC medium 803 page lists 5 g Hipolypepton, 5 g yeast extract, 5 g glucose, 5 g lactose, 0.5 g Tween 80, 1 g `MgSO4.7H2O`, 1 L distilled water, 15 g agar if needed, pH 6.5 to 6.8, and the Hipolypepton Wako supplier footnote.
- The normalized NBRC owner already preserves those eight source rows and the pH range.
- The generated target instead has one concatenated source-table blob as an ingredient name and one empty-name ingredient whose concentration value is `Hipolypepton*`.

## Completeness

- The generated target is missing all eight structured NBRC ingredient rows that exist in the repaired normalized owner.
- The generated target has no `media_term`, no `ph_range`, no NBRC reference entry, and no useful ontology grounding.
- NBRC medium 803 is also represented by Togo M1604 as a separate generated record, but that Togo record omits the source magnesium sulfate row and moves Hipolypepton to an empty solution.

## Findings

- BLOCKER: `data/merge_yaml/merged/804.yaml` is stale and unusable: it still contains the pre-recovery concatenated NBRC table rather than the eight repaired ingredients in `data/normalized_yaml/bacterial/NBRC_804.yaml`.
- BLOCKER: the generated target does not preserve pH 6.5 to 6.8 or the NBRC medium 803 structured source identity now present in the normalized owner.
- BLOCKER: NBRC medium 803 is duplicated by `data/merge_yaml/merged/togo_medium_m1604.yaml`; both generated records point at the same NBRC `NO=803` recipe but remain separate active CultureMech records.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/804.yaml` from `data/normalized_yaml/bacterial/NBRC_804.yaml`; do not edit the generated YAML directly.
- Reconcile `NBRC_804.yaml` and `togo_medium_m1604.yaml` so one canonical NBRC medium 803 record is emitted with both the direct NBRC and Togo M1604 source identities retained.
- During reconciliation, preserve Hipolypepton 5 g/L, yeast extract 5 g/L, glucose 5 g/L, lactose 5 g/L, Tween 80 0.5 g/L, `MgSO4.7H2O` 1 g/L, distilled water 1 L, optional agar 15 g/L, pH 6.5 to 6.8, and the Wako Hipolypepton footnote.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge artifacts.
- Re-run an exact ignored-file-inclusive search for `NO=803`; only one active generated NBRC medium 803 recipe should remain.
- Confirm that the generated page for `804` has eight readable ingredient rows and no concatenated source-table blob.

## Additional Notes

- This is one of the records where a normalized-file repair exists but `data/merge_yaml/merged` has not been regenerated from that repaired source.
