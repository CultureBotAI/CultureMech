# YAML Record Review: Asp2

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/Asp2.yaml
- Started UTC: 2026-09-21T15:44:00Z
- Finished UTC: 2026-09-21T15:46:36Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/Asp2.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:007196` |
| Label | `Asp2` |
| Normalized owner | `data/normalized_yaml/bacterial/asp2.yaml` |
| Source | MediaDB `MEDIADB:295` |
| Generated status | Generated one-input merge of `asp2.yaml`; future fixes belong in the normalized owner and should be propagated by regenerating merges/pages. |

## Validation

| Check | Result |
|---|---|
| Open-schema LinkML validation | Passed: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/Asp2.yaml` exited with status 0 and no validation errors. |
| Strict validation | Passed: the no-project Python 3.11 invocation of `scripts/validate_strict.py data/merge_yaml/merged/Asp2.yaml --out /private/tmp/Asp2.strict.tsv --workers 1 --quiet` scanned 1 file and wrote 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/Asp2.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 snippet checks, and reported all validations passed. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/Asp2.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` reported validation passed after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone records under `history/`, not a focused embedded-`MediaRecipe.curation_history` validator for one generated merge record. |

The documented `just` entry points remain blocked by the project-level Python
3.13 `llvmlite==0.46.0` build failure, so the focused no-project Python 3.11
validator invocations above were used for this one record.

## Identity and Grounding

The top-level identity is coherent. `CultureMech:007196` is registered to
`data/normalized_yaml/bacterial/asp2.yaml`, the generated target has the same
ID, and MediaDB's formulation listing resolves `defined_media/media/295/` to
`Media: Asp2`.

Several ingredient identities still need curation:

| Ingredient | Record state | Review |
|---|---|---|
| Iron(III) chloride | `preferred_term: '''Iron(III` with no `term` | Wrong label: MediaDB Medium 295 lists `Iron(III) chloride`, compound 16413, at the same 0.00493 mM amount. |
| EDTA | `CHEBI:4735`, `ethylenediaminetetraacetic acid` | Weak grounding: MediaDB's tabular export maps EDTA to CHEBI 42191, while this record points at the neutral acid form. Re-check exact source speciation before leaving or changing it. |
| L-Inositol | no `term` | Incomplete grounding: MediaDB supplies `C06151`, PubChem 892, and CHEBI 27374 for the source compound. |

## Evidence

The generated record has no structured `references` or snippet-level
`evidence`, but its root source URL still serves MediaDB pages. The MediaDB
media listing links `Asp2` to `/defined_media/media/295/`, that page exposes 24
compounds and a tab-delimited `/defined_media/media_text/295/` export, and the
compound table exactly matches the YAML's 24 mM amounts.

Supported claims:

- The 24 MediaDB concentrations are supported by the current MediaDB page and
  its tab-delimited export, including sodium chloride 308 mM, magnesium sulfate
  20.3 mM, potassium chloride 8.05 mM, calcium chloride anhydrous 0.901 mM,
  sodium nitrate 0.588 mM, potassium dibasic phosphate 0.0287 mM, tromethamine
  8.25 mM, vitamin B12 1.48e-06 mM, EDTA 0.131 mM, and sodium silicate 1.23
  mM.
- The `MEDIADB:295` source identifier and `Asp2` name match MediaDB's own
  medium listing.
- The `high_metal: true` flag is plausible for this record because MediaDB
  reports 308 mM sodium chloride plus high magnesium and potassium salts.

Unsupported or over-scoped assertions:

| Record assertion | Problem |
|---|---|
| `'''Iron(III` is the MediaDB ingredient name. | MediaDB lists the source compound as `Iron(III) chloride`; the YAML still carries the leading-quote parse fragment. |
| The medium should be dissolved in distilled water, pH-adjusted if specified, and filter-sterilized through 0.22 um. | The inspected MediaDB medium page and tab-delimited export list compounds, amounts, organism, source, and growth-data links, but no pH or sterilization protocol. |
| `ph_value` may remain empty because no pH is specified. | Supported: MediaDB Medium 295 does not expose a pH value. |

## Completeness

- The source MediaDB page lists one organism association, `Cyanothece ATCC
  51142`, one source, `Schneegurt et al, 1994`, and one growth-data record,
  `Cyanothece ATCC 51142 on Asp2`. None of those are represented as structured
  `target_organisms`, `references`, or `growth_metrics` in the YAML.
- `find data/normalized_yaml -name 'asp2.yaml'` found exactly one normalized
  owner, `data/normalized_yaml/bacterial/asp2.yaml`.
- A gitignore-independent exact search for
  `CultureMech:007196|MEDIADB:295|Medium ID: 295|Asp2` under `data`,
  `scripts`, `history`, `src`, `reports`, and `references_cache` found the
  normalized owner, the generated target, generated indexes, and reports; it
  did not find a second current normalized owner for `CultureMech:007196`.
- `find reports/yaml_record_review -maxdepth 1 -name '*Asp2.md'` found no prior
  report for this generated record.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | A MediaDB SQL parser fragment is still present as an ingredient name. | The YAML has `preferred_term: '''Iron(III`; MediaDB Medium 295 lists compound 16413 as `Iron(III) chloride` at 0.00493 mM. The normalized owner already carries a 2026-08-31 `REPAIRED_MEDIADB_TRUNCATED_NAME` event, so the previous guarded repair did not catch this remaining fragment. | `data/normalized_yaml/bacterial/asp2.yaml` |
| Major | Generic preparation steps assert unsupported protocol details. | The three YAML steps say to dissolve everything in distilled water, adjust pH if specified, and filter-sterilize by 0.22 um. MediaDB Medium 295 gives only a compound table, organism, source, and growth data; it does not specify pH or filter sterilization. | `data/normalized_yaml/bacterial/asp2.yaml`, or the MediaDB importer/defaulting rule that added generic defined-medium preparation. |
| Minor | MediaDB source, organism, and growth association are not represented structurally. | MediaDB Medium 295 names `Schneegurt et al, 1994`, `Cyanothece ATCC 51142`, and a `Cyanothece ATCC 51142 on Asp2` growth-data record, while the YAML has no structured references or target organism. | `data/normalized_yaml/bacterial/asp2.yaml` |
| Minor | One imported CHEBI grounding needs exact-speciation review. | The record grounds EDTA to `CHEBI:4735`; MediaDB's tabular export maps this compound to CHEBI 42191. | `data/normalized_yaml/bacterial/asp2.yaml` |
| Minor | One source-supported CHEBI grounding is missing. | `L-Inositol` has no `term` in the YAML even though MediaDB exports KEGG `C06151`, PubChem 892, and CHEBI 27374 for that compound. | `data/normalized_yaml/bacterial/asp2.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/asp2.yaml`, restore the damaged
   `'''Iron(III` label to `Iron(III) chloride` and ground it only after
   checking exact ferric-chloride identity against MediaDB compound 16413.
2. Remove or replace the generic preparation steps unless a primary source for
   Schneegurt et al. 1994 supplies pH, vessel, water, and filter-sterilization
   instructions for Asp2.
3. Add a structured source reference for Schneegurt et al. 1994 after resolving
   the full citation from MediaDB source 108.
4. Add `Cyanothece ATCC 51142` as a target organism only if MediaDB's organism
   and growth-data records give enough strain and outcome detail for a narrow
   CultureMech growth claim.
5. Re-check EDTA and L-Inositol against OAK or the packaged MIM label index
   before editing either CHEBI mapping.

## Follow-up Checks

- Re-run focused schema, strict, term, and reference validation on
  `data/normalized_yaml/bacterial/asp2.yaml`.
- Re-run `just verify-merges` and `just audit-merge-freshness` after
  regenerating `data/merge_yaml/merged/Asp2.yaml`.
- Re-run the MediaDB parser-damage detector against `asp2.yaml` and the
  gitignored `media_database.07Oct2015.sql` source to prove no leading-quote
  fragments remain.
- Re-fetch `https://mediadb.systemsbiology.net/defined_media/media_text/295/`
  and compare the YAML's 24 top-level ingredients and mM amounts to the
  tab-delimited source rows.

## Additional Notes

- MediaDB's public downloads page exposes
  `media_database.07Oct2015.sql.gz`, the SQL dump named in the local MediaDB
  name-repair tooling. The focused review did not need to parse it because the
  live tab-delimited Medium 295 export exposed every compound name and amount.
