# YAML Record Review: CHM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/CHM.yaml
- Started UTC: 2026-09-22T07:21:47Z
- Finished UTC: 2026-09-22T07:23:01Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000060 |
| Label | CHM |
| Generated record | data/merge_yaml/merged/CHM.yaml |
| Maintained owner | data/normalized_yaml/algae/chm.yaml |
| Source | CCAP CHM, `MR_CHM.pdf` |

The reviewed file is a generated one-source merge of the CCAP algae `chm`
normalized record. Future fixes belong in
`data/normalized_yaml/algae/chm.yaml`, the CCAP import/repair scripts, or the
merge step that carries CCAP source provenance into generated YAML rather than
in `data/merge_yaml/merged/CHM.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/CHM.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; 0 checks |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected
`eutils/pkg_resources is deprecated` warning before passing.

## Identity and Grounding

The primary source identity is coherent: `CultureMech:000060`, original name
`CHM`, `CCAP:CHM`, and `MR_CHM.pdf` all denote CCAP CHM, or Chilomonas Medium.

The generated merge lost the structured `sources` slot present in the
normalized owner, although the CCAP identity remains recoverable from
`references`, `notes`, and `curation_history`.

The record's auxiliary `kg_microbe_match: mediadive.medium:C78` is not verified
by the CCAP PDF, also appears on unrelated local CCAP records `EG` and `PM`,
and could not be resolved through the checked `media/C78` and `media_text/C78`
MediaDB routes. Treat it as untrusted until the C78 mapping can be traced.

An ignored-independent `rg --no-ignore --hidden` search across
`data/normalized_yaml` and `data/merge_yaml/merged` for the CultureMech ID,
`CCAP:CHM`, the `MR_CHM.pdf` source, the C78 KG-Microbe match, and the merge
fingerprint found this generated record, its CCAP algae owner, a separate
bacterial `chm.yaml` import from the same CCAP PDF, `EG` and `PM` records with
the same C78 match, and the expected normalized index entries.

## Evidence

The CCAP `MR_CHM.pdf` source supports the two populated solutes: 1.0 g/L sodium
acetate trihydrate and 1.0 g/L Lab-Lemco powder.

The same source also lists the solvent and preparation instruction that are
missing from the generated record: add the constituents to 1 litre of
deionised water and autoclave at 15 psi for 15 minutes. A separate maintained
`data/normalized_yaml/bacterial/chm.yaml` import from the same PDF has these
water and autoclave assertions, so the omission is specific to the CCAP algae
input path.

Several generic CCAP import assertions overrun the inspected PDF. The source
identifies CHM as Chilomonas Medium for Freshwater Protozoa; it does not say
the medium is for freshwater algae, phytoplankton culture, or microalgae
research, and it does not provide the record's species-dependent light
intensity, light cycle, or 15-30 C temperature range.

## Completeness

The empty `target_organisms` and growth-observation fields are not defects for
this CCAP source record.

The recipe is incomplete until deionised water and the autoclave step are
represented. The provenance is less complete in the generated record than in
the maintained owner because merge output drops the structured `sources` list.

## Findings

| Severity | Finding |
| --- | --- |
| Major | The CCAP CHM solvent and autoclave instruction are absent even though the source lists 1 litre of deionised water and autoclaving at 15 psi for 15 minutes. **Owner:** `data/normalized_yaml/algae/chm.yaml` and the CCAP repair script that expands simple recipes. |
| Major | The description, applications, light fields, and temperature range are generic algae assertions not supported by `MR_CHM.pdf`, which identifies CHM as a Freshwater Protozoa medium. **Owner:** `data/normalized_yaml/algae/chm.yaml` and the CCAP import defaults. |
| Minor | Generated `CHM.yaml` drops the structured `sources` entry that exists in the normalized owner. **Owner:** `merge_recipes.py` or the merge configuration for generated source provenance. |
| Minor | `kg_microbe_match: mediadive.medium:C78` is unresolved and appears on multiple unrelated CCAP records. **Owner:** the CCAP-to-KG-Microbe matching overlay. |

## Recommended Edits

1. Add deionised water at 1 litre per litre and the 15 psi, 15 minute autoclave
   instruction to `data/normalized_yaml/algae/chm.yaml`.
2. Replace the generic algae description, applications, light intensity, light
   cycle, and temperature range with source-supported CHM/Chilomonas wording,
   or remove fields not stated by CCAP.
3. Preserve the normalized `sources` entry when this record is merged into
   `data/merge_yaml/merged/CHM.yaml`.
4. Trace `mediadive.medium:C78`; keep the `kg_microbe_match` only if it can be
   tied specifically to CCAP CHM rather than copied from other CCAP records.
5. Regenerate merged YAML after the maintained algae owner and any merge logic
   are corrected.

## Follow-up Checks

Run the narrow generated-record validators after regeneration:

1. Open LinkML validation against `MediaRecipe`.
2. `scripts/validate_strict.py` on the regenerated record.
3. `linkml-reference-validator validate data` on the regenerated record.
4. `linkml-term-validator validate-data` on the regenerated record.

Then manually compare the regenerated YAML against `MR_CHM.pdf` and confirm
that the two solutes, deionised water, autoclave instruction, source identity,
and any retained KG-Microbe match are all source-backed.

## Additional Notes

`data/normalized_yaml/bacterial/chm.yaml` is a separate import from the same
CCAP PDF and should be reconciled with the algae owner after both generated
records have been reviewed.
