# YAML Record Review: bhiy_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bhiy_agar__c89652dd.yaml
- Started UTC: 2026-09-21T21:35:21Z
- Finished UTC: 2026-09-21T21:36:13Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/bhiy_agar__c89652dd.yaml` |
| ID | `CultureMech:002500` |
| Label | `bhiy_agar` / `BHIY AGAR` |
| Category | `bacterial` |
| Maintained owner | `data/normalized_yaml/bacterial/bhiy_agar.yaml` |
| Source | JCM Medium J141 |
| Merge fingerprint | `c89652dd9bd41afe7da45bacda3f395a8ac359f9f431599f96ed6a0dff3aab82` |
| Merge inputs | `bhiy_agar` |

The reviewed file is a derived merge record generated from the direct JCM J141
normalized record.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bhiy_agar__c89652dd.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bhiy_agar__c89652dd.yaml --out /private/tmp/bhiy_agar__c89652dd.strict.tsv --workers 1 --quiet` | Passed with 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bhiy_agar__c89652dd.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bhiy_agar__c89652dd.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | `just validate-history` | Not checked: the documented focused history validator targets standalone files under `history/`; no documented record-local embedded `curation_history` validator exists for one merge record |

Direct `just` validation remains blocked by the project environment's Python
3.13 build of `llvmlite==0.46.0` before those recipes reach this file; the
no-project Python 3.11 commands above exercised the same focused validators.

## Identity and Grounding

- `CultureMech:002500`, `media_term.id: mediadive.medium:J141`, and the source
  notes agree on JCM Medium J141 / BHIY AGAR.
- The inspected JCM page for `GRMD=141` has title `141 BHIY AGAR` and the
  expected four source ingredients.
- The hidden-inclusive exact search for
  `CultureMech:002500|c89652dd9bd41afe7da45bacda3f395a8ac359f9f431599f96ed6a0dff3aab82|JCM Medium J141|BHIY AGAR`
  covered normalized bacterial YAML, merged YAML, report files, and caches; it
  found `data/normalized_yaml/bacterial/bhiy_agar.yaml` as the normalized owner
  and this generated merge for `CultureMech:002500`.
- `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` /
  `CultureMech:007865` is a separate TOGO M132 import of the same JCM 141
  formulation and generated as `data/merge_yaml/merged/BHIY_AGAR.yaml`.

## Evidence

The inspected JCM source lists:

| Source ingredient | Source amount |
|---|---:|
| Brain heart infusion (BD-Difco) | 37.0 g |
| Yeast extract (BD-Difco) | 20.0 g |
| Agar | 15.0 g |
| Distilled water | 1.0 L |

It then instructs `Adjust pH to 7.4.`

The generated record preserves the source pH and pH-adjustment comment, and it
preserves 15 g/L agar and 20 g/L yeast extract.

## Completeness

- The source 1 L Distilled water component is absent.
- The source 37 g Brain heart infusion component is absent as a direct
  commercial-product row.
- No target-organism or growth-metric claim is expected for this JCM recipe.
- No stock solutions are expected; all source components are direct additions to
  distilled water.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The source's 37 g Brain Heart Infusion product was decomposed into unsupported inferred constituents. | JCM 141 lists `Brain heart infusion (BD-Difco)`, 37.0 g; the generated record instead contains variable Calf brains, variable Beef heart, 10 g/L Proteose peptone, 2 g/L Dextrose, 5 g/L Sodium chloride, and 2.5 g/L Disodium phosphate rows derived from a product reconstruction. | `data/normalized_yaml/bacterial/bhiy_agar.yaml` and the commercial-product decomposition rule |
| Major | The 1 L distilled-water component is missing. | JCM 141 lists `Distilled water`, 1.0 L; this record has no Distilled water ingredient. | `data/normalized_yaml/bacterial/bhiy_agar.yaml` or the MediaDive/JCM importer |
| Major | Two active normalized records denote the same JCM 141 BHIY AGAR formulation but do not merge. | `bhiy_agar.yaml` is a direct JCM J141 import, while `TOGO_M132_BHIY_Agar.yaml` points at Togo M132's original `JCM_M141`; their generated products have different fingerprints because both normalized records preserve different subsets of the same source recipe. | Both normalized BHIY AGAR records plus merge canonicalization after the source-level fixes |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/bhiy_agar.yaml`, keep the source formula
   as 37 g Brain Heart Infusion (BD-Difco), 20 g Yeast extract (BD-Difco),
   15 g Agar, and 1 L Distilled water; do not replace the commercial BHI
   component with calf-brain/beef-heart/product constituents unless those rows
   are modeled as a nested product composition rather than final-medium
   additions.
2. Align this direct JCM J141 normalized record with the TOGO M132 copy so they
   can merge or be intentionally de-duplicated under one canonical BHIY Agar
   record.

## Follow-up Checks

- Rerun focused schema, strict, term, and reference validation on the corrected
  direct JCM normalized owner.
- Re-fetch or inspect the JCM `GRMD=141` page after the edit and compare the
  four source rows, pH, and pH adjustment field by field.
- Regenerate merge products, then run `just verify-merges` and
  `just audit-merge-freshness` to confirm the generated
  `bhiy_agar__c89652dd` record is fresh and the TOGO duplicate is either merged
  or explicitly retained.

## Additional Notes

- Unlike `BHIY_AGAR.yaml`, this direct JCM copy already preserved
  `ph_value: 7.4` and `Adjust pH to 7.4.`
