# YAML Record Review: MODIFIED M1 AGAR

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/modified_m1_agar__c07d47dd.yaml`
- Started UTC: 2026-09-24T11:29:34Z
- Finished UTC: 2026-09-24T11:30:35Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003143` |
| Name | `modified_m1_agar` |
| Original name | MODIFIED M1 AGAR |
| Category | `bacterial` |
| Generated path | `data/merge_yaml/merged/modified_m1_agar__c07d47dd.yaml` |
| Maintained source | `data/normalized_yaml/bacterial/modified_m1_agar.yaml` |
| Merge status | Generated by `merge_recipes.py`; `merged_from` contains only `modified_m1_agar` |

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_m1_agar__c07d47dd.yaml`; output was `No issues found`. |
| Strict schema | Passed with `scripts/validate_strict.py`; `/private/tmp/modified_m1_agar__c07d47dd.strict.tsv` contained only the header row. |
| Reference integrity | Passed with `linkml-reference-validator validate data data/merge_yaml/merged/modified_m1_agar__c07d47dd.yaml`; 1 file was validated, 0 checks were emitted, and all validations passed. |
| Term validation | Passed with `linkml-term-validator validate-data` and `conf/oak_config.yaml`. |
| Embedded history | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` arrays embedded in merged YAML. |

## Identity and Grounding

The generated record identifies MediaDive J799, MODIFIED M1 AGAR, and links to
the direct JCM 799 page. MediaDive J799, JCM 799, and TOGO M834 all support the
same Modified M1 Agar identity.

The generated starch, NaCl, and agar ChEBI terms are chemically aligned, but
the record loses MediaDive's `soluble` starch attribute and JCM's `Soluble
starch` supplied form. Yeast extract, Bacto peptone, and seawater are
appropriately ungrounded opaque ingredients.

The `kg_microbe_match` identifier is wrong: `mediadive.medium:687` resolves to
DSMZ COLUMBIA BLOOD AGAR, not Modified M1 Agar.

## Evidence

JCM 799 and MediaDive J799 support the generated pH 7.2 and these amounts:

| Component | Source amount | Generated amount |
|---|---:|---:|
| Soluble starch | 10 g/L | 10 `G_PER_L` as `Starch` |
| Yeast extract (BD-Difco) | 4 g/L | 4 `G_PER_L` |
| Bacto peptone (BD-Difco) | 2 g/L | 2 `G_PER_L` |
| NaCl | 20 g/L | 20 `G_PER_L` |
| Agar | 15 g/L | 15 `G_PER_L` |
| Seawater | 1 L | 1000 `G_PER_L` |

The direct JCM source says artificial seawater can be substituted for seawater,
and the generated preparation step preserves that comment.

An ignored-inclusive normalized YAML search found
`data/normalized_yaml/bacterial/TOGO_M834_Modified_M1_Agar.yaml`, another JCM
M799/TOGO representation of Modified M1 Agar. It has a different stable
CultureMech ID, `CultureMech:010248`.

## Completeness

The generated record is mostly complete for the JCM 799 formula, pH, and
artificial-seawater note. It is dimensionally wrong until the 1 L seawater row
is restored as a volume rather than a mass concentration.

Optional `target_organisms` and `discussion` are empty. Those omissions are not
defects because JCM 799 and MediaDive J799 provide a recipe, not
strain-specific growth curation for a target organism.

## Findings

### Blocker

None found.

### Major

1. `data/normalized_yaml/bacterial/modified_m1_agar.yaml` imports the 1 L
   seawater component as 1000 `G_PER_L`. The future fix should keep seawater as
   a 1 L or 1000 ml volume, matching MediaDive J799 and JCM 799.

2. `data/normalized_yaml/bacterial/modified_m1_agar.yaml` maps
   `kg_microbe_match` to `mediadive.medium:687`, but MediaDive 687 is DSMZ
   COLUMBIA BLOOD AGAR. Remove that cross-match or replace it with an
   inspected equivalent for JCM 799.

3. The repository contains both
   `data/normalized_yaml/bacterial/modified_m1_agar.yaml` and
   `data/normalized_yaml/bacterial/TOGO_M834_Modified_M1_Agar.yaml` for the
   same JCM 799 recipe. They have different CultureMech IDs and should be
   reconciled before the M1 Agar recipe is regenerated.

### Minor

1. `data/normalized_yaml/bacterial/modified_m1_agar.yaml` stores MediaDive's
   `Starch` compound without its `soluble` attribute, whereas JCM 799 lists
   `Soluble starch`.

## Recommended Edits

1. Correct the seawater row in
   `data/normalized_yaml/bacterial/modified_m1_agar.yaml` to use a supported
   volume unit for 1 L seawater.

2. Remove or repair the `kg_microbe_match: mediadive.medium:687` mismatch.

3. Preserve the `soluble` starch supplied form on the starch ingredient.

4. Reconcile MediaDive J799 with
   `data/normalized_yaml/bacterial/TOGO_M834_Modified_M1_Agar.yaml` so JCM 799
   is not published under two unrelated CultureMech IDs.

5. Regenerate merged recipes and downstream media pages/browser artifacts from
   the repaired normalized records.

## Follow-up Checks

- Run the focused open schema, strict schema, term, and reference validators on
  the regenerated `data/merge_yaml/merged/modified_m1_agar__c07d47dd.yaml`.
- Re-fetch MediaDive J799, JCM 799, and TOGO M834 and verify the regenerated
  record preserves 1 L seawater, pH 7.2, 10 g/L soluble starch, and the
  artificial-seawater note.
- Resolve the replacement for `kg_microbe_match`, if any, and verify that it
  does not point to DSMZ 687.

## Additional Notes

- None found.
