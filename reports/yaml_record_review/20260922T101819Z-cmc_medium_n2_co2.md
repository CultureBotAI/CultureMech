# YAML Record Review: CMC Medium (N2/CO2)

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cmc_medium_n2_co2.yaml`
- Started UTC: `2026-09-22T10:13:25Z`
- Finished UTC: `2026-09-22T10:18:19Z`
- Verdict: needs curation

## Target

Generated bacterial `MediaRecipe` record `CultureMech:009268` for TOGO Medium `M2717`, `CMC Medium (N2/CO2)`, generated from `data/normalized_yaml/bacterial/TOGO_M2717_CMC_Medium_N2_CO2.yaml` on merge fingerprint `a5cfe0a50c606f7e0c689740836a05220840857a5c1bdafcf6589d80d53dea32`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/cmc_medium_n2_co2.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The source identity is correct: TOGO `M2717` is named `CMC Medium (N2/CO2)` and points back to the DSMZ 110a `CMC MEDIUM (N2/CO2)` PDF.

The simple salt and carbohydrate groundings match the source closely enough. K2HPO4, D-Glucose, Na2CO3, Cellobiose, and cysteine hydrochloride hydrate use appropriate CHEBI terms. The TOGO-side `Maltose` row is grounded to `alpha-maltose`; the direct DSMZ branch keeps the source label `D-Maltose` but is grounded to generic maltose. `Starch (soluble)`, Casitone, meat filtrate, 1 N NaOH, and ground beef are intentionally unresolved or undefined materials in this generated branch.

An ignored-inclusive search of `data/merge_yaml/merged` found exactly one sibling generated branch, `data/merge_yaml/merged/cmc_medium_n2_co2__083142b0.yaml`. Ignored files were included.

## Evidence

The TOGO API and the DSMZ PDF support the top-level medium identity and the base amounts: 30 g Casitone, 5 g yeast extract, 5 g K2HPO4, 0.5 g `L-Cysteine HCl x H2O`, 1.5 g Na2CO3, and 1000 ml meat filtrate with a 10 ml sugar mix containing 4 g D-Glucose, 1 g Cellobiose, 1 g D-Maltose, and 1 g soluble starch.

The generated record preserves those carbohydrate amounts much better than the direct MediaDive branch, but it then inlines preparation internals from the `Meat filtrate` subcomponent as final medium rows. DSMZ lists ground beef, 1 N NaOH, and distilled water only as inputs used to prepare a 1000 ml meat filtrate.

DSMZ lists `Sodium resazurin (0.1% w/v)` as a 0.50 ml liquid addition. The generated `solutions` entry stores `Na-resazurin solution (0.1% w/v)` with `0.5 G_PER_L`, an empty composition, and placeholder name `Unknown solution`, so the record loses the source volume and stock boundary.

## Completeness

The generated record omits the structured `ph_value: 7.0` supplied by both TOGO and DSMZ.

The generated record has no `preparation_steps`. DSMZ's required anaerobic handling, 80% N2 / 20% CO2 cooling and dispensing atmosphere, Hungate tube instruction, 121 C for 20 min autoclave, post-autoclave sugar and carbonate additions, and meat-filtrate preparation are present only in the source text.

No target organism or growth evidence is present. The source recipe alone supports microbial cultivation as an application but not any particular growth assertion.

## Findings

- Severe: `Meat filtrate` is represented as `1000 G_PER_L`, and its internal 1000 ml distilled water, 25 ml 1 N NaOH, and 500 g ground beef recipe rows are also represented as final top-level ingredients. DSMZ uses those rows to make 1000 ml meat filtrate; they are not additive final-medium g/L concentrations.
- Severe: the 0.5 ml 0.1% sodium resazurin solution is migrated to a `solutions` entry with `0.5 G_PER_L`, empty `composition`, and `Unknown solution` name.
- Major: `ph_value` is missing even though the source pH is 7.0.
- Major: all preparation instructions are missing from the TOGO branch.
- Major: this TOGO branch and the direct DSMZ 110a branch remain unmerged even though both represent the same DSMZ 110a formulation and should reconcile after their stock/solution handling is fixed.

## Recommended Edits

- In `data/normalized_yaml/bacterial/TOGO_M2717_CMC_Medium_N2_CO2.yaml`, replace the flattened meat-filtrate subcomponent rows with a structured meat-filtrate solution or preparation boundary that does not turn NaOH, water, and beef into final g/L medium ingredients.
- Represent `Na-resazurin solution (0.1% w/v)` as a 0.5 ml solution addition, or as a correctly calculated final sodium resazurin concentration with the stock concentration retained as source context.
- Preserve `ph_value: 7.0` and port the DSMZ preparation text into ordered `preparation_steps`.
- Regenerate `data/merge_yaml/merged/` and merge or deliberately link this TOGO branch with `data/normalized_yaml/bacterial/cmc_medium_n2_co2.yaml` after the TOGO and direct DSMZ stock handling is corrected.

## Follow-up Checks

- Validate the regenerated record with open schema, strict, reference, and term validators.
- Confirm that no final `Meat filtrate` row remains with `1000 G_PER_L`.
- Confirm that ground beef, 1 N NaOH, and the meat-filtrate water are nested under the meat-filtrate preparation rather than presented as final ingredients.
- Confirm that the sodium resazurin row preserves the 0.5 ml 0.1% stock addition or a mathematically equivalent final concentration.
- Confirm `cmc_medium_n2_co2.yaml` and `cmc_medium_n2_co2__083142b0.yaml` merge once both inputs encode DSMZ 110a equivalently.

## Additional Notes

The ignored-inclusive duplicate searches covered generated merge records and the full repository for `cmc_medium_n2_co2`, `M2717`, and `110a`; the full-repository search included ignored files. Empty optional fields are not defects.
