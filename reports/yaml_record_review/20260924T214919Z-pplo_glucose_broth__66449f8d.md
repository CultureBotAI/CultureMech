# YAML Record Review: pplo_glucose_broth__66449f8d

- Repository: CultureMech
- Record: data/merge_yaml/merged/pplo_glucose_broth__66449f8d.yaml
- Started UTC: 2026-09-24T21:49:19Z
- Finished UTC: 2026-09-24T21:49:40Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:002522`, the generated bacterial liquid `PPLO-GLUCOSE BROTH` record merged from one direct MediaDive/JCM import, `data/normalized_yaml/bacterial/pplo_glucose_broth.yaml`.  The record is distinct from the repaired `TOGO:M154` branch at `data/merge_yaml/merged/pplo_glucose_broth.yaml`.

## Validation

- LinkML validation: Passed; `linkml-validate` reported no issues.
- Strict validation: Passed; `scripts/validate_strict.py` scanned one file and reported zero `ERROR` rows.
- Reference validation: Passed; `linkml-reference-validator` validated one file and reported zero reference checks.
- Term validation: Passed; `linkml-term-validator` exited 0.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/` files, not `MediaRecipe.curation_history` blocks in generated YAML.

## Identity and Grounding

The source identity is plausible but stale. MediaDive REST data for `J163` still carries the former JCM `PPLO-GLUCOSE BROTH` recipe at pH 7.6, while the current JCM `GRMD=163` page returns no recipe. The exact ignored YAML search for `TOGO:M154`, `JCM_M163`, `GRMD=163`, `TOGO_M154_PPLO-Glucose_Broth`, and `pplo_glucose_broth` found this direct MediaDive branch and the TOGO `M154` branch, so this generated record is a duplicate of the repaired `pplo_glucose_broth` output that still needs the direct normalized JCM input repaired or merged intentionally.

## Evidence

MediaDive `J163` reports a main solution with 700 ml distilled water, 21 g PPLO broth, 5 g glucose, 5 ml 0.4% phenol red, 200 ml sterile horse serum, 100 ml 25% fresh baker's yeast extract solution from Gibco 360-8180, 1,000,000 U penicillin, and pH 7.6. The generated YAML preserves the 21 g PPLO broth and 5 g glucose after scaling by MediaDive's 1005 ml total recipe volume, but it drops distilled water and imports the 5 ml phenol red solution, 200 ml horse serum, 100 ml yeast extract solution, and 1,000,000 U penicillin as `G_PER_L` mass concentrations.

The repaired `TOGO:M154` normalized input already models the 700 ml water, 200 ml serum, 5 ml 0.4% phenol red solution, and 100 ml 25% yeast extract solution with `ML_PER_L`, retains penicillin as `VARIABLE` because the schema has no activity-unit slot, carries references to TOGO, JCM, and MediaDive, and notes that the current JCM URL no longer exposes the recipe.

## Completeness

The record carries the source pH and pH adjustment step, but it is incomplete as a liter recipe because the 700 ml water row is absent. It also lacks source-specific ingredient notes, references, and solution boundaries for phenol red and yeast extract; those boundaries are needed to avoid treating stock solution aliquots as pure dry ingredients.

## Findings

- The direct import converts milliliter and activity-unit additives into impossible `G_PER_L` solids: 5 ml of 0.4% phenol red solution appears as 5 g/L phenol red, 200 ml serum appears as 200 g/L, 100 ml 25% baker's yeast extract solution appears as 100 g/L baker's yeast, and 1,000,000 U penicillin appears as 1,000,000 g/L penicillin.
- Distilled water is omitted even though MediaDive `J163` records the former JCM recipe's 700 ml water addition.
- The generated record remains split from `pplo_glucose_broth.yaml`, whose curated `TOGO:M154` input describes the same former JCM Medium 163 recipe with corrected liquid units.
- The `notes` source URL points to the now-empty current JCM `GRMD=163` page and the record has no surviving source URL to MediaDive `J163`.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/pplo_glucose_broth.yaml` to match the evidence and the already curated `TOGO:M154` representation: add the 700 ml water row, keep horse serum as a 200 ml/L opaque complex addition, move phenol red and baker's yeast extract into `solutions` with 5 ml/L and 100 ml/L additions, and retain penicillin as `VARIABLE` with notes explaining MediaDive's 1,000,000 U evidence.
- Add `references` for `https://mediadive.dsmz.de/rest/medium/J163` and the stale JCM URL, and update the record notes so the source remains auditable after the JCM page removal.
- Regenerate `data/merge_yaml/merged` and verify that the direct MediaDive/JCM input coalesces with the repaired `TOGO:M154` recipe instead of producing `pplo_glucose_broth__66449f8d.yaml`.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validation on the regenerated merged record.
- Search exact source IDs with ignored files included after regeneration to confirm that `TOGO:M154`, MediaDive `J163`, and the old `GRMD=163` reference all ground the same merged output.

## Additional Notes

None.
