# YAML Record Review: togo_medium_m1491

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1491.yaml`
- Started UTC: 2026-09-25T14:02:53Z
- Finished UTC: 2026-09-25T14:02:53Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1491, imported from NBRC medium 274 and merged with Enriched Cytophaga Agar source records.

The generated record merges `data/normalized_yaml/bacterial/TOGO_M1540_Enriched_Cytophaga_Agar.yaml`, `data/normalized_yaml/bacterial/flavobacterium_columnare_agar.yaml`, and `data/normalized_yaml/bacterial/togo_medium_m1491.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The primary medium identity is grounded to `TOGO:M1491`; the TOGO API identifies NBRC_M274 as its original medium ID and links NBRC medium 274.

The TOGO M1491 payload and NBRC medium 274 page agree on 0.5 g Bacto Tryptone, 0.5 g yeast extract, 0.2 g beef extract, 0.2 g sodium acetate, 1 L distilled water, 15 g agar if needed, and pH 7.2.

The exact parent records show that Enriched Cytophaga Agar uses higher Bacto Tryptone and beef extract concentrations than NBRC medium 274, and Flavobacterium columnare Agar additionally uses 10 g agar rather than 15 g agar.

## Evidence

The generated record retains the shared 0.5 g/L yeast extract, 0.2 g/L sodium acetate, and 15 g/L agar rows.

The generated record is stale relative to the repaired normalized sources. The normalized M1491 source has already corrected NBRC 274 to 0.5 g/L Bacto Tryptone, 0.2 g/L Beef extract, pH 7.2, and 1 L distilled water and models M1491 as a concentration variant of Enriched Cytophaga Agar rather than as a source duplicate.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The source has no preparation comments beyond the pH target and optional agar label.

## Findings

- Major issue: the generated recipe gives `Bacto Tryptone (Difco)` as 2 g/L instead of the M1491 source value of 0.5 g/L.
- Major issue: the generated recipe gives `Beef extract` as 0.5 g/L instead of the M1491 source value of 0.2 g/L.
- Major issue: the generated merge collapses M1491 with Enriched Cytophaga Agar sources even though the repaired normalized records differ by tryptone, beef extract, and agar concentration.
- Minor issue: pH 7.2 is absent.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.

## Recommended Edits

- Regenerate the merged YAML from the repaired normalized sources so the generated output models M1491 as a concentration variant rather than a duplicate merge.
- Confirm M1491 keeps 0.5 g/L Bacto Tryptone, 0.2 g/L Beef extract, pH 7.2, 1 L distilled water, and a concentration-variant relationship to Enriched Cytophaga Agar.
- Confirm TOGO M1540 and M1542 remain curated as their own parent and agar-concentration variant records.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge.
- Add a merge regression check that includes all ingredient concentrations when deciding whether Enriched Cytophaga Agar family members are source duplicates or concentration variants.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1491 source for this stem. The generated record explicitly references normalized TOGO M1540 and M1542 sources, and both exact source paths were reviewed as part of this merged-record review. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
