# YAML Record Review: 7H9

- Repository: CultureMech
- Record: data/merge_yaml/merged/7H9.yaml
- Started UTC: 2026-09-21T07:21:31Z
- Finished UTC: 2026-09-21T07:22:52Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/7H9.yaml`.
- Stable identifier: `CultureMech:015481`.
- Source identity asserted by the record: CultureBotHT `7H9`, imported from `7H9.yaml`.
- The generated record was merged from one owner, `7h9.yaml`, on fingerprint `eb24aeed82f41812974748750b56612074cc8dadc2219620dd48d7d55a5293d9`.
- Current authoritative owner: `data/normalized_yaml/specialized/7h9.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/7H9.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/7H9.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/7H9.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/7H9.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The local import identity is internally coherent: `sources[0].database_id` is `7H9`, the curation history names `7H9.yaml`, and the record name is `7H9`.
- A gitignore-independent exact search for `database_id: 7H9` and the `7H9.yaml` import note found only one active normalized owner and its generated merge.
- A gitignore-independent exact search for `name: 7H9` likewise found only this active owner and generated merge.
- The source URL is only the repository root, `https://github.com/CultureBotAI/CultureBotHT`; GitHub now returns 404 for that repository, the guessed raw `7H9.yaml` root URL returns 404, and the repository tree could not be fetched for source-file discovery.

## Evidence

- The record states `Source: FEBA media definitions` and cites CultureBotHT as its database source.
- No primary or archived CultureBotHT `7H9.yaml` content was available from the current source URL.
- A repository-wide `find` for the exact filename `7H9.yaml` found only the generated `data/merge_yaml/merged/7H9.yaml`; no local raw-source copy is checked in under that filename.

## Completeness

- The generated target contains 16 ingredient rows with g/L concentrations and ChEBI terms for all rows except bovine serum albumin.
- The `has_unmapped_ingredients` quality flag is present, matching the unmapped bovine serum albumin row.
- The recipe has no preparation steps, pH, temperature, atmosphere, target organism, or literature reference, but the inaccessible upstream file prevents an evidence-backed decision on whether those details are absent from the source or lost during import.

## Findings

- BLOCKER: source provenance is not reproducible. The only external source URL is a repository root that currently returns 404, and the record does not pin a raw `7H9.yaml` file URL, a commit SHA, or an archived copy of the CultureBotHT source used by `import_from_culturebotht.py`.
- MINOR: bovine serum albumin is unmapped and only identified by CAS in free text; if no single ChEBI term is intended, the row should carry an explicit no-single-ChEBI audit note instead of only the coarse record-level `has_unmapped_ingredients` flag.

## Recommended Edits

- Add durable CultureBotHT provenance for the exact imported `7H9.yaml`, preferably a content-addressed Git commit URL or an archived raw source file.
- Add the original FEBA media definition reference, not only the intermediate CultureBotHT repository URL.
- Resolve the bovine serum albumin grounding or document why it remains intentionally unmapped.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after provenance edits.
- Re-run an exact ignored-file-inclusive search for `database_id: 7H9` and `name: 7H9`; this import should still resolve to one active owner and one generated record.
- Compare every concentration against the recovered CultureBotHT or FEBA source before upgrading the verdict, because the current source link could not be retrieved.

## Additional Notes

- The record is structurally valid and locally unique; the failure is the loss of auditable external evidence for the imported CultureBotHT recipe.
