# YAML Record Review: asw_barley

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asw_barley__95e90355.yaml
- Started UTC: 2026-09-21T16:12:58Z
- Finished UTC: 2026-09-21T16:12:58Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asw_barley__95e90355.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000371` |
| Label | `asw_barley` |
| Category | `bacterial` |
| MediaDive term | `mediadive.medium:C5` / `ASW + barley` |
| Source link | `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW_Barley.pdf` |
| Generated status | Generated on 2026-08-06 from `data/normalized_yaml/bacterial/asw_barley.yaml` |
| Merge fingerprint | `95e9035572ea5dbdfa448d61ea5db03da30beadc38af9b23f0eb5b26e611723a` |

The generated record is a one-input merge and mirrors
`data/normalized_yaml/bacterial/asw_barley.yaml`. Future fixes belong in that
normalized owner or in the MediaDive importer.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asw_barley__95e90355.yaml` | Pass; `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/asw_barley__95e90355.yaml --out /private/tmp/asw_barley__95e90355.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/asw_barley__95e90355.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/asw_barley__95e90355.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass. The only emitted message was the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked | No focused validator for embedded `MediaRecipe.curation_history` is documented for one merged recipe. `just validate-history` targets standalone records under `history/`. |

Direct `just` validation was not used because this checkout currently reaches a
project `uv` build of `llvmlite==0.46.0` under Python 3.13 before target-specific
validation and fails in `setuptools` with `TypeError: Popen.__init__() got an
unexpected keyword argument 'dry_run'`.

## Identity and Grounding

- The ID, name, category, pH range, `media_term`, and source link identify the
  bacterial MediaDive C5 / CCAP `ASW + barley` record.
- MediaDive C5 is live, titles the medium `ASW + barley`, lists CCAP as source,
  and links to `MR_ASW_Barley.pdf`.
- The current CCAP PDF at that URL is a 2026-05-29 `ASW (Artificial Seawater) +
  Barley` revision with the same stock families and final pH range.
- Direct Tricine, sodium nitrate, phosphate, and vitamin chemical groundings are
  plausible, but the nitrate, phosphate, vitamin, and SE1 entries are modeled at
  the wrong recipe layer.
- Natural sea water is entered as `1000` `G_PER_L` even though MediaDive and
  CCAP use it as the filtered aqueous make-up volume.

## Evidence

| Claim in generated record | Review |
|---|---|
| `mediadive.medium:C5` / `ASW + barley` | Supported by the inspected MediaDive page. |
| Tricine at 0.5 g/L | Supported as a direct main-medium ingredient. |
| Natural sea water at 1000 g/L | Unit is unsupported; the source says to make up to 1 litre with filtered natural seawater and MediaDive represents that as 1000 ml, not grams. |
| `NaNO3`, `Na2HPO4`, `K2HPO4`, and vitamins as direct g/L ingredients | The compounds are source-backed as stock ingredients, but their stock concentrations are not final-medium concentrations. |
| `Soil extract` as a direct variable ingredient | Incomplete. The main recipe adds 25 ml/L SE1; Soil Extract 1 is a nested stock solution with its own components and preparation steps. |
| Main preparation says to add approximately one barley grain per 25 ml before autoclaving | Supported by MediaDive C5 and by the current CCAP PDF, which says one barley grain per 25 ml. |
| Alternate sea salt uses `Ultramarine Synthetica` | Supported by MediaDive C5, but the current CCAP revision says `Instant Ocean`. |

## Completeness

- The generated record lacks `solutions` entries for Extra salts, Vitamin
  solution, and SE1.
- The SE1 preparation was flattened into ASW `preparation_steps`, so a nested
  stock-solution protocol is indistinguishable from final-medium preparation.
- No target organisms or growth metrics are required for this source recipe.
  MediaDive C5 strain associations were not inspected as primary growth
  evidence.
- `find reports/yaml_record_review -maxdepth 1 -name '*asw_barley__95e90355.md'`
  searched the ignored report directory independently of `.gitignore` and found
  no prior review report for this exact generated record stem before this report
  was written.
- An ignored-file-inclusive exact search for `CultureMech:000371`,
  `asw_barley__95e90355`, `mediadive.medium:C5`, `CCAP:C5`, `Medium C5`, and
  `MR_ASW_Barley` across `data`, `reports`, `history`, and `.claude` found the
  maintained bacterial owner, this generated record, the adjacent algae
  `ASW_Barley` record, registry/index rows, validation reports, and
  concentration-plausibility reports.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | Stock solution compositions have been flattened into direct final-medium ingredient concentrations. | MediaDive C5 adds Extra salts, Vitamin solution, and SE1 to the main solution at 3.75, 2.5, and 25 ml/L; the YAML lists the internal stock ingredients directly at their stock g/L values. | `data/normalized_yaml/bacterial/asw_barley.yaml` or the MediaDive importer. |
| major | Filtered natural seawater was converted from a 1000 ml make-up volume to 1000 g/L. | MediaDive shows `Natural sea water (filtered)` as `1000.00 ml`; the YAML says `1000` `G_PER_L`. | Same normalized owner/importer. |
| major | The SE1 stock boundary is absent. | MediaDive C5 has SE1 as solution 6117 with its own Soil extract, Distilled water, and preparation steps; the YAML flattens this to one variable Soil extract ingredient and top-level preparation steps. | Same normalized owner/importer. |
| minor | Source-version drift is not captured. | MediaDive C5 was last modified in 2022 and says `Ultramarine Synthetica`; the current 2026 CCAP PDF says `Instant Ocean`. | Add versioned CCAP/MediaDive provenance in `data/normalized_yaml/bacterial/asw_barley.yaml`. |
| minor | Preparation text carries raw line breaks. | Multiple `description` values contain embedded carriage returns from the MediaDive page. | The MediaDive importer or normalized owner. |

## Recommended Edits

1. Rebuild ASW + barley with explicit `solutions` for Extra salts, Vitamin
   solution, and SE1 at their source-stated main-recipe volumes.
2. Represent Natural sea water as a volume or preparation solvent, not as a
   1000 g/L chemical concentration.
3. Move SE1 internal ingredients and preparation into a nested solution
   representation.
4. Preserve both source versions or pick one: MediaDive C5 carries the 2022
   `Ultramarine Synthetica` text, while the 2026 CCAP PDF uses `Instant Ocean`
   and a different-but-equivalent Extra salts stock strength.
5. Normalize MediaDive preparation text to remove raw carriage returns.

## Follow-up Checks

- Rerun open-schema, strict, term, and reference validation on
  `data/normalized_yaml/bacterial/asw_barley.yaml` after adding solution
  descriptors.
- Recalculate final nitrate, phosphate, and vitamin concentrations from the
  stock strengths and addition volumes.
- Regenerate `data/merge_yaml/merged/asw_barley__95e90355.yaml` and verify the
  generated record no longer flattens SE1 or stock ingredients.
- Re-open MediaDive C5 and the current CCAP PDF before closing the source-drift
  decision.

## Additional Notes

- The adjacent algae `ASW_Barley` record has the same CCAP URL but a different
  source lineage and a distinct generated record, reviewed separately at
  `reports/yaml_record_review/20260921T161155Z-asw_barley.md`.
