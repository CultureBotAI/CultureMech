# YAML Record Review: dNMS

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/dNMS.yaml`
- Started UTC: 2026-09-22T13:29:00Z
- Finished UTC: 2026-09-22T13:31:02Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Generated ID | `CultureMech:015515` |
| Name | `dNMS` |
| Description | `Dilution Nitrate Mineral Salts medium` |
| Category | `specialized` |
| Source | CultureBotHT `dNMS`, imported from `dNMS.yaml` |
| Generated from | `dnms` |
| Merge fingerprint | `8a5cceca96135dece22463274b6b97583c45cf5bea77d119c3bd83e4d01fcf73` |

The generated record is a one-source projection of
`data/normalized_yaml/specialized/dnms.yaml`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open-schema LinkML | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/dNMS.yaml` | Passed with `No issues found`. |
| Strict closed-schema validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/dNMS.yaml --out /private/tmp/dNMS.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/dNMS.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 references were checked. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/dNMS.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after a harmless `eutils`/`pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` equivalent for `curation_history` | Not checked: the repository history validator targets standalone `history/*.yaml` records, not embedded generated `MediaRecipe.curation_history` events. |

## Identity and Grounding

- The record consistently identifies the CultureBotHT medium `dNMS`, described
  locally as Dilution Nitrate Mineral Salts medium and sourced from FEBA media
  definitions.
- All 25 ingredients have primary `term` CURIEs, and term validation accepted
  the stored labels.
- The source identity is only partially reproducible. `sources[0].url` points
  to the CultureBotHT repository root, and `curation_history` names
  `dNMS.yaml`, but the record lacks a file path, commit SHA, raw blob URL, or
  archived `dNMS.yaml` capture.
- `find data -iname '*dnms*'` included ignored files and found only
  `data/normalized_yaml/specialized/dnms.yaml` and this generated
  `data/merge_yaml/merged/dNMS.yaml`. No raw CultureBotHT `dNMS.yaml` capture
  is present locally.
- A gitignore-independent `rg --no-ignore --hidden` search for `dNMS`,
  `Dilution Nitrate Mineral Salts`, `FEBA media definitions`, and
  `import_from_culturebotht` across `data/raw`, `data/normalized_yaml/specialized`,
  the generated target, `scripts`, `src`, `reports`, and `history` found the
  maintained and generated `dNMS` records, many sibling CultureBotHT imports,
  prior review reports, and the importer, but no raw upstream `dNMS.yaml`.

## Evidence

- Supported by local provenance only: the 25-component recipe was imported from
  CultureBotHT `dNMS.yaml` by `import_from_culturebotht.py`.
- Not independently verified: the exact KNO3, magnesium sulfate heptahydrate,
  calcium chloride dihydrate, trace-metal, HEPES, phosphate, and vitamin
  concentrations. The CultureBotHT source file used for import was not
  recoverable from local data or from the guessed
  `media_yaml/dNMS.yaml` raw GitHub path.
- The generated record makes no target-organism, growth-metric, pH,
  temperature, atmosphere, or preparation claims.

## Completeness

- Missing reproducible upstream source: the maintained record has only the
  repository root for CultureBotHT. A reader cannot recover the exact imported
  `dNMS.yaml` blob or verify whether later CultureBotHT edits changed this
  formula.
- Stale quality flag: `has_unmapped_ingredients` remains even though every
  ingredient currently has a primary ontology `term`.
- Correctly empty: no target-organism or growth-evidence slots are filled, and
  the available local CultureBotHT provenance does not support adding them.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The CultureBotHT source is not reproducible at file level. | `sources[0].url` is `https://github.com/CultureBotAI/CultureBotHT`; history names `dNMS.yaml`; local gitignore-independent searches found no raw `dNMS.yaml`; the guessed raw GitHub URL for `media_yaml/dNMS.yaml` returned `404: Not Found`. | Add a file-level, commit-pinned CultureBotHT URL or an archived raw source to `data/normalized_yaml/specialized/dnms.yaml`; update `scripts/import_from_culturebotht.py` so future imports preserve that provenance. |
| Minor | `has_unmapped_ingredients` is stale. | All 25 ingredients now have primary ontology `term` entries, and the term validator passed. | Remove or recompute `data_quality_flags` in `data/normalized_yaml/specialized/dnms.yaml`, then regenerate. |

## Recommended Edits

1. Recover the exact CultureBotHT `dNMS.yaml` that produced this record and
   store a commit-pinned blob URL or archived raw capture in the maintained
   normalized record.
2. Recompare every dNMS concentration and chemical form against that recovered
   source.
3. Remove or refresh `has_unmapped_ingredients` if all ingredients remain
   grounded after the source comparison.
4. Regenerate `data/merge_yaml/merged/dNMS.yaml`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on the edited
  normalized source and regenerated `dNMS.yaml`.
- Run `just verify-merges` after regeneration.
- Manually compare the regenerated ingredient list against the recovered
  CultureBotHT/FEBA `dNMS.yaml` file, especially hydration states for trace
  metals and the MG_PER_L vitamin concentrations.

## Additional Notes

- The CultureBotHT repository root loaded as a GitHub HTML page, but it is too
  broad to prove the contents of the imported `dNMS.yaml` source file.
