# YAML Record Review: R - CW medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_cw_medium.yaml
- Started UTC: 2026-09-25T01:02:30Z
- Finished UTC: 2026-09-25T01:03:52Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/r_cw_medium.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:006432` |
| Label | `R - CW medium` |
| Category | `bacterial` |
| Source identity | `komodo.medium:775`, annotated as copied from DSMZ Medium 775 |
| Generated status | Generated merge of `data/normalized_yaml/bacterial/KOMODO_775_R_-_CW_medium.yaml` and `data/normalized_yaml/bacterial/r_cw_medium.yaml`, with `merge_fingerprint: 181a5438b92ce363c2f927e5dcd5ef0388f31689d0a144d980c14af879f8f108`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_cw_medium.yaml` reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/r_cw_medium.yaml --out /private/tmp/r_cw_medium.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed trivially; `linkml-reference-validator validate data data/merge_yaml/merged/r_cw_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 checks, and reported all validations passed. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/r_cw_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- KOMODO 775 is annotated locally as DSMZ Medium 775, and the merged record explicitly marks `KOMODO_775_R_-_CW_medium` as a `SOURCE_DUPLICATE` of the DSMZ/MediaDive owner `data/normalized_yaml/bacterial/r_cw_medium.yaml`.
- Live MediaDive Medium 775 resolves to DSMZ source `R - CW MEDIUM`, pH 5.5, and the same DSMZ Medium 775 PDF link recorded in the direct owner.
- The inspected DSMZ PDF confirms the R-CW Medium identity, pH 5.5 adjustment, 10-component formula, 1.0 ml Tween 80, and 1000.0 ml cheese whey.
- The generated merge has the right DSMZ/KOMODO identity and correctly collapses the two duplicate owners, but both maintained owners still contain the same Trypticase grounding and liquid-unit errors.

## Evidence

- Supported:
  - DSMZ Medium 775 and MediaDive 775 support the label, pH 5.5, liquid state, and all 10 ingredient names.
  - Trypticase, tryptone, yeast extract, KH2PO4, Na-acetate, `(NH4)2 citrate`, MgSO4 x 7 H2O, and MnSO4 x H2O concentrations match the DSMZ formula.
  - The `SOURCE_DUPLICATE` relationship between the KOMODO and DSMZ owners is locally supported: the KOMODO record says it copied DSMZ Medium 775, and the duplicate report names this pair.
- Unsupported or over-scoped:
  - `Trypticase` is grounded to `CHEBI:78018` with label `dodecylphosphocholine`; that CHEBI term is not Trypticase and is unrelated to the DSMZ peptide component.
  - Tween 80 is encoded as `1 G_PER_L`, but DSMZ and MediaDive both specify a milliliter amount.
  - Cheese whey is encoded as `1000 G_PER_L`, but DSMZ and MediaDive both specify a milliliter amount.
  - The pH adjustment and distribution step from the DSMZ/MediaDive recipe is absent from the generated KOMODO-canonical merge, even though the direct DSMZ maintained owner carries it.

## Completeness

- Both maintained duplicate owners, `KOMODO_775_R_-_CW_medium.yaml` and `r_cw_medium.yaml`, have the same Trypticase CHEBI misgrounding and the same Tween 80 and cheese-whey unit errors.
- The generated record records source URLs only in `notes`; it lacks structured references for the KOMODO provenance, the DSMZ/MediaDive source, and the DSMZ Medium 775 PDF.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `komodo.medium:775`, `mediadive.medium:775`, `DSMZ Medium: 775`, `CultureMech:006432`, `CultureMech:001909`, `KOMODO_775_R_-_CW_medium`, and `r_cw_medium` found the KOMODO owner, the DSMZ/MediaDive owner, their generated merged record, source indexes, the DSMZ/KOMODO duplicate review row, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Major | `Trypticase` has a wrong CHEBI grounding. | The generated record and both maintained owners map `Trypticase` to `CHEBI:78018` `dodecylphosphocholine`, which does not denote Trypticase. | Fix both `data/normalized_yaml/bacterial/KOMODO_775_R_-_CW_medium.yaml` and `data/normalized_yaml/bacterial/r_cw_medium.yaml`. |
| Major | Tween 80 has the wrong unit. | DSMZ Medium 775, MediaDive 775, and the DSMZ PDF list 1 ml Tween 80; the generated record and both owners encode it as `1 G_PER_L`. | Fix both duplicate normalized owners, then regenerate. |
| Major | Cheese whey has the wrong unit. | DSMZ Medium 775, MediaDive 775, and the DSMZ PDF list 1000 ml cheese whey; the generated record and both owners encode it as `1000 G_PER_L`. | Fix both duplicate normalized owners, then regenerate. |
| Major | The merged KOMODO-canonical artifact drops the source pH-adjustment step. | The direct DSMZ owner has an `ADJUST_PH` step from MediaDive/DSMZ; the generated merge chooses the KOMODO owner and emits no `preparation_steps`. | Repair the KOMODO owner or merge duplicate preparation steps during generation so the DSMZ pH-adjustment instruction survives. |

## Recommended Edits

1. Remove the erroneous `CHEBI:78018` Trypticase grounding from both maintained owners; leave Trypticase unmapped unless an exact supported term is curated.
2. Change Tween 80 from `1 G_PER_L` to a source-faithful milliliter representation in both duplicate owners.
3. Change Cheese whey from `1000 G_PER_L` to a source-faithful milliliter representation in both duplicate owners.
4. Add structured references for KOMODO 775 and DSMZ/MediaDive 775, and preserve the DSMZ pH-adjustment/distribution step in the regenerated duplicate merge.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/r_cw_medium.yaml`.
- Re-run a gitignore-independent exact search for `komodo.medium:775`, `mediadive.medium:775`, and `CultureMech:006432` to confirm the duplicate relation remains explicit after both owners are repaired.
- Re-fetch MediaDive Medium 775 and the DSMZ Medium 775 PDF to confirm the milliliter source units and pH 5.5 instruction were preserved.

## Additional Notes

- Unlike the adjacent R-CW Agar records, this record is genuinely liquid: DSMZ and MediaDive both name Medium 775 as R-CW Medium and list 1000 ml cheese whey as the carrier.
- The KOMODO and DSMZ normalized owners are already linked as `SOURCE_DUPLICATE`; the needed work is field repair in both maintained records, not a merge-candidate discovery task.
