# YAML Record Review: j_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/j_agar__3476a10f.yaml`
- Started UTC: 2026-09-23T16:21:40Z
- Finished UTC: 2026-09-23T16:22:44Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:008930` |
| Label | `j_agar` |
| Source term | `TOGO:M2342`, `J-Agar` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml` |
| Generated target | `data/merge_yaml/merged/j_agar__3476a10f.yaml` |
| Merge | Single source, `merged_from: [TOGO_M2342_J-Agar]` |

This generated record is the TOGO M2342 wrapper for the DSMZ Medium 262 J -
AGAR recipe. Future fixes belong in the maintained TOGO owner or importer, not
in the generated merge copy.

## Validation

| Check | Result |
|---|---|
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/j_agar__3476a10f.yaml` | Passed with `No issues found`. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/j_agar__3476a10f.yaml --out /private/tmp/j_agar__3476a10f.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/j_agar__3476a10f.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the configured reference pass performed 0 checks. |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/j_agar__3476a10f.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed with the known `pkg_resources` warning from `eutils`. |
| Embedded `curation_history` validator | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not the embedded `MediaRecipe.curation_history` block in generated YAML. |

## Identity and Grounding

TOGO M2342 identifies the same DSMZ Medium 262 J - AGAR recipe as the direct
DSMZ/MediaDive 262 import and the KOMODO 262 wrapper. The target's source ID,
name, category, physical state, and ingredient amounts therefore point to the
right medium.

The wrapper is split from its equivalent generated sibling
`data/merge_yaml/merged/j_agar.yaml`, which already merges the KOMODO and
direct DSMZ owners. An ignored-file-inclusive exact `rg` for `TOGO:M2342`,
`mediadive.medium:262`, and `komodo.medium:262` under `data/normalized_yaml`,
`data/merge_yaml/merged`, and `reports/yaml_record_review` found these three
normalized owners, this TOGO target, the KOMODO/DSMZ target, and generated JSON
index rows.

## Evidence

DSMZ Medium 262, MediaDive 262, and TOGO M2342 support the six ingredient rows:
5 g tryptone, 15 g yeast extract, 2 g glucose, 3 g K2HPO4, 15 g agar, and
1000 ml distilled water. The generated TOGO target preserves all six rows.

The pH and preparation details are missing. DSMZ and MediaDive specify pH
7.3 to 7.5. DSMZ also says to sterilize glucose separately and add it after
sterilization; MediaDive expands that to dissolving glucose in 10 ml distilled
water and adding it aseptically to the basal medium after sterilization. TOGO's
API comment mirrors the pH and preparation sentence, but the normalized and
generated TOGO records lost it.

## Completeness

The TOGO target is a partial view of DSMZ 262: it has the water row that the
direct DSMZ/KOMODO merge lacks, but it lost pH and the glucose handling
instruction. Those missing fields are the likely reason it did not merge with
`j_agar.yaml`.

`target_organisms` and `growth_metrics` can remain empty because the inspected
sources define only the formulation.

The exact `find reports/yaml_record_review -maxdepth 1 -name
'*j_agar__3476a10f*.md'` search included ignored files and found no previous
report for this generated stem before this report was created.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The pH range 7.3 to 7.5 is missing. | DSMZ Medium 262, MediaDive 262, and the TOGO M2342 API record all carry that pH range; the generated target has no `ph_range`. | `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml` or the TOGO importer. |
| Major | The glucose sterilization instruction is missing. | DSMZ says to sterilize glucose separately and add it after sterilization; MediaDive and TOGO preserve equivalent preparation text outside the generated TOGO YAML. | `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml` or the TOGO importer. |
| Major | This TOGO view remains split from the equivalent KOMODO/DSMZ generated record. | The exact source search found `j_agar.yaml` and `j_agar__3476a10f.yaml`, both pointing to DSMZ Medium 262. | The TOGO, DSMZ, and KOMODO J - AGAR normalized owners plus merge reconciliation. |

## Recommended Edits

1. Add pH 7.3 to 7.5 to `data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`.
2. Preserve the DSMZ glucose sterilization and post-sterilization aseptic
   addition instruction in the TOGO owner.
3. Keep the 1000 ml distilled-water row from TOGO M2342, but repair the direct
   DSMZ and KOMODO owners so the shared source signature is complete in all
   three wrappers.
4. Regenerate `data/merge_yaml/merged/` and verify that TOGO M2342, MediaDive
   262, and KOMODO 262 co-merge to one J - AGAR target.

## Follow-up Checks

- Run `just validate-schema data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`.
- Run `just validate-strict data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`.
- Run `just validate-terms data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml`.
- Run `just validate-references data/normalized_yaml/bacterial/TOGO_M2342_J-Agar.yaml` after adding structured TOGO/DSMZ references.
- Regenerate merged YAML and manually compare the single J - AGAR target with
  DSMZ Medium 262, MediaDive 262, and TOGO M2342 for ingredient rows, water,
  pH, and glucose handling.

## Additional Notes

- No blocker findings were found.
- The DSMZ Medium 262 PDF, MediaDive REST `/rest/medium/262`, and the TOGO
  M2342 API were already fetched while reviewing the adjacent generated
  `j_agar.yaml` record.
