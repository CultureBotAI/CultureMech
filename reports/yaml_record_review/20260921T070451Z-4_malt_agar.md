# YAML Record Review: 4_malt_agar

- Repository: CultureMech
- Record: data/merge_yaml/merged/4_malt_agar.yaml
- Started UTC: 2026-09-21T07:04:16Z
- Finished UTC: 2026-09-21T07:04:51Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/4_malt_agar.yaml`.
- Stable identifier: `CultureMech:009038`.
- Source identity asserted by the record: Togo `M245`, `4% Malt Agar`, from original JCM `JCM_M253`.
- The generated record was merged from `2_malt_agar.yaml` and `4_malt_agar.yaml` on fingerprint `85fbdafdbe49448bbd4402d2c7c3127fe5451b96673f0a934a4091f0170f5eaf`.
- Current authoritative owners: `data/normalized_yaml/bacterial/2_malt_agar.yaml` and `data/normalized_yaml/bacterial/4_malt_agar.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/4_malt_agar.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/4_malt_agar.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/4_malt_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/4_malt_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The generated target is identified as Togo `M245`, `4% Malt Agar`.
- Togo `M245` currently resolves to `4% Malt Agar`, original source JCM `JCM_M253`, original URL `GRMD=253`, and pH 7.0.
- The merged parent, Togo `M30`, currently resolves to `2% Malt Agar`, original source JCM `JCM_M38`, original URL `GRMD=38`, and pH 6.5.
- Gitignore-independent exact searches with digit boundaries found one active `TOGO:M245` normalized owner and one active `TOGO:M30` normalized owner. The only generated hit for `TOGO:M30` is the synonym embedded in `4_malt_agar.yaml`.

## Evidence

- Togo `M245` lists 1 L distilled water, 40 g `Malt extract (BD-Difco)`, 20 g agar, and an instruction to adjust pH to 7.0.
- Togo `M30` lists the same 1 L distilled water and 20 g agar, but it has 20 g `Malt extract (BD-Difco)` and an instruction to adjust pH to 6.5.
- The normalized owners preserve the 20 g/L versus 40 g/L malt-extract difference and link the two records with a `CONCENTRATION_VARIANT` relationship.

## Completeness

- The generated target has three ingredient rows, but it contains the 2% malt agar ingredient values while carrying the 4% malt agar ID and title.
- The Togo pH values are absent from both normalized owners and from the generated merge.
- The generated target also uses `G_PER_L` for `Distilled water` even though the source amount is 1 L.

## Findings

- BLOCKER: `data/merge_yaml/merged/4_malt_agar.yaml` is identified as `4% Malt Agar` / `TOGO:M245` but records `Malt extract (BD-Difco)` at `20 G_PER_L`, the 2% Malt Agar value. The M245 source and `data/normalized_yaml/bacterial/4_malt_agar.yaml` both carry the correct 40 g/L value.
- BLOCKER: the 2% and 4% malt agar records are real variants and should not merge as duplicate recipes. The generated merge collapses away the only ingredient concentration that distinguishes the two media.
- MAJOR: the variant metadata is incomplete because it says the records differ only in malt extract concentration, but the Togo sources also differ in pH: 6.5 for M30 versus 7.0 for M245.
- MINOR: the `Distilled water` row should be represented as 1 L or omitted as solvent, not as `1 G_PER_L`.

## Recommended Edits

- Do not edit `data/merge_yaml/merged/4_malt_agar.yaml` directly; it is generated.
- Prevent `data/normalized_yaml/bacterial/2_malt_agar.yaml` and `data/normalized_yaml/bacterial/4_malt_agar.yaml` from merging, while preserving the `CONCENTRATION_VARIANT` relationship.
- Add source pH metadata to both normalized owners and update their variant notes so the pH change is explicit.
- Regenerate the merged YAML and verify that 4% Malt Agar has 40 g/L malt extract while 2% Malt Agar remains at 20 g/L.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation on the regenerated 2% and 4% malt agar outputs.
- Search the generated layer for fingerprint `85fbdafdbe49448bbd4402d2c7c3127fe5451b96673f0a934a4091f0170f5eaf`; after repair, no generated record should merge M30 and M245 on that fingerprint.
- Confirm that Togo `M245` and `M30` each appear in the generated layer as primary media terms rather than one being retained only as a synonym of the other.

## Additional Notes

- Aside from pH and the water unit, the individual normalized owners already preserve the core Togo ingredient tables.
