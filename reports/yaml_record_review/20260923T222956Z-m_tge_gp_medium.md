# YAML Record Review: m_tge_gp_medium

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/m_tge_gp_medium.yaml`
- Started UTC: `2026-09-23T22:29:55Z`
- Finished UTC: `2026-09-23T22:29:55Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:010305` |
| Record name | `m_tge_gp_medium` |
| Original name | `m TGE-GP Medium` |
| Source accession | `TOGO:M887` |
| Source label | `m TGE-GP Medium` |
| Original source | `JCM_M851` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml` |
| Generated record | `data/merge_yaml/merged/m_tge_gp_medium.yaml` |

`data/merge_yaml/merged/m_tge_gp_medium.yaml` is a generated merge from
`data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml` with
fingerprint `a19d24f3dbb560fe24cd07e7af14456a1315569788fb918005db34833df412bb`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_tge_gp_medium.yaml` | Passed; `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_tge_gp_medium.yaml --out /private/tmp/m_tge_gp_medium.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows emitted to `/private/tmp/m_tge_gp_medium.strict.tsv`. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_tge_gp_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_tge_gp_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The record identity is correct at the medium level: `CultureMech:010305`,
  `name: m_tge_gp_medium`, `original_name: m TGE-GP Medium`,
  `media_term.term.id: TOGO:M887`, TOGO's `original_media_id: JCM_M851`, and
  JCM `GRMD=851` all denote m TGE-GP Medium.
- An ignored-file-inclusive exact search for `CultureMech:010305`,
  `TOGO_M887_m_TGE-GP_Medium`, `m_tge_gp_medium`, and `TOGO:M887` across
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`
  found this TOGO M887 input plus a separate MediaDive/JCM `J851` normalized
  record that has already been source-repaired.
- The `glycerol` and `KH2PO4` CHEBI groundings are correct for the two JCM 851
  supplement rows.
- The base-medium reference is not correctly represented. TOGO M887 points to
  its local M828 representation of JCM Medium 795, while JCM 851 points
  directly to JCM Medium 795; the YAML has a 1 `G_PER_L` empty solution named
  `m TGE BROTH MEDIUM (see Medium [M828])`.

## Evidence

- The inspected JCM `GRMD=851` source says to use Medium 795 with 5.0 g/L final
  glycerol and 0.1 mM final `KH2PO4`.
- The inspected TOGO M887 payload encodes the same recipe as three rows:
  5 g/L glycerol, 1 L of `m TGE BROTH MEDIUM (see Medium [M828])`, and
  0.1 mM `KH2PO4`.
- The generated record preserves the 5 `G_PER_L` glycerol row.
- The generated record drops the 0.1 mM phosphate concentration and stores
  `KH2PO4` as `VARIABLE`.
- The generated `solutions` row for the M828/JCM 795 base is empty,
  ungrounded, and dimensionally wrong: it stores the 1 L base-medium reference
  as `1 G_PER_L`.
- The record omits the JCM 795 base composition, namely 18 g/L
  `Bacto m TGE broth (BD-Difco)` and 1 L distilled water. The separate
  `data/normalized_yaml/bacterial/m_tge_gp_medium.yaml` record already shows a
  maintained representation that copies those base components and keeps
  `KH2PO4` at `0.1 MILLIMOLAR`.

## Completeness

- Consequentially incomplete: the JCM 795 base medium is not resolved into this
  TOGO M887 record, so only the glycerol supplement is usable.
- Consequentially incomplete: the final 0.1 mM `KH2PO4` supplement has no
  concentration in the YAML.
- Consequentially incomplete: the recipe lacks source references to JCM 851 and
  JCM 795, and it has no `preparation_steps` preserving the source instruction
  to use Medium 795 with the two final supplements.
- Empty optional target-organism and growth-evidence fields were not treated
  as defects; JCM 851 and TOGO M887 are recipe sources, not primary growth
  evidence for a named target organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The M828/JCM 795 base medium is represented as an empty, wrong-unit solution reference. | JCM 851 says to use Medium 795; TOGO M887 represents that as 1 L of M828. The YAML stores an empty `solutions` entry at `1 G_PER_L`, so consumers lose the 18 g/L broth powder and 1 L water base. | `data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml` or the TOGO reference-medium resolver. |
| Major | The 0.1 mM final `KH2PO4` supplement was defaulted to variable concentration. | JCM 851 and TOGO M887 specify 0.1 mM final `KH2PO4`; the YAML has `value: variable` and `unit: VARIABLE`. | `data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml` or the TOGO importer for `conc_value`/`conc_unit` rows. |
| Major | The TOGO M887 and MediaDive J851 source records are duplicate unreconciled records for the same JCM medium. | Local search found `CultureMech:010305` for TOGO M887 and `CultureMech:003197` for `mediadive.medium:J851`; the latter already carries a repaired JCM 795 base and final `KH2PO4` concentration. | Reconcile the TOGO and MediaDive J851 maintained records during merge or duplicate curation. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml`, resolve
   the 1 L M828 reference to the same JCM 795 base composition used by JCM 851:
   18 g/L `Bacto m TGE broth (BD-Difco)` plus 1 L distilled water.
2. Replace the `KH2PO4` variable concentration with `0.1 MILLIMOLAR`.
3. Add references to JCM `GRMD=851` and `GRMD=795`, and add a preparation step
   preserving the instruction to use Medium 795 with 5 g/L final glycerol and
   0.1 mM final `KH2PO4`.
4. Reconcile `data/normalized_yaml/bacterial/TOGO_M887_m_TGE-GP_Medium.yaml`
   with `data/normalized_yaml/bacterial/m_tge_gp_medium.yaml` so TOGO M887 and
   MediaDive J851 do not continue as divergent duplicate records.
5. Regenerate `data/merge_yaml/merged/` so the generated
   `m_tge_gp_medium.yaml` reflects the corrected maintained source.

## Follow-up Checks

- Run open-schema validation for the corrected normalized record and generated
  merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and generated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference and term validation after adding source references and the
  `MILLIMOLAR` phosphate row.
- Manually compare the regenerated record against JCM `GRMD=851` and
  `GRMD=795`, verifying that the base TGE broth, water, glycerol, and final
  0.1 mM `KH2PO4` supplement are all present and not represented through an
  empty solution placeholder.

## Additional Notes

- The focused validators pass because empty solution references and variable
  concentrations are structurally valid.
