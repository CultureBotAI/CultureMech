# YAML Record Review: bcye_agar__b6eee245

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bcye_agar__b6eee245.yaml
- Started UTC: 2026-09-21T19:23:45Z
- Finished UTC: 2026-09-21T19:25:37Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bcye_agar__b6eee245.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M111_BCYE_Agar.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:007640` |
| Name | `bcye_agar` |
| Original name | `BCYE Agar` |
| Source | Togo Medium `M111`; original JCM Medium `119` |
| Merge status | Stale generated one-source merge from `TOGO_M111_BCYE_Agar` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated Togo M111 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this stale generated merge has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated Togo M111 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bcye_agar__b6eee245.yaml` denotes Togo Medium M111, `BCYE Agar`, whose Togo API record mirrors JCM Medium 119.
- The inspected Togo M111 API lists 37 g BCYE agar from BD-Difco, 100 ml Legionella agar enrichment from BD-Difco, and 900 ml distilled water.
- The inspected JCM 119 HTML page lists the same three amounts and states the default JCM autoclave instruction, 121 C for 15 min.
- The current normalized M111 owner already corrected the two unit errors, added the default autoclave step, and records a `SOURCE_DUPLICATE` parent link to the direct `JCM_J119_BCYE_AGAR` source record.
- The generated merge is stale even though its ingredient-set fingerprint still recomputes: it stores distilled water as `900 G_PER_L`, stores Legionella agar enrichment as `100 G_PER_L`, lacks the autoclave step, lacks Togo and JCM references, and has not yet merged with `JCM_J119_BCYE_AGAR`.
- Exact gitignore-independent searches for `CultureMech:007640`, exact `TOGO:M111`, `JCM_M119`, `TOGO_M111_BCYE_Agar`, exact `mediadive.medium:J119`, and exact `GRMD=119` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, direct JCM normalized owner, generated merge, indexes, import reports, and the September repair script, but no repository-local raw Togo M111 or JCM 119 source dump under `data`.

## Evidence

- Togo M111 supports the source component amounts: 900 ml distilled water, 37 g BCYE agar, and 100 ml Legionella agar enrichment.
- JCM 119 supports the same commercial BCYE recipe and the 121 C for 15 min default autoclaving.
- The generated target preserves the three source component labels and amount magnitudes but assigns `G_PER_L` to both volume rows.
- The current normalized owner represents BCYE agar as 37 g/L, Legionella agar enrichment as 100 ml/L, and distilled water as 900 ml/L, and it links back to the direct JCM 119 owner.
- The current normalized owner and the current direct JCM 119 owner both fingerprint to `b6eee2453f1b88dc4eecd6a97e171c329821ba2dfcaf45051180f83dc46b1d61`; a fresh merge should represent both source records together, not emit a one-source M111 merge.

## Completeness

- Consequential gaps:
  - Two volume rows are modeled as grams per liter in the generated target.
  - The generated target lacks the default JCM autoclave instruction.
  - The generated target lacks references and the September `RESOLVED_JCM_J119_BCYE_AGAR` curation event.
  - The generated target is split from its repaired direct JCM 119 duplicate.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; Togo M111 and JCM 119 are formulation sources and not strain-specific growth assays.
  - The BCYE agar base and Legionella agar enrichment are commercial components and should remain ungrounded rather than being forced to narrow ChEBI terms.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw Togo M111 JSON dump or JCM 119 HTML capture under `data/raw`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated record gives two volume rows the wrong unit. | Togo M111 and JCM 119 list 900 ml distilled water and 100 ml Legionella agar enrichment; the generated merge stores `900 G_PER_L` and `100 G_PER_L`. | Already repaired in `data/normalized_yaml/bacterial/TOGO_M111_BCYE_Agar.yaml`; regenerate the merge. |
| Major | The generated merge is stale and split from its JCM duplicate. | The normalized M111 owner now links to `JCM_J119_BCYE_AGAR` as a `SOURCE_DUPLICATE`, and both current normalized records fingerprint to `b6eee245...`; the generated record is still an August one-source merge. | Regenerate `data/merge_yaml/merged` from current `data/normalized_yaml`; do not hand-edit the generated file. |
| Major | Source provenance added by the September repair is absent. | The normalized owner now has Togo M111 and JCM 119 references plus the September `RESOLVED_JCM_J119_BCYE_AGAR` curation event; the generated record has neither. | Already repaired in `data/normalized_yaml/bacterial/TOGO_M111_BCYE_Agar.yaml`; regenerate the merge. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged` from current `data/normalized_yaml` with `just merge-recipes`.
2. Confirm a fresh generated `b6eee245` BCYE Agar merge contains both `TOGO_M111_BCYE_Agar` and `JCM_J119_BCYE_AGAR`.
3. Confirm the fresh generated record carries the 100 ml/L Legionella agar enrichment row, 900 ml/L distilled-water row, JCM autoclave step, and Togo plus JCM references.
4. Remove or ignore any stale one-source M111 generated output left after regeneration.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the fresh generated BCYE M111/JCM 119 merge.
- Re-fetch Togo M111 and JCM 119 and manually compare the three commercial-component rows and autoclave instruction.
- Run `just verify-merges` and `just audit-merge-freshness --json --list` after regeneration.
- Re-run exact gitignore-independent searches for `TOGO:M111`, `mediadive.medium:J119`, `TOGO_M111_BCYE_Agar`, and `JCM_J119_BCYE_AGAR` under `data/merge_yaml/merged` to confirm the source duplicates are not split.

## Additional Notes

- The inspected Togo source was the SPARQList `gmdb_medium_by_gmid?gm_id=M111` API response.
- The inspected JCM source was the live `jcm_grmd?GRMD=119` HTML page.
- Exact gitignore-independent searches included ignored files where present.
