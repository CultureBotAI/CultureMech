# YAML Record Review: 1/5 Nutrient agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_5_Nutrient_agar.yaml`
- Started UTC: 2026-09-21T05:42:33Z
- Finished UTC: 2026-09-21T05:44:06Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008251` |
| Name | `1_5_nutrient_agar` |
| Original name | `1/5 Nutrient agar` |
| Category | `bacterial` |
| Source term | `TOGO:M1691`, `1/5 Nutrient agar` |
| Merge fingerprint | `4b28147050f4b578e4080fa0f709dea99ab38cde0e093cd39463d66d577e2459` |
| Maintained owner | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The generated merge exactly mirrors its maintained normalized owner plus the Aug 6 merge event. Future scientific fixes belong in `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` or the commercial-constituent enrichment that appended unsupported rows, followed by regeneration of `data/merge_yaml/merged/`.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_5_Nutrient_agar.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_5_Nutrient_agar.yaml --out /private/tmp/1_5_Nutrient_agar.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_5_Nutrient_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_5_Nutrient_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The source identity is correct:

- Live TOGO M1691 reports `name: 1/5 Nutrient agar`, `original_media_id: NBRC_M898`, and `src_url` ending in `NO=898`.
- Live NBRC Medium 898 reports Medium No. 898 and `1/5 Nutrient agar`.
- The generated `media_term.term.id` is `TOGO:M1691`, with the expected TOGO label.

The ingredient grounding is not source-faithful:

- TOGO M1691 and NBRC Medium 898 both represent the commercial product as one wrapper row, `Bacto Nutrient Agar (Difco)`, 4.6 g.
- The record lacks that wrapper row.
- The record adds five full-strength generic Nutrient Agar constituents from Microbe Notes, including `Agar 15.0 G_PER_L`, while also preserving NBRC's separate `Agar 14 G_PER_L` row.
- `Distilled water` is grounded to `CHEBI:15377` correctly, but its source amount is 1 L and the record stores `1 G_PER_L`.

## Evidence

The inspected TOGO M1691 JSON payload and NBRC Medium 898 page support exactly three quantitative source rows:

- `Bacto Nutrient Agar (Difco)`, 4.6 g.
- `Agar`, 14 g.
- `Distilled water`, 1 L.

The generated record supports the title, source accession, agar 14 g row, and water identity, but it is not an evidence-preserving representation of the formulation:

- It omits the 4.6 g `Bacto Nutrient Agar (Difco)` source row.
- It converts 1 L distilled water to `1 G_PER_L`.
- It appends `Peptone`, `Yeast extract`, `Beef extract`, `Sodium chloride`, and a second `Agar` row at full generic Nutrient Agar grams-per-liter values.

The inspected Microbe Notes page has a generic Nutrient Agar table with peptone, yeast extract, beef extract, sodium chloride, and agar amounts in g/L. It does not mention NBRC Medium 898, TOGO M1691, Difco Nutrient Agar, or a 4.6 g 1/5-strength powder addition, so it does not support replacing a single 4.6 g commercial-powder row with full-strength constituent rows in this medium.

## Completeness

Consequential gaps:

- The commercial powder wrapper row is missing.
- There is no structured direct NBRC or TOGO reference.
- There is no row-level source evidence for the original imported NBRC rows.
- The record has an unsupported duplicate agar row; `data/import_tracking/reports/merged_duplicates.tsv` independently reports repeated `Agar` rows at 14 and 15 g/L for `CultureMech:008251`.

Empty optional slots:

- `ph_value` is correctly absent; neither inspected TOGO M1691 nor NBRC Medium 898 provides pH.
- `target_organisms` and `growth_metrics` are correctly absent because the inspected recipe pages do not establish a specific organism-growth claim.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `CultureMech:008251`, `TOGO:M1691`, `M1691`, the merge fingerprint, `NBRC_M898`, `NO=898`, and `1/5 Nutrient agar` found the normalized owner, the generated merge, active registry/index rows, historical March validation rows, one deep-research priority row, the repeated-ingredient report row, and no curated repair script for this record.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The source commercial-powder row is missing. | TOGO M1691 and NBRC Medium 898 list `Bacto Nutrient Agar (Difco)` 4.6 g; the generated record has no such row. | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` or the enrichment rule that rewrote commercial powders |
| Major | Full-strength generic Nutrient Agar constituents were appended as final-medium ingredients. | Microbe Notes gives a generic Nutrient Agar composition table, but it does not support replacing 4.6 g Bacto Nutrient Agar in NBRC Medium 898 with 5.0 g peptone, 1.5 g yeast extract, 1.5 g beef extract, 5.0 g sodium chloride, and 15.0 g agar. | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` or the commercial-constituent enrichment |
| Major | Distilled water has the wrong unit. | TOGO M1691 and NBRC Medium 898 both list Distilled water as 1 L; the record stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` or the TOGO importer |
| Major | Agar is duplicated. | The source has one added `Agar` row at 14 g, but the record has the source-supported 14 g row and an extra generic 15.0 g row from the decomposed Nutrient Agar table. | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` |
| Minor | Structured references are absent. | The record cites TOGO, NBRC, and Microbe Notes only in free-text `notes`, so the reference validator had no structured references to check. | `data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` |

## Recommended Edits

1. Replace the five decomposed full-strength generic rows with one `Bacto Nutrient Agar (Difco)` ingredient at 4.6 g/L and preserve that it is an undefined commercial wrapper, not a single ChEBI molecule.
2. Keep one added `Agar` row at 14 g/L.
3. Correct Distilled water from `1 G_PER_L` to `1 L`.
4. Remove the Microbe Notes supplier-catalog blocks unless a later curation intentionally adds them as explanatory background without altering the source formulation.
5. Add structured TOGO M1691 and NBRC Medium 898 references plus row-level source notes, then append an accurate curation-history event.
6. Regenerate `data/merge_yaml/merged/` and confirm the generated record has exactly the three source rows.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` after curation.
- `just validate-references data/normalized_yaml/bacterial/1_5_nutrient_agar.yaml` after adding structured references.
- `just review-media-content` or a focused duplicate-ingredient report to confirm `CultureMech:008251` no longer appears as a repeated-`Agar` record.
- `just verify-merges` and `just audit-merge-freshness` after regenerating the generated merge.
- Manual comparison against TOGO M1691 and NBRC Medium 898 for `Bacto Nutrient Agar (Difco)` 4.6 g, agar 14 g, and distilled water 1 L.

## Additional Notes

- `1_5_Nutrient_agar.yaml` is schema-valid because the extra commercial-constituent rows are individually well-formed; schema validation cannot prove they are the right abstraction boundary for a diluted commercial powder.
- `medium_type: COMPLEX`, `composition_type: UNDEFINED`, `physical_state: SOLID_AGAR`, and `application: Microbial cultivation` are reasonable given the source's Bacto Nutrient Agar plus additional agar.
- The reference validator passed with 0 checks because this record has no structured `references` entries.
