# YAML Record Review: s6_medium__6a863f12

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s6_medium__6a863f12.yaml`
- Started UTC: `2026-09-25T03:24:38Z`
- Finished UTC: `2026-09-25T03:24:38Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s6_medium__6a863f12`, a single-source merge of `data/normalized_yaml/bacterial/TOGO_M82_S6_Medium.yaml`.

The record represents TOGO Medium `M82`, a JCM Medium 90 `S6 MEDIUM` import. The source is compact, but the normalized and generated YAML promote multiple milligram amounts to gram-per-liter values and drop the parent aliquot for a 10% sodium thiosulfate stock.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to TOGO `M82`, which imports JCM Medium 90 `S6 MEDIUM`. No duplicate or variant merge issue was found for this generated YAML.

## Evidence

TOGO `M82` and the live JCM 90 page list 1.8 g `KH2PO4`, 1.2 g `Na2HPO4`, 0.1 g `(NH4)2SO4`, 0.1 g `MgSO4 x 7H2O`, 30 mg `FeCl3 x 6H2O`, 30 mg `MnSO4 x xH2O`, 40 mg `CaCl2 x 2H2O`, 100 ml 10% `Na2S2O3` solution, and 900 ml distilled water.

The source says to adjust the medium to pH 7.0, sterilize the `Na2S2O3` solution separately by filtration, aseptically add the sterile `Na2S2O3` solution to the medium, and add 15 g/L agar for solid medium.

## Completeness

The generated record preserves the gram-scale phosphate, ammonium sulfate, magnesium sulfate, 900 ml water, and 15 g/L agar rows.

It loses or corrupts these source details:

- 40 mg `CaCl2 x 2H2O` is imported as `40 G_PER_L`.
- 30 mg `FeCl3 x 6H2O` is imported as `30 G_PER_L`.
- 30 mg `MnSO4 x xH2O` is imported as `30 G_PER_L`.
- The 100 ml 10% `Na2S2O3` solution parent aliquot is absent.
- 900 ml distilled water is represented as `900 G_PER_L`.
- The pH 7.0 adjustment and separate filtration plus aseptic addition of thiosulfate are absent from `preparation_steps`.

## Findings

- `needs curation`: Three milligram rows are off by 1000-fold. The generated record reports `CaCl2 x 2H2O` at `40 G_PER_L`, `FeCl3 x 6H2O` at `30 G_PER_L`, and `MnSO4 x xH2O` at `30 G_PER_L` even though the source lists 40 mg, 30 mg, and 30 mg.
- `needs curation`: The 100 ml 10% `Na2S2O3` stock aliquot is lost. The generated `solutions` entry keeps only a 100 g/L stock concentration with an empty composition and no indication that 100 ml is added to the parent medium.
- `needs curation`: The 900 ml water row is recorded as `900 G_PER_L`, converting a volume to a mass concentration.
- `needs curation`: The generated record has no preparation steps, so it loses pH 7.0 adjustment, separate filtration of the thiosulfate solution, aseptic thiosulfate addition, and the conditional agar instruction for solid medium.
- `pass with minor issues`: The TOGO `M82` identity, direct gram-scale salt amounts, magnesium sulfate heptahydrate grounding, and agar amount are otherwise plausible.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s6_medium__6a863f12.yaml`; fix the TOGO `M82` normalized source, then rerun the merge.
- Correct `CaCl2 x 2H2O`, `FeCl3 x 6H2O`, and `MnSO4 x xH2O` so the JCM milligram values are not promoted to g/L.
- Model 10% `Na2S2O3` solution as a filtered stock with a 100 ml parent aliquot.
- Keep 900 ml distilled water as a volume row, not `900 G_PER_L`.
- Preserve preparation metadata for pH 7.0, separate thiosulfate filtration, aseptic thiosulfate addition, and the solid-medium agar addition.
- Consider normalizing the Unicode hydrate separators in preferred terms to the same ASCII `x` spelling used by neighboring generated records.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/TOGO_M82_S6_Medium.yaml`.
- Regenerate `data/merge_yaml/merged/s6_medium__6a863f12.yaml` and verify that `CaCl2 x 2H2O`, `FeCl3 x 6H2O`, and `MnSO4 x xH2O` are no longer 30-40 g/L rows.
- Verify that the 100 ml 10% `Na2S2O3` solution aliquot survives regeneration.
- Verify that the generated record carries pH and thiosulfate sterilization or addition preparation steps.
- Run an exact ignored-file-inclusive search for `TOGO:M82` and `TOGO_M82_S6_Medium` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The generated `SOLID_AGAR` physical state and 15 g/L agar row are acceptable for the solid form of the recipe, but the source also describes the base liquid medium before that optional agar addition.
