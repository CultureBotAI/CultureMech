# YAML Record Review: asw_barley

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asw_barley.yaml
- Started UTC: 2026-09-21T16:11:55Z
- Finished UTC: 2026-09-21T16:11:55Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asw_barley.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000043` |
| Label | `asw_barley` |
| Category | `algae` |
| Source identity | `CCAP:ASW_Barley`; `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW_Barley.pdf` |
| Generated status | Generated on 2026-08-06 from `data/normalized_yaml/algae/asw_barley.yaml` |
| Merge fingerprint | `fec2f0359e128887f8bcea8f4d3955c96f74bfc61d9cd7dd05431eafe3420945` |

`data/merge_yaml/merged/` is derived output. Future fixes belong in
`data/normalized_yaml/algae/asw_barley.yaml` or the CCAP import/repair code,
then this generated file should be regenerated.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asw_barley.yaml` | Pass. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/asw_barley.yaml --out /private/tmp/asw_barley.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/asw_barley.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/asw_barley.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass. The only emitted message was the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked | No focused validator for embedded `MediaRecipe.curation_history` is documented for one merged recipe. `just validate-history` targets standalone records under `history/`. |

Direct `just` validation was not used because this checkout currently reaches a
project `uv` build of `llvmlite==0.46.0` under Python 3.13 before target-specific
validation and fails in `setuptools` with `TypeError: Popen.__init__() got an
unexpected keyword argument 'dry_run'`.

## Identity and Grounding

- The ID, name, category, `original_name`, and references identify the CCAP algae
  `ASW_Barley` recipe.
- The refetched CCAP `MR_ASW_Barley.pdf` is a one-page `ASW (Artificial
  Seawater) + Barley` recipe reviewed on 2026-05-29.
- The first generated ingredient is chemically wrong: the source states
  `NaNO3`, but the generated record has preferred term `NaNO` grounded to
  `CHEBI:34545` Azimilide. Sodium nitrate should be grounded to `CHEBI:63005`.
- Tricine and most phosphate/vitamin groundings are chemically plausible, but
  their amounts are stock-solution concentrations, not final-medium
  concentrations.
- The generated record has no Barley grain row even though barley is part of the
  ASW + barley identity.

## Evidence

| Claim in generated record | Review |
|---|---|
| Tricine at 0.50 g/L | Supported as a direct main-medium row. |
| `NaNO` / Azimilide at 15.00 g/L | Not supported. The CCAP source lists `NaNO3` in Stock solution 1, not Azimilide. |
| `Na2HPO4`, `K2HPO4`, and the eight vitamins as direct g/L ingredients | The stock recipes contain these ingredients, but the target incorrectly presents their stock concentrations as direct final-medium amounts. |
| `HCl. Add` at 1 g/L | Not supported. This is broken pH-adjustment and barley-addition preparation text parsed as an ingredient. |
| Lack of `preparation_steps` | Incomplete. The source says to make up to 1 litre with filtered natural seawater, adjust pH to 7.6-7.8, add one barley grain per 25 ml, and autoclave. |

## Completeness

- The current normalized algae owner has already replaced the bad `HCl. Add`
  row with a variable `Barley grain` row, added structured CCAP `sources`, and
  added preparation steps for final volume, pH adjustment, barley addition, and
  autoclaving. The generated record is stale relative to that owner.
- The current normalized owner still needs a solution-aware representation for
  Stock solution 1, Vitamin solution, and SE1. As in CCAP ASW, the source
  amounts belong inside nested stocks that are diluted into the final medium.
- Empty `target_organisms` and `growth_metrics` are acceptable for this
  source-formulation record.
- `find reports/yaml_record_review -maxdepth 1 -name '*asw_barley.md'` searched
  the ignored report directory independently of `.gitignore` and found no prior
  review report for this exact generated record stem before this report was
  written.
- An ignored-file-inclusive exact search for `CultureMech:000050`, `ASW
  barley`, `ASW_barley`, `asw_barley`, `MR_ASW_barley`, `ASW_Barley`, and
  `barley grain` across `data`, `reports`, `history`, and `.claude` found this
  algae owner, the adjacent bacterial `asw_barley` owner, generated records,
  index rows, media-variant proposals, label-plausibility reports, and the
  earlier ASW salinity-grid review.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | The generated merge is stale and still has the malformed `HCl. Add` pseudo-ingredient instead of source-supported barley and preparation steps. | The reviewed merge lists `HCl. Add` at 1 g/L and no steps; `data/normalized_yaml/algae/asw_barley.yaml` now lists `Barley grain` and four ordered preparation steps. | Regenerate `data/merge_yaml/merged/asw_barley.yaml` from the normalized algae owner. |
| major | Sodium nitrate is truncated and grounded to the wrong chemical. | The CCAP source lists `NaNO3`; the record says `NaNO` with `CHEBI:34545` Azimilide. | `data/normalized_yaml/algae/asw_barley.yaml` and the CCAP ingredient parser/grounder. |
| major | Stock-solution ingredients are flattened as if they were final-medium g/L ingredients. | CCAP defines Extra salts, Vitamin solution, and SE1 stocks, then adds 7.50, 2.50, and 25.00 ml of those stocks per litre of medium. | Same normalized owner or CCAP importer. |
| minor | Generic algae light and temperature defaults are not source-supported. | The inspected CCAP PDF has no light-intensity, light-cycle, or incubation-temperature values. | `data/normalized_yaml/algae/asw_barley.yaml` or the CCAP import defaulting rule. |

## Recommended Edits

1. Regenerate the generated merge from the current normalized owner to remove
   `HCl. Add`, add `Barley grain`, copy structured `sources`, and expose the
   repaired preparation steps.
2. Correct `NaNO` / Azimilide to `NaNO3` / sodium nitrate in the maintained
   algae owner.
3. Re-model Extra salts, Vitamin solution, and SE1 as stock solutions added at
   the source-stated millilitre-per-litre volumes instead of as direct final
   ingredients.
4. Remove or specifically source the generic light and temperature values.

## Follow-up Checks

- Re-run open-schema, strict, term, and reference validation on the normalized
  ASW + barley owner after correction.
- Run the ingredient label plausibility check that previously flagged `NaNO`
  versus Azimilide.
- Recalculate final nitrate, phosphate, and vitamin amounts from stock
  strengths and addition volumes to verify dilution arithmetic.
- Regenerate `data/merge_yaml/merged/asw_barley.yaml` and confirm the generated
  record no longer has `HCl. Add`.

## Additional Notes

- The current CCAP PDF includes a Provasoli, McLaughlin, and Droop 1957
  reference for artificial media development. The record does not currently
  make a narrow growth-performance claim that depends on that citation.
- The bacterial MediaDive C5 `asw_barley` record is a separate generated target
  in `data/merge_yaml/merged/asw_barley__95e90355.yaml`.
