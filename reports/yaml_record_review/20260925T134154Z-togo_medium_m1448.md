# YAML Record Review: togo_medium_m1448

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1448.yaml`
- Started UTC: 2026-09-25T13:41:54Z
- Finished UTC: 2026-09-25T13:41:54Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1448, imported from NBRC medium 220.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1448.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1448`; the TOGO API identifies NBRC_M220 as its original medium ID and links NBRC medium 220.

The TOGO payload and NBRC page agree on 2 g yeast extract, 1 g Bacto Tryptone, 1 g sodium acetate, 50 ml soil extract, 950 ml distilled water, 15 g agar if needed, pH 7.4, and the soil-extract preparation from 400 g farm soil in 1 L distilled water.

The normalized source YAML already contains the September 2026 NBRC 220 repair, but the generated merged YAML was last rebuilt from an older source snapshot.

## Evidence

The generated yeast extract, Bacto Tryptone, sodium acetate, and agar rows match the source amounts.

The generated merge still has pH absent, collapses 950 ml main-medium water and 1 L soil-extract water into a single 951 g/L water row, leaves `farm soil` as a top-level ingredient, and represents 50 ml soil extract as 50 g/L.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated merge omits the pH 7.4 value, the source-specific nested soil-extract recipe, the soil-extract autoclaving and filtration steps, and the source references present in the repaired normalized source.

## Findings

- Major issue: the generated merge is stale relative to `data/normalized_yaml/bacterial/togo_medium_m1448.yaml` and does not include the September 2026 NBRC 220 repair.
- Major issue: the main 950 ml distilled-water row and the 1 L soil-extract water row are incorrectly merged into 951 g/L top-level water.
- Major issue: 50 ml soil extract is represented as 50 g/L and linked to the generic `mediadive.solution:5555` term instead of the NBRC 220 source-specific extract.
- Major issue: `farm soil` is modeled as a top-level medium ingredient instead of nested under soil-extract preparation.
- Minor issue: pH 7.4 is absent.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/togo_medium_m1448.yaml` from the current normalized TOGO M1448 source.
- Confirm the regenerated record preserves 950 ml/L main distilled water, 50 ml/L source-specific soil extract, nested 400 g/L farm soil, pH 7.4, preparation steps, and references.
- Do not manually patch the generated merge; keep any remaining formulation curation in `data/normalized_yaml/bacterial/togo_medium_m1448.yaml`.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge.
- Confirm the regenerated fingerprint and `MERGED_RECIPES` history entry correspond to the repaired normalized source, not the stale August 2026 merge.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1448 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
