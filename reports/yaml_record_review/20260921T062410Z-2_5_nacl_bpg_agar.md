# YAML Record Review: 2_5_nacl_bpg_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_5_nacl_bpg_agar.yaml`
- Started UTC: 2026-09-21T06:22:47Z
- Finished UTC: 2026-09-21T06:24:10Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:002537` |
| Name | `2_5_nacl_bpg_agar` |
| Original name | `2.5% NaCl-BPG AGAR` |
| Category | `bacterial` |
| Source term | `mediadive.medium:J177`, `2.5% NaCl-BPG AGAR` |
| Source URL | `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=177` in free-text `notes` |
| Merge fingerprint | `bac58d18c2bf1a422d63676d7dcf7ce513f82e74e61ba386f5b4a0c411330233` |
| Maintained owner | `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated MediaDive/JCM representation of JCM Medium 177, 2.5% NaCl-BPG AGAR. The generated artifact and its maintained owner currently match for the fields reviewed here, so future corrections belong in `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` followed by merge regeneration.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_5_nacl_bpg_agar.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_5_nacl_bpg_agar.yaml --out /private/tmp/2_5_nacl_bpg_agar.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_5_nacl_bpg_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_5_nacl_bpg_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes JCM Medium 177:

- `media_term.term.id` is `mediadive.medium:J177`.
- The free-text source URL points to the JCM `GRMD=177` page.
- The inspected JCM page resolves to medium number 177 and title `2.5% NaCl-BPG AGAR`.

Ingredient grounding is sound for glucose, agar, NaCl, MgSO4 x 7 H2O, and KCl, including the heptahydrate identity for magnesium sulfate. Bacto peptone and fish meat extract are appropriately left without single-compound CHEBI terms, but the source's `BD-Difco` Bacto peptone qualifier is missing from the JCM/MediaDive record.

## Evidence

The inspected JCM Medium 177 page supports this formulation for a 1 L distilled-water basis:

- 5 g Bacto peptone (BD-Difco).
- 5 g fish meat extract.
- 1 g glucose.
- 15 g agar.
- 25 g NaCl.
- 2.5 g MgSO4 x 7 H2O.
- 1 g KCl.
- 1 L distilled water.
- pH 7.2.

The JCM page also states a source-wide default to sterilize media by autoclaving at 121 C for 15 minutes unless otherwise stated.

The target preserves all seven dry components and pH 7.2. It loses the distilled-water row, broadens `Bacto peptone (BD-Difco)` to `Bacto peptone`, and omits the default autoclaving instruction.

## Completeness

Consequential gaps:

- The generated target has no distilled-water row.
- The generated target has no structured `references` entry, so the reference validator had no JCM URL to inspect.
- The generated target has no preparation step for the default JCM autoclave instruction.
- The duplicate TOGO M170 import remains a separate normalized and generated record for the same JCM Medium 177 source.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM recipe supports a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_5_nacl_bpg_agar.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:002537`, `CultureMech:008272`, `mediadive.medium:J177`, `TOGO_M170_2.5_NaCl-BPG_Agar`, the JCM GRMD URL, and both merge fingerprints found the expected MediaDive/JCM and TOGO M170 normalized/generated pairs. Ignored files were included under the searched paths. The short `TOGO:M170` token also matched unrelated longer TOGO IDs, so only exact JCM 177 and TOGO M170 hits were used.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The final-volume water row is missing. | JCM Medium 177 lists 1 L distilled water after KCl; the generated and normalized JCM records omit water entirely. | `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Major | The JCM default autoclaving instruction is missing. | JCM states that media should be autoclaved at 121 C for 15 min unless otherwise stated; neither the generated target nor the maintained normalized owner represents that step. | `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Major | The TOGO M170 source duplicate is unreconciled and contains a water-unit defect. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` points to `Original source: JCM - JCM_M177` but stores distilled water as `1 G_PER_L` and is not linked to `CultureMech:002537`; the generated layer keeps it as a separate `2_5_nacl_bpg_agar__bf5a0a80.yaml` record. | `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` plus reciprocal variant links in `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Minor | The Bacto peptone source qualifier is absent. | JCM names `Bacto peptone (BD-Difco)`; the JCM/MediaDive record stores generic `Bacto peptone`. | `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |
| Minor | Structured source provenance is absent from the generated target. | The record cites the JCM URL only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` |

## Recommended Edits

1. Add `Distilled water` at 1 L final volume to `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml`.
2. Add a JCM-supported preparation step for autoclaving at 121 C for 15 minutes.
3. Preserve the source qualifier by renaming `Bacto peptone` to `Bacto peptone (BD-Difco)` or by adding an equivalent ingredient note.
4. Add a structured JCM Medium 177 reference.
5. Curate `data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml`: correct distilled water from `1 G_PER_L` to a 1 L final-volume representation, add the JCM reference, preserve the TOGO source ID, and link it to `data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` as a `SOURCE_DUPLICATE` in both directions.
6. Regenerate `data/merge_yaml/merged/` after the maintained records are corrected.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` after curation.
- `just validate-references data/normalized_yaml/bacterial/2_5_nacl_bpg_agar.yaml` after adding structured JCM provenance.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M170_2.5_NaCl-BPG_Agar.yaml` after repairing the TOGO duplicate's water row and variant link.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 177 for 5 g/L Bacto peptone (BD-Difco), 5 g/L fish meat extract, 1 g/L glucose, 15 g/L agar, 25 g/L NaCl, 2.5 g/L MgSO4 x 7 H2O, 1 g/L KCl, 1 L distilled water, pH 7.2, and default autoclaving.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
