# YAML Record Review: ch

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/CH.yaml
- Started UTC: 2026-09-22T06:52:19Z
- Finished UTC: 2026-09-22T06:53:05Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:000330 |
| Name | ch |
| Original name | CH |
| Category | bacterial |
| Source | CCAP Medium C22 / MR_CH.pdf |
| Generated status | Generated merged output |
| Maintained owner | data/normalized_yaml/bacterial/ch.yaml |

The reviewed file is the generated merge of one CCAP/MediaDive owner. Future
fixes belong in `data/normalized_yaml/bacterial/ch.yaml` or upstream CCAP
repair logic, followed by merge/page regeneration.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/CH.yaml` | Passed; no issues found. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/CH.yaml --out /private/tmp/CH.strict.tsv --workers 1 --quiet` | Passed with 0 errors; TSV written to `/private/tmp/CH.strict.tsv`. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/CH.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/CH.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not checked | `just validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in the merged YAML. |

## Identity and Grounding

- The reviewed record points to `mediadive.medium:C22` and the CCAP
  `MR_CH.pdf` source for CH / Chalkley's Medium.
- The direct CCAP PDF says CH is a Freshwater Protozoa medium. The reviewed
  record's source identity is correct, but its `bacterial` category and
  `Microbial cultivation` application are legacy MediaDive placement rather
  than CCAP-specific classification.
- The maintained bacterial owner was already rebuilt on 2026-08-25 with one
  deionized-water ingredient and three stock solutions. The generated record
  was merged on 2026-08-06, so it still contains the stale flattened
  composition.

## Evidence

The inspected CCAP CH PDF lists three stock solutions prepared per 100 ml:
2.0 g NaCl, 0.08 g KCl, and 0.12 g CaCl2. It then adds 5.0 ml each of Stock
solutions 1-3 to the final litre, brings the medium to 1 litre with deionised
water, and autoclaves at 15 psi for 15 minutes.

The reviewed generated record instead has NaCl, KCl, and CaCl2 as direct
top-level ingredients, each at 5 g/L. That repeats the source addition volume
as a mass concentration and loses the stock concentrations:

| Stock | Source stock concentration | Source final dose | Generated value |
|---|---:|---:|---:|
| NaCl | 20 g/L | 5 ml/L | 5 g/L |
| KCl | 0.8 g/L | 5 ml/L | 5 g/L |
| CaCl2 | 1.2 g/L | 5 ml/L | 5 g/L |

## Completeness

- The stock solution boundaries are absent from the generated record.
- The final deionized-water volume is absent from the generated record.
- The generated record does retain the autoclave instruction.
- Exact `rg --no-ignore --hidden` searches over YAML for `MR_CH.pdf`,
  `CCAP:CH`, `CultureMech:000330`, `CultureMech:000059`,
  `mediadive.medium:C22`, and exact `name: ch` identities included ignored and
  hidden files. They found the bacterial MediaDive owner, a separate algae CCAP
  owner for the same PDF, and their generated outputs.
- Empty optional slots such as pH and target organisms were not treated as
  defects.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Stock addition volumes were imported as direct g/L concentrations. | CCAP uses 5 ml/L of three stocks; the generated record has NaCl, KCl, and CaCl2 at 5 g/L each. | Already repaired in `data/normalized_yaml/bacterial/ch.yaml`; regenerate |
| Major | The generated record is stale relative to its maintained owner. | The maintained owner has three `ML_PER_L` stock solution entries and deionized water from 2026-08-25; the generated record predates that repair. | Regenerate from `data/normalized_yaml/bacterial/ch.yaml` |
| Minor | Category is unsupported by the CCAP source. | CCAP labels CH as Freshwater Protozoa, but the reviewed record is categorized as bacterial. | `data/normalized_yaml/bacterial/ch.yaml` or cross-category CCAP de-duplication |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/CH.yaml` from the repaired bacterial
   owner so NaCl, KCl, and CaCl2 move back into 5 ml/L stock solution entries
   and deionized water is restored.
2. Compare the bacterial `ch` owner with `data/normalized_yaml/algae/ch.yaml`
   and decide whether these same-source CCAP CH records should be merged,
   cross-linked, or recategorized.
3. If the bacterial owner remains, change the category/application language so
   it no longer implies bacterial use unsupported by a CCAP Freshwater Protozoa
   recipe.

## Follow-up Checks

- Re-run open-schema, strict, term, and reference validation on the maintained
  bacterial owner and regenerated `data/merge_yaml/merged/CH.yaml`.
- Manually compare regenerated stock concentrations and 5 ml/L addition amounts
  against `MR_CH.pdf`.
- Run a focused duplicate check for the bacterial and algae `ch` owners before
  publishing both same-source records.

## Additional Notes

- The CCAP PDF text was extractable with `pypdf`; no OCR was needed.
- The MediaDive JSON endpoints reject `C22` with `wrong ID given`, so this
  review used the local MediaDive import, the repaired normalized owners, and
  the direct CCAP PDF.
