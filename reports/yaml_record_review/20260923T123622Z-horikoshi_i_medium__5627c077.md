# YAML Record Review: HORIKOSHI-I MEDIUM
- Repository: CultureMech
- Record: data/merge_yaml/merged/horikoshi_i_medium__5627c077.yaml
- Started UTC: 2026-09-23T12:35:30Z
- Finished UTC: 2026-09-23T12:36:22Z
- Verdict: needs curation

## Target

Reviewed the generated MediaDive/JCM branch for JCM Medium 181, `HORIKOSHI-I MEDIUM`, at `data/merge_yaml/merged/horikoshi_i_medium__5627c077.yaml`. The maintained source is `data/normalized_yaml/bacterial/horikoshi_i_medium.yaml`.

## Validation

- Open LinkML validation: passed for `MediaRecipe`.
- Strict validation: passed with zero error rows in `/private/tmp/horikoshi_i_medium__5627c077.strict.tsv`.
- Reference validation: passed with zero checks.
- Term validation: passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone YAML files under `history/`, not embedded `MediaRecipe.curation_history` entries in generated merge artifacts.

## Identity and Grounding

The stable ID, original label, and `mediadive.medium:J181` source all identify the direct JCM Medium 181 Horikoshi-I medium import. The record is not a 2%, 3.5%, 5%, or 10% NaCl child recipe, and the maintained source now links those child variants explicitly.

This generated branch is a same-JCM-source sibling of the TOGO M174 branch at `data/merge_yaml/merged/horikoshi_i_medium.yaml`. Its different merge fingerprint reflects source-version and importer-shape differences, not a distinct named medium.

## Evidence

The MediaDive REST payload for JCM `J181` names the medium `HORIKOSHI-I MEDIUM`, reports pH 10, links JCM `GRMD=181`, and models a 900 ml main solution containing 10 g glucose, 5 g Polypeptone with the `Nihon Pharm. Co.` attribute, 5 g yeast extract, 1 g K2HPO4, 0.2 g MgSO4 x 7 H2O, 15 g agar, and 900 ml distilled water. Its only preparation step says to add 100 ml of sterile 10% Na2CO3 solution after autoclaving and check the final pH around 10.0.

The inspected JCM page agrees on the six base components, the 900 ml water amount, the post-autoclave 10% Na2CO3 addition, and the final pH. It also supplies the site-wide default sterilization condition, autoclaving at 121 C for 15 min unless otherwise stated. The current JCM peptone line is `Hipolypepton (FUJIFILM Wako)`, while MediaDive still carries `Polypeptone` plus a `Nihon Pharm. Co.` attribute.

## Completeness

The generated artifact preserves pH 10.0, but it omitted the distilled-water row and the sodium-carbonate stock solution. It also copied MediaDive's 900 ml base `g_l` values, so glucose is `11.1111 G_PER_L` instead of the source 10 g final-medium amount, Polypeptone and yeast extract are each `5.55556 G_PER_L` instead of 5 g, K2HPO4 is `1.11111 G_PER_L` instead of 1 g, MgSO4 x 7 H2O is `0.222222 G_PER_L` instead of 0.2 g, and agar is `16.6667 G_PER_L` instead of 15 g.

No `target_organisms`, growth metrics, or strain-specific growth evidence are asserted, so there are no over-scoped organism claims to review.

## Findings

- The generated artifact is stale and no longer reflects the maintained September 2026 source repair in `data/normalized_yaml/bacterial/horikoshi_i_medium.yaml`.
- The six base solutes are stored as MediaDive's 900 ml stock concentrations instead of the source recipe's per-final-liter masses.
- The JCM 900 ml distilled-water row is missing.
- The sterile 10% Na2CO3 post-autoclave addition is missing as a structured solution and survives only as free text in an `ADJUST_PH` preparation step.
- The JCM default 121 C for 15 min autoclave instruction is missing from `sterilization` and `preparation_steps`.
- The direct MediaDive source and current JCM page disagree on the peptone product string; the stale generated artifact follows MediaDive's `Polypeptone` row rather than the fetched JCM `Hipolypepton (FUJIFILM Wako)` row used by the maintained source.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/horikoshi_i_medium__5627c077.yaml` from `data/normalized_yaml/bacterial/horikoshi_i_medium.yaml` so the merged branch inherits source-scale masses, the 900 ml water row, the 100 ml 10% Na2CO3 solution, the default JCM autoclave step, and the current JCM peptone label.
- Audit the merge/import boundary if regeneration still prefers MediaDive `g_l` values over the curated source amounts for media that add a stock solution after a 900 ml base.
- Preserve the MediaDive-versus-JCM peptone label difference explicitly if both source versions continue to be retained as distinct branches.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation against the regenerated merged record.
- Re-open JCM Medium 181 and MediaDive `J181` to confirm the regenerated direct-JCM branch carries the intended source amount scale and an explicit note for any retained peptone label conflict.
- Run the repository's merge freshness audit to prove `data/merge_yaml/merged/horikoshi_i_medium__5627c077.yaml` no longer lags its normalized source.

## Additional Notes

None.
