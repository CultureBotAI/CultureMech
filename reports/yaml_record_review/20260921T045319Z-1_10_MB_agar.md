# YAML Record Review: 1_10_mb_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_MB_agar.yaml`
- Started UTC: 20260921T045138Z
- Finished UTC: 20260921T045319Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008604` |
| Label | `1_10_mb_agar` |
| Original label | `1/10 MB agar` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/1_10_mb_agar.yaml`, TOGO/NBRC import normalization, or merge regeneration |
| Merge owner | `merged_from: [1_10_mb_agar]` |
| Maintained owner | `data/normalized_yaml/bacterial/1_10_mb_agar.yaml` |

This generated singleton is the TOGO Medium M2017 import of NBRC Medium 1306.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_MB_agar.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_MB_agar.yaml --out /private/tmp/1_10_MB_agar.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_MB_agar.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_MB_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_MB_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly denotes TOGO Medium M2017 / NBRC Medium 1306, `1/10 MB agar`. The NBRC source page for medium 1306 lists Bacto Marine Broth 2216 (Difco), distilled water, artificial seawater, optional agar, and pH 7.6.

The generated artifact is stale relative to its maintained normalized owner. `data/normalized_yaml/bacterial/1_10_mb_agar.yaml` was repaired on 2026-09-11 with `ph_value: 7.6`, an agar row source/note clarifying that agar is optional, `data_quality_flags`, and structured TOGO/NBRC references that are all absent from the Aug 6 merge artifact.

## Evidence

Supported by inspected source text:

- NBRC Medium 1306 supports the `1/10 MB agar` identity and ingredient set.
- NBRC supports 3.74 g Bacto Marine Broth 2216 (Difco), 250 ml distilled water, 750 ml artificial seawater, 15 g agar if needed, and pH 7.6.

Unsupported, missing, or stale in the generated record:

- `ph_value: 7.6` is absent from the generated artifact even though NBRC and the normalized owner both carry it.
- The generated artifact lacks the maintained owner's structured TOGO/NBRC `references` and `data_quality_flags`.
- Distilled water and artificial seawater are typed as `G_PER_L` despite source milliliter volumes in a 1 L recipe.

## Completeness

- Agar is complete enough as an optional solidifying row, and the maintained owner now marks that optionality in the row notes.
- The source has no preparation steps beyond pH, so the generated record's lack of autoclave or incubation steps is not a defect.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the NBRC page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:008604`, `TOGO:M2017`, `NBRC_M1306`, the NBRC 1306 URL slug, and the merge fingerprint found only this normalized owner, this generated singleton, and ID/catalog entries. A separate filename search for `*M2017*` found no accession-bearing filename because this TOGO import is normalized under `1_10_mb_agar.yaml`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact is stale and omits NBRC pH 7.6. | NBRC 1306 lists pH 7.6, and the maintained normalized record already has `ph_value: 7.6`; `data/merge_yaml/merged/1_10_MB_agar.yaml` does not. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_10_mb_agar.yaml`. |
| Major | Two milliliter solvent rows are represented as grams per liter. | NBRC 1306 lists distilled water at 250 ml and artificial seawater at 750 ml, while the generated and normalized YAML both store `250 G_PER_L` and `750 G_PER_L`. | `data/normalized_yaml/bacterial/1_10_mb_agar.yaml` or the TOGO/NBRC volume import mapping. |
| Minor | Structured references are stale-missing in the generated artifact. | The maintained owner has TOGO and NBRC references, but the generated artifact still has only free-text `notes`; `linkml-reference-validator` performed zero checks on the generated file. | Regenerate `data/merge_yaml/merged/` from the normalized owner. |

## Recommended Edits

1. Correct distilled-water and artificial-seawater quantities in `data/normalized_yaml/bacterial/1_10_mb_agar.yaml` so NBRC milliliter volumes are not encoded as grams per liter.
2. Regenerate `data/merge_yaml/merged/` so `1_10_MB_agar.yaml` receives the normalized owner's pH 7.6, source-supported optional-agar note, `data_quality_flags`, and structured references.
3. Recheck whether future TOGO/NBRC volume imports are also mapping `ml` solvent rows to `G_PER_L`.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/1_10_mb_agar.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/1_10_mb_agar.yaml` to confirm the existing structured TOGO/NBRC references still resolve.
- `just verify-merges` to prove merge regeneration preserves a singleton M2017 canonical and carries the September normalized repair into the generated output.
- `just validate-strict` and `just validate-terms` on the regenerated `1_10_MB_agar.yaml` merge artifact.
- Manual comparison to NBRC Medium 1306 for the 3.74 g Marine Broth, 250 ml water, 750 ml artificial seawater, optional 15 g agar, and pH 7.6 rows.

## Additional Notes

- A direct fetch of TOGO Medium M2017 returned only the TogoMedium JavaScript shell; the local record points to NBRC Medium 1306, and that NBRC source page resolved.
- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
