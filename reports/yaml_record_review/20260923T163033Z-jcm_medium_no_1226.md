# YAML Record Review: JCM MEDIUM No. 1226

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_1226.yaml
- Started UTC: 2026-09-23T16:30:03Z
- Finished UTC: 2026-09-23T16:30:33Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_1226.yaml`
- Stable ID: `CultureMech:002393`
- Name: `jcm_medium_no_1226`
- Original name: `JCM MEDIUM No. 1226`
- Category: `bacterial`
- Media term: `mediadive.medium:J1226`
- Source: MediaDive import of JCM Medium 1226, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1226`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_1226.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_1226`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_1226.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_1226.yaml --out /private/tmp/jcm_medium_no_1226.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_1226.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_1226.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The schema checks do not prove the source formula is complete or that hydrate-specific ingredient names are grounded at the same specificity.

## Identity and Grounding

The generated record identifies MediaDive `J1226` for JCM Medium 1226. The current live JCM `GRMD=1226` page returns `Nothing found`, but MediaDive J1226 and TogoMedium M1318 both point to that same historical JCM accession and agree on the formula and pH.

The bacterial solid-agar, complex, and undefined classifications agree with the 15 g agar, Bacto peptone, and yeast extract rows.

Most represented small molecules match their source strings, but `Trisodium citrate x 2 H2O` is grounded to `CHEBI:53258` with the label `sodium citrate`. That generic sodium-citrate grounding does not preserve the dihydrate form supplied by both MediaDive and TOGO.

## Evidence

Inspected evidence:

- MediaDive REST `medium/J1226` reports `JCM MEDIUM No. 1226`, source `JCM`, pH 7.3, link `GRMD=1226`, and one 1000 ml solution with 50 g NaCl, 9.7 g MgSO4 x 7 H2O, 3 g trisodium citrate x 2 H2O, 2 g KCl, 0.2 g CaCl2 x 2 H2O, 10 g Bacto peptone with attribute `BD-Difco`, 2 g yeast extract with attribute `BD-Difco`, 15 g agar, and 1000 ml distilled water.
- MediaDive carries one preparation step: adjust pH to 7.3.
- TogoMedium M1318 points to `JCM_M1226` and `GRMD=1226` and carries the same nine source ingredient rows, including 1 L distilled water and the two `BD-Difco` extract qualifiers.
- The live JCM `GRMD=1226` endpoint was inspected but no longer returns the Medium 1226 formulation.

Supported:

- The MediaDive ID, original name, source link, category, pH, physical state, and eight represented ingredient quantities are supported by the MediaDive J1226 response.
- The `ADJUST_PH` step is supported by MediaDive and TOGO.

Unsupported or incomplete:

- MediaDive includes 1000 ml distilled water; the generated record omits that row.
- MediaDive and TOGO both qualify Bacto peptone and yeast extract as `BD-Difco`, while the generated rows are generic.
- The hydrate-specific trisodium-citrate source row is grounded to a generic sodium-citrate CHEBI term.

## Completeness

Consequential gaps:

- The solvent row is missing.
- Supplier qualifiers are missing from both undefined extract rows.
- The exact citrate hydrate is not represented in the ontology grounding.

Empty slots that are acceptable:

- No stock solution appears in MediaDive or TOGO for this formulation.
- No target-organism or growth-metric claim is represented, so no strain-specific growth evidence is required here.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=1226`, exact `JCM_M1226`, and exact `jcm_medium_no_1226` IDs found only the direct MediaDive/JCM record and the TOGO M1318 wrapper. No other normalized or merged copy of this JCM 1226 source was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated direct MediaDive/JCM record omits the recipe solvent. | MediaDive order 9 lists 1000 ml `Distilled water`, and TOGO M1318 lists 1 L distilled water for the same original `JCM_M1226` source. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1226.yaml`, followed by merge regeneration. |
| Major | `Trisodium citrate x 2 H2O` is grounded too broadly as generic sodium citrate. | Both MediaDive and TOGO identify a dihydrate citrate row, but the generated row uses `CHEBI:53258` labeled `sodium citrate`. | CHEBI enrichment or curation in `data/normalized_yaml/bacterial/jcm_medium_no_1226.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/togo_medium_m1318.yaml` and `data/merge_yaml/merged/togo_medium_m1318.yaml` pointing to `JCM_M1226` and `GRMD=1226`. The TOGO wrapper keeps the missing water and supplier qualifiers but stores the water quantity as `1 G_PER_L`. | Both normalized owners, plus `scripts/merge_recipes.py` after source repair. |
| Minor | The `BD-Difco` supplier qualifiers are missing from Bacto peptone and yeast extract. | MediaDive keeps `attribute: "BD-Difco"` for both undefined rows, and TOGO includes the qualifiers directly in the component names. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1226.yaml`. |

## Recommended Edits

1. Add or import the 1000 ml distilled-water row in `data/normalized_yaml/bacterial/jcm_medium_no_1226.yaml`.
2. Replace the generic sodium-citrate grounding with a hydrate-specific term for `Trisodium citrate x 2 H2O`, or leave it ungrounded with the exact source string explicit if a verified ontology term is not available.
3. Preserve `BD-Difco` qualifiers on Bacto peptone and yeast extract.
4. Repair `data/normalized_yaml/bacterial/togo_medium_m1318.yaml` so its 1 L water row is not represented as `1 G_PER_L`.
5. Regenerate `data/merge_yaml/merged/` and verify whether `jcm_medium_no_1226` and `togo_medium_m1318` collapse to one canonical generated recipe.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Re-fetch MediaDive `medium/J1226` and TogoMedium `M1318` before editing, because the current JCM endpoint no longer returns the formula.
- Compare the regenerated merged record against MediaDive's recipe orders 1-9 and pH step.
- Search with `rg --no-ignore --hidden 'GRMD=1226([^0-9]|$)|JCM_M1226([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm that same-source duplicate handling is intentional.

## Additional Notes

- The live JCM URL still resolves, but it currently reports `Nothing found`; this review therefore treats MediaDive J1226 and TOGO M1318 as mirrors of a historical JCM record rather than as independent primary evidence.
