# YAML Record Review: LB Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/lb_medium__2df88211.yaml
- Started UTC: 2026-09-23T19:13:56Z
- Finished UTC: 2026-09-23T19:15:04Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:008037 |
| Name | lb_medium |
| Original name | LB Medium |
| Category | bacterial |
| Physical state | SOLID_AGAR |
| Media grounding | TOGO:M1492 |
| Source provenance | Togo M1492 imported from NBRC_M275, merged with Togo M2042 / NBRC_M1340 |
| Generated file | data/merge_yaml/merged/lb_medium__2df88211.yaml |
| Maintained full-strength owner | data/normalized_yaml/bacterial/lb_medium.yaml |
| Maintained 1/3 variant owner | data/normalized_yaml/bacterial/1_3_lb.yaml |
| Merge fingerprint | 2df8821150f0a1b9de668720dee513f429a882f0fdcc08dcf75ac376ceece6a6 |

The reviewed target is a generated merge that collapsed the full-strength NBRC LB Medium record with its one-third concentration variant. The maintained normalized owners have since been repaired to an explicit parent/child concentration-variant relationship, so the next correction is regeneration rather than direct editing under `data/merge_yaml/merged/`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema validation | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/lb_medium__2df88211.yaml`. |
| Strict validator | Passed with zero error rows in `/private/tmp/lb_medium__2df88211.strict.tsv`. |
| LinkML reference validator | Passed; the reference validator completed with zero checks because this generated record has no `references` collection. |
| LinkML term validator | Passed. |
| Embedded curation history | Not checked: the repository history validator validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

NBRC Medium 275 and Togo M1492 identify the full-strength `LB Medium` recipe with 10 g/L peptone, 5 g/L yeast extract, 5 g/L NaCl, 15 g/L optional agar, 1 L distilled water, and pH 7.0. NBRC Medium 1340 and Togo M2042 identify `1/3 LB` with 3.33 g/L peptone, 1.67 g/L yeast extract, 5 g/L NaCl, 15 g/L optional agar, 1 L distilled water, and pH unadjusted.

The generated record's canonical ID and `TOGO:M1492` media term identify full-strength LB Medium, but the generated ingredients use one-third LB peptone and yeast extract concentrations. The record therefore no longer denotes its canonical source recipe even though it records `1_3_lb` as a concentration variant in `variant_children`.

An ignored-inclusive search over `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive` for `CultureMech:008037`, `TOGO:M1492`, `TOGO:M2042`, `NBRC_M275`, the NBRC 275 URL token, and the merge fingerprint found the generated target, both maintained owners, source indexes, and no additional overlay for this exact merge.

## Evidence

Supported in the inspected sources and maintained owners:

- NBRC Medium 275 and Togo M1492 support the full-strength LB Medium identity, 10 g/L peptone, 5 g/L yeast extract, 5 g/L NaCl, 15 g/L optional agar, 1 L water, and pH 7.0.
- NBRC Medium 1340 and Togo M2042 support the 1/3 LB identity, 3.33 g/L peptone, 1.67 g/L yeast extract, 5 g/L NaCl, 15 g/L optional agar, 1 L water, and pH unadjusted.
- The maintained `lb_medium.yaml` and `1_3_lb.yaml` files both already preserve those distinct source formulas and link them with `CONCENTRATION_VARIANT` rather than treating them as duplicate recipes.

Unsupported or stale in the generated record:

- The full-strength `TOGO:M1492` record contains 3.33 g/L peptone and 1.67 g/L yeast extract, values that belong only to 1/3 LB.
- The generated record stores water as `1 G_PER_L`, but both inspected source recipes encode 1 L water.
- The generated record omits the pH distinction that separates pH 7.0 full-strength LB from pH-unadjusted 1/3 LB.
- The generated record has no structured references and therefore exposes no checkable link to either NBRC source page.

## Completeness

The generated record is materially incomplete and misleading because it conflates a parent medium with a lower-strength concentration variant. It also predates the September 2026 repairs that restored water, pH, ingredient groundings, structured references, and the parent/child variant relationship in the maintained owners.

Empty optional organism, incubation, storage, and discussion fields are acceptable; NBRC Medium 275 and 1340 are formulation pages and do not assert a strain-specific growth outcome in these source records.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Blocker | The generated merge gives full-strength NBRC LB Medium the peptone and yeast extract concentrations of 1/3 LB. | NBRC 275 and Togo M1492 list 10 g peptone and 5 g yeast extract per liter; NBRC 1340 and Togo M2042 list 3.33 g peptone and 1.67 g yeast extract per liter. The generated `TOGO:M1492` record has the 1/3 LB amounts. | `data/merge_yaml/merged/` regeneration from repaired `lb_medium.yaml` and `1_3_lb.yaml` |
| Major | The generated record still represents 1 L distilled water as `1 G_PER_L`. | Both inspected source recipes list distilled water as 1 L, and both repaired maintained owners use `1000 ML_PER_L`. | `data/merge_yaml/merged/` regeneration from repaired normalized owners |
| Major | The generated record omits pH, losing the distinction between pH 7.0 LB Medium and pH-unadjusted 1/3 LB. | NBRC 275 and Togo M1492 specify pH 7.0; NBRC 1340 and Togo M2042 specify pH unadjusted. | `data/merge_yaml/merged/` regeneration from repaired normalized owners |
| Minor | The generated record lacks structured references. | The maintained owners now carry Togo and NBRC references, while the generated reference validator performed zero checks. | `data/merge_yaml/merged/` regeneration from repaired normalized owners |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` from the maintained normalized corpus so full-strength `lb_medium` and `1_3_lb` are emitted as distinct parent and concentration-variant records.
2. Ensure merge fingerprinting does not merge concentration variants after the September 2026 repairs.
3. Regenerate downstream pages and indexes so the NBRC LB page no longer publishes one-third LB peptone and yeast extract amounts under `TOGO:M1492`.

## Follow-up Checks

- Rerun open schema validation, the strict validator, the reference validator, and the term validator on the regenerated `lb_medium` output.
- Manually verify that the regenerated full-strength record has 10 g/L peptone, 5 g/L yeast extract, 5 g/L NaCl, 1000 ml/L water, 15 g/L optional agar, and pH 7.0.
- Manually verify that the 1/3 LB child has 3.33 g/L peptone, 1.67 g/L yeast extract, 5 g/L NaCl, 1000 ml/L water, 15 g/L optional agar, and no pH adjustment.
- Re-run an ignored-inclusive search for `TOGO:M1492`, `TOGO:M2042`, and `2df8821150f0a1b9de668720dee513f429a882f0fdcc08dcf75ac376ceece6a6` to confirm that the old merged fingerprint is gone.

## Additional Notes

The generated record is schema-valid because its merged 1/3 LB amounts are numerically legal. The defect is a stale semantic merge, already corrected at the maintained normalized layer.
