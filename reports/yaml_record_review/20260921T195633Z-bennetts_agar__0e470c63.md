# YAML Record Review: Bennett's Agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bennetts_agar__0e470c63.yaml
- Started UTC: 2026-09-21T19:55:00Z
- Finished UTC: 2026-09-21T19:56:33Z
- Verdict: needs curation

## Target

- Reviewed `data/merge_yaml/merged/bennetts_agar__0e470c63.yaml`.
- Class: `MediaRecipe`.
- ID: `CultureMech:009750`.
- Name: `bennetts_agar`.
- Original name: `Bennett's Agar`.
- Source identity: `TOGO:M36`, label `Bennett's Agar`; TOGO metadata says `original_media_id: JCM_M44` and links JCM GRMD 44.
- Generated status: derived merge output under `data/merge_yaml/merged/`; the maintained owner is `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml`.
- Merge metadata: one source record, `TOGO_M36_Bennett_s_Agar`, on fingerprint `0e470c633d1bcf3affb62a9548fdc64cebf28ce006884071dd279a11af8f6a43`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bennetts_agar__0e470c63.yaml` via the no-project Python 3.11 workaround | Passed |
| Closed-schema strict validation, `scripts/validate_strict.py data/merge_yaml/merged/bennetts_agar__0e470c63.yaml --out /private/tmp/bennetts_agar__0e470c63.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/bennetts_agar__0e470c63.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks were applicable |
| `linkml-term-validator validate-data data/merge_yaml/merged/bennetts_agar__0e470c63.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not a focused embedded-history validator for one merged record |

The documented `just` commands were not run directly because the project runtime currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation.

## Identity and Grounding

- `CultureMech:009750` resolves in `data/culturemech_id_registry.tsv` to `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml`.
- TOGO M36, JCM GRMD 44, the `Bennett's Agar` label, and the source URL all denote JCM Bennett's Agar.
- The reviewed TOGO M36 record is an active sibling of the JCM/DSMZ Bennett's Agar merge; it remains separated by a different fingerprint because it retains a malformed water row and TOGO-specific supplier strings.
- Glucose, agar, and water are grounded to exact ChEBI terms. Yeast extract, beef extract, and N-Z amine are correctly left as undefined mixture ingredients, but the N-Z amine spelling carries TOGO's `N--Z` rendering rather than the inspected JCM source's `N-Z`.

## Evidence

- TOGO M36 supports the source identity, its `JCM_M44` provenance, pH 7.3, 1 L distilled water, 10 g glucose, 15 g agar, 1 g yeast extract from BD-Difco, 2 g N-Z amine type A from Sheffield, 1 g beef extract from BD-Difco, and the instruction to adjust pH to 7.3.
- The live JCM GRMD 44 page agrees with TOGO M36 on every inspected amount, the JCM source identity, and pH 7.3.
- The maintained owner stores TOGO's 1 L distilled water item as `value: '1'` with `unit: G_PER_L`; this is neither the source unit nor a dimensionally meaningful final water concentration.
- The maintained owner drops TOGO's pH 7.3 metadata and pH-adjustment comment entirely: `ph_value` is absent and `preparation_steps` is empty.
- A gitignore-independent exact search over the maintained owner, generated merge, ID registry, and recipe catalog for `CultureMech:009750`, `TOGO:M36`, `TOGO_M36_Bennett_s_Agar`, `JCM_M44`, and the exact JCM GRMD 44 token found only the expected owner and generated record references. It did not find a raw TOGO M36 or JCM 44 capture committed for this source record.

## Completeness

- Consequentially incorrect: distilled water has the wrong unit.
- Consequentially incomplete: supported pH 7.3 and pH-adjustment preparation data are absent.
- Empty target-organism and growth-evidence slots are acceptable here; TOGO M36 and JCM GRMD 44 do not name a strain-specific growth result.
- Optional chemical groundings are appropriately empty for the three complex mixture ingredients.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | TOGO's `1 L` distilled water row was normalized as `1` `G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml` |
| Major | TOGO M36 and JCM GRMD 44 both support pH 7.3 and the pH-adjustment instruction, but `ph_value` and `preparation_steps` are absent. | `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml` |
| Minor | The preferred term `N--Z amine, type A (Sheffield)` preserves TOGO's doubled hyphen instead of JCM's `N-Z amine` spelling. | `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml` |

## Recommended Edits

1. Correct the distilled-water amount to source-faithful `1 L` or omit water consistently if this importer treats solvent water as implied.
2. Add `ph_value: 7.3` and a preparation step for pH adjustment to 7.3 from the TOGO M36 comment.
3. Normalize `N--Z amine, type A (Sheffield)` to `N-Z amine, type A (Sheffield)` while preserving the exact supplier/type detail.
4. Regenerate `data/merge_yaml/merged/bennetts_agar__0e470c63.yaml` after the normalized owner is corrected.

## Follow-up Checks

- Rerun the open schema validator, `scripts/validate_strict.py`, `linkml-term-validator`, and `linkml-reference-validator` on `data/normalized_yaml/bacterial/TOGO_M36_Bennett_s_Agar.yaml` and the regenerated merged record.
- Manually compare the regenerated record against the TOGO M36 JSON and JCM GRMD 44 to verify all six TOGO component rows, pH 7.3, and the pH-adjustment comment survive with source-faithful units.
- Run `just verify-merges` after regeneration to see whether fixing the water and pH artifacts lets this JCM-derived TOGO record converge with the canonical JCM Bennett's Agar merge.

## Additional Notes

- The fetched TOGO M36 API record and JCM GRMD 44 page agreed for every inspected ingredient amount and the pH comment.
- Reports are read-only review artifacts; no normalized recipe, generated merge, page, history entry, or GitHub issue was edited for this review.
