# YAML Record Review: DT medium (pH 3.5)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/dt_medium_ph_3_5.yaml
- Started UTC: 2026-09-22T22:22:48Z
- Finished UTC: 2026-09-22T22:24:02Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:009364` at `data/merge_yaml/merged/dt_medium_ph_3_5.yaml`.

- Label: `dt_medium_ph_3_5`; original name: `DT medium (pH 3.5)`.
- Generated status: generated merge output with `merge_fingerprint` `99dfccc0f8fa7888912e688403f8dd90d40cfd77a264954066cd0a370cc00ac0`.
- Maintained owner for direct record repair: `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`.
- Merge lineage: one source record, `dt_medium_ph_3_5`.
- Declared external source: `TOGO:M2816`, `https://togomedium.org/medium/M2816`.
- Internal solution reference: `mediadive.solution:5478` / `Trace mineral stock solution`.

## Validation

Focused validation on `data/merge_yaml/merged/dt_medium_ph_3_5.yaml`:

| Check | Command | Result |
|---|---|---|
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/dt_medium_ph_3_5.yaml` | Passed; `No issues found`. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/dt_medium_ph_3_5.yaml --out /private/tmp/dt_medium_ph_3_5.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/dt_medium_ph_3_5.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 checks, all validations passed. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/dt_medium_ph_3_5.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the expected `eutils` / `pkg_resources` warning. |
| Embedded curation history | `just validate-history` | Not checked: the available history gate validates standalone `history/` files, not embedded `MediaRecipe.curation_history` in one merged YAML. |

These validators establish syntactic and ontology-link shape only; they do not check TOGO amount semantics or whether a MediaDive stock ID denotes the same stock as a TOGO local subcomponent.

## Identity and Grounding

The generated record denotes the requested TOGO accession: the record label, original name, `media_term` preferred term, `TOGO:M2816` identifier, and TOGO API response all point to `DT medium (pH 3.5)`.

The ingredient and solution grounding is not sound:

- TOGO `M2816` has a local subcomponent named `trace mineral stock solution`; its five components are manganese chloride tetrahydrate, cobalt dichloride hexahydrate, copper(II) chloride dihydrate, zinc chloride, and iron(III) chloride at percent units.
- The record also points that stock addition at `mediadive.solution:5478`.
- MediaDive solution `5478` is a different stock recipe containing boric acid, manganese chloride tetrahydrate, zinc sulfate heptahydrate, copper(II) sulfate pentahydrate, sodium molybdate dihydrate, cobalt chloride hexahydrate, and distilled water.
- `CoCl2 x 6 H2O` is present in the TOGO subcomponent, but the generated stock component is grounded to `CHEBI:35696` / `cobalt dichloride`, not a cobalt chloride hexahydrate term.

## Evidence

Supported against inspected source records:

- TOGO `M2816` supports the record identity `DT medium (pH 3.5)`.
- TOGO `M2816` supports these main-paragraph components as gram additions: calcium chloride dihydrate `0.1`, magnesium sulfate `0.145`, potassium sulfate `3`, and sodium dihydrogen phosphate `0.5`.
- TOGO `M2816` supports the component names `dextrin` and `EZMix-N-Z-amine A`, but reports their unit as `(w/v)`, not `g`.
- TOGO `M2816` supports a `20 ul` addition of `trace mineral stock solution`.
- TOGO `M2816` supports `pH 3.5`.
- TOGO `M2816` includes a source comment that *S. islandicus* M.16.4 and derivatives were routinely cultivated in tissue culture flasks at 75 to 78 C without shaking.

Unsupported or over-scoped in the generated record:

- The record converts the `20 ul` trace-mineral stock addition into `20 G_PER_L`, which is dimensionally wrong and loses the stock-addition semantics.
- The five TOGO trace-mineral stock components are flattened into top-level `ingredients` with their stock percent units; following the generated final-medium rows would add percent levels of manganese, cobalt, copper, zinc, and ferric chloride directly to the medium.
- The generated stock reference to `mediadive.solution:5478` is not supported by TOGO `M2816`; it denotes a different stock formula.
- Dextrin and EZMix-N-Z-amine A are represented as `0.1 G_PER_L`; the inspected TOGO API reports `0.1` with unit `(w/v)`.
- Source pH 3.5 is only embedded in the name and external label, not represented as structured pH.

## Completeness

Consequential gaps:

- The local TOGO trace-mineral stock subcomponent needs to be represented as its own nested formula or stock solution with the five supported chloride components, rather than as top-level final-medium ingredients.
- The main recipe needs the `20 ul` stock addition to that local TOGO stock.
- The source pH needs structured retention.
- The `(w/v)` units for dextrin and EZMix-N-Z-amine A need a curator or importer review before conversion.
- The TOGO culture comment about *S. islandicus* M.16.4 at 75 to 78 C without shaking should be preserved if the source transform has a supported place for organism or incubation notes.

An ignored-inclusive `find data -iname '*dt_medium_ph_3_5*' -o -iname '*DT*3*5*'` search found only the generated record and `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`. An ignored-inclusive `rg --no-ignore --hidden` search for `M2816`, `dt_medium_ph_3_5`, `mediadive.solution:5478`, and `mediadive_5478_Trace_mineral_stock_solution` across `data`, `docs`, `src`, `scripts`, and `conf` found the active normalized record, the active generated record, generated indexes, and import reports; it did not find another active TOGO `M2816` record to reconcile.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The TOGO trace-mineral stock formula has been flattened into final-medium ingredients and the `20 ul` stock addition was converted to `20 G_PER_L`. | TOGO `M2816` lists `trace mineral stock solution` as a `20 ul` main component and puts the five metal chlorides in that subcomponent. The generated YAML keeps those five metal chlorides as top-level `ingredients` and stores the solution addition as `20 G_PER_L`. | `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`; if the same pattern recurs in other TOGO imports, the TOGO solution migration/import transform. |
| major | The generated solution reference points to a different MediaDive stock recipe. | The DT source stock contains manganese, cobalt, copper, zinc, and ferric chlorides. `mediadive.solution:5478` contains boric acid, manganese chloride, zinc sulfate, copper sulfate, sodium molybdate, cobalt chloride, and water, so it is not the same formula. | `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`. |
| major | Source pH and `(w/v)` units were dropped or coerced during import. | The TOGO API reports `ph: 3.5`; the YAML has no structured pH. TOGO reports dextrin and EZMix-N-Z-amine A with unit `(w/v)`, while the YAML stores each as `0.1 G_PER_L`. | `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`; the TOGO unit mapper for a general fix. |
| major | `CoCl2 x 6 H2O` is grounded to an anhydrous cobalt chloride concept. | The source component is cobalt dichloride hexahydrate, but the YAML term is `CHEBI:35696` / `cobalt dichloride`. | `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`. |

No blocker findings were found: the stable record identity points at the intended TOGO medium, and the YAML is valid. No minor-only issues were separated from the major import defects.

## Recommended Edits

1. In `data/normalized_yaml/bacterial/dt_medium_ph_3_5.yaml`, remove the five trace-mineral stock components from top-level `ingredients`, preserve them in a local TOGO `trace mineral stock solution`, and encode the medium's addition of that stock as `20 ul` rather than `20 G_PER_L`.
2. Remove the `mediadive.solution:5478` link from this TOGO record unless a future source inspection proves that the TOGO stock intentionally aliases that exact MediaDive stock; the current REST formula proves it is not an exact alias.
3. Add structured pH 3.5 and preserve the TOGO culture comment about *S. islandicus* M.16.4 cultivation if the importer has a lossless destination for those comments.
4. Revisit the unit mapping for the dextrin and EZMix-N-Z-amine A `(w/v)` rows and store a supported unit/amount pair instead of `0.1 G_PER_L`.
5. Correct the `CoCl2 x 6 H2O` term to a hydrated cobalt chloride concept that matches the supplied form.
6. Regenerate `data/merge_yaml/merged/dt_medium_ph_3_5.yaml` and downstream browser/page outputs from the maintained normalized repair.

## Follow-up Checks

- Re-run the focused LinkML, strict, reference, and term validators on the repaired normalized record and regenerated merged record.
- Re-run the merge generation and verify that the regenerated `dt_medium_ph_3_5.yaml` no longer contains top-level `MnCl2 x 4H2O`, `CoCl2 x 6H2O`, `CuCl2 x 2H2O`, `ZnCl2`, or `FeCl3` ingredient rows.
- Manually compare the regenerated record to `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2816` and confirm pH 3.5, the two `(w/v)` rows, and the `20 ul` stock addition are retained.
- Confirm no `mediadive.solution:5478` reference remains in the DT medium unless it is replaced by a curated equivalence note backed by a source.

## Additional Notes

The `https://mediadive.dsmz.de/rest/medium/2816` endpoint returned `DataNotFound`, which is consistent with `M2816` being a TOGO accession rather than DSMZ medium 2816. The review used the TOGO `M2816` API as the source recipe and the MediaDive `rest/solution/5478` endpoint only to verify the generated internal stock reference.
