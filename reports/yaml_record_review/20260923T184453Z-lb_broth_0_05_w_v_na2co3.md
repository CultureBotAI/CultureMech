# YAML Record Review: lb_broth_0_05_w_v_na2co3

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml
- Started UTC: 2026-09-23T18:43:43Z
- Finished UTC: 2026-09-23T18:44:53Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:008237` for `lb_broth_0_05_w_v_na2co3` in `data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml`.

- Source identity: TogoMedium `TOGO:M1679`, imported from original NBRC source `NBRC_M884`.
- Original source URL: `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=884`.
- Generated lineage: `merge_fingerprint: 33d7c63a583bca3d86c4a1bb2f020cccf07c8e91abdaf020e6d14accc9d815cc`, `merged_from: lb_broth_0_05_w_v_na2co3`.
- Maintained owner for fixes: `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema validation, target class `MediaRecipe`, against `src/culturemech/schema/culturemech.yaml` | Passed with "No issues found". |
| `scripts/validate_strict.py data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml --workers 1 --quiet` | Passed with 0 error rows. |
| `linkml-reference-validator validate data data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not for embedded `MediaRecipe.curation_history` blocks inside one generated YAML file. |

## Identity and Grounding

The TOGO and NBRC identifiers match the record name: TogoMedium `M1679` imports NBRC medium 884 as `LB Broth + 0.05%(w/v) Na2CO3`. The source record is an LB Lennox broth recipe supplemented with 0.5 g/L sodium carbonate, optional agar, and 1 L water.

The generated biochemical identity does not match that source closely enough. It replaces the `Bacto LB Broth, Lennox (Difco)` base with a Miller-formulation LB constituent expansion from unrelated supplier metadata, even though Lennox LB and Miller LB differ in NaCl concentration. It also moves `Na2CO3*` into an `Unknown solution` with an empty `composition`, so sodium carbonate is no longer a normal resolved component despite the source listing it directly.

## Evidence

NBRC medium 884 lists 20 g `Bacto LB Broth, Lennox (Difco)`, 0.5 g `Na2CO3*`, 15 g `Agar (if needed)`, and 1 L distilled water. Its footnote states that the starred sodium carbonate is sterilized separately by autoclaving and added to the other ingredients, with final pH around 8.0. TogoMedium M1679 exposes the same component names, amounts, pH metadata, NBRC accession, and source URL.

The generated YAML preserves the source sodium-carbonate mass as 0.5 g/L, but it loses several linked claims:

- `Distilled water` is represented as `1 G_PER_L`, not 1 L.
- The Bacto LB Broth, Lennox base is absent and has been replaced with three Miller LB constituents from a non-source `laboratorynotes.com` entry.
- `Na2CO3*` is not grounded as sodium carbonate and has been migrated into an empty `solutions` item named `Unknown solution`.
- The sodium-carbonate sterilization footnote and final pH around 8.0 are absent.
- The source calls agar optional, but the YAML is marked unconditionally `SOLID_AGAR`.

## Completeness

The record is not complete enough because it changes the LB base, omits the actual sodium-carbonate handling instruction, uses the wrong water unit, lacks pH, and blurs the optional agar state. It also has no top-level `references`; TOGO M1679 and NBRC 884 are only in `notes`, so the reference validator had no URLs to check.

No consequential source was found for incubation temperature, atmosphere, storage, target-organism evidence, or a separate sodium-carbonate stock recipe. NBRC M884 gives a separately sterilized direct sodium-carbonate addition, not a stock solution composition.

An ignored-inclusive, hidden-file-inclusive search over `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive` for `CultureMech:008237`, `TOGO:M1679`, `NBRC_M884`, `NO=884`, the source slug, and the merge fingerprint found the maintained normalized owner, this generated merge, and normalized index rows. It did not find an alternate maintained overlay or a prior archived curation report for this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The LB base is wrong. | NBRC M884 and TogoMedium M1679 list 20 g/L commercial `Bacto LB Broth, Lennox (Difco)`; the YAML substitutes tryptone, yeast extract, and 10 g/L sodium chloride from LB Miller metadata. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |
| Major | Distilled water has the wrong unit. | Both inspected source views list 1 L distilled water; the YAML stores `value: '1'` with `unit: G_PER_L`. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |
| Major | Sodium carbonate is represented as an empty unknown solution. | The source lists `Na2CO3*` as a 0.5 g/L component and says to sterilize it separately before adding it to the other ingredients; the YAML moved it to a `solutions` entry with `name: Unknown solution` and `composition: []`. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |
| Major | Final pH and separate sterilization are missing. | NBRC M884 says the final pH is around 8.0 and the starred `Na2CO3` is separately autoclaved; neither claim is structured in the YAML. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |
| Major | The physical state overstates optional agar. | The source says `Agar (if needed)`, but the YAML marks the recipe `SOLID_AGAR`. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |
| Minor | Material source URLs are present only in free text. | The generated record names TogoMedium M1679 and the NBRC 884 URL in `notes`, but it has no `references`, so the reference validator had 0 URL checks. | `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml` |

No blocker findings were found.

## Recommended Edits

1. Restore `Bacto LB Broth, Lennox (Difco)` as the source LB base unless a primary NBRC-equivalent source justifies expanding it into constituents; remove the unsupported Miller LB supplier expansion.
2. Change distilled water to a volume-preserving 1 L amount.
3. Move `Na2CO3*` back into the ingredient list, ground it to sodium carbonate if an exact term is available, and preserve its 0.5 g/L concentration.
4. Add preparation steps for separately autoclaving sodium carbonate and adding it to the other ingredients.
5. Add the final pH around 8.0 and add source references for TogoMedium M1679 and NBRC medium 884.
6. Revisit `physical_state` so optional agar is not represented as unconditional solid agar.
7. Regenerate `data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml` after the normalized YAML is curated.

## Follow-up Checks

- Re-run strict, schema, term, and reference validation on `data/normalized_yaml/bacterial/lb_broth_0_05_w_v_na2co3.yaml`.
- Re-run the merge pipeline and verify that the generated `CultureMech:008237` record carries the Bacto LB Broth, Lennox base, 1 L water, 0.5 g/L sodium carbonate, pH around 8.0, NBRC sodium-carbonate autoclave note, and source references.
- Re-run strict, schema, term, and reference validation on the regenerated `data/merge_yaml/merged/lb_broth_0_05_w_v_na2co3.yaml`.
- Manually recompare the regenerated file against NBRC medium 884 and TogoMedium M1679, especially the starred `Na2CO3` footnote and optional agar row.

## Additional Notes

This review did not create or mutate GitHub issues, pull requests, comments, labels, settings, source YAML, generated YAML, or rendered pages.
