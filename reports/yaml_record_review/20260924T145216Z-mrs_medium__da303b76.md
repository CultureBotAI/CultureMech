# YAML Record Review: MRS Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_medium__da303b76.yaml
- Started UTC: 2026-09-24T14:51:03Z
- Finished UTC: 2026-09-24T14:52:16Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:009018` / `mrs_medium` at `data/merge_yaml/merged/mrs_medium__da303b76.yaml`.

- Generated source: `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml`
- Merge source: `TOGO_M2439_MRS_Medium`
- Merge fingerprint: `da303b76f0dd1d4e0aa4b0684441d2051267fafa5b3ddf900597b8c4e2c4ae3a`
- Category: `bacterial`
- Medium term: `TOGO:M2439`, label `MRS Medium`
- Original source: DSMZ Medium 11 PDF

## Validation

| Check | Result |
|---|---|
| Open LinkML schema with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__da303b76.yaml` | Passed. |
| Strict validator with `scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__da303b76.yaml --out /private/tmp/mrs_medium__da303b76.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator with `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__da303b76.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were applicable. |
| Term validator with `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__da303b76.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml` records, not embedded `MediaRecipe.curation_history` events. |

## Identity and Grounding

The record identifies TOGO M2439, a TOGO import of DSMZ Medium 11, MRS MEDIUM. TOGO reports `gm` `http://togomedium.org/medium/M2439`, name `MRS Medium`, source URL `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium11.pdf`, and pH `6.2 - 6.5`; the inspected DSMZ PDF is titled `11. MRS MEDIUM`.

TOGO M2439 and the DSMZ PDF support every non-water ingredient amount in the generated YAML:

| Source row | YAML row | Review |
|---|---|---|
| MgSO4 x 7 H2O, 0.2 g | `MgSO4 x 7 H2O`, 0.2 `G_PER_L` | Amount and primary `CHEBI:31795` grounding supported; the `mediaingredientmech_chebi_term` still points at generic `CHEBI:32599`. |
| Yeast extract, 5 g | `Yeast extract`, 5 `G_PER_L` | Supported. |
| K2HPO4, 2 g | `K2HPO4`, 2 `G_PER_L`, `CHEBI:131527` | Supported. |
| Na-acetate, 5 g | `Na-acetate`, 5 `G_PER_L`, `CHEBI:32954` | Supported. |
| Tween 80, 1 g | `Tween 80`, 1 `G_PER_L`, `CHEBI:53426` | Supported. |
| MnSO4 x H2O, 0.05 g | `MnSO4 x H2O`, 0.05 `G_PER_L`, `CHEBI:86364` | Supported. |
| (NH4)3 citrate, 2 g | `(NH4)3 citrate`, 2 `G_PER_L`, `CHEBI:63037` | Supported. |
| Glucose, 20 g | `Glucose`, 20 `G_PER_L`, `CHEBI:17234` | Supported. |
| Meat extract, 10 g | `Meat extract`, 10 `G_PER_L` | Supported; unresolved complex ingredient is acceptable. |
| Casein peptone, tryptic digest, 10 g | `Casein peptone, tryptic digest`, 10 `G_PER_L` | Supported; unresolved complex ingredient is acceptable. |

The exact `CultureMech:009018` search across `data/normalized_yaml` and `data/merge_yaml` used `rg --no-ignore --hidden`. It found one maintained YAML owner, this generated merge, and generated indexes only.

## Evidence

The DSMZ PDF lists the MRS MEDIUM solutes and 1000 ml distilled water, then instructs adjusting pH to 6.2-6.5. The TOGO M2439 API preserves the same source URL, same solute formula, same 1000 ml water row, and pH 6.2-6.5 as both metadata and a comment.

The YAML changes the 1000 ml water row into 1000 `G_PER_L`, omits the source pH and pH-adjustment instruction, and contains a stale `mediaingredientmech_chebi_term` on the magnesium sulfate heptahydrate row.

TOGO M2439 and MediaDive medium 11 are both direct imports of DSMZ Medium 11. This generated record and `data/merge_yaml/merged/mrs_medium__abcd760b.yaml` therefore denote the same DSMZ source formula but remain separate generated records.

## Completeness

The recipe is incomplete because the DSMZ pH and pH-adjustment instruction are missing. Its water row is present but dimensionally incorrect. Target organisms and growth evidence are empty optional slots in this record; TOGO M2439 and DSMZ Medium 11 only define the recipe.

## Findings

### Major

1. The distilled-water row uses a mass concentration for a source volume.
   - Evidence: TOGO M2439 and the DSMZ PDF both list `Distilled water`, 1000 ml; the maintained and generated YAML store `1000 G_PER_L`.
   - Impact: the final-volume solvent is dimensionally wrong and should not be interpreted as a weighed solute.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__da303b76.yaml`.

2. The source pH and preparation instruction are missing.
   - Evidence: TOGO reports pH `6.2 - 6.5`, and both TOGO and the DSMZ PDF state to adjust pH to that range.
   - Impact: the generated recipe lacks the final pH target and pH-adjustment operation from DSMZ Medium 11.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__da303b76.yaml`.

3. The magnesium sulfate heptahydrate row has conflicting CHEBI fields.
   - Evidence: the source and `preferred_term` are MgSO4 x 7 H2O, and the primary `term` is correctly `CHEBI:31795`; the colocated `mediaingredientmech_chebi_term` still contains generic `CHEBI:32599` / magnesium sulfate.
   - Impact: downstream consumers that read the legacy MIM migration field instead of `term` will see a less exact ingredient identity than the row and source require.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml`, then regenerate `data/merge_yaml/merged/mrs_medium__da303b76.yaml`.

4. DSMZ Medium 11 remains split across direct TOGO and MediaDive imports.
   - Evidence: TOGO M2439 cites the DSMZ Medium 11 PDF directly, while `CultureMech:000647` / `mediadive.medium:11` also imports DSMZ Medium 11 as MRS MEDIUM.
   - Impact: the same provider formulation is emitted as at least two generated records instead of a single merged recipe.
   - Owner: `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml`, `data/normalized_yaml/bacterial/mrs_medium.yaml`, and the merge/de-duplication rule for equivalent DSMZ sources.

### Minor

None found.

### Blocker

None found.

## Recommended Edits

1. Convert the `Distilled water` row in `data/normalized_yaml/bacterial/TOGO_M2439_MRS_Medium.yaml` from `1000 G_PER_L` to a supported 1000 ml volume representation.
2. Add pH 6.2-6.5 and a pH-adjustment preparation step from TOGO M2439 / DSMZ Medium 11.
3. Refresh the MgSO4 x 7 H2O `mediaingredientmech_chebi_term` so it agrees with the `CHEBI:31795` primary term, or remove the legacy link if exact heptahydrate MIM linkage is not supported.
4. Mark TOGO M2439 and MediaDive medium 11 as equivalent DSMZ Medium 11 sources or update merge normalization so the direct duplicate no longer emits a separate generated record after both inputs are complete.
5. Append a `curation_history` event to each changed normalized source and regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

After curation, run the same focused generated-record checks:

- `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium__da303b76.yaml`
- `python scripts/validate_strict.py data/merge_yaml/merged/mrs_medium__da303b76.yaml --workers 1 --quiet`
- `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium__da303b76.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium__da303b76.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`

Also rerun the merge freshness and merge audit checks that own `data/merge_yaml/merged/` to confirm TOGO M2439 and MediaDive medium 11 no longer publish as separate DSMZ Medium 11 records.

## Additional Notes

None found.
