# YAML Record Review: 1379

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1379.yaml`
- Started UTC: 20260921T044029Z
- Finished UTC: 20260921T044141Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007475` |
| Label | `1379` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1379.yaml` or merge regeneration |
| Merge owner | `merged_from: [NBRC_1379]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1379.yaml` |

This generated record is the Aug 6 merge of an NBRC direct import whose import history points at NBRC Medium 1376. The maintained owner later recovered the five-row MY50G table, added `nbrc.medium:1376`, and resolved the official title as `MY50G`.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1379.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1379.yaml --out /private/tmp/1379.strict.tsv --workers 1 --quiet` | Pass; command exited 0 and TSV at `/private/tmp/1379.strict.tsv` contained only the header |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1379.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1379.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The target's source identity is NBRC Medium 1376:

- The generated import history says `Media No. 1376` and links the legacy NBRC `NO=1376` page.
- Live NBRC Medium 1376 is titled `MY50G`.
- The maintained owner now has `name: MY50G`, `media_term: nbrc.medium:1376`, the direct NBRC 1376 reference, and the five ingredients listed by the NBRC table.

Two identity gaps remain in the generated artifact:

- The generated record is still labeled `1379`; live NBRC Medium 1379 is the unrelated `Medium for Acidithiobacillus ferrooxidans`.
- The generated artifact has no `media_term`, no source reference, and no ingredient groundings.

## Evidence

Supported by inspected source text:

- NBRC Medium 1376 lists malt extract 20 g, yeast extract 5 g, sucrose 500 g, distilled water 1 L, and agar 20 g.
- The inspected NBRC 1376 page has no pH row or preparation note.

Unsupported or malformed in the generated record:

- The first ingredient row concatenates all five NBRC ingredient rows into one `preferred_term`.
- The second ingredient row has an empty `preferred_term` and stores `Malt extract` as a concentration value.
- The generated artifact has no separate rows for the yeast extract, sucrose, water, or agar amounts that are already present in the maintained owner.
- The generated file omits `nbrc.medium:1376`, the direct NBRC reference, and all ingredient term mappings.
- The generated `ADJUST_PH` and autoclaving steps are not in the inspected NBRC 1376 table.

## Completeness

- The generated file omits the Aug 19 recovered NBRC composition, the Aug 20 MIM groundings, and the Sep 8 NBRC source-metadata repair in `data/normalized_yaml/bacterial/NBRC_1379.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRC_1379`, `NO=1376`, `NBRC Medium 1376`, `nbrc.medium:1376`, and `CultureMech:007475` found the stale generated NBRC direct import, the repaired normalized owner, its ID/catalog entries, and a separate TOGO-derived `my50g` copy of the same NBRC source.
- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `NBRCMediumDetailServlet?NO=1379`, `nbrc.medium:1379`, and `Medium for Acidithiobacillus ferrooxidans` found no NBRC direct import for live NBRC Medium 1379, but did find the separate TOGO-derived `medium_for_acidithiobacillus_ferrooxidans` record.
- Empty pH, preparation, and growth-evidence slots are acceptable in the normalized NBRC 1376 source import because the NBRC table does not state pH, a preparation sequence, or an organism-specific growth observation.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated merge is stale and malformed relative to its repaired normalized owner. | The Aug 6 generated file still has the pre-recovery two-row parse, while `data/normalized_yaml/bacterial/NBRC_1379.yaml` now carries five NBRC 1376 ingredients, `nbrc.medium:1376`, the official `MY50G` title, and the direct NBRC reference. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1379.yaml`; fix merge source selection if the stale rows persist. |
| Blocker | The generated label `1379` is misleading for an NBRC Medium 1376 record. | The import event points to `NO=1376`, live NBRC 1376 is `MY50G`, and live NBRC 1379 is the distinct `Medium for Acidithiobacillus ferrooxidans`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1379.yaml`, whose label has already been repaired. |
| Major | The five-row NBRC table is collapsed into two malformed rows. | NBRC Medium 1376 has five component rows; the generated file has one concatenated formula blob and one empty ingredient. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1379.yaml`. |
| Major | NBRC source grounding, direct reference, and ingredient groundings are missing from the generated record. | The maintained owner now represents `nbrc.medium:1376`, the direct source URL, and term mappings for malt extract, yeast extract, sucrose, water, and agar, all absent from the generated file. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1379.yaml`. |
| Major | Placeholder preparation steps are unsupported. | The inspected NBRC 1376 page supplies only a composition table; it does not state a pH adjustment or autoclaving step, and the maintained owner now has `preparation_steps: []`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1379.yaml`. |
| Minor | A TOGO-derived copy of the same NBRC Medium 1376 remains separate. | Gitignore-independent search found `my50g` with the same NBRC 1376 source URL; that TOGO copy has `Distilled water` as `1 G_PER_L` and a parent-variant relation not present on the NBRC direct owner. | Merge-fingerprint rules for NBRC/TOGO duplicate handling after the direct NBRC import is regenerated. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` and confirm the generated MY50G artifact carries the five recovered ingredient rows, `nbrc.medium:1376`, the NBRC 1376 reference, and the Sep 8 curation event from `data/normalized_yaml/bacterial/NBRC_1379.yaml`.
2. If regeneration preserves the concatenated formula blob, fix the NBRC merge source path that is still using the pre-recovery import output.
3. Review duplicate handling between the NBRC direct import and the TOGO `my50g` copy, preserving any deliberate TOGO parent/variant relationship if those records merge.
4. Review the `medium_for_acidithiobacillus_ferrooxidans` TOGO record to decide whether live NBRC Medium 1379 needs an NBRC direct normalized owner or whether the existing TOGO-derived record is enough.

## Follow-up Checks

- `just verify-merges` to prove generated outputs are fresh after regeneration.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1376>.yaml` after regeneration.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1376>.yaml` for the grounded subset.
- Manual comparison to live NBRC Medium 1376 for all five ingredient rows.
- Manual duplicate check against `data/normalized_yaml/bacterial/my50g.yaml`.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated record has no `references` block.
