# YAML Record Review: bhiy_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BHIY_AGAR.yaml
- Started UTC: 2026-09-21T21:33:58Z
- Finished UTC: 2026-09-21T21:35:10Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated record | `data/merge_yaml/merged/BHIY_AGAR.yaml` |
| ID | `CultureMech:007865` |
| Label | `bhiy_agar` / `BHIY Agar` |
| Category | `bacterial` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` |
| Source | TOGO M132, original source JCM M141 |
| Merge fingerprint | `3371e1e1ed9c813a9fd3fbf646e19f88daabff3908a434dba0fb45d8ed3a560d` |
| Merge inputs | `TOGO_M132_BHIY_Agar` |

The reviewed file is a derived merge record generated from the TOGO M132
normalized record.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BHIY_AGAR.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BHIY_AGAR.yaml --out /private/tmp/BHIY_AGAR.strict.tsv --workers 1 --quiet` | Passed with 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BHIY_AGAR.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file, 0 checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BHIY_AGAR.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded curation history | `just validate-history` | Not checked: the documented focused history validator targets standalone files under `history/`; no documented record-local embedded `curation_history` validator exists for one merge record |

Direct `just` validation remains blocked by the project environment's Python
3.13 build of `llvmlite==0.46.0` before those recipes reach this file; the
no-project Python 3.11 commands above exercised the same focused validators.

## Identity and Grounding

- `CultureMech:007865`, `media_term.id: TOGO:M132`, and source notes agree on
  TOGO Medium M132 / BHIY Agar, whose original source is JCM_M141.
- The Togo SPARQList API for M132 reports `name: BHIY Agar`,
  `original_media_id: JCM_M141`, `ph: 7.4`, source URL
  `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=141`, and the expected
  four source ingredients.
- The hidden-inclusive exact search for
  `CultureMech:007865|3371e1e1ed9c813a9fd3fbf646e19f88daabff3908a434dba0fb45d8ed3a560d|TOGO_M132_BHIY_Agar`
  covered normalized bacterial YAML, merged YAML, report files, and caches; it
  found only `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` as the
  normalized owner and this generated merge for `CultureMech:007865`.
- `data/normalized_yaml/bacterial/bhiy_agar.yaml` / `CultureMech:002500` is a
  separate direct MediaDive/JCM J141 import of the same JCM formulation and
  generated as `data/merge_yaml/merged/bhiy_agar__c89652dd.yaml`.

## Evidence

The inspected Togo M132 source has one main solution:

| Source ingredient | Source amount |
|---|---:|
| Distilled water | 1 L |
| Agar | 15 g |
| Yeast extract (BD-Difco) | 20 g |
| Brain heart infusion (BD-Difco) | 37 g |

It also carries one preparation comment: `Adjust pH to 7.4.`

The generated record preserves 15 g/L agar and 20 g/L yeast extract, but it
does not otherwise preserve the source formula exactly.

## Completeness

- The M132 `ph: 7.4` source value is absent from the TOGO normalized owner and
  from this generated merge.
- The M132 `Adjust pH to 7.4.` source comment is absent from
  `preparation_steps`.
- No target-organism or growth-metric claim is expected for this Togo recipe.
- No stock solutions are expected; all source components are direct additions to
  the 1 L main solution.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The 1 L water row was converted to `1 G_PER_L`. | Togo M132 reports Distilled water as 1 L in the main solution; `BHIY_AGAR.yaml` records `Distilled water` as `value: '1'`, `unit: G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` and the Togo importer/unit conversion path |
| Major | The source's 37 g Brain Heart Infusion product was decomposed into unsupported inferred constituents. | Togo M132 lists `Brain heart infusion (BD-Difco)`, 37 g; the generated record instead contains variable Calf brains, variable Beef heart, 10 g/L Proteose peptone, 2 g/L Dextrose, 5 g/L Sodium chloride, and 2.5 g/L Disodium phosphate rows derived from a product reconstruction. | `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` and the commercial-product decomposition rule |
| Major | The pH value and pH-adjustment step from M132 are missing. | The inspected M132 API returns `ph: 7.4` and comment `Adjust pH to 7.4.`; this record has no `ph_value` and no `preparation_steps`. | `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml` or the Togo importer comment/pH mapping |
| Major | Two active normalized records denote the same JCM J141 BHIY AGAR formulation but do not merge. | `TOGO_M132_BHIY_Agar.yaml` points to original JCM_M141, while `data/normalized_yaml/bacterial/bhiy_agar.yaml` is a direct JCM J141 import; their generated products have different fingerprints because the Togo copy has the erroneous water row and lacks pH/prep. | Both normalized BHIY AGAR records plus merge canonicalization after the source-level fixes |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M132_BHIY_Agar.yaml`, keep the source
   formula as 1 L Distilled water, 15 g Agar, 20 g Yeast extract (BD-Difco),
   and 37 g Brain Heart Infusion (BD-Difco); do not replace the commercial BHI
   component with calf-brain/beef-heart/product constituents unless those rows
   are modeled as a nested product composition rather than final-medium
   additions.
2. Add `ph_value: 7.4` and an `ADJUST_PH` preparation step that preserves the
   M132 `Adjust pH to 7.4.` comment.
3. Align the TOGO M132 and direct JCM J141 normalized records so they can merge
   or be intentionally de-duplicated under one canonical BHIY Agar record.

## Follow-up Checks

- Rerun focused schema, strict, term, and reference validation on the corrected
  TOGO M132 normalized owner.
- Re-fetch or inspect Togo M132 after the edit and compare the four source rows,
  units, pH, and comment field by field.
- Regenerate merge products, then run `just verify-merges` and
  `just audit-merge-freshness` to confirm the generated `BHIY_AGAR` record is
  fresh and the JCM duplicate is either merged or explicitly retained.

## Additional Notes

- The direct MediaDive/JCM record already carries `ph_value: 7.4` and
  `Adjust pH to 7.4.`, which helps confirm the Togo comment/pH omission on
  this target.
