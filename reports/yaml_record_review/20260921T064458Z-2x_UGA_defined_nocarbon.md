# YAML Record Review: 2x UGA defined nocarbon

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml`
- Started UTC: 2026-09-21T06:40:58Z
- Finished UTC: 2026-09-21T06:44:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:015480` |
| Name | `2x UGA defined nocarbon` |
| Category | `specialized` |
| Source | `CultureBotHT` |
| Source database ID | `2x UGA defined nocarbon` |
| Source URL | `https://github.com/CultureBotAI/CultureBotHT` |
| Import source file | `2x_UGA_defined_nocarbon.yaml` |
| Merge fingerprint | `2968c1efb9c3697e6947f60c561c6f111bfd55171ee160e1ce201086bd6f8bee` |
| Maintained owner | `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated CultureBotHT import for `2x_UGA_defined_nocarbon.yaml`. Future corrections belong in `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml --out /private/tmp/2x_UGA_defined_nocarbon.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The CultureMech identity is internally consistent. The generated record has ID `CultureMech:015480`, carries `name: 2x UGA defined nocarbon`, and is a one-to-one merge of `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml`.

The source identity is only partially auditable. The `sources` entry records `CultureBotHT` and `database_id: 2x UGA defined nocarbon`, while the importer history records `Imported from CultureBotHT repository: 2x_UGA_defined_nocarbon.yaml`. The local `scripts/import_from_culturebotht.py` importer writes that filename from a source file in CultureBotHT's `media_yaml` directory, but it preserves only the repository root URL in `sources`.

All 32 ingredient rows have term IDs and labels. The manganese sulfate monohydrate row now uses `CHEBI:86364`, matching the record's later CHEBI regrounding history rather than the stale `CHEBI:75219` row flagged by older label-plausibility reports.

## Evidence

The record describes a liquid, defined 2x UGA nocarbon variant with five basal salts, Wolfe's mineral mix components, Wolfe's vitamin mix components, and two sodium phosphate rows. It carries the free-text note `Source: FEBA media definitions`.

The maintained normalized owner is byte-for-byte equivalent in curated fields to the generated target: generated-only `merge_fingerprint` and `merged_from` are the only substantive additions in `data/merge_yaml/merged/2x_UGA_defined_nocarbon.yaml`.

The upstream source could not be verified. A raw GitHub fetch for `media_yaml/2x_UGA_defined_nocarbon.yaml` returned `404: Not Found`; `https://api.github.com/repos/CultureBotAI/CultureBotHT/contents` returned a JSON `404`; unauthenticated GitHub code search returned `401`; and a status check for the recorded `https://github.com/CultureBotAI/CultureBotHT` repository URL returned HTTP 404.

## Completeness

Consequential gaps:

- The recorded CultureBotHT URL is only a repository root and currently resolves to HTTP 404, so this review cannot verify the recipe against immutable upstream source text.
- The record has no structured `references`, so the CultureBotHT source URL and FEBA provenance are not validator-visible.
- The `has_unmapped_ingredients` data-quality flag is stale now that all 32 ingredient rows have `term.id` values.

Empty optional slots:

- `solutions` is absent. The inspected CultureMech record is a flat recipe and does not encode named source stock solutions.
- `ph_range`, `preparation_steps`, `target_organisms`, and `growth_metrics` are absent. The accessible CultureMech provenance identifies FEBA media definitions via CultureBotHT but does not include enough source context for a pH, preparation, organism, or growth claim.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2x_UGA_defined_nocarbon.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `scripts`, `src`, `data`, and `.github` for `2x_UGA_defined_nocarbon.yaml` found only the normalized owner and this generated merge record.
- A gitignore-independent `rg --no-ignore --hidden` search over `scripts`, `src`, `data`, and `.github` for `2x UGA defined nocarbon` found the normalized owner, this generated merge record, and generated indexes/manifests.
- A gitignore-independent `find` under `/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs` for `*2x_UGA_defined_nocarbon.yaml` found only this generated merge record in CultureMech. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Upstream source provenance is not reproducible from the record. | The CultureBotHT source records only a repository-root URL and the import filename. The recorded GitHub URL now returns HTTP 404, the GitHub Contents API reports 404 for the repo, and exact gitignore-independent local searches did not find `2x_UGA_defined_nocarbon.yaml` outside CultureMech's own normalized and generated records. | `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` |
| Minor | Structured source references are absent. | The only source detail is in `sources` and free-text notes/history; no `references` entries cite CultureBotHT or FEBA, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` |
| Minor | `data_quality_flags` still reports unmapped ingredients. | The target lists `has_unmapped_ingredients`, but all 32 ingredient rows include `term.id` values and pass the term validator. | `data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` |

## Recommended Edits

1. Replace or supplement `sources[0].url` with an immutable, directly retrievable CultureBotHT source URL for `media_yaml/2x_UGA_defined_nocarbon.yaml`, ideally pinned to a commit.
2. Add structured `references` for the CultureBotHT source file and any FEBA upstream definition that supplied the recipe.
3. Remove `has_unmapped_ingredients` once all ingredient rows remain grounded after source verification.
4. Recompare the source `2x_UGA_defined_nocarbon.yaml` values against the maintained owner after the original source is reachable, paying specific attention to the ammonium chloride, potassium chloride, and doubled Wolfe mix and sodium phosphate values relative to the 1x `UGA_defined_nocarbon.yaml` sibling.
5. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` after curation.
- `just validate-strict data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` after curation.
- `just validate-terms data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` after removing the stale flag.
- `just validate-references data/normalized_yaml/specialized/2x_uga_defined_nocarbon.yaml` after adding structured CultureBotHT and FEBA references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against immutable CultureBotHT source YAML before treating the 2x UGA formula as source-verified.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
- The 1x CultureBotHT sibling `UGA_defined_nocarbon.yaml` omits the leading `2x`, has its own source filename, and differs from this formula by more than a uniform twofold multiplier; without upstream source text, that difference is review context rather than a confirmed defect.
