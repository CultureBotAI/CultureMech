# YAML Record Review: togo_medium_m1613

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1613.yaml
- Started UTC: 2026-09-25T14:45:00Z
- Finished UTC: 2026-09-25T14:49:07Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008167
- Name: togo_medium_m1613
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1613.yaml`.
- Source identity in the generated record: TOGO:M1613, original source NBRC_M813, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=813`.
- Merge lineage in the generated record: only `togo_medium_m1613`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1613.yaml`; the command exited 0 with no diagnostics.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1613.yaml --out /private/tmp/m1613.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1613.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it reported zero checks.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1613.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1613 resolves to NBRC_M813. The source identity, TOGO identifier, and NBRC source URL agree.
- The record is not merged with a sibling or variant.
- The amountless FeCl3 row is supported by the source comment that only a trace amount of FeCl3 is added.

## Evidence

- TOGO M1613 and NBRC 813 support 50 g NaCl, 2 g KCl, 1 g MgSO4 x 7 H2O, 0.36 g CaCl2 x 2 H2O, 0.23 g NaBr, 0.06 g NaHCO3, 5 g Proteose-Peptone No.3, 10 g Bacto Yeast Extract, 1 g glucose, 1 L distilled water, 15 g agar if needed, pH 7.2-7.4, and a trace amount of FeCl3.
- The generated record preserves all non-water ingredient rows and correctly leaves FeCl3 with a variable amount.
- The generated record encodes 1 L distilled water as 1 `G_PER_L` and omits the source pH range.

## Completeness

- Consequential missing or wrong fields: 1 L water as final volume, pH 7.2-7.4, and optional agar scope.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1613` found no existing report for this record stem.

## Findings

- major: `data/normalized_yaml/bacterial/togo_medium_m1613.yaml` stores the 1 L distilled-water source row as 1 g/L.
- minor: the normalized owner omits the NBRC_M813 pH 7.2-7.4 value and treats optional agar as an unconditional solid physical state.

## Recommended Edits

- Convert the distilled-water row to a final-volume representation matching 1 L.
- Add pH 7.2-7.4 from NBRC_M813.
- Preserve the conditional agar scope instead of treating the whole medium as unconditionally solid.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1613.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1613.yaml` and verify water, pH, FeCl3, and optional agar scope.

## Additional Notes

- Exact source inspection was bounded to TOGO M1613 and NBRC 813.
