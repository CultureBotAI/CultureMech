# YAML Record Review: aswp__9c90436e

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/aswp__9c90436e.yaml
- Started UTC: 2026-09-21T16:22:56Z
- Finished UTC: 2026-09-21T16:24:25Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/aswp__9c90436e.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000319` |
| Label | `aswp` |
| Original label | `ASWP` |
| Category | `bacterial` |
| Source identity | `mediadive.medium:C12`; CCAP `MR_ASWP.pdf` |
| Source URL | `https://www.ccap.ac.uk/wp-content/uploads/MR_ASWP.pdf` |
| Generated status | Generated on 2026-08-06 from `data/normalized_yaml/bacterial/aswp.yaml` |
| Merge fingerprint | `9c90436ecd37b8de5de24868a7da0e1375394d78838c8bcf940983e9734941d8` |

`data/merge_yaml/merged/` is derived output. Future fixes belong in
`data/normalized_yaml/bacterial/aswp.yaml`, the MediaDive/CCAP importer, or a
deliberate consolidation with the direct CCAP ASWP algae owner; then this
generated file should be regenerated.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/aswp__9c90436e.yaml` | Pass. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/aswp__9c90436e.yaml --out /private/tmp/aswp__9c90436e.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/aswp__9c90436e.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/aswp__9c90436e.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass. The only emitted message was the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked | No focused validator for embedded `MediaRecipe.curation_history` is documented for one merged recipe. `just validate-history` targets standalone records under `history/`. |

Direct `just` validation was not used because this checkout currently reaches a
project `uv` build of `llvmlite==0.46.0` under Python 3.13 before target-specific
validation and fails in `setuptools` with `TypeError: Popen.__init__() got an
unexpected keyword argument 'dry_run'`.

## Identity and Grounding

- The ID, `media_term`, source URL, generated filename suffix, and
  `merged_from` value identify the bacterial MediaDive/CCAP C12 ASWP import, not
  the direct CCAP algae owner reviewed in `data/merge_yaml/merged/ASWP.yaml`.
- The source PDF confirms the same ASWP, CCAP's Artificial Seawater for
  Protozoa, reviewed on 2026-05-29.
- The source organismal scope is algae/protozoa, while this imported duplicate
  is filed as `bacterial`.
- `Sea Salt`, `Tricine`, NaNO3, Na2HPO4, and K2HPO4 have plausible ingredient
  identities, but the ASW Stock 1 salts are present at incorrect materialized
  concentrations and should be scoped to a stock added at 3.75 ml/L.

## Evidence

| Claim in generated record | Review |
|---|---|
| 33.6 g/L `Sea Salt` | Amount supported, with a precision loss: CCAP specifies `Instant Ocean` sea salts. |
| Tricine at 0.5 g/L | Supported as a direct ASWP main-medium row. |
| NaNO3 at 5.625 g/L, Na2HPO4 at 0.225 g/L, and K2HPO4 at 0.188 g/L | Not supported. CCAP lists those three rows inside ASW Stock 1 at 15.00, 0.60, and 0.50 g/L, then adds 3.75 ml of that stock per litre. If materialized as final concentrations, the generated values are 100-fold too high. |
| `Soil` with `VARIABLE` concentration | Not supported as a direct ASWP ingredient. CCAP lists Stock solution 2 as 50.0 ml/L SE2. |
| Make-up, pH, and autoclave text | Supported as top-level ASWP preparation. |
| Soil-selection and soil-extract autoclave prose as top-level ASWP steps | Wrong boundary for this record. The ASWP PDF only references an SE2 stock recipe; any SE2 procedure should be sourced and nested on that stock, not run after the main ASWP autoclave step. |

## Completeness

- `find data/normalized_yaml \( -name 'ASWP.yaml' -o -name 'aswp.yaml' \)
  -print` searched independently of `.gitignore` and found both
  `data/normalized_yaml/bacterial/aswp.yaml` and the same-stem direct CCAP owner
  at `data/normalized_yaml/algae/aswp.yaml`.
- An ignored-file-inclusive exact search for `CultureMech:000319`,
  `mediadive.medium:C12`, `aswp__9c90436e`, `ASWP`, and `MR_ASWP` across
  `data`, `reports`, `history`, and `.claude` found this bacterial owner, the
  direct algae owner, generated records, indexes, label-plausibility reports,
  and archived validation rows.
- `find reports/yaml_record_review -maxdepth 1 -name
  '*aswp__9c90436e.md'` searched the ignored report directory and found no prior
  review for this exact generated record stem before this report was written.
- Empty `target_organisms` and `growth_metrics` are acceptable for this
  source-formulation record.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | ASW Stock 1 is flattened and materialized with 100-fold too much NaNO3, Na2HPO4, and K2HPO4. | The source adds 3.75 ml/L of a stock containing 15.00, 0.60, and 0.50 g/L of those salts; the generated direct rows are 5.625, 0.225, and 0.188 g/L. | `data/normalized_yaml/bacterial/aswp.yaml` or the MediaDive/CCAP importer. |
| major | SE2 is missing as a 50.0 ml/L stock solution and replaced by variable raw soil. | CCAP lists Stock solution 2 / SE2 in the main ASWP recipe; the generated record has only `Soil` with `VARIABLE` concentration. | Same maintained bacterial owner or importer. |
| major | SE2 preparation prose is attached to ASWP top level without explicit SE2 provenance. | The inspected ASWP PDF references the separate SE2 recipe but does not define the full soil-selection and soil-extract procedure copied into steps 2-4. | Same maintained bacterial owner, a maintained SE2 stock record, or importer. |
| minor | The `Sea Salt` ingredient loses the source's commercial mix name. | CCAP specifies `Instant Ocean` sea salts; the generated row says only `Sea Salt`. | Same maintained bacterial owner or importer. |

## Recommended Edits

1. Decide whether the bacterial MediaDive/CCAP C12 record and the direct algae
   CCAP `ASWP` record should be merged, deduplicated, or retained as source
   variants before repairing generated ASWP output.
2. Re-model ASW Stock 1 as a stock added at 3.75 ml/L instead of direct
   over-concentrated NaNO3, Na2HPO4, and K2HPO4 final-medium rows.
3. Replace `Soil` with a Stock solution 2 / SE2 solution addition at 50.0 ml/L
   and move the SE2 procedure onto that stock with a direct SE2 source.
4. Preserve `Instant Ocean` in the 33.6 g/L sea-salts ingredient name or note.
5. Regenerate `data/merge_yaml/merged/aswp__9c90436e.yaml` after the normalized
   owner and any source-specific importer rule are repaired.

## Follow-up Checks

- Rerun open-schema, strict, term, and reference validation on the normalized
  ASWP C12 owner after correction.
- Recalculate Stock solution 1 dilution from 3.75 ml/L and confirm the
  regenerated row no longer has 5.625 g/L NaNO3.
- Confirm the regenerated ASWP C12 merge has a structured 50.0 ml/L SE2 entry
  instead of variable `Soil`.
- Re-run duplicate-stem review for the algae and bacterial ASWP owners after
  curation.

## Additional Notes

- The direct algae ASWP owner has already removed a preparation-text
  pseudo-ingredient that is not present in this bacterial C12 import; however,
  both same-stem owners still need solution-aware handling for ASW Stock 1 and
  SE2.
