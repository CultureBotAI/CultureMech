# YAML Record Review: rye_bran_mrs

- Repository: CultureMech
- Record: `data/merge_yaml/merged/rye_bran_mrs.yaml`
- Started UTC: `2026-09-25T03:21:18Z`
- Finished UTC: `2026-09-25T03:21:18Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `rye_bran_mrs`, a single-source merge of `data/normalized_yaml/bacterial/TOGO_M753_RYE-Bran_MRS.yaml`.

The record represents TOGO Medium `M753`, a JCM Medium 730 `RYE-BRAN MRS` import. The normalized source has already been repaired to link TOGO `M753` to the MediaDive JCM `J730` record as a source duplicate and to keep Rye-bran extract as a nested 900 ml/L solution, but the generated merge YAML flattens that stock back into the parent ingredient list.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with no diagnostics.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The TOGO identity is correct for this generated record: `TOGO:M753` names JCM Medium 730, `RYE-BRAN MRS`, as its original source. The normalized source records the duplicate relationship to `mediadive.medium:J730`, but this generated YAML still exists separately under the TOGO identity and was reviewed as emitted.

## Evidence

TOGO `M753` and the live JCM 730 page list a main medium containing 55 g Lactobacilli MRS broth, 900 ml Rye-bran extract, and 100 ml distilled water.

The source defines Rye-bran extract as its own 1 L subrecipe containing 40 g rye-bran, 2 g malted wheat meal, 0.8 g Trypsin (Sigma-Aldrich), and 1 L distilled water. It then instructs the curator to mix the extract components, incubate at 50 C for 24 h, clarify by centrifugation at 500 x g for 15 min, and paper-filter the resulting turbid liquid.

## Completeness

The generated record preserves the 55 g Lactobacilli MRS broth row and the bran-extract solids at their source amounts.

It loses or corrupts these details:

- The 900 ml/L Rye-bran extract aliquot is represented as `900 G_PER_L`.
- The 100 ml parent distilled water and 1 L Rye-bran-extract water are merged into one `101.0 G_PER_L` row.
- Rye-bran, malted wheat meal, and trypsin are flattened from the Rye-bran-extract subrecipe into the parent recipe.
- The 50 C for 24 h incubation, 500 x g centrifugation, and paper-filtration instructions are absent from the generated record.
- The curated `SOURCE_DUPLICATE` link to the MediaDive JCM `J730` record is dropped from the generated merge YAML.

## Findings

- `needs curation`: The merge output flattens the repaired Rye-bran extract solution. JCM and TOGO use 900 ml Rye-bran extract per final medium, but the generated record emits a parent `Rye--bran extract (see below)` row at `900 G_PER_L` plus separate direct parent rows for 40 g/L rye-bran, 2 g/L malted wheat meal, and 0.8 g/L trypsin.
- `needs curation`: Water from different scopes was merged and converted to mass concentration. The generated `101.0 G_PER_L` Distilled water row is derived from 100 ml final-medium water and 1 L extract water.
- `needs curation`: The generated file loses the normalized source's preparation steps for producing Rye-bran extract: 50 C incubation for 24 h, 500 x g clarification for 15 min, and paper filtration.
- `needs curation`: The generated file drops the normalized source's `parent_media` / `variant_relationship: SOURCE_DUPLICATE` relationship to the MediaDive JCM `J730` record, so the already-curated source duplicate is not visible in the generated artifact.
- `pass with minor issues`: Lactobacilli MRS broth remains ungrounded in the generated record even though the normalized source carries a `culturemech_term` for `CultureMech:009017`.
- `pass with minor issues`: The generated preferred terms preserve TOGO's doubled hyphens in `Rye--bran` and `Rye--bran extract`, while the normalized repair had already canonicalized those labels.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/rye_bran_mrs.yaml`; fix the solution-preserving merge path so repaired nested solutions in `data/normalized_yaml/bacterial/TOGO_M753_RYE-Bran_MRS.yaml` survive regeneration.
- Preserve Rye-bran extract as a nested 900 ml/L parent aliquot with 40 g/L rye-bran, 2 g/L malted wheat meal, 0.8 g/L trypsin, and 1 L distilled water scoped to the extract.
- Keep the final-medium 100 ml distilled water separate from the Rye-bran-extract 1 L distilled water.
- Carry the three extract preparation steps from the normalized source into the generated record.
- Preserve or materialize the curated source-duplicate link to MediaDive JCM `J730` so the TOGO copy and direct JCM copy do not remain disconnected.
- Preserve the `CultureMech:009017` grounding for Lactobacilli MRS broth during merge generation.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on the normalized TOGO source and regenerated merge YAML.
- Regenerate `data/merge_yaml/merged/rye_bran_mrs.yaml` and verify that Rye-bran extract remains a nested 900 ml/L solution rather than a `900 G_PER_L` ingredient.
- Verify that the regenerated file has no direct parent rows for rye-bran, malted wheat meal, or trypsin.
- Verify that the 50 C incubation, 500 x g centrifugation, and paper-filtration steps are retained.
- Run an exact ignored-file-inclusive search for `TOGO_M753_RYE-Bran_MRS`, `mediadive.medium:J730`, and `rye_bran_mrs` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The normalized TOGO source is more complete than the generated YAML here; the primary problem is that generation discards the repaired nested solution structure.
