# YAML Record Review: mwc__f5ba97ae

- Repository: CultureMech
- Record: data/merge_yaml/merged/mwc__f5ba97ae.yaml
- Started UTC: 2026-09-24T15:55:35Z
- Finished UTC: 2026-09-24T15:57:29Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mwc__f5ba97ae.yaml`.

The generated record represents DSMZ/MediaDive medium 1828:

- canonical `id`: `CultureMech:001255`
- canonical `media_term`: `mediadive.medium:1828`
- canonical `name`: `mwc`
- canonical `original_name`: `MWC`
- `medium_type`: `DEFINED`
- `composition_type`: `DEFINED`
- `physical_state`: `LIQUID`
- `ph_range`: 7.0-8.0
- `ingredients`: 18
- `solutions`: 0
- `merged_from`: `DSMZ_1828_MWC` and `mwc`

## Validation

- Open schema validation: Passed with `No issues found`.
- Strict schema validation: Passed with 0 errors; the TSV contained only the
  header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked; `just validate-history` validates
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The high-level DSMZ 1828 identity is correct. The merge provenance is
ambiguous, because the generated record says it merged `DSMZ_1828_MWC` and
`mwc` without retaining a second path or provider ID.

An exact ignored-file-inclusive search for `CultureMech:001255`,
`mediadive.medium:1828`, and `DSMZ, ID: 1828` found one active normalized owner
for DSMZ 1828:

- `CultureMech:001255`:
  `data/normalized_yaml/bacterial/DSMZ_1828_MWC.yaml`

An exact ignored-file-inclusive search for `CultureMech:000370`,
`CultureMech:000094`, `mediadive.medium:C59`, and `CCAP:MWC` found two other
active MWC owners, both from CCAP rather than DSMZ:

- `CultureMech:000370`: `data/normalized_yaml/bacterial/mwc.yaml`
- `CultureMech:000094`: `data/normalized_yaml/algae/mwc.yaml`

MediaDive's current DSMZ 1828 and CCAP C59 recipes have different provider
identities and different MWC stock-addition recipes, so the CCAP MWC records
must remain distinct from this DSMZ 1828 record.

## Evidence

The current MediaDive REST record for DSMZ 1828 defines a 1000 ml `Main sol.
1828` with 0.115 g TES, seven main stock additions, 1 ml `Combined trace
element solution*`, and 1 ml `MWC Vitamin mix**`.

The DSMZ 1828 main stock additions are volumes, not final grams per liter:
2 ml NaNO3 stock at 42.5 g/L, 1 ml MgSO4 x 7 H2O stock at 36.97 g/L, 1 ml
CaCl2 x 2 H2O stock at 36.76 g/L, 2.62 ml K2HPO4 x 3 H2O stock at 4.35 g/L,
0.4 ml NaHCO3 stock at 31.5 g/L, and 1 ml Na2SiO3 x 5 H2O stock at 21.2 g/L.

The current MediaDive record also defines `Combined trace element solution*`
as a separate 1000 ml stock and `MWC Vitamin mix**` as a separate 100 ml stock.
The maintained `data/normalized_yaml/bacterial/DSMZ_1828_MWC.yaml` owner
partially recognized this on 2026-08-07 by moving six trace entries and three
vitamin entries into nested `solutions`, but that repair was not complete and
the generated YAML still predates it.

## Completeness

The generated record flattens every DSMZ 1828 stock and emits no `solutions`
array. It also omits the water used to make the 1000 ml main solution, the
1000 ml combined trace solution, and the 100 ml vitamin mix.

The six main mineral-stock additions after `TES` are wrong because their ml
addition volumes were copied as g/L concentrations. For example, NaNO3 is listed
at 2 g/L instead of as 2 ml of a 42.5 g/L stock, and K2HPO4 x 3 H2O is listed
at 2.62 g/L instead of as 2.62 ml of a 4.35 g/L stock.

The combined trace solution and vitamin mix have a nested-stock problem of
their own. CuSO4 x 5 H2O, ZnSO4 x 7 H2O, CoCl2 x 6 H2O, MnCl2 x 4 H2O, and
Na2MoO4 x 2 H2O are ml-scale additions to the 1 L trace stock, but the
generated record carries their addition volumes as g/L. Biotin and Vitamin B12
are likewise copied from 0.1 ml and 0.25 ml additions to the 100 ml vitamin mix
as 0.1 g/L and 0.25 g/L.

## Findings

- The generated record is stale relative to the 2026-08-07 partial nesting
  repair in the maintained DSMZ owner.
- The main 1000 ml water, combined trace-stock water, and vitamin-mix water are
  missing.
- Six main stock-solution additions were cast from ml/L volumes into g/L
  concentrations.
- The combined trace-element and vitamin stock components remain at stock
  strength or at copied stock-addition volumes instead of being nested cleanly.
- The generated `merged_from` value `mwc` is ambiguous in a repository that also
  has active CCAP MWC records; DSMZ 1828 and CCAP C59 should not be source
  duplicates.

## Recommended Edits

- Continue repairing `data/normalized_yaml/bacterial/DSMZ_1828_MWC.yaml`, not
  the generated YAML, so every DSMZ stock addition is either nested or scaled.
- Move the six main mineral-stock additions into explicit stock additions, or
  scale them from their MediaDive stock concentrations into final g/L values.
- Finish the `Combined trace element solution*` curation so CuSO4 x 5 H2O is
  nested and all trace-stock ml additions are represented as additions to the
  trace stock, not as g/L.
- Finish `MWC Vitamin mix**` by representing the Biotin and Vitamin B12 primary
  stocks instead of casting their 0.1 ml and 0.25 ml additions to g/L.
- Preserve main, trace-stock, and vitamin-stock water.
- Regenerate the merged YAML and confirm the `merged_from` provenance no longer
  ambiguously suggests a CCAP MWC source duplicate.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the
  regenerated YAML.
- Confirm the regenerated record contains nested Combined trace element
  solution and MWC Vitamin mix entries.
- Confirm `NaNO3`, `MgSO4 x 7 H2O`, `CaCl2 x 2 H2O`, `K2HPO4 x 3 H2O`,
  `NaHCO3`, and `Na2SiO3 x 5 H2O` are not emitted with their ml addition
  volumes as g/L concentrations.
- Confirm an ignored-file-inclusive exact search for `mediadive.medium:C59` and
  `CCAP:MWC` still resolves to distinct CCAP MWC records after regeneration.

## Additional Notes

The active CCAP C59 and CCAP algae MWC owners share the display name `MWC` but
are not DSMZ 1828 source duplicates. They should be reviewed in their own
generated records.
