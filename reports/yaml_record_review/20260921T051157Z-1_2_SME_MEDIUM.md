# YAML Record Review: 1_2_sme_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2_SME_MEDIUM.yaml`
- Started UTC: 20260921T051040Z
- Finished UTC: 20260921T051157Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:002907` |
| Slug | `1_2_sme_medium` |
| Original name | `1/2 SME MEDIUM` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owner | `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` |
| Source accession | `mediadive.medium:J559`, JCM Medium 559 |
| Merge fingerprint | `90c53a6aad024ffd14b884f526442e47fdfcb40fdb90f8fe3cba351e682cb8d6` |

Reviewed the singleton generated merge for JCM Medium 559. A gitignore-independent exact search for `CultureMech:002907`, `mediadive.medium:J559`, and the merge fingerprint found only this generated record, its normalized owner, and their expected catalog/index rows; a separate gitignore-independent raw/import/reference search found only the `deep_research_priority.json` inventory entry for this exact ID/source pair.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2_SME_MEDIUM.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2_SME_MEDIUM.yaml --out /private/tmp/1_2_SME_MEDIUM.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2_SME_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2_SME_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validator wrappers remain blocked by the project `llvmlite==0.46.0` build under Python 3.13; the no-project `uv` commands above were used as focused equivalents.

## Identity and Grounding

- The record correctly denotes JCM Medium 559 / `1/2 SME MEDIUM`.
- JCM 559 lists 15 solute rows plus 1 L distilled water. The generated record carries all 15 solutes but omits distilled water.
- The gram-based salts and sodium sulfide amounts match the one-liter source; the milligram rows are correctly normalized: SrCl2 x 6 H2O 7.5 mg to `0.0075 G_PER_L`, KI 0.05 mg to `5e-05 G_PER_L`, and resazurin 1.0 mg to `0.001 G_PER_L`.
- The pH and anaerobic preparation text match the JCM page in substance: omit Na2S x 9H2O from the initial mix, distribute under N2-CO2 4:1, add Na2S as a 5% solution, adjust to pH 6.0, autoclave at 110 C for 1 hr, replace the gas phase with H2-CO2 4:1, and pressurize inoculated bottles to 200 kPa.
- `Sulfur (powder)` is grounded to `CHEBI:26833` / sulfur atom. The source is a bulk sulfur powder, and the local divergence report maps `Sulfur (powder)` to `CHEBI:33403` / elemental sulfur instead.
- `KI` has the correct primary `CHEBI:8346` term but still carries deprecated `mediaingredientmech_term: MediaIngredientMech:000232` instead of the CHEBI-keyed mirror field.

## Evidence

- The inspected JCM 559 HTML page supports every non-water amount, the pH, the 110 C autoclave step, the 5% Na2S addition boundary, the N2-CO2 and H2-CO2 gas atmospheres, and the 200 kPa post-inoculation pressure.
- The same page supports a 1.0 L distilled-water row after the Na2S x 9H2O row; neither the generated merge nor the maintained owner contains that row.
- `src/culturemech/data/mediaingredientmech/label_index.csv` maps `KI` to `CHEBI:8346`, consistent with the record's primary term.
- `data/import_tracking/reports/mim_grounding_divergences.tsv` maps `Sulfur (powder)` to elemental sulfur, which is more specific than the generated record's `sulfur atom` grounding.
- Broader name searches find other TOGO `1/2 SME` imports, including TOGO M563 from JCM 559, but those are not listed under this generated record's `merged_from` and need separate duplicate/variant review.

## Completeness

- Consequential gap: 1 L distilled water is missing from the ingredient list.
- Consequential gap: the sulfur row uses a wrong ontology class for powdered elemental sulfur.
- Non-blocking provenance gap: the JCM URL is only free text in `notes`; no structured `references` row exists.
- Non-blocking audit gap: the June 5 MIM migration curation event says legacy MIM links were replaced with CHEBI-keyed mirrors, but the KI row still has a legacy `mediaingredientmech_term`.
- The record has no target-organism evidence; that is acceptable because the JCM source page is a recipe page and does not make an organism-specific growth claim.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is missing. | JCM 559 lists 1.0 L distilled water after the Na2S x 9H2O row; `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` has no water ingredient. | `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` |
| Major | Sulfur powder is grounded to the wrong ontology class. | The row denotes JCM `Sulfur (powder)` at 10 g/L but uses `CHEBI:26833` / sulfur atom. The local divergence report maps `Sulfur (powder)` to `CHEBI:33403` / elemental sulfur. | `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` |
| Minor | The KI row still has a legacy MIM mirror field. | The row has exact primary `CHEBI:8346` grounding and the local label index maps `KI` to `CHEBI:8346`, but `mediaingredientmech_term` still points at deprecated `MediaIngredientMech:000232`. | `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` |
| Minor | The JCM source is not represented as a structured reference. | The URL appears only in the top-level `notes`, so focused reference validation performs 0 checks. | `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` |

## Recommended Edits

1. Add the JCM 559 `Distilled water` row to `data/normalized_yaml/bacterial/1_2_sme_medium.yaml` as `1000 ML_PER_L`.
2. Reground `Sulfur` from `CHEBI:26833` / sulfur atom to `CHEBI:33403` / elemental sulfur and preserve the source label `Sulfur (powder)`.
3. Replace the KI row's legacy `mediaingredientmech_term` mirror with `mediaingredientmech_chebi_term: CHEBI:8346`.
4. Add a structured reference for `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=559`.
5. Keep the current anaerobic preparation semantics, but split the single long `AUTOCLAVE` entry into ordered steps if the curation layer supports preserving the gas replacement and post-inoculation pressurization as distinct actions.

## Follow-up Checks

- Re-run strict, term, and reference validation for `data/normalized_yaml/bacterial/1_2_sme_medium.yaml`.
- Re-run merge generation and verify `data/merge_yaml/merged/1_2_SME_MEDIUM.yaml` contains the water row, elemental-sulfur grounding, structured JCM reference, and CHEBI-keyed KI mirror.
- Separately review the TOGO M563/M3145 `1/2 SME` records to determine whether they should merge with or remain variants of this direct JCM import.

## Additional Notes

- `1_2_sme_medium_for_ya` is a distinct `1/2 SME MEDIUM FOR YA` record and was not treated as a duplicate of this JCM 559 target.
- The current preparation statement is compressed but source-aligned; it should not be dropped while adding the missing water and grounding fixes.
