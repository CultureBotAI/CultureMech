# YAML Record Review: 1% TRYPTONE AGAR

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_tryptone_agar.yaml`
- Started UTC: 2026-09-21T05:53:20Z
- Finished UTC: 2026-09-21T05:54:47Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:002718` |
| Name | `1_tryptone_agar` |
| Original name | `1% TRYPTONE AGAR` |
| Category | `bacterial` |
| Source term | `mediadive.medium:J359`, `1% TRYPTONE AGAR` |
| Merge fingerprint | `71b310f236d0c907497b43c390c146c0e741572c56941d5cb61bb1a499fd132e` |
| Maintained owner | `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is a direct MediaDive/JCM import of JCM Medium 359. The maintained owner was repaired on 2026-09-11, so this generated artifact is stale relative to the source record that owns future fixes.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_tryptone_agar.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_tryptone_agar.yaml --out /private/tmp/1_tryptone_agar.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_tryptone_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_tryptone_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes MediaDive / JCM Medium 359, 1% Tryptone Agar. The live JCM `GRMD=359` page resolves and supports the `1% TRYPTONE AGAR` identity, the three-row formula, pH 7.0, and the JCM default autoclave condition.

The current generated grounding is incomplete:

- `Agar` is grounded to `CHEBI:2509`, matching the generic agar material but not preserving the source's Bacto agar product label.
- `Tryptone` is left ungrounded, which is acceptable for a BD-Difco digest mixture.
- The source distilled-water row is absent, so water grounding is stale-missing in the generated artifact.

The maintained owner now has exact `Tryptone (BD-Difco)`, `Bacto agar (BD-Difco)`, and `Distilled water` rows with source notes and a `SOURCE_DUPLICATE` link to the TOGO M353 JCM mirror.

## Evidence

The inspected JCM Medium 359 page supports:

- 10.0 g Tryptone (BD-Difco).
- 15.0 g Bacto agar (BD-Difco).
- 1.0 L Distilled water.
- pH adjustment to 7.0.
- Default sterilization by autoclaving at 121C for 15 min unless otherwise stated.

The inspected TOGO M353 payload mirrors the same JCM_M359 source URL, same three rows, and same pH 7.0. The current MediaDive J359 page also resolves to the same formula and pH.

The generated record preserves only the two non-water ingredient amounts and the pH adjustment. It lacks the water row, the exact BD-Difco labels, JCM default autoclaving, the maintained owner's structured reference, and the maintained owner's link to the TOGO M353 source duplicate.

## Completeness

Consequential gaps:

- The 1.0 L distilled-water row is absent.
- The Tryptone and Bacto agar product labels are underspecified.
- The 121C, 15 min default JCM autoclave condition is absent.
- The generated record has only a free-text JCM URL; the maintained owner's structured reference is stale-missing.
- The generated record is not linked to the repaired TOGO M353 source duplicate.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected source pages verify the recipe but do not support a target-organism assertion.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` for `CultureMech:002718`, `mediadive.medium:J359`, `GRMD=359`, `JCM_M359`, the direct merge fingerprint, and `1_tryptone_agar` found the direct JCM owner, the stale direct generated merge, the TOGO M353 JCM owner and generated merge, the TOGO M1839 NBRC owner and generated merge, and source index rows.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact is stale and omits source-supported water. | JCM Medium 359 lists 1.0 L Distilled water, and `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` now carries a `Distilled water` row; the generated merge still has only Tryptone and Agar rows. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` |
| Major | The generated artifact is stale and omits the JCM default autoclave condition. | The JCM page says to autoclave media at 121C for 15 min unless otherwise stated, and the maintained owner now stores that as an `AUTOCLAVE` preparation step and structured `sterilization`; the generated merge has only `ADJUST_PH`. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` |
| Minor | The generated ingredient labels are stale and less exact than the source. | JCM Medium 359 lists `Tryptone (BD-Difco)` and `Bacto agar (BD-Difco)`; the generated merge still uses generic `Tryptone` and `Agar`, while the maintained owner has exact product labels. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` |
| Minor | Structured references and source-duplicate linkage are stale-missing. | The maintained owner has a structured JCM reference and links TOGO M353 as a `SOURCE_DUPLICATE`; the generated merge still has only free-text `notes`, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `1_tryptone_agar.yaml` receives the maintained owner's water row, exact BD-Difco labels, structured JCM reference, data-quality flags, JCM autoclave condition, and TOGO M353 `SOURCE_DUPLICATE` link.
2. After regeneration, check whether the direct JCM record and repaired TOGO M353 generated record merge into one canonical source-duplicate output.
3. Keep TOGO M1839 separate unless a future review explicitly establishes that NBRC Medium 1072 is the same source formulation rather than a same-name same-formula recipe from another collection.

## Follow-up Checks

- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- `just validate-strict` and `just validate-terms` on the regenerated `1_tryptone_agar.yaml`.
- `just validate-references` on the regenerated merge to confirm the structured JCM reference resolves.
- Manual comparison against JCM Medium 359 for 10.0 g Tryptone (BD-Difco), 15.0 g Bacto agar (BD-Difco), 1.0 L Distilled water, pH 7.0, and the 121C, 15 min default autoclave condition.

## Additional Notes

- The maintained normalized owner is already repaired; no additional normalized edit was identified for the direct JCM record in this review.
- TOGO M353 imports the same JCM Medium 359 source and was also repaired on 2026-09-11. The next generated record, `1_tryptone_agar__551e7d77.yaml`, is the stale generated copy of that TOGO M353 owner.
- TOGO M1839 imports NBRC Medium 1072 as another same-name 1% Tryptone Agar record; it is source-distinct from JCM Medium 359.
- `linkml-reference-validator` performed zero checks because this generated record has no structured `references` entries.
