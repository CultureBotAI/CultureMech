# YAML Record Review: ACY

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ACY.yaml
- Started UTC: 2026-09-21T09:43:09Z
- Finished UTC: 2026-09-21T09:45:04Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/ACY.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:015483`
- Label: `ACY`
- Original name: absent
- Category: `specialized`
- Media term: absent
- Generated status: generated single-source record from `data/normalized_yaml/specialized/acy.yaml`

## Validation

All narrow record-level validators passed for the generated YAML shape:

- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ACY.yaml`
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ACY.yaml --out /private/tmp/acy.strict.tsv --workers 1 --quiet`
  - Summary: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ACY.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Summary: 1 file validated, 0 total checks, all validations passed.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ACY.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Summary: term validation passed; the command also printed the expected `eutils` / `pkg_resources` deprecation warning.
- Not checked: embedded `MediaRecipe.curation_history` semantics. The repository documents `just validate-history` for standalone records under `history/`, not as a focused one-file validator for generated embedded history.

The documented `just validate-schema`, `just validate-strict`, and `just validate-terms` entrypoints still fail before target-specific validation because this project resolves `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`; the no-project Python 3.11 invocations above exercise the same validators against the target file.

## Identity and Grounding

The generated identity is a faithful copy of one local CultureBotHT import: `sources.database` is `CultureBotHT`, `sources.database_id` is `ACY`, and the curation history says `import_from_culturebotht.py` imported `ACY.yaml`. The description records this recipe as `50% ABB, 25% Columbia, 25% YCFA`.

The upstream identity is not reproducible. The only external URL preserved in the record is the CultureBotHT repository root, `https://github.com/CultureBotAI/CultureBotHT`; that GitHub page currently returns 404, the GitHub Contents API for `CultureBotAI/CultureBotHT` returns JSON `404`, and the record has no commit SHA, raw `ACY.yaml` path, archived copy, or direct FEBA definition URL.

## Evidence

Supported:

- The generated record exactly preserves the local `data/normalized_yaml/specialized/acy.yaml` source and its 42 flat ingredients.
- The local source says the formula came from FEBA media definitions through the CultureBotHT `ACY` recipe.
- `find data/normalized_yaml -name 'acy.yaml' -print` searched ignored files and found only `data/normalized_yaml/specialized/acy.yaml`.

Unsupported or over-scoped:

- The 50% ABB / 25% Columbia / 25% YCFA arithmetic could not be rechecked because the imported CultureBotHT `ACY.yaml` source file is not recoverable from the preserved source URL.
- No original FEBA page, exact CultureBotHT file URL, CultureBotHT commit SHA, or archived source text is preserved in `sources` or `references`.
- The `Tris hydrochloride` row is grounded to `CHEBI:9754` / `tris`, conflating the hydrochloride salt with the Tris base.

## Completeness

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*ACY*' -print` searched the ignored timestamped-report directory and found no pre-existing ACY report.
- Exact `rg --no-ignore --hidden` searches for `filename": "acy.yaml"`, `path": "specialized/acy.yaml"`, `source: acy.yaml`, and `^name: ACY$` covered tracked and ignored files. They found one normalized source, one generated target, and index or priority-report references.
- Exact `rg --no-ignore --hidden 'CultureBotHT|FEBA media definitions|ACY\.yaml' .` covered tracked and ignored files but did not find a raw CultureBotHT `ACY.yaml` copy or a more precise upstream URL; it found only CultureMech imports, generated records, schema/docs, tests, and previous review reports for other CultureBotHT records.
- GitHub lookups for the recorded CultureBotHT repository root and its Contents API endpoint both failed with 404.
- Optional pH, preparation, organism, growth metric, evidence, and temperature arrays are absent on the source import; without the upstream FEBA/CultureBotHT recipe, those omissions cannot be classified as source-specific defects.

## Findings

### blocker: CultureBotHT provenance is not reproducible

The ACY record only preserves `https://github.com/CultureBotAI/CultureBotHT`, a repository root that currently returns a GitHub 404. The GitHub Contents API for the same repository also returns 404, and the YAML does not pin the importer to a commit, branch path, raw `ACY.yaml` URL, archive, or FEBA source. The record may be an exact local copy, but the 42 ingredient concentrations cannot be audited against their asserted source.

### major: structured references are absent

`references` is absent, so `linkml-reference-validator` performed zero checks. The only evidence is in `sources` and curation-history prose, which is not enough to validate either the CultureBotHT source file or the FEBA media-definition provenance.

### minor: Tris hydrochloride is grounded as Tris

The final row is `Tris hydrochloride` and carries CAS `1185-53-1`, but its term is `CHEBI:9754` / `tris`. That should be checked against ChEBI and either re-grounded to the hydrochloride salt or de-grounded if no appropriate term is available.

## Recommended Edits

1. Recover the exact CultureBotHT `ACY.yaml` imported by `import_from_culturebotht.py` and add a durable, commit-pinned URL or archived source copy to `data/normalized_yaml/specialized/acy.yaml`.
2. Add the original FEBA media-definition citation for ACY, or explicitly document that CultureBotHT is the only available source of the composite 50% ABB / 25% Columbia / 25% YCFA formula.
3. Add structured `references` entries for the exact CultureBotHT and FEBA evidence so reference validation can perform nonzero checks.
4. Recheck and correct the `Tris hydrochloride` term grounding.
5. Recalculate all weighted concentrations against the recovered ACY source before marking the generated recipe source-verified.

## Follow-up Checks

- Rerun the four one-file validators above against regenerated `data/merge_yaml/merged/ACY.yaml`.
- Fetch the new durable CultureBotHT `ACY.yaml` URL and compare all 42 ingredients against the generated record.
- Search with `rg --no-ignore --hidden 'Tris hydrochloride|CHEBI:9754' data/normalized_yaml/specialized/acy.yaml data/merge_yaml/merged/ACY.yaml` and confirm the Tris hydrochloride row no longer uses the Tris-base grounding.
- Rerun an ignored-file-inclusive exact search for `database_id: ACY` or `^name: ACY$` and confirm ACY still has only one normalized source record.

## Additional Notes

- The generated record keeps `data_quality_flags: [has_unmapped_ingredients]`; unmapped undefined ingredients such as peptic digest, yeast extract, Casitone, proteose peptone, pancreatic digest of casein, and beef heart infusion are acceptable as long as they remain intentionally ungrounded mixtures.
