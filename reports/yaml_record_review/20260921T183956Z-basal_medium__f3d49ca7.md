# YAML Record Review: basal medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/basal_medium__f3d49ca7.yaml`
- Started UTC: 2026-09-21T18:38:55Z
- Finished UTC: 2026-09-21T18:39:56Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:009371` |
| Merged record | `data/merge_yaml/merged/basal_medium__f3d49ca7.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |
| Source identity | TOGO `M2825` |
| Merge status | Generated from one normalized source, `TOGO_M2825_basal_medium` |

The reviewed record is the generated singleton for TOGO M2825, named "basal medium". A gitignore-independent search for `CultureMech:009371`, `TOGO_M2825_basal_medium`, `TOGO:M2825`, `M2825`, and the merge fingerprint `f3d49ca7` across `data/normalized_yaml`, `data/merge_yaml/merged`, `reports/yaml_record_review`, `data/culturemech_id_registry.tsv`, `data/culturemech_recipe_catalog.tsv`, and `data/import_tracking/reports` found one live normalized owner plus the generated singleton and registry/index/import-tracking rows. It found no prior review report. The search included ignored files.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/basal_medium__f3d49ca7.yaml`. |
| Strict schema | Passed with `scripts/validate_strict.py`; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Reference snippets | Passed with `linkml-reference-validator validate data ...`; 1 file validated, 0 snippet checks, all validations passed. |
| Term labels | Passed with `linkml-term-validator validate-data ... --labels -c conf/oak_config.yaml`. |
| Embedded curation history | Not checked: no documented focused validator exists for embedded `MediaRecipe.curation_history` on one generated merge record; `just validate-history` targets standalone history files. |

The direct `just` validators remain unavailable in this checkout because project `uv` tries to build `llvmlite==0.46.0` under Python 3.13 and exits inside `setuptools`. I used the no-project Python 3.11 validator workaround for the focused schema, strict, reference, and term checks above.

## Identity and Grounding

The record ID, TOGO accession, label, and singleton merge source are internally consistent with the fetched TOGO M2825 payload.

The generated merge is stale relative to its normalized owner: `TOGO_M2825_basal_medium.yaml` has a `2026-08-20` curation event that grounded `5-fluorouracil` to `CHEBI:46345`, but `data/merge_yaml/merged/basal_medium__f3d49ca7.yaml` was generated on `2026-08-06` and still lacks that term.

The record should not "repair" `NH4CI` to ammonium chloride without an inspected source beyond TOGO. The TOGO payload spells the component with a final capital `I` and no amount; `data/import_tracking/reports/ungrounded_ingredients.tsv` already preserves it as unresolved.

## Evidence

TOGO M2825 directly supports thiamine at 5 ug/ml, rabbit serum at 10%, 5-fluorouracil at 100 ug/ml, 0.02 M Na2HPO4-KH2PO4 buffer, and a variable or amountless `NH4CI` row. It then defines the phosphate buffer as 1000 ml distilled water, 0.6 g KH2PO4, and 2 g Na2HPO4.

Thiamine and 5-fluorouracil were imported 1000-fold too high: 5 ug/ml is 0.005 g/L, not 5 g/L, and 100 ug/ml is 0.1 g/L, not 100 g/L. The buffer boundary is also flattened: the record stores the 0.02 M Na2HPO4-KH2PO4 buffer and the buffer's component salts as separate top-level ingredients.

The source provides no pH, atmosphere, or preparation steps, and no primary upstream URL is present in the TOGO payload.

## Completeness

The source formulation is incomplete because the two ug/ml compounds are 1000-fold high and the phosphate buffer is not scoped as its own solution. The 10% rabbit-serum row is supported only as a bare percentage in TOGO; its weight/volume interpretation should be treated as uncertain unless the upstream protocol confirms it.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Blocker | The generated merge is stale relative to the normalized owner. | The live normalized owner grounded 5-fluorouracil on `2026-08-20`; the generated merge from `2026-08-06` does not contain that exact `CHEBI:46345` term. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |
| Major | Thiamine and 5-fluorouracil are 1000-fold too high. | TOGO lists 5 ug/ml thiamine and 100 ug/ml 5-fluorouracil; the YAML stores 5 g/L and 100 g/L. | `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |
| Major | The 0.02 M Na2HPO4-KH2PO4 buffer is flattened. | TOGO nests 1000 ml water, 0.6 g KH2PO4, and 2 g Na2HPO4 under the buffer; the YAML stores all three buffer components as top-level ingredients alongside the buffer. | `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |
| Major | The buffer solvent unit is wrong. | TOGO states the buffer's distilled water as 1000 ml; the YAML stores `1000 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |
| Minor | The rabbit-serum percentage is underspecified. | TOGO gives only `10 %`; the YAML picks `PERCENT_W_V`, which is probably not the intended interpretation for a serum addition but cannot be corrected from TOGO alone. | `data/normalized_yaml/bacterial/TOGO_M2825_basal_medium.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/basal_medium__f3d49ca7.yaml` so the current 5-fluorouracil grounding is published.
2. Convert thiamine to 0.005 g/L and 5-fluorouracil to 0.1 g/L.
3. Move distilled water, KH2PO4, and Na2HPO4 under a source-local `Na2HPO4-KH2PO4 buffer` solution, preserving the 1000 ml buffer volume and 0.02 M final buffer addition.
4. Keep `NH4CI` unresolved, with a quality flag noting the likely source typo, until the underlying TOGO source can be inspected.
5. Mark the rabbit-serum unit as uncertain or inspect a primary protocol before deciding whether it is volume/volume, weight/volume, or another percentage basis.

## Follow-up Checks

1. Re-run the focused schema, strict, term, and reference validators on `data/merge_yaml/merged/basal_medium__f3d49ca7.yaml` after regeneration.
2. Re-fetch TOGO `gmdb_medium_by_gmid?gm_id=M2825` and confirm the regenerated thiamine, 5-fluorouracil, and phosphate-buffer representation match the API payload.
3. Re-run merge freshness checks and confirm `basal_medium__f3d49ca7.yaml` includes normalized changes newer than `2026-08-06`.

## Additional Notes

`data/import_tracking/reports/concentration_plausibility.tsv` already flags the thiamine unit slip and buffer water row, and `ungrounded_ingredients.tsv` already flags the unresolved `NH4CI` row. This review confirms those warnings against the TOGO M2825 API.
