# YAML Record Review: kd_1_medium__10323f04

- Repository: CultureMech
- Record: data/merge_yaml/merged/kd_1_medium__10323f04.yaml
- Started UTC: 2026-09-23T17:40:52Z
- Finished UTC: 2026-09-23T17:41:19Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:003315`, the generated `kd_1_medium__10323f04` record merged from `data/normalized_yaml/bacterial/kd_1_medium.yaml`. The record represents MediaDive/JCM `J967`, "KD-1 MEDIUM", as a bacterial, complex, undefined, liquid medium.

## Validation

- LinkML open schema validation passed for `data/merge_yaml/merged/kd_1_medium__10323f04.yaml`.
- Strict validation passed with 0 error rows in `/private/tmp/kd_1_medium__10323f04.strict.tsv`.
- Reference validation passed with 0 checks.
- Term validation passed.
- Embedded `curation_history` entries were not checked: the available history validator validates standalone files under `history/`, not embedded generated YAML history.

## Identity and Grounding

An ignored-inclusive exact search for `CultureMech:003315`, `mediadive.medium:J967`, and `GRMD=967` found the MediaDive owner, this generated record, and the TOGO-derived duplicate at `data/normalized_yaml/bacterial/TOGO_M1017_KD-1_Medium.yaml` and `data/merge_yaml/merged/kd_1_medium.yaml`.

This record and `CultureMech:007531` are both grounded to JCM 967. MediaDive identifies `J967` as JCM "KD-1 MEDIUM" and links the same JCM `GRMD=967` source URL, so the two generated records should converge once the TOGO solution placeholders and this direct JCM stock-flattening artifact are repaired.

## Evidence

JCM 967 lists a main formula containing 10 g Bacto peptone, 2 g urea, 2.5 g K2HPO4, 3 g KH2PO4, 3 g NaCl, 0.2 g MgSO4.7H2O, 10 ml trace vitamins, 10 ml trace element solution, 1 mg resazurin, and 1 L distilled water. The JCM preparation text sets final pH to 8.0, distributes the medium under N2-CO2 9:1, seals with butyl rubber stoppers, and autoclaves.

MediaDive keeps this structure as `Main sol. J967` with two nested 10 ml solution additions. It separately stores 1 L formulas for `Trace vitamins` and `Trace element solution`, so those stock concentrations are not final-medium concentrations.

## Completeness

The direct JCM import has the main formula, pH 8.0, and the main anaerobic autoclave step, but it loses the boundary between the final recipe and its two 10 ml stock additions. All trace-vitamin and trace-element stock components are top-level `ingredients`; there are no nested `solutions`; and distilled water is absent.

## Findings

- The record is a duplicate of TOGO `M1017` / JCM `M967` and should not remain as a separate generated KD-1 recipe.
- Trace vitamins and trace elements are flattened at stock strength instead of being represented as 10 ml additions. For example, MediaDive lists biotin as 2 mg per 1 L trace-vitamin stock, but the generated final record stores `0.002 G_PER_L`; a 10 ml addition to `Main sol. J967` should contribute only about 0.0000196 g/L to the final medium.
- Trace-stock salts were merged with base-medium salts. The generated `NaCl` row is `3.94118 G_PER_L` from merged `2.94118` and `1.0` rows, and `MgSO4 x 7 H2O` is `3.196078 G_PER_L` from merged `0.196078` and `3.0` rows, even though the `1.0` and `3.0` values are stock-solution g/L values.
- Trace element solution preparation steps were promoted to the main recipe. They adjust the trace stock first to pH 6.5 and then to pH 7.0, which conflicts with the actual KD-1 final pH of 8.0 when attached to the generated final record.
- The source water volumes for the main solution and stock solutions are not represented as water/solvent rows or as nested solution volumes after flattening.

## Recommended Edits

- Rebuild `data/normalized_yaml/bacterial/kd_1_medium.yaml` from the MediaDive nested solution structure rather than the flattened stock ingredient list.
- Keep `Trace vitamins` and `Trace element solution` as 10 ml additions to `Main sol. J967`; either encode their 1 L formulas as nested stocks or link them as referenced stock media.
- Prevent stock ingredients from merging with base ingredients unless their concentrations are scaled by their aliquot and final volume.
- Keep the trace-element pH steps scoped to the trace-element solution, not to KD-1 final-medium preparation.
- Merge this source with `TOGO_M1017_KD-1_Medium.yaml` after both imports produce the same structured JCM 967 recipe.

## Follow-up Checks

- Re-run the open schema, strict, reference, and term validators against the regenerated JCM record.
- Confirm the regenerated KD-1 JCM and TOGO owners have matching fingerprints or are intentionally linked as duplicate source records.
- Confirm no top-level KD-1 ingredient is sourced from the unscaled 1 L trace-vitamin or trace-element stock.
- Confirm only one generated `kd_1_medium*.yaml` remains after deduplication.

## Additional Notes

The exact search included ignored files and hidden files.
