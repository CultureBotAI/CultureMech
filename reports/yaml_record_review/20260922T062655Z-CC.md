# YAML Record Review: cc

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/CC.yaml
- Started UTC: 2026-09-22T06:25:05Z
- Finished UTC: 2026-09-22T06:26:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| class | MediaRecipe |
| id | CultureMech:001250 |
| name | cc |
| original_name | CC |
| category | bacterial |
| generated path | data/merge_yaml/merged/CC.yaml |
| generated status | Derived singleton from `data/normalized_yaml/bacterial/cc.yaml` |
| media term | `mediadive.medium:1817`, DSMZ Medium 1817 / CC |
| merge fingerprint | `13500aa2e9ac9a1a33d30822ab7af779b454a32c8eef8b40dcc4976177468921` |

`find data -iname 'cc.yaml' -print` found only two exact local YAML records, `data/normalized_yaml/bacterial/cc.yaml` and this generated target. The search covered ignored files because `find` does not honor `.gitignore`.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/CC.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/CC.yaml --out /private/tmp/CC.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| Reference integrity | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/CC.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file, 0 checks, all validations passed. |
| Term labels | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/CC.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only an `eutils` / `pkg_resources` deprecation warning was emitted. |
| Embedded history | Not checked | `just validate-history` validates standalone `history/*.yaml` files, not embedded `MediaRecipe.curation_history` blocks. |

## Identity and Grounding

The primary source identity is correct: the MediaDive medium 1817 JSON reports id 1817, name `CC`, source DSMZ, and a 1000 ml main solution. The local `media_term` agrees with `mediadive.medium:1817`.

One cross-reference is wrong: `kg_microbe_match: mediadive.medium:102` points to FLAVOBACTERIUM AQUATILE MEDIUM. Local exact search also found `mediadive.medium:102` attached to `data/normalized_yaml/bacterial/flavobacterium_aquatile_medium.yaml` and its KOMODO duplicate, which is consistent with the live MediaDive 102 payload and inconsistent with CC.

The K2HPO4 and Agar CHEBI groundings match the inspected source rows. Czapek-Dox liquid medium and `Cellobiose or Cellulose MN 300` are left ungrounded rather than forced to adjacent narrow ingredients, which is correct for this source text.

## Evidence

| Claim | Status |
|---|---|
| DSMZ / MediaDive Medium 1817 identity | Supported by the inspected MediaDive 1817 JSON. |
| Czapek-Dox liquid medium 35 g/L | Supported: MediaDive lists 35 g in `Main sol. 1817`, volume 1000 ml. |
| Cellobiose or Cellulose MN 300 30 g/L | Supported as a source-level disjunction: MediaDive lists the same combined ingredient text at 30 g in the 1000 ml main solution. |
| K2HPO4 1 g/L | Supported by MediaDive; the dipotassium hydrogen phosphate grounding is exact. |
| Agar 15 g/L | Supported by MediaDive. |
| `kg_microbe_match: mediadive.medium:102` | Unsupported; MediaDive 102 is FLAVOBACTERIUM AQUATILE MEDIUM, not CC. |

## Completeness

The MediaDive 1817 payload did not expose pH, atmosphere, sterilization, target organisms, preparation steps, source references, or a solvent ingredient row. Those empty slots are not defects in this generated record.

The MediaDive composition endpoint for medium 1817 returned the plain-text message `nothing to be shown`, so the main medium JSON was the only usable public MediaDive composition source for this review. The cited DSMZ PDF path returned an HTML noindex shell rather than a PDF.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | `kg_microbe_match` points to an unrelated MediaDive medium. | The CC record claims `mediadive.medium:102`, but the inspected MediaDive 102 JSON is FLAVOBACTERIUM AQUATILE MEDIUM with Na-caseinate, Yeast extract, Proteose peptone, K2HPO4, Agar, Distilled water, and pH 7.4. The exact local search found the same `kg_microbe_match` on `flavobacterium_aquatile_medium.yaml`, where it belongs. | Remove `kg_microbe_match: mediadive.medium:102` from `data/normalized_yaml/bacterial/cc.yaml` or replace it with a verified exact CC match. Regenerate `data/merge_yaml/merged/CC.yaml`. |

## Recommended Edits

1. Delete the stale `kg_microbe_match` from `data/normalized_yaml/bacterial/cc.yaml`, unless a checked KG-Microbe CC match is available.
2. Audit `scripts/enrich_with_kg_microbe_matches.py` outputs for other single-token or abbreviation labels that may have inherited a high-scoring but wrong MediaDive match.
3. Regenerate `data/merge_yaml/merged/CC.yaml` from the maintained owner.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validation against `data/normalized_yaml/bacterial/cc.yaml` and the regenerated `data/merge_yaml/merged/CC.yaml`.
- Search with `rg --no-ignore --hidden -n "kg_microbe_match: mediadive.medium:102" data src scripts reports` and confirm the remaining hits are limited to Flavobacterium aquatile records.
- Manually compare the regenerated CC formula against MediaDive medium 1817: Czapek-Dox liquid medium 35 g/L, Cellobiose or Cellulose MN 300 30 g/L, K2HPO4 1 g/L, and Agar 15 g/L.

## Additional Notes

- `rg --no-ignore --hidden -n "CultureMech:001250|mediadive.medium:1817|kg_microbe_match: mediadive.medium:102|data/normalized_yaml/bacterial/cc.yaml" data/normalized_yaml data/merge_yaml data/culturemech_id_registry.tsv data/culturemech_recipe_catalog.tsv reports scripts src` covered ignored files while resolving the generated target, maintained owner, and stale MediaDive 102 match.
- A broader `find data -iname '*cc*' -print` confirmed the repository has many unrelated records with `cc` in the name, including ATCC strings and archaeal coccus names. Those were not treated as ambiguous exact matches because the exact `cc.yaml` and `CultureMech:001250` searches resolved only this MediaDive 1817 owner.
