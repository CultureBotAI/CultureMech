# YAML Record Review: HD (1:10 diluted)

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/hd_1_10_diluted.yaml`
- Started UTC: 2026-09-23T11:36:27Z
- Finished UTC: 2026-09-23T11:37:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003828` |
| Name | `hd_1_10_diluted` |
| Original name | `HD (1:10 diluted)` |
| Category | `bacterial` |
| Physical state | `SOLID_AGAR` |
| pH | `5.0` |
| Generated from | `data/normalized_yaml/bacterial/KOMODO_1124_HD_1_10_diluted.yaml` and `data/normalized_yaml/bacterial/hd_1_10_diluted.yaml` |
| Source accession | `komodo.medium:1124` |
| DSMZ source | `mediadive.medium:1124` |
| Merge fingerprint | `aefeec1eab81bc6ec50d123667e9d603358631c063f3b731f2e44cd888a8f678` |

I reviewed the generated merged record, the two normalized records named in
`merged_from`, the DSMZ Medium 1124 PDF, and the MediaDive 1124 REST payload.

I searched `data/normalized_yaml` and `data/merge_yaml` YAML files with
`rg --no-ignore --hidden` for `hd_1_10_diluted`, `HD 1/10`, `HD-1`,
`1/10 DILUTED`, and `DILUTED`. Ignored files were included. The search found
this KOMODO/DSMZ HD 1:10 duplicate pair, an ASW-HD 1:10 record, and unrelated
diluted media; only `KOMODO_1124_HD_1_10_diluted.yaml` and
`hd_1_10_diluted.yaml` feed this reviewed fingerprint.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/hd_1_10_diluted.yaml` exited 0 with no diagnostics. |
| Strict schema | Passed: `python scripts/validate_strict.py data/merge_yaml/merged/hd_1_10_diluted.yaml --out /private/tmp/hd_1_10_diluted.strict.tsv --workers 1 --quiet` exited 0 and wrote a header-only TSV. |
| References | Passed with no checked references: `linkml-reference-validator validate data data/merge_yaml/merged/hd_1_10_diluted.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported `Total checks: 0`. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/hd_1_10_diluted.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported `Validation passed`. |
| Embedded history | Not checked: `just validate-history` validates standalone records under `history/`, not embedded `MediaRecipe.curation_history` entries. |

The `just` validator entrypoints were not used because this checkout attempts
to resolve a Python 3.13 environment with `llvmlite==0.46.0`; the focused
validators above were run offline with Python 3.11 against the local uv cache.

## Identity and Grounding

The merged identity is correct. KOMODO 1124 and DSMZ/MediaDive 1124 both denote
`HD (1:10 diluted)`, and the generated record marks them as
`SOURCE_DUPLICATE`, which is consistent with the same DSMZ medium number and
same ingredient signature.

The casein peptone, glucose, and yeast extract amounts match DSMZ 1124, and
the pH 5.0 value is source-supported.

## Evidence

DSMZ Medium 1124 lists 0.50 g casein peptone, 0.10 g glucose, 0.25 g yeast
extract, optional 15 g agar, and 1000 ml distilled water, then instructs
adjustment to pH 5.0.

The generated record preserves the three nutrient amounts, the optional agar
amount, and pH 5.0. It omits distilled water and promotes the optional agar
branch to top-level `SOLID_AGAR`.

## Completeness

The 1000 ml water component is missing. The only source preparation step is pH
adjustment to 5.0; it is captured by `ph_value`, but no structured
`preparation_steps` remain.

No target organisms, growth evidence, incubation temperature, atmosphere, or
stock-solution references are present in DSMZ 1124 or the MediaDive 1124
payload. The corresponding empty optional fields are not defects.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The liquid base and optional agar branch are collapsed into one `SOLID_AGAR` record. | DSMZ lists agar as `If required`; MediaDive carries `condition: "if required"`. | `data/normalized_yaml/bacterial/hd_1_10_diluted.yaml`, `data/normalized_yaml/bacterial/KOMODO_1124_HD_1_10_diluted.yaml`, or a variant-aware importer |
| Major | The 1000 ml distilled-water component is missing. | DSMZ and MediaDive both list 1000 ml distilled water. | DSMZ/KOMODO normalized owners or importers |
| Minor | The pH adjustment is not represented as a preparation step. | DSMZ says to adjust to pH 5.0; the record has `ph_value: 5.0` but no `preparation_steps`. | DSMZ/KOMODO normalized owners or importers |

## Recommended Edits

1. Preserve the liquid HD 1:10 base and optional 15 g agar branch as distinct
   variant structure instead of setting the merged top-level physical state to
   `SOLID_AGAR`.
2. Restore the 1000 ml distilled-water row.
3. Add a preparation step for pH 5.0 adjustment.
4. Apply the same correction to both source-duplicate normalized owners or to
   the import rule that populates them.
5. Regenerate merged recipes from the corrected normalized owners; do not patch
   `data/merge_yaml/merged/hd_1_10_diluted.yaml` directly.

## Follow-up Checks

1. Run `just validate data/normalized_yaml/bacterial/hd_1_10_diluted.yaml` and
   `just validate data/normalized_yaml/bacterial/KOMODO_1124_HD_1_10_diluted.yaml`
   after the normalized records are corrected.
2. Run `just validate-media-variant-links` if the optional agar branch is
   modeled as a variant.
3. Run `just verify-merges` to prove the source-duplicate pair still merges
   after the corrections.
4. Manually compare the regenerated record with DSMZ 1124 and MediaDive 1124
   to confirm agar remains optional.

## Additional Notes

The ASW-HD 1:10 medium is a separate formulation and should not be merged with
DSMZ/KOMODO 1124 solely on the abbreviated `HD 1:10` name.
