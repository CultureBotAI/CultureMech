# YAML Record Review: JCM MEDIUM No. 206

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_206.yaml
- Started UTC: 2026-09-23T16:42:17Z
- Finished UTC: 2026-09-23T16:42:48Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_206.yaml`
- Stable ID: `CultureMech:002568`
- Name: `jcm_medium_no_206`
- Original name: `JCM MEDIUM No. 206`
- Category: `bacterial`
- Media term: `mediadive.medium:J206`
- Source: MediaDive import of JCM Medium 206, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=206`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_206`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_206.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_206.yaml --out /private/tmp/jcm_medium_no_206.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_206.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_206.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators do not detect lost nested solution boundaries, missing inherited salt-base rows, or stock-strength trace minerals flattened into final-medium ingredients.

## Identity and Grounding

The record identifies MediaDive `J206` for JCM Medium 206. Live JCM, MediaDive, and TogoMedium M199 agree that JCM 206 uses 1 L Salt base solution, 10 ml Trace minerals from JCM 151, 5 mg citric acid, 6 g Tryptone, 6 g sulfur powder, 0.5 g sodium sulfide nonahydrate, and 1 mg resazurin, with the main medium adjusted to pH 6.5.

The generated record has the correct JCM accession and pH, but it is missing the salt-base solution composition and has flattened the JCM 151 Trace minerals stock into direct final-medium ingredient rows.

## Evidence

Inspected evidence:

- Live JCM `GRMD=206` lists a 1 L Salt base solution, 10 ml Trace minerals from Medium 151, 5 mg citric acid, 6 g Tryptone (BD-Difco), 6 g sulfur powder, 0.5 g Na2S x 9 H2O, and 1 mg Resazurin.
- The JCM 206 page says the salt base should use the Medium 151 Salt base solution with 17.0 g/L final NaCl and 2.5 mg/L final KI.
- Live JCM `GRMD=151` defines the Salt base solution and the Trace minerals stock used by JCM 206.
- MediaDive REST `medium/J206` preserves Salt base solution and Trace minerals as two nested solution references rather than direct final ingredients.
- TogoMedium M199 points to `JCM_M206` and the same JCM `GRMD=206` page.

Supported:

- The MediaDive ID, JCM link, category, main pH 6.5, main citric acid, Tryptone, sulfur, sodium sulfide, resazurin quantities, and anaerobic/filtration preparation prose are supported.
- The Trace minerals rows are source-supported only inside the inherited JCM 151 Trace minerals stock.

Unsupported or incomplete:

- The generated record omits the JCM 206 row for 1 L Salt base solution and omits that salt base's ingredient rows.
- The generated record omits the JCM 206 row for 10 ml Trace minerals and emits the JCM 151 stock rows at stock concentrations instead.
- The Trace minerals pH 7.0 adjustment is represented as a main JCM 206 preparation step.
- `Sulfur (powder)` is flattened to `Sulfur` and grounded to `CHEBI:26833` sulfur atom.
- Tryptone loses the `BD-Difco` supplier qualifier.

## Completeness

Consequential gaps:

- The JCM 151-derived salt base is not actionable because its NaCl, magnesium salts, KCl, NaBr, boric acid, SrCl2 x 6 H2O, KI, CaCl2, KH2PO4, ammonium nickel sulfate, and water rows are absent from the generated direct JCM record.
- The Trace minerals stock needs to be represented as a 10 ml/L stock addition with its own 1 L water and pH adjustment.

Empty slots that are acceptable:

- No target-organism or growth-metric claim is represented.

Bounded negative search:

- An ignored-file-inclusive owner search with `rg --no-ignore --hidden` across `data/normalized_yaml` and `data/merge_yaml/merged` for `Source: JCM, ID: J206`, `Original source: JCM - JCM_M206`, and exact `jcm_medium_no_206` found only the direct MediaDive/JCM record and the TOGO M199 wrapper as source owners for JCM 206.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The modified JCM 151 salt base is not represented. | JCM 206 and MediaDive add 1 L Salt base solution and state that it should use Medium 151 Salt base solution with 17.0 g/L final NaCl and 2.5 mg/L final KI. The generated record has no direct salt-base row and no nested salt-base composition; it only carries the prose instruction. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`. |
| Major | The JCM 151 Trace minerals stock is flattened into final-medium rows. | JCM 206 and MediaDive add 10 ml Trace minerals from Medium 151. The generated record instead emits the NTA, MgSO4, MnSO4, NaCl, FeSO4, CoSO4, CaCl2, ZnSO4, CuSO4, AlK(SO4)2, boric acid, and Na2MoO4 stock rows as direct final ingredients and omits the 10 ml stock-addition row and stock water row. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`. |
| Major | The Trace minerals stock pH is over-scoped to JCM 206. | The pH 7.0 KOH adjustment belongs to the JCM 151 Trace minerals stock. The JCM 206 final medium is adjusted to pH 6.5 with H2SO4 before filtration. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`. |
| Major | The sulfur powder identity is over-broadened. | JCM specifies sulfur powder and TogoMedium preserves that form; the generated record stores `Sulfur` grounded to sulfur atom. | MediaDive import/normalization and CHEBI enrichment for `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive owner search found `data/normalized_yaml/bacterial/togo_medium_m199.yaml` and `data/merge_yaml/merged/togo_medium_m199.yaml` pointing to `JCM_M206` and `GRMD=206`. The TOGO wrapper has empty unknown-solution stubs plus several milligram rows, including citric acid, boric acid, SrCl2 x 6 H2O, KI, and ammonium nickel sulfate, imported as g/L. | Both normalized owners, plus merge regeneration after source repair. |
| Minor | The Tryptone supplier qualifier is missing. | JCM and TogoMedium specify Tryptone (BD-Difco); the target stores only `Tryptone`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_206.yaml`. |

## Recommended Edits

1. Restore the 1 L Salt base solution row and represent the modified JCM 151 salt-base composition under that solution.
2. Restore Trace minerals as a 10 ml/L stock addition linked to the JCM 151 Trace minerals stock instead of direct JCM 206 ingredient rows.
3. Scope the pH 7.0 KOH adjustment to the Trace minerals stock and keep the H2SO4 adjustment to pH 6.5 on the final JCM 206 recipe.
4. Ground sulfur powder to an elemental sulfur or sulfur-powder ingredient, not sulfur atom, and preserve the powder source qualifier.
5. Preserve `BD-Difco` on Tryptone.
6. Repair `data/normalized_yaml/bacterial/togo_medium_m199.yaml` so its JCM 151 stock references and milligram quantities are represented with correct solution boundaries and units, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on repaired normalized records and regenerated merged records.
- Compare the regenerated JCM 206 against MediaDive `medium/J206`: the main recipe should expose seven JCM 206 rows, with Salt base solution and Trace minerals remaining nested references.
- Verify the repaired direct record no longer exposes undiluted Trace minerals rows as final-medium ingredients.
- Search with `rg --no-ignore --hidden 'Source: JCM, ID: J206|Original source: JCM - JCM_M206' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to confirm same-source duplicate handling with ignored files included.

## Additional Notes

- The source-level relationship to JCM 151 is real; the defect is the loss of structured recipe scopes while importing and merging the inherited JCM 151 solution recipes.
