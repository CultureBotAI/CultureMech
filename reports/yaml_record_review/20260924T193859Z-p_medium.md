# YAML Record Review: p_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/p_medium.yaml
- Started UTC: 2026-09-24T19:38:59Z
- Finished UTC: 2026-09-24T19:38:59Z
- Verdict: needs curation

## Target

Reviewed generated MediaRecipe `CultureMech:001272`, `p_medium`, generated from `data/normalized_yaml/bacterial/p_medium.yaml` for DSMZ Medium 1854 / P-medium.

## Validation

- Open LinkML validation: Passed; exited 0 with `No issues found`.
- Strict validation: Passed; scanned 1 file with 0 ERROR rows. `/private/tmp/p_medium.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The generated record has the expected `mediadive.medium:1854` grounding for DSMZ Medium 1854 / P-medium. An ignored-inclusive exact search for `mediadive.medium:1854`, `DSMZ Medium 1854`, and `P-medium` across `data/merge_yaml` and `data/normalized_yaml` found one normalized source record, its index entries, and this generated output.

## Evidence

MediaDive REST for medium 1854 lists P-medium as a DSMZ complex medium with min and max pH 7.5. Its 1 L main solution lists 10 g Soy peptone, 10 g Maltose, 1 g Calcium Chloride, 1 g Magnesium sulfate heptahydrate, 11.9 g HEPES, and 8 mg FeNa-EDTA, and its sole preparation step is `Adjust pH to 7.2.` The DOI reference in the generated record resolves to Pradella et al. 2002, `Characterisation, genome size and genetic manipulation of the myxobacterium Sorangium cellulosum So ce56`.

## Completeness

The generated record preserves the six MediaDive ingredient rows and DOI reference. It still needs curation because the pH field and preparation step disagree, and because FeNa-EDTA remains ungrounded.

## Findings

1. The record carries two conflicting pH values.

   `ph_value` is 7.5 from MediaDive's medium-level pH range, while the only preparation step says `Adjust pH to 7.2.` These are both source-imported values, but the generated recipe gives consumers no indication which pH should be used.

2. FeNa-EDTA is ungrounded.

   The DSMZ/MediaDive row for FeNa-EDTA 8 mg/L is present, but unlike the other simple defined components it has no `term` or `mediaingredientmech_chebi_term`.

## Recommended Edits

- Resolve the 7.5 versus 7.2 pH conflict against the DSMZ 1854 source or the Pradella et al. 2002 recipe, then update either `ph_value`, the preparation step, or both in normalized YAML.
- Add a source-specific note if DSMZ intentionally carries both values for different stages of the recipe.
- Ground FeNa-EDTA when an exact term is available; otherwise add a note explaining why the row is intentionally left ungrounded.

## Follow-up Checks

- Re-run the ignored-inclusive exact search for `mediadive.medium:1854`, `DSMZ Medium 1854`, and `P-medium` after curation to confirm there are no new duplicate P-medium records.
- Re-run open, strict, reference, and term validation on the regenerated YAML.

## Additional Notes

No ingredient rows were missing relative to the MediaDive 1854 REST payload.
