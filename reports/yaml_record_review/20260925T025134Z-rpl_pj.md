# YAML Record Review: rpl_pj

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/rpl_pj.yaml
- Started UTC: 2026-09-25T02:50:05Z
- Finished UTC: 2026-09-25T02:51:34Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Generated file | `data/merge_yaml/merged/rpl_pj.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:000398` |
| Name | `rpl_pj` |
| Original name | `RPL:PJ` |
| Category | `bacterial` |
| Media term | `CCAP Medium C84` / `mediadive.medium:C84` |
| Immediate maintained inputs | `data/normalized_yaml/bacterial/rpl_pj.yaml`, `data/normalized_yaml/bacterial/rpl.yaml` |
| Merge fingerprint | `07bbf0ff0b91919678fc85c3298b7bf71dc2498ba832bf0c35ce69484c9282b955c4d36` |
| Merged from | `rpl`, `rpl_pj` |

The generated file merges the MediaDive/CCAP C84 `RPL:PJ` record with the MediaDive/CCAP C82 `RPL` record as a `SOURCE_DUPLICATE`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/rpl_pj.yaml` exited 0 and reported `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/rpl_pj.yaml --out /private/tmp/rpl_pj.strict.tsv --workers 1 --quiet` scanned 1 file and reported 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/rpl_pj.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file and reported 0 checks. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/rpl_pj.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` blocks embedded in merged YAML. |

## Identity and Grounding

- The canonical source identity points at CCAP C84 / `RPL:PJ`, and MediaDive `medium/C84` plus the CCAP `MR_RPL_PJ.pdf` both confirm that identity.
- The generated source merge is incorrect. C84 `RPL:PJ` is a 3:1 mixture of 750 ml C82/RPL plus 250 ml C76/PJ; C82 `RPL` is only rye grass plus 1 L PJ. The two source media flatten to the same five ingredient names after stock structure is erased, but they are not source duplicates.
- The `bacterial` category is not supported by the CCAP PDF, which describes `RPL:PJ` under `Freshwater protozoa`.
- An exact ignored-file search for `CultureMech:000398`, `CultureMech:000396`, `mediadive.medium:C84`, `mediadive.medium:C82`, `RPL:PJ`, `MR_RPL_PJ`, `source: rpl_pj.yaml`, and `source: rpl.yaml` found both maintained source records, this generated merge, related algae CCAP imports, and the `rpl_pj_0_01_rpa` CCAP variant records; ignored files were included.

## Evidence

### Supported

- The CCAP C84 `RPL:PJ` identity, 3:1 mixture text, aseptic mixing instruction, RPL child recipe, PJ child recipe, and child autoclave steps are supported by the CCAP PDF and by MediaDive `medium/C84`.
- C82 `RPL` correctly appears as a child of C84 in the MediaDive graph, with 750 ml used in 1 L C84.

### Unsupported Or Over-scoped

- The `SOURCE_DUPLICATE` relationship to C82 is false. C84 is a 750 ml C82 plus 250 ml PJ mixture, not a duplicate copy of C82.
- The generated ingredient list flattens child C82, child PJ, and PJ stocks into five parent rows. Rows such as `K2HPO4 1 G_PER_L`, `MgSO4 x 7 H2O 1 G_PER_L`, `CaCl2 x 2 H2O 4.3 G_PER_L`, and `KCl 1.6 G_PER_L` are not final C84 parent concentrations.
- Two generated solute values came from stock-addition volumes: CCAP defines `K2HPO4` as a `0.51 g/100 ml` stock added at 1 ml/L to PJ and `MgSO4 x 7 H2O` as a `0.28 g/100 ml` stock added at 1 ml/L to PJ. The generated record instead emits `1 G_PER_L` for each.

## Completeness

- The parent 750 ml C82 and 250 ml C76/PJ child-solution rows are missing from the generated ingredient list.
- The PJ stock solution additions and 100 ml CaCl2/KCl stock recipe are missing as scoped child structures.
- Preparation steps are present but flattened; the C82/RPL and C76/PJ autoclave steps are not attached to their own child solutions.
- `target_organisms`, growth metrics, strain-level assertions, citations, and literature evidence are empty. Those absences are not defects for this generated CCAP import; the inspected CCAP and MediaDive sources only define the formulation.

## Findings

### Blocker

1. **The merge falsely makes C82 `RPL` a source duplicate of C84 `RPL:PJ`.** The generated record lists `merged_from: rpl, rpl_pj`, a `parent_media` relationship to `CultureMech:000396`, `variant_relationship: SOURCE_DUPLICATE`, and a `synonyms` entry for `rpl`. MediaDive C84 and the CCAP PDF show that `RPL:PJ` is a distinct 3:1 mix of RPL and PJ, so this merge makes one generated record denote two different CCAP media.

### Major

1. **The C84 child-media mixture is flattened into unsupported parent ingredient concentrations.** `data/normalized_yaml/bacterial/rpl_pj.yaml` preserves the 3:1 mixture only as a text preparation step, while the ingredient rows erase the 750 ml C82 and 250 ml C76/PJ source rows. That erased structure is why the merge fingerprint incorrectly matches plain C82 `RPL`.

2. **PJ stock additions are imported as `G_PER_L` solutes.** The generated `K2HPO4` and `MgSO4 x 7 H2O` rows represent 1 ml additions of 0.51 g/100 ml and 0.28 g/100 ml stock solutions, not 1 g/L of the parent `RPL:PJ` medium.

3. **The CCAP category is unsupported.** CCAP labels `RPL:PJ` as a freshwater protozoa medium, but the MediaDive path files it under `category: bacterial`. This record needs an explicit category/source-ownership decision alongside the related algae CCAP import.

### Minor

None found.

## Recommended Edits

1. Change `merge_recipes.py` or the normalized fingerprint inputs so C82 `RPL` and C84 `RPL:PJ` no longer merge as `SOURCE_DUPLICATE` records.

2. In `data/normalized_yaml/bacterial/rpl_pj.yaml`, represent C84 as 750 ml of C82/RPL plus 250 ml of C76/PJ, then keep PJ's 1 ml stock additions and the CaCl2/KCl 100 ml stock below the PJ solution instead of flattening descendants into the C84 parent.

3. Re-import stock-addition rows from MediaDive/CCAP with their source units and scopes; do not turn 1 ml stock additions into `1 G_PER_L` solute concentrations.

4. Review the category and duplicate state across `data/normalized_yaml/bacterial/rpl_pj.yaml`, `data/normalized_yaml/bacterial/rpl.yaml`, and the same-named CCAP algae records before regenerating `data/merge_yaml/merged/`.

## Follow-up Checks

- Run `just verify-merges` and confirm C82 and C84 emit as distinct generated records.
- Run `just audit-merge-freshness` after regenerating merged YAML.
- Run focused open-schema, strict, reference, and term validation on both repaired normalized records and on the regenerated `RPL` and `RPL:PJ` merged outputs.
- Manually compare the regenerated C84 record to CCAP `MR_RPL_PJ.pdf` and MediaDive `medium/C84`, checking the 3:1 mixture, 750 ml and 250 ml child rows, PJ 1 ml stock additions, CaCl2/KCl 100 ml stock, and preparation-step scopes.

## Additional Notes

- The source and duplicate search used `rg --no-ignore --hidden`; ignored files were included.
- `data/merge_yaml/merged/rpl_pj.yaml` is generated output. Future fixes belong in `data/normalized_yaml/bacterial/rpl_pj.yaml`, `data/normalized_yaml/bacterial/rpl.yaml`, and merge logic that currently treats their flattened fingerprints as duplicates.
