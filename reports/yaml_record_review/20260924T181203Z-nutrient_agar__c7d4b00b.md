# YAML Record Review: nutrient_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml
- Started UTC: 2026-09-24T18:10:48Z
- Finished UTC: 2026-09-24T18:12:03Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| CultureMech ID | CultureMech:008926 |
| Name | nutrient_agar |
| Original name | Nutrient Agar |
| Category | bacterial |
| Media term | TOGO:M2339 / Nutrient Agar |
| Generated path | data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml |
| Maintained sources | data/normalized_yaml/bacterial/TOGO_M2339_Nutrient_Agar.yaml; data/normalized_yaml/bacterial/TOGO_M3116_Nutrient_Agar.yaml |
| Merge fingerprint | c7d4b00b2beeef2425f5424169a030b9a8e96e02ccafc28f7cce3dc1b43762c2 |
| Merged from | TOGO_M2339_Nutrient_Agar, TOGO_M3116_Nutrient_Agar |

This is a generated merge of two TOGO DSMZ Medium 1 mirrors. The pair is a real source duplicate, but the generated YAML predates the September repairs already present in both maintained inputs.

## Validation

| Check | Result |
| --- | --- |
| Open schema validation with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml` | Passed with "No issues found". |
| Strict validation with `scripts/validate_strict.py data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml --out /private/tmp/nutrient_agar__c7d4b00b.strict.tsv --workers 1 --quiet` | Passed; the TSV contained only the header line. |
| Reference validation with `linkml-reference-validator validate data data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 total checks and no failures. |
| Term validation with `linkml-term-validator validate-data data/merge_yaml/merged/nutrient_agar__c7d4b00b.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the documented history validator validates standalone `history/` entries, not embedded generated `MediaRecipe.curation_history`. |

## Identity and Grounding

The generated record represents TOGO `M2339`, which imports DSMZ Medium 1 Nutrient Agar. TOGO `M3116` imports the same DSMZ Medium 1 recipe, and both current maintained YAML records now declare the same canonical `data/normalized_yaml/bacterial/nutrient_agar.yaml` parent with `SOURCE_DUPLICATE` relationships.

An ignored-file-inclusive PCRE2 search for exact `TOGO:M2339`, `TOGO:M3116`, `TOGO_M2339_Nutrient_Agar`, and `TOGO_M3116_Nutrient_Agar` across `data/normalized_yaml` and `data/merge_yaml/merged` found only these two maintained sources, their index entries, their parent links from canonical `nutrient_agar.yaml`, and this generated merge.

## Evidence

DSMZ Medium 1 lists, per liter, 5.0 g Peptone, 3.0 g Meat extract, 15.0 g Agar if necessary, 1000 ml Distilled water, and pH adjustment to 7.0. The repaired maintained TOGO M2339 and M3116 records now match that formula with 1.0 L water, pH 7.0, DSMZ references, and intentionally ungrounded Meat extract.

The generated record still has the stale TOGO import shape: `1000 G_PER_L` Distilled water, no `ph_value`, no preparation step for pH 7.0, no references, and an ungrounded `Agar, if necessary` row despite the maintained sources already linking that ingredient to CHEBI:2509.

## Completeness

The core DSMZ ingredient amounts are present, and merging M2339 with M3116 is correct. The generated record is incomplete only because it was not regenerated after the maintained files were repaired: pH 7.0, reference URLs, the 1.0 L water unit, ingredient provenance notes, data-quality flags, and the canonical parent relationship are all missing.

Empty target-organism and citation slots are acceptable for these TOGO mirrors of a provider formula. DSMZ Medium 1 does not name a strain-specific target organism.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| Major | The generated DSMZ Nutrient Agar merge is stale relative to both repaired maintained sources. | Current M2339 and M3116 normalized YAML both have 1.0 L water, pH 7.0, a pH preparation step, DSMZ Medium 1 references, curated ingredient terms, and a canonical `nutrient_agar.yaml` parent; generated `nutrient_agar__c7d4b00b.yaml` still has the pre-repair 1000 g/L water row, no pH, and no references. | Regenerate `data/merge_yaml/merged` from `data/normalized_yaml/bacterial/TOGO_M2339_Nutrient_Agar.yaml` and `data/normalized_yaml/bacterial/TOGO_M3116_Nutrient_Agar.yaml`. |

## Recommended Edits

1. Regenerate merged YAML so TOGO M2339 and TOGO M3116 collapse into the canonical DSMZ Medium 1 Nutrient Agar source-duplicate cluster with pH 7.0 and 1.0 L water.
2. Verify the regenerated record keeps Meat extract intentionally ungrounded while retaining CHEBI grounding for water and agar and MICRO grounding for Peptone.

## Follow-up Checks

1. Run the focused open schema, strict, reference, and term validators on the regenerated DSMZ Medium 1 Nutrient Agar record.
2. Search with ignored files included for exact `TOGO:M2339` and `TOGO:M3116` to confirm both source IDs now contribute to the intended canonical merge and no stale `nutrient_agar__c7d4b00b.yaml` artifact remains.

## Additional Notes

None found.
