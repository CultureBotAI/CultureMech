# YAML Record Review: PYG Medium (C)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_medium_c.yaml
- Started UTC: 2026-09-24T22:35:29Z
- Finished UTC: 2026-09-24T22:36:11Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009843 |
| Name | pyg_medium_c |
| Original name | PYG Medium (C) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_medium_c.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml |
| Merge source | TOGO_M456_PYG_Medium_C |
| Source grounding | TOGO:M456, JCM_M456 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO normalization and in TOGO cross-medium import logic
that currently preserves the M423 base medium as an unresolved empty solution.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_medium_c.yaml` | Passed; exited 0 with no diagnostics. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_medium_c.yaml --out /private/tmp/pyg_medium_c.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_medium_c.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_medium_c.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_medium_c.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_medium_c.yaml` is the generated TOGO M456 branch
  for CultureMech:009843 and JCM_M456 `PYG Medium (C)`.
- The TOGO M456 API reports the same label, original medium `JCM_M456`, and
  same JCM GRMD 456 URL embedded in the YAML.
- The live JCM 456 page confirms the PYG MEDIUM (C) identity and describes it
  as Medium 423 supplemented with 1.0 ml/L Tween 80 and 0.5 g/L
  L-cysteine.HCl.H2O under an N2 atmosphere.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M456`, TOGO M456
  URLs, `JCM_M456`, `GRMD=456`, and `TOGO_M456_PYG_Medium_C` found the
  reviewed TOGO branch, its maintained owner, and the equivalent direct JCM
  branch.
- Exact ignored-file search across `data/**/*.yaml` for
  `mediadive.medium:264` found that the same KG-Microbe cross-reference is
  assigned to unrelated Tomato Juice records and one R agar with Tween 80
  record, so it does not ground this PYG C recipe.

## Evidence

- JCM 456 and TOGO M456 both define PYG Medium C as Medium 423 plus 1.0 ml/L
  Tween 80 and 0.5 g/L L-cysteine.HCl.H2O, prepared under an N2 atmosphere.
- The reviewed YAML preserves the TOGO M456 identity and the
  L-cysteine.HCl.H2O supplement amount.
- The reviewed YAML stores Medium 423 as a nested `solutions` entry with an
  empty `composition` and a concentration of 1 `G_PER_L`; that row is a
  one-liter base-medium reference, not a gram of unknown dry stock.
- The reviewed YAML stores Tween 80 as 1 `G_PER_L`, but both upstream sources
  specify 1.0 ml/L.
- The reviewed YAML carries N2 as a variable concentration ingredient and does
  not preserve the instruction to prepare the medium under N2.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- The one-liter Medium 423 base is unresolved, so the generated recipe lacks
  every ingredient, salt stock, water amount, and pH inherited from PYG Medium
  B.
- The N2 preparation atmosphere is missing from preparation context even though
  both JCM and TOGO specify it.
- `kg_microbe_match` points to a MediaDive Tomato Juice medium ID rather than a
  PYG Medium C equivalent.
- The equivalent direct JCM branch remains separate and needs reconciliation
  once both branches resolve M423 inheritance the same way.
- Exact ignored-file searches included ignored files before assessing exact
  TOGO/JCM duplicate scope and the MediaDive 264 cross-reference.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The required Medium 423 base recipe was collapsed to an empty one-gram solution. | JCM 456 says to use Medium 423 and TOGO M456 encodes a 1 L `reference_media_id: M423` item; the YAML stores it as an empty `PYG MEDIUM (B)` solution with `1` `G_PER_L`. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml; TOGO cross-medium import |
| Major | The inherited PYG Medium B recipe is therefore absent. | The reviewed record contains only Tween 80, L-cysteine.HCl.H2O, N2, and the empty base-medium placeholder, while Medium 423 contains peptide, yeast extract, glucose, salts solution, water, and pH 7.2 rows that define the actual base medium. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml; TOGO cross-medium import |
| Major | `kg_microbe_match` is over-broad or wrong. | Exact ignored-file search for `mediadive.medium:264` found that the same MediaDive ID is used by Tomato Juice medium records, not just this PYG Medium C branch. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml; kg_microbe matching |
| Minor | Tween 80 has the wrong unit family. | JCM 456 and TOGO M456 both specify 1.0 ml/L Tween 80; the YAML stores the supplement as `1` `G_PER_L`. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml |
| Minor | The N2 atmosphere is not modeled as preparation context. | The sources instruct that the medium be prepared under N2, but the YAML only stores N2 as a variable-concentration gas ingredient. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml |
| Minor | The equivalent direct JCM branch remains a separate generated record. | Exact ignored-file search found another `pyg_medium_c` owner for the same JCM 456 recipe. | data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml; merge_recipes.py |

## Recommended Edits

1. Recurate `data/normalized_yaml/bacterial/TOGO_M456_PYG_Medium_C.yaml` so
   the M423 base medium is expanded or linked in a way that preserves a real
   one-liter base aliquot rather than an empty 1 g/L stock.
2. After M423 itself is fixed, ensure PYG C inherits its peptide, yeast
   extract, glucose, salt-stock, water, and pH 7.2 values correctly.
3. Store Tween 80 as the source 1.0 ml/L amount.
4. Move the N2 atmosphere out of ordinary variable ingredients into an explicit
   preparation or gas-atmosphere representation supported by the schema.
5. Remove or correct `kg_microbe_match: mediadive.medium:264`.
6. Regenerate the merged output and verify this TOGO M456 branch converges with
   the equivalent direct JCM branch.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_medium_c.yaml`.
- Manually confirm the regenerated record no longer has an empty `PYG MEDIUM
  (B)` solution or a `G_PER_L` Tween 80 conversion.
- Re-run an exact ignored-file search for `mediadive.medium:264` to ensure the
  PYG C records no longer point to the Tomato Juice medium identifier.
- Manually compare the regenerated TOGO M456 branch against the direct JCM M456
  branch after both normalize the M423 base consistently.
- Run an exact ignored-file search for `TOGO:M456`, `JCM_M456`, and `GRMD=456`
  to verify no stale duplicate remains after regeneration.

## Additional Notes

None found.
