# YAML Record Review: GAM SEMISOLID

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/gam_semisolid__c8a000d8.yaml
- Started UTC: 2026-09-23T05:28:01Z
- Finished UTC: 2026-09-23T05:29:09Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:003195`, `gam_semisolid`, category `bacterial`, for JCM Medium J84 / MediaDive `mediadive.medium:J84`.

The generated file has one source, `gam_semisolid`, with merge fingerprint `c8a000d8a48d11584e03ff93e34fbc968eba5386166f44e2728a7e8bc9987848`; future YAML edits belong in `data/normalized_yaml/bacterial/gam_semisolid.yaml`.

## Validation

| Check | Result |
|---|---|
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/gam_semisolid__c8a000d8.yaml` | Passed. |
| `python scripts/validate_strict.py data/merge_yaml/merged/gam_semisolid__c8a000d8.yaml --out /private/tmp/gam_semisolid_c8a000d8.strict.tsv --workers 1 --quiet` | Passed; the TSV had one header row and no error rows. |
| `linkml-reference-validator validate data data/merge_yaml/merged/gam_semisolid__c8a000d8.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the reference validator reported zero checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/gam_semisolid__c8a000d8.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the repository documents `just validate-history` for standalone files under `history/`, not for generated `MediaRecipe.curation_history` entries. |

## Identity and Grounding

MediaDive REST resolves `J84` to JCM `GAM SEMISOLID`; the live JCM `GRMD=84` page has the same medium name and formula. TOGO M75 also names original medium `JCM_M84` and the same JCM URL.

An ignored-file-inclusive slug search over `data/normalized_yaml` and `data/merge_yaml/merged` found the direct JCM source plus the TOGO M75 source, emitted separately as `gam_semisolid__c8a000d8.yaml` and `GAM_SEMISOLID.yaml`.

The generated direct-JCM file also has `kg_microbe_match: mediadive.medium:12`. MediaDive accession `12` resolves to DSMZ `SOIL EXTRACT MEDIUM`, so that cross-link is unrelated to JCM Medium 84.

## Evidence

JCM 84, MediaDive J84, and TOGO M75 agree on the three source rows:

| Ingredient | Source amount |
|---|---:|
| GAM broth (Nissui) | 59 g |
| Agar | 2 g |
| Distilled water | 1 L |

The generated direct-JCM record preserves the 59 g/L broth concentration and 2 g/L agar concentration, but it omits the `Nissui` qualifier and the 1 L distilled-water row.

The maintained direct normalized source was repaired on 2026-09-10 to store `GAM broth (Nissui)`, agar, 1000 ml distilled water, `physical_state: SEMISOLID`, JCM-derived preparation steps, `sterilization`, `data_quality_flags`, and the JCM URL reference. The generated August merge predates that repair and still says `physical_state: SOLID_AGAR`.

## Completeness

The live JCM page has no pH value or free-text preparation note, so those fields are not missing. The generated output is incomplete because it is stale relative to the repaired normalized source and because the duplicate TOGO M75 import has not been reconciled.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | Direct JCM J84 and TOGO M75 remain split despite denoting the same source recipe. | TOGO M75 cites `JCM_M84` and the same JCM `GRMD=84` URL used by the direct MediaDive `J84` source, but the generated corpus contains two CultureMech IDs and two files. | `data/normalized_yaml/bacterial/gam_semisolid.yaml`, `data/normalized_yaml/bacterial/TOGO_M75_GAM_Semisolid.yaml`, and merge reconciliation. |
| Major | `kg_microbe_match` points to an unrelated DSMZ medium. | The generated and normalized direct-JCM records store `mediadive.medium:12`, which resolves to DSMZ `SOIL EXTRACT MEDIUM`, not GAM SEMISOLID. | `data/normalized_yaml/bacterial/gam_semisolid.yaml`. |
| Major | The generated merge is stale relative to the 2026-09-10 official JCM repair. | The normalized source now has 1000 ml water, the Nissui qualifier, `SEMISOLID` physical state, preparation steps, sterilization, references, and data-quality flags; the generated record lacks those fields and still says `SOLID_AGAR`. | Regenerate from `data/normalized_yaml/bacterial/gam_semisolid.yaml`. |
| Minor | The generated `GAM broth` row omits the Nissui supplier. | JCM, MediaDive, TOGO, and the repaired normalized record specify `GAM broth (Nissui)`. | Regenerate from `data/normalized_yaml/bacterial/gam_semisolid.yaml`. |

## Recommended Edits

1. Remove the unsupported `kg_microbe_match: mediadive.medium:12` from `data/normalized_yaml/bacterial/gam_semisolid.yaml`.
2. Reconcile the direct JCM J84 import with TOGO M75.
3. Regenerate the direct merged record so it carries `GAM broth (Nissui)`, 1000 ml distilled water, `physical_state: SEMISOLID`, preparation steps, `sterilization`, `data_quality_flags`, and `references`.

## Follow-up Checks

After curation and regeneration, rerun focused schema, strict, reference, and term validation on the regenerated direct-JCM or reconciled TOGO/JCM record.

Manually compare the result against:

- JCM `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=84`
- MediaDive REST `https://mediadive.dsmz.de/rest/medium/J84`
- TOGO API `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M75`

## Additional Notes

The record-level slug search included ignored files. A later accession search for `TOGO:M75` also matched neighboring TOGO identifiers such as `TOGO:M750`; those broader matches were not used for absence claims.
