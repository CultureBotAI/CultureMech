# YAML Record Review: rt2_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/rt2_medium__df06dcd8.yaml
- Started UTC: 2026-09-25T02:58:20Z
- Finished UTC: 2026-09-25T02:59:39Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Generated file | `data/merge_yaml/merged/rt2_medium__df06dcd8.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002789` |
| Name | `rt2_medium` |
| Original name | `RT2 MEDIUM` |
| Category | `bacterial` |
| pH | `9.0` |
| Media term | `JCM Medium J438` / `mediadive.medium:J438` |
| Immediate maintained input | `data/normalized_yaml/bacterial/rt2_medium.yaml` |
| Merge fingerprint | `df06dcd87f47c4ed517bc58ea731510cc9174b8ab82573b9b12b913826543529` |
| Merged from | `rt2_medium` |

The generated file is the single-source merge of MediaDive/JCM Medium 438, `RT2 MEDIUM`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/rt2_medium__df06dcd8.yaml` exited 0 and reported `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/rt2_medium__df06dcd8.yaml --out /private/tmp/rt2_medium__df06dcd8.strict.tsv --workers 1 --quiet` scanned 1 file and reported 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/rt2_medium__df06dcd8.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file and reported 0 checks. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/rt2_medium__df06dcd8.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` blocks embedded in merged YAML. |

## Identity and Grounding

- The source identity is correct: JCM GRMD 438 and MediaDive `medium/J438` both identify the formulation as `RT2 MEDIUM` with pH 9.0.
- The generated ingredient quantities are not source-faithful. MediaDive parsed `Main sol. J438` as `volume: 2`, which turns 1 g NaCl into `500 G_PER_L` and 11.86 g CAPSO into `5930 G_PER_L`; JCM 438 instead instructs dissolving the base in 900 ml distilled water, then adding sodium lactate as 1/9 volume of a 0.2 M solution.
- JCM 438 adds both a trace element solution from Medium 294 and a selenite-tungstate solution from Medium 431 at 1 ml. The generated record leaves only an `Unknown solution` for the trace stock and flattens selenite-tungstate components as final parent ingredients.
- An exact ignored-file search for `CultureMech:002789`, `mediadive.medium:J438`, `JCM Medium J438`, `GRMD=438`, `RT2 MEDIUM`, `rt2_medium`, and `source: rt2_medium.yaml` found this generated merge, its maintained MediaDive input, and a separate TOGO M438 `rt2_medium` input that cites the same JCM 438 URL; ignored files were included.

## Evidence

### Supported

- JCM 438 supports the `RT2 MEDIUM` identity, pH 9.0, the listed base salts, yeast extract, CAPSO, sodium sulfate, 1 ml trace element solution, 1 ml selenite-tungstate solution, sodium lactate, resazurin, sodium dithionite, and the anaerobic preparation text.
- The inspected JCM 431 page supports the selenite-tungstate stock composition: NaOH, Na2SeO3 x 5 H2O, Na2WO4 x 2 H2O, and 1 L distilled water.

### Unsupported Or Over-scoped

- Nearly every generated parent concentration is a 500x inflation caused by the `Main sol. J438` 2 ml volume parse. The source lists 1 g NaCl, 0.1 g MgCl2 x 6 H2O, 0.05 g CaCl2 x 2 H2O, 0.25 g NH4Cl, 0.1 g KH2PO4, 0.5 g KCl, 0.5 g yeast extract, 11.86 g CAPSO, 4 g Na2SO4, 2.24 g sodium lactate, 1 mg resazurin, and 0.1 g sodium dithionite; it does not support the generated 500, 50, 25, 125, 50, 250, 250, 5930, 2000, 1120, 0.5, and 50 `G_PER_L` values.
- The 1 ml JCM stock additions are not represented correctly. The Medium 294 trace element solution has been reduced to `Trace element solution` / `1 G_PER_L` / `Unknown solution`, and the Medium 431 selenite-tungstate internals are stock recipe amounts, not final RT2 parent ingredients.
- Sodium lactate and sodium dithionite require special handling in the source procedure. Sodium lactate is a separately autoclaved 0.2 M solution added at 1/9 volume after cooling, and the medium is reduced immediately before use with freshly prepared filter-sterilized 1% sodium dithionite.

## Completeness

- The generated record lacks the 900 ml distilled-water row from the JCM 438 base medium and the 1 L water row from the selenite-tungstate stock.
- The Medium 294 trace element stock is only a dangling `solutions` entry with `Unknown solution`; its Medium 294 composition is not represented.
- The generated record does preserve the main JCM 438 preparation text, but the referenced stock and post-autoclave additions are not scoped to solution records.
- `target_organisms`, growth metrics, strain-level assertions, citations, and literature evidence are empty. Those absences are not defects for this source import; JCM 438 only defines the medium.

## Findings

### Blocker

None found.

### Major

1. **The MediaDive import inflated parent concentrations by parsing the parent volume as 2 ml.** `data/normalized_yaml/bacterial/rt2_medium.yaml` publishes impossible values such as `500 G_PER_L` NaCl, `5930 G_PER_L` CAPSO, `2000 G_PER_L` Na2SO4, and `1120 G_PER_L` sodium lactate. JCM 438 gives gram and milligram amounts for a base prepared in 900 ml distilled water and then amended; the generated `G_PER_L` values are not chemically possible final concentrations.

2. **JCM stock additions are missing or flattened.** JCM 438 adds 1 ml trace element solution from Medium 294 and 1 ml selenite-tungstate solution from Medium 431. The trace stock became `Unknown solution` with `1 G_PER_L`, while NaOH, Na2SeO3 x 5 H2O, and Na2WO4 x 2 H2O from the selenite-tungstate stock are incorrectly listed as parent ingredients.

3. **Post-autoclave sodium lactate and sodium dithionite are represented as direct base ingredients.** The source explicitly excludes sodium lactate and sodium dithionite from the initial solution: sodium lactate is added later as a 0.2 M anaerobic stock at 1/9 volume, and 1% sodium dithionite is filter-sterilized and used immediately before inoculation.

4. **The JCM and TOGO captures of RT2 remain split.** The exact ignored-file search found `data/normalized_yaml/bacterial/TOGO_M438_RT2_Medium.yaml`, whose provenance also cites JCM 438, and the generated `data/merge_yaml/merged/RT2_MEDIUM.yaml`. These source views should be reconciled or deliberately kept separate with a documented reason.

### Minor

None found.

## Recommended Edits

1. In `data/normalized_yaml/bacterial/rt2_medium.yaml`, preserve the source gram/milligram amounts from JCM 438 instead of MediaDive's `volume: 2`-derived `G_PER_L` conversions, or repair the MediaDive importer so the final volume is inferred from the 900 ml base plus 1/9 volume lactate addition.

2. Represent the 1 ml Medium 294 trace solution and 1 ml Medium 431 selenite-tungstate solution as scoped stock additions, including their own stock recipes and water rows.

3. Scope sodium lactate and sodium dithionite to the preparation steps that add them after autoclaving; do not treat them as direct base salts.

4. Compare the MediaDive J438 input with `data/normalized_yaml/bacterial/TOGO_M438_RT2_Medium.yaml` and adjust the normalized records or merge rules so the JCM and TOGO captures do not produce unexplained duplicate RT2 generated records.

## Follow-up Checks

- Run `just verify-merges` and confirm repaired JCM and TOGO RT2 inputs merge or stay intentionally distinct.
- Run `just audit-merge-freshness` after regenerating merged YAML.
- Run focused open-schema, strict, reference, and term validation on the repaired normalized inputs and regenerated merge.
- Manually compare the regenerated record against JCM 438, JCM 294, JCM 431, and MediaDive `medium/J438`, checking source amounts, final-volume handling, 1 ml stock additions, pH 9.0 adjustment, and anaerobic addition order.

## Additional Notes

- The source and duplicate search used `rg --no-ignore --hidden`; ignored files were included.
- `data/merge_yaml/merged/rt2_medium__df06dcd8.yaml` is generated output. Future fixes belong in `data/normalized_yaml/bacterial/rt2_medium.yaml`, the MediaDive/JCM importer, or merge logic.
