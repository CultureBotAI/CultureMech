# YAML Record Review: 1_2my

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2MY.yaml`
- Started UTC: 20260921T050638Z
- Finished UTC: 20260921T050820Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:008305` |
| Slug | `1_2my` |
| Original name | `1/2MY` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owner | `data/normalized_yaml/bacterial/1_2my.yaml` |
| Source accession | `TOGO:M1742`, original NBRC `NBRC_M951` |
| Merge fingerprint | `621a70c3e8f2826fb5869a0ba0706b636a133a91d441ce2e8dc2b01820ba6082` |

Reviewed the singleton merge for TOGO Medium M1742, imported from NBRC Medium 951. The exact ID/accession/fingerprint search included ignored and hidden files under `data/normalized_yaml`, `data/merge_yaml/merged`, `data/culturemech_recipe_catalog.tsv`, and `data/normalized_yaml/*_index.json`; it found only this generated record, its normalized owner, and the expected active recipe/catalog entries. A gitignore-independent search under `data/raw`, `data/import_tracking`, and `data/reference` found only this record's `deep_research_priority.json` inventory entry for `M1742`/`1/2MY`, with no cached raw TOGO or NBRC payload.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2MY.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2MY.yaml --out /private/tmp/1_2MY.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2MY.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2MY.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validators remain blocked in this checkout by the project dependency build for `llvmlite==0.46.0` under Python 3.13, so the equivalent no-project `uv` commands above were used for focused validation.

## Identity and Grounding

- The record identity is coherent: the merge, maintained owner, TOGO M1742 API response, and NBRC Medium 951 page all identify the same 1/2MY formulation.
- TOGO M1742 preserves NBRC Medium 951 as `NBRC_M951` and records `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=951` as `src_url`; the generated record carries those in free text under `notes`.
- The source supports solid agar identity. NBRC and TOGO both list agar, and `physical_state: SOLID_AGAR` matches that formula.
- Five ingredient amounts are correctly normalized to `G_PER_L` because NBRC provides the batch as grams in 1 L of distilled water: Malt Extract Agar (Nissui) 2.7, yeast extract 1.5, Bacto Peptone (Difco) 2.5, glucose 5, and agar 18.8.
- Distilled water is incorrectly normalized as `1 G_PER_L`; TOGO and NBRC both give `1 L`.

## Evidence

- The live TOGO response for `gm_id=M1742` was inspected and lists the same six components, units, and amounts present in the generated CultureMech record except that water is still a liter value before the importer unit conversion.
- The NBRC Medium 951 HTML page was inspected and confirms the original source ID, medium name, component names, amounts, 1 L water basis, and `pH unadjusted` note.
- The TOGO API response carries the `pH unadjusted` comment from NBRC; the CultureMech record omits it.
- No inspected source gave an incubation temperature, sterilization step, or target organism for this medium; the record is correctly empty for those slots.

## Completeness

- Consequential gap: the water amount should be represented as `1000 ML_PER_L`, not `1 G_PER_L`.
- Non-blocking gap: `pH unadjusted` is source-supported but not represented in `ph_value`, `preparation_steps`, `notes`, or `discussion`.
- Non-blocking provenance gap: the TOGO URL and NBRC original URL are embedded in `notes`, but the record has no structured `references` row for either inspected source.
- Non-blocking grounding gap: TOGO maps yeast extract to GMO `GMO_001003`, but the ingredient lacks the exact `FOODON:03315426` grounding already used for yeast extract in other maintained records.
- Bacto Peptone (Difco) and Malt Extract Agar (Nissui) are unresolved commercial/undefined materials and are reasonably left without an approximate CHEBI grounding.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water has a dimensionally wrong concentration of `1 G_PER_L`. | TOGO M1742 and NBRC 951 both list 1 L distilled water. The record converted that to a gram-per-liter concentration instead of `1000 ML_PER_L`. | `data/normalized_yaml/bacterial/1_2my.yaml` |
| Minor | The source's `pH unadjusted` instruction was dropped. | TOGO has a comment row `pH unadjusted`; NBRC shows the same note in the composition table. The CultureMech record has no pH or preparation representation for the instruction. | `data/normalized_yaml/bacterial/1_2my.yaml` |
| Minor | Source provenance is only free text in `notes`. | The generated and normalized YAML embed TOGO M1742 and NBRC 951 URLs in a multiline note; neither has a structured `references` entry. | `data/normalized_yaml/bacterial/1_2my.yaml` |
| Minor | Yeast extract is an exact ontology miss. | TOGO identifies yeast extract with GMO `GMO_001003`, and CultureMech elsewhere uses exact `FOODON:03315426` grounding for yeast extract; this ingredient is still ungrounded. | `data/normalized_yaml/bacterial/1_2my.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/1_2my.yaml`, change distilled water from `1 G_PER_L` to `1000 ML_PER_L`, preserving the source as NBRC/TOGO.
2. Add structured `references` for `https://togomedium.org/medium/M1742` and `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=951`.
3. Preserve the source `pH unadjusted` statement in an appropriate preparation note or discussion item without inventing a numeric pH.
4. Add exact yeast-extract grounding from the local ingredient label index if the guarded curation path can resolve `FOODON:03315426` for this row.
5. Regenerate the merge layer from the normalized owner so `data/merge_yaml/merged/1_2MY.yaml` reflects the corrected water unit and structured provenance.

## Follow-up Checks

- Re-run `just validate-strict data/normalized_yaml/bacterial/1_2my.yaml` or the no-project strict equivalent after the normalized edit.
- Re-run `just validate-terms data/normalized_yaml/bacterial/1_2my.yaml` or the no-project term equivalent after adding yeast-extract grounding.
- Re-run `just validate-references data/normalized_yaml/bacterial/1_2my.yaml` or the no-project reference equivalent after adding structured TOGO/NBRC references.
- Re-run merge generation and verify the `1_2MY.yaml` generated artifact no longer contains `1 G_PER_L` for water.

## Additional Notes

- The CultureMech ingredient order follows the TOGO component array rather than NBRC table order; that is not a scientific defect because neither source encodes a preparation order.
- `Malt Extract Agar (Nissui)` names a commercial ingredient from the NBRC recipe, while `Agar` is a separate 18.8 g component. The apparent duplicate use of the word `Agar` is source-supported.
