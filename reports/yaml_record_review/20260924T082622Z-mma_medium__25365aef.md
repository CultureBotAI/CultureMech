# YAML Record Review: mma_medium__25365aef

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mma_medium__25365aef.yaml
- Started UTC: 2026-09-24T08:26:22Z
- Finished UTC: 2026-09-24T08:26:22Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/mma_medium__25365aef.yaml`
- Class: `MediaRecipe`
- Record ID: `CultureMech:010348`
- Label: `mma_medium`
- Original label: `MMA Medium`
- Source grounding: `TOGO:M927`, labelled `MMA Medium`, derived from JCM medium J886
- Category: `bacterial`
- Generated status: generated merged record with `merge_fingerprint: 25365aefee612489275b8b86b565c38267cc8f51f7c4461375267dd12785ac93`
- Maintained owner for future edits: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`

## Validation

- Open schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mma_medium__25365aef.yaml`
  - Result: passed with `No issues found`.
- Strict schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mma_medium__25365aef.yaml --out /private/tmp/mma_medium__25365aef.strict.tsv --workers 1 --quiet`
  - Result: passed; 1 file scanned, 0 files with errors, 0 total error rows. `/private/tmp/mma_medium__25365aef.strict.tsv` has 1 line, the header only.
- LinkML reference validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mma_medium__25365aef.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 1 file validated, 0 reference checks, all validations passed.
- Term validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mma_medium__25365aef.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed.
- Embedded history validation:
  - Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in generated merged YAML.

## Identity and Grounding

- The generated record identity matches TOGO medium M927: the inspected TOGO API response names `MMA Medium` and points to JCM medium J886 as the original source.
- The inspected JCM 886 page and MediaDive J886 REST response confirm the same parent medium, 10 ml trace-element solution addition, pH 7.8 adjustment, and nested trace-element stock.
- The TOGO parent medium has `(NH4)2SO4`, `K2HPO4`, `NaHCO3`, `HEPES`, and 10 ml `Trace element solution`; the separate stock has 1 L water, seven salt rows, and `EDTA x Fe(III)`.
- The maintained TOGO source was found with an exact `rg --files --no-ignore --hidden` search for `TOGO_M927_MMA_Medium.yaml` under `data/normalized_yaml`; ignored files were included.
- An exact `rg --files --no-ignore --hidden` search for `mma_medium` under `data/normalized_yaml` also found a separate MediaDive-derived `data/normalized_yaml/bacterial/mma_medium.yaml` for JCM J886.

## Evidence

- The final source medium adds 10 ml of `Trace element solution`; the YAML moved that row to a `solutions` entry with `concentration.value: '10'`, `unit: G_PER_L`, and an empty `composition`.
- All trace-element stock components are flattened into top-level `ingredients` even though TOGO, JCM, and MediaDive represent them under the trace-element stock, not as final-medium additions.
- TOGO and JCM list `Na2MoO4 x 2 H2O` at 5 mg and `CoCl2 x 6 H2O` at 0.1 mg inside the trace-element stock. The generated YAML carries the same numeric values as 5 and 0.1 `G_PER_L`.
- The generated `CoCl2 x 6 H2O` row is grounded to `CHEBI:35696`, `cobalt dichloride`, despite the source specifying the hexahydrate.
- The generated merged record is stale relative to the maintained normalized source: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml` has a September 2, 2026 `repair_merged_duplicates.py` event that changed duplicated distilled water from the generated `2.0` to `1.0`.

## Completeness

- The generated record preserves the M927 identity, the JCM 886 source note, the parent grams-per-liter rows, and the source pH adjustment text.
- The generated and maintained TOGO records are incomplete for the trace-element stock: they contain an empty stock row and do not preserve the stock composition as nested composition.
- The generated record is stale versus the maintained source because it has not incorporated the duplicate-water repair.
- Medium type and composition type should be rechecked after the stock is repaired: the current `COMPLEX` and `UNDEFINED` classification is hard to reconcile with the defined JCM formula and MediaDive's `complex_medium: no` flag.

## Findings

1. **major - nested trace-element stock was flattened and left empty**
   - Evidence: TOGO M927 and JCM J886 add 10 ml of `Trace element solution` to the parent medium, and the trace stock has its own composition. The YAML has an empty `Trace element solution (see below)` row in `solutions` and flattens stock salts into final-medium `ingredients`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`, via the TOGO importer and `solution-migrator-v1.0`.
2. **major - milligram trace-stock rows were imported as grams per liter**
   - Evidence: `Na2MoO4 x 2 H2O` is 5 mg and `CoCl2 x 6 H2O` is 0.1 mg in the TOGO and JCM source stock. The YAML records 5 `G_PER_L` and 0.1 `G_PER_L`, preserving the numbers but losing the milligram unit.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`, via the TOGO unit mapper.
3. **major - generated distilled water remains a stale duplicate sum**
   - Evidence: the generated record still has distilled water at 2.0 with notes that two 1.0 rows were merged; the maintained normalized source has already collapsed that row back to 1.0.
   - Maintained owner: regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`.
4. **minor - cobalt chloride hexahydrate is grounded to an anhydrous cobalt chloride term**
   - Evidence: the source row is `CoCl2 x 6 H2O`; the YAML grounds it to `CHEBI:35696`, `cobalt dichloride`, rather than a hexahydrate-specific cobalt chloride term.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`, via the source ontology enrichment.
5. **minor - medium classification is inconsistent with the original JCM formula**
   - Evidence: the generated record asserts `medium_type: COMPLEX` and `composition_type: UNDEFINED`, while the source formula is fully chemically specified and the MediaDive J886 response marks `complex_medium` as `no`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml`.

## Recommended Edits

1. Move the trace salts out of top-level `ingredients` and into the `Trace element solution` composition, preserving the parent 10 ml addition.
2. Preserve `Na2MoO4 x 2 H2O` 5 mg and `CoCl2 x 6 H2O` 0.1 mg as stock-composition amounts rather than `G_PER_L` final-medium rows.
3. Correct the `CoCl2 x 6 H2O` ontology term to a cobalt chloride hexahydrate term.
4. Reclassify the record as defined after the nested solution is represented correctly.
5. Regenerate `data/merge_yaml/merged/mma_medium__25365aef.yaml` from the already repaired normalized source so distilled water is no longer 2.0.
6. Investigate whether `data/normalized_yaml/bacterial/TOGO_M927_MMA_Medium.yaml` and the separate `data/normalized_yaml/bacterial/mma_medium.yaml` should be reconciled as JCM 886 variants once units and nesting are correct.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the regenerated `data/merge_yaml/merged/mma_medium__25365aef.yaml`.
- Compare the regenerated parent and trace-stock rows against TOGO M927, JCM 886, and MediaDive J886.
- Re-run exact ignored-inclusive `rg --no-ignore --hidden` checks for `Trace element solution`, `Na2MoO4`, `CoCl2`, `Collapsed 2 identical duplicates`, and `repair_merged_duplicates` against the maintained normalized owner and regenerated merge output.

## Additional Notes

- The generated record predates the September 2, 2026 duplicate-water repair, but the stock flattening and milligram-to-gram problems are still present in the maintained TOGO normalized record and need source-level curation.
