# YAML Record Review: oatmeal_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/oatmeal_agar__628cc513.yaml
- Started UTC: 2026-09-24T18:59:36Z
- Finished UTC: 2026-09-24T19:00:02Z
- Verdict: needs curation

## Target

- Generated record: `data/merge_yaml/merged/oatmeal_agar__628cc513.yaml`
- Maintained owner: `data/normalized_yaml/bacterial/TOGO_M139_Oatmeal_Agar.yaml`
- Source-duplicate sibling: `data/normalized_yaml/bacterial/JCM_J148_OATMEAL_AGAR.yaml`
- Class: `MediaRecipe`
- ID: `CultureMech:007935`
- Label: `oatmeal_agar`
- Source grounding: `TOGO:M139`, imported from JCM medium 148
- Inspected source URL: `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=148`
- Merge state: generated from one source recipe, `TOGO_M139_Oatmeal_Agar`

## Validation

All focused validators passed for the generated YAML.

| Check | Command | Result |
| --- | --- | --- |
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/oatmeal_agar__628cc513.yaml` | Passed with `No issues found`. |
| Strict schema wrapper | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/oatmeal_agar__628cc513.yaml --out /private/tmp/oatmeal_agar__628cc513.strict.tsv --workers 1 --quiet` | Passed; the output TSV had the header only, 1 line and 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/oatmeal_agar__628cc513.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/oatmeal_agar__628cc513.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` deprecation warning. |
| Embedded history | Not run | Not checked: the available `just validate-history` target validates standalone `history/`, not embedded `MediaRecipe.curation_history` in merged YAML. |

## Identity and Grounding

The record denotes the TOGO copy of JCM medium 148, Oatmeal Agar. TOGO `M139` points to `JCM_M148`, and the JCM page lists the same oatmeal, agar, and distilled-water formula.

The maintained TOGO owner has already been repaired to match the inspected JCM source: it uses `1000 ML_PER_L` for the 1 L distilled-water row, adds the heating and autoclaving steps, adds references, and declares the direct JCM import as a `SOURCE_DUPLICATE` parent.

## Evidence

The TOGO API lists 1 L distilled water, 20 g agar, and 30 g Oatmeal (Quaker White Oats). The JCM table confirms those same rows and adds the source preparation instruction: boil the oatmeal with 1 L water over a water bath for 1 hr with occasional stirring, filter through cloth, make back up to 1 L, add agar, and boil until dissolved. JCM also states its default autoclave condition for media unless otherwise stated.

An ignored-inclusive exact search over `data`, `src`, `tests`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md` found both the TOGO `M139` owner and the direct `JCM_J148_OATMEAL_AGAR.yaml` source duplicate, plus the local repair script and tests that updated both owners in September 2026. The generated record still reflects its August 2026 pre-repair state.

## Completeness

The reviewed generated YAML is stale: it encodes 1 L water as `1 G_PER_L`, omits all JCM preparation steps, omits the maintained `SOURCE_DUPLICATE` link to the direct JCM source, and lacks the September curation history, references, role annotation, and data-quality flags now present in the maintained TOGO owner.

Empty pH and organism fields are acceptable for this source because TOGO and JCM do not provide a pH target or organism list for JCM medium 148.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The generated record is stale and still carries the repaired 1 L water-unit bug. | The generated YAML has `Distilled water` at `1 G_PER_L`; the maintained owner and inspected source both represent that source row as 1 L. | Regenerate from `data/normalized_yaml/bacterial/TOGO_M139_Oatmeal_Agar.yaml` |
| Major | The generated record omits source-supported preparation steps that are present upstream. | JCM provides the oatmeal boiling and filtration instructions plus the default autoclave condition, and both maintained JCM 148 source records now preserve them. | Regenerate from `data/normalized_yaml/bacterial/TOGO_M139_Oatmeal_Agar.yaml` and `data/normalized_yaml/bacterial/JCM_J148_OATMEAL_AGAR.yaml` |
| Major | The generated record is still split from the direct JCM source duplicate. | The maintained TOGO owner and direct JCM owner both now declare the `SOURCE_DUPLICATE` relationship, but this generated record was produced from only `TOGO_M139_Oatmeal_Agar`. | Regenerate the JCM 148 source-duplicate group |
| Major | The direct JCM sibling still carries a false KG match. | `JCM_J148_OATMEAL_AGAR.yaml` has `kg_microbe_match: mediadive.medium:12`, but MediaDive medium 12 is Soil Extract Medium, not Oatmeal Agar. | `data/normalized_yaml/bacterial/JCM_J148_OATMEAL_AGAR.yaml` |

## Recommended Edits

1. Remove the false `kg_microbe_match: mediadive.medium:12` from the direct JCM sibling before using it as the canonical merge parent.
2. Regenerate merged YAML so the repaired TOGO/JCM 148 water row, preparation steps, source-duplicate relationship, references, curation history, and data-quality flags replace the stale August output.
3. Confirm the regenerated group coalesces the `TOGO:M139` and `mediadive.medium:J148` records instead of leaving separate generated Oatmeal Agar copies for JCM medium 148.

## Follow-up Checks

- Rerun LinkML schema, strict schema, term, and reference validation on the regenerated JCM 148 Oatmeal Agar record.
- Manually compare the regenerated ingredients and preparation steps against TOGO `M139` and the JCM `GRMD=148` page.
- Rerun an ignored-inclusive exact search for `TOGO_M139_Oatmeal_Agar`, `TOGO:M139`, `JCM_M148`, `GRMD=148`, `JCM_J148_OATMEAL_AGAR`, and `mediadive.medium:J148` to confirm no stale source duplicate remains.

## Additional Notes

No normalized TOGO edit is needed for the water and preparation issues; `TOGO_M139_Oatmeal_Agar.yaml` already contains the September 2026 repair. The generated artifact should not be patched directly.
