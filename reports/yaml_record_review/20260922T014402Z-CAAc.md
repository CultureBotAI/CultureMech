# YAML Record Review: CAAc

- Repository: CultureMech
- Record: data/merge_yaml/merged/CAAc.yaml
- Started UTC: 2026-09-22T01:44:02Z
- Finished UTC: 2026-09-22T01:44:12Z
- Verdict: needs curation

## Target

- Generated record: `CultureMech:015500`, `CAAc`, class `MediaRecipe`.
- Merge lineage: one source recipe, `caac`, on fingerprint `134751c0fef4e9bd44ad616cd4742f463a3d56db2af0797d6c1401113cdb2775`.
- Authoritative owner: `data/normalized_yaml/specialized/caac.yaml`.
- Claimed source identity: CultureBotHT medium `CAAc`, imported from `CAAc.yaml`, source note `Source: FEBA media definitions`.

## Validation

- Open LinkML schema validation passed for the generated merge.
- Strict CultureMech validation passed for the generated merge.
- LinkML reference validation passed for the generated merge with 0 reference checks.
- LinkML term validation passed for the generated merge.
- Embedded `curation_history` was not separately validated because `just validate-history` checks standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in a merged record.

## Identity and Grounding

- The generated record and normalized owner agree on ID `CultureMech:015500`, label `CAAc`, source database `CultureBotHT`, source accession `CAAc`, and the complete 18-row ingredient list. The generated merge only adds its `MERGED_RECIPES` event and fingerprint.
- The specialized category is the CultureBotHT importer's default when a name does not match a bacterial, algae, or fungal name pattern; that fallback is internally consistent with the imported record.
- Seventeen ingredient rows preserve explicit source material names and concentrations and are grounded to exact or source-compatible forms, including hydrate-specific calcium chloride, iron sulfate, zinc sulfate, manganese sulfate, copper sulfate, cobalt nitrate, borax, sodium thiosulfate, and 4-aminobenzoic acid.
- `Yeast Extract` and `casamino acids` are intentionally ungrounded undefined mixtures.
- `ammonium molybdate tetrahydrate` is intentionally explicit but remains ungrounded in the record even though the packaged MediaIngredientMech label index maps the same preferred term to `CHEBI:91249`.

## Evidence

- The only structured source is the CultureBotHT repository root plus the import event naming `CAAc.yaml`; no pinned CultureBotHT commit, blob URL, local raw source file, or primary reference is attached.
- The current `main` branch of `CultureBotAI/CultureBotHT` exposes a reduced `media_yaml/` directory and a recursive Git tree search found no path matching `caa`, so the named upstream `CAAc.yaml` source file was not recoverable from the live source repository.
- Because the source YAML was not recoverable, the review could verify local lineage and importer behavior but could not compare the stated 12.5 mM phosphate salts, 1 g/L ammonium sulfate, UW concentrated-base trace amounts, 0.1 mM sodium thiosulfate pentahydrate, 0.002 g/L 4-aminobenzoic acid, 2 g/L yeast extract, or 5 g/L casamino acids against the authoritative CultureBotHT input.

## Completeness

- Optional preparation, pH, incubation, sterilization, application, `target_organisms`, `variants`, and `references` fields are empty. The inspected local record has no source text for those details, so their absence is not independently a defect.
- The description's `RPal` shorthand is not normalized to a target organism or strain-specific growth evidence; that is appropriate until a CultureBotHT snapshot or primary source makes the organism and medium relationship explicit.
- The `has_unmapped_ingredients` flag is still justified by the missing `term` on ammonium molybdate tetrahydrate, but the local MediaIngredientMech index now supplies a candidate exact mapping.

## Findings

- Major: CultureBotHT `CAAc.yaml` is not reproducible from the record. `data/normalized_yaml/specialized/caac.yaml` stores only the repository root and a filename note; the live upstream tree no longer contains a matching file, leaving every source-derived ingredient and concentration unsupported by a recoverable citation.
- Minor: `ammonium molybdate tetrahydrate` lacks a structured `term` despite a unique packaged MediaIngredientMech preferred-term row for the exact same label and `CHEBI:91249`.
- Minor: the CultureBotHT source identity is encoded only in `sources` and the import event. There is no `source_data` object, pinned upstream commit, or blob/path reference that would let a future curator distinguish the imported FEBA content from later curator additions.

## Recommended Edits

- Curate `data/normalized_yaml/specialized/caac.yaml`, not the generated merge.
- Recover the original CultureBotHT `media_yaml/CAAc.yaml` from a pinned historical commit or another durable import snapshot, then add stable source provenance for that exact input.
- After recovering the upstream file, compare every ingredient identity, amount, unit, and the `Harwood UW media for growing RPal` description against the source before marking the record curated.
- Ground `ammonium molybdate tetrahydrate` to the exact MediaIngredientMech/CHEBI identity if the source also names that hydrate form; otherwise leave the explicit source string unresolved.
- Add a structured reference or `source_data` block for the FEBA/CultureBotHT source path and commit so future merges can be reproduced.

## Follow-up Checks

- Re-run open schema, strict validation, reference validation, and term validation against `data/normalized_yaml/specialized/caac.yaml` after the provenance and grounding repair.
- Re-run `just verify-merges` and `just audit-merge-freshness` after regenerating `data/merge_yaml/merged/CAAc.yaml`.
- Re-check CultureBotHT at the recovered commit and current `main` so the report can distinguish historical upstream provenance from the intentionally reduced live repository.
- Confirm that `data_quality_flags` drops `has_unmapped_ingredients` only if no other ingredient remains ungrounded after the ammonium molybdate repair.

## Additional Notes

- `find . -iname '*caac*'` included ignored and hidden files and found only the generated `CAAc.yaml`, normalized `caac.yaml`, two unrelated generated filenames containing `caac`, and one `.git` object path; no local CultureBotHT raw `CAAc.yaml` capture is present under this checkout.
- A narrower gitignore-independent search for `CultureMech:015500`, `database_id: CAAc`, `CAAc.yaml`, `Harwood UW media for growing RPal`, and `source: caac.yaml` across source and data paths found only the active owner, generated merge, current registry/index references, and one stale July 2026 label-plausibility row for a manganese sulfate grounding that has since been corrected.
