# YAML Record Review: 280

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/280.yaml`
- Started UTC: 2026-09-21T06:17:29Z
- Finished UTC: 2026-09-21T06:18:26Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007484` |
| Name | `280` |
| Original name | `280` |
| Category | `bacterial` |
| Source term | Missing from the generated target; `data/normalized_yaml/bacterial/NBRC_280.yaml` now carries `nbrc.medium:279`, `NBRC Medium 279` |
| Source URL | Missing from structured `references`; the maintained owner now cites `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=279` |
| Merge fingerprint | `579f5214459f27af4136b1379bbd624560b796aab2a3ffecfebc0eead316023e` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_280.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 279 record imported through `NBRC_280.yaml`. The generated artifact is stale: its maintained owner has since been recovered as Tryptone Yeast Extract Agar (TYG) with five structured ingredient rows, final pH, source grounding, and a structured NBRC reference.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/280.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/280.yaml --out /private/tmp/280.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/280.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/280.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The intended source identity is NBRC Medium 279, Tryptone Yeast Extract Agar (TYG):

- The legacy target name is `280`, but its import event says `Media No. 279`, and the maintained owner now carries `name: Tryptone Yeast Extract Agar (TYG)` plus `media_term.term.id: nbrc.medium:279`.
- The inspected live NBRC page for `NO=279` resolves to Medium No. 279, Tryptone Yeast Extract Agar (TYG), and lists the same Bacto Tryptone, yeast extract, glucose, distilled water, agar, and pH 7.0 formulation represented in `data/normalized_yaml/bacterial/NBRC_280.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:007484`, `NBRC_280`, `NO=279`, `Media No. 279`, the merge fingerprint, and the concatenated legacy ingredient found the expected normalized owner, generated target, and a separate TOGO M1496 import for the same NBRC URL. The search included ignored files under the searched paths.

Ingredient grounding in the generated target is unusable because the five source components and pH are collapsed into one ungrounded `VARIABLE` ingredient, followed by an empty-label artifact row.

## Evidence

The inspected NBRC Medium 279 page supports this TYG formulation:

- 3 g Bacto Tryptone (Difco).
- 5 g yeast extract.
- 5 g glucose.
- 1 L distilled water.
- 20 g agar.
- pH 7.0.

Those five components and the final pH are represented in `data/normalized_yaml/bacterial/NBRC_280.yaml`, but the generated target predates that recovery and still has the malformed pre-recovery rows. The target's generic `Dissolve ingredients in distilled water`, `Adjust pH if needed`, and `Sterilize by autoclaving` steps are unsupported by the inspected NBRC table; the NBRC page inspected for this review lists the formulation and final pH but not those procedural steps.

## Completeness

Consequential gaps:

- Tryptone Yeast Extract Agar's five source components are not present as five structured ingredient rows in the generated target.
- `ph_value: 7.0`, `name: Tryptone Yeast Extract Agar (TYG)`, `media_term: nbrc.medium:279`, and the structured NBRC reference are absent from the generated target.
- The generated record is not linked to the separate TOGO M1496 TYG import, even though `data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` names the same NBRC `NO=279` original URL.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected NBRC source establishes a medium formulation, not a growth outcome.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-280.md` report before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated target is stale and still stores the NBRC Medium 279 formulation as a malformed concatenated ingredient plus an empty-label artifact row. | The inspected NBRC page lists five separate component rows and pH 7.0; `data/normalized_yaml/bacterial/NBRC_280.yaml` already carries that recovered structure; `data/merge_yaml/merged/280.yaml` instead has `Bacto Tryptone (Difco)3gYeast extract5gGlucose5gDistilled water1LAgar20gpH 7.0` as one `VARIABLE` ingredient and a second ingredient with `preferred_term: ''`. | Generated `data/merge_yaml/merged/280.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_280.yaml` |
| Major | The generated target omits the recovered TYG source identity, pH, and structured NBRC provenance. | `NBRC_280.yaml` now has `name: Tryptone Yeast Extract Agar (TYG)`, `media_term: nbrc.medium:279`, `ph_value: 7.0`, and a structured NBRC reference; the generated target has none of those fields and was last merged on 2026-08-06. | Generated `data/merge_yaml/merged/280.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_280.yaml` |
| Major | TOGO M1496 duplicates NBRC Medium 279 but remains unreconciled and contains water and pH defects. | `data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` points to `NBRC_M279` and the same NBRC `NO=279` URL, but stores `Distilled water` as `1 G_PER_L`, omits `ph_value: 7.0`, and is not linked to `CultureMech:007484`; the generated layer keeps it as `tryptone_yeast_extract_agar_tyg.yaml`. | `data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` plus reciprocal variant links in `data/normalized_yaml/bacterial/NBRC_280.yaml` |
| Major | Generic unsupported preparation steps remain on the NBRC owner. | The NBRC table inspected for Medium 279 lists the ingredient amounts and pH, but not the generated dissolve/pH/autoclave instructions; unlike nearby recovered NBRC records, `NBRC_280.yaml` still carries these generic steps. | `data/normalized_yaml/bacterial/NBRC_280.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/280.yaml` from `data/normalized_yaml/bacterial/NBRC_280.yaml` so the generated target carries the recovered TYG name, five-row composition, final pH, `media_term: nbrc.medium:279`, and structured NBRC reference.
2. Remove or replace the generic unsupported preparation steps in `data/normalized_yaml/bacterial/NBRC_280.yaml` unless a source with those exact steps is found.
3. Curate `data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml`: correct distilled water from `1 G_PER_L` to a 1 L solvent representation, add `ph_value: 7.0`, add structured TOGO/NBRC references, preserve `TOGO:M1496`, and link it to `data/normalized_yaml/bacterial/NBRC_280.yaml` as a `SOURCE_DUPLICATE` in both directions.
4. Regenerate `data/merge_yaml/merged/` after the maintained curation so the NBRC and TOGO TYG imports are no longer unrelated generated records.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/NBRC_280.yaml` after any source-owner curation.
- `just validate-schema data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` after duplicate curation.
- `just validate-strict data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` after duplicate curation.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` after correcting distilled water, pH, and variant links.
- `just validate-references data/normalized_yaml/bacterial/TOGO_M1496_Tryptone_Yeast_Extract_Agar_TYG.yaml` after adding structured references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 279 for 3 g/L Bacto Tryptone (Difco), 5 g/L yeast extract, 5 g/L glucose, 1 L distilled water, 20 g/L agar, and pH 7.0.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated target has no structured `references` entries.
- The official NBRC page gives the title `Tryptone Yeast Extract Agar (TYG)`; the generated target's `name: 280` is a legacy import artifact already repaired upstream in `NBRC_280.yaml`.
