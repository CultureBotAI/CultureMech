# YAML Record Review: ams1_pyruvate_glycine

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml`
- Started UTC: 2026-09-21T11:37:15Z
- Finished UTC: 2026-09-21T11:37:30Z
- Verdict: needs curation

## Target

| Field | Observed value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:007386` |
| Name | `ams1_pyruvate_glycine` |
| Original name | `'''AMS1 (Pyruvate+Glycine` |
| Source identity | MediaDB Medium 479, `MEDIADB:479` |
| Generated status | Generated one-source merge |
| Generated path | `data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml` |
| Maintained input | `data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml` |

This is the AMS1 pyruvate/glycine plus thiamin plus phosphite MediaDB variant. The `__625e5170` suffix distinguishes it from other AMS1 glycine formulations with the same normalized name.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml` | Passed; no issues found. |
| Strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml --out /private/tmp/ams1_pyruvate_glycine__625e5170.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 `ERROR` rows emitted. |
| Reference | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Documented focused validator | Not checked: no focused embedded `MediaRecipe.curation_history` validator is documented for one merged record; `just validate-history` targets standalone files under `history/`. |

The documented `just validate-schema`, `just validate-strict`, and `just validate-terms` entrypoints are unavailable in this checkout because project `uv` attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools before target-specific validation. The no-project commands above exercise the same schema and validation code with Python 3.11.

## Identity and Grounding

The generated record's ID, source accession, and phosphite ingredient point to MediaDB Medium 479, but several source labels are stale:

- `id: CultureMech:007386`
- `media_term.term.id: MEDIADB:479`
- stale `original_name: '''AMS1 (Pyruvate+Glycine`
- stale `media_term.term.label: '''AMS1 (Pyruvate+Glycine`
- stale `preferred_term: '''Iron(III`

The normalized owner has already repaired these to `AMS1 (Pyruvate+Glycine) + Thiamin + Phosphite` and `Iron(III) chloride`; two `repair_mediadb_names.py` events documenting that repair are present only upstream, not in this generated target.

Unresolved or legacy groundings remain:

- `Thiamine` still uses legacy `mediaingredientmech_term: MediaIngredientMech:000898`.
- `Sodium Phosphite` has no ontology `term`.
- `Iron(III) chloride` has no ontology `term` in the repaired normalized owner.

## Evidence

Inspected source documents and maintained evidence:

- MediaDB Medium 479 page and tab-delimited export from `https://mediadb.systemsbiology.net/defined_media/media/479/`
- `data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml`

Supported by MediaDB Medium 479:

- Media name `AMS1 (Pyruvate+Glycine) + Thiamin + Phosphite`.
- Sodium Phosphite as the distinguishing 0.001 mM compound on top of the AMS1 pyruvate/glycine plus thiamin base.
- `Iron(III) chloride`, not the generated `'''Iron(III`, as the iron ingredient.

Unsupported or mismatched claims:

- The generated medium label and iron ingredient are stale and still truncated at an opening parenthesis.
- The generic preparation steps are not supported by the inspected MediaDB 479 page, which exposes composition and growth metadata but not a distilled-water dissolve step, conditional pH adjustment, or 0.22 micrometer filtration instruction.

## Completeness

Consequential gaps:

- The generated target was not regenerated after the August 31, 2026 normalized MediaDB name repairs.
- Exact ontology groundings for `Sodium Phosphite` and `Iron(III) chloride` are still absent.
- MediaDB Medium 479 has growth rows that have not been curated into `target_organisms`; the current record does not make a contradictory growth claim.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*ams1_pyruvate_glycine__625e5170.md'` found no prior report for this exact generated target.
- `rg --no-ignore --hidden` for the generated fingerprint, `MEDIADB:479`, and `CultureMech:007386` found the stale generated record and the repaired normalized MediaDB 479 owner.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The generated record is stale relative to `data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml`. | The generated file still has `'''AMS1 (Pyruvate+Glycine` and `'''Iron(III`; the normalized owner repaired them to `AMS1 (Pyruvate+Glycine) + Thiamin + Phosphite` and `Iron(III) chloride` on 2026-08-31. | `data/merge_yaml/merged/` regeneration from `data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml` |
| Major | Preparation steps are unsupported generic import text. | The inspected MediaDB 479 page lists formula/growth metadata but no water solvent, pH adjustment, or filter-sterilization protocol. | MediaDB importer or `data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml` |
| Minor | Three source-supported MediaDB compounds remain legacy-linked or ungrounded. | `Thiamine` still has a legacy MIM link, and `Sodium Phosphite` plus `Iron(III) chloride` lack ontology `term` mappings. | Post-repair ingredient grounding |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/ams1_pyruvate_glycine__625e5170.yaml` so the existing normalized MediaDB name repair is reflected in the generated record.
2. Replace the generic importer preparation steps with inspected MediaDB-supported preparation only.
3. Ground `Sodium Phosphite` and `Iron(III) chloride` to exact ontology terms, and migrate `Thiamine` to the CHEBI-keyed MIM link form if an exact id-safe entry is available.

## Follow-up Checks

- Rerun `just verify-merges` and `just audit-merge-freshness` after regenerating the merge outputs.
- Rerun `just validate data/normalized_yaml/bacterial/MEDIADB_479_AMS1_Pyruvate_Glycine.yaml` and the no-project single-record schema/strict/term/reference checks after any normalized curation.
- Manually compare the regenerated MediaDB 479 record with the live MediaDB tab-delimited export to ensure only the phosphite AMS1 variant is represented.

## Additional Notes

- This record intentionally stays separate from MediaDB 475 and 476; the common normalized name is not enough to merge across their different compound sets.
