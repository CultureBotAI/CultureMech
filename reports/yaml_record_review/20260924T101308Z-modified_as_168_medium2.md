# YAML Record Review: Modified AS-168 Medium2

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/modified_as_168_medium2.yaml
- Started UTC: 2026-09-24T10:13:08Z
- Finished UTC: 2026-09-24T10:13:08Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Record | `data/merge_yaml/merged/modified_as_168_medium2.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008739` |
| Name | `modified_as_168_medium2` |
| Original name | `Modified AS-168 Medium2` |
| Category | `bacterial` |
| Medium source | TOGO `M2145`; original source NBRC `NBRC_M1496` |
| Maintained owners | `data/normalized_yaml/bacterial/modified_as_168_medium.yaml`; `data/normalized_yaml/bacterial/modified_as_168_medium2.yaml`; merge logic |
| Generated status | Incorrectly merged output from two distinct normalized TOGO records |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema validation, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/modified_as_168_medium2.yaml` | Passed; exited 0 with "No issues found". |
| Strict validation, `python scripts/validate_strict.py data/merge_yaml/merged/modified_as_168_medium2.yaml --out /private/tmp/modified_as_168_medium2.strict.tsv --workers 1 --quiet` | Passed; 0 strict errors were reported and `/private/tmp/modified_as_168_medium2.strict.tsv` contained only the header line. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/modified_as_168_medium2.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the focused run completed with 0 reference checks. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/modified_as_168_medium2.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not inline `MediaRecipe.curation_history` entries in a merged YAML record. |

## Identity and Grounding

The generated record has the wrong source identity. `CultureMech:008739`, `TOGO:M2145`, and `NBRC_M1496` identify Modified AS-168 Medium2, but the generated ingredient values for MgSO4 x 7H2O, NaCl, and sodium pyruvate come from a different record: Modified AS-168 Medium, `CultureMech:008736`, `TOGO:M2141`, and `NBRC_M1484`.

Exact `find data/normalized_yaml -name ...` searches, which do not honor gitignore exclusions, found both maintained owners:

| Normalized file | Source |
| --- | --- |
| `data/normalized_yaml/bacterial/modified_as_168_medium.yaml` | TOGO `M2141`; NBRC `NBRC_M1484`; pH 8.0; 20 g MgSO4 x 7H2O; 150 g NaCl; 0.1 g sodium pyruvate |
| `data/normalized_yaml/bacterial/modified_as_168_medium2.yaml` | TOGO `M2145`; NBRC `NBRC_M1496`; pH 8.5; 5 g MgSO4 x 7H2O; 200 g NaCl; 0.5 g sodium pyruvate |

The source records are related but not duplicate recipes; they should not have been merged into one fingerprint.

## Evidence

NBRC `NO=1496` and TOGO `M2145` support the Medium2 formulation:

| Source claim | Record representation | Review |
| --- | --- | --- |
| Medium2 contains 5 g MgSO4 x 7H2O, 200 g NaCl, and 0.5 g sodium pyruvate. | The generated Medium2 record has 20, 150, and 0.1 `G_PER_L` for those rows. | Unsupported; those are Medium values from NBRC `NO=1484`. |
| Medium2 contains 50 mg FeSO4 x 7H2O and 0.36 mg MnCl2 x 4H2O. | The record stores those as 50 and 0.36 `G_PER_L`. | Unsupported milligram-to-gram promotion. |
| Medium2 uses 1 L distilled water. | The record stores water as `1 G_PER_L`. | Unsupported final-volume import. |
| Medium2 is pH 8.5. | No pH is represented. | Incomplete. |
| Agar is 15 g if needed. | `Agar (if needed)` is a 15 `G_PER_L` ingredient, but the whole merged recipe is classified as `SOLID_AGAR`. | The optional agar amount is present; the physical state overstates it as unconditional solid agar. |

NBRC `NO=1484` / TOGO `M2141` support the sibling Modified AS-168 Medium formulation with 20 g MgSO4 x 7H2O, 150 g NaCl, 0.1 g sodium pyruvate, the same 50 mg FeSO4 x 7H2O and 0.36 mg MnCl2 x 4H2O, and pH 8.0.

## Completeness

The merged file is not complete enough to use for either AS-168 recipe because it conflates Medium2 identity with Medium ingredient values. It also loses the pH of both source records and carries the shared FeSO4 and MnCl2 milligram values as gram-per-liter concentrations.

Empty target-organism and growth-evidence fields were not treated as defects. NBRC `NBRC_M1484` and `NBRC_M1496` are medium formulation pages, not growth-evidence pages.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| blocker | Two distinct AS-168 formulations were merged into one generated Medium2 record. | M2141/NBRC 1484 and M2145/NBRC 1496 differ in MgSO4 x 7H2O, NaCl, sodium pyruvate, and pH; the generated `CultureMech:008739` Medium2 record carries M2141 ingredient values under M2145 identity. | `data/normalized_yaml/bacterial/modified_as_168_medium.yaml`; `data/normalized_yaml/bacterial/modified_as_168_medium2.yaml`; `merge_recipes.py` fingerprinting. |
| major | Source milligram amounts were promoted to gram-per-liter concentrations. | Both NBRC sources list FeSO4 x 7H2O as 50 mg and MnCl2 x 4H2O as 0.36 mg; the record stores them as 50 and 0.36 `G_PER_L`. | Both normalized AS-168 records; TOGO unit normalization. |
| major | The 1 L water row is represented as a mass concentration. | Both NBRC sources list 1 L distilled water; the record stores `Distilled water` as `1 G_PER_L`. | Both normalized AS-168 records; TOGO unit normalization. |
| major | Source pH values are absent. | M2145/NBRC 1496 has pH 8.5 and M2141/NBRC 1484 has pH 8.0; the generated merge has no pH. | Both normalized AS-168 records; TOGO/NBRC pH import. |
| minor | Optional agar is modeled as unconditional `SOLID_AGAR`. | Both NBRC sources say `Agar (if needed)` at 15 g; the record classifies the merged recipe as `SOLID_AGAR`. | Both normalized AS-168 records; physical-state/variant curation. |

## Recommended Edits

1. Fix merge fingerprinting so `data/normalized_yaml/bacterial/modified_as_168_medium.yaml` and `data/normalized_yaml/bacterial/modified_as_168_medium2.yaml` are no longer merged as duplicate recipes.
2. Re-curate both normalized AS-168 records so FeSO4 x 7H2O and MnCl2 x 4H2O retain milligram quantities, water is represented as a final 1 L volume, and the distinct pH values 8.0 and 8.5 are imported.
3. Preserve the optional status of the 15 g agar row instead of classifying either base medium as unconditional solid agar.
4. Regenerate merge output and confirm that M2141 and M2145 remain two distinct generated records or are represented as explicit source-supported variants rather than one merged record.

## Follow-up Checks

1. Re-run focused open-schema, strict, reference, and term validators on both normalized AS-168 records and on the regenerated merge output.
2. Manually compare regenerated M2141 against NBRC `NO=1484` and regenerated M2145 against NBRC `NO=1496`, checking MgSO4 x 7H2O, NaCl, sodium pyruvate, FeSO4 x 7H2O, MnCl2 x 4H2O, water, pH, and agar.
3. Verify that no generated AS-168 record mixes a stable ID or `media_term` from one source with ingredient values from the other.

## Additional Notes

The repository-level `just` validators were not used for this focused record review because the project currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused validators above were run with Python 3.11 and the offline uv cache.
