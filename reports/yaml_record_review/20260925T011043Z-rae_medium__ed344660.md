# YAML Record Review: RAE medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/rae_medium__ed344660.yaml
- Started UTC: 2026-09-25T01:09:20Z
- Finished UTC: 2026-09-25T01:10:43Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/rae_medium__ed344660.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:006655` |
| Label | `RAE medium` |
| Category | `bacterial` |
| Source identity | `komodo.medium:848`, annotated as copied from DSMZ Medium 848 |
| Generated status | Generated merge of `data/normalized_yaml/bacterial/KOMODO_848_RAE_medium.yaml` and `data/normalized_yaml/bacterial/rae_medium.yaml`, with `merge_fingerprint: ed3446604448637060e9d83bdf1473619bca09ec12eb00f3836bbf8bdf18f970`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/rae_medium__ed344660.yaml` reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/rae_medium__ed344660.yaml --out /private/tmp/rae_medium_ed344660.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed trivially; `linkml-reference-validator validate data data/merge_yaml/merged/rae_medium__ed344660.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 checks, and reported all validations passed. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/rae_medium__ed344660.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- KOMODO 848 is annotated locally as DSMZ Medium 848, and the generated record marks `KOMODO_848_RAE_medium` as a `SOURCE_DUPLICATE` of the DSMZ/MediaDive owner `data/normalized_yaml/bacterial/rae_medium.yaml`.
- Live MediaDive Medium 848 resolves to DSMZ source `RAE MEDIUM`, pH 3.8, and the same DSMZ Medium 848 PDF link recorded in the direct owner.
- The inspected DSMZ PDF confirms that Medium 848 has eight ingredients: glucose, peptone, yeast extract, citric acid x H2O, Na2HPO4 x 2 H2O, glacial acetic acid, absolute ethanol, and distilled water.
- The record has the right RAE Medium 848 identity and duplicate-owner relationship, but its ingredients contain Medium 631 rows that were imported from a plating-method cross-reference rather than from the Medium 848 formula.

## Evidence

- Supported:
  - DSMZ Medium 848 and MediaDive 848 support the RAE Medium identity, pH 3.8, liquid state, and the core Medium 848 gram-valued rows for glucose, peptone, yeast extract, citric acid x H2O, and Na2HPO4 x 2 H2O.
  - The `SOURCE_DUPLICATE` relationship between the KOMODO and DSMZ owners is locally supported: the KOMODO record says it copied DSMZ Medium 848, and the duplicate report names this pair.
- Unsupported or over-scoped:
  - The source has 40 g/L glucose, 10 g/L peptone, and 10 g/L yeast extract; the generated record has 45, 13, and 12 g/L after summing the 5, 3, and 2 g rows from DSMZ Medium 631.
  - The 40 g/L `Acetic acid` and 30 g/L `Ethanol` rows come from DSMZ Medium 631, not DSMZ Medium 848.
  - Distilled water is represented as `980 G_PER_L`, but DSMZ Medium 848 and MediaDive 848 list 980 ml.
  - Glacial acetic acid and Absolute ethanol are represented as `10 G_PER_L`, but DSMZ Medium 848 and MediaDive 848 list 10 ml each.

## Completeness

- Both maintained duplicate owners, `KOMODO_848_RAE_medium.yaml` and `rae_medium.yaml`, have the same Medium 631 ingredient flattening, inflated duplicate amounts, and liquid-unit errors.
- The generated KOMODO-canonical record drops the source pH and preparation text that the DSMZ/MediaDive owner still carries.
- The DSMZ/MediaDive owner also carries the actual Medium 631 preparation text as a full preparation step; Medium 631 should instead be referenced only as the analogy for preparing a lower 0.5% agar layer overlaid with a 1.0% agar layer using Medium 848 components.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `komodo.medium:848`, `mediadive.medium:848`, `DSMZ Medium: 848`, `CultureMech:006655`, `CultureMech:002005`, `KOMODO_848_RAE_medium`, and `rae_medium__ed344660` found the KOMODO owner, the DSMZ/MediaDive owner, their generated merged record, source indexes, the DSMZ/KOMODO duplicate review row, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Blocker | DSMZ Medium 631 ingredients have been flattened into RAE Medium 848. | MediaDive lists Medium 631 only because the DSMZ 848 solid-medium instructions compare the double-layer agar method to Medium 631. The maintained owners summed Medium 631 glucose, peptone, and yeast amounts into the RAE rows and retained Medium 631 acetic acid and ethanol as extra ingredients. | Fix both `data/normalized_yaml/bacterial/KOMODO_848_RAE_medium.yaml` and `data/normalized_yaml/bacterial/rae_medium.yaml`. |
| Major | Three legitimate liquid rows have wrong mass units. | DSMZ Medium 848 lists 980 ml distilled water, 10 ml glacial acetic acid, and 10 ml absolute ethanol; the generated artifact encodes those as `G_PER_L`. | Fix both duplicate normalized owners, then regenerate. |
| Major | Source preparation semantics are distorted or missing. | The generated KOMODO-canonical record has no `preparation_steps`; the DSMZ owner includes the RAE preparation text plus the full Medium 631 step rather than only the RAE-specific analogy to the Medium 631 double-layer method. | Repair the DSMZ and KOMODO owners so only Medium 848 preparation claims are represented. |

## Recommended Edits

1. Remove the imported Medium 631 ingredient contributions from both duplicate owners so glucose, peptone, and yeast extract return to 40, 10, and 10 g/L and the extra 40 g acetic acid plus 30 g ethanol rows disappear.
2. Change Distilled water, Glacial acetic acid, and Absolute ethanol to source-faithful milliliter units.
3. Preserve the pH 3.8 statement and encode the DSMZ Medium 848 preparation text without copying Medium 631's own ingredients as part of the RAE formula.
4. Add structured references for KOMODO 848 and DSMZ/MediaDive 848, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/rae_medium__ed344660.yaml`.
- Re-run a gitignore-independent exact search for `komodo.medium:848`, `mediadive.medium:848`, and `CultureMech:006655` to confirm the duplicate relation remains explicit after both owners are repaired.
- Re-fetch MediaDive Medium 848 and the DSMZ Medium 848 PDF to confirm only Medium 848 rows are retained and the Medium 631 plating method is represented only as a referenced analogy.

## Additional Notes

- This DSMZ/KOMODO RAE record is distinct from the JCM M891 and NBRC M1349 RAE Medium branches reviewed immediately before it.
