# YAML Record Review: CMC MEDIUM (N2/CO2)

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cmc_medium_n2_co2__083142b0.yaml`
- Started UTC: `2026-09-22T10:18:19Z`
- Finished UTC: `2026-09-22T10:20:04Z`
- Verdict: needs curation

## Target

Generated bacterial `MediaRecipe` record `CultureMech:000545` for DSMZ Medium `110a`, `CMC MEDIUM (N2/CO2)`, generated from `data/normalized_yaml/bacterial/cmc_medium_n2_co2.yaml` on merge fingerprint `083142b0d27b6b6851521afc1e1a3a70352566da07db83e18053870da6dad629`.

## Validation

- LinkML open schema validation: passed.
- Strict validation: passed with no errors in `/private/tmp/cmc_medium_n2_co2_083142b0.strict.tsv`.
- LinkML reference validation: passed; the validator reported 0 total checks.
- LinkML term validation: passed.
- Embedded `curation_history` entries: not checked by the standalone history validator.

## Identity and Grounding

The source identity is correct: `mediadive.medium:110a` denotes DSMZ Medium 110a, `CMC MEDIUM (N2/CO2)`.

The main K2HPO4, L-cysteine hydrochloride hydrate, Na2CO3, D-Glucose, Cellobiose, and Starch groundings are chemically plausible. The generated `NaOH` row is grounded to sodium hydroxide, but the source material is 25 ml of 1 N NaOH used to make meat filtrate rather than a 25 g/L final NaOH addition.

An ignored-inclusive search of `data/merge_yaml/merged` found exactly one sibling generated branch, `data/merge_yaml/merged/cmc_medium_n2_co2.yaml`. The full-repository ignored-inclusive search also found the standalone direct-MediaDive solution input `data/normalized_yaml/bacterial/mediadive_188_Main_sol_110a.yaml`.

## Evidence

The DSMZ Medium 110a PDF supports the medium label, pH 7.0, the 80% N2 / 20% CO2 anaerobic atmosphere, the 121 C for 20 min autoclave step, and the separate post-autoclave sugar and carbonate stock additions.

The source does not support the generated `400 G_PER_L` D-Glucose, `100 G_PER_L` Cellobiose, `100 G_PER_L` D-Maltose, or `100 G_PER_L` Starch rows. DSMZ lists those masses as the contents of a 10 ml sugar mix added to the medium, not as final g/L ingredient concentrations.

DSMZ lists 500 g ground beef, 25 ml 1 N NaOH, and 975 ml distilled water as inputs to prepare meat filtrate and then calls for 1000 ml meat filtrate in the main medium. The generated record discards the meat-filtrate boundary and lists `Ground beef` and `NaOH` as final top-level medium ingredients.

## Completeness

The record preserves `ph_value: 7.0` and the main DSMZ preparation prose better than the TOGO branch.

The sugar mix and meat filtrate stock/preparation boundaries are absent. The generated record also omits a final `Meat filtrate` component, so a reader sees 500 g/L ground beef and 25 g/L NaOH but no structured statement that these rows produce the filtrate used by the medium.

The second preparation step is typed as `FILTER_STERILIZE`, but the source says to boil the meat/water/NaOH mixture, cool, skim, and filter it; that is a filtration operation, not sterilization by filtration.

No target organism or growth evidence is present.

## Findings

- Severe: the sugar-mix components are stored at stock strength as final medium concentrations: D-Glucose `400 G_PER_L`, Cellobiose `100 G_PER_L`, D-Maltose `100 G_PER_L`, and Starch `100 G_PER_L`.
- Severe: meat-filtrate internals are flattened into final ingredients, including `Ground beef` at `500 G_PER_L` and NaOH at `25 G_PER_L`.
- Major: the meat-filtrate component itself is missing from the final recipe even though DSMZ supplies the main medium with 1000 ml meat filtrate.
- Major: preparation step 2 is mislabeled `FILTER_STERILIZE`; the DSMZ text describes boiling and filtering meat filtrate, not filter-sterilizing a medium component.
- Major: this direct DSMZ branch remains unmerged with TOGO M2717, which points to the same DSMZ 110a source PDF.

## Recommended Edits

- In `data/normalized_yaml/bacterial/cmc_medium_n2_co2.yaml`, replace the flattened sugar stock-strength rows with a structured 10 ml sugar mix addition or correctly diluted final carbohydrate concentrations while preserving the stock recipe.
- Restore `Meat filtrate` as the top-level 1000 ml component and move ground beef, 1 N NaOH, and distilled water into the meat-filtrate preparation boundary.
- Retype or split the meat-filtrate preparation step so it does not assert filter sterilization.
- Regenerate `data/merge_yaml/merged/` and merge or deliberately link this direct DSMZ branch with `data/normalized_yaml/bacterial/TOGO_M2717_CMC_Medium_N2_CO2.yaml` after both records encode DSMZ 110a equivalently.

## Follow-up Checks

- Validate the regenerated record with open schema, strict, reference, and term validators.
- Confirm D-Glucose, Cellobiose, D-Maltose, and Starch are no longer present at 100x stock concentrations.
- Confirm ground beef and NaOH are not final top-level g/L medium ingredients.
- Confirm the meat-filtrate preparation no longer has a `FILTER_STERILIZE` action unless source evidence for filter sterilization is added.
- Confirm `cmc_medium_n2_co2.yaml` and `cmc_medium_n2_co2__083142b0.yaml` merge once both inputs encode DSMZ 110a equivalently.

## Additional Notes

`data/normalized_yaml/bacterial/mediadive_188_Main_sol_110a.yaml` carries MediaDive's separate `Main sol. 110a` solution, but the generated DSMZ branch has the solution flattened into final ingredients rather than referenced as a solution. Empty optional fields are not defects.
