# YAML Record Review: togo_medium_m1479

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1479.yaml`
- Started UTC: 2026-09-25T13:59:13Z
- Finished UTC: 2026-09-25T13:59:13Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1479, imported from NBRC medium 261.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1479.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1479`; the TOGO API identifies NBRC_M261 as its original medium ID and links NBRC medium 261.

The TOGO payload and NBRC page agree on 5 g yeast extract, 5 g malt extract, 5.74 g K2HPO4, 1.15 g NH4H2PO4, 0.06 g MgSO4 x 7 H2O, 60 mg catalase, 1 L distilled water, 15 g agar if needed, pH 7.0, and separate filter sterilization for catalase.

## Evidence

The generated yeast extract, malt extract, phosphate, ammonium phosphate, magnesium sulfate, and agar rows match the source gram quantities.

The generated record moves catalase into an empty `Unknown solution` and converts 60 mg to 60 g/L, which is a 1000-fold mass-unit error before accounting for the empty solution structure.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The catalase filter-sterilization instruction is absent from the generated record.

## Findings

- Major issue: `Catalase*` is modeled as an empty solution named `Unknown solution` instead of a filter-sterilized final-medium additive.
- Major issue: catalase is represented as 60 g/L instead of the source 60 mg/L-scale addition.
- Minor issue: pH 7.0 is absent.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.
- Minor issue: the catalase filter-sterilization instruction is absent.

## Recommended Edits

- Move catalase out of the empty solution wrapper, preserve the 60 mg source amount, and add a filter-sterilization preparation note.
- Add `ph_value: 7.0`.
- Correct or omit the water row according to the repo's final-volume convention.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Add or run a regression check for starred filter-sterilization notes so they annotate the relevant additive rather than creating empty solutions.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1479 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
