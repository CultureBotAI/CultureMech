# YAML Record Review: EUBACTERIUM MEDIUM

- Repository: CultureMech
- Record: data/merge_yaml/merged/eubacterium_medium__c1146137.yaml
- Started UTC: 2026-09-23T00:41:56Z
- Finished UTC: 2026-09-23T00:44:29Z
- Verdict: needs curation

## Target

`data/merge_yaml/merged/eubacterium_medium__c1146137.yaml` is the generated direct JCM/MediaDive record for `mediadive.medium:J157`. The maintained owner is `data/normalized_yaml/bacterial/eubacterium_medium.yaml`.

The exact local source search also found `data/merge_yaml/merged/EUBACTERIUM_MEDIUM.yaml`, generated from TOGO M148 / JCM M157. That sibling was repaired in `data/normalized_yaml/bacterial/TOGO_M148_Eubacterium_Medium.yaml` on 2026-09-11, but the direct JCM owner reviewed here still has the original collapsed commercial-medium row.

## Validation

- LinkML open schema: passed.
- Strict validator: passed with 0 error rows in `/private/tmp/eubacterium_j157.strict.tsv`.
- Reference validator: passed; 0 reference checks were applicable.
- Term validator: passed.
- Embedded history: Not checked; the history validator covers standalone files under `history/`, not embedded `MediaRecipe.curation_history` lists.

## Identity and Grounding

The `mediadive.medium:J157` media term and source URL identify the EUBACTERIUM MEDIUM import. JCM's live GRMD 157 URL currently returns "Nothing found", but MediaDive J157 and TOGO M148 both preserve the same JCM-derived formula.

Three ingredient identities are sound: beef brain powder at 0.3 g/l, sodium thioglycolate at 1.3 g/l, and yeast extract at 10 g/l. The fourth dry ingredient is not sound: the source has `Fluid thioglycolate` with a BD-BBL attribute, whereas the generated row is `Thioglycolate` grounded to `CHEBI:30066` for the single thioglycolate anion and still uses deprecated `mediaingredientmech_term`.

## Evidence

MediaDive J157 describes one 1000 ml main solution with:

- 0.3 g Beef brain powder.
- 1.3 g Sodium thioglycolate.
- 10 g Yeast extract.
- 29.5 g Thioglycolate with the `BD-BBL, fluid` attribute.
- 1000 ml Distilled water.

TOGO M148 carries the same JCM M157 recipe and makes the intended fourth ingredient explicit as `Fluid thioglycolate (BD-BBL)`, labelled as a complex and undefined BBL Fluid Thioglycollate Medium product.

## Completeness

No preparation steps, pH, incubation conditions, or target organisms are available from the accessible MediaDive J157 and TOGO M148 payloads, so their absence is not a source mismatch in the reviewed record.

The generated record omits the explicit 1000 ml distilled-water row that is present in both accessible source payloads.

## Findings

| Severity | Finding | Evidence | Recommendation |
| --- | --- | --- | --- |
| Major | A commercial BD-BBL Fluid Thioglycollate Medium powder is represented as a single thioglycolate anion. | MediaDive J157 records `Thioglycolate` with attribute `BD-BBL, fluid`, and TOGO M148 records `Fluid thioglycolate (BD-BBL)` as a complex undefined product; the generated YAML has `preferred_term: Thioglycolate`, `29.5 G_PER_L`, and `CHEBI:30066` for thioglycolate(1-). | Rename the ingredient to `Fluid thioglycolate (BD-BBL)` or equivalent and remove the single-molecule CHEBI grounding. |
| Major | The direct MediaDive J157 and TOGO M148 records have not been de-duplicated. | Both derive from JCM M157 Eubacterium Medium, but the generated directory contains `eubacterium_medium__c1146137.yaml` and `EUBACTERIUM_MEDIUM.yaml` with different CultureMech IDs. | Merge or alias the two records after applying the TOGO M148 commercial-medium repair to the direct JCM owner. |
| Minor | The solvent row is missing. | MediaDive J157 and TOGO M148 both include 1000 ml distilled water; the generated direct JCM record has only the four dry rows. | Add `Distilled water` at 1 L if solvent rows are retained for this corpus. |
| Minor | The Fluid Thioglycollate row still has a deprecated MIM field. | The generated YAML carries `mediaingredientmech_term: MediaIngredientMech:000649` on the misidentified `Thioglycolate` row, while most rows have already moved to CHEBI-keyed fields or no single-compound grounding. | Remove the deprecated MIM field when de-grounding the commercial ingredient. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/eubacterium_medium.yaml`, replace `Thioglycolate` with ungrounded `Fluid thioglycolate (BD-BBL)` at `29.5 G_PER_L`.
2. Remove the `CHEBI:30066` and `MediaIngredientMech:000649` mappings from that commercial product row.
3. Optionally copy the curated source annotations from the repaired TOGO M148 owner so the direct record records the TOGO/MediaDive agreement and the live-JCM limitation.
4. De-duplicate the direct JCM and TOGO M148 records, then regenerate the merged YAML.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation on the regenerated record.
- Confirm no generated Eubacterium Medium row points 29.5 g/l at `CHEBI:30066`.
- Confirm only one generated JCM M157 / TOGO M148 Eubacterium Medium record remains unless duplicate source records are intentionally preserved.

## Additional Notes

None found.
