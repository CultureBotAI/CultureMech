# YAML Record Review: pby_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pby_medium__0191b6b3.yaml
- Started UTC: 2026-09-24T19:52:48Z
- Finished UTC: 2026-09-24T19:52:48Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:002553 |
| Name | pby_medium |
| Original name | PBY MEDIUM |
| Source identity | JCM Medium 191 / MediaDive J191 |
| Source path | data/normalized_yaml/bacterial/pby_medium.yaml |
| Generated path | data/merge_yaml/merged/pby_medium__0191b6b3.yaml |
| Merge fingerprint | 0191b6b3ad4cfc24ee62c9a6fb1b79f3e979c7b78c8133c82e3c33210cafb921 |

The target is a generated merged record. Future edits should be made in the
maintained normalized inputs and by regenerating merged YAML, not directly in
`data/merge_yaml/merged/pby_medium__0191b6b3.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pby_medium__0191b6b3.yaml` exited 0 and reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/pby_medium__0191b6b3.yaml --out /private/tmp/pby_medium__0191b6b3.strict.tsv --workers 1 --quiet` exited 0 and wrote only the TSV header. |
| Reference validator | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/pby_medium__0191b6b3.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 checks. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/pby_medium__0191b6b3.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

## Identity and Grounding

The medium identity is correct for JCM Medium 191 / PBY MEDIUM. The generated
`mediadive.medium:J191` term resolves through MediaDive to the same JCM GRMD
191 URL in the record notes.

The three represented complex nutrients match the JCM page and MediaDive J191:

| JCM 191 row | Generated representation |
| --- | --- |
| Bacto peptone (BD-Difco), 5.0 g | `Bacto peptone`, 5 `G_PER_L` |
| Beef extract (BD-Difco), 3.0 g | `Beef extract`, 3 `G_PER_L` |
| Yeast extract (BD-Difco), 1.0 g | `Yeast extract`, 1 `G_PER_L` |

## Evidence

The inspected JCM 191 HTML and MediaDive J191 REST payload both support the PBY
name, pH 7.0, 5 g Bacto peptone, 3 g beef extract, 1 g yeast extract, and the
1 L distilled-water row. The generated target omits that water row.

The maintained input `data/normalized_yaml/bacterial/pby_medium.yaml` has
already been repaired after this generated record was produced. It now includes
the 1 L water row, source notes, exact FoodOn and MICRO groundings for the
three complex ingredients, and a JCM reference.

An ignored-inclusive PCRE2 search for `mediadive.medium:J191`, `Source: JCM,
ID: J191`, `GRMD=191`, and `PBY MEDIUM` found:

- `data/normalized_yaml/bacterial/pby_medium.yaml`, the direct JCM/MediaDive
  maintained input.
- `data/normalized_yaml/bacterial/TOGO_M184_PBY_Medium.yaml`, a TOGO M184
  projection from the same JCM 191 source.
- `data/merge_yaml/merged/pby_medium__0191b6b3.yaml`, this stale generated
  direct JCM record.
- `data/merge_yaml/merged/PBY_MEDIUM.yaml`, a stale generated TOGO M184 sibling.
- Repair scripts that have already updated the maintained direct JCM and TOGO
  records.

## Completeness

The generated target is materially incomplete because it is stale relative to
its maintained input and omits the repaired water row and exact complex
ingredient groundings.

The duplicate TOGO M184 projection has also been repaired in
`data/normalized_yaml/bacterial/TOGO_M184_PBY_Medium.yaml`, but the reviewed
generated record still emits as a singleton instead of merging with that exact
source duplicate.

No `target_organisms` are expected on this provider recipe. That optional slot
is correctly empty.

## Findings

### Major

- `data/merge_yaml/merged/pby_medium__0191b6b3.yaml` is stale relative to
  `data/normalized_yaml/bacterial/pby_medium.yaml`. It lacks the maintained
  input's `Distilled water`, 1000 ml ingredient, repaired ingredient
  groundings, source annotations, and JCM reference.
- The direct JCM J191 and TOGO M184 projections describe the same PBY/JCM 191
  recipe but currently emit as separate generated records.

## Recommended Edits

- Regenerate merged YAML from the repaired `data/normalized_yaml/bacterial/pby_medium.yaml`
  input so JCM water, references, source notes, and exact complex-ingredient
  terms reach the generated record.
- Reconcile the direct JCM J191 and TOGO M184 PBY records so the two repaired
  maintained inputs merge or are explicitly deduplicated as one JCM 191 recipe.

## Follow-up Checks

- Rerun the open schema, strict, reference, and term validators on the
  regenerated PBY record.
- Compare the regenerated record against JCM 191 and MediaDive J191 and confirm
  it contains all four JCM rows, including 1 L distilled water.
- Re-run an ignored-inclusive exact search for `mediadive.medium:J191`,
  `TOGO:M184`, `GRMD=191`, and `PBY MEDIUM` to confirm the direct JCM and TOGO
  projections no longer produce stale split generated records.

## Additional Notes

None found.
