# YAML Record Review: mdy_v

- Repository: CultureMech
- Record: data/merge_yaml/merged/mdy_v.yaml
- Started UTC: 2026-09-24T01:03:04Z
- Finished UTC: 2026-09-24T01:04:02Z
- Verdict: needs curation

## Target

Reviewed generated merged record `data/merge_yaml/merged/mdy_v.yaml` for CCAP/MediaDive medium C52, `MDY-V`. Exact `find` over `data/normalized_yaml` with ignored files included found two same-named owners: `data/normalized_yaml/bacterial/mdy_v.yaml` and `data/normalized_yaml/algae/mdy_v.yaml`.

The generated record uses only the bacterial owner, which came from `mediadive.medium:C52`. Exact source-ID searches for `mediadive.medium:C52` over the generated record, both same-named owners, and the normalized indexes included ignored files and found the MediaDive identifier only on the generated and bacterial records plus bacterial/recipe index entries. The algae owner cites the same CCAP PDF URL and has later 2026-08 and 2026-09 CCAP repair events but is not part of this generated merge.

## Validation

- Open LinkML validation against `src/culturemech/schema/culturemech.yaml` as `MediaRecipe`: passed with `No issues found`.
- Strict validation with `scripts/validate_strict.py`: passed with 0 error rows; the strict TSV had a header only.
- LinkML reference validation: passed; 0 reference checks were emitted for this file.
- LinkML term validation with labels and `conf/oak_config.yaml`: passed.
- Embedded `curation_history`: Not checked. The repository `just validate-history` target validates standalone `history/` files rather than embedded generated-record history blocks.

## Identity and Grounding

`media_term.term.id` is `mediadive.medium:C52`, matching MediaDive C52 and the CCAP PDF at `MR_MDY_V.pdf`. The source defines MDY-V as a CCAP Culture Collection of Algae and Protozoa medium at pH 6.8, with 15 g/L agar added only for the agar formulation.

The generated `category: bacterial` is not aligned with CCAP's algae/protozoa provenance and conflicts with the repaired `data/normalized_yaml/algae/mdy_v.yaml` owner.

## Evidence

The target stores stock-solution recipes as if they were final 1 L concentrations:

- Main salts `MgSO4 x 7 H2O`, `KCl`, `NH4Cl`, `NaNO3`, `Sodium beta-glycerophosphate`, `H3BO3`, and `CaCl2` are each 1 ml additions from separate stock solutions into 1 L final medium. The target records them as large g/L final rows instead.
- `Trace element solution` is added at 6 ml/L. The target records its `Na2-EDTA`, `FeCl3 x 6 H2O`, `MnCl2 x 4 H2O`, `ZnCl2 x 6 H2O`, `CoCl2 x 6 H2O`, and `Na2MoO4 x 2 H2O` stock values without the required 0.006 factor.
- `Vitamin B1 solution` is 0.12 g thiamine hydrochloride in 100 ml distilled water, then 1 ml is added to 1 L final medium. The target stores 1.2 g/L thiamine hydrochloride, the stock strength rather than the final 0.0012 g/L.
- `Vitamin B12 solution` is a two-step dilution: 0.1 g cyanocobalamin in 100 ml, take 1 ml of that into 99 ml water, then add 1 ml of the diluted working solution to 1 L final medium. The target stores 1 g/L cyanocobalamin, which is 100000x the final 0.00001 g/L concentration.

The CCAP PDF specifies `ZnCl2` in the trace solution, while MediaDive and the generated record use `ZnCl2 x 6 H2O`. The generated row is also ungrounded.

## Completeness

The generated record has 17 ingredient rows because it collapses the main formula plus three nested stock solutions into one flat list. All non-water MediaDive rows are represented, and the main pH and preparation text are present, but the flattening loses the stock addition factors that are essential for quantitative correctness.

The repaired algae owner correctly keeps the six trace-element entries as source stock rows with notes that they are stock composition, not final-medium concentrations. The generated record ignores that later repair and still treats CCAP C52 as a bacterial medium.

## Findings

1. `needs curation` - Seven main-solution stock additions are recorded as final g/L rows instead of 1 ml/L contributions.
2. `needs curation` - The six trace-element stock rows are recorded 166.7x too high because the 6 ml/L trace-solution addition was not applied.
3. `needs curation` - Vitamin B1 and B12 are recorded at stock strength. B1 should be scaled by 0.001; B12 should be scaled through the 1:100 intermediate dilution and then by 0.001 into final medium.
4. `needs curation` - Two CCAP `mdy_v` owners exist, but the generated record only merges the stale bacterial/MediaDive owner and omits the later repaired algae/CCAP owner for the same PDF recipe.
5. `minor` - The generated trace row says `ZnCl2 x 6 H2O`, while the primary CCAP PDF says `ZnCl2`; the row also lacks CHEBI grounding.

## Recommended Edits

Deduplicate the bacterial and algae CCAP MDY-V records around a single repaired owner in the algae/CCAP category. Preserve agar as a conditional 15 g/L addition for the solid formulation, and either retain stock-solution boundaries or compute final per-liter values using the CCAP source volumes: 1 ml/L for each main stock, 6 ml/L for trace elements, 1 ml/L for B1, and the two-step dilution for B12.

Re-check the exact zinc chloride hydrate state against the CCAP PDF before grounding the row.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validators on the regenerated merged record.
- Confirm the generated set contains one CCAP MDY-V record, not one bacterial record and one repaired algae record.
- Confirm main salts, trace elements, B1, and B12 are scaled by their stock addition volumes.
- Re-check the exact CCAP source identifiers and PDF URL in normalized indexes with ignored files included.

## Additional Notes

The original CCAP PDF, the MediaDive C52 REST endpoint, and the rendered MediaDive C52 page were inspected directly. MediaDive names the main solution `Mail sol. C52`; this appears to be a typo for `Main sol. C52` and was not treated as a CultureMech defect.
