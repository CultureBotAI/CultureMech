# YAML Record Review: JCM MEDIUM No. 116

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_116.yaml
- Started UTC: 2026-09-23T16:28:34Z
- Finished UTC: 2026-09-23T16:29:09Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_116.yaml`
- Stable ID: `CultureMech:002342`
- Name: `jcm_medium_no_116`
- Original name: `JCM MEDIUM No. 116`
- Category: `bacterial`
- Media term: `mediadive.medium:J116`
- Source: MediaDive import of JCM Medium 116, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=116`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_116.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_116`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_116.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_116.yaml --out /private/tmp/jcm_medium_no_116.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_116.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_116.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The record is valid YAML and conforms to the represented schema slots. The validators do not compare the ingredient list against the JCM table.

## Identity and Grounding

The generated record identifies MediaDive `J116` for JCM Medium 116 and links to JCM `GRMD=116`. MediaDive, the live JCM page, and TogoMedium M108 all resolve to the same potato/glucose/yeast-extract broth adjusted to pH 5.4-5.6.

The `LIQUID`, `COMPLEX`, and `UNDEFINED` classifications are consistent with the source recipe. Potato is intentionally ungrounded because the source row is a food material with a preparation condition, not a single purified chemical.

## Evidence

Inspected evidence:

- MediaDive REST `medium/J116` reports `JCM MEDIUM No. 116`, source `JCM`, min/max pH 5.5, source link `GRMD=116`, and one 1000 ml solution with 200 g potato, 10 g glucose, 3 g yeast extract, and 1000 ml distilled water.
- The same MediaDive response preserves the JCM preparation comments: boil potatoes for 20 min and adjust pH to 5.4-5.6.
- The live JCM `GRMD=116` page lists 200 g `Potato, peeled and cut`, 10 g glucose, 3 g yeast extract, 1 L distilled water, the same two preparation lines, and the default JCM instruction to autoclave media at 121 C for 15 min unless otherwise stated.
- TogoMedium M108 points to `JCM_M116` and `GRMD=116` and carries the same four ingredient rows and two preparation comments.

Supported:

- The MediaDive ID, original name, JCM link, bacterial category, liquid state, and complex/undefined composition all match the inspected sources.
- Potato at 200 g/L, glucose at 10 g/L, and yeast extract at 3 g/L are supported by both JCM and MediaDive.
- `ph_value: 5.5` is consistent with MediaDive's pH field and the represented step still preserves the JCM range, 5.4-5.6.
- The two represented preparation steps match JCM and MediaDive.

Unsupported or incomplete:

- The source formula includes 1 L or 1000 ml distilled water; the generated record has no water row.
- The source's default autoclaving instruction is not represented in `preparation_steps`.

## Completeness

Consequential gaps:

- Missing basal distilled water makes the represented formula less complete than both the direct MediaDive source and the live JCM page.
- The record has no sterilization step even though the JCM page states the default 121 C, 15 min autoclave condition.

Empty slots that are acceptable:

- No target-organism, growth-metric, or strain-specific claim is present.
- No stock solution is missing; JCM 116 is a single main solution.

Bounded negative search:

- The first search for `GRMD=116` was overbroad and matched JCM 1160-1169. I did not use that result to establish absence.
- A corrected gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=116` with a following non-digit or end-of-line boundary, exact `JCM_M116` with a following non-digit or end-of-line boundary, and exact `jcm_medium_no_116` or `togo_medium_m108` IDs found only the direct MediaDive/JCM record and the TOGO M108 wrapper. No other normalized or merged copy of JCM 116 was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated direct MediaDive/JCM record omits the recipe solvent. | JCM lists 1 L distilled water and MediaDive order 4 lists 1000 ml `Distilled water`; `data/merge_yaml/merged/jcm_medium_no_116.yaml` has only potato, glucose, and yeast extract. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_116.yaml`, followed by merge regeneration. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/togo_medium_m108.yaml` and `data/merge_yaml/merged/togo_medium_m108.yaml` pointing to `JCM_M116` and `GRMD=116`. The TOGO wrapper also misrepresents `1 L` as `1 G_PER_L`, so the two source imports no longer have equivalent ingredient lists. | Both normalized owners, plus `scripts/merge_recipes.py` after source repair. |
| Minor | The source's default autoclave condition is missing. | The live JCM page states its default 121 C, 15 min sterilization instruction before the Medium 116 table, and no contrary Medium 116-specific instruction overrides it. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_116.yaml`. |

## Recommended Edits

1. Preserve MediaDive order 4 by adding distilled water, 1000 ml, to `data/normalized_yaml/bacterial/jcm_medium_no_116.yaml` or by repairing the importer that dropped it.
2. Repair `data/normalized_yaml/bacterial/togo_medium_m108.yaml` so the JCM 1 L distilled-water row is not encoded as `1 G_PER_L`.
3. Add JCM's default autoclave condition as a preparation step or preparation note, scoped as the page default.
4. Regenerate `data/merge_yaml/merged/` and confirm that the direct MediaDive/JCM and TOGO M108 imports either collapse to one canonical generated recipe or remain intentionally separate with explicit provenance.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Compare the regenerated `jcm_medium_no_116` ingredient list with the live JCM page and MediaDive `medium/J116` response; it should include exactly potato, glucose, yeast extract, and distilled water.
- Search with `rg --no-ignore --hidden 'GRMD=116([^0-9]|$)|JCM_M116([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` after the TOGO repair to check for same-source duplicates with ignored files included.

## Additional Notes

- The TOGO M108 wrapper is closer to JCM's row count because it contains water, but its water unit bug means it should not be used as a merge survivor without curation.
- No pH defect was found. The scalar `5.5` agrees with MediaDive's pH field, and the generated preparation step preserves the JCM range.
