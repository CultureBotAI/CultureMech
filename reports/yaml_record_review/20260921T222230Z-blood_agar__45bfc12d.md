# YAML Record Review: blood_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/blood_agar__45bfc12d.yaml
- Started UTC: 2026-09-21T22:22:30Z
- Finished UTC: 2026-09-21T22:23:21Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/blood_agar__45bfc12d.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:003056` |
| Name | `blood_agar` |
| Original name | `BLOOD AGAR` |
| Source identity | `mediadive.medium:J70`, JCM Medium 70 |
| Category | `bacterial` |
| Physical state | `LIQUID` |
| Merge fingerprint | `45bfc12d73e50979e2254eda907d95b0a172e8a525bd53b82f5c77c343519e92` |
| Merged from | `blood_agar` |
| Maintained owner | `data/normalized_yaml/bacterial/blood_agar.yaml` |
| Generated status | Derived merge product; future fixes belong in `data/normalized_yaml/bacterial/blood_agar.yaml` or merge regeneration, not in this generated YAML. |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/blood_agar__45bfc12d.yaml` | Passed with exit 0 and no diagnostics. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/blood_agar__45bfc12d.yaml --out /private/tmp/blood_agar__45bfc12d.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/blood_agar__45bfc12d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with exit 0 and no output. The generated record has no structured `references` to check. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/blood_agar__45bfc12d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after a non-fatal `eutils` `pkg_resources` warning. |
| Embedded curation history | Not checked: no focused embedded `MediaRecipe.curation_history` validator is documented for one generated merge record; `just validate-history` targets standalone files under `history/`. |

`just validate-schema`, `just validate-strict`, and `just validate-terms` were not used directly because the project `uv` environment currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation starts. The no-project Python 3.11 commands above are the same focused checks run without installing the project dependency set.

## Identity and Grounding

- `CultureMech:003056`, `original_name: BLOOD AGAR`, and `media_term.term.id: mediadive.medium:J70` agree with the direct JCM Medium 70 source identity.
- The live JCM Medium 70 page identifies record 70 as `BLOOD AGAR` and says to prepare Blood agar base (BD-Difco), sterilize, cool to about 45 C, aseptically add 5% final sterile defibrinated rabbit blood, mix, and quickly dispense into sterile test tubes.
- `physical_state: LIQUID` conflicts with Blood Agar and with the same source's plate/tube agar handling; this should be `SOLID_AGAR`.
- The direct JCM owner over-specifies the source recipe. JCM does not list the internal composition of Blood agar base (BD-Difco), but the YAML expands the commercial base into five BD BBL 211037-derived ingredient rows.
- The active TOGO M62 duplicate at `data/normalized_yaml/bacterial/TOGO_M62_Blood_Agar.yaml` has already been linked to this JCM owner as a `SOURCE_DUPLICATE`; the generated merge predates that September 6 link and has no `variant_children`.

## Evidence

| Claim | Review |
|---|---|
| JCM source identity | Supported by the live JCM Medium 70 page. |
| Blood agar base amount | Not source-supported as modeled. JCM says to prepare Blood agar base according to the manufacturer's directions; it does not say the record contains 2 g/L heart muscle infusion solids, 13 g/L pancreatic digest of casein, 5 g/L yeast extract, 5 g/L sodium chloride, and 15 g/L agar. |
| Rabbit blood amount and timing | Supported: JCM states a 5% final sterile defibrinated rabbit blood addition after sterilization and cooling to about 45 C. |
| Physical state | Incorrect: JCM describes Blood Agar and sterile test-tube dispensing, not a liquid medium. |
| TOGO duplicate link | Present only in the maintained owner. `data/normalized_yaml/bacterial/blood_agar.yaml` now lists TOGO M62 as a `SOURCE_DUPLICATE`, but the generated file was last merged before that repair. |

## Completeness

- The generated record has no structured `references`, so neither the JCM page nor the BD-Difco product source, if retained, can be dereferenced.
- The live JCM source does not state pH, a target organism, incubation temperature, or storage conditions. Empty optional pH, target-organism, growth-metric, and storage fields are not defects.
- The one-step `preparation_steps` row contains all source operations but would be easier to validate if split like the repaired TOGO M62 duplicate: prepare base, sterilize, cool, add 5% rabbit blood, then dispense.
- Exact ignored-file-inclusive searches for `CultureMech:003056`, the JCM `GRMD=70` URL, and `Defibrinated rabbit blood` over `data/normalized_yaml`, `data/merge_yaml/merged`, the registry/catalog TSVs, `scripts`, `tests`, `reports/yaml_record_review`, and `history` found the direct JCM owner, this generated merge, the active linked TOGO M62 owner, and related blood-agar records with different source accessions.
- No existing `*-blood_agar__45bfc12d.md` report was found under ignored `reports/yaml_record_review/` before this report was written.

## Findings

| Severity | Finding | Evidence | Maintained owner for a fix |
|---|---|---|---|
| Major | The commercial base expansion is not justified by the cited JCM source. | JCM Medium 70 names Blood agar base (BD-Difco) as a commercial base prepared according to directions; it does not list the five BD BBL 211037 ingredient rows currently in the YAML. | Either collapse the base back to an opaque Blood agar base component, as in `data/normalized_yaml/bacterial/TOGO_M62_Blood_Agar.yaml`, or add a structured BD source that supports the product expansion in `data/normalized_yaml/bacterial/blood_agar.yaml`. |
| Major | The physical state is wrong. | JCM identifies this as Blood Agar and instructs the reader to dispense the cooled, blood-supplemented agar into sterile test tubes; `physical_state` is `LIQUID`. | Change `data/normalized_yaml/bacterial/blood_agar.yaml` to `physical_state: SOLID_AGAR`. |
| Major | The generated merge is stale and missing the TOGO M62 source-duplicate link. | The normalized owner has a September 6 `variant_children` entry for `CultureMech:010030`; the generated merge has no variant child because it was last emitted on August 6. | Regenerate `data/merge_yaml/merged/` after fixing the normalized owner. |
| Major | Structured references are absent. | The generated and maintained JCM owner cite JCM only in `notes`; the BD product expansion names BD BBL 211037 only in per-ingredient `source` strings. | Add structured references for JCM Medium 70 and for the BD formulation source if the expansion is retained. |

## Recommended Edits

1. Decide whether `data/normalized_yaml/bacterial/blood_agar.yaml` should preserve the BD-Difco base as an opaque commercial component or cite a verified BD BBL 211037 formulation; then adjust `ingredients` and `references` accordingly.
2. Set `physical_state: SOLID_AGAR` in the maintained JCM owner.
3. Split the preparation text into discrete prepare, sterilize, cool, rabbit-blood addition, and dispense steps, matching the live JCM sequence.
4. Regenerate `data/merge_yaml/merged/` so `blood_agar__45bfc12d.yaml` picks up the active TOGO M62 `SOURCE_DUPLICATE` link.

## Follow-up Checks

- Rerun focused schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/blood_agar.yaml`, the regenerated direct JCM merge, and `data/merge_yaml/merged/blood_agar__914b0515.yaml`.
- Rerun `just validate-media-variant-links` to verify the JCM/TOGO M62 source-duplicate relationship.
- Rerun `just verify-merges` to prove `blood_agar__45bfc12d.yaml` is fresh.
- Re-open the regenerated direct JCM Blood Agar page and confirm it renders as a solid agar medium and links the TOGO M62 duplicate.

## Additional Notes

- This was a read-only review. I did not edit normalized YAML, generated merge YAML, generated pages, GitHub issues, or PR state.
- The TOGO M62 duplicate is reviewed separately as `data/merge_yaml/merged/blood_agar__914b0515.yaml` in the next sorted record.
