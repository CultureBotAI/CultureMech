# YAML Record Review: R-CW AGAR

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_cw_agar__392caa15.yaml
- Started UTC: 2026-09-25T01:01:15Z
- Finished UTC: 2026-09-25T01:02:11Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/r_cw_agar__392caa15.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002521` |
| Label | `R-CW AGAR` |
| Category | `bacterial` |
| Source identity | `mediadive.medium:J162` |
| Generated status | Generated singleton with `merge_fingerprint: 392caa15da165ed0ae0c874f542a3b8ffdee659ec08180846a22baf86e792ccf`; owned upstream by `data/normalized_yaml/bacterial/r_cw_agar.yaml`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_cw_agar__392caa15.yaml` reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/r_cw_agar__392caa15.yaml --out /private/tmp/r_cw_agar_392caa15.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed trivially; `linkml-reference-validator validate data data/merge_yaml/merged/r_cw_agar__392caa15.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 checks, and reported all validations passed. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/r_cw_agar__392caa15.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- MediaDive J162 identifies the target as a JCM recipe named `R-CW AGAR` and links to JCM GRMD 162.
- Live JCM GRMD 162 resolves to `R-CW AGAR` and lists the same 10 components as the direct MediaDive mirror.
- TOGO M153 records `original_media_id: JCM_M162` and the same JCM GRMD 162 URL, making the TOGO M153 record source-equivalent with this direct JCM import.
- The gram-valued solutes in this direct MediaDive record use MediaDive's 1001 ml final-volume normalization, which is consistent with the live MediaDive J162 REST payload.
- The two liquid components, Tween 80 and cheese whey, are still encoded with mass-concentration units.

## Evidence

- Supported:
  - Live MediaDive J162 supports the direct `mediadive.medium:J162` identity, `R-CW AGAR` label, JCM source link, complex medium type, and the 1001 ml total-volume basis for the normalized gram-per-liter values.
  - Live JCM GRMD 162 and TOGO M153 support the same original JCM identity and raw 5 g, 2 g, 0.5 g, 1 ml, and 1 L component amounts.
  - The CHEBI groundings for KH2PO4, sodium acetate, diammonium hydrogen citrate, MgSO4 x 7 H2O, MnSO4 x H2O, and Tween 80 are appropriate for the named components.
- Unsupported or over-scoped:
  - Tween 80 is encoded as `1 G_PER_L`, but MediaDive J162, TOGO M153, and JCM GRMD 162 all list it with milliliter units.
  - Cheese whey is encoded as `1000 G_PER_L`, but MediaDive J162 lists 1000 ml and TOGO M153 plus JCM GRMD 162 list 1 L.
  - The JCM default autoclaving instruction is absent from the generated and maintained direct-JCM records.

## Completeness

- The current maintained direct owner has the same Tween 80 and cheese whey unit errors as the generated file.
- The generated and maintained direct-JCM records both keep the JCM source URL in `notes` but do not expose it as a structured `references` entry.
- An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `mediadive.medium:J162`, `TOGO:M153`, `GRMD=162`, `CultureMech:002521`, `CultureMech:008087`, `r_cw_agar__392caa15`, and `r_cw_agar.yaml` found the direct MediaDive/JCM J162 owner, this generated artifact, the source-equivalent TOGO M153 owner and generated artifact, indexes, the immediately preceding TOGO M153 review, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Major | Tween 80 has the wrong unit. | MediaDive J162 lists 1 ml Tween 80, but `data/merge_yaml/merged/r_cw_agar__392caa15.yaml` and `data/normalized_yaml/bacterial/r_cw_agar.yaml` encode it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/r_cw_agar.yaml` |
| Major | Cheese whey has the wrong unit. | MediaDive J162 lists 1000 ml cheese whey, but the generated and normalized direct-JCM records encode it as `1000 G_PER_L`. | `data/normalized_yaml/bacterial/r_cw_agar.yaml` |
| Major | The source-equivalent MediaDive J162 and TOGO M153 records are split in generated output. | MediaDive J162 and TOGO M153 both point to JCM GRMD 162; the exact search found separate generated files at `r_cw_agar__392caa15.yaml` and `r_cw_agar.yaml`. | Add a source-equivalence or merge-key repair between `data/normalized_yaml/bacterial/r_cw_agar.yaml` and `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml`, then regenerate. |
| Minor | The JCM default sterilization instruction is missing. | Live JCM GRMD 162 states the default autoclave condition, and this record has no `preparation_steps`. | `data/normalized_yaml/bacterial/r_cw_agar.yaml` |

## Recommended Edits

1. Change Tween 80 in `data/normalized_yaml/bacterial/r_cw_agar.yaml` from `1 G_PER_L` to a milliliter-volume representation backed by MediaDive J162 and JCM GRMD 162.
2. Change Cheese whey from `1000 G_PER_L` to a liquid-volume representation backed by MediaDive J162 and JCM GRMD 162.
3. Add a structured GRMD 162 reference and the default JCM autoclaving step.
4. Reconcile the direct MediaDive/JCM J162 owner with `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml`, then regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/r_cw_agar__392caa15.yaml`.
- Re-run a gitignore-independent exact search for `mediadive.medium:J162`, `TOGO:M153`, and `GRMD=162` to confirm the direct MediaDive/JCM and TOGO imports are no longer emitted as unrelated generated records.
- Re-fetch MediaDive J162, TOGO M153, and JCM GRMD 162 to confirm the raw milliliter and liter source units were preserved.

## Additional Notes

- `physical_state: LIQUID` is surprising for a medium named `R-CW AGAR`, but the inspected MediaDive, TOGO, and JCM ingredient tables do not list agar. The physical-state decision should be revisited with source context instead of changing it from the label alone.
- The small differences between this direct-JCM record's 4.99501 g/L, 1.998 g/L, and 0.499501 g/L concentrations and the TOGO import's 5 g/L, 2 g/L, and 0.5 g/L values come from MediaDive normalizing gram solutes over 1001 ml rather than 1000 ml.
