# YAML Record Review: GV-medium

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gv_medium.yaml`
- Started UTC: 2026-09-23T07:59:51Z
- Finished UTC: 2026-09-23T08:01:27Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:008620`, `gv_medium`, imported from Togo Medium `M2031` / `NBRC_M1323`.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gv_medium.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The direct identity is coherent: both the Togo normalized parent `data/normalized_yaml/bacterial/TOGO_M2031_GV-medium.yaml` and generated record carry `TOGO:M2031`, `GV-medium`, and the NBRC `NBRC_M1323` provenance. Exact ignored-file-inclusive search in `data/normalized_yaml/bacterial` and `data/merge_yaml/merged` found the expected Togo parent and generated pair, plus separate GV-related KOMODO records and records that reuse `gv_medium` as a named stock solution.

Several hydrate labels are still grounded too generically: `CoCl2 x 6 H2O` is grounded to anhydrous cobalt dichloride, and `NiCl2 x 6 H2O` is grounded to anhydrous nickel dichloride. `Yeast extract`, `HCl (25% or 7.7 M)`, and the stock-solution rows are ungrounded.

## Evidence

The Togo `M2031` payload describes a 1 L main medium with gram-scale salts, 0.5 mg Resazurin, 2 mg `FeSO4 x 7 H2O`, three stock additions, and 80% N2 / 20% CO2 handling. Its nested stocks are `Trace element solution*` at 1 ml, `Selenite/tungstate solution**` at 1 ml, and `Vitamin solution***` at 10 ml. Togo also records pH `7.0 - 7.2` and preparation comments for boiling and cooling under the gas mixture, adding bicarbonate before autoclaving, adding sterile anoxic stocks after sterilization, preparing vitamins under 100% N2, filter-sterilizing vitamins, and adjusting the completed medium to pH 7.0-7.2 if needed.

The generated record matches the source provider and carries the 17 main-medium rows, but it collapses every stock component into the top-level ingredient list and treats source milligram values as if they were grams per liter.

## Completeness

The record is structurally valid but not recipe-complete. It loses the stock hierarchy, the stock addition volumes, the pH range, and the anaerobic/post-sterilization instructions that define how the medium is prepared.

## Findings

- Milligram rows were imported as grams per liter. The source 0.5 mg Resazurin and 2 mg `FeSO4 x 7 H2O` main-medium rows are stored as 0.5 and 2 `G_PER_L`, and milligram-scale stock ingredients such as 36 mg `Na2MoO4 x 2 H2O`, 6 mg `H3BO3`, and 2 mg Biotin are likewise inflated into whole `G_PER_L` quantities.
- Nested stocks were flattened at stock strength. Togo adds 1 ml trace-element stock, 1 ml selenite/tungstate stock, and 10 ml vitamin stock to the main medium, but the generated record emits their internal solutes as top-level final-medium ingredient rows and leaves only placeholder `solutions` entries with `G_PER_L` units.
- Water rows from the main medium and three stock solutions were merged into one final ingredient. The generated `Distilled water` row sums `1.0`, `990.0`, `1.0`, and `1.0` into 993.0 `G_PER_L`, which conflates one final-medium liter with three stock-solution solvent volumes.
- Togo pH `7.0 - 7.2` is missing from the generated record.
- Preparation instructions are missing, including the 80% N2 / 20% CO2 gas mixture, the staged bicarbonate/magnesium/calcium/glucose/cysteine/sulfide additions, post-sterilization addition of sterile anoxic stocks, and filter sterilization of vitamins.
- `CoCl2 x 6 H2O` and `NiCl2 x 6 H2O` are grounded to anhydrous ChEBI salts despite hydrate-specific source labels.

## Recommended Edits

- Reimport Togo `M2031` with nested stock solutions preserved as recipes, and link the 1 ml, 1 ml, and 10 ml additions from the main medium to those stock recipes.
- Convert milligram source quantities to `G_PER_L` before emitting generated concentration values.
- Preserve `pH 7.0 - 7.2` and the anaerobic/post-sterilization preparation text.
- Keep water additions local to their main or stock solution instead of merging all solvent rows.
- Reground cobalt chloride hexahydrate and nickel chloride hexahydrate to hydrate-specific ChEBI terms, and attempt grounding for yeast extract, HCl, and stock-solution rows.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Recompare the regenerated ingredient and solution rows against Togo `M2031`, with explicit checks for milligram conversion, stock-addition volumes, pH, gas composition, and hydrate-specific ChEBI identifiers.

## Additional Notes

The KOMODO `gv_medium.yaml` parent refers to DSMZ/MediaDive medium 500 and is a separate source record despite sharing the normalized `gv_medium` name.
