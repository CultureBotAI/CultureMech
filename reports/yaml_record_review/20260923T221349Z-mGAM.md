# YAML Record Review: mGAM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mGAM.yaml
- Started UTC: 2026-09-23T22:11:45Z
- Finished UTC: 2026-09-23T22:13:49Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mGAM.yaml`.

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:015590 |
| Name | mGAM |
| Description | Gifu Anaerobic Broth |
| Source | CultureBotHT `mGAM` |
| Category | specialized |
| Generated status | Generated canonical merge under `data/merge_yaml/merged/`; owned by `data/normalized_yaml/specialized/mgam.yaml` and regenerated by `src/culturemech/merge/merge_recipes.py` |

The exact uppercase filename search for `mGAM.yaml` included ignored and
hidden files under `data/normalized_yaml` and found no maintained owner. The
merge history and an exact lowercase filename search resolve the maintained
owner to `data/normalized_yaml/specialized/mgam.yaml`.

## Validation

| Check | Command | Result |
|---|---|---|
| LinkML open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mGAM.yaml` | Passed: `No issues found`. |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mGAM.yaml --out /private/tmp/mGAM.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mGAM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 reference checks, all validations passed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mGAM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils`/`pkg_resources` warning. |
| Embedded history | Not run | Not checked: the repository `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays. |

## Identity and Grounding

The merged record is a single-source generated copy of
`data/normalized_yaml/specialized/mgam.yaml`. It consistently identifies the
CultureMech record as `CultureMech:015590`, `mGAM`, `Gifu Anaerobic Broth`,
specialized, complex, undefined, and liquid.

The upstream source identity is too weak to audit fully:

- `sources[0]` names `CultureBotHT`, `database_id: mGAM`, and only the
  repository root `https://github.com/CultureBotAI/CultureBotHT`.
- `curation_history` says the imported file was `mGAM.yaml`.
- `scripts/import_from_culturebotht.py` reads from a `media_yaml` directory in
  a CultureBotHT checkout, but the importer records neither that path nor a
  commit-pinned raw URL.
- The GitHub repository API returned 404, and direct raw guesses for
  `main/mGAM.yaml`, `master/mGAM.yaml`, and `main/mgam.yaml` returned 404.
- A clean ignored-file-inclusive `data/` filename scan for `mGAM` and `mgam`
  found only the maintained/generated `mgam` and `mgam_plus` YAML records, not
  a raw CultureBotHT capture.

Six complex ingredients are grounded with MICRO, FOODON, or UBERON mixture
terms rather than ChEBI. That is appropriate in principle for undefined
ingredients such as peptones and extracts, but the exact CultureBotHT source
file is unavailable for this review, so the nearest inspected support is the
existing normalized record rather than the upstream FEBA definition.

## Evidence

Supported by inspected local records:

| Claim | Assessment |
|---|---|
| The generated record is single-source | Supported by `merged_from: [mgam]` and the `MERGED_RECIPES` event naming only `mgam.yaml`. |
| The maintained owner carries 13 ingredients at g/L strengths | Supported by `data/normalized_yaml/specialized/mgam.yaml`; not independently verified against CultureBotHT. |
| `mGAM plus` is an adjacent CultureBotHT record with the same first 13 ingredients plus Hemin and Menadione | Supported by `data/normalized_yaml/specialized/mgam_plus.yaml`; not evidence for the exact source of `mGAM`. |

Unsupported or unresolved against primary source:

- No exact CultureBotHT `mGAM.yaml` blob was recoverable from the recorded
  provenance, so every ingredient amount still needs comparison with the
  original FEBA/CultureBotHT source.
- The importer collapses provenance to a repository root; a reader cannot tell
  which commit produced this formula.
- `Liver extract` was deliberately de-grounded by the June 14, 2026
  `idnf-grounding-fix-v1.0` event, but `data_quality_flags` remains empty.

## Completeness

Consequential gaps:

- File-level, commit-pinned CultureBotHT provenance is missing.
- The exact upstream file was not vendored under `data/`, so the formula is not
  reproducible without a separate CultureBotHT checkout.
- `Liver extract` has no modeled `term`, no `mediaingredientmech_chebi_term`,
  and no quality flag explaining that its previous identifier was absent or
  obsolete.

Empty optional slots for pH, preparation steps, storage, atmosphere, and
target organisms are not record defects on the evidence currently available:
the inspected CultureMech record and importer metadata do not expose those
fields from CultureBotHT.

Bounded local searches included ignored and hidden files:

- `CultureMech:015590` and the generated merge fingerprint were searched under
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`.
- File paths matching exact `mGAM`/`mgam` prefixes were enumerated under
  `data/`; only the normalized and generated `mGAM`/`mGAM plus` records were
  found.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | CultureBotHT provenance is not recoverable at file level. | The record stores only the repository root and `mGAM.yaml`; GitHub raw/API checks returned 404; ignored-file-inclusive local `data/` scans found no raw source capture. | `data/normalized_yaml/specialized/mgam.yaml` and `scripts/import_from_culturebotht.py`. |
| major | `Liver extract` is ungrounded but no quality flag records the remaining unresolved ingredient. | The June 14, 2026 event says one absent or obsolete CHEBI term was de-grounded as a mixture or unverifiable; the only current ungrounded ingredient is `Liver extract`; `data_quality_flags` is `[]`. | `data/normalized_yaml/specialized/mgam.yaml`. |

## Recommended Edits

1. Recover the exact CultureBotHT `media_yaml/mGAM.yaml` source at a stable
   commit and add file-level provenance, either as a commit-pinned raw URL or
   an archived source under `data/raw`.
2. Update `scripts/import_from_culturebotht.py` so future CultureBotHT imports
   preserve the imported relative path and commit SHA, not only the repository
   root.
3. Re-compare all 13 ingredient names, amounts, units, and source order
   against the recovered `mGAM.yaml`.
4. Add an explicit quality flag or discussion for `Liver extract`, or map it to
   an appropriate MICRO/FOODON mixture term if an exact curated term is
   available.

## Follow-up Checks

- Re-run focused open-schema, strict, reference, and term validators on
  `data/normalized_yaml/specialized/mgam.yaml`.
- Re-run the same validators on regenerated `data/merge_yaml/merged/mGAM.yaml`.
- Run `just verify-merges` and `just audit-merge-freshness` after any
  regenerated merge output.
- Manually diff the recovered CultureBotHT `mGAM.yaml` against the maintained
  record and record any unsupported mapping or amount differences.

## Additional Notes

- The generated merge is not stale relative to the current normalized file in
  any field inspected for this report.
- `data/normalized_yaml/specialized/mgam_plus.yaml` should be reviewed as a
  separate adjacent CultureBotHT record; it is not evidence that `mGAM` should
  contain Hemin or Menadione.
