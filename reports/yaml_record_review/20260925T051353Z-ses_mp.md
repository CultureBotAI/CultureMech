# YAML Record Review: ses_mp

- Repository: CultureMech
- Record: data/merge_yaml/merged/ses_mp.yaml
- Started UTC: 2026-09-25T05:13:53Z
- Finished UTC: 2026-09-25T05:13:53Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:000132`, `ses_mp`, from `data/merge_yaml/merged/ses_mp.yaml`.

The target record is the collection-level CCAP `SES:MP` import after an August merge with `PER`.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is correctly grounded to CCAP `SES_MP`.

`SES:MP` and `PER` are different CCAP recipes and should not be merged as duplicates.

The older direct CCAP `ses_mp__bfd7b41b` generated record has the same `MR_SES_MP.pdf` source but is a separate stale import branch.

## Evidence

CCAP `MR_SES_MP.pdf` defines `SES:MP` as a 3:1 mixture of separately autoclaved SES and Chapman-Andresen's Modified Pringsheim's Solution.

CCAP `MR_PER.pdf` defines `PER` as 1 g Complan added to 1 L SES, then autoclaved.

The repaired normalized `data/normalized_yaml/algae/ses_mp.yaml` source already models `SES:MP` as 750 ml/L SES plus 250 ml/L Chapman-Andresen's Modified Pringsheim's Solution and no direct ingredients.

## Completeness

The generated record carries CCAP `SES_MP` identity, references, and provenance.

The generated record is stale relative to the August and September CCAP repairs in the normalized algae source.

The only generated ingredient is a 105 g/L `Soil is prepared as above.` row from `MR_PER.pdf`, not `MR_SES_MP.pdf`.

The generated record lacks the 750 ml/L SES and 250 ml/L Modified Pringsheim's constituent-medium rows from the repaired normalized source.

The false merge with `PER` adds the `per` synonym and `merged_from` entry to a record that should only describe `SES:MP`.

## Findings

`SES:MP` was falsely merged with `PER`.

An instructional soil-extract sentence from `MR_PER.pdf` was promoted to the only ingredient in the `SES:MP` generated record.

The generated record does not reflect the repaired constituent-medium representation now present in the normalized source.

## Recommended Edits

Regenerate `data/merge_yaml/merged/ses_mp.yaml` from `data/normalized_yaml/algae/ses_mp.yaml`.

Remove the duplicate relationship between `SES:MP` and `PER`; `PER` should remain a separate CCAP medium that uses SES as its base.

Ensure the regenerated `SES:MP` record has no direct ingredient rows and instead carries 750 ml/L SES plus 250 ml/L Chapman-Andresen's Modified Pringsheim's Solution.

Keep `MR_PER.pdf` provenance out of the regenerated `SES:MP` record.

## Follow-up Checks

Confirm the regenerated `ses_mp` record has no `Soil is prepared as above.` ingredient.

Confirm the regenerated `ses_mp` record has no `per` synonym or `merged_from: per` entry.

Confirm both CCAP source references, `CCAP:SES_MP` and `MR_SES_MP.pdf`, remain on the regenerated record.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
