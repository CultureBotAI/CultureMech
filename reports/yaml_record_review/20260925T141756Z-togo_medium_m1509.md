# YAML Record Review: togo_medium_m1509

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1509.yaml
- Started UTC: 2026-09-25T14:13:00Z
- Finished UTC: 2026-09-25T14:17:56Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008053 |
| Label | togo_medium_m1509 |
| Generated record | data/merge_yaml/merged/togo_medium_m1509.yaml |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m1509.yaml |
| Source | TOGO Medium M1509, NBRC M289-2; merged with TOGO M688 |

The reviewed file is a generated merge of the TOGO M1509 semisolid NBRC 289
branch and TOGO M688, a JCM medium 669 semisolid Magnetospirillum Medium B
branch. Future fixes belong in the normalized owners or merge/TOGO import
logic, not in the generated merge.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/togo_medium_m1509.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; exited 0 with no diagnostics |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected `eutils/pkg_resources is
deprecated` warning before passing.

## Identity and Grounding

`CultureMech:008053`, `TOGO:M1509`, the `NBRC_M289-2` original source note,
and the NBRC medium 289 page all identify the semisolid form of NBRC medium
289: the M1508 basal recipe with 1 g/L agar and cross-references to the M1508
Vitamin mixture, Ferric quinate solution, and HMSS solution.

The generated record also merges TOGO M688 from JCM medium 669. M688 is the
semisolid Magnetospirillum Medium B branch, but its source uses 10 ml Trace
vitamins from M190, 2 ml Ferric quinate solution from M687, and 1 ml Mineral
salt solution from M299. That is related to, but not source-identical with,
the NBRC M1509 cross-references.

## Evidence

The M1509 source adds 5 ml Vitamin mixture, 2 ml Ferric quinate solution, and
1 ml HMSS solution from M1508. The generated YAML stores these three additions
as empty `Unknown solution` entries at 5, 2, and 1 g/L.

The source adjusts the medium to pH 6.75 with 1 N NaOH. The generated record
keeps NaOH as a variable final ingredient and has no `ph_value` or preparation
step for the adjustment.

The source calls this a semisolid 1 g/L agar branch. The generated record keeps
1 g/L agar but classifies the recipe as `SOLID_AGAR`.

## Completeness

The absent `target_organisms`, growth evidence, references, and local stock
definitions are not defects for this imported source record because M1509
points back to M1508 for stock definitions. The three source cross-references,
pH 6.75, and the semisolid state are not represented precisely.

Before this report was written, an exact `find` search under the ignored
`reports/yaml_record_review` directory found no pre-existing
`*-togo_medium_m1509.md` report.

## Findings

| Severity | Finding |
| --- | --- |
| major | M1509 stock cross-references are empty 5, 2, and 1 g/L solutions instead of 5, 2, and 1 ml/L additions from M1508. Owner: `data/normalized_yaml/bacterial/togo_medium_m1509.yaml` or the TOGO cross-reference importer. |
| major | The generated merge treats TOGO M1509 and TOGO M688 as exact duplicates even though their vitamin stock source and volume differ. Owner: the merge fingerprint logic and `data/normalized_yaml/bacterial/togo_medium_m1509.yaml`. |
| minor | pH 6.75 and the 1 N NaOH adjustment are absent. Owner: `data/normalized_yaml/bacterial/togo_medium_m1509.yaml` or the TOGO/NBRC importer. |
| minor | The 1 g/L agar source branch is semisolid, not `SOLID_AGAR`. Owner: `data/normalized_yaml/bacterial/togo_medium_m1509.yaml`. |

## Recommended Edits

1. Encode M1509 as the NBRC 289 semisolid branch with M1508 stock references
   at 5 ml/L, 2 ml/L, and 1 ml/L.
2. Split TOGO M688 from the M1509 exact-duplicate merge or preserve it as a
   related source variant with its 10 ml M190 trace-vitamin dose intact.
3. Add pH 6.75 and model 1 N NaOH as a pH-adjustment reagent, not a final
   variable ingredient.
4. Reclassify the 1 g/L agar branch as `SEMISOLID` if the schema state is
   intended for source formulations.
5. Regenerate `data/merge_yaml/merged/togo_medium_m1509.yaml`.

## Follow-up Checks

Run the focused schema, strict, reference, and term validators on the
regenerated record, then manually compare it with TOGO M1509, TOGO M1508,
NBRC 289, and TOGO M688 to confirm the generated merge no longer hides the
5 ml versus 10 ml vitamin-stock difference.

## Additional Notes

An exact gitignore-independent search covered `data/normalized_yaml/bacterial`,
`data/merge_yaml/merged`, and ignored `reports/yaml_record_review` for
`TOGO:M688`, `Magnetospirillum Medium B`, `magnetospirillum_medium_b`,
`NBRC_M289-2`, and `M1509`; it found the expected M1509 record, the M688/M687
Magnetospirillum sibling records, and the earlier Magnetospirillum generated
record reports.
