# YAML Record Review: 26

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/26.yaml`
- Started UTC: 2026-09-21T06:16:02Z
- Finished UTC: 2026-09-21T06:17:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007482` |
| Name | `26` |
| Original name | `26` |
| Category | `bacterial` |
| Source term | Missing from the generated target; `data/normalized_yaml/bacterial/NBRC_26.yaml` now carries `nbrc.medium:25`, `NBRC Medium 25` |
| Source URL | Missing from structured `references`; the maintained owner now cites `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=25` |
| Merge fingerprint | `f2135594a4bc47bc99d0878cd10d82b1908d96dda5b0be6a3e12df6992096b12` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_26.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 25 record imported through `NBRC_26.yaml`. The generated artifact is stale: its maintained owner has since been recovered as PYG Agar with five structured ingredient rows, source grounding, and a structured NBRC reference.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/26.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/26.yaml --out /private/tmp/26.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/26.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/26.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The intended source identity is NBRC Medium 25, PYG Agar:

- The legacy target name is `26`, but its import event says `Media No. 25`, and the maintained owner now carries `name: PYG Agar` plus `media_term.term.id: nbrc.medium:25`.
- The inspected live NBRC page for `NO=25` resolves to Medium No. 25, PYG Agar, and lists the same proteose peptone, yeast extract, glucose, distilled water, and agar formulation represented in `data/normalized_yaml/bacterial/NBRC_26.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:007482`, `NBRC_26`, `NO=25`, `Media No. 25`, the target merge fingerprint, and the concatenated legacy ingredient found the expected normalized owner and generated target. The search included ignored files under the searched paths. The `NO=25` token also found TOGO/NBRC source URLs for different NBRC accessions such as `NO=250`, so only exact `NO=25` rows were used.

Ingredient grounding in the generated target is unusable. The five source components are collapsed into one ungrounded `VARIABLE` ingredient, and the second row has an empty ingredient label with `Proteose peptone` incorrectly stored in `concentration.value`.

## Evidence

The inspected NBRC Medium 25 page supports this PYG Agar formulation:

- 1 g proteose peptone.
- 1 g yeast extract.
- 1 g glucose.
- 1 L distilled water.
- 15 g agar.

Those five components are represented in `data/normalized_yaml/bacterial/NBRC_26.yaml`, but the generated target predates that recovery and still has the malformed pre-recovery rows. The target's generic `Dissolve ingredients in distilled water`, `Adjust pH if needed`, and `Sterilize by autoclaving` steps are unsupported by the inspected NBRC table; the NBRC page inspected for this review lists no pH or preparation text for Medium 25.

## Completeness

Consequential gaps:

- PYG Agar's five source components are not present as five structured ingredient rows in the generated target.
- `name: PYG Agar`, `media_term: nbrc.medium:25`, and the structured NBRC reference are absent from the generated target.
- The generated record is not linked to the separate TOGO M1426 PYG Agar import, even though `data/normalized_yaml/bacterial/pyg_agar.yaml` names the same NBRC `NO=25` original URL.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected NBRC source establishes a medium formulation, not a growth outcome.
- `ph_value` is correctly absent. The inspected NBRC Medium 25 source does not list a pH.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-26.md` report before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated target is stale and still stores the NBRC Medium 25 formulation as a malformed concatenated ingredient plus an empty-label artifact row. | The inspected NBRC page lists five separate component rows; `data/normalized_yaml/bacterial/NBRC_26.yaml` already carries those five rows; `data/merge_yaml/merged/26.yaml` instead has `Proteose peptone1gYeast extract1gGlucose1gDistilled water1LAgar15g` as one `VARIABLE` ingredient and a second ingredient with `preferred_term: ''`. | Generated `data/merge_yaml/merged/26.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_26.yaml` |
| Major | The generated target omits the recovered PYG Agar source identity and structured NBRC provenance. | `NBRC_26.yaml` now has `name: PYG Agar`, `media_term: nbrc.medium:25`, and a structured NBRC reference; the generated target has none of those fields and was last merged on 2026-08-06. | Generated `data/merge_yaml/merged/26.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_26.yaml` |
| Major | TOGO M1426 duplicates NBRC Medium 25 but remains unreconciled and contains a water-unit defect. | `data/normalized_yaml/bacterial/pyg_agar.yaml` points to `NBRC_M25` and the same NBRC `NO=25` URL, but stores `Distilled water` as `1 G_PER_L` and is not linked to `CultureMech:007482`; the generated layer keeps it as `PYG_Agar.yaml`. | `data/normalized_yaml/bacterial/pyg_agar.yaml` plus reciprocal variant links in `data/normalized_yaml/bacterial/NBRC_26.yaml` |
| Major | The generated target carries generic unsupported preparation steps. | The NBRC table inspected for Medium 25 lists the ingredient amounts but no pH or dissolve/pH/autoclave instructions. | `data/normalized_yaml/bacterial/NBRC_26.yaml` if source-supported preparation instructions are found later; otherwise regenerate so the empty maintained `preparation_steps` list is reflected in the merge |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/26.yaml` from `data/normalized_yaml/bacterial/NBRC_26.yaml` so the generated target carries the recovered PYG Agar name, five-row composition, `media_term: nbrc.medium:25`, structured NBRC reference, and no generic unsupported preparation steps.
2. Curate `data/normalized_yaml/bacterial/pyg_agar.yaml`: correct distilled water from `1 G_PER_L` to a 1 L solvent representation, add structured TOGO/NBRC references, preserve `TOGO:M1426`, and link it to `data/normalized_yaml/bacterial/NBRC_26.yaml` as a `SOURCE_DUPLICATE` in both directions.
3. Regenerate `data/merge_yaml/merged/` after adding the maintained duplicate relationship so the NBRC and TOGO PYG Agar imports are no longer unrelated generated records.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/NBRC_26.yaml` after any source-owner curation.
- `just validate-schema data/normalized_yaml/bacterial/pyg_agar.yaml` after duplicate curation.
- `just validate-strict data/normalized_yaml/bacterial/pyg_agar.yaml` after duplicate curation.
- `just validate-terms data/normalized_yaml/bacterial/pyg_agar.yaml` after correcting distilled water and adding variant links.
- `just validate-references data/normalized_yaml/bacterial/pyg_agar.yaml` after adding structured references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 25 for 1 g/L proteose peptone, 1 g/L yeast extract, 1 g/L glucose, 1 L distilled water, and 15 g/L agar.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated target has no structured `references` entries.
- The official NBRC page gives the title `PYG Agar`; the generated target's `name: 26` is a legacy import artifact already repaired upstream in `NBRC_26.yaml`.
