# YAML Record Review: sap2_medium__095b7571

- Repository: CultureMech
- Record: data/merge_yaml/merged/sap2_medium__095b7571.yaml
- Started UTC: 2026-09-25T04:22:55Z
- Finished UTC: 2026-09-25T04:22:55Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:002997`, `sap2_medium`, from `data/merge_yaml/merged/sap2_medium__095b7571.yaml`.

The record is the direct MediaDive/JCM J651 import for `SAP2 MEDIUM`, with `modified_sap2_medium` / JCM J910 merged into it as a synonym.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The primary identity is correctly grounded to JCM Medium J651.

The synonym and `merged_from` link to `modified_sap2_medium` are not valid as an exact duplicate. JCM 910 is a derivative of JCM 651 that uses 1.0x artificial seawater instead of 0.5x artificial seawater, with an additional instruction to omit agar for liquid medium.

## Evidence

JCM 651 lists 1 g Tryptone, 1 g Yeast extract, 15 g Agar, and 1 L 0.5x Artificial seawater, then adjusts pH to 7.0.

JCM 651 defines the 0.5x Artificial seawater as 15 g NaCl, 0.35 g KCl, 5.4 g MgCl2 x 6H2O, 2.7 g MgSO4 x 7H2O, 0.5 g CaCl2 x 2H2O, and 1 L distilled water.

JCM 910 explicitly says to use JCM 651 with 1.0x artificial seawater instead of 0.5x artificial seawater, and to omit agar for preparation of liquid medium.

## Completeness

The generated JCM 651 formula contains the tryptone, yeast extract, agar, pH 7.0, and all five 0.5x artificial-seawater salts.

The local artificial-seawater distilled-water row is absent.

The generated record does not preserve JCM 910 as a separate modified recipe or variant; it folds JCM 910 into the JCM 651 SAP2 record as a synonym.

## Findings

The generated merge layer falsely merged base SAP2 medium with Modified SAP2 medium. JCM 910 intentionally changes the artificial-seawater strength and can also be prepared as a liquid by omitting agar.

The JCM 651 local 0.5x Artificial seawater table was flattened into parent ingredients. That is chemically tolerable for the five salt rows because JCM 651 adds 1 L of this stock, but the flattening dropped the 1 L distilled-water row and the stock label.

## Recommended Edits

Reject the JCM 651/JCM 910 duplicate edge before regeneration so `modified_sap2_medium` is not listed as a synonym or `merged_from` source of `sap2_medium`.

Represent JCM 910 as a modified SAP2 variant with 1.0x artificial seawater, and retain the agar omission as a liquid-medium preparation variant.

Restore the 1 L distilled-water row from the JCM 651 0.5x Artificial seawater table in the normalized SAP2 source or preserve that table as a local solution.

Regenerate the merge layer after the normalized source and duplicate set are repaired.

## Follow-up Checks

Confirm regenerated `sap2_medium` has no `modified_sap2_medium` synonym and no JCM 910 `merged_from` source.

Confirm regenerated `modified_sap2_medium` carries 1.0x artificial seawater and no longer collapses onto JCM 651 by fingerprint.

Confirm the base JCM 651 record still has 15 g/L agar and pH 7.0.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
