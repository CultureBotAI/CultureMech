# YAML Record Review: HALOBIUS MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/halobius_medium__3e415a75.yaml
- Started UTC: 2026-09-23T10:04:46Z
- Finished UTC: 2026-09-23T10:05:45Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/halobius_medium__3e415a75.yaml`, a generated
`MediaRecipe` with `id: CultureMech:002687`, `category: bacterial`,
`physical_state: SOLID_AGAR`, `ph_value: 6.2`, and
`media_term.term.id: mediadive.medium:J329`.

The generated record is a single-source merge from the maintained parent
`data/normalized_yaml/bacterial/halobius_medium.yaml`, which imports JCM Medium
J329 through MediaDive. Future fixes belong in that normalized parent or in the
MediaDive/JCM importer, then require regeneration of `data/merge_yaml/merged/`.

## Validation

All focused structural validators passed on the generated YAML:

| Check | Result |
|---|---|
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/halobius_medium__3e415a75.yaml` | Passed |
| `python scripts/validate_strict.py data/merge_yaml/merged/halobius_medium__3e415a75.yaml --out /private/tmp/halobius_medium__3e415a75.strict.tsv --workers 1 --quiet` | Passed, 0 error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/halobius_medium__3e415a75.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks |
| `linkml-term-validator validate-data data/merge_yaml/merged/halobius_medium__3e415a75.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |

Embedded `MediaRecipe.curation_history` was not checked: the repository's
`just validate-history` target validates standalone files under `history/`,
not embedded curation events in generated media YAML.

## Identity and Grounding

The record identifies the correct medium. JCM GRMD 329 and MediaDive J329 both
name `HALOBIUS MEDIUM`, and the generated pH 6.2, solid state, and ingredient
amounts match the inspected source.

Most exact chemical forms are preserved. `Trisodium citrate x 2 H2O` is
grounded only as generic sodium citrate, so the hydrate needs an exact verified
mapping or should remain unresolved.

## Evidence

Supported by inspected JCM, MediaDive, and Togo sources:

- JCM 329 is `HALOBIUS MEDIUM`.
- The recipe contains 7.5 g BD-Difco vitamin-assay casamino acids, 10 g
  BD-Difco yeast extract, 3 g trisodium citrate dihydrate, 2 g KCl, 20 g
  `MgSO4 x 7 H2O`, 116 g NaCl, 23 mg `FeCl2 x 4 H2O`, 20 g agar, and 1 L
  distilled water.
- The medium is adjusted to pH 6.2.

Unsupported or incomplete claims:

- Distilled water is absent from the direct JCM generated ingredient list.
- The yeast-extract row lost its BD-Difco qualifier.
- The casamino-acids row splits the JCM `Casamino acids, vitamin assay
  (BD-Difco)` label into a plain preferred term and a free-text note.
- JCM's default autoclaving condition is absent.

## Completeness

- The record has no `target_organisms` or `growth_metrics`; no organism-growth
  claims are asserted or missing evidence.
- Source identity is recoverable through `media_term` and `notes`, but there
  are no first-class `sources` entries for MediaDive J329 or JCM GRMD 329.
- An exact `rg --no-ignore --hidden` search over `data/merge_yaml` and
  `data/normalized_yaml` for `mediadive.medium:J329`, `JCM Medium J329`, the
  JCM GRMD 329 URL, and the source label found this direct JCM/MediaDive record
  plus a separate Togo M324 import for the same JCM source page.

## Findings

### Blocker

None found.

### Major

1. The JCM distilled-water row is absent. The source table explicitly includes
   1 L distilled water, and the direct MediaDive REST record carries 1000 ml
   water, but the generated direct record omits it. Owner:
   `data/normalized_yaml/bacterial/halobius_medium.yaml` or the MediaDive/JCM
   importer.

2. The same JCM GRMD 329 source is split across two generated records. The Togo
   M324 sibling preserves the water row and the BD-Difco qualifiers but has the
   common Togo `FeCl2 x 4 H2O` unit bug, storing 23 mg as 23 g/L. Owner: the
   Togo M324 parent and duplicate/merge rules.

### Minor

1. BD-Difco/vitamin-assay product qualifiers are incomplete on the direct JCM
   record. Owner: the MediaDive attribute and condition importer.

2. `Trisodium citrate x 2 H2O` is grounded to generic sodium citrate. Owner:
   the normalized ingredient grounding and packaged MediaIngredientMech label
   index.

3. JCM's default autoclaving condition is absent. Owner: the normalized parent
   or importer.

4. First-class `sources` are absent; the JCM URL and accession are present only
   in `media_term` and `notes`. Owner: the normalized parent or importer.

## Recommended Edits

1. Add the 1 L distilled-water row to the direct JCM/MediaDive parent.
2. Preserve `Yeast extract (BD-Difco)` and `Casamino acids, vitamin assay
   (BD-Difco)` as exact ingredient labels or qualifiers.
3. Recheck trisodium citrate dihydrate against the packaged MIM label index and
   OAK.
4. Repair the Togo M324 sibling's `FeCl2 x 4 H2O` unit from `23 G_PER_L` to
   `0.023 G_PER_L`, then reconcile it with the direct JCM record.
5. Add JCM's default autoclaving condition and first-class source entries if
   the importer supports them.
6. Regenerate merge products after repairing the maintained inputs or merge
   rules.

## Follow-up Checks

- Re-run schema, strict, term, and reference validation on the edited direct
  JCM and Togo M324 parents.
- Fetch MediaDive J329, JCM GRMD 329, and Togo M324 again and compare water,
  iron units, pH, and product qualifiers.
- Re-run merge freshness checks to ensure the repaired Togo import no longer
  remains as a duplicate generated canonical record.

## Additional Notes

None found.
