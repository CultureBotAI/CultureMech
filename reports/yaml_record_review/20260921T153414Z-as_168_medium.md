# YAML Record Review: AS-168 Medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/as_168_medium.yaml`
- Started UTC: 2026-09-21T15:34:04Z
- Finished UTC: 2026-09-21T15:34:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008707` |
| Name | `as_168_medium` |
| Original name | AS-168 Medium |
| Category | `bacterial` |
| Source identity | `TOGO:M2114`, NBRC `NBRC_M1439` |
| Generated status | Generated single-source merge of `data/normalized_yaml/bacterial/as_168_medium.yaml` |

The similarly named `modified_as_168_medium` and
`modified_as_168_medium2` normalized records are distinct TOGO recipes and are
not part of this review.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/as_168_medium.yaml` | Passed; no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/as_168_medium.yaml --out /private/tmp/as_168_medium.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/as_168_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the validator reported 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/as_168_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: no focused embedded `MediaRecipe.curation_history` validator is documented for one generated merge record; `just validate-history` targets standalone records under `history/`. |

The documented `just` validator entrypoints were not used because project `uv`
fails while building `llvmlite==0.46.0` under Python 3.13. The no-project
commands above ran the relevant focused validator families.

## Identity and Grounding

The record's `CultureMech:008707`, `TOGO:M2114`, original NBRC `M1439`, and
`AS-168 Medium` labels agree. TOGO `M2114` points to NBRC medium 1439, and the
NBRC page for `NO=1439` is fetchable and reports the same composition.

All grounded simple salts match the hydrate or salt form named by TOGO/NBRC.
`Bacto Yeast Extract (Difco)`, `Bacto Casamino acids (Difco)`, and optional
agar are intentionally undefined or unlinked proprietary/complex materials.

## Evidence

NBRC M1439 and TOGO M2114 agree on a single 1 L recipe with NaCl 150 g, MgSO4.7H2O
20 g, KCl 2 g, trisodium citrate 3 g, sodium glutamate 1.8 g, FeSO4.7H2O 50 mg,
MnCl2.4H2O 0.36 mg, Bacto Casamino Acids 5 g, Bacto Yeast Extract 5 g, optional
agar 15 g, distilled water 1 L, and pH 8.0.

The generated record changes three source quantities:

| Source row | Current representation |
|---|---|
| Distilled water, 1 L | `1 G_PER_L` |
| FeSO4.7H2O, 50 mg | `50 G_PER_L` |
| MnCl2.4H2O, 0.36 mg | `0.36 G_PER_L` |

## Completeness

The ingredient set is otherwise complete for the NBRC formulation, including
the optional agar row. The record is missing pH 8.0, and `physical_state:
SOLID_AGAR` overstates a source row that says agar is included only if needed.

Optional target-organism and growth-evidence slots are empty. I did not flag
that as a defect because the inspected NBRC and TOGO records establish only the
recipe.

Before reporting raw-source absence, I searched for `CultureMech:008707`,
`M2114`, `NBRC_M1439`, and `AS-168 Medium` with `rg --no-ignore --hidden` across
`data`, `scripts`, `history`, `src`, `reports`, and `references_cache`. That
gitignore-independent search found the normalized owner, generated merge,
indexes, related Modified AS-168 records, and archived reports, but no
maintained raw YAML or source-specific importer file for M2114.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | Two trace rows have milligram-to-gram unit slips. | NBRC and TOGO list 50 mg FeSO4.7H2O and 0.36 mg MnCl2.4H2O; the generated record stores `50 G_PER_L` and `0.36 G_PER_L`. | `data/normalized_yaml/bacterial/as_168_medium.yaml` |
| major | The water row uses the wrong unit. | NBRC and TOGO list 1 L distilled water; the record stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/as_168_medium.yaml` |
| major | pH 8.0 is missing. | Both inspected sources list pH 8.0 at the end of the recipe. | `data/normalized_yaml/bacterial/as_168_medium.yaml` |
| minor | The physical state should preserve that agar is optional. | The source row is `Agar (if needed)`, while the generated record sets `physical_state: SOLID_AGAR`. | `data/normalized_yaml/bacterial/as_168_medium.yaml` |

## Recommended Edits

1. Correct FeSO4.7H2O and MnCl2.4H2O to source milligram amounts or equivalent grams per liter.
2. Represent 1 L distilled water as a volume row, not `1 G_PER_L`.
3. Add pH 8.0.
4. Clarify that agar is optional and revisit the always-solid `physical_state` classification.
5. Regenerate `data/merge_yaml/merged/as_168_medium.yaml`.

## Follow-up Checks

- Run `just validate-schema data/normalized_yaml/bacterial/as_168_medium.yaml`.
- Run `just validate-strict data/normalized_yaml/bacterial/as_168_medium.yaml`.
- Run `just validate-terms data/normalized_yaml/bacterial/as_168_medium.yaml`.
- Run `just validate-references data/normalized_yaml/bacterial/as_168_medium.yaml`.
- Run `just verify-merges` after regeneration.
- Manually compare the regenerated record against TOGO M2114 and NBRC M1439 to confirm the two milligram rows, the water unit, pH 8.0, and optional agar semantics are represented.

## Additional Notes

- `high_metal: true` is plausible for a formulation containing 150 g NaCl and
  high MgSO4, so I did not treat that flag as a defect.
