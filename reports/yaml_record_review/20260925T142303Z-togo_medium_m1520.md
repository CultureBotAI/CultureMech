# YAML Record Review: togo_medium_m1520

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1520.yaml
- Started UTC: 2026-09-25T14:18:00Z
- Finished UTC: 2026-09-25T14:23:03Z
- Verdict: needs curation

## Target

- Class: MediaRecipe
- ID: CultureMech:008066
- Name: togo_medium_m1520
- Generated status: generated canonical merge under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/togo_medium_m1520.yaml`.
- Source identity in the generated record: TOGO:M1520, original source NBRC_M306, original URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=306`.
- Merge lineage in the generated record: only `togo_medium_m1520`.

## Validation

- Schema: passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/togo_medium_m1520.yaml`; the command printed `No issues found`.
- Strict: passed with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1520.yaml --out /private/tmp/m1520.strict.tsv --workers 1 --quiet`; the TSV contained one header row and zero error rows.
- References: passed with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1520.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`; it reported zero checks.
- Terms: passed with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1520.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`; it emitted only the known `eutils`/`pkg_resources` warning plus a pass message.
- Embedded history: Not checked: the repository exposes `just validate-history` for standalone `history/` records, not for `MediaRecipe.curation_history` blocks in merged YAML.

## Identity and Grounding

- TOGO M1520 resolves to NBRC_M306 at NBRC medium 306 with pH 7.0. The source identity, TOGO identifier, and source URL all agree.
- The record is not merged with a sibling or variant.
- The resolved CHEBI terms for MgSO4 x 7 H2O, NaCl, K2HPO4, Fe(NH4)2(SO4)2 x 6 H2O, sodium citrate, and water match the source ingredient labels.
- Yeast extract, agar if needed, Bacto Casamino Acids, and Hipolypepton are intentionally left without ontology terms rather than being grounded to a near miss.

## Evidence

- TOGO M1520 and NBRC 306 support the ingredient names and amounts in the generated record, including 80 g NaCl, 20 g MgSO4 x 7 H2O, 7.5 g Bacto Casamino Acids, 5 g Hipolypepton, and optional 15 g agar.
- The asterisk after `Hipolypepton*` is only a supplier footnote to Wako Pure Chemical in the NBRC page. The generated `solutions` entry turns Hipolypepton into an empty unknown solution and therefore drops the 5 g medium ingredient.
- NBRC 306 gives pH 7.0; no pH field is present.
- The source marks agar as "if needed." The generated `physical_state: SOLID_AGAR` implies a solid medium even though the formulation also supports a no-agar liquid form.

## Completeness

- Consequential missing slots: pH 7.0 and an explicit representation of Hipolypepton as a 5 g ingredient instead of an empty solution.
- Empty optional slots for target organisms and growth metrics are not a defect in this generated NBRC medium review.
- Before this report was written, an exact `find` in `reports/yaml_record_review` and an exact `rg --no-ignore --hidden` search for `togo_medium_m1520` found no existing report for this record stem.

## Findings

- major: `data/normalized_yaml/bacterial/togo_medium_m1520.yaml` migrated `Hipolypepton*` into an empty `solutions` entry. NBRC_M306 uses the asterisk for a supplier footnote, so the 5 g Hipolypepton component has been removed from `ingredients` without creating a real stock solution.
- major: `data/normalized_yaml/bacterial/togo_medium_m1520.yaml` omits source pH 7.0 and represents the optional `Agar (if needed)` branch as unconditional `physical_state: SOLID_AGAR`.

## Recommended Edits

- In `data/normalized_yaml/bacterial/togo_medium_m1520.yaml`, move Hipolypepton back to `ingredients` at 5 g and preserve the Wako Pure Chemical supplier note as a note, not a stock-solution boundary.
- Add the NBRC pH 7.0 value.
- Reconsider the physical-state model for `Agar (if needed)`: either represent the base recipe as liquid with optional agar in preparation text or split a solid variant if the corpus pattern supports that distinction.
- Regenerate `data/merge_yaml/merged/` after the normalized owner is fixed.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation for `data/normalized_yaml/bacterial/togo_medium_m1520.yaml`.
- Re-read the regenerated `data/merge_yaml/merged/togo_medium_m1520.yaml` and verify that `Hipolypepton*` is not an empty unknown solution.

## Additional Notes

- The current validators are shape validators; all passed while a source ingredient was represented as a blank stock solution.
- Exact source inspection was bounded to TOGO M1520 and NBRC 306.
