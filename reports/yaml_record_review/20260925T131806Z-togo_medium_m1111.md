# YAML Record Review: togo_medium_m1111

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1111.yaml`
- Started UTC: 2026-09-25T13:18:06Z
- Finished UTC: 2026-09-25T13:18:06Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1111, imported from JCM medium 1046.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1111.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/` records, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1111`; the TOGO API identifies JCM_M1046 as its original medium ID and links JCM medium 1046.

The JCM source lists 1 ml of a trace element solution in the parent medium and defines that trace stock in 100 ml distilled water.

## Evidence

The parent salts, glucose, yeast extract, agar, and 1 ml trace solution addition are present in the TOGO source.

The generated record flattens the 100 ml trace solution into parent `ingredients`, merges trace water with parent water, and emits an empty `Unknown solution` stub for the source trace solution.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated YAML cannot reconstruct the JCM trace stock because its trace `solutions` entry has no composition.

## Findings

- Major issue: trace stock ingredients are recorded as final g/L amounts. The source gives milligram amounts in a 100 ml trace stock and adds only 1 ml of that stock to the parent medium.
- Major issue: the parent 1 L water and trace 100 ml water were merged into `Distilled water: 101 G_PER_L`, which crosses a stock boundary and converts volumes to mass units.
- Major issue: the generated `solutions` entry preserves `Trace element solution (see below)` only as an empty `Unknown solution`.
- Minor issue: hydrate-specific source names such as `CoCl2 x 6H2O` are grounded to less specific CHEBI terms in a few rows.

## Recommended Edits

- Restore the trace element solution as a nested component or dilute its ingredients by the 1 ml addition to the final liter.
- Keep the trace stock water out of the parent water row.
- Populate or remove the empty placeholder solution entry after preserving the actual trace component.
- Recheck CHEBI grounding for trace salts with explicit hydration states.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Verify that duplicate cleanup no longer merges water or salts across parent and trace levels.

## Additional Notes

Exact local source search found only the expected normalized TOGO M1111 source for this merge. Ignored files were included for duplicate-report checks with `find`, and no pre-existing ignored report for this generated record was found.
