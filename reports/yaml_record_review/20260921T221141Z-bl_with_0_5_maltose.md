# YAML Record Review: bl_with_0_5_maltose

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bl_with_0_5_maltose.yaml
- Started UTC: 2026-09-21T22:10:30Z
- Finished UTC: 2026-09-21T22:11:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/bl_with_0_5_maltose.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:009801` |
| Name | `bl_with_0_5_maltose` |
| Original name | `BL With 0.5% Maltose` |
| Category | `bacterial` |
| Source term | `TOGO:M416` / `BL With 0.5% Maltose` |
| Merge fingerprint | `922eb405178f210cf96538eb7999175330eb8d833bc97d511ca8e8d19ad27437` |
| Merged from | `bl_with_0_5_maltose` |
| Maintained owner | `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml` |
| Generated status | Derived merge product; future fixes belong in `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml` or merge regeneration, not in this generated YAML. |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bl_with_0_5_maltose.yaml` | Passed, `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/bl_with_0_5_maltose.yaml --out /private/tmp/bl_with_0_5_maltose.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/bl_with_0_5_maltose.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with exit 0 and no reference diagnostics. The record has no structured `references` to check. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/bl_with_0_5_maltose.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after a non-fatal `eutils` `pkg_resources` warning. |
| Embedded curation history | Not checked: no focused embedded `MediaRecipe.curation_history` validator is documented for one generated merge record; `just validate-history` targets standalone files under `history/`. |

Direct `just validate-schema`, `just validate-strict`, `just validate-references`, and `just validate-terms` were not rerun because this checkout still fails before target-specific validation while the project `uv` environment tries to build `llvmlite==0.46.0` under Python 3.13. The no-project Python 3.11 commands above are the same validators with cached dependencies and the target file supplied directly.

## Identity and Grounding

- `CultureMech:009801`, `name: bl_with_0_5_maltose`, `original_name: BL With 0.5% Maltose`, `media_term.term.id: TOGO:M416`, `category: bacterial`, `medium_type: COMPLEX`, and `physical_state: SOLID_AGAR` all match the TOGO M416 source identity.
- The live TOGO M416 API identifies the recipe as `BL With 0.5% Maltose`, `original_media_id: JCM_M418`, and `src_url: https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=418`.
- The live JCM GRMD 418 page identifies JCM 418 as `BL WITH 0.5% MALTOSE` and lists only two rows: 1.0 L BL Agar from Medium 13 and 5.0 g maltose.
- The generated merge is stale. Its last merge event is from 2026-08-06; the normalized owner was repaired on 2026-09-10 by `scripts/repair_official_simple_score20.py` and relinked on 2026-09-11 by `scripts/repair_togo_m6_score15.py`.
- Exact ignored-file-inclusive searches over `data/normalized_yaml`, `data/merge_yaml/merged`, `data/culturemech_id_registry.tsv`, `data/culturemech_recipe_catalog.tsv`, `scripts`, `tests`, `reports/yaml_record_review`, and `history` for `CultureMech:009801`, `CultureMech:010526`, exact `TOGO:M416`, exact `mediadive.medium:J418`, exact `GRMD=418`, the exact merge fingerprint, and `bl_with_0_5_maltose.yaml` found the stale generated TOGO M416 merge, the repaired TOGO M416 owner, the active repaired direct JCM J418 owner, the stale direct JCM J418 generated sibling, and the September repair scripts.

## Evidence

### Source Claims That Are Supported

| Claim | Support |
|---|---|
| TOGO M416 mirrors JCM Medium 418. | TOGO M416 points to `JCM_M418` and the live JCM GRMD 418 URL. |
| JCM 418 is 1.0 L BL Agar plus 5.0 g maltose. | The live JCM page lists `BL Agar (see Medium No. 13)` at 1.0 L and `Maltose` at 5.0 g. The live TOGO M416 API lists `Maltose` at 5 g and `BL AGAR (GLUCOSE BLOOD LIVER AGAR) (see Medium [M6])` at 1 L. |
| The maltose row is chemically grounded. | The generated record and normalized owner both ground `Maltose` to `CHEBI:18167` / `alpha-maltose`. |
| The BL Agar row should reference the canonical JCM Medium 13 parent. | JCM 418 links directly to JCM 13; the repaired normalized owner links `BL Agar` to `CultureMech:002499`, the canonical direct JCM 13 BL Agar record. |

### Unsupported or Misplaced Claims in the Generated Record

| Generated claim | Problem |
|---|---|
| `solutions[0]` has `composition: []`, `name: Unknown solution`, and `concentration: 1 G_PER_L`. | The source row is 1.0 L BL Agar. The repaired owner correctly represents this as 1000 `ML_PER_L` of `BL Agar` linked to `CultureMech:002499`. |
| No `parent_media`, `variant_relationship`, or `variant_modifications`. | JCM 418 is explicitly a supplemented BL Agar variant; the repaired owner already models it as `SUPPLEMENTED_VARIANT` of `CultureMech:002499`. |
| No `preparation_steps`. | The repaired owner carries the source-level assembly instruction: use 1.0 L BL Agar with 5.0 g/L maltose. |
| No `references`. | The repaired owner has structured references to TOGO M416, JCM 418, TOGO M6, and JCM 13. |
| Separate active direct-JCM duplicate. | `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml` is `mediadive.medium:J418` for the same JCM 418 recipe; the generated TOGO M416 record is not merged with or linked to that active source duplicate. |

## Completeness

- **Ingredients:** the maltose row is source-supported. The generated `value: '5'` should be normalized to the repaired `5.0` presentation on regeneration but is not materially wrong.
- **Solutions:** stale and incomplete. The generated solution is an empty placeholder with a mass unit; the source says to add 1.0 L of JCM Medium 13 BL Agar.
- **Parent/variant links:** missing in the generated merge even though the maintained owner now has a canonical JCM 13 `parent_media`.
- **References:** missing in the generated merge even though the maintained owner now has JCM and TOGO references.
- **Growth context:** `target_organisms` and `growth_metrics` are correctly absent. TOGO M416 and JCM 418 are formulation sources, not strain-specific growth assays.
- **Optional fields:** pH, atmosphere, salinity, and storage are acceptable to leave empty because neither inspected JCM 418 nor TOGO M416 gave those conditions for this wrapper recipe.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still has a broken BL Agar placeholder. | The generated file stores BL Agar as an empty `Unknown solution` at `1 G_PER_L`. The live source row is 1.0 L BL Agar, and the repaired owner stores `BL Agar` at `1000 ML_PER_L` with a `CultureMech:002499` link. | Already corrected in `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`; regenerate `data/merge_yaml/merged/`. |
| Major | Source-supported variant structure and references are missing from the generated merge. | The normalized owner now has `parent_media`, `variant_relationship: SUPPLEMENTED_VARIANT`, `variant_modifications`, `preparation_steps`, `data_quality_flags`, and four `references`; the generated merge has none of them. | Already corrected in `data/normalized_yaml/bacterial/bl_with_0_5_maltose.yaml`; regenerate `data/merge_yaml/merged/`. |
| Major | The active direct JCM J418 duplicate is neither merged nor linked in the generated output. | The exact ignored-file-inclusive search found `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml`, which represents `mediadive.medium:J418` and the same 1.0 L BL Agar plus 5.0 g maltose recipe. | Add source-duplicate linkage between the TOGO M416 and direct JCM J418 owners or adjust merge generation to co-merge them. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `data/merge_yaml/merged/bl_with_0_5_maltose.yaml` picks up the repaired 1000 `ML_PER_L` BL Agar solution, `CultureMech:002499` parent link, preparation step, references, and quality flags.
2. Add explicit source-duplicate linkage between the TOGO M416 owner and `data/normalized_yaml/fungal/bl_with_0_5_maltose.yaml`, or make merge generation combine the TOGO M416 and direct JCM J418 wrappers.
3. Verify that regenerated outputs do not contain `Unknown solution` for the BL Agar row or the stale `922eb405178f210cf96538eb7999175330eb8d833bc97d511ca8e8d19ad27437` fingerprint.

## Follow-up Checks

- Run `just verify-merges` and `just audit-merge-freshness` after regenerating merge outputs.
- Re-fetch TOGO M416 and JCM GRMD 418 and compare the regenerated record for 5.0 g/L maltose and 1000 ml/L BL Agar.
- Run focused schema, strict, term, and reference validators on the regenerated merge and on both normalized JCM 418 source duplicates.

## Additional Notes

- The exact `find` for `*-bl_with_0_5_maltose.md` under ignored `reports/yaml_record_review/` found no existing report before this file was written.
