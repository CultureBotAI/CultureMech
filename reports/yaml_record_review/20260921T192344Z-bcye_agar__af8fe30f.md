# YAML Record Review: bcye_agar__af8fe30f

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bcye_agar__af8fe30f.yaml
- Started UTC: 2026-09-21T19:22:14Z
- Finished UTC: 2026-09-21T19:23:44Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bcye_agar__af8fe30f.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M2133_BCYE<Greek alpha>_Agar.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008727` |
| Name | `bcye_agar` |
| Original name | `BCYEalpha Agar` |
| Source | Togo Medium `M2133`; original NBRC Medium `1469` |
| Merge status | Stale generated one-source merge from `TOGO_M2133_BCYEalpha_Agar` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated Togo M2133 merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this stale generated merge has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated Togo M2133 merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bcye_agar__af8fe30f.yaml` denotes Togo Medium M2133, `BCYEalpha Agar`, originally from NBRC Medium 1469.
- The generated merge is stale. Its stored fingerprint is `af8fe30f9ec416c498f766c49339b6a04c6f9f6399da160572307976e3512807`, while the current normalized owner fingerprints as `1cdcc230d2cc7e75337aec54a2f022912e1a09d2260a5c69688bbf2fb9010ea9` after a September repair.
- The inspected Togo M2133 API and NBRC Medium 1469 HTML page support the current normalized owner: yeast extract 10 g, ferric pyrophosphate 0.25 g, activated charcoal 1.5 g, ACES buffer 10 g, alpha-ketoglutarate 1 g, L-cysteine 0.4 g, distilled water 1 L, agar 15 g, pH 7.0, and microaerobic growth in an anaerobic jar with a CO2 gas pack.
- The generated record still has the older import state: it models 1 L distilled water as `1 G_PER_L`, omits pH 7.0, leaves ACES buffer ungrounded, and leaves CO2 gas ungrounded with a schema-defaulted variable concentration.
- Exact gitignore-independent searches for `CultureMech:008727`, `TOGO:M2133`, `NBRC_M1469`, and `TOGO_M2133` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the normalized owner, generated merge, indexes, import reports, and the September repair script, but no repository-local raw Togo M2133 or NBRC 1469 source dump under `data`.

## Evidence

- Togo M2133 reports pH 7.0 and lists all source component amounts in one `main solution 1`: 1 L distilled water, 10 g yeast extract, 1.5 g activated charcoal, 0.4 g L-cysteine, 10 g ACES buffer, 15 g agar, 0.25 g ferric pyrophosphate, 1 g alpha-ketoglutarate, and CO2 gas.
- Togo M2133 also records NBRC Medium 1469 as the original medium ID and links the same NBRC catalogue page inspected in this review.
- NBRC Medium 1469 confirms the same component list and pH 7.0, and carries the same note about microaerobic growth in an anaerobic jar equipped with a CO2 gas pack.
- The current normalized owner preserves those amounts, adds pH 7.0, represents distilled water as `1.0 L`, grounds ACES and CO2 gas, and records Togo plus NBRC references.
- The generated merge predates that correction and lacks each of those September additions.

## Completeness

- Consequential gaps:
  - The generated record has the wrong water unit.
  - The generated record omits source pH 7.0.
  - The generated record omits the Togo and NBRC references that now exist in the normalized owner.
  - The generated record's curation history lacks the September `RESOLVED_TOGO_M2133_SCORE15` event documenting the fix.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; neither the Togo API nor the NBRC Medium 1469 page gives a strain-specific growth metric.
  - Activated charcoal and alpha-ketoglutarate remain ungrounded in the normalized owner for defensible reasons: charcoal is an opaque commercial/material component, and the source does not specify the acid or salt form for alpha-ketoglutarate.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw Togo M2133 JSON dump or raw NBRC Medium 1469 HTML capture under `data/raw`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale relative to its normalized owner. | The Togo M2133 normalized owner was repaired in September with source-backed amounts, pH, references, and groundings; `bcye_agar__af8fe30f.yaml` still has the August fingerprint and none of that repair metadata. | Regenerate `data/merge_yaml/merged` from current `data/normalized_yaml`; do not hand-edit the generated file. |
| Major | The generated water amount has the wrong unit. | Togo M2133 and NBRC Medium 1469 list 1 L distilled water; the generated merge stores `Distilled water` as `1 G_PER_L`. | Already repaired in the Togo M2133 normalized owner; regenerate the merge. |
| Major | The generated record omits the source pH. | Togo M2133 and NBRC Medium 1469 both report pH 7.0; the generated merge has no pH slot. | Already repaired in the Togo M2133 normalized owner; regenerate the merge. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged` from the current normalized corpus with `just merge-recipes`.
2. Confirm the fresh Togo M2133 BCYE-alpha generated record carries `ph_value: 7.0`, a 1 L distilled-water row, ACES and CO2 gas groundings, the Togo and NBRC references, and the September `RESOLVED_TOGO_M2133_SCORE15` curation event.
3. Remove or ignore the stale `bcye_agar__af8fe30f.yaml` output after regeneration if the repaired fingerprint writes a new suffix.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the fresh generated Togo M2133 merge.
- Re-fetch Togo M2133 and NBRC Medium 1469 and manually compare all ingredient rows, pH 7.0, and the microaerobic CO2 note.
- Run `just verify-merges` and `just audit-merge-freshness --json --list` after regeneration.
- Re-run exact gitignore-independent searches for `CultureMech:008727`, `TOGO:M2133`, and `NBRC_M1469` under `data/merge_yaml/merged` to confirm no stale generated Togo M2133 representation remains.

## Additional Notes

- The report path uses the generated file stem. The authoritative normalized filename contains the Greek alpha character between `BCYE` and `_Agar`; this ASCII report renders that character as `<Greek alpha>`.
- Exact gitignore-independent searches included ignored files where present.
