# YAML Record Review: PYG Agar (E)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_agar_e.yaml
- Started UTC: 2026-09-24T22:22:05Z
- Finished UTC: 2026-09-24T22:23:11Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009983 |
| Name | pyg_agar_e |
| Original name | PYG Agar (E) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_agar_e.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml |
| Merge source | TOGO_M587_PYG_Agar_E |
| Source grounding | TOGO:M587, JCM_M582 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO normalization or in the TOGO import path that
converted the 1 L final-volume water row into the current `1 G_PER_L` row.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_agar_e.yaml` | Passed with `No issues found`. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_agar_e.yaml --out /private/tmp/pyg_agar_e.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_agar_e.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_agar_e.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_agar_e.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_agar_e.yaml` is the generated TOGO M587 branch
  for CultureMech:009983 and `PYG Agar (E)`.
- The TOGO M587 API reports original medium `JCM_M582`, pH 7.0, and the same
  JCM GRMD 582 URL that is embedded in the reviewed record. The JCM page title
  is `582 PYG AGAR (E)`.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M587`, TOGO M587
  URLs, `JCM_M582`, `GRMD=582`, and `TOGO_M587_PYG_Agar_E` found the reviewed
  TOGO branch, its maintained owner, a `data/metal_ree_analysis.yaml` index
  entry, and a direct JCM normalized file for the same GRMD page.
- The exact JCM GRMD 582 direct import is already linked as
  `data/normalized_yaml/bacterial/pyg_agar_e.yaml` under the canonical
  generated `data/merge_yaml/merged/pyg_medium_e.yaml` record. The TOGO copy
  therefore remains an unmerged duplicate of the same source page.

## Evidence

- JCM GRMD 582 supports the PYG Agar (E) identity, Bacto peptone 5.0 g, beef
  extract 3.0 g, yeast extract 0.2 g, glucose 5.0 g, NaCl 0.5 g, MgSO4.7H2O
  1.5 g, agar 15.0 g, and distilled water 1.0 L.
- The TOGO API agrees with the JCM component list and pH 7.0.
- The reviewed record preserves the listed solute masses for the one-liter
  medium, including 15 g/L agar and the `SOLID_AGAR` physical state.
- The reviewed record encodes the JCM 1.0 L distilled water row as
  1 `G_PER_L`, which is not a supported final volume or amount.
- The reviewed record omits pH 7.0 and the JCM default instruction to autoclave
  media at 121C for 15 min unless otherwise stated.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- `ph_value` is missing even though both JCM and TOGO provide pH 7.0.
- No preparation step captures the JCM default autoclaving condition.
- The MgSO4.7H2O primary term was fixed to magnesium sulfate heptahydrate, but
  there is no corresponding `mediaingredientmech_chebi_term`.
- Exact ignored-file search included ignored files before assessing the exact
  TOGO/JCM source scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The source 1 L water row is misencoded as `1 G_PER_L`. | JCM GRMD 582 and TOGO M587 both describe a one-liter recipe. The YAML stores the final water row as 1 g/L, which is not the source volume and prevents this TOGO branch from matching the equivalent direct JCM import. | data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml; TOGO import |
| Major | This exact JCM source remains a generated duplicate instead of merging with the canonical PYG medium E branch. | Exact ignored-file search found `data/normalized_yaml/bacterial/pyg_agar_e.yaml` for GRMD 582, and that direct JCM copy is already linked into `data/merge_yaml/merged/pyg_medium_e.yaml`; the TOGO M587 copy remains separate. | data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml; merge_recipes.py |
| Minor | pH and default sterilization guidance are absent. | The JCM page states pH 7.0 and the site default says to autoclave at 121C for 15 min unless otherwise stated, but the TOGO-derived YAML has no pH field or preparation step. | data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml |
| Minor | MgSO4.7H2O lacks a MediaIngredientMech CHEBI back-link. | The row has primary `CHEBI:31795` magnesium sulfate heptahydrate but no `mediaingredientmech_chebi_term`, leaving it inconsistent with the direct JCM duplicate. | data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml; MediaIngredientMech enrichment |

## Recommended Edits

1. Fix the water conversion in
   `data/normalized_yaml/bacterial/TOGO_M587_PYG_Agar_E.yaml` so the source 1 L
   row is represented consistently with other one-liter JCM imports instead of
   as 1 g/L.
2. Add pH 7.0 and the JCM default 121C for 15 min autoclaving step.
3. Refresh the MediaIngredientMech CHEBI link for MgSO4.7H2O after the primary
   heptahydrate grounding.
4. Regenerate merged records and verify the TOGO M587 copy merges with, or is
   explicitly deduplicated against, the direct JCM GRMD 582 branch already
   represented in `pyg_medium_e.yaml`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_agar_e.yaml`.
- Manually compare the regenerated TOGO M587 branch against
  `data/normalized_yaml/bacterial/pyg_agar_e.yaml` to ensure both JCM GRMD 582
  imports have equivalent pH, composition, and final-volume handling.
- Run an exact ignored-file search for `TOGO:M587`, `JCM_M582`, and `GRMD=582`
  to confirm no stale duplicate remains after regeneration.

## Additional Notes

- The local source fetches for TOGO and JCM initially failed with sandbox DNS
  errors. Both official URLs were then fetched successfully with the approved
  escalated `curl -L` rule.
