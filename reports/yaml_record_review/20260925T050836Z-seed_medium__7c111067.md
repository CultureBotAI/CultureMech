# YAML Record Review: seed_medium__7c111067

- Repository: CultureMech
- Record: data/merge_yaml/merged/seed_medium__7c111067.yaml
- Started UTC: 2026-09-25T05:08:36Z
- Finished UTC: 2026-09-25T05:08:36Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:009656`, `seed_medium`, from `data/merge_yaml/merged/seed_medium__7c111067.yaml`.

The target record is a single-source TOGO Medium M3215 import.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is correctly grounded to TOGO Medium M3215.

The same-name TOGO M3233 `Seed medium` record has a different glucose, phosphate, magnesium, and monosodium glutamate formulation and should stay separate.

An exact M3215 search found no same-source duplicate in the generated or normalized YAML.

## Evidence

TOGO Medium M3215 lists 1 L Distilled water, 10 g/L Yeast extract, 3 g/L NaCl, 5 g/L Beef extract, and 10 g/L `Trypton`.

The TOGO M3215 payload grounds the record pH at 7.0.

Its comments state that the seed medium had the same composition as nutrient agar slants but with agar eliminated.

## Completeness

The generated merge layer is stale relative to the repaired normalized source.

The generated record keeps the TOGO 1 L water row as 1 g/L Distilled water instead of 1 L/L or 1000 ml/L.

The pH 7.0 source value is absent.

The generated record predates the September repair that added source-backed ingredient notes, the M3215 reference, and FOODON groundings for Yeast extract and Beef extract while retaining source-spelled `Trypton` as an opaque complex component.

## Findings

The TOGO liter unit for Distilled water was converted to `G_PER_L`.

The TOGO pH 7.0 field is missing from the generated `MediaRecipe`.

The generated record needs to be refreshed from the repaired normalized M3215 source.

## Recommended Edits

Regenerate the merge layer from `data/normalized_yaml/bacterial/seed_medium.yaml` so the repaired 1.0 L Distilled water unit, ingredient source notes, FOODON groundings, and M3215 reference are carried forward.

Add the TOGO M3215 pH 7.0 value to the normalized source if the generator still omits it.

Keep the TOGO source spelling `Trypton` as an opaque complex component unless a primary source resolves it unambiguously to tryptone.

## Follow-up Checks

Confirm the regenerated record no longer represents Distilled water as 1 g/L.

Confirm pH 7.0 is present or an explicit curation note explains why the TOGO pH was not carried forward.

Confirm the regenerated M3215 and M3233 `Seed medium` records remain separate because their formulations differ.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
