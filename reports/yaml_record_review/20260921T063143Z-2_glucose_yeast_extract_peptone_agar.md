# YAML Record Review: 2_glucose_yeast_extract_peptone_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar.yaml`
- Started UTC: 2026-09-21T06:30:32Z
- Finished UTC: 2026-09-21T06:31:43Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009517` |
| Name | `2_glucose_yeast_extract_peptone_agar` |
| Original name | `2% Glucose-Yeast Extract-Peptone Agar` |
| Category | `bacterial` |
| Source term | `TOGO:M29`, `2% Glucose-Yeast Extract-Peptone Agar` |
| Source URL | `https://togomedium.org/medium/M29`; notes also name `JCM_M37` and `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=37` |
| Merge fingerprint | `6042452653d9d937cc0d96b7445a9618e9ef6c73f269e508bc8c0633a017e7f7` |
| Maintained owner | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated bacterial TOGO M29 import, which its own notes describe as sourced from JCM Medium 37. The same filename stem also exists under `data/normalized_yaml/fungal/`; the stable ID, source term, category, and merge fingerprint resolve this generated target to `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml`. Future corrections belong in that normalized bacterial TOGO file; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar.yaml --out /private/tmp/2_glucose_yeast_extract_peptone_agar.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_glucose_yeast_extract_peptone_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M29`, the label is `2% Glucose-Yeast Extract-Peptone Agar`, the live TOGO API payload identifies M29 with the same label, and its metadata names `JCM_M37` plus the JCM GRMD 37 URL. The inspected JCM page resolves GRMD 37 to `2% GLUCOSE-YEAST EXTRACT-PEPTONE AGAR`.

Ingredient identity is mostly sound. Peptone, yeast extract, glucose, K2HPO4, MgSO4-7H2O, FeSO4-7H2O, CaCO3, agar, and distilled water all appear on the JCM page and in the TOGO API payload. The CHEBI groundings for water, magnesium sulfate heptahydrate, dipotassium hydrogen phosphate, iron(2+) sulfate heptahydrate, calcium carbonate, glucose, and agar are chemically compatible with their labels, while peptone and yeast extract are correctly left as unresolved complex/undefined materials.

Fetching `https://togomedium.org/medium/M29` returned the TOGO single-page application shell without a static M29 recipe table, so the inspected TOGO source was the live REST API route used by the repository fetcher. The inspected original formulation was the JCM GRMD 37 page named in the TOGO metadata.

## Evidence

The inspected JCM Medium 37 page and live TOGO M29 API payload both support this formulation:

- 5 g peptone.
- 1 g yeast extract.
- 20 g glucose.
- 0.5 g K2HPO4.
- 0.5 g MgSO4-7H2O.
- 50 mg FeSO4-7H2O.
- 1 g CaCO3.
- 15 g agar.
- 1 L distilled water.
- Unadjusted pH 6.8.

The generated target preserves the ingredient list and all gram-scale dry-component amounts. It misimports the 50 mg FeSO4-7H2O row as `50 G_PER_L` instead of 0.05 g/L and stores 1 L distilled water as `1 G_PER_L`. It also omits the unadjusted pH 6.8 comment carried by both the JCM page and the TOGO API payload.

## Completeness

Consequential gaps:

- FeSO4-7H2O is 1,000 times too concentrated.
- The distilled-water amount has the wrong dimension.
- The unadjusted pH 6.8 instruction is absent.
- The TOGO M29 import is a source duplicate of `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml`, the MediaDive/JCM J37 import, but neither record has a reciprocal `SOURCE_DUPLICATE` link.
- The record has no structured `references`, so neither the TOGO M29 nor JCM 37 URL is validator-visible.

Empty optional slots:

- `solutions` is correctly absent. The inspected JCM recipe is a flat one-part formulation.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM and TOGO sources support a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_glucose_yeast_extract_peptone_agar.md` report before this report was created.
- A gitignore-independent `find` over `data/normalized_yaml` found two normalized files with this basename: `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` and `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, and `data/import_tracking` for `CultureMech:009517`, `CultureMech:010522`, exact TOGO M29/JCM 37 source tokens, the merge fingerprint, and both title capitalizations found the expected bacterial TOGO M29 and fungal MediaDive/JCM J37 normalized/generated records plus derived indexes, manifests, plausibility reports, and archive report rows. Ignored files were included under the searched paths. Short `mediadive.medium:J37` searches also match unrelated longer JCM 370-379 identifiers, so those longer IDs were ignored.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | FeSO4-7H2O has the wrong unit. | JCM Medium 37 and live TOGO M29 both list 50 mg FeSO4-7H2O; the generated and normalized TOGO M29 records store `FeSO4 x 7H2O` as 50 `G_PER_L`, which inflates the final amount by a factor of 1,000. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |
| Major | Distilled water is represented with the wrong dimension. | JCM Medium 37 and live TOGO M29 list 1 L distilled water. The generated and normalized TOGO M29 records store `Distilled water` as `1 G_PER_L`, turning a final solvent volume into a 1 g/L mass concentration. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |
| Major | The pH comment is missing. | JCM Medium 37 states that unadjusted pH will be 6.8, and the live TOGO M29 payload carries both `ph: "6.8"` and the same comment. The generated and normalized TOGO M29 records have no pH value or preparation note. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |
| Major | TOGO M29 duplicates the MediaDive/JCM J37 owner but is not linked to it. | TOGO M29 names `JCM_M37` and GRMD 37 as its original source; `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` already represents JCM Medium J37. Neither record has a reciprocal `SOURCE_DUPLICATE` link. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` and `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO and JCM URLs only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` |

## Recommended Edits

1. Correct `FeSO4 x 7H2O` in `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` from `50 G_PER_L` to the 50 mg amount supported by JCM and TOGO.
2. Correct the distilled-water row so it represents the source's 1 L solvent volume rather than `1 G_PER_L`.
3. Add the unadjusted pH 6.8 detail carried by JCM and TOGO.
4. Add structured references for `https://togomedium.org/medium/M29`, `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M29`, and `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=37`.
5. Add reciprocal `SOURCE_DUPLICATE` links between `data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` and `data/normalized_yaml/fungal/2_glucose_yeast_extract_peptone_agar.yaml`.
6. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` after correcting the FeSO4 unit.
- `just validate-references data/normalized_yaml/bacterial/2_glucose_yeast_extract_peptone_agar.yaml` after adding structured TOGO and JCM references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 37 and TOGO M29 for 5 g/L peptone, 1 g/L yeast extract, 20 g/L glucose, 0.5 g/L K2HPO4, 0.5 g/L MgSO4-7H2O, 50 mg/L FeSO4-7H2O, 1 g/L CaCO3, 15 g/L agar, 1 L distilled water, and unadjusted pH 6.8.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
- `data/import_tracking/reports/concentration_plausibility.tsv` already flags this FeSO4 row as stock-solution magnitude.
