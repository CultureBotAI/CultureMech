# YAML Record Review: phb_agar

- Repository: CultureMech
- Record: data/merge_yaml/merged/phb_agar__ec580e59.yaml
- Started UTC: 2026-09-24T21:05:14Z
- Finished UTC: 2026-09-24T21:05:14Z
- Verdict: needs curation

## Target

- MediaRecipe ID: CultureMech:002437
- Name: phb_agar
- Source import: phb_agar
- Primary external ID: mediadive.medium:J1271
- Source URL: `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1271`

This generated record represents JCM Medium 1271, PHB AGAR.

## Validation

- Open LinkML validation: passed with no reported issues.
- Strict validation: passed for 1 file with 0 error rows; `/private/tmp/phb_agar__ec580e59.strict.tsv` is header-only.
- Reference validation: passed for 1 file with 0 reference checks.
- Term validation: passed.
- Embedded curation history: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` entries inside merged YAML.

## Identity and Grounding

The direct MediaDive/JCM identity is correct for JCM_M1271 / GRMD 1271. The live JCM URL returned "Nothing found" on 2026-09-24, but TOGO M1367 still preserves the same original JCM_M1271 identifier and original URL.

Exact ignored-file searches across `data` found a sibling TOGO M1367 generated record at `data/merge_yaml/merged/PHB_AGAR.yaml`. Both generated records describe the same PHB Agar recipe and should not remain as separate CultureMech identities.

The non-water ingredients in the direct record have source-consistent amounts and units:

- Na2HPO4 x 12 H2O 11.6 G_PER_L
- KH2PO4 4.6 G_PER_L
- NH4Cl 1 G_PER_L
- Poly(3-hydroxybutyrate) 2 G_PER_L
- MgSO4 x 7 H2O 0.5 G_PER_L
- CaCl2 x 2 H2O 0.05 G_PER_L
- FeCl3 x 6 H2O 0.06 G_PER_L
- Agar 15 G_PER_L

## Evidence

- TOGO M1367 identifies its original source as JCM_M1271 and lists the same eight non-water PHB Agar ingredients with the same one-liter quantities.
- The generated MediaDive/JCM record already has the same non-water quantities as the TOGO source snapshot.
- The live JCM GRMD 1271 URL now returns a `Nothing found` page rather than the historical recipe.

## Completeness

The target record is chemically complete for the available JCM snapshot and omits only distilled water, which is implicit as the solvent to 1 L. The major completeness issue is corpus-level: the equivalent TOGO import is still a separate merged YAML with a separate CultureMech ID.

## Findings

1. The MediaDive/JCM and TOGO M1367 records are duplicate representations of the same JCM_M1271 PHB Agar source but did not merge.
2. The embedded JCM URL is stale; a curator needs to preserve the TOGO snapshot or another archived copy if the original JCM recipe is no longer public.

## Recommended Edits

- Normalize the MediaDive J1271 and TOGO M1367 sources so they merge into one PHB Agar record.
- Keep the direct record's non-water ingredient amounts; they match the TOGO source snapshot.
- Preserve a source note explaining that the live JCM GRMD 1271 endpoint no longer serves the original medium page.

## Follow-up Checks

- Regenerate merged YAML and verify that PHB Agar has one record for JCM_M1271 / TOGO M1367.
- Re-run open schema, strict, term, and reference validation after regeneration.
- Run exact ignored-file searches for `GRMD=1271`, `mediadive.medium:J1271`, and `TOGO:M1367` with ignored files included to ensure no stale duplicate remains.

## Additional Notes

The ignored-file local searches cited above included generated YAML under both `data/merge_yaml/merged` and `data/normalized_yaml`.
