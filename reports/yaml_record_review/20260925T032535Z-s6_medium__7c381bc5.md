# YAML Record Review: s6_medium__7c381bc5

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s6_medium__7c381bc5.yaml`
- Started UTC: `2026-09-25T03:25:35Z`
- Finished UTC: `2026-09-25T03:25:35Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s6_medium__7c381bc5`, a single-source merge of `data/normalized_yaml/bacterial/TOGO_M81_S6_Medium.yaml`.

The record represents TOGO Medium `M81`, the liquid JCM Medium 90 `S6 MEDIUM` import. It has the same source as the solid TOGO `M82` import but omits the optional 15 g/L agar row.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with no diagnostics.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to TOGO `M81`, which imports the liquid `JCM_M90` form of JCM Medium 90. No false duplicate merge was found; the adjacent TOGO `M82` record is the agar-containing `JCM_M90-2` form and should remain a separate solid-medium variant.

## Evidence

TOGO `M81` and the live JCM 90 page list 1.8 g `KH2PO4`, 1.2 g `Na2HPO4`, 0.1 g `(NH4)2SO4`, 0.1 g `MgSO4 x 7H2O`, 30 mg `FeCl3 x 6H2O`, 30 mg `MnSO4 x xH2O`, 40 mg `CaCl2 x 2H2O`, 100 ml 10% `Na2S2O3` solution, and 900 ml distilled water.

TOGO carries pH 7.0, and JCM says to adjust pH to 7.0, sterilize the `Na2S2O3` solution separately by filtration, and aseptically add the sterile thiosulfate solution to the medium. The JCM page also says to add 15 g/L agar for solid medium; that agar row belongs to the sibling solid `M82` record, not to this liquid `M81` record.

## Completeness

The generated record preserves the direct gram-scale phosphate, ammonium sulfate, and magnesium sulfate amounts.

It loses or corrupts these source details:

- 40 mg `CaCl2 x 2H2O` is imported as `40 G_PER_L`.
- 30 mg `FeCl3 x 6H2O` is imported as `30 G_PER_L`.
- 30 mg `MnSO4 x xH2O` is imported as `30 G_PER_L`.
- The 100 ml 10% `Na2S2O3` solution aliquot is absent.
- 900 ml distilled water is represented as `900 G_PER_L`.
- pH 7.0 and the separate thiosulfate filtration plus aseptic addition steps are absent from structured fields.

## Findings

- `needs curation`: Three milligram rows are off by 1000-fold. The generated record reports `CaCl2 x 2H2O` at `40 G_PER_L`, `FeCl3 x 6H2O` at `30 G_PER_L`, and `MnSO4 x xH2O` at `30 G_PER_L` even though TOGO and JCM list 40 mg, 30 mg, and 30 mg.
- `needs curation`: The 100 ml 10% `Na2S2O3` stock aliquot is lost. The generated `solutions` entry keeps only a 100 g/L stock concentration with an empty composition and no parent volume.
- `needs curation`: The 900 ml water row is recorded as `900 G_PER_L`, converting a volume to a mass concentration.
- `needs curation`: The generated record has no `ph_value`, even though TOGO `M81` carries pH 7.0 and JCM says to adjust pH to 7.0.
- `needs curation`: The generated record has no preparation steps, so it loses separate filtration of the thiosulfate solution and aseptic thiosulfate addition.
- `pass with minor issues`: The liquid TOGO `M81` identity, absence of agar, direct gram-scale salt amounts, and magnesium sulfate heptahydrate grounding are otherwise plausible.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s6_medium__7c381bc5.yaml`; fix the TOGO `M81` normalized source, then rerun the merge.
- Correct `CaCl2 x 2H2O`, `FeCl3 x 6H2O`, and `MnSO4 x xH2O` so the JCM milligram values are not promoted to g/L.
- Model 10% `Na2S2O3` solution as a filtered stock with a 100 ml parent aliquot.
- Keep 900 ml distilled water as a volume row, not `900 G_PER_L`.
- Preserve pH 7.0 and preparation metadata for separate thiosulfate filtration and aseptic thiosulfate addition.
- Keep the solid-agar instruction scoped to the sibling `M82` solid variant rather than adding agar to this liquid `M81` record.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/TOGO_M81_S6_Medium.yaml`.
- Regenerate `data/merge_yaml/merged/s6_medium__7c381bc5.yaml` and verify that `CaCl2 x 2H2O`, `FeCl3 x 6H2O`, and `MnSO4 x xH2O` are no longer 30-40 g/L rows.
- Verify that the regenerated record keeps the 100 ml 10% `Na2S2O3` solution aliquot and pH 7.0.
- Verify that this liquid `M81` artifact remains distinct from the agar-containing TOGO `M82` artifact.
- Run an exact ignored-file-inclusive search for `TOGO:M81`, `TOGO_M81_S6_Medium`, and `TOGO_M82_S6_Medium` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The absence of agar was also not treated as a defect because this is the liquid `JCM_M90` import, not the solid `JCM_M90-2` import.
