# YAML Record Review: ottow_medium__14f3ee6a

- Repository: CultureMech
- Record: data/merge_yaml/merged/ottow_medium__14f3ee6a.yaml
- Started UTC: 2026-09-24T19:35:34Z
- Finished UTC: 2026-09-24T19:35:34Z
- Verdict: needs curation

## Target

Reviewed generated MediaRecipe `CultureMech:009575`, `ottow_medium`, generated from `data/normalized_yaml/bacterial/TOGO_M3060_OTTOW_MEDIUM.yaml` for TOGO M3060 / NBRC Medium 1631.

## Validation

- Open LinkML validation: Passed; exited 0 with `No issues found`.
- Strict validation: Passed; scanned 1 file with 0 ERROR rows. `/private/tmp/ottow_medium__14f3ee6a.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The generated identity matches TOGO M3060, which imports NBRC Medium 1631 / OTTOW MEDIUM. An ignored-inclusive exact search for `TOGO:M3060`, `NBRC_M1631`, `NO=1631`, `ottow_medium`, and `OTTOW MEDIUM` across `data/merge_yaml` and `data/normalized_yaml` found the expected TOGO M3060 source plus DSMZ Medium 467 Ottow sources and a TOGO M2316 DSMZ 467 duplicate. The NBRC 1631 record is not a source duplicate of DSMZ 467 because it uses distilled water instead of tap water and adds optional gellan gum.

## Evidence

TOGO M3060 and the NBRC Medium 1631 source page list 1 L distilled water, 1.0 g glucose, 7.5 g peptone, 5.0 g meat extract, 2.5 g yeast extract, 2.5 g casamino acids, 5 g NaCl, 7 g Gellan Gum if needed, and pH 8.5.

The repaired normalized TOGO M3060 record now encodes `ph_value: 8.5`, keeps Distilled water as `1.0 L`, grounds Peptone, Meat extract, Yeast extract, Casamino acids, NaCl, water, glucose, and gellan gum, preserves the "if needed" solidifier note, adds a DISSOLVE step plus an ADJUST_PH step, and links the record as a `SUPPLEMENTED_VARIANT` of DSMZ Medium 467.

## Completeness

The generated record preserves the NBRC ingredient names and masses but omits pH 8.5, drops all curated preparation steps, drops references and data-quality flags, drops the `SUPPLEMENTED_VARIANT` parent link, and regresses distilled water from a 1 L volume row to an impossible 1 `G_PER_L` mass concentration.

## Findings

1. Distilled water has the wrong unit.

   NBRC 1631 and TOGO M3060 specify Distilled water 1 L. The normalized record stores this as `1.0` with unit `L`, but the generated record emits Distilled water as `1` `G_PER_L`, which is not a source-equivalent solvent amount.

2. pH 8.5 and the pH-adjustment step were lost.

   The TOGO API carries `ph: "8.5"` and NBRC prints pH 8.5. The normalized record has `ph_value: 8.5` plus an `ADJUST_PH` step. The generated record has no pH field and no preparation steps.

3. Variant structure was dropped.

   TOGO M3060 is curated as an NBRC supplemented variant of DSMZ Medium 467 because it uses distilled water and optional 7.0 g/L gellan gum. The generated record does not preserve the `parent_media`, `variant_relationship`, `variant_modifications`, `references`, or `data_quality_flags` fields, leaving it disconnected from the DSMZ 467 Ottow parent generated as `OTTOW_MEDIUM.yaml`.

## Recommended Edits

- Preserve repaired volume units, `ph_value`, `preparation_steps`, references, data-quality flags, and variant relationship fields from `data/normalized_yaml/bacterial/TOGO_M3060_OTTOW_MEDIUM.yaml` when generating merged YAML.
- Keep NBRC 1631 as a distinct supplemented variant of DSMZ Medium 467, not as a DSMZ 467 source duplicate, because the optional gellan gum changes the formulation.
- After regeneration, confirm `Distilled water` remains `1.0 L` or an equivalent 1000 `ML_PER_L`, and that pH 8.5 is present.

## Follow-up Checks

- Re-run an ignored-inclusive exact search for `NO=1631`, `TOGO:M3060`, and `SUPPLEMENTED_VARIANT` across the regenerated Ottow records to confirm the variant link survived generation.
- Re-run open, strict, reference, and term validation on the regenerated YAML.

## Additional Notes

The other generated Ottow files are different DSMZ Medium 467 / TOGO M2316 source paths and should be reviewed separately.
