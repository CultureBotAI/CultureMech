# YAML Record Review: pas

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pas__8de619ba.yaml
- Started UTC: 2026-09-24T19:48:27Z
- Finished UTC: 2026-09-24T19:48:27Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:000385 |
| Name | pas |
| Original name | PAS |
| Source identity | CCAP Medium C72 / PAS |
| Source path | data/normalized_yaml/bacterial/pas.yaml |
| Generated path | data/merge_yaml/merged/pas__8de619ba.yaml |
| Merge fingerprint | 8de619bad2d9f121c8940a2d7fd3396868d4076b5b7631537b4efb5613f837ec |

The target is a generated merged record. Future edits should be made in the
maintained CCAP input and MediaDive stock-solution import logic, not directly
in `data/merge_yaml/merged/pas__8de619ba.yaml`.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pas__8de619ba.yaml` exited 0 and reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/pas__8de619ba.yaml --out /private/tmp/pas__8de619ba.strict.tsv --workers 1 --quiet` exited 0 and wrote only the TSV header. |
| Reference validator | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/pas__8de619ba.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 checks. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/pas__8de619ba.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

## Identity and Grounding

The medium grounding points to `mediadive.medium:C72`, which resolves through
MediaDive to CCAP medium PAS and the CCAP `MR_PAS.pdf` document. The source PDF
identifies PAS as Page's Amoeba Saline Solution.

The individual CHEBI groundings are syntactically valid, but the generated
concentrations are stock concentrations rather than final-medium
concentrations. CCAP defines two 500 ml stocks and then adds 5 ml of each stock
per liter of final PAS:

| Ingredient | Generated value | Expected flattened final value |
| --- | --- | --- |
| NaCl | 24 `G_PER_L` | 0.12 `G_PER_L` |
| MgSO4 x 7 H2O | 0.8 `G_PER_L` | 0.004 `G_PER_L` |
| CaCl2 x 6 H2O | 1.2 `G_PER_L` | 0.006 `G_PER_L` |
| Na2HPO4 | 28.4 `G_PER_L` | 0.142 `G_PER_L` |
| KH2PO4 | 27.2 `G_PER_L` | 0.136 `G_PER_L` |

The record category is `bacterial`, but the inspected CCAP source is a
Culture Collection of Algae and Protozoa recipe for an amoeba saline solution;
the source does not support bacterial categorization.

## Evidence

The CCAP PDF supports the PAS identity, the two stock-solution formulation, the
5 ml-per-liter addition of each stock, the final make-up to 1 liter with
deionised water, and the 15 psi for 15 minutes autoclave step. MediaDive C72
preserves the same stock structure: its main solution contains 5 ml additions
of Stock solution 1 and Stock solution 2 plus deionized water, while the five
chemical rows live inside the two 500 ml stocks.

The generated target is not an exact representation of either source form. It
does not preserve the two stock solutions, does not multiply each stock by the
5 ml/L final-medium dilution factor, and omits the final water.

An ignored-inclusive search for `mediadive.medium:C72`, `Source: CCAP, ID:
C72`, `MR_PAS`, and `original_name: PAS` found two maintained PAS inputs:

- `data/normalized_yaml/bacterial/pas.yaml`, the MediaDive/CCAP C72 projection
  that generated this target.
- `data/normalized_yaml/algae/pas.yaml`, an earlier direct CCAP curation with
  only three malformed stock-level ingredient names and legacy quality flags.

The direct CCAP and MediaDive imports also emit separate generated files,
`data/merge_yaml/merged/PAS.yaml` and
`data/merge_yaml/merged/pas__8de619ba.yaml`, even though both describe CCAP PAS.

## Completeness

The generated target is materially incomplete because the final `Deionized
water`, 1000 ml row is missing and the two stock-solution additions are not
represented.

No `target_organisms` are expected on this provider recipe. That optional slot
is correctly empty.

## Findings

### Major

- `data/normalized_yaml/bacterial/pas.yaml` flattens 500 ml stock-solution
  concentrations as if they were final-medium concentrations. If the recipe is
  flattened, the final PAS concentrations should be 0.005x the represented
  stock concentrations because 5 ml of each stock is added per liter.
- `data/normalized_yaml/bacterial/pas.yaml` and the generated target omit the
  final 1000 ml deionised-water row from the CCAP PAS medium recipe.
- CCAP PAS is split into two generated records, `pas__8de619ba.yaml` from
  MediaDive C72 and `PAS.yaml` from the direct CCAP input, instead of being
  reconciled as one maintained recipe.
- The generated target keeps a `bacterial` category, but the inspected CCAP
  source describes Page's Amoeba Saline Solution from the Culture Collection of
  Algae and Protozoa and does not support a bacterial classification.

## Recommended Edits

- Fix the MediaDive CCAP import or maintained `data/normalized_yaml/bacterial/pas.yaml`
  so PAS preserves its two 500 ml stock recipes or stores correctly diluted
  final-medium concentrations.
- Add the 1000 ml final deionised-water row to the maintained PAS representation.
- Reconcile `data/normalized_yaml/bacterial/pas.yaml` with
  `data/normalized_yaml/algae/pas.yaml` so the two CCAP PAS imports do not emit
  separate generated records for the same recipe.
- Reclassify PAS into a source-supported category or document why it belongs in
  the bacterial collection despite the CCAP amoeba-source identity.

## Follow-up Checks

- Rerun the open schema, strict, reference, and term validators on the
  regenerated PAS record.
- Compare the regenerated PAS ingredient list against MediaDive C72 and the
  CCAP PDF to confirm that stock concentrations have not been used as final
  concentrations.
- Re-run an ignored-inclusive exact search for `mediadive.medium:C72`, `MR_PAS`,
  and `CCAP:PAS` to confirm only one maintained CCAP PAS recipe is active or
  that a deliberate category split is explicitly documented.

## Additional Notes

None found.
