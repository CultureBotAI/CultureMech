# YAML Record Review: r2a_agar_with_2_nacl
- Repository: CultureMech
- Record: data/merge_yaml/merged/r2a_agar_with_2_nacl.yaml
- Started UTC: 2026-09-25T00:17:51Z
- Finished UTC: 2026-09-25T00:18:50Z
- Verdict: needs curation

## Target
Reviewed generated record `data/merge_yaml/merged/r2a_agar_with_2_nacl.yaml`.

Owner record:

- `data/normalized_yaml/bacterial/TOGO_M1235_R2A_Agar_With_2_NaCl.yaml`
- `merge_fingerprint`: `a6f5c285c95384f1cb190524aa4b3add096ac5a0e47d509d8f3365df93ec20a9`
- `merged_from`: `TOGO_M1235_R2A_Agar_With_2_NaCl`

The generated YAML differs from the normalized owner only by generated merge metadata.

## Validation
- Open LinkML validation against `MediaRecipe`: passed; no issues found.
- Strict validation: passed; `/private/tmp/r2a_agar_with_2_nacl.strict.tsv` was header-only with 0 data rows.
- LinkML reference validation: passed; 0 reference checks, all validations passed.
- LinkML term validation with labels: passed.
- Embedded history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding
- Identity is correct for TOGO `M1235` / JCM `1153`, R2A Agar with 2% NaCl.
- Live TOGO M1235 and live JCM 1153 agree that the formula is `R2A agar (BD-Difco)` 18.2 g, NaCl 20 g, and distilled water 1 L.
- The exact ignored-file search for `TOGO:M1235`, `togomedium.org/medium/M1235`, `JCM_M1153`, and `GRMD=1153` found this TOGO owner and generated target, the repaired direct JCM 1153 owner, and the repaired base R2A Agar owner that references JCM 1153 in its variant children.
- A narrower exact ignored-file search for `mediadive.medium:J1153` and `Source: JCM, ID: J1153` found the direct owner and an unrelated generated synonym list in `r_agar_with_5_nacl.yaml`; it did not find a generated direct JCM 1153 record.

## Evidence
- `data/normalized_yaml/bacterial/r2a_agar_with_2_nacl.yaml` has already been repaired as a salinity variant of base R2A Agar with one 20 g/L NaCl ingredient and a 1000 ml/L `R2A Agar` solution link to `CultureMech:002706`.
- `data/normalized_yaml/bacterial/r2a_agar.yaml` now has the reciprocal child link back to `r2a_agar_with_2_nacl`.
- The generated TOGO target is still a self-contained three-row formula with no variant relationship to the base R2A Agar owner.

## Completeness
The JCM 1153 formula is complete. The generated record is stale with respect to the September R2A-family repair and should be deduplicated into the direct JCM salinity-variant owner.

## Findings
1. The same JCM 1153 source is present in two owner records: TOGO M1235 and the repaired direct JCM owner `data/normalized_yaml/bacterial/r2a_agar_with_2_nacl.yaml`.
2. The generated TOGO record lacks the direct owner model that represents this as a `SALINITY_VARIANT` of base R2A Agar.
3. No regenerated direct JCM 1153 output exists, so the generated layer is still using the pre-repair TOGO representation for the `r2a_agar_with_2_nacl` slug.

## Recommended Edits
1. Merge or retire `data/normalized_yaml/bacterial/TOGO_M1235_R2A_Agar_With_2_NaCl.yaml` in favor of the repaired direct JCM owner.
2. Regenerate outputs so `r2a_agar_with_2_nacl` is emitted from the direct owner with its `SALINITY_VARIANT` parent relationship and the base R2A Agar reciprocal child relationship is preserved.
3. Recheck `r_agar_with_5_nacl.yaml` separately; its synonym list still includes `mediadive.medium:J1153` from an unrelated R2A variant, which suggests a broader salt-agar over-merge.

## Follow-up Checks
- Re-run open LinkML, strict, reference, and term validators for the regenerated R2A salinity-variant records.
- Re-run exact ignored-file searches for `TOGO:M1235`, `togomedium.org/medium/M1235`, `JCM_M1153`, `GRMD=1153`, and `mediadive.medium:J1153` to confirm the TOGO duplicate has been intentionally merged or retired.

## Additional Notes
None.
