# YAML Record Review: R AGAR WITH 5% NaCl

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_agar_with_5_nacl.yaml
- Started UTC: 2026-09-25T00:55:00Z
- Finished UTC: 2026-09-25T00:56:49Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/r_agar_with_5_nacl.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:003111` |
| Label | `R AGAR WITH 5% NaCl` |
| Category | `bacterial`; generated synonym metadata also includes `specialized` |
| Source identity | `mediadive.medium:J76` |
| Generated status | Generated merged artifact with `merge_fingerprint: 2a1694ad0eee0fc62ea8d46e0144cf0d6d8c453fe41c87337660ee7005829d62`; owned upstream by `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml` and generated by the merge layer in `src/culturemech/merge/merge_recipes.py`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_agar_with_5_nacl.yaml` exited 0 and reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/r_agar_with_5_nacl.yaml --out /private/tmp/r_agar_with_5_nacl.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed trivially; `linkml-reference-validator validate data data/merge_yaml/merged/r_agar_with_5_nacl.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 checks, and reported all validations passed. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/r_agar_with_5_nacl.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- The top-level ID, label, category, and `mediadive.medium:J76` CURIE all point to the direct MediaDive mirror of JCM Medium 76, `R AGAR WITH 5% NaCl`.
- Live JCM GRMD 76 and live MediaDive J76 agree that JCM Medium 76 is 1 L of R Agar, JCM Medium 26, plus 50 g NaCl.
- Live JCM GRMD 26 supports the referenced R Agar base medium.
- The maintained JCM owner `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml` and the source-equivalent TOGO M67 owner have both already been repaired to encode 50 g/L NaCl plus a 1000 ml/L R Agar base solution.
- The generated record has the right top-level source identity, but its composition, synonym set, `merged_from` list, and multi-category metadata describe a false 17-record merge of unrelated salinity variants.

## Evidence

- Supported:
  - The JCM J76 source identity, `CultureMech:003111`, name, bacterial category, undefined complex composition, and solid agar state match the intended R Agar with 5% NaCl record.
  - `NaCl` is a supported additive and `CHEBI:26710` is the right chemical grounding for that ingredient.
- Unsupported or over-scoped:
  - `Glycerol-asparagine agar` at `1000 G_PER_L` is not part of JCM Medium 76. It is the base recipe from the distinct JCM Medium 410 Glycerol-Asparagine Agar (ISP-5) with 10% NaCl owner.
  - The NaCl amount is 100 g/L in the generated record, but live JCM GRMD 76, MediaDive J76, and the repaired maintained owner all require 50 g/L.
  - The synonym and `merged_from` lists combine ISP-5, ISP-4, R2A Agar, R Agar, Brewer Anaerobic Agar, and Marine Agar 2216 variants with 1%, 2%, 3%, 3.1%, 5%, 8%, 8.0%, 10%, and 15% NaCl. Those are not duplicate JCM J76 recipes.
  - The generated `mediaingredientmech_chebi_term` for `Glycerol-asparagine agar` points to `CHEBI:2509` `agar`; the field is attached to a base medium, not to agar as a chemical ingredient.

## Completeness

- The generated record omits the required base solution: 1000 ml/L `R agar` linked to `CultureMech:002627`.
- The generated record is stale relative to `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml`: it lacks the September 2026 repair event, `preparation_steps`, `parent_media`, `variant_relationship`, `variant_modifications`, `data_quality_flags`, and explicit GRMD 76 and GRMD 26 references.
- The `MERGED_RECIPES` curation-history `source` scalar is cut off at `marine_agar_2216_with_3_`, even though the adjacent notes and the top-level `merged_from` list name all 17 merged stems.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `mediadive.medium:J76`, `GRMD=76`, `CultureMech:003111`, `TOGO:M67`, and `r_agar_with_5_nacl.yaml` found the direct JCM owner, the source-equivalent TOGO M67 owner, the generated target, reciprocal R Agar parent links, indexes, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Blocker | `r_agar_with_5_nacl.yaml` is a false merge of 17 distinct salted agar variants. | The generated artifact lists R Agar, R2A Agar, ISP-4, ISP-5, Brewer Anaerobic Agar, and Marine Agar 2216 variants at multiple NaCl percentages under one `CultureMech:003111` JCM J76 identity. | Regenerate `data/merge_yaml/merged/` through `src/culturemech/merge/merge_recipes.py` from the repaired normalized owners, including `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml`. |
| Blocker | The generated JCM J76 formula is wrong. | JCM GRMD 76 and MediaDive J76 say J76 is 1 L R Agar plus 50 g NaCl; the generated record has 1000 g/L Glycerol-asparagine agar plus 100 g/L NaCl. | `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml` already has the right formula; the generated merge layer must be rebuilt. |
| Major | The generated copy is stale relative to its maintained owner. | The generated record ends at August 2026 merge events, while the maintained owner has a September 2026 `RESOLVED_R_AGAR_SALINITY_SCORE20` repair, correct solution modeling, preparation steps, parent-media fields, references, and data-quality flags. | `data/merge_yaml/merged/r_agar_with_5_nacl.yaml` regeneration from normalized inputs. |
| Minor | The generated `MERGED_RECIPES` provenance is truncated. | The `source` scalar stops at `marine_agar_2216_with_3_`; the complete 17-item list appears only in `notes` and `merged_from`. | `src/culturemech/merge/merge_recipes.py` output regeneration. |

## Recommended Edits

1. Rebuild `data/merge_yaml/merged/r_agar_with_5_nacl.yaml` from `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml` so it contains NaCl at 50 g/L and a 1000 ml/L R Agar solution.
2. Ensure the previously merged J410, J351, J863, J1156, J912, J1153, J1280, J877, J113, J667, J733, J922, J357, J732, J1015, and J532 records no longer share fingerprint `2a1694ad0eee0fc62ea8d46e0144cf0d6d8c453fe41c87337660ee7005829d62`.
3. Preserve the direct JCM GRMD 76 and GRMD 26 references, `parent_media`, and salinity-variant fields already present in `data/normalized_yaml/bacterial/r_agar_with_5_nacl.yaml`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/r_agar_with_5_nacl.yaml`.
- Re-run a gitignore-independent exact search for `mediadive.medium:J76`, `GRMD=76`, `TOGO:M67`, and `CultureMech:003111` to confirm the direct JCM J76 and TOGO M67 records remain auditable and are no longer absorbed into unrelated salt-agar variants.
- Compare the regenerated direct JCM J76 artifact against live JCM GRMD 76 and MediaDive J76 to confirm it encodes 50 g/L NaCl, not the 100 g/L amount from JCM J410.

## Additional Notes

- The current MediaDive J76 REST payload agrees with the live JCM GRMD 76 page. Unlike the stale J302 mirror reviewed immediately before this target, the wrong J76 generated composition is not inherited from a current MediaDive source conflict.
- This review only covers the direct `mediadive.medium:J76` generated artifact. The exact search also found the source-equivalent TOGO M67 maintained owner and TOGO-derived generated content that should be reviewed in their own turns.
