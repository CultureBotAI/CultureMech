# YAML Record Review: pys_medium
- Repository: CultureMech
- Record: data/merge_yaml/merged/pys_medium__51cd64ac.yaml
- Started UTC: 2026-09-25T00:04:36Z
- Finished UTC: 2026-09-25T00:04:36Z
- Verdict: needs curation

## Target
Reviewed generated record `data/merge_yaml/merged/pys_medium__51cd64ac.yaml`.

Owner record:

- `data/normalized_yaml/bacterial/TOGO_M1568_PYS_Medium.yaml`
- `merge_fingerprint`: `51cd64ac943a53fb048d9227abdcc01c2e5169f69ce6ada0f1f3612ab297c852`
- `merged_from`: `TOGO_M1568_PYS_Medium`

The generated YAML is stale relative to the normalized owner: the owner has already collapsed duplicate water from 2.0 to 1.0, while the generated file still shows `Distilled water` as `2.0 G_PER_L`.

## Validation
- Open LinkML validation against `MediaRecipe`: passed; no issues found.
- Strict validation: passed; `/private/tmp/pys_medium__51cd64ac.strict.tsv` was header-only with 0 data rows.
- LinkML reference validation: passed; 0 reference checks, all validations passed.
- LinkML term validation with labels: passed.
- Embedded history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding
- Identity is correct for TOGO `M1568` / NBRC `370`, PYS Medium.
- The main NBRC formula contains `KH2PO4` 1 g, `(NH4)2SO4` 1 g, `NaCl` 0.2 g, `MgCl2 x 6H2O` 0.2 g, `CaCl2 x 2H2O` 0.05 g, `Trace element solution SL8` 1 ml, `Sodium pyruvate` 2.2 g, `Yeast extract` 1 g, and distilled water to 1 L, at pH 6.8.
- The exact ignored-file search for `TOGO:M1568`, `togomedium.org/medium/M1568`, `NBRC_M370`, and `NBRCMediumDetailServlet?NO=370` found only the TOGO owner, this generated target, and source indexes. No duplicate NBRC 370 recipe was found.
- The exact ignored-file search for `mediadive.solution:3851` and `mediadive_3851_Trace_element_solution_SL8` found the reusable SL8 solution record plus this PYS owner, this generated target, and the related TOGO M1569 MMYS-II Medium record.

## Evidence
- Live TOGO M1568 and the live NBRC 370 page agree on the PYS formula, pH 6.8, the 1 ml/L `Trace element solution SL8` addition, and the instruction to separately sterilize sodium pyruvate by filtration before aseptic addition.
- The NBRC page provides the SL8 stock formula beneath the main recipe: `2Na-EDTA` 5.2 g/L, `FeCl2 x 4H2O` 1.5 g/L, `ZnCl2` 70 mg/L, `MnCl2 x 4H2O` 100 mg/L, `H3BO3` 62 mg/L, `CoCl2 x 6H2O` 190 mg/L, `Na2MoO4 x 2H2O` 36 mg/L, `CuCl2 x 2H2O` 17 mg/L, `NiCl2 x 6H2O` 24 mg/L, and distilled water to 1 L.
- `data/normalized_yaml/bacterial/mediadive_3851_Trace_element_solution_SL8.yaml` already has the same stock at liter-normalized concentrations for use as a nested stock solution.
- The current generated YAML carries SL8 stock salts as top-level final ingredients, with NBRC mg values such as `70 mg`, `100 mg`, and `190 mg` converted to `70`, `100`, and `190 G_PER_L`.

## Completeness
The imported ingredient set is not complete as a final PYS formulation. Sodium pyruvate is represented only as an empty solution placeholder, the SL8 stock has been split between an empty solution placeholder and flattened stock rows, pH 6.8 is absent, and the filter-sterilization instruction for sodium pyruvate is absent.

## Findings
1. The generated file is stale: `Distilled water` is `2.0 G_PER_L` even though `data/normalized_yaml/bacterial/TOGO_M1568_PYS_Medium.yaml` already repaired the summed duplicate to `1.0 G_PER_L`.
2. `ph_value` is missing even though TOGO M1568 and NBRC 370 both state pH 6.8.
3. `Sodium pyruvate**` was migrated into `solutions` with empty `composition`; it should be a 2.2 g/L final ingredient, with the double-asterisk footnote modeled as a filter-sterilization preparation step.
4. `Trace element solution SL8*` is represented as an empty 1 `G_PER_L` solution while its internal stock rows are flattened as top-level final ingredients. Those rows preserve stock amounts but not stock semantics, inflating `ZnCl2`, `MnCl2 x 4H2O`, `CoCl2 x 6H2O`, `CuCl2 x 2H2O`, `NiCl2 x 6H2O`, and `Na2MoO4 x 2H2O` by 1000x if read as grams per liter of final PYS.
5. Hydrated trace salts need grounding review after they are moved under SL8: `CoCl2 x 6H2O` is grounded to anhydrous cobalt dichloride, and `NiCl2 x 6H2O` is grounded to nickel dichloride.

## Recommended Edits
1. Regenerate the merged YAML so the generated `Distilled water` row reflects the owner-side duplicate repair.
2. Edit `data/normalized_yaml/bacterial/TOGO_M1568_PYS_Medium.yaml` to add `ph_value: 6.8`.
3. Move `Sodium pyruvate**` back to final `ingredients` as `Sodium pyruvate` at 2.2 g/L and add a preparation step for separate filter sterilization and aseptic addition.
4. Remove the flattened SL8 stock salts from top-level `ingredients`; keep a single `Trace element solution SL8` addition at 1 ml/L and nest or link the composition from `data/normalized_yaml/bacterial/mediadive_3851_Trace_element_solution_SL8.yaml`.
5. Recheck CHEBI grounding for hydrated cobalt and nickel chloride while repairing the SL8 stock.

## Follow-up Checks
- Re-run open LinkML, strict, reference, and term validators for the edited TOGO owner.
- Regenerate `data/merge_yaml/merged/pys_medium__51cd64ac.yaml` and confirm it no longer has a top-level `high_metal: true` symptom from flattened SL8 stock salts.
- Re-run exact ignored-file searches for `TOGO:M1568`, `togomedium.org/medium/M1568`, `NBRC_M370`, `NBRCMediumDetailServlet?NO=370`, `mediadive.solution:3851`, and `mediadive_3851_Trace_element_solution_SL8` to confirm only the intended owner and stock linkage remain.

## Additional Notes
None.
