# YAML Record Review: md1_medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/md1_medium.yaml
- Started UTC: 2026-09-24T00:59:51Z
- Finished UTC: 2026-09-24T01:00:50Z
- Verdict: needs curation

## Target

Reviewed generated merged record `data/merge_yaml/merged/md1_medium.yaml`, a duplicate merge of KOMODO/ModelSEED medium 1118 and DSMZ/MediaDive medium 1118. Exact `find` over `data/normalized_yaml` with ignored files included located the two expected normalized source files: `data/normalized_yaml/bacterial/KOMODO_1118_MD1-MEDIUM.yaml` and `data/normalized_yaml/bacterial/md1_medium.yaml`.

Exact source-ID searches for `komodo.medium:1118` and `mediadive.medium:1118` with ignored files included found only the generated record, the two source owners, and their bacterial/recipe index entries.

## Validation

- Open LinkML validation against `src/culturemech/schema/culturemech.yaml` as `MediaRecipe`: passed with `No issues found`.
- Strict validation with `scripts/validate_strict.py`: passed with 0 error rows; the strict TSV had a header only.
- LinkML reference validation: passed; 0 reference checks were emitted for this file.
- LinkML term validation with labels and `conf/oak_config.yaml`: passed.
- Embedded `curation_history`: Not checked. The repository `just validate-history` target validates standalone `history/` files rather than embedded generated-record history blocks.

## Identity and Grounding

The merged record carries `media_term.term.id: komodo.medium:1118`, notes that identify DSMZ/MediaDive medium 1118, and a `parent_media` relationship to the DSMZ owner `CultureMech:000554`. MediaDive REST for medium 1118 identifies `MD1-MEDIUM`, a complex DSMZ medium, final pH 7.0-7.2, main solution 1118, and stock solution 2248, `Trace element solution SL-4`.

The source duplicate relationship is plausible: both normalized owners have the same medium number and the same 13 ingredient names after expanding SL-4.

## Evidence

MediaDive 1118 defines the final MD1 recipe as 3 g Casitone, 0.7 g `CaCl2 x 2 H2O`, 2 g `MgSO4 x 7 H2O`, 1 ml `Trace element solution SL-4`, distilled water to 1000 ml, and 0.5 mg/L vitamin B12 added after autoclaving and cooling. SL-4 is a separate 1000 ml stock containing EDTA, `FeSO4 x 7 H2O`, `ZnSO4 x 7 H2O`, `MnCl2 x 4 H2O`, `H3BO3`, `CoCl2 x 6 H2O`, `CuCl2 x 2 H2O`, `NiCl2 x 6 H2O`, and `Na2MoO4 x 2 H2O`.

The target flattens the nine SL-4 stock ingredients as if 1 L of stock, not 1 ml of stock, were present in 1 L of final medium. For example, `EDTA` is recorded as 0.5 g/L instead of 0.0005 g/L, `FeSO4 x 7 H2O` as 0.2 g/L instead of 0.0002 g/L, and `Na2MoO4 x 2 H2O` as 0.003 g/L instead of 0.000003 g/L.

The generated record also omits the DSMZ owner preparation step: after autoclaving and cooling, add 0.5 mg/L vitamin B12 from a filter-sterilized stock solution and leave the pH at 7.0-7.2. An exact search for `preparation_steps` and the vitamin B12 instruction across the generated record plus both normalized source files included ignored files and found the step only in `data/normalized_yaml/bacterial/md1_medium.yaml`.

## Completeness

No MediaDive solute is fully absent after SL-4 expansion, and the final pH was preserved. However, all nine SL-4 rows need 1:1000 scaling before the flattened record is quantitatively faithful to DSMZ 1118.

`NiCl2 x 6 H2O` is grounded to `CHEBI:34887`/`nickel dichloride`, which loses the hexahydrate form specified by DSMZ. `Casitone` remains ungrounded.

## Findings

1. `needs curation` - The nine `Trace element solution SL-4` ingredients are copied at 1000x their final concentrations because the importer did not account for the 1 ml/L stock addition before flattening the stock recipe into MD1.
2. `needs curation` - The generated merge drops the DSMZ preparation step for adding filter-sterilized vitamin B12 after autoclaving and cooling, even though the DSMZ normalized owner retained it.
3. `minor` - `NiCl2 x 6 H2O` is mapped to an anhydrous nickel dichloride CHEBI term, and `Casitone` lacks ontology grounding.

## Recommended Edits

Apply the stock dilution when flattening `Trace element solution SL-4`: each SL-4 component should be multiplied by 0.001 before being stored as a final MD1 g/L concentration. Regenerate the KOMODO/DSMZ duplicate merge so it preserves the DSMZ preparation step or otherwise records that 0.5 mg/L vitamin B12 is added from a filter-sterilized stock after autoclaving and cooling.

Review `NiCl2 x 6 H2O` for a hydrate-specific CHEBI term and ground `Casitone` if a stable term is available.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validators on the regenerated merged record.
- Confirm the flattened recipe still has 13 non-water ingredient rows but the nine SL-4 rows have been scaled by 0.001.
- Search the regenerated record for the vitamin B12 preparation instruction and final pH evidence.
- Re-check `komodo.medium:1118` and `mediadive.medium:1118` in the normalized source indexes with ignored files included.

## Additional Notes

The MediaDive REST response for medium 1118 was sufficient to verify the formula, the SL-4 stock volume, and the preparation instruction; the rendered MediaDive page agreed on the MD1 identity and final pH.
