# YAML Record Review: 15_mh_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/15_mh_medium.yaml`
- Started UTC: 20260921T044543Z
- Finished UTC: 20260921T044824Z
- Verdict: pass with minor issues

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:001708` |
| Label | `15_mh_medium` |
| Original label | `15% MH MEDIUM` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/15_mh_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_582_15_MH_medium.yaml`, their MediaDive/KOMODO import normalization, or merge regeneration |
| Merge owner | `merged_from: [15_mh_medium, KOMODO_582_15_MH_medium]` |
| Maintained owners | `data/normalized_yaml/bacterial/15_mh_medium.yaml`; `data/normalized_yaml/bacterial/KOMODO_582_15_MH_medium.yaml` |

This generated record is the Aug 6 merge of the direct MediaDive/DSMZ Medium 582 import and a KOMODO record that states it copied DSMZ Medium 582.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/15_mh_medium.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/15_mh_medium.yaml --out /private/tmp/15_mh_medium.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/15_mh_medium.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/15_mh_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/15_mh_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated canonical record correctly denotes DSMZ Medium 582 / `15% MH MEDIUM`, not the JCM/TOGO solid record with the same display name.

- DSMZ Medium 582 is `15% MH MEDIUM`, pH 7.5, with NaCl, MgCl2, MgSO4, CaCl2, KCl, NaHCO3, NaBr, proteose peptone no. 3, yeast extract, glucose, and 1000 ml distilled water. DSMZ instructs adding NaHCO3 from a sterilized stock after cooling, and says agar can be added at 20 g/L for solid media.
- `data/normalized_yaml/bacterial/KOMODO_582_15_MH_medium.yaml` states `DSMZ Medium: 582` and has the same non-solvent ingredient signature as the direct DSMZ import, so its `SOURCE_DUPLICATE` relationship to `data/normalized_yaml/bacterial/15_mh_medium.yaml` is source-plausible.
- `data/merge_yaml/merged/15_mh_medium__b586cb85.yaml` is JCM Medium 408 and `data/merge_yaml/merged/15_mh_medium__cef1d1f5.yaml` is TOGO Medium M406, imported from the JCM 408 page. JCM 408 is a solid formulation with 20 g agar, hydrate-specific magnesium and calcium salts, distilled water, and no NaBr row, so those records are related near-misses but should not be merged with the DSMZ liquid canonical record without an explicit variant relationship.

## Evidence

Supported by inspected source text:

- The generated record's `mediadive.medium:582` identity, `15% MH MEDIUM` label, liquid pH 7.5, NaBr row, and ten non-solvent ingredients agree with the DSMZ Medium 582 PDF.
- The optional solid-medium agar instruction is properly retained as prose instead of being added as a top-level ingredient to this `physical_state: LIQUID` recipe.
- The NaHCO3 cooling instruction is retained from DSMZ.

Unsupported or lossy in the generated record:

- DSMZ's two Difco supplier qualifiers are not represented in the `Proteose peptone no. 3` and `Yeast extract` rows.
- The source line giving the final pH is typed as a generic `MIX` preparation step instead of an `ADJUST_PH` or pH-specific step.
- The DSMZ PDF URL is present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The record is complete enough for the DSMZ liquid recipe: leaving agar out of the ingredient list is consistent with DSMZ scoping agar to solid media only.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the DSMZ PDF does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:001708`, `CultureMech:002764`, `CultureMech:009790`, the DSMZ/JCM/TOGO source accessions, the three 15% MH source slugs, and the three merge fingerprints found the expected DSMZ+KOMODO merged canonical and the two separate JCM/TOGO same-name generated records.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Minor | DSMZ supplier qualifiers are dropped from the two undefined complex ingredients. | The DSMZ PDF qualifies proteose peptone no. 3 and yeast extract as Difco materials; the generated record stores both as bare names with no note preserving that provider detail. | `data/normalized_yaml/bacterial/15_mh_medium.yaml` and `data/normalized_yaml/bacterial/KOMODO_582_15_MH_medium.yaml`, or their source import normalization. |
| Minor | The final-pH assertion is mis-typed as `MIX`. | DSMZ gives the final pH as a pH target, while the generated preparation step says `action: MIX` with `description: Final pH 7.5`. | `data/normalized_yaml/bacterial/15_mh_medium.yaml` or the MediaDive preparation-step importer. |
| Minor | Structured references are absent. | The DSMZ PDF URL is present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | Both normalized 15% MH owners or their MediaDive/KOMODO import mapping. |

## Recommended Edits

1. Preserve the `(Difco)` qualifiers for `Proteose peptone no. 3` and `Yeast extract` in the two normalized DSMZ-derived owners, either in their preferred ingredient labels or in structured ingredient notes.
2. Recode the direct DSMZ import's `Final pH 7.5` preparation step from `MIX` to a pH-specific action.
3. Add a structured reference for DSMZ Medium 582 instead of relying on the raw PDF URL embedded in `notes`.
4. Regenerate `data/merge_yaml/merged/` and confirm the DSMZ and KOMODO copies still merge, while JCM 408 and TOGO M406 remain separate same-name records.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/15_mh_medium.yaml data/normalized_yaml/bacterial/KOMODO_582_15_MH_medium.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/15_mh_medium.yaml` after adding a structured DSMZ reference.
- `just verify-merges` to prove the two DSMZ-derived normalized records still merge after regeneration.
- `just validate-strict` and `just validate-terms` on the regenerated `15_mh_medium.yaml` merge artifact.
- Manual comparison to the DSMZ Medium 582 PDF for retained pH 7.5, NaBr, cooling, and optional-agar details.

## Additional Notes

- The direct TOGO Medium M406 fetch returned the TogoMedium JavaScript shell rather than embedded recipe rows; the local TOGO record points back to JCM 408, and that JCM source page resolved and confirmed that the TOGO/JCM solid formulation differs from DSMZ Medium 582.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
