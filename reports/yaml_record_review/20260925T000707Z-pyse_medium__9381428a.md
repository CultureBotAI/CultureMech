# YAML Record Review: pyse_medium
- Repository: CultureMech
- Record: data/merge_yaml/merged/pyse_medium__9381428a.yaml
- Started UTC: 2026-09-25T00:07:07Z
- Finished UTC: 2026-09-25T00:07:46Z
- Verdict: needs curation

## Target
Reviewed generated record `data/merge_yaml/merged/pyse_medium__9381428a.yaml`.

Owner record:

- `data/normalized_yaml/bacterial/TOGO_M227_Pyse_Medium.yaml`
- `merge_fingerprint`: `9381428a95c196b90e872459388228556b842cab91b25b1e7ccc97c76664fc91`
- `merged_from`: `TOGO_M227_Pyse_Medium`

The generated YAML differs from the normalized owner only by generated merge metadata.

## Validation
- Open LinkML validation against `MediaRecipe`: passed; no issues found.
- Strict validation: passed; `/private/tmp/pyse_medium__9381428a.strict.tsv` was header-only with 0 data rows.
- LinkML reference validation: passed; 0 reference checks, all validations passed.
- LinkML term validation with labels: passed.
- Embedded history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding
- Identity is correct for TOGO `M227` / JCM `235`, PYSE Medium.
- Live TOGO M227 and live JCM 235 agree on the ingredient formula: peptone 8 g, yeast extract 3 g, NaCl 15 g, KCl 0.35 g, `MgSO4 x 7H2O` 2.65 g, `CaSO4 x 2H2O` 0.65 g, `MgCl2 x 6H2O` 5.4 g, and distilled water to 1 L.
- The exact ignored-file search for `TOGO:M227`, `togomedium.org/medium/M227`, `JCM_M235`, and `GRMD=235` found this TOGO owner and generated target, plus a direct JCM sibling in `data/normalized_yaml/bacterial/JCM_J235_PYSE_MEDIUM.yaml` and `data/merge_yaml/merged/PYSE_MEDIUM.yaml`.

## Evidence
- Live TOGO M227 records pH 7.5 for this medium.
- Live JCM 235 lists the same eight formula rows and says to adjust pH to 7.5.
- `data/merge_yaml/merged/PYSE_MEDIUM.yaml` is another generated record sourced from JCM 235. It captures `ph_value: 7.5` and an `ADJUST_PH` step, but the direct JCM import is missing the `Distilled water` row from the JCM page.

## Completeness
The source ingredient formula is complete, including distilled water. The record is missing the final pH and pH-adjustment step from the same source page.

## Findings
1. `ph_value` is missing even though TOGO M227 and JCM 235 both record pH 7.5.
2. `preparation_steps` is missing the JCM instruction to adjust pH to 7.5.
3. The same JCM 235 source is split across two generated records: the TOGO owner has water but no pH metadata, while the direct JCM sibling has pH metadata but omits the explicit water row.

## Recommended Edits
1. Edit `data/normalized_yaml/bacterial/TOGO_M227_Pyse_Medium.yaml` to add `ph_value: 7.5` and an `ADJUST_PH` preparation step.
2. Deduplicate the direct JCM 235 sibling after reconciling the water row so one generated PYSE record carries the complete JCM 235 formula and pH metadata.

## Follow-up Checks
- Re-run open LinkML, strict, reference, and term validators for the edited TOGO owner.
- Regenerate `data/merge_yaml/merged/pyse_medium__9381428a.yaml` and confirm it includes pH 7.5 and the pH-adjustment preparation step.
- Re-run exact ignored-file searches for `TOGO:M227`, `togomedium.org/medium/M227`, `JCM_M235`, and `GRMD=235` to confirm the exact-source duplicate has been intentionally merged or retired.

## Additional Notes
None.
