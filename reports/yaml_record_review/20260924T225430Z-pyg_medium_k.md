# YAML Record Review: PYG Medium (K)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_medium_k.yaml
- Started UTC: 2026-09-24T22:54:30Z
- Finished UTC: 2026-09-24T22:55:52Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009528 |
| Name | pyg_medium_k |
| Original name | PYG Medium (K) |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_medium_k.yaml |
| Maintained owner | data/normalized_yaml/bacterial/pyg_medium_k.yaml |
| Merge source | pyg_medium_k |
| Source grounding | TOGO:M3010, JCM_M1338 |

The reviewed file is a generated merge product. The maintained owner is already
repaired; future corrections should be made by regenerating the merged output
from `data/normalized_yaml/bacterial/pyg_medium_k.yaml`, then verifying that
the serializer preserves nested solution compositions and preparation steps.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_medium_k.yaml` | Passed with `No issues found`. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_medium_k.yaml --out /private/tmp/pyg_medium_k.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_medium_k.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_medium_k.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_medium_k.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_medium_k.yaml` is the generated merge for
  CultureMech:009528, TOGO M3010, and JCM_M1338 `PYG Medium (K)`.
- The TOGO M3010 API reports the same label, original medium `JCM_M1338`, pH
  7.0-7.2, and the same JCM GRMD 1338 URL referenced by the maintained owner.
- The live JCM 1338 page confirms the PYG MEDIUM (K) identity, pH 7.0-7.2,
  final medium recipe, N2 autoclave instruction, and post-autoclave hemin and
  menadione solution additions.
- The maintained owner records a 2026-09-12
  `RESOLVED_TOGO_M3010_PYG_MEDIUM_K_SCORE15` curation step that corrected ml
  and mg imports, added pH 7.0-7.2, expanded M695 salt plus M470 hemin and
  menadione stocks, and grounded the stock components.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M3010`, TOGO
  M3010 URLs, `JCM_M1338`, `GRMD=1338`, and `pyg_medium_k` found the reviewed
  generated file, its maintained owner, and a metal-analysis index entry.

## Evidence

- JCM 1338 and TOGO M3010 both define a base containing 5 g Trypticase peptone,
  5 g peptone, 10 g yeast extract, 2 g KH2PO4, 0.5 g L-cysteine.HCl.H2O,
  1 mg resazurin, 40 ml salt solution from M695, and 940 ml distilled water.
- The sources instruct curators to mix and autoclave the basal medium under N2,
  cool, aseptically and anaerobically add 10 ml hemin solution and 10 ml
  menadione solution from M470 per liter, and adjust to pH 7.0-7.2 if needed.
- The generated record still stores the 40 ml salt, 10 ml hemin, and 10 ml
  menadione additions as empty `solutions` rows using `G_PER_L` values.
- The generated record still stores 1 mg resazurin as 1 `G_PER_L`, 940 ml water
  as 940 `G_PER_L`, and N2 as a variable-concentration ingredient.
- The maintained owner already stores resazurin as 1 `MG_PER_L`, water as
  940 `ML_PER_L`, the salt solution as 40 `ML_PER_L` with scoped composition,
  hemin and menadione as 10 `ML_PER_L` stocks with scoped compositions, and pH
  7.0-7.2 with preparation steps.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- The generated artifact is stale relative to its maintained owner.
- The generated salt, hemin, and menadione solution compositions are empty even
  though the maintained owner has complete nested compositions.
- `ph_range` and the source preparation sequence are absent from the generated
  YAML even though they are present in the maintained owner.
- Exact ignored-file search included ignored files before assessing exact
  TOGO/JCM duplicate scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The generated merged record is stale relative to the repaired normalized owner. | `data/normalized_yaml/bacterial/pyg_medium_k.yaml` contains the 2026-09-12 repair that fixed units, nested M695/M470 stocks, pH, and preparation steps, but the generated YAML still contains the pre-repair empty `G_PER_L` solution rows. | merge_recipes.py; generation workflow |
| Major | Repaired stock boundaries are missing from the generated artifact. | The maintained owner has non-empty `Salt solution`, `Hemin solution`, and `Menadione solution` compositions; the generated YAML leaves all three compositions empty. | merge_recipes.py; generation workflow |
| Major | Repaired pH and preparation context are missing from the generated artifact. | The maintained owner stores pH 7.0-7.2 and five preparation steps for N2 autoclaving, cooling, anaerobic hemin/menadione addition, and pH adjustment; the generated YAML has no `ph_range` or `preparation_steps`. | merge_recipes.py; generation workflow |
| Minor | Repaired source units are absent from the generated artifact. | The maintained owner stores resazurin as `MG_PER_L`, 940 ml water as `ML_PER_L`, and 40/10/10 ml stock aliquots as `ML_PER_L`; the generated YAML still uses `G_PER_L` for all of those rows. | merge_recipes.py; generation workflow |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/pyg_medium_k.yaml` from
   `data/normalized_yaml/bacterial/pyg_medium_k.yaml`.
2. Confirm the regenerated YAML contains the maintained owner's non-empty salt,
   hemin, and menadione solution compositions.
3. Confirm the regenerated YAML retains `ph_range`, the five preparation
   steps, the `references` block, and the
   `RESOLVED_TOGO_M3010_PYG_MEDIUM_K_SCORE15` curation-history entry.
4. Confirm regeneration emits the repaired `MG_PER_L` and `ML_PER_L` units
   rather than the stale `G_PER_L` values.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_medium_k.yaml`.
- Manually compare the regenerated `data/merge_yaml/merged/pyg_medium_k.yaml`
  against `data/normalized_yaml/bacterial/pyg_medium_k.yaml` for nested
  solution and preparation-step preservation.
- Run an exact ignored-file search for `TOGO:M3010`, `JCM_M1338`, and
  `GRMD=1338` to verify no stale duplicate remains after regeneration.

## Additional Notes

- The local TOGO M3010 fetch initially failed with a sandbox DNS error. The
  official URL was then fetched successfully with the approved escalated
  `curl -L` rule.
