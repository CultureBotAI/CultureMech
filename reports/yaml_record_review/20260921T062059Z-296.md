# YAML Record Review: 296

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/296.yaml`
- Started UTC: 2026-09-21T06:19:47Z
- Finished UTC: 2026-09-21T06:20:59Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007485` |
| Name | `296` |
| Original name | `296` |
| Category | `bacterial` |
| Source term | Missing from the generated target; `data/normalized_yaml/bacterial/NBRC_296.yaml` now carries `nbrc.medium:294`, `NBRC Medium 294` |
| Source URL | Missing from structured `references`; the maintained owner now cites `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=294` |
| Merge fingerprint | `24c17fc3d5272f26602cf3566871a3f9014543d08e10c8abe2153d3335e65f27` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_296.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC Medium 294 record imported through `NBRC_296.yaml`. The generated artifact is stale: its maintained owner has since been recovered with two structured ingredient rows, final pH 5.0, a pH-adjustment preparation step, source grounding, and a structured NBRC reference.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/296.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/296.yaml --out /private/tmp/296.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/296.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/296.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The intended source identity is NBRC Medium 294:

- The legacy target name is `296`, but its import event says `Media No. 294`, and the maintained owner now grounds the record to `nbrc.medium:294`.
- The inspected live NBRC page for `NO=294` resolves to Medium No. 294 and lists the same Bacto Yeast Extract, distilled water, and pH 5.0 adjustment represented in `data/normalized_yaml/bacterial/NBRC_296.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for `CultureMech:007485`, `NBRC_296`, `NO=294`, `Media No. 294`, the target merge fingerprint, and the concatenated legacy ingredient found the expected normalized owner, generated target, and a separate TOGO M1510 import for the same NBRC URL. The search included ignored files under the searched paths.

Ingredient grounding in the generated target is unusable. The two source components and pH adjustment text are collapsed into one ungrounded `VARIABLE` ingredient, and the second row has an empty ingredient label with `Bacto Yeast Extract (Difco)` incorrectly stored in `concentration.value`.

## Evidence

The inspected NBRC Medium 294 page supports this liquid formulation:

- 5 g Bacto Yeast Extract (Difco).
- 1 L distilled water.
- Adjust pH to 5.0 with 1 N HCl.

Those two components and the pH-adjustment instruction are represented in `data/normalized_yaml/bacterial/NBRC_296.yaml`, but the generated target predates that recovery and still has the malformed pre-recovery rows plus three generic preparation steps.

## Completeness

Consequential gaps:

- NBRC Medium 294's two source components are not present as two structured ingredient rows in the generated target.
- `ph_value: 5.0`, the 1 N HCl adjustment step, `media_term: nbrc.medium:294`, and the structured NBRC reference are absent from the generated target.
- The generated record is not linked to the separate TOGO M1510 import, even though `data/normalized_yaml/bacterial/togo_medium_m1510.yaml` names the same NBRC `NO=294` original URL.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected NBRC source establishes a medium formulation, not a growth outcome.
- Agar and solid-state metadata are correctly absent; the inspected formulation is liquid.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-296.md` report before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated target is stale and still stores the NBRC Medium 294 formulation as a malformed concatenated ingredient plus an empty-label artifact row. | The inspected NBRC page lists Bacto Yeast Extract (Difco), distilled water, and a pH 5.0 adjustment; `data/normalized_yaml/bacterial/NBRC_296.yaml` already carries those as structured fields; `data/merge_yaml/merged/296.yaml` instead has one concatenated `VARIABLE` ingredient and a second ingredient with `preferred_term: ''`. | Generated `data/merge_yaml/merged/296.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_296.yaml` |
| Major | The generated target omits the recovered source identity, pH, and structured NBRC provenance. | `NBRC_296.yaml` now has `media_term: nbrc.medium:294`, `ph_value: 5.0`, a structured 1 N HCl pH-adjustment step, and a structured NBRC reference; the generated target has none of those fields and was last merged on 2026-08-06. | Generated `data/merge_yaml/merged/296.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_296.yaml` |
| Major | TOGO M1510 duplicates NBRC Medium 294 but remains unreconciled and models procedural HCl as a variable ingredient. | `data/normalized_yaml/bacterial/togo_medium_m1510.yaml` points to `NBRC_M294` and the same NBRC `NO=294` URL, but stores `Distilled water` as `1 G_PER_L`, stores HCl as a `VARIABLE` ingredient instead of `ph_value: 5.0` plus an `ADJUST_PH` step, and is not linked to `CultureMech:007485`; the generated layer keeps it as `togo_medium_m1510.yaml`. | `data/normalized_yaml/bacterial/togo_medium_m1510.yaml` plus reciprocal variant links in `data/normalized_yaml/bacterial/NBRC_296.yaml` |
| Major | The generated target carries generic unsupported preparation steps instead of the source pH adjustment. | The NBRC table supports adjusting to pH 5.0 with 1 N HCl; it does not support the generated `Dissolve ingredients in distilled water`, `Adjust pH if needed`, or `Sterilize by autoclaving` steps. | Generated `data/merge_yaml/merged/296.yaml`; regenerate from `data/normalized_yaml/bacterial/NBRC_296.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/296.yaml` from `data/normalized_yaml/bacterial/NBRC_296.yaml` so the generated target carries the recovered two-row composition, final pH, structured pH-adjustment step, `media_term: nbrc.medium:294`, and structured NBRC reference.
2. Curate `data/normalized_yaml/bacterial/togo_medium_m1510.yaml`: correct distilled water from `1 G_PER_L` to a 1 L solvent representation, remove the variable HCl ingredient, add `ph_value: 5.0`, add an `ADJUST_PH` step for 1 N HCl, add structured TOGO/NBRC references, preserve `TOGO:M1510`, and link it to `data/normalized_yaml/bacterial/NBRC_296.yaml` as a `SOURCE_DUPLICATE` in both directions.
3. Regenerate `data/merge_yaml/merged/` after the maintained duplicate relationship is added.

## Follow-up Checks

- `just validate-schema data/merge_yaml/merged/296.yaml` after regeneration.
- `just validate-strict data/merge_yaml/merged/296.yaml` after regeneration.
- `just validate-schema data/normalized_yaml/bacterial/togo_medium_m1510.yaml` after duplicate curation.
- `just validate-terms data/normalized_yaml/bacterial/togo_medium_m1510.yaml` after repairing distilled water and removing the variable HCl ingredient.
- `just validate-references data/normalized_yaml/bacterial/togo_medium_m1510.yaml` after adding structured references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 294 for 5 g/L Bacto Yeast Extract (Difco), 1 L distilled water, and adjustment to pH 5.0 with 1 N HCl.

## Additional Notes

- `linkml-reference-validator` performed zero checks because the stale generated target has no structured `references` entries.
