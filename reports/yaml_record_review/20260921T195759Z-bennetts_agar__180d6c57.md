# YAML Record Review: BENNETT'S AGAR

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bennetts_agar__180d6c57.yaml
- Started UTC: 2026-09-21T19:56:40Z
- Finished UTC: 2026-09-21T19:57:59Z
- Verdict: needs curation

## Target

- Reviewed `data/merge_yaml/merged/bennetts_agar__180d6c57.yaml`.
- Class: `MediaRecipe`.
- ID: `CultureMech:005993`.
- Name: `bennetts_agar`.
- Original name: `BENNETT'S AGAR`.
- Source identity: `komodo.medium:548`, label `BENNETT'S AGAR`; record notes say `Source: KOMODO ModelSEED | ID: 548 | DSMZ Medium: 548 (mediadive.medium:548)`.
- Generated status: derived merge output under `data/merge_yaml/merged/`; the maintained owner is `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml`.
- Merge metadata: one source record, `KOMODO_548_BENNETT_S_AGAR`, on fingerprint `180d6c5798e105fa29e0a239a2d9791239702ee03913f79d6403e0e373d663f1`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/bennetts_agar__180d6c57.yaml` via the no-project Python 3.11 workaround | Passed |
| Closed-schema strict validation, `scripts/validate_strict.py data/merge_yaml/merged/bennetts_agar__180d6c57.yaml --out /private/tmp/bennetts_agar__180d6c57.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `linkml-reference-validator validate data data/merge_yaml/merged/bennetts_agar__180d6c57.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 reference checks were applicable |
| `linkml-term-validator validate-data data/merge_yaml/merged/bennetts_agar__180d6c57.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `MediaRecipe.curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not a focused embedded-history validator for one merged record |

The documented `just` commands were not run directly because the project runtime currently tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'` before target-specific validation.

## Identity and Grounding

- `CultureMech:005993` resolves in `data/culturemech_id_registry.tsv` to `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml`.
- The KOMODO ID, DSMZ Medium 548 note, imported name, five main ingredients, and pH 7.3 all identify base Bennett's Agar.
- This standalone KOMODO Medium 548 record duplicates the DSMZ/JCM Bennett's Agar formulation but remains separated from `data/merge_yaml/merged/bennetts_agar.yaml` because NaOH is modeled as a sixth ingredient.
- Glucose, agar, and NaOH are exactly grounded. Beef extract, N-Z amine, and yeast extract remain explicit complex mixture names with no forced ChEBI grounding.

## Evidence

- The inspected DSMZ Medium 548 PDF supports 1 g/L beef extract, 10 g/L glucose, 2 g/L N-Z amine A, 1 g/L yeast extract, 15 g/L agar, 1000 ml distilled water, and pH 7.3.
- DSMZ Medium 548 says to adjust the pH to 7.3 with NaOH. The current record instead has no `preparation_steps` and stores NaOH as a variable-concentration bulk ingredient with `notes: pH adjustment (extracted from notes)`.
- DSMZ Medium 548 gives a modified Bennett's agar option in which 10 g glycerol replaces glucose and 2 g Bacto casein replaces N-Z amine. This is not represented as a variant, preparation note, or discussion item in the KOMODO owner.
- A gitignore-independent exact search over the maintained owner, generated merge, ID registry, recipe catalog, and KOMODO indexes found the expected `CultureMech:005993` owner and generated record references. It did not find a raw KOMODO or DSMZ source payload committed for this source record.

## Completeness

- The five base non-water ingredients and pH 7.3 are complete and agree with DSMZ Medium 548.
- Consequentially incorrect: NaOH is in the ingredient list even though it is only the pH adjuster.
- Consequentially incomplete: no preparation step states that pH should be adjusted to 7.3 with NaOH.
- Minor gap: the DSMZ modified Bennett's agar option is absent.
- Empty target-organism and growth-evidence slots are acceptable here; DSMZ Medium 548 does not name a strain-specific growth result.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | NaOH is encoded as a variable-concentration ingredient rather than as the reagent used to adjust pH to 7.3. | `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml` |
| Major | The record has pH 7.3 but no preparation step carrying DSMZ's instruction to adjust pH with NaOH. | `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml` |
| Minor | DSMZ's modified Bennett's agar option is dropped instead of being represented as a variant or retained source note. | `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml` |

## Recommended Edits

1. Move NaOH out of `ingredients` into a source-faithful pH adjustment preparation step for pH 7.3.
2. Add a structured preparation step stating that pH is adjusted to 7.3 with NaOH.
3. Decide whether the DSMZ modified Bennett's agar note should become a glycerol/Bacto casein variant or an explicit discussion item.
4. Regenerate `data/merge_yaml/merged/bennetts_agar__180d6c57.yaml` after the normalized owner is corrected.

## Follow-up Checks

- Rerun the open schema validator, `scripts/validate_strict.py`, `linkml-term-validator`, and `linkml-reference-validator` on `data/normalized_yaml/bacterial/KOMODO_548_BENNETT_S_AGAR.yaml` and the regenerated merged record.
- Manually compare the regenerated record against the DSMZ Medium 548 PDF to verify the five base ingredients, pH 7.3, and the NaOH pH-adjustment instruction survive without NaOH as an unsupported bulk ingredient.
- Run `just verify-merges` after regeneration to see whether removing the variable NaOH ingredient lets this KOMODO record converge with the canonical DSMZ/JCM Bennett's Agar merge.

## Additional Notes

- `CultureMech:005993` is distinct from the `CultureMech:005992` KOMODO `548_41755` source reviewed through `data/merge_yaml/merged/bennetts_agar.yaml`; the latter still has a separate pH-range conflict.
- Reports are read-only review artifacts; no normalized recipe, generated merge, page, history entry, or GitHub issue was edited for this review.
