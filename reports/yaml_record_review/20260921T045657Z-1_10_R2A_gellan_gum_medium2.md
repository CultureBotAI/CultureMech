# YAML Record Review: 1_10_r2a_gellan_gum_medium2

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_R2A_gellan_gum_medium2.yaml`
- Started UTC: 20260921T045556Z
- Finished UTC: 20260921T045657Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008724` |
| Label | `1_10_r2a_gellan_gum_medium2` |
| Original label | `1/10 R2A gellan gum medium2` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml`, TOGO/NBRC import normalization, or merge regeneration |
| Merge owner | `merged_from: [1_10_r2a_gellan_gum_medium2]` |
| Maintained owner | `data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` |

This generated singleton is the TOGO Medium M2130 import of NBRC Medium 1462.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_R2A_gellan_gum_medium2.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_R2A_gellan_gum_medium2.yaml --out /private/tmp/1_10_R2A_gellan_gum_medium2.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_R2A_gellan_gum_medium2.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_R2A_gellan_gum_medium2.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_R2A_gellan_gum_medium2.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly denotes TOGO Medium M2130 / NBRC Medium 1462, `1/10 R2A gellan gum medium2`. The NBRC source supports the listed 1/10 R2A components, optional gellan gum, distilled water, and pH 6.5.

The `CaCl2 x 2H2O` value that looks high relative to the other inorganic salts is source-supported: NBRC lists calcium chloride dihydrate at 2 g in the 1 L recipe.

## Evidence

Supported by inspected source text:

- NBRC Medium 1462 supports Bacto Yeast Extract, Bacto Proteose Peptone No. 3, Bacto Casamino Acids, Glucose, Soluble starch, Sodium pyruvate, K2HPO4, MgSO4 x 7H2O, CaCl2 x 2H2O, Gellan gum if needed, and Distilled water.
- The gram amounts for every non-water ingredient match the NBRC 1 L recipe.

Unsupported or incomplete in the generated record:

- NBRC states pH 6.5; the generated and normalized records have no `ph_value`.
- NBRC lists distilled water as 1 L; the generated and normalized records store that row as `1 G_PER_L`.
- The NBRC and TOGO source URLs are present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The optional gellan-gum row is complete enough because the preferred term itself preserves `if needed`.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the NBRC page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:008724`, `TOGO:M2130`, `NBRC_M1462`, the NBRC 1462 URL slug, and the merge fingerprint found only this normalized owner, this generated singleton, and ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The NBRC pH 6.5 is missing. | NBRC Medium 1462 lists pH 6.5, but neither the generated artifact nor the normalized owner has `ph_value`. | `data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` or the TOGO/NBRC import mapping. |
| Major | The distilled-water row has the wrong dimension. | NBRC lists distilled water as `1 L`; the generated and normalized YAML store it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` or the TOGO/NBRC volume import mapping. |
| Minor | Structured references are absent. | The TOGO and NBRC URLs are present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` or the TOGO import mapping. |

## Recommended Edits

1. Add `ph_value: 6.5` from NBRC Medium 1462 to the normalized owner.
2. Correct the distilled-water quantity so NBRC's 1 L volume is not represented as grams per liter.
3. Add structured references for TOGO Medium M2130 and NBRC Medium 1462.
4. Regenerate `data/merge_yaml/merged/` so the generated artifact picks up the normalized corrections.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/1_10_r2a_gellan_gum_medium2.yaml` after adding structured references.
- `just verify-merges` to prove merge regeneration preserves a singleton M2130 canonical.
- `just validate-strict` and `just validate-terms` on the regenerated `1_10_R2A_gellan_gum_medium2.yaml` merge artifact.
- Manual comparison to NBRC Medium 1462 for pH 6.5, the 1 L water row, optional gellan gum, and all 1/10 R2A component quantities.

## Additional Notes

- A direct fetch of TOGO Medium M2130 returned only the TogoMedium JavaScript shell; the local record points to NBRC Medium 1462, and that NBRC source page resolved.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
