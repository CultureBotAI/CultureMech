# YAML Record Review: HNN MEDIUM
- Repository: CultureMech
- Record: data/merge_yaml/merged/hnn_medium__6f840a26.yaml
- Started UTC: 2026-09-23T12:22:16Z
- Finished UTC: 2026-09-23T12:22:57Z
- Verdict: needs curation

## Target

Reviewed the generated direct JCM branch for JCM Medium J1282, `HNN MEDIUM`, at `data/merge_yaml/merged/hnn_medium__6f840a26.yaml`. The maintained source is `data/normalized_yaml/bacterial/hnn_medium.yaml`.

## Validation

- Open LinkML validation: passed for `MediaRecipe`.
- Strict validation: passed with zero error rows in `/private/tmp/hnn_medium__6f840a26.strict.tsv`.
- Reference validation: passed with zero checks.
- Term validation: passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone YAML files under `history/`, not embedded `MediaRecipe.curation_history` entries in generated merge artifacts.

## Identity and Grounding

The record is grounded to `mediadive.medium:J1282`, JCM Medium 1282 `HNN MEDIUM`.

## Evidence

JCM 1282 lists a 1000 ml main medium with 10 ml Trace mineral solution. The Trace mineral solution starts with 1 L Trace minerals from JCM Medium 151 and adds NiCl2 x 6 H2O, Na2SeO3 x 5 H2O, and Na2WO4 x 2 H2O. JCM 151 Trace minerals is itself a 1 L stock whose preparation dissolves nitrilotriacetic acid, adjusts pH to 6.5 with KOH, adds minerals, and adjusts the final pH to 7.0.

The generated record flattens both the JCM 1282 Trace mineral solution and the JCM 151 Trace minerals stock into top-level HNN ingredients at full stock strength. It then sums stock rows with main rows for `NaCl`, `MgSO4 x 7 H2O`, and `CaCl2 x 2 H2O`.

## Completeness

The main anaerobic preparation text from JCM 1282 is present, but the Trace minerals pH workflow is promoted to top-level HNN `preparation_steps` even though it is scoped only to the stock. The 1000 ml main-water row and the 1000 ml Trace minerals stock-water row are missing.

## Findings

- The JCM 1282 Trace mineral solution is added at 10 ml/L, but NiCl2 x 6 H2O, Na2SeO3 x 5 H2O, and Na2WO4 x 2 H2O are present at full stock concentration.
- The JCM 151 Trace minerals stock is nested inside the JCM 1282 Trace mineral solution, but all JCM 151 Trace minerals are present at full stock concentration.
- Main-medium `NaCl`, `MgSO4 x 7 H2O`, and `CaCl2 x 2 H2O` were summed with stock rows across solution boundaries.
- The JCM 151 Trace minerals nitrilotriacetic-acid and KOH pH-adjustment instructions are attached to the final HNN medium.
- Main and stock water rows are missing.

## Recommended Edits

- Restore the JCM 1282 Trace mineral solution and JCM 151 Trace minerals as nested stock recipes in `data/normalized_yaml/bacterial/hnn_medium.yaml`, or convert all stock rows to final HNN concentrations after applying the 10 ml/L addition.
- Prevent duplicate merging across solution boundaries for `NaCl`, `MgSO4 x 7 H2O`, and `CaCl2 x 2 H2O`.
- Scope the nitrilotriacetic-acid/KOH preparation text to the JCM 151 Trace minerals stock.
- Restore the main and stock distilled-water rows.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation after regeneration.
- Re-open JCM 1282 and JCM 151 to confirm regenerated HNN preserves the 10 ml/L Trace mineral solution and the nested Trace minerals stock.
- Confirm regenerated `NaCl`, `MgSO4 x 7 H2O`, and `CaCl2 x 2 H2O` rows are not sums of final-medium and stock concentrations.

## Additional Notes

None.
