# YAML Record Review: r75s_nsw

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r75s_nsw.yaml
- Started UTC: 2026-09-25T00:43:50Z
- Finished UTC: 2026-09-25T00:45:25Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:000119 |
| Name | r75s_nsw |
| Class | MediaRecipe |
| Category | algae |
| Source identity | CCAP R75S_NSW, `R75S:NSW` |
| Maintained owner | data/normalized_yaml/algae/r75s_nsw.yaml |
| Generated path | data/merge_yaml/merged/r75s_nsw.yaml |

The reviewed file is a generated August 2026 merge product. Its maintained algae owner was repaired in September 2026, so the generated YAML must be regenerated after the merge logic can preserve prepared-stock solution references.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r75s_nsw.yaml` exited 0 and printed `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r75s_nsw.yaml --out /private/tmp/r75s_nsw.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r75s_nsw.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r75s_nsw.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

Shape validation does not catch that the only generated ingredient is a parsed verb from the wrong source PDF.

## Identity and Grounding

- CCAP R75S:NSW denotes a 2:1 mixture of prepared R75S and prepared NSW. Its R75S stock is rye grass in 75% seawater; NSW is natural seawater filtered through GF/C filters and autoclaved.
- The maintained `data/normalized_yaml/algae/r75s_nsw.yaml` matches that identity with a 666.7 ml/L R75S solution reference and a 333.3 ml/L NSW solution reference.
- The generated record instead merged `n75s`, `n75s_nsw`, `r75s`, and `r75s_nsw`, then selected one unsupported direct ingredient named `Bring` with a `75 PERCENT_W_V` concentration and `CCAP Medium MR_N75S.pdf` provenance. `Bring` is a verb from the N75S preparation instruction, not a R75S:NSW ingredient.
- The CCAP R75S and N75S PDFs support distinct base media: R75S uses dried milled rye grass, while N75S uses Cerophyll cereal grass leaves. Their NSW blends are related, not interchangeable source duplicates.

## Evidence

Supported source-backed claims:

- CCAP R75S_NSW supports the two-part R75S, one-part NSW mixture and says the two parts are autoclaved separately and mixed aseptically when cool.
- The nested R75S section of the CCAP R75S_NSW PDF lists 750 ml natural filtered seawater, 250 ml deionized water, and 1 g dried milled rye grass per liter.
- The nested N75S section of the CCAP N75S_NSW PDF lists 750 ml natural filtered seawater, 250 ml deionized water, and 1 g Cerophyll cereal grass leaves per liter.

Unsupported or over-scoped claims:

- The generated `ingredients` entry `Bring` at 75% w/v is unsupported by the R75S_NSW, N75S_NSW, R75S, and N75S PDFs. All four use "Bring 75% seawater to the boil" as an instruction.
- The generated `merged_from` claim that `n75s`, `n75s_nsw`, `r75s`, and `r75s_nsw` are duplicates is unsupported; R75S and N75S have different grass substrates, and the `*_nsw` recipes refer to different prepared parents.
- The generated CCAP R75S_NSW record is missing the September 2026 maintained `sources`, `solutions`, `preparation_steps`, `sterilization`, `parent_media`, and `variant_modifications` content.

## Completeness

- The relevant source set was checked from the generated YAML, all four normalized source records named in `merged_from`, the newer generated `r75s_nsw__b8064bbd.yaml` collision record, and the exact CCAP PDFs for R75S_NSW, N75S_NSW, R75S, and N75S.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, `data/raw`, and `reports` for `r75s_nsw`, `R75S`, `R75S NSW`, and `R75S_NSW` found active algae owners, older bacterial CCAP C87/C88 imports, generated outputs, and archived review reports.
- Empty `target_organisms` is not a defect; the inspected CCAP PDFs describe formulations and do not assert a specific organism-growth result for R75S:NSW.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Blocker | The generated record denotes CCAP R75S:NSW but its scientific content comes from a false N75S/R75S merge. | `merged_from` lists `n75s`, `n75s_nsw`, `r75s`, and `r75s_nsw`; the generated lone ingredient is from `CCAP Medium MR_N75S.pdf` even though the record and references identify `CCAP:R75S_NSW`. | `src/culturemech/merge/fingerprint.py`, `src/culturemech/merge/matcher.py`, and regenerated `data/merge_yaml/merged/` |
| Major | The generated formula lost the source's prepared-medium mixture model. | Maintained `data/normalized_yaml/algae/r75s_nsw.yaml` records R75S at 666.7 ml/L plus NSW at 333.3 ml/L, with separate autoclaving; the generated record has no `solutions` and only an invalid direct ingredient. | Regenerated `data/merge_yaml/merged/r75s_nsw.yaml` from the repaired normalized owner |
| Major | Merge fingerprinting cannot safely compare CCAP prepared-medium blends that share a nested NSW composition. | `RecipeFingerprinter._extract_signatures` records nested `solutions[*].composition` ingredients, but it ignores each solution's `preferred_term` and `culturemech_term`; different records that are base-medium plus NSW can therefore collapse to the same sea-water signature. | `src/culturemech/merge/fingerprint.py` |
| Minor | The generated record is stale relative to source-normalization repair events. | The maintained algae owner has September 2026 `repair_algae_score45_blends_soils.py` and `repair_ccap_sources_score10.py` events; the generated record ends with the August 6, 2026 merge event. | Merge regeneration workflow |

## Recommended Edits

1. Include stock or prepared-medium solution identity in merge fingerprints. For this family, `R75S + NSW` and `N75S + NSW` must not have the same signature merely because both contain a natural-seawater nested solution.
2. Regenerate `data/merge_yaml/merged/r75s_nsw.yaml` from the September 2026 normalized algae record after the merge fix.
3. Verify the regenerated record keeps `sources` for `CCAP:R75S_NSW`, solution quantities of 666.7 ml/L R75S and 333.3 ml/L NSW, separate autoclaving and aseptic mixing preparation steps, and the `r75s` parent-media link.
4. Add a merge regression fixture with `n75s_nsw.yaml` and `r75s_nsw.yaml` that asserts they do not merge.

## Follow-up Checks

- Run `just verify-merges` and `just audit-merge-freshness` after regenerating merge outputs.
- Run focused schema, strict, reference, and term validation on regenerated `data/merge_yaml/merged/r75s_nsw.yaml`.
- Inspect `data/merge_yaml/merged/r75s_nsw.yaml` and the rendered page for any remaining `Bring` ingredient or `CCAP Medium MR_N75S.pdf` provenance under the R75S:NSW record.
- Re-run a gitignore-independent exact search for `r75s_nsw` and `n75s_nsw` to confirm there are two separate generated records after the merge change.

## Additional Notes

- The generated bacterial collision record `data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml` has a related but separate stale merge of older CCAP C87 and C88 imports. This report is limited to the algae `CultureMech:000119` target.
- This review did not modify generated YAML or maintained CCAP records.
