# YAML Record Review: jcm_medium_no_623

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_623.yaml
- Started UTC: 2026-09-23T17:14:07Z
- Finished UTC: 2026-09-23T17:15:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/jcm_medium_no_623.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002969` |
| Label | `jcm_medium_no_623` |
| Source term | `mediadive.medium:J623` |
| Upstream source | JCM MEDIUM No. 623, mirrored by MediaDive as JCM `J623` |
| Generated from | `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml` |

This is a generated merge artifact with `merge_fingerprint: 8bb966e0e0d8d1574b5af3818bf1f6d57863941f1d713d4157793410ca8fd016` and `merged_from: [jcm_medium_no_623]`. Future edits belong in the maintained MediaDive/JCM normalized owner, not in `data/merge_yaml/merged/`.

An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, `data/import_tracking`, `history`, and `data/culturemech_recipe_catalog.tsv` found the maintained JCM owner and TOGO M633 as an independently imported sibling for the same original JCM GRMD=623 formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_623.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_623.yaml --out /private/tmp/jcm623.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_623.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 0 reference checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_623.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. The only output before the pass line was the known `eutils` `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not `MediaRecipe.curation_history` embedded in merged YAML. |

The repository-level `just` validators were not used because this checkout's `uv` resolution currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails inside `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused Python 3.11 validators above exercise the relevant schema, strict, reference, and term gates for this one generated `MediaRecipe`.

## Identity and Grounding

The record denotes the intended JCM medium. `CultureMech:002969` is `jcm_medium_no_623`, is grounded to `mediadive.medium:J623`, and links to live JCM GRMD=623.

The generated record is wrong because the recipe's sub-solution boundary is erased. JCM 623 consists of a 500 ml Solution A with yeast extract, tryptone, agar, and water, plus a 500 ml Solution B with NaCl, MgSO4 x 7 H2O, KCl, CaCl2 x 2 H2O, and water; each stock is separately autoclaved before aseptic mixing. MediaDive preserves those as solution 4490 and solution 4491. The generated record flattens the two 500 ml stocks into parent-level ingredients at the stock concentration, so each amount is doubled relative to the final mixed liter.

## Evidence

Supported claims:

- JCM and MediaDive support the J623 identity, complex/agar classification, pH 7.4, Solution A and Solution B source ingredients, separate autoclaving, aseptic mixing, and pH readjustment if necessary.
- TOGO M633 independently supports the same two-solution topology through `original_media_id: JCM_M623`.

Unsupported or incomplete generated claims:

- Yeast extract is `20 G_PER_L` but should be 10 g in the final 1 L mixture.
- Tryptone is `5 G_PER_L` but should be 2.5 g in the final 1 L mixture.
- Agar is `40 G_PER_L` but should be 20 g in the final 1 L mixture.
- NaCl is `240 G_PER_L` but should be 120 g in the final 1 L mixture.
- MgSO4 x 7 H2O is `20 G_PER_L` but should be 10 g in the final 1 L mixture.
- KCl is `10 G_PER_L` but should be 5 g in the final 1 L mixture.
- CaCl2 x 2 H2O is `0.4 G_PER_L` but should be 0.2 g in the final 1 L mixture.
- Both 500 ml distilled-water rows are missing.
- The BD-Difco qualifiers on yeast extract and tryptone were dropped.

## Completeness

The generated preparation text mentions separately autoclaving and mixing Solutions A and B, but the structured formulation no longer has Solution A or Solution B; it has only their doubled stock concentrations. The record is therefore not complete enough to reproduce JCM 623.

Empty optional evidence, target-organism, salinity, atmosphere, and storage fields are acceptable: the inspected JCM, MediaDive, and TOGO records are source recipes and do not provide growth evidence or incubation/storage conditions.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | Solution A and Solution B were flattened at twice the final concentration. | JCM and MediaDive define two 500 ml solutions that are mixed into 1 L. The generated record stores each stock's g/L values directly, doubling every final ingredient amount. | `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml` and the MediaDive import handling for named half-volume solutions. |
| Major | Both solvent rows are missing. | JCM and MediaDive list 500 ml distilled water in Solution A and 500 ml in Solution B. The generated record has no water rows. | `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml` and the MediaDive import handling for water rows. |
| Minor | Two BD-Difco qualifiers were dropped. | JCM identifies yeast extract and tryptone as BD-Difco, and MediaDive carries `attribute: BD-Difco` on both rows; the generated record stores unqualified labels. | `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml` or the MediaDive import mapping for row attributes. |

## Recommended Edits

1. Preserve Solution A and Solution B as 500 ml solution descriptors in `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml`, or convert their component masses to final 1 L concentrations before any flattening.
2. Keep the two 500 ml distilled-water rows inside their proper sub-solutions.
3. Preserve BD-Difco on yeast extract and tryptone.
4. Repair `data/normalized_yaml/bacterial/togo_medium_m633.yaml` separately: it already has solution references, but their `500 ml` additions are stored as `500 G_PER_L`.

## Follow-up Checks

- Rerun the focused schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/jcm_medium_no_623.yaml` after the maintained owner is edited.
- Regenerate `data/merge_yaml/merged/jcm_medium_no_623.yaml` and verify that Solution A and Solution B are represented without doubled concentrations.
- Compare the regenerated record against live JCM GRMD=623, MediaDive `/rest/medium/J623`, and TOGO M633.
- Run the merge freshness and product validators after regeneration.

## Additional Notes

- TOGO M633 shows the same original JCM topology but is stale in a different representation: it has empty Solution A and Solution B stubs instead of populated sub-solutions.
