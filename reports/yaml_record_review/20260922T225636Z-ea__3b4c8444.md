# YAML Record Review: ea

- Repository: CultureMech
- Record: data/merge_yaml/merged/ea__3b4c8444.yaml
- Started UTC: 2026-09-22T22:55:40Z
- Finished UTC: 2026-09-22T22:56:36Z
- Verdict: needs curation

## Target

Generated bacterial CCAP/MediaDive C33 record `CultureMech:000342`, canonicalized as `ea`, with `CCAP Medium C33` / `mediadive.medium:C33` as the medium term and `https://www.ccap.ac.uk/wp-content/uploads/MR_EA.pdf` as the CCAP source URL.

## Validation

- Open LinkML validation against `MediaRecipe`: passed.
- Strict validation against `scripts/validate_strict.py`: passed with 0 error rows.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded `curation_history`: Not checked: the focused history validator targets standalone files under `history/`, not inline `MediaRecipe.curation_history` entries.

## Identity and Grounding

The C33/EA source identity is internally consistent. However, the generated record is categorized as `bacterial`, while the same CCAP EA recipe also exists as the algae-side `CultureMech:000069`; the CCAP source belongs with the algae medium family rather than as a separate bacterial duplicate.

Grounding for KNO3, K2HPO4, NaCl, KCl, MgCl2 x 6 H2O, CaSO4 x 2 H2O, and agar is acceptable in the MediaDive-derived record. The older algae copy uses the damaged label `KNO` and leaves `Bacto agar` ungrounded, so regeneration should preserve the bacterial copy's better groundings while keeping the algae identity.

## Evidence

The CCAP EA PDF lists, per litre, 0.20 g KNO3, 0.02 g K2HPO4, 100 ml Soil Extract SE1, 15 g NaCl, 0.375 g KCl, 2.5 g MgCl2.6H2O, 0.5 g CaSO4.2H2O, and 10 g Bacto agar.

MediaDive C33 has the same measured top-level values and links to solution `6256` for SE1. The generated record keeps the measured salts and agar correctly, but collapses that 100 ml SE1 stock into a default `Soil` ingredient with `VARIABLE` concentration.

## Completeness

The simple mineral and agar composition is mostly complete, but the soil-extract stock is not. The target record loses the 100 ml SE1 dose and does not structurally encode the stock preparation in which air-dried soil is autoclaved with twice its volume of distilled water, decanted, filtered, aliquoted, and autoclaved again.

EA has no medium-level preparation steps in either the CCAP PDF or the MediaDive C33 top-level solution beyond the SE1 stock procedure, so the absence of an EA-specific top-level autoclave instruction is not a defect in this source.

## Findings

- `Soil` `VARIABLE` is a lossy placeholder for the required 100 ml SE1 stock addition.
- The SE1 stock recipe is carried only as free-text preparation steps; its 2:1 distilled-water-to-air-dried-soil extraction relationship is not represented as a stock formula.
- `category: bacterial` and `applications: Microbial cultivation` leave this CCAP algae recipe on a separate generated path from `data/merge_yaml/merged/EA.yaml`.
- The generated bacterial copy has no structured `sources` or `references` entries even though the normalized algae copy now has `CCAP:EA` and the source PDF represented in those slots.

## Recommended Edits

- Model `Soil Extract (SE1)` as a 100 ml ingredient or subsolution in the final EA recipe.
- Add a structured SE1 stock recipe that preserves the air-dried soil plus twice-volume distilled-water extraction.
- Reconcile the MediaDive C33 import with the algae `EA.yaml` generated record so the CCAP recipe has one canonical generated record with the complete groundings from C33 and the structured CCAP provenance from the algae copy.
- Keep the measured top-level concentrations for KNO3, K2HPO4, NaCl, KCl, MgCl2 x 6 H2O, CaSO4 x 2 H2O, and agar unchanged.

## Follow-up Checks

- Re-run open, strict, reference, and term validators after rebuilding the normalized and merged records.
- Compare the rebuilt EA record against `MR_EA.pdf` and MediaDive C33.
- Search with ignored files included for stale `KNO` and ungrounded `Bacto agar` artifacts in generated EA records after the algae/bacterial reconciliation.

## Additional Notes

The generated record has no explicit organism growth data to review.
