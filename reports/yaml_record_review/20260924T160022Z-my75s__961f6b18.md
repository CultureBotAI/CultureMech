# YAML Record Review: my75s__961f6b18

- Repository: CultureMech
- Record: data/merge_yaml/merged/my75s__961f6b18.yaml
- Started UTC: 2026-09-24T15:59:02Z
- Finished UTC: 2026-09-24T16:00:22Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/my75s__961f6b18.yaml`.

The generated record represents the bacterial MediaDive import of CCAP Medium
C60:

- canonical `id`: `CultureMech:000372`
- canonical `media_term`: `mediadive.medium:C60`
- canonical `name`: `my75s`
- canonical `original_name`: `MY75S`
- `medium_type`: `COMPLEX`
- `composition_type`: `UNDEFINED`
- `physical_state`: `SOLID_AGAR`
- `ingredients`: 4
- `merged_from`: `my75s`

## Validation

- Open schema validation: Passed with `No issues found`.
- Strict schema validation: Passed with 0 errors; the TSV contained only the
  header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed.
- Embedded curation history: Not checked; `just validate-history` validates
  standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The CCAP C60 identity is correct, but the generated record is stale. Its only
maintained owner, `data/normalized_yaml/bacterial/my75s.yaml`, was repaired on
2026-09-12 to restore the two liquid fractions, use ml/L for seawater, and
split the preparation into source-faithful heat, dissolve, add-agar, and
autoclave steps. The generated YAML still reflects the older 2026-08-06 merge.

An exact ignored-file-inclusive search for `CultureMech:000372`,
`CultureMech:000095`, `mediadive.medium:C60`, `CCAP:MY75S`, and
`MR_MY75S.pdf` found two active normalized MY75S owners:

- `CultureMech:000372`: `data/normalized_yaml/bacterial/my75s.yaml`
- `CultureMech:000095`: `data/normalized_yaml/algae/my75s.yaml`

Those two records are the same CCAP MY75S recipe imported through different
paths, but they remain unlinked and generate two files:
`my75s__961f6b18.yaml` and `MY75S.yaml`.

The generated `kg_microbe_match: mediadive.medium:12` backlink is not credible:
the current MediaDive 12 record is DSMZ `SOIL EXTRACT MEDIUM`, not CCAP MY75S.

## Evidence

The current MediaDive C60 REST record lists `Natural sea water` at 750 ml,
`Deionized water` at 250 ml, 0.1 g Malt extract, 0.1 g Yeast extract, and
15 g Agar in a 1000 ml main solution, followed by the CCAP preparation text.

The maintained bacterial MY75S owner now matches that structure with
`Natural filtered seawater` at 750 ml/L, `Deionised water` at 250 ml/L,
0.1 g/L Malt extract, 0.1 g/L Yeast extract, and 15 g/L Bacteriological agar.
The maintained algae owner has also been repaired to carry the same five
source ingredients.

The current MediaDive 12 record lists 400 g air-dried garden soil in 1000 ml
tap water with 15 g agar and pH 6.8-7.0. It is unrelated to MY75S.

## Completeness

The generated record is incomplete because it omits the source 250 ml/L
deionised-water fraction. It also types the 750 ml/L natural-seawater fraction
as `750` `G_PER_L`, which makes a liquid volume look like a mass.

The generated preparation step is source-derived but still contains embedded
CRLF line breaks and combines heating, dissolving, agar dispersion, and
autoclaving into one string. The repaired owner has already split those actions
into separate preparation steps.

## Findings

- The generated record is stale relative to the 2026-09-12 repair in
  `data/normalized_yaml/bacterial/my75s.yaml`.
- The 250 ml/L deionised-water component is missing.
- `Natural sea water` is represented as 750 g/L instead of 750 ml/L.
- The unrelated `kg_microbe_match: mediadive.medium:12` backlink points to DSMZ
  Soil Extract Medium.
- The same CCAP MY75S source still exists as an unlinked active algae owner and
  a separate generated `MY75S.yaml`.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/my75s__961f6b18.yaml` from the repaired
  bacterial owner so seawater, deionised water, agar naming, and preparation
  steps carry forward.
- Remove the stale `kg_microbe_match: mediadive.medium:12` backlink.
- Link `data/normalized_yaml/algae/my75s.yaml` to the MediaDive/CCAP C60 owner,
  or otherwise decide which CCAP MY75S owner should be canonical.
- Confirm the generated record's category is intentional after reconciling the
  bacterial MediaDive import with the algae CCAP import.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the
  regenerated YAML.
- Confirm the regenerated record has 750 ml/L seawater and 250 ml/L deionised
  water.
- Confirm no MY75S generated record contains `kg_microbe_match:
  mediadive.medium:12`.
- Confirm an ignored-file-inclusive exact search for `CCAP:MY75S` and
  `mediadive.medium:C60` shows the two CCAP imports linked rather than drifting
  independently.

## Additional Notes

The provider data and the repaired local owners agree on the ingredient list.
This is a stale-generation and duplicate-owner issue, not a source ambiguity.
