# YAML Record Review: MRS broth

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_broth__b44dc935.yaml
- Started UTC: 2026-09-24T14:34:42Z
- Finished UTC: 2026-09-24T14:35:52Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:009068 |
| Name | mrs_broth |
| Original name | MRS broth |
| Category | bacterial |
| Media term | TOGO:M2494 |
| Source | TOGO M2494 |
| Reviewed path | data/merge_yaml/merged/mrs_broth__b44dc935.yaml |
| Maintained source | data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml |
| Generated status | Generated by merge_recipes.py on 2026-08-06; stale relative to the maintained 2026-09-08 repair |

TOGO M2494 is not a decomposed MRS formula. The live TOGO record provides one
opaque `MRS broth (Oxoid)` product row.

## Validation

| Check | Result |
| --- | --- |
| LinkML schema | Passed. `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_broth__b44dc935.yaml` exited 0 and printed `No issues found`. |
| Strict validator | Passed. `scripts/validate_strict.py data/merge_yaml/merged/mrs_broth__b44dc935.yaml --out /private/tmp/mrs_broth__b44dc935.strict.tsv --workers 1 --quiet` reported 0 errors; the TSV had only its header. |
| Reference validator | Passed. `linkml-reference-validator validate data data/merge_yaml/merged/mrs_broth__b44dc935.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` reported 0 checks and 0 failures. |
| Term validator | Passed. `linkml-term-validator validate-data data/merge_yaml/merged/mrs_broth__b44dc935.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0. |
| Embedded history | Not checked: the documented `just validate-history` target validates standalone `history/` records, not `MediaRecipe.curation_history` embedded in merged YAML. |

The generated record passes structural validators, but it predates the
maintained September 2026 repair for this TOGO product row.

## Identity and Grounding

- `media_term.id` is `TOGO:M2494`, `media_term.label` is `MRS broth`, and TOGO
  M2494 is the same record.
- TOGO M2494 has no NBRC `src_url`; the generated record correctly does not
  invent one.
- TOGO M2494 exposes `MRS broth (Oxoid)` as a 1 L opaque product. The generated
  record keeps the product name but stores it as `1 G_PER_L`, which is the
  wrong representation for a one-liter product volume.
- The maintained normalized source has already been repaired to use
  `1000 ML_PER_L` and to document that the source does not disclose the
  product's internal formulation.

## Evidence

- TOGO M2494 reports one component: `MRS broth (Oxoid)`, 1 L.
- TOGO M2494 records the cultivation context as MRS broth from Oxoid at 37 C
  for 18 h before genomic DNA isolation.
- `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml` preserves the
  one-liter product row as `1000 ML_PER_L`, adds a MIX preparation step, records
  `temperature_value: 37.0`, carries `ingredients_curated` and
  `has_unmapped_ingredients` quality flags, and cites the TOGO URL.
- `data/merge_yaml/merged/mrs_broth__b44dc935.yaml` was generated on
  2026-08-06 and still has `1 G_PER_L`, no preparation step, no temperature,
  no quality flags, and no `references` entry.

## Completeness

- The generated record omits the maintained opaque-product note that explains
  why the Oxoid MRS broth row is intentionally not decomposed.
- The generated record omits the TOGO 37 C cultivation temperature that is
  present in the maintained YAML.
- The generated record omits the maintained MIX preparation step.
- The generated record omits maintained `ingredients_curated` and
  `has_unmapped_ingredients` data-quality flags.
- The generated record omits the maintained TOGO reference entry.
- An ignored-inclusive exact search across `data/normalized_yaml` and
  `data/merge_yaml` for `TOGO:M2494`, `M2494`,
  `TOGO_M2494_MRS_broth`, and `CultureMech:009068` found one maintained
  normalized source and this generated merged output. A `find` search,
  which included ignored files, found no normalized file named
  `mrs_broth__b44dc935.yaml`; the maintained file is
  `TOGO_M2494_MRS_broth.yaml`.

## Findings

| Severity | Finding | Evidence | Future owner |
| --- | --- | --- | --- |
| major | The generated product-row concentration is stale and uses the wrong unit. | TOGO M2494 lists `MRS broth (Oxoid)` as 1 L, and the maintained 2026-09-08 repair stores that as `1000 ML_PER_L`; the generated YAML still stores `1 G_PER_L`. | Regenerate `data/merge_yaml/merged/mrs_broth__b44dc935.yaml` from `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml` |
| major | The generated record lost the maintained opaque-product provenance. | The normalized YAML notes that TOGO M2494 records Oxoid MRS broth and does not disclose the product's internal formulation; the generated notes contain only the TOGO source URL. | Regeneration from `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml` |
| major | The generated record lost curated source metadata. | The normalized YAML has `temperature_value: 37.0`, a MIX step, `ingredients_curated`, `has_unmapped_ingredients`, and a TOGO reference; the generated YAML has none of those fields. | Regeneration from `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/mrs_broth__b44dc935.yaml` from
   `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml`.
2. Confirm the regenerated ingredient remains an opaque `MRS broth (Oxoid)`
   row at `1000 ML_PER_L`, not `1 G_PER_L`.
3. Confirm regeneration carries forward the 37 C temperature, the MIX
   preparation step, the `ingredients_curated` and `has_unmapped_ingredients`
   flags, and the TOGO reference.

## Follow-up Checks

- Rerun the focused LinkML, strict, reference, and term validators on the
  regenerated `data/merge_yaml/merged/mrs_broth__b44dc935.yaml`.
- Search with `rg --no-ignore --hidden` for `TOGO:M2494` and
  `CultureMech:009068` across `data/normalized_yaml` and `data/merge_yaml` to
  confirm the generated output still traces to only the one maintained source.
- Manually compare the regenerated record with TOGO M2494 and
  `data/normalized_yaml/bacterial/TOGO_M2494_MRS_broth.yaml` to confirm no
  decomposed MRS recipe was inferred.

## Additional Notes

None found.
