# YAML Record Review: ny_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ny_agar__9574a4a0.yaml
- Started UTC: 2026-09-24T18:52:38Z
- Finished UTC: 2026-09-24T18:54:13Z
- Verdict: needs curation

## Target

- Generated record: `data/merge_yaml/merged/ny_agar__9574a4a0.yaml`
- Maintained owner: `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml`
- Class: `MediaRecipe`
- ID: `CultureMech:007649`
- Label: `ny_agar`
- Source grounding: `TOGO:M1128`, imported from JCM source `JCM_M1060`
- Inspected source URL: `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1060`
- Merge state: generated from one source recipe, `TOGO_M1128_NY_Agar`

## Validation

All focused validators passed for the generated YAML.

| Check | Command | Result |
| --- | --- | --- |
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ny_agar__9574a4a0.yaml` | Passed with `No issues found`. |
| Strict schema wrapper | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ny_agar__9574a4a0.yaml --out /private/tmp/ny_agar__9574a4a0.strict.tsv --workers 1 --quiet` | Passed; the output TSV had the header only, 1 line and 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ny_agar__9574a4a0.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ny_agar__9574a4a0.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` deprecation warning. |
| Embedded history | Not run | Not checked: the available `just validate-history` target validates standalone `history/`, not embedded `MediaRecipe.curation_history` in merged YAML. |

## Identity and Grounding

The record is the TOGO import of JCM medium 1060, NY Agar. The TOGO API identifies the medium as `M1128` with source ID `JCM_M1060`, source URL `GRMD=1060`, name `NY Agar`, and pH `7.0`; the local `media_term` correctly preserves `TOGO:M1128`.

The component identity is also mostly grounded: the source formula contains nutrient broth, yeast extract, NaCl, magnesium sulfate heptahydrate, agar, and distilled water. The generated sodium chloride, magnesium sulfate heptahydrate, agar, and water ontology terms are compatible with those source rows.

## Evidence

The TOGO API and the inspected JCM page agree on these source rows: 7.0 g nutrient broth (BD-Difco), 5.0 g yeast extract (BD-Difco), 3.0 g NaCl, 0.5 g MgSO4 x 7 H2O, 15.0 g agar, and 1.0 L distilled water. They also both carry the instruction to adjust pH to 7.0.

The generated record preserves the five dry component amounts in `G_PER_L`, but it does not preserve the source water unit: `Distilled water` is represented as `1 G_PER_L` instead of 1 L. It also omits both `ph_value: 7.0` and a preparation step for pH adjustment, even though both appear in the TOGO API metadata/comment and the JCM page.

An ignored-inclusive exact search over `data`, `src`, `tests`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md` found that `data/normalized_yaml/bacterial/JCM_J1060_NY_AGAR.yaml` and its generated copy `data/merge_yaml/merged/NY_AGAR.yaml` represent the same JCM `GRMD=1060` source through `mediadive.medium:J1060`. That sibling carries the pH 7.0 adjustment but omits explicit water, so the two source records are split into separate generated recipes for the same upstream recipe.

## Completeness

The source recipe is a formula plus a single pH instruction. The record does not carry target organisms, citations, incubation conditions, or a complex protocol; those are optional and were correctly left empty for the inspected source material.

The record is materially incomplete because it drops the pH adjustment from TOGO/JCM, and the de-duplication view is incomplete because the TOGO copy of JCM 1060 and the direct JCM/MediaDive copy of JCM 1060 do not merge or cross-reference each other.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | Distilled water was converted from 1 L to `1 G_PER_L`. | The TOGO API and JCM page both list distilled water as 1.0 L; the generated record stores that row as value `1`, unit `G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml` |
| Major | The pH 7.0 adjustment was dropped from the TOGO-owned record. | The TOGO API has `meta.ph` of `7.0` and a comment to adjust pH to 7.0; the JCM page has the same pH instruction, while the generated record has no `ph_value` or `preparation_steps`. | `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml` |
| Major | The TOGO import and direct JCM/MediaDive import of `GRMD=1060` are duplicate source records that now generate separate `ny_agar` recipes. | The ignored-inclusive exact search found `TOGO_M1128_NY_Agar` grounded to `TOGO:M1128` and `JCM_M1060`, while `JCM_J1060_NY_AGAR` is grounded to `mediadive.medium:J1060`; both cite `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1060`. | `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml`, `data/normalized_yaml/bacterial/JCM_J1060_NY_AGAR.yaml`, and merge/import de-duplication |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml`, correct the distilled-water representation so the source 1 L final volume is no longer encoded as `1 G_PER_L`.
2. In `data/normalized_yaml/bacterial/TOGO_M1128_NY_Agar.yaml`, add `ph_value: 7.0` and an `ADJUST_PH` preparation step matching the inspected TOGO/JCM instruction.
3. Collapse or explicitly de-duplicate the TOGO `M1128` and direct JCM `J1060` imports of JCM `GRMD=1060` so regeneration yields one NY Agar recipe instead of two generated `ny_agar` records for the same source URL.

## Follow-up Checks

- Rerun LinkML schema, strict schema, term, and reference validation on the regenerated NY Agar record.
- Re-run an ignored-inclusive exact search for `TOGO:M1128`, `M1128`, `JCM_M1060`, `JCM_J1060_NY_AGAR`, `mediadive.medium:J1060`, and `GRMD=1060` to confirm the corrected records no longer leave the same JCM source as two independent recipes.
- Manually compare the regenerated formula against the TOGO API and JCM page to confirm it still has the five dry ingredients, 1 L distilled water, and pH 7.0.

## Additional Notes

No YAML edits were made as part of this read-only review. Generated `data/merge_yaml/merged` files should be regenerated only after their maintained normalized source records or merge/import logic are corrected.
