# YAML Record Review: 915

- Repository: CultureMech
- Record: data/merge_yaml/merged/915.yaml
- Started UTC: 2026-09-21T07:47:01Z
- Finished UTC: 2026-09-21T07:47:47Z
- Verdict: needs curation

## Target

- Reviewed generated record `data/merge_yaml/merged/915.yaml`.
- Stable identifier: `CultureMech:007504`.
- Source identity asserted by the record: NBRC medium 913, imported as `NBRC_915.yaml`.
- The generated record was merged from one owner, `NBRC_915.yaml`, on fingerprint `bb994fc5988c1c4e193cf59c17c332737451a8477fb6153fb56634321cf8a455`.
- Current authoritative owner: `data/normalized_yaml/bacterial/NBRC_915.yaml`.

## Validation

- PASS: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/915.yaml`
- PASS: `scripts/validate_strict.py data/merge_yaml/merged/915.yaml`
- PASS: `linkml-reference-validator validate data data/merge_yaml/merged/915.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- PASS: `linkml-term-validator validate-data data/merge_yaml/merged/915.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
- Not checked: embedded `MediaRecipe.curation_history` entries; the documented history validator targets standalone files under `history/`.

## Identity and Grounding

- The intended NBRC identity is recoverable from curation history, but the generated record is stale relative to `data/normalized_yaml/bacterial/NBRC_915.yaml`.
- The current normalized owner now carries the official name `Casamino acids-peptone-Czapek's agar`, identifies `nbrc.medium:913`, and has a structured NBRC medium 913 reference.
- A gitignore-independent exact search for `NO=913` and `Media No. 913` found this NBRC owner and the Togo M1707 import that points at the same NBRC medium 913 URL.
- `data/merge_yaml/merged/casamino_acids_peptone_czapeks_agar.yaml` is a second active generated record for the same NBRC source through Togo `M1707`.

## Evidence

- The current NBRC medium 913 page names the recipe `Casamino acids-peptone-Czapek's agar`.
- NBRC lists 1 g Casamino Acids, 2 g Bacto Peptone, 1 g `K2HPO4`, 0.5 g KCl, 0.5 g `MgSO4.7H2O`, 10 mg `FeSO4.7H2O`, 30 g sucrose, 15 g agar, and 1 L distilled water.
- The normalized NBRC owner already preserves those nine source rows and keeps the ferrous sulfate heptahydrate amount in `MG_PER_L`.
- The generated target instead has one concatenated source-table blob as an ingredient name and one empty-name ingredient whose concentration value is `Casamino Acids (Difco)`.

## Completeness

- The generated target is missing all nine structured NBRC ingredient rows that exist in the repaired normalized owner.
- The generated target has no `media_term`, no NBRC reference entry, and no useful ontology grounding.
- NBRC medium 913 is duplicated by the Togo M1707 generated record; that peer keeps the same row labels but converts the 10 mg `FeSO4.7H2O` amount to 10 g/L.

## Findings

- BLOCKER: `data/merge_yaml/merged/915.yaml` is stale and unusable: it still contains the pre-recovery concatenated NBRC table rather than the nine repaired ingredients in `data/normalized_yaml/bacterial/NBRC_915.yaml`.
- BLOCKER: the generated target does not preserve the official NBRC recipe name or the NBRC medium 913 structured source identity now present in the normalized owner.
- BLOCKER: NBRC medium 913 is represented by two separate active generated records: this stale direct NBRC record and the Togo M1707 `casamino_acids_peptone_czapeks_agar.yaml` record.

## Recommended Edits

- Regenerate `data/merge_yaml/merged/915.yaml` from `data/normalized_yaml/bacterial/NBRC_915.yaml`; do not edit the generated YAML directly.
- Reconcile `NBRC_915.yaml` and `TOGO_M1707_Casamino_acids-peptone-Czapek_s_agar.yaml` so one canonical NBRC medium 913 record is emitted with both source identities retained.
- Preserve the direct NBRC row values during reconciliation, especially `FeSO4.7H2O` at 10 mg/L, not 10 g/L.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after regenerating the merge artifacts.
- Re-run an exact ignored-file-inclusive search for `NO=913`; only one active generated NBRC medium 913 recipe should remain.
- Confirm that the generated page for `915` or its canonical successor has nine readable ingredient rows and no concatenated source-table blob.

## Additional Notes

- The current normalized NBRC owner already fixes the mg-scale ferrous sulfate unit that is wrong in the Togo import.
