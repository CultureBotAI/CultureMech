# YAML Record Review: r83_medium__322a42cb

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r83_medium__322a42cb.yaml
- Started UTC: 2026-09-25T00:46:50Z
- Finished UTC: 2026-09-25T00:48:17Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:003058 |
| Name | r83_medium |
| Class | MediaRecipe |
| Category | bacterial |
| Source identity | JCM Medium J711 / MediaDive J711, R83 MEDIUM |
| Maintained owner | data/normalized_yaml/bacterial/r83_medium.yaml |
| Generated path | data/merge_yaml/merged/r83_medium__322a42cb.yaml |

The reviewed file is a one-input generated merge product. Future formula fixes belong in the maintained JCM/MediaDive owner, or in the importer that translated JCM/MediaDive post-autoclave additions into YAML.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r83_medium__322a42cb.yaml` exited 0 and printed `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r83_medium__322a42cb.yaml --out /private/tmp/r83_medium_322a42cb.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r83_medium__322a42cb.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r83_medium__322a42cb.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

## Identity and Grounding

- The generated record correctly denotes R83 MEDIUM from JCM/MediaDive J711 rather than the separate TOGO M734 import.
- The live JCM GRMD 711 endpoint now returns no medium, but the MediaDive J711 REST mirror resolves the JCM source ID, source name, pH 6.5, main-solution salt formula, and preparation comments.
- TOGO M734 reports the same original JCM source URL and also includes the two compounds that the JCM/MediaDive import left in prose: Na2CO3 and glucose, both at 4 g/L.

## Evidence

Supported source-backed claims:

- MediaDive J711 supports the R83 MEDIUM name, pH 6.5, seven direct salts/reductant entries in the generated ingredient list, and the comment that Na2CO3 at 4.0 g/L is added before dispensing/autoclaving while glucose at 4.0 g/L is added sterile.
- TOGO M734 independently preserves the same JCM_M711 source metadata and represents Na2CO3 and glucose as components at 4 g/L.

Unsupported or incomplete claims:

- The ingredient list is incomplete for the preparation as written. A reader following the YAML ingredient list would omit Na2CO3 and glucose even though the same record's preparation step says to add both.
- The MediaDive REST recipe includes 1000 ml distilled water; the generated record omits it. That omission is common in this import path, but it still means the target lacks the source's final-volume carrier.

## Completeness

- The review checked the generated YAML, its maintained normalized owner, the parallel TOGO M734 normalized and generated records, the live MediaDive J711 REST mirror, the TOGO M734 API response, and the live JCM GRMD 711 page.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `TOGO:M734`, `TOGO_M734_R83_Medium`, `JCM_M711`, and `mediadive.medium:J711` found the direct JCM/MediaDive owner, the parallel TOGO owner, their two generated outputs, source indexes, and historical validation archives.
- Empty `target_organisms` is not a defect because the inspected source records only define the medium formulation.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | Na2CO3 and glucose are required additions but are not modeled as ingredients. | The generated record says to add Na2CO3 and sterile glucose at 4 g/L in a preparation step. TOGO M734 represents both as components. | data/normalized_yaml/bacterial/r83_medium.yaml or the JCM/MediaDive importer |
| Major | Distilled water / final volume from the MediaDive J711 mirror is absent. | MediaDive J711 lists 1000 ml distilled water in `Main sol. J711`; the generated CultureMech record has no water ingredient or volume-bearing solution wrapper. | data/normalized_yaml/bacterial/r83_medium.yaml or the JCM/MediaDive importer |
| Minor | The current `notes` source link points at an unavailable live JCM page. | An exact fetch of `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=711` returned a JCM page with no GRMD 711 medium data, while MediaDive and TOGO still mirror the old JCM_M711 content. | Source provenance on data/normalized_yaml/bacterial/r83_medium.yaml |

## Recommended Edits

1. Add Na2CO3 and glucose to the maintained JCM/MediaDive R83 ingredients at 4 g/L, preserving their preparation timing in `preparation_steps`.
2. Represent the 1000 ml distilled-water carrier explicitly, either as a direct ingredient with a volume unit or as a main-solution structure consistent with the rest of the repository.
3. Add `references` or `sources` metadata that record the resolvable MediaDive J711 mirror and note that the original JCM GRMD 711 page no longer returns data.
4. Decide whether the TOGO M734 import should be reconciled as a source duplicate only after correcting its mg-to-g/L conversion errors; do not copy its 100, 240, 480, and 600 g/L values into the JCM/MediaDive owner.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation on `data/normalized_yaml/bacterial/r83_medium.yaml` and the regenerated `data/merge_yaml/merged/r83_medium__322a42cb.yaml`.
- Regenerate merge outputs and check that R83 has Na2CO3 and glucose as ingredients plus preparation prose for when to add them.
- Re-fetch MediaDive J711 or TOGO M734 to confirm the 4 g/L Na2CO3 and glucose additions before finalizing the correction.
- Re-run a gitignore-independent exact search for `mediadive.medium:J711` and `TOGO:M734` to confirm the two R83 source mirrors remain distinct or have been deliberately reconciled.

## Additional Notes

- The parallel TOGO generated record `data/merge_yaml/merged/R83_MEDIUM.yaml` has severe unit conversion errors because TOGO milligram amounts were imported as g/L values. It was used only as evidence that Na2CO3 and glucose came from JCM_M711; its quantities for the milligram salts need separate curation before any source-duplicate merge.
- This review did not modify the generated record or the maintained R83 records.
