# YAML Record Review: py_medium_c

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/py_medium_c.yaml
- Started UTC: 2026-09-24T22:11:40Z
- Finished UTC: 2026-09-24T22:12:28Z
- Verdict: needs curation

## Target

- Reviewed generated `MediaRecipe`: `data/merge_yaml/merged/py_medium_c.yaml`
- Stable ID: `CultureMech:009991`
- Label: `py_medium_c`
- Original label: `PY Medium (C)`
- Category: `bacterial`
- Source grounding: TOGO Medium M594, imported from JCM Medium 589
- Merge provenance: generated from one normalized record, `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml`
- Maintained owner path for future fixes: `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml`, or the TOGO importer if regenerated normalized records would overwrite direct edits

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/py_medium_c.yaml` | Passed; exited 0 with `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/py_medium_c.yaml --out /private/tmp/py_medium_c.strict.tsv --workers 1 --quiet` | Passed; the TSV at `/private/tmp/py_medium_c.strict.tsv` had 1 header row and 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/py_medium_c.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/py_medium_c.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the usual `eutils` `pkg_resources` warning. |
| Embedded history | Not run | Not checked: the repository exposes `just validate-history` for standalone `history/` files, not for `MediaRecipe.curation_history` embedded in merged YAML. |

## Identity and Grounding

- TOGO M594 identifies `PY Medium (C)` with original media ID `JCM_M589` and source URL `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=589`.
- JCM GRMD 589 is titled `PY MEDIUM (C)` and supports the TOGO M594 identity.
- An ignored-file-inclusive exact search under `data/**/*.yaml` for `TOGO:M594`, `medium/M594`, `JCM_M589`, `GRMD=589`, `mediadive.medium:J589`, and `name: py_medium_c` found the TOGO M594 owner reviewed here and the direct MediaDive J589 duplicate at `data/normalized_yaml/bacterial/py_medium_c.yaml`.

## Evidence

- JCM 589 and TOGO M594 directly support a main medium with 10.0 g peptone, 10.0 g yeast extract, 40.0 ml Salts solution, 1.0 L distilled water, and preparation by thorough mixing and autoclaving under N2.
- JCM 589 and TOGO M594 directly support post-autoclave anaerobic addition of 10.0 ml 20% Na2CO3 solution and 10.0 ml 5% Na2S x 9 H2O solution.
- JCM 589 and TOGO M594 directly support a separate Salts solution containing 0.2 g CaCl2 x 2 H2O, 0.2 g MgSO4 x 7 H2O, 1.0 g KH2PO4, 1.0 g K2HPO4, 10.0 g NaHCO3, 2.0 g NaCl, and 1.0 L distilled water.
- The generated record instead flattens all six Salts solution components into the final ingredient list, records `Salts solution` and `20% Na2CO3 solution` as `G_PER_L` additions, leaves the `Salts solution` composition empty, and stores both source water rows as one merged `2.0 G_PER_L` ingredient.

## Completeness

- Empty optional organism, growth, pH, and literature slots are not defects: JCM 589 provides no explicit final pH.
- The generated TOGO branch is not complete enough to reproduce JCM 589 because stock solution boundaries, milliliter additions, and the anaerobic post-autoclave addition order are not represented.
- The ignored-file-inclusive search described above found one duplicate MediaDive owner for the same JCM 589 medium.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The 40 ml/L Salts solution addition is stored as `40 G_PER_L` and its composition is empty. | JCM 589 lists `Salts solution` as a 40.0 ml addition and then defines six salts in 1 L distilled water. The generated `solutions` entry has `unit: G_PER_L` and `composition: []`. | `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml` or the TOGO importer |
| Major | Salts solution components are flattened at stock strength. | The source puts CaCl2 x 2 H2O, MgSO4 x 7 H2O, KH2PO4, K2HPO4, NaHCO3, and NaCl inside the stock that is added at 40 ml/L. The generated final medium lists those salts directly as grams per liter. | `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml` or the TOGO importer |
| Major | The post-autoclave 20% Na2CO3 and 5% Na2S x 9 H2O solution additions are not represented as 10 ml/L solution additions. | JCM 589 says to aseptically and anaerobically add 10.0 ml of each autoclaved solution after the main medium is autoclaved under N2. The generated `20% Na2CO3 solution` entry is `10 G_PER_L`, and `5% Na2S x 9 H2O` appears as a top-level `10 G_PER_L` ingredient. | `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml` or the TOGO importer |
| Major | Main and stock distilled waters are merged into an unsupported `2.0 G_PER_L` ingredient. | JCM 589 has 1.0 L distilled water in the main medium and 1.0 L in the Salts solution stock. The generated record merges the two as `2.0 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml` or the TOGO importer |
| Major | The anaerobic autoclave and post-autoclave addition order is absent. | JCM 589 says to autoclave the mixed components under N2, then aseptically and anaerobically add the two autoclaved solutions. The generated record has no `preparation_steps` and only a variable `N2` ingredient. | `data/normalized_yaml/bacterial/TOGO_M594_PY_Medium_C.yaml` |

## Recommended Edits

1. Rework the maintained TOGO M594 record so the top-level medium contains 10 g/L peptone, 10 g/L yeast extract, 1 L distilled water, and a 40 ml/L Salts solution reference.
2. Move CaCl2 x 2 H2O, MgSO4 x 7 H2O, KH2PO4, K2HPO4, NaHCO3, NaCl, and 1 L distilled water into the Salts solution stock.
3. Represent 20% Na2CO3 solution and 5% Na2S x 9 H2O as 10 ml/L post-autoclave additions rather than `G_PER_L` final ingredients.
4. Restore the N2 autoclave context and anaerobic post-autoclave addition order as preparation steps.
5. Link the repaired TOGO M594 record to the repaired direct JCM `data/normalized_yaml/bacterial/py_medium_c.yaml` record as a source duplicate.
6. Regenerate merged YAML after the maintained records are repaired.

## Follow-up Checks

- Re-run the schema, strict, reference, and term validators against the regenerated TOGO M594 and MediaDive J589 outputs.
- Manually compare the regenerated TOGO M594 branch with JCM GRMD 589 and verify that the Salts solution, 20% Na2CO3, 5% Na2S x 9 H2O, and 1 L water rows are scoped to the correct recipe layer.
- Confirm that TOGO M594 and MediaDive J589 no longer produce two independent generated records once stock solution modeling is repaired.

## Additional Notes

- None found.
