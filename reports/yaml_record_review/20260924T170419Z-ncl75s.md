# YAML Record Review: ncl75s

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ncl75s.yaml
- Started UTC: 2026-09-24T17:04:13Z
- Finished UTC: 2026-09-24T17:04:50Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/ncl75s.yaml` as a generated `MediaRecipe` for CCAP NCL75S, `CultureMech:000100`, label `ncl75s`, category `algae`, and physical state `LIQUID`.

The generated record was merged from `ncl75s` on 2026-08-06. Its maintained upstream owner is `data/normalized_yaml/algae/ncl75s.yaml`; curation fixes belong there or in the merge regeneration workflow, not as manual edits to `data/merge_yaml/merged/ncl75s.yaml`.

## Validation

| Check | Result |
|---|---|
| Open schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ncl75s.yaml` | Passed; exited 0 with no diagnostics. |
| Strict schema, `python scripts/validate_strict.py data/merge_yaml/merged/ncl75s.yaml --out /private/tmp/ncl75s.strict.tsv --workers 1 --quiet` | Passed; TSV had the header only, 1 line and 0 errors. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/ncl75s.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/ncl75s.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries inside a single merged YAML file. |

## Identity and Grounding

- The CCAP source identity is coherent: the record is for `CCAP:NCL75S`, original name `NCL75S`, and the inspected CCAP PDF is the NCL75S New Cereal Leaf-75% Seawater recipe.
- A gitignore-independent hidden-file search for `CCAP:NCL75S`, `database_id: NCL75S`, and `MR_NCL75S` across `data/normalized_yaml` and `data/merge_yaml` found this generated file and `data/normalized_yaml/algae/ncl75s.yaml`; no second active YAML record for the same CCAP accession or PDF was found in that bounded corpus.
- The generated record is stale relative to `data/normalized_yaml/algae/ncl75s.yaml`: it contains only the 2026-08-06 merge event and missed the 2026-09-06 `repair_algae_score45_blends_soils.py` ingredient repair and the 2026-09-13 `repair_ccap_sources_score10.py` structured-source repair.
- The `algae` category is an import filing choice, but the description and application strings overstate the source. The CCAP PDF labels NCL75S for marine protozoa, while the record says it is suitable for saltwater algae cultivation and lists algae, phytoplankton, and microalgae applications without source support.

## Evidence

The inspected CCAP PDF supports these core formulation facts:

- 750.0 ml natural filtered seawater per liter.
- 250.0 ml deionised water per liter.
- 1.0 g cereal grass leaves per liter.
- Bring 500 ml of 75% seawater to a boil, add cereal leaves, continue boiling for 5 minutes, cool to room temperature, restore the volume to 500 ml with deionised water, filter through GF/C paper, decant into the remaining 500 ml, and autoclave at 15 psi for 15 minutes.

The generated ingredient table is not source-supported. It parsed the phrase `Bring 500 ml of` from the preparation paragraph as an ingredient at `75 PERCENT_W_V`; the PDF uses `75% seawater` as a mixture description, not as a 75% w/v concentration of that phrase.

The generated `notes` field only points readers to the PDF URL. The later maintained record has source-backed NCL75S notes and three ingredients matching the PDF, so the generated failure is a stale merge artifact rather than an unresolved CCAP transcription problem.

## Completeness

- Consequential gap: the generated file lacks all three source ingredients from `data/normalized_yaml/algae/ncl75s.yaml`.
- Consequential gap: the generated file lacks the CCAP preparation steps, GF/C filtration step, and 15 psi for 15 minutes autoclave sterilization captured in the maintained owner.
- Consequential gap: the generated file lacks the structured `sources` block with `database: CCAP`, `database_id: NCL75S`, and the PDF URL that the maintained owner added on 2026-09-13.
- Consequential unsupported scope: the source establishes a marine protozoa medium, not the algae/phytoplankton/microalgae application claims.
- Empty optional fields are not defects. This source recipe does not provide a pH, incubation temperature, light intensity, or target organism list specific enough for structured growth evidence.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The generated ingredient is a malformed parse of a preparation sentence and leaves the three CCAP components absent from the merged record. | `data/merge_yaml/merged/ncl75s.yaml` has one ingredient, `Bring 500 ml of`, at `75 PERCENT_W_V`; the inspected CCAP PDF lists natural filtered seawater, deionised water, and cereal grass leaves per liter. | Regenerate `data/merge_yaml/merged/ncl75s.yaml` from the already repaired `data/normalized_yaml/algae/ncl75s.yaml`; if regeneration still emits the stale row, fix the merge pipeline. |
| major | The merged record is stale relative to its normalized owner and therefore omits structured CCAP source provenance plus the 2026-09-06 formulation repair. | The generated record ends with a 2026-08-06 `MERGED_RECIPES` event. The normalized owner contains 2026-09-06 and 2026-09-13 curation events, structured `sources`, three source ingredients, preparation steps, and `sterilization`. | Regenerate the generated merge from `data/normalized_yaml/algae/ncl75s.yaml`. |
| major | The biological-use claims are broader than the inspected source. | The CCAP PDF labels NCL75S as a marine protozoa medium; the record description and `applications` assert algae cultivation, phytoplankton culture, and microalgae research. | Narrow the description and applications in `data/normalized_yaml/algae/ncl75s.yaml`; then regenerate merged YAML and pages. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/ncl75s.yaml` so it incorporates the repaired ingredients, preparation steps, sterilization block, and structured CCAP source already present in `data/normalized_yaml/algae/ncl75s.yaml`.
2. In `data/normalized_yaml/algae/ncl75s.yaml`, replace the generic algae/phytoplankton/microalgae description and applications with claims scoped to the CCAP NCL75S marine-protozoa recipe, or remove those applications if no inspected source supports them.
3. After regeneration, confirm the merged record no longer has an ingredient named `Bring 500 ml of` and no longer records a `75 PERCENT_W_V` concentration.

## Follow-up Checks

- Re-run the merge pipeline and review `data/merge_yaml/merged/ncl75s.yaml`.
- Re-run open schema, strict schema, reference, and term validation on the regenerated file.
- Re-run a gitignore-independent hidden-file search for `CCAP:NCL75S`, `database_id: NCL75S`, and `MR_NCL75S` across `data/normalized_yaml` and `data/merge_yaml` to confirm the CCAP accession still resolves to one active normalized YAML record and one generated merged YAML record.
- Manually compare the regenerated ingredient and preparation blocks against the current CCAP NCL75S PDF.

## Additional Notes

The CCAP PDF was fetched directly from the record URL and rendered with `mutool draw -F txt`. It had revision 002, approval date 29 May 2026, and review date 30 June 2026 at inspection time.
