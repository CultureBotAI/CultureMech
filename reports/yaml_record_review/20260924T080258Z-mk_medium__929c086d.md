# YAML Record Review: mk_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mk_medium__929c086d.yaml
- Started UTC: 2026-09-24T08:02:58Z
- Finished UTC: 2026-09-24T08:02:58Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/mk_medium__929c086d.yaml`
- Class: `MediaRecipe`
- Record ID: `CultureMech:003255`
- Label: `mk_medium`
- Original label: `MK MEDIUM`
- Source grounding: `mediadive.medium:J906`, labelled `MK MEDIUM`
- Category: `bacterial`
- Generated status: generated merged record with `merge_fingerprint: 929c086d15b979525bf322570972d13c67aa01228765ed52a8718342a0acca94`
- Maintained owner for future edits: `data/normalized_yaml/bacterial/mk_medium.yaml`

## Validation

- Open schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mk_medium__929c086d.yaml`
  - Result: passed with `No issues found`.
- Strict schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mk_medium__929c086d.yaml --out /private/tmp/mk_medium__929c086d.strict.tsv --workers 1 --quiet`
  - Result: passed; 1 file scanned, 0 files with errors, 0 total error rows. `/private/tmp/mk_medium__929c086d.strict.tsv` has 1 line, the header only.
- LinkML reference validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mk_medium__929c086d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 1 file validated, 0 reference checks, all validations passed.
- Term validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mk_medium__929c086d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed.
- Embedded history validation:
  - Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in generated merged YAML.

## Identity and Grounding

- The generated record identity matches MediaDive `J906`: the source payload names `id: J906`, `name: MK MEDIUM`, `source: JCM`, and fixed pH 7.0.
- The inspected JCM medium 906 page supports the MK MEDIUM main recipe, including 1 g trisodium citrate x 2 H2O, 2 g Tryptone, 1 g Peptone, 0.6 g KH2PO4, 0.6 g NH4Cl, 200 g NaCl, 0.2 g MgCl2 x 6 H2O, 20 g K2SO4, 36 mg FeCl2 x 4 H2O, 0.36 mg MnCl2 x 4 H2O, and 1 ml SL-6 trace element solution before bringing the main portion to 900 ml.
- JCM and MediaDive both represent `0.5 M Glycine-NaOH buffer`, pH 10.0, as a 100 ml separately autoclaved solution that is added after the main MK solution is autoclaved. The generated record instead carries `Glycine-NaOH buffer` as a direct ingredient with `100 G_PER_L`.
- JCM and MediaDive represent SL-6 trace element solution as a 1 ml stock addition. The generated record has no `solutions` key and flattens the SL-6 stock contents into final-medium `ingredients`.
- An exact gitignore-independent search with `rg --no-ignore --hidden` over `data/normalized_yaml` and this generated target found the maintained MediaDive source at `data/normalized_yaml/bacterial/mk_medium.yaml`; the same label also occurs in `data/normalized_yaml/bacterial/TOGO_M949_MK_Medium.yaml` and `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`.

## Evidence

- The source identity, fixed pH, JCM provenance, main J906 formulation, and preparation prose are supported by the inspected MediaDive REST payload for `rest/medium/J906` and the inspected JCM medium 906 page.
- MediaDive main solution 4907 carries a 1 ml `SL-6 trace element solution` addition as `solution_id: 3822`. The generated record omits that solution row and materializes the SL-6 stock compounds as direct final-medium rows.
- MediaDive SL-6 solution 3822 is a 1000 ml stock containing 0.1 g ZnSO4 x 7 H2O, 0.03 g MnCl2 x 4 H2O, 0.3 g H3BO3, 0.2 g CoCl2 x 6 H2O, 0.01 g CuCl2 x 2 H2O, 0.02 g NiCl2 x 6 H2O, and 0.03 g Na2MoO4 x 2 H2O. The medium uses only 1 ml of that stock, so the generated stock-strength rows are not final-medium concentrations.
- The generated MnCl2 x 4 H2O row merges the main recipe's 0.36 mg MnCl2 row with the SL-6 stock's 0.03 g/L MnCl2 row, producing `0.03356436 G_PER_L` and explicitly noting the two merged duplicate values. Those source rows are not duplicates because one is in the 101 ml main solution and the other is inside a 1 L stock used at 1 ml.
- An exact `rg --no-ignore --hidden` search for `^solutions:`, `solution_id: 3822`, and `SL-6 trace element solution` across `data/merge_yaml/merged/mk_medium__929c086d.yaml` and `data/normalized_yaml/bacterial/mk_medium.yaml` returned no matches, with ignored files included by the search. The same search found `Glycine-NaOH buffer` only as a direct ingredient.
- The SL-6 stock has its own `Adjust pH to 3.6` instruction. The generated record records that as a third final-medium `ADJUST_PH` step after the post-autoclave buffer addition, even though MK MEDIUM itself has fixed pH 7.0.

## Completeness

- The direct main-solution quantities and the JCM instruction to bring the main fraction to 900 ml are preserved.
- The generated `Glycine-NaOH buffer` row is not a complete representation of the 100 ml, separately autoclaved, pH 10.0, 0.5 M buffer addition.
- The record is materially incomplete for SL-6 stock-solution structure and quantitatively wrong for the flattened SL-6 components.
- The optional instruction to add 20.0 g/L Bacto agar for a solid variant is correctly preserved as conditional preparation prose; it does not need to appear as a required ingredient in this liquid record.
- Empty optional organism, source-data, and discussion fields are not defects for this generated import. The inspected JCM and MediaDive entries support the recipe but do not attach a specific growth observation that should have produced a `target_organisms` claim here.

## Findings

1. **major - the 100 ml Glycine-NaOH buffer addition is represented as a mass concentration**
   - Evidence: JCM 906 lists a separately autoclaved 100 ml `0.5 M Glycine-NaOH buffer (pH 10.0)` solution. The generated record has a direct `Glycine-NaOH buffer` ingredient at `100 G_PER_L`, losing the molarity, pH, volume, and post-autoclave addition boundary.
   - Maintained owner: `data/normalized_yaml/bacterial/mk_medium.yaml`, via the MediaDive solution importer that flattened the buffer row.
2. **major - the 1 ml SL-6 trace element stock is flattened at stock strength**
   - Evidence: MediaDive main solution 4907 uses 1 ml of solution 3822, but the generated record copies the one-liter SL-6 stock formula into final-medium ingredient rows. The final ZnSO4, H3BO3, CoCl2, CuCl2, NiCl2, and Na2MoO4 amounts are therefore the stock concentrations rather than the result of adding 1 ml stock into MK medium.
   - Maintained owner: `data/normalized_yaml/bacterial/mk_medium.yaml`, via the MediaDive solution importer.
3. **major - duplicate cleanup merged two distinct MnCl2 contexts**
   - Evidence: the source has 0.36 mg MnCl2 x 4 H2O in the main solution and 0.03 g/L MnCl2 x 4 H2O inside SL-6 solution 3822. The generated row combines them into `0.03356436 G_PER_L` with a merged-duplicates note, so a stock ingredient became inseparable from a final-medium ingredient.
   - Maintained owner: `data/normalized_yaml/bacterial/mk_medium.yaml` and the duplicate-ingredient cleanup that ran after import.
4. **major - the SL-6 stock pH is scoped as the final medium pH**
   - Evidence: MediaDive solution 3822 alone says `Adjust pH to 3.6`; generated step 3 applies `Adjust pH to 3.6` to the top-level recipe after MK MEDIUM has already been adjusted to pH 7.0.
   - Maintained owner: `data/normalized_yaml/bacterial/mk_medium.yaml`, via the MediaDive solution importer.

## Recommended Edits

1. Rework the MediaDive import or post-import normalization for `data/normalized_yaml/bacterial/mk_medium.yaml` so the 100 ml Glycine-NaOH buffer remains a separate post-autoclave solution with 0.5 M and pH 10.0 context.
2. Preserve SL-6 trace element solution 3822 as a structured 1 ml stock addition, or scale its components by the 1 ml stock volume if the importer intentionally materializes nested stocks.
3. Update duplicate-ingredient cleanup so ingredients with the same label are never merged across a main recipe and a nested stock solution.
4. Keep `Adjust pH to 3.6` scoped to SL-6 solution 3822 rather than emitting it as a final-medium step for J906.
5. Regenerate `data/merge_yaml/merged/mk_medium__929c086d.yaml` after the normalized source and any importer fixes land; do not patch the generated merged file directly.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the regenerated `data/merge_yaml/merged/mk_medium__929c086d.yaml`.
- Inspect the regenerated YAML for explicit representations of the 100 ml Glycine-NaOH buffer and the 1 ml `SL-6 trace element solution` addition.
- Verify that the two MnCl2 source rows remain in their own solution contexts and are not combined as duplicates.
- Re-run exact ignored-inclusive `rg --no-ignore --hidden` checks for `SL-6 trace element solution`, `solution_id: 3822`, `Glycine-NaOH buffer`, and `Adjust pH to 3.6` against the maintained normalized owner and regenerated merge output.

## Additional Notes

- The sibling TOGO-derived `TOGO_M949_MK_Medium.yaml` and `TOGO_M950_MK_Medium.yaml` records use the same normalized label. After the J906 solution hierarchy is preserved, compare those TOGO sources with this MediaDive source to decide whether any generated MK records should merge.
