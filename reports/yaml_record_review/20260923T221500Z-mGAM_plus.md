# YAML Record Review: mGAM plus

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mGAM_plus.yaml
- Started UTC: 2026-09-23T22:13:50Z
- Finished UTC: 2026-09-23T22:15:00Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mGAM_plus.yaml`.

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:015591 |
| Name | mGAM plus |
| Description | Gifu Anaerobic Broth, with added Hemin and Menadione |
| Source | CultureBotHT `mGAM plus` |
| Category | specialized |
| Generated status | Generated canonical merge under `data/merge_yaml/merged/`; owned by `data/normalized_yaml/specialized/mgam_plus.yaml` and regenerated by `src/culturemech/merge/merge_recipes.py` |

The maintained owner is `data/normalized_yaml/specialized/mgam_plus.yaml`.
The merged YAML is a single-source generated record with
`merged_from: [mgam_plus]`.

## Validation

| Check | Command | Result |
|---|---|---|
| LinkML open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mGAM_plus.yaml` | Passed: `No issues found`. |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mGAM_plus.yaml --out /private/tmp/mGAM_plus.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mGAM_plus.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 reference checks, all validations passed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mGAM_plus.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils`/`pkg_resources` warning. |
| Embedded history | Not run | Not checked: the repository `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays. |

## Identity and Grounding

The generated record is a single-source copy of
`data/normalized_yaml/specialized/mgam_plus.yaml`. The local record
consistently identifies `CultureMech:015591` as `mGAM plus`, a specialized,
complex, undefined, liquid CultureBotHT medium described as `Gifu Anaerobic
Broth, with added Hemin and Menadione`.

The upstream source identity is under-specified:

- `sources[0]` names `CultureBotHT`, `database_id: mGAM plus`, and only the
  repository root `https://github.com/CultureBotAI/CultureBotHT`.
- `curation_history` says the imported file was `mGAM_plus.yaml`.
- `scripts/import_from_culturebotht.py` reads from a `media_yaml` directory in
  a CultureBotHT checkout, but the importer records neither that path nor a
  commit-pinned raw URL.
- The GitHub repository API returned 404 while reviewing the adjacent `mGAM`
  record, and direct raw `main/mGAM_plus.yaml` returned 404 here.
- A clean ignored-file-inclusive `data/` filename scan for exact
  `mGAM_plus`/`mgam_plus` prefixes found only the normalized owner and this
  generated merge file, not a raw CultureBotHT capture.

The seven undefined complex ingredients that intentionally lack ontology
terms are at least explicitly signaled with `data_quality_flags:
[has_unmapped_ingredients]`. The exact source file is still unavailable, so
the ingredient list and all 15 amounts remain unverified against CultureBotHT.

## Evidence

Supported by inspected local records:

| Claim | Assessment |
|---|---|
| The generated record is single-source | Supported by `merged_from: [mgam_plus]` and the `MERGED_RECIPES` event naming only `mgam_plus.yaml`. |
| The maintained owner carries 15 ingredients at g/L strengths | Supported by `data/normalized_yaml/specialized/mgam_plus.yaml`; not independently verified against CultureBotHT. |
| The record contains the 13 `mGAM` ingredients plus Hemin 0.005 G_PER_L and Menadione 0.001 G_PER_L | Supported by the maintained `mgam_plus` and adjacent `mgam` YAML files; not evidence for the original CultureBotHT source. |

Unsupported or unresolved against primary source:

- No exact CultureBotHT `mGAM_plus.yaml` blob was recoverable from the recorded
  provenance, so the full ingredient list still needs comparison with the
  original FEBA/CultureBotHT source.
- The importer collapses provenance to a repository root; a reader cannot tell
  which commit produced this formula.
- Most undefined complex ingredients are ungrounded. The record flags this as
  `has_unmapped_ingredients`, so that is transparent, but the exact source
  cannot be used to determine whether any additional curated mixture terms are
  available.

## Completeness

Consequential gaps:

- File-level, commit-pinned CultureBotHT provenance is missing.
- The exact upstream file was not vendored under `data/`, so the formula is
  not reproducible without a separate CultureBotHT checkout.

Empty optional slots for pH, preparation steps, storage, atmosphere, and target
organisms are not record defects on the evidence currently available: the
inspected CultureMech record and importer metadata do not expose those fields
from CultureBotHT.

Bounded local searches included ignored and hidden files:

- `CultureMech:015591` and the generated merge fingerprint were searched under
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`.
- Exact file paths matching `mGAM_plus` or `mgam_plus` were enumerated under
  `data/`; only the maintained normalized and generated merged YAML files were
  found.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | CultureBotHT provenance is not recoverable at file level. | The record stores only the repository root and `mGAM_plus.yaml`; the likely raw GitHub URL returned 404; ignored-file-inclusive local `data/` scans found no raw source capture. | `data/normalized_yaml/specialized/mgam_plus.yaml` and `scripts/import_from_culturebotht.py`. |

## Recommended Edits

1. Recover the exact CultureBotHT `media_yaml/mGAM_plus.yaml` source at a
   stable commit and add file-level provenance, either as a commit-pinned raw
   URL or an archived source under `data/raw`.
2. Update `scripts/import_from_culturebotht.py` so future CultureBotHT imports
   preserve the imported relative path and commit SHA, not only the repository
   root.
3. Re-compare all 15 ingredient names, amounts, units, and source order
   against the recovered `mGAM_plus.yaml`.
4. Revisit the seven ungrounded complex ingredients only after the source file
   is recovered; preserve `has_unmapped_ingredients` for any mixture that still
   has no exact curated term.

## Follow-up Checks

- Re-run focused open-schema, strict, reference, and term validators on
  `data/normalized_yaml/specialized/mgam_plus.yaml`.
- Re-run the same validators on regenerated
  `data/merge_yaml/merged/mGAM_plus.yaml`.
- Run `just verify-merges` and `just audit-merge-freshness` after any
  regenerated merge output.
- Manually diff the recovered CultureBotHT `mGAM_plus.yaml` against the
  maintained record and record any unsupported mapping or amount differences.

## Additional Notes

- The generated merge is not stale relative to the current normalized file in
  any field inspected for this report.
- `mGAM` and `mGAM plus` are adjacent CultureBotHT media and should remain
  separate records because `mGAM plus` includes Hemin and Menadione.
