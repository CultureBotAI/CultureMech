# YAML Record Review: lb_1_tween_80

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/lb_1_tween_80.yaml
- Started UTC: 2026-09-23T18:35:31Z
- Finished UTC: 2026-09-23T18:36:36Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:009568` for `lb_1_tween_80` in `data/merge_yaml/merged/lb_1_tween_80.yaml`.

- Source identity: TogoMedium `TOGO:M3054`, imported from original NBRC source `NBRC_M1608`.
- Original source URL: `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=1608`.
- Generated lineage: `merge_fingerprint: e535cc8031b91d6d4fbb1ea4330699cec6dd9c70c2c646526eae326cbb8fc57e`, `merged_from: lb_1_tween_80`.
- Maintained owner for fixes: `data/normalized_yaml/bacterial/lb_1_tween_80.yaml`.

## Validation

| Check | Result |
|---|---|
| Open LinkML schema validation, target class `MediaRecipe`, against `src/culturemech/schema/culturemech.yaml` | Passed with "No issues found". |
| `scripts/validate_strict.py data/merge_yaml/merged/lb_1_tween_80.yaml --workers 1 --quiet` | Passed with 0 error rows. |
| `linkml-reference-validator validate data data/merge_yaml/merged/lb_1_tween_80.yaml` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/lb_1_tween_80.yaml` | Passed. |
| Embedded `curation_history` validation | Not checked: the repository exposes `just validate-history` for standalone files under `history/`, not for embedded `MediaRecipe.curation_history` blocks inside one generated YAML file. |

## Identity and Grounding

The source identity is internally aligned: TogoMedium API record `M3054` names `LB + 1% Tween 80`, cites `NBRC_M1608` as the original medium ID, and links to NBRC medium detail `NO=1608`; the NBRC page for medium 1608 has the same title.

The checked small-molecule groundings are appropriate. `NaCl` is grounded to sodium chloride, `Tween 80` to polysorbate 80, and distilled water to water. Bacto Tryptone, yeast extract, and optional agar are not grounded to misleading single ChEBI molecules.

## Evidence

NBRC medium 1608 lists 10 g Bacto Tryptone, 5 g yeast extract, 5 g NaCl, 10 g Tween 80, 15 g agar if needed, and pH 7.0. TogoMedium `M3054` returns the same named medium, source accession, and non-water amounts, including 10 g Tween 80 for the title's 1% w/v Tween 80 claim.

The water row needs curation rather than direct acceptance: the TogoMedium API says the recipe has 1 L distilled water, while the currently inspected NBRC HTML table says 1 g distilled water. The CultureMech YAML represents the row as `1 G_PER_L`, which follows the mass unit but still leaves an impossible LB base formulation and conflicts with the direct TogoMedium API value.

Both TogoMedium and NBRC state `pH 7.0`, but the YAML omits a structured pH. The generated record also keeps both source URLs only in `notes`, so its reference list is empty and the reference validator has no URLs to check.

## Completeness

The record is not complete enough because it omits the stated pH and does not preserve the unresolved NBRC/TogoMedium water-volume conflict in a structured `discussion` or quality flag. The conditional `Agar (if needed)` row is preserved textually, but the unconditional `SOLID_AGAR` `physical_state` should be revisited when the source conflict is curated because the NBRC recipe can be prepared without agar.

No consequential source was found for incubation temperature, sterilization, strain-specific growth, atmosphere, or storage. NBRC M1608 and TogoMedium M3054 do not state those details.

An ignored-inclusive, hidden-file-inclusive search over `data/normalized_yaml`, `data/merge_yaml`, and `reports/archive` for `CultureMech:009568`, `TOGO:M3054`, `NBRC_M1608`, `NO=1608`, the source slug, and the merge fingerprint found the maintained normalized owner, this generated merge, and normalized index rows. It did not find an alternate maintained overlay or a prior archived curation report for this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The pH is missing. | NBRC M1608 and TogoMedium M3054 both state `pH 7.0`; the YAML has no pH field. | `data/normalized_yaml/bacterial/lb_1_tween_80.yaml` |
| Major | The distilled-water row is unresolved across sources and probably misrepresented. | TogoMedium M3054 records `Distilled water` as 1 L; the currently inspected NBRC M1608 HTML records the same row as 1 g; the YAML stores `1 G_PER_L`, leaving LB salts and nutrients in 1 g/L water. | `data/normalized_yaml/bacterial/lb_1_tween_80.yaml` |
| Minor | Material source URLs are present only in free text. | The generated record names TogoMedium M3054 and the NBRC 1608 URL in `notes`, but it has no `references`, so the reference validator had 0 URL checks. | `data/normalized_yaml/bacterial/lb_1_tween_80.yaml` |

No blocker findings were found.

## Recommended Edits

1. Add `pH 7.0` from NBRC M1608 / TogoMedium M3054 in the structured pH slot.
2. Curate the distilled-water row against the TogoMedium API and NBRC HTML discrepancy; if the intended source value is 1 L, change the unit to a volume unit and add a discussion noting that the inspected NBRC page currently says 1 g.
3. Add `references` for `https://togomedium.org/medium/M3054`, the TogoMedium API source if retained, and `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=1608`.
4. Revisit `physical_state: SOLID_AGAR` after the water row is corrected and decide whether an optional-agar source should be represented as strictly solid.
5. Regenerate `data/merge_yaml/merged/lb_1_tween_80.yaml` after the normalized YAML is curated.

## Follow-up Checks

- Re-run strict, schema, term, and reference validation on `data/normalized_yaml/bacterial/lb_1_tween_80.yaml`.
- Re-run the merge pipeline and verify that the generated `CultureMech:009568` record carries the curated water amount, `pH 7.0`, source references, and any discussion needed for the NBRC water-row discrepancy.
- Re-run strict, schema, term, and reference validation on the regenerated `data/merge_yaml/merged/lb_1_tween_80.yaml`.
- Manually recompare the regenerated record against NBRC medium 1608 and TogoMedium M3054, especially the water row and optional agar row.

## Additional Notes

This review did not create or mutate GitHub issues, pull requests, comments, labels, settings, source YAML, generated YAML, or rendered pages.
