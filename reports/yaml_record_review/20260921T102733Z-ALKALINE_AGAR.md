# YAML Record Review: Alkaline Agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/ALKALINE_AGAR.yaml`
- Started UTC: 2026-09-21T10:27:33Z
- Finished UTC: 2026-09-21T10:28:02Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/ALKALINE_AGAR.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008414` |
| Name | `alkaline_agar` |
| Original name | `Alkaline Agar` |
| Category | `bacterial` |
| Media term | `TOGO:M1840` / Alkaline Agar |
| Generated from | `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml` |

This is a generated merge record with a single upstream source,
`TOGO_M1840_Alkaline_Agar.yaml`. Future scientific fixes should be made in that
normalized TOGO import or in the TOGO import/stock-solution repair path and then
regenerated into `data/merge_yaml/merged`.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Pass: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ALKALINE_AGAR.yaml` |
| Strict validator | Pass: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ALKALINE_AGAR.yaml --out /private/tmp/ALKALINE_AGAR.strict.tsv --workers 1 --quiet` emitted 0 error rows |
| Reference validator | Pass: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ALKALINE_AGAR.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` completed with 0 total checks |
| Term validator | Pass: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ALKALINE_AGAR.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not a single embedded `MediaRecipe.curation_history` list in `data/merge_yaml/merged` |

The project-level `just validate-schema`, `just validate-strict`, and
`just validate-terms` wrappers currently fail before target-specific validation
because project `uv` tries to build `llvmlite==0.46.0` under Python 3.13 and
crashes in setuptools with `TypeError: Popen.__init__() got an unexpected
keyword argument 'dry_run'`; the same LinkML and validator entry points were
therefore run through an offline `--no-project` Python 3.11 environment.

## Identity and Grounding

`TOGO:M1840` resolves to the TOGO Medium entry named Alkaline Agar. Its TOGO
metadata points to original media ID `NBRC_M1074` and to the NBRC Medium 1074
URL. The live NBRC page for medium 1074 also identifies the medium as Alkaline
Agar, so the generated record's ID, label, category, solid-agar state, and
single-source merge lineage are internally consistent.

The exact gitignore-independent search
`TOGO:M1840\b|NBRC_M1074\b|NO=1074\b|TOGO_M1840_Alkaline_Agar|Alkaline Agar|alkaline_agar`
covered `data/normalized_yaml`, `data/merge_yaml`, and `data/import_tracking`.
It found this normalized TOGO M1840 source, this generated merge, a direct NBRC
repair for NBRC Medium 1074 at `data/normalized_yaml/bacterial/NBRC_1075.yaml`,
and separate JCM M318/M313 Alkaline Agar imports. The direct NBRC record shows a
more faithful parse of NBRC Medium 1074, but it is not the upstream owner of
this generated TOGO record.

## Evidence

- Supported: the identity, source, original source, and URL match TOGO M1840 and
  its NBRC 1074 metadata.
- Supported: TOGO M1840 and NBRC 1074 both list 10 g glucose, 5 g Bacto Peptone
  (Difco), 5 g Bacto Yeast Extract (Difco), 1 g KH2PO4, 0.2 g MgSO4 x 7H2O,
  15 g agar, 200 ml of 20% NaCl solution, 100 ml of 10% Na2CO3 solution, and
  700 ml distilled water.
- Supported: `CHEBI:15377` water, `CHEBI:31795` magnesium sulfate heptahydrate,
  `CHEBI:63036` potassium dihydrogen phosphate, `CHEBI:17234` glucose, and
  `CHEBI:2509` agar are reasonable ingredient groundings for the source rows.
- Not supported: distilled water is represented as `700 G_PER_L`. Both inspected
  sources express it as 700 ml, not as a 700 g/L solute.
- Not supported: the 20% NaCl and 10% Na2CO3 stock additions are represented as
  empty `solutions` with `200 G_PER_L` and `100 G_PER_L` concentrations. TOGO
  records them as 200 ml and 100 ml volume additions, while NBRC has the same
  volumes in its composition table.
- Missing: neither `preparation_steps` nor structured stock-addition metadata
  preserve the instruction to autoclave the NaCl and Na2CO3 solutions separately
  before aseptically adding them to the base medium.

## Completeness

- Consequential gaps are limited to liquid-quantity semantics, stock-solution
  structure, aseptic stock handling, and structured source provenance.
- Empty target-organism, temperature, pH, salinity, storage, and growth-evidence
  slots are acceptable for this import because neither inspected source page
  supplies organism growth claims or additional operating conditions for NBRC
  Medium 1074.
- The adjacent direct NBRC record at
  `data/normalized_yaml/bacterial/NBRC_1075.yaml` confirms that the current
  normalized corpus already contains a later repair capable of preserving the
  three liquid rows as `ML_PER_L` and splitting the autoclave/mix instruction
  into explicit steps.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The 20% NaCl and 10% Na2CO3 stock additions have been transformed from 200 ml and 100 ml volume additions into empty solution records with `G_PER_L` amounts. | TOGO M1840 and NBRC 1074 both encode the two rows as milliliter additions of 20% and 10% stock solutions; the generated record has `solutions` with empty `composition` arrays and concentrations of `200 G_PER_L` and `100 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml` or the TOGO solution migration/import repair that owns stock-solution lifting |
| Major | Distilled water is unit-normalized as `700 G_PER_L` instead of `700 ML_PER_L`. | TOGO M1840 and NBRC 1074 both list the water row as 700 ml; the record makes it a gram-per-liter concentration. | `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml` or the TOGO unit normalizer |
| Major | Required preparation semantics were dropped. | Both inspected source layers carry the instruction to autoclave NaCl and Na2CO3 stocks separately and add those solutions aseptically; the record has no `preparation_steps`. | `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml` |
| Minor | The cited TOGO and original NBRC sources exist only in a multiline `notes` string. | There is no structured `references` entry or evidence object even though the record depends on TOGO M1840 and NBRC 1074 for identity, composition, and the stock-preparation instruction. | `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/TOGO_M1840_Alkaline_Agar.yaml`, replace
   the two empty stock `solutions` with faithful stock-addition rows: 200
   `ML_PER_L` of 20% NaCl solution and 100 `ML_PER_L` of 10% Na2CO3 solution,
   or an equivalent schema-native stock-solution representation that preserves
   both the percent concentration and the added volume.
2. Change distilled water from `700 G_PER_L` to `700 ML_PER_L` in the maintained
   TOGO M1840 source.
3. Add preparation steps for separate autoclaving of the NaCl and Na2CO3 stock
   solutions and aseptic post-autoclave addition to the medium.
4. Promote the TOGO M1840 and NBRC Medium 1074 sources from free-text `notes`
   into structured `references` or per-claim evidence fields supported by the
   current schema.
5. Regenerate the merge layer so `data/merge_yaml/merged/ALKALINE_AGAR.yaml`
   inherits the repaired normalized source.

## Follow-up Checks

- Re-run open schema validation on the repaired normalized TOGO source and the
  regenerated `data/merge_yaml/merged/ALKALINE_AGAR.yaml`.
- Re-run `scripts/validate_strict.py` on both files with a clean temporary TSV
  and confirm that it emits 0 error rows.
- Re-run the term validator and verify that the existing ChEBI groundings remain
  accepted after the liquid rows are corrected.
- Re-run the reference validator after adding structured source links and
  confirm that both TOGO M1840 and NBRC Medium 1074 resolve.
- Manually compare the regenerated merge against TOGO M1840 and NBRC 1074 to
  confirm that 200 ml, 100 ml, and 700 ml remain volume additions and that the
  separate autoclave/aseptic-addition instruction is present.

## Additional Notes

- No previous `ALKALINE_AGAR` review report existed in
  `reports/yaml_record_review`; this was checked with `find`, which includes
  gitignored report files.
- `data/normalized_yaml/bacterial/TOGO_M313_Alkaline_Agar.yaml` and
  `data/normalized_yaml/bacterial/alkaline_agar.yaml` are JCM-derived Alkaline
  Agar records with similar solution-flattening defects, but they denote the JCM
  M318/M313 import path rather than NBRC M1074/TOGO M1840.
