# YAML Record Review: BCYE_AGAR

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/BCYE_AGAR.yaml
- Started UTC: 2026-09-21T19:17:00Z
- Finished UTC: 2026-09-21T19:20:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/BCYE_AGAR.yaml` |
| Maintained owner | Generated from multiple `data/normalized_yaml` records by `src/culturemech/merge/merge_recipes.py` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002367` |
| Name | `bcye_agar` |
| Original name | `BCYE AGAR` |
| Source identity in record | JCM Medium `J119`; `mediadive.medium:J119` |
| Merge status | Generated 15-source merge on fingerprint `4aa0cdc82922e6c9424793721864cf1b8ef0ef4c3d561c7c9037ae0dad41c7d4` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and `data/merge_yaml/merged/BCYE_AGAR.yaml`. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this stale generated merge has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- The generated record's ID, source term, and label say it denotes JCM Medium 119, `BCYE AGAR`.
- The inspected JCM 119 page lists BCYE agar from BD-Difco at 37 g, Legionella agar enrichment from BD-Difco at 100 ml, and distilled water at 900 ml, with JCM's default 121 C for 15 min autoclave instruction.
- The current normalized owner for JCM 119, `data/normalized_yaml/bacterial/JCM_J119_BCYE_AGAR.yaml`, now matches that JCM table and records the September `RESOLVED_JCM_J119_BCYE_AGAR` curation event.
- The inspected generated merge does not match JCM 119. It carries only `R2A agar` at 1.82 g/L and `Agar` at 15 g/L, which is the R2A wrapper composition present in `data/normalized_yaml/bacterial/1_10_r2a_agar.yaml`.
- The stored 15-way `merged_from` list combines the JCM BCYE import with unrelated R2A agar, corn meal agar, marine agar, and other commercial agar wrapper recipes. Current exact fingerprints for the JCM 119 BCYE owner, TOGO M2859 BCYE owner, JCM 804 R2A owner, JCM 1329 corn meal agar owner, and Marine Agar 2216 owner are all different.
- Exact gitignore-independent searches for `CultureMech:002367`, `CultureMech:009399`, `TOGO:M2859`, `M2859_BCYE`, `JCM_J119_BCYE_AGAR`, exact `mediadive.medium:J119`, and exact `GRMD=119` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the maintained BCYE source records, the stale generated merge, indexes, import reports, and repair scripts, but no repository-local raw JCM 119 or TOGO M2859 source dump.

## Evidence

- JCM 119 supports three components: 37 g BCYE agar from BD-Difco, 100 ml Legionella agar enrichment from BD-Difco, and 900 ml distilled water.
- Togo M2859 is a sparse usage record: its API says *L. pneumophila* strains Paris and Lens were grown on BCYE agar at 37 C for 3 d, and its only component row is 1 L `BCYE agar`; it does not disclose a commercial product or formulation.
- The generated target contains neither the JCM 119 components nor the Togo M2859 sparse 1 L BCYE agar row. Its ingredient set is R2A agar plus agar.
- The current normalized `JCM_J119_BCYE_AGAR.yaml` and `TOGO_M111_BCYE_Agar.yaml` already have the same JCM BCYE fingerprint, `b6eee2453f1b88dc4eecd6a97e171c329821ba2dfcaf45051180f83dc46b1d61`.
- The current normalized `TOGO_M2859_BCYE_agar.yaml` fingerprints as `93e8b8398b368944c7e9f7f273088e83bc08a526a4a6b5780e8a55a40905db7f`, and the sampled R2A, corn meal agar, and marine agar records also fingerprint separately. Their present normalized forms do not support the stale 15-way generated merge.

## Completeness

- Consequential gaps:
  - The generated recipe lost the current JCM 119 BCYE composition, its JCM reference, its preparation step, and its link to the TOGO M111 source duplicate.
  - The generated `merged_from` list falsely presents unrelated media as exact ingredient-set duplicates of JCM 119 BCYE AGAR.
  - Togo M2859 is a usage record for 1 L of unspecified BCYE agar and should not be exact-merged with the fully specified JCM 119 BCYE recipe.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent in this generated merge; no inspected source in this review supplies a CultureMech-ready strain-specific growth metric for JCM 119.
  - The complex commercial components in the current normalized JCM BCYE records are intentionally ungrounded rather than forced to narrow ChEBI small-molecule terms.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw JCM 119 HTML capture or raw Togo M2859 JSON dump under `data`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | `BCYE_AGAR.yaml` denotes the wrong formulation. | The record's ID and `media_term` point to JCM 119 BCYE AGAR, but its only ingredients are `R2A agar` and `Agar`; the inspected JCM 119 source lists BCYE agar, Legionella agar enrichment, and distilled water. | Regenerate `data/merge_yaml/merged` from current normalized owners with `just merge-recipes`; do not hand-edit this generated file. |
| Major | The 15-source merge is stale and no longer supported by the normalized corpus. | `BCYE_AGAR.yaml` still records an August 2026 15-way merge on `4aa0cdc...`; current normalized JCM BCYE, TOGO M2859 BCYE, R2A agar, corn meal agar, and marine agar source records all produce different fingerprints. | `src/culturemech/merge/merge_recipes.py` output over `data/normalized_yaml`; use `scripts/audit_merge_yaml_freshness.py` to measure and prove freshness. |
| Major | The generated record drops current curated JCM BCYE provenance and preparation. | `data/normalized_yaml/bacterial/JCM_J119_BCYE_AGAR.yaml` now has the JCM reference, three supported components, the default autoclave step, and a SOURCE_DUPLICATE child for TOGO M111; the generated merge predates those repairs. | Regenerate `data/merge_yaml/merged` after the normalized September BCYE repairs. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged` from current `data/normalized_yaml` with `just merge-recipes`, so the stale August `BCYE_AGAR.yaml` file is replaced by a fresh derivation.
2. Confirm the fresh `CultureMech:002367` BCYE merge contains only the exact JCM 119 source duplicates, currently `JCM_J119_BCYE_AGAR` and `TOGO_M111_BCYE_Agar`, under fingerprint `b6eee2453f1b88dc4eecd6a97e171c329821ba2dfcaf45051180f83dc46b1d61`.
3. Confirm `TOGO_M2859_BCYE_agar`, the R2A agar records, corn meal agar records, and marine agar records no longer appear in the same generated `merged_from` list as `JCM_J119_BCYE_AGAR`.
4. Keep the generated fix out of `data/normalized_yaml`; the normalized JCM 119 owner already matches the inspected JCM source for this record-level issue.

## Follow-up Checks

- Run `just verify-merges` after regenerating to prove every normalized source is represented in a merge output and every stored fingerprint recomputes.
- Run `just audit-merge-freshness --json --list` to confirm `data/merge_yaml/merged` is a current derivation of `data/normalized_yaml`.
- Re-run an exact gitignore-independent search for `JCM_J119_BCYE_AGAR`, `TOGO_M111_BCYE_Agar`, `TOGO_M2859_BCYE_agar`, `1_10_r2a_agar`, `JCM_J1329_CORN_MEAL_AGAR`, and `marine_agar_2216` under `data/merge_yaml/merged` and confirm they are not co-merged.
- Re-run open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on each fresh BCYE generated output.

## Additional Notes

- The inspected JCM source was the live `jcm_grmd?GRMD=119` HTML page.
- The inspected Togo source was the SPARQList `gmdb_medium_by_gmid?gm_id=M2859` API response.
- Exact gitignore-independent searches included ignored files where present.
