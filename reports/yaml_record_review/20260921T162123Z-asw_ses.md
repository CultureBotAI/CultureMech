# YAML Record Review: asw_ses

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/asw_ses.yaml
- Started UTC: 2026-09-21T16:19:21Z
- Finished UTC: 2026-09-21T16:21:23Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/asw_ses.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:000318` |
| Label | `asw_ses` |
| Original label | `ASW:SES` |
| Category | `bacterial` |
| Source identity | `mediadive.medium:C11`; CCAP `MR_ASW_SES.pdf` |
| Source URL | `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW_SES.pdf` |
| Generated status | Generated on 2026-08-06 from `data/normalized_yaml/bacterial/asw_ses.yaml` |
| Merge fingerprint | `f44a300319644e63c83100f759c0490426432cf9bf50c4b129b914dd0958967f` |

`data/merge_yaml/merged/` is derived output. Future fixes belong in
`data/normalized_yaml/bacterial/asw_ses.yaml`, the MediaDive/CCAP importer, or
a deliberate consolidation with the direct CCAP `ASW_SES` algae owner; then
this generated file should be regenerated.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/asw_ses.yaml` | Pass. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/asw_ses.yaml --out /private/tmp/asw_ses.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/asw_ses.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/asw_ses.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass. The only emitted message was the known `eutils`/`pkg_resources` deprecation warning. |
| Embedded curation history | Not checked | No focused validator for embedded `MediaRecipe.curation_history` is documented for one merged recipe. `just validate-history` targets standalone records under `history/`. |

Direct `just` validation was not used because this checkout currently reaches a
project `uv` build of `llvmlite==0.46.0` under Python 3.13 before target-specific
validation and fails in `setuptools` with `TypeError: Popen.__init__() got an
unexpected keyword argument 'dry_run'`.

## Identity and Grounding

- The reviewed merge is a single-source generated copy of
  `data/normalized_yaml/bacterial/asw_ses.yaml`, with ID `CultureMech:000318`
  and MediaDive/CCAP accession `mediadive.medium:C11`.
- `find data/normalized_yaml -name 'asw_ses.yaml' -print` found a same-stem
  direct CCAP owner at `data/normalized_yaml/algae/asw_ses.yaml`; the generated
  target does not merge that record.
- The CCAP PDF identifies ASW:SES as a marine-algae medium made as a 3:1
  mixture of two submedia, ASW and SES, before autoclaving. The record's CCAP
  identity is right, but the bacterial category and direct flattened
  ingredient list do not preserve that algae/submedium identity.
- Sodium nitrate, sodium phosphate, potassium phosphate, and the named vitamin
  identities in the bacterial owner are chemically plausible after the June
  2026 regrounding pass, but many of their amounts belong to ASW or SES stock
  solutions rather than to finished ASW:SES.

## Evidence

| Claim in generated record | Review |
|---|---|
| ASW:SES as a 3:1 mixture | Supported by the CCAP PDF header and first preparation lines. |
| Tricine at 0.50 g/L | Supported for the ASW submedium, not yet explicitly scaled or nested for the final 3:1 ASW:SES mixture. |
| `Natural sea water` at 1000 `G_PER_L` | Wrong unit and wrong scope. CCAP uses natural seawater to make ASW to 1 litre; ASW is then only the 3-part side of the ASW:SES mixture. |
| NaNO3, Na2HPO4, and K2HPO4 at 30, 1.2, and 21.0 g/L | Not supported as final ASW:SES rows. The first two are ASW Extra salts stock ingredients, while the 21.0 g/L K2HPO4 row sums one ASW stock ingredient with a SES stock addition. |
| Biotin, Calcium pantothenate, Cyanocobalamin, Folic acid, Inositol, Nicotinic acid, Thiamine HCl, and Thymine as direct g/L rows | Not supported as direct final rows. CCAP lists them in the ASW Vitamin solution stock, which is added to ASW at 2.5 cm3 per litre before ASW is mixed 3:1 with SES. |
| MgSO4 x 7 H2O and KNO3 at 20 g/L | Not supported. CCAP lists MgSO4.7H2O at 1.0 g/L and KNO3 at 10.0 g/L as separate SES stocks; 20.0 ml of each stock is added per litre of SES. |
| `Soil` with `VARIABLE` concentration | Underspecified. CCAP defines a SE1 Soil Extract preparation and adds it to ASW and SES at stated volumes; raw soil is not a variable direct ASW:SES ingredient. |
| ASW, SES, and SE1 instructions as one top-level preparation sequence | Partially supported text, wrong boundary. The source uses separate ASW, SES, and SE1 recipes, then mixes ASW and SES; the generated record has no structured boundary for those subrecipes. |

## Completeness

- An ignored-file-inclusive exact search for `asw_ses`, `ASW_SES`, `ASW SES`,
  `CultureMech:000318`, `CCAP:ASW_SES`, and `MR_ASW_SES` across `data`,
  `reports`, `history`, and `.claude` found the bacterial MediaDive/CCAP owner,
  the direct algae CCAP owner, generated/index records, media-variant proposals,
  source-mapping reports, and import QC reports.
- `find reports/yaml_record_review -maxdepth 1 -name '*asw_ses.md'` searched
  the ignored report directory independently of `.gitignore` and found no prior
  review for this exact generated record stem before this report was written.
- The source needs a hierarchical representation: finished ASW:SES as a 3:1
  mixture, ASW with Extra salts, Vitamin solution, and SE1 additions, SES with
  three stock additions and SE1, and SE1 as its own soil-extract preparation.
  The current direct ingredient list cannot encode the stock volumes, final
  dilution, or duplicate K2HPO4 provenance correctly.
- Empty `target_organisms` and `growth_metrics` are acceptable for this
  source-formulation record.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | ASW:SES is flattened across multiple recipe layers into a single direct ingredient list. | The CCAP source makes ASW:SES from separate ASW and SES parts; ASW itself contains Extra salts, Vitamin solution, and SE1 stocks, and SES contains three salt stocks plus SE1. | `data/normalized_yaml/bacterial/asw_ses.yaml`, the CCAP/MediaDive importer, or a consolidation with `data/normalized_yaml/algae/asw_ses.yaml`. |
| major | K2HPO4 is summed across unrelated stock contexts, and SES salt-stock volumes are imported as final g/L amounts. | The generated K2HPO4 row has `21.0 G_PER_L` with a duplicate-merge note, while CCAP has 1.0 g/L K2HPO4 inside ASW Extra salts and 20.0 ml/L of a 1.0 g/L K2HPO4 stock inside SES. | Same maintained bacterial owner or importer. |
| major | ASW water and SE1 Soil Extract are represented as direct ingredients with incorrect quantities. | `Natural sea water` is 1000 `G_PER_L`, but CCAP uses a 1 litre make-up volume inside ASW; `Soil` is a variable direct ingredient, but CCAP uses prepared SE1 stock at stated ASW and SES addition volumes. | Same maintained bacterial owner or importer. |
| major | Preparation steps do not preserve the separate ASW, SES, and SE1 preparation boundaries. | The generated record copies ASW, SES, and SE1 preparation prose into one top-level sequence and has no nested stock or submedium structure. | Same maintained bacterial owner or importer. |

## Recommended Edits

1. Decide whether the bacterial MediaDive/CCAP C11 record and the direct algae
   CCAP `ASW_SES` record should be merged, deduplicated, or retained as source
   variants before repairing generated ASW:SES output.
2. Re-model `data/normalized_yaml/bacterial/asw_ses.yaml` with explicit ASW,
   SES, Extra salts, Vitamin solution, SES salt stocks, and SE1 boundaries
   instead of stock ingredients copied into the main `ingredients` list.
3. Preserve the source mixture as 3 parts ASW plus 1 part SES rather than
   presenting ASW submedium ingredients at 100% final-medium strength.
4. Replace `Soil` with a Soil Extract stock representation and keep the SE1 soil
   collection, autoclaving, decanting, filtering, aliquoting, and storage
   details on that stock preparation.
5. Regenerate `data/merge_yaml/merged/asw_ses.yaml` after the normalized owner
   and any source-specific importer rule are repaired.

## Follow-up Checks

- Rerun open-schema, strict, term, and reference validation on the normalized
  ASW:SES owner after correction.
- Rerun the concentration plausibility audit and confirm this record no longer
  emits `WATER_AS_VOLUME` for natural seawater or `INDICATOR_UNIT_SLIP` for
  Thiamine HCl.
- Verify by hand that regenerated K2HPO4 is not the sum of unrelated stock
  rows.
- Regenerate the merged ASW:SES record and confirm ASW, SES, SE1, and the
  subordinate stock recipes remain separated.

## Additional Notes

- `data/import_tracking/reports/concentration_plausibility.tsv` already flags
  `Natural sea water` and `Thiamine HCl` in the bacterial owner, which is
  consistent with the volume-as-concentration and stock-flattening defects
  found against the CCAP source.
