# YAML Record Review: 1% Tryptone Agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_tryptone_agar__cfec473e.yaml`
- Started UTC: 2026-09-21T05:55:45Z
- Finished UTC: 2026-09-21T05:56:28Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:008412` |
| Name | `1_tryptone_agar` |
| Original name | `1% Tryptone Agar` |
| Category | `bacterial` |
| Source term | `TOGO:M1839`, `1% Tryptone Agar` |
| Original source | `NBRC_M1072` |
| Merge fingerprint | `cfec473ed110b01c920b767ec478fcb2e2d1c205d98b45c55c2e7eb723f23109` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M1839_1_Tryptone_Agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO Medium M1839 mirror of NBRC Medium 1072. The maintained owner was repaired on 2026-09-11, so this generated artifact is stale relative to the source record that owns future fixes.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_tryptone_agar__cfec473e.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_tryptone_agar__cfec473e.yaml --out /private/tmp/1_tryptone_agar__cfec473e.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_tryptone_agar__cfec473e.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_tryptone_agar__cfec473e.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes TOGO Medium M1839:

- The YAML carries `media_term.term.id: TOGO:M1839` and `media_term.term.label: 1% Tryptone Agar`.
- Its free-text notes preserve the TOGO URL, `NBRC_M1072` original source accession, and NBRC Medium 1072 URL.
- The inspected NBRC Medium 1072 page reports `1% Tryptone Agar` with the same three component rows and pH 7.0.
- The inspected TOGO M1839 API payload reports `original_media_id: NBRC_M1072`, the same NBRC URL, the same component rows, and pH 7.0.

The generated record maps distilled water to CHEBI but leaves Bacto Agar ungrounded. The maintained owner now grounds Bacto Agar to `CHEBI:2509` and carries a broader `MICRO:0000182` Tryptone term for the Bacto Tryptone row.

## Evidence

The inspected NBRC Medium 1072 page and TOGO M1839 API payload support:

- 10 g Bacto Tryptone (Difco).
- 15 g Bacto Agar.
- 1 L Distilled water.
- pH 7.0.

The generated TOGO artifact preserves the three component labels and the 10 g / 15 g masses. It imports the 1 L water row as `1 G_PER_L`, omits pH 7.0, leaves Bacto Agar ungrounded, and lacks the maintained owner's structured TOGO/NBRC references.

## Completeness

Consequential gaps:

- The 1 L distilled-water row is present but assigned the wrong unit.
- pH 7.0 is absent.
- Bacto Agar is no longer grounded in the generated record.
- The generated record has only free-text TOGO/NBRC URLs; the maintained owner's structured references are stale-missing.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected NBRC and TOGO sources verify the recipe but do not support a target-organism assertion.
- No sterilization or incubation-temperature row is expected. NBRC Medium 1072 and TOGO M1839 did not state either value.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over the TOGO M1839 normalized owner, its generated merge, TOGO source-index JSON, and `reports/yaml_record_review` for `CultureMech:008412`, `TOGO:M1839`, `NBRC_M1072`, `NO=1072`, and the `cfec473e` merge fingerprint found the expected normalized owner, expected generated merge, and source index row.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact is stale and preserves the imported water-unit artifact. | NBRC Medium 1072 and TOGO M1839 list 1 L Distilled water, and `data/normalized_yaml/bacterial/TOGO_M1839_1_Tryptone_Agar.yaml` now stores `1.0 L`; the generated merge still stores `1 G_PER_L`. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M1839_1_Tryptone_Agar.yaml` |
| Major | The generated artifact is stale and omits pH 7.0. | NBRC Medium 1072 and TOGO M1839 both state pH 7.0; the maintained owner now has `ph_value: 7.0` and an `ADJUST_PH` step, while the generated merge has no pH or preparation steps. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M1839_1_Tryptone_Agar.yaml` |
| Minor | Bacto Agar grounding and structured references are stale-missing. | The maintained owner grounds Bacto Agar to `CHEBI:2509` and has structured TOGO/NBRC references; the generated merge leaves Bacto Agar ungrounded and has only free-text `notes`, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M1839_1_Tryptone_Agar.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `1_tryptone_agar__cfec473e.yaml` receives the maintained owner's corrected water unit, pH, Bacto Agar grounding, structured references, and data-quality flags.
2. Keep TOGO M1839 separate from the JCM Medium 359 pair unless a future review explicitly establishes that NBRC Medium 1072 is the same source formulation rather than a same-name same-formula recipe from another collection.

## Follow-up Checks

- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- `just validate-strict` and `just validate-terms` on the regenerated TOGO M1839 merge.
- `just validate-references` on the regenerated merge to confirm the structured TOGO/NBRC references resolve.
- Manual comparison against NBRC Medium 1072 and TOGO M1839 for 10 g Bacto Tryptone (Difco), 15 g Bacto Agar, 1 L Distilled water, and pH 7.0.

## Additional Notes

- The maintained normalized TOGO M1839 owner is already repaired; no additional normalized edit was identified for this TOGO/NBRC record in this review.
- TOGO M1839 imports NBRC Medium 1072, not JCM Medium 359. The direct JCM record and TOGO M353 are the source-duplicate pair for JCM Medium 359.
- `linkml-reference-validator` performed zero checks because this generated record has no structured `references` entries.
