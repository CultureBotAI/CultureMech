# YAML Record Review: TOGO Medium M1991

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1991.yaml
- Started UTC: 2026-09-25T15:14:58Z
- Finished UTC: 2026-09-25T15:15:37Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/togo_medium_m1991.yaml`
- Class: `MediaRecipe`
- Stable ID: `CultureMech:008575`
- Label: `TOGO Medium M1991`
- Source accession: TOGO M1991 importing NBRC Medium 1277
- Generated status: derived merge artifact with `merged_from: togo_medium_m1991`; future source edits belong in `data/normalized_yaml/bacterial/togo_medium_m1991.yaml`, followed by rerunning `src/culturemech/merge/merge_recipes.py`

## Validation

| Check | Result |
|---|---|
| LinkML schema validation on the generated record with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` through Python 3.11 `uv --no-project --offline` | Passed; `No issues found` |
| Strict schema validation with `scripts/validate_strict.py data/merge_yaml/merged/togo_medium_m1991.yaml --out /private/tmp/m1991.strict.tsv --workers 1 --quiet` | Passed; 1-line TSV containing only the header and 0 error rows |
| Reference validation with `linkml-reference-validator validate data data/merge_yaml/merged/togo_medium_m1991.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks and all validations passed |
| Term validation with `linkml-term-validator validate-data data/merge_yaml/merged/togo_medium_m1991.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` `pkg_resources` deprecation warning |
| Embedded curation-history validation | Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` blocks embedded in merged YAML |

## Identity and Grounding

The record identity is internally consistent: `CultureMech:008575`, `name: togo_medium_m1991`, the `TOGO:M1991` media term, and the source notes all identify TOGO M1991 / NBRC Medium 1277. The inspected TOGO M1991 payload reports `original=NBRC_M1277` and pH 7.2; the fetched NBRC page for `NO=1277` contains the same ingredient set and pH.

Groundings for MgSO4 x 7H2O, K2HPO4, sodium pyruvate, glucose, water, and NaCl-like core salts are label-compatible where present. The four complex Difco or peptone ingredients are intentionally left ungrounded rather than forced to a narrow CHEBI term.

## Evidence

The generated ingredients agree with the TOGO/NBRC component list for MgSO4 x 7H2O, K2HPO4, sodium pyruvate, glucose, soluble starch, Bacto Yeast Extract, Bacto Tryptone, Soytone Peptone, and Hipolypepton amounts.

Two source claims are not represented faithfully:

- The source has 1 L distilled water; the record encodes it as `1 G_PER_L`.
- The source specifies pH 7.2; the record has no `ph_value` or preparation step carrying that adjustment.

## Completeness

There are no stock solutions in NBRC Medium 1277, and the generated record correctly has no `solutions` block. No target-organism or growth claims are asserted, so those empty optional fields are acceptable.

The record is incomplete for pH and water units. With only `1 G_PER_L` water, the structured composition is not directly usable as a recipe for 1 L final medium.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is imported as a mass concentration instead of the source volume. | TOGO/NBRC list `Distilled water 1 L`; the generated record stores `value: 1`, `unit: G_PER_L`. | `data/normalized_yaml/bacterial/togo_medium_m1991.yaml` |
| Major | The pH 7.2 condition is absent from structured fields. | TOGO M1991 and NBRC Medium 1277 both report pH 7.2, but the record has no `ph_value` or pH preparation step. | `data/normalized_yaml/bacterial/togo_medium_m1991.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/togo_medium_m1991.yaml`, correct distilled water to 1 L of water for the batch recipe instead of `1 G_PER_L`.
2. Add structured pH 7.2 information, either as `ph_value: 7.2` and/or a preparation step consistent with the repository's current pH representation.
3. Regenerate `data/merge_yaml/merged/togo_medium_m1991.yaml` through `src/culturemech/merge/merge_recipes.py`.

## Follow-up Checks

- Rerun schema, strict, reference, and term validation for `data/normalized_yaml/bacterial/togo_medium_m1991.yaml` and the regenerated merge YAML.
- Recompare the regenerated ingredients against NBRC Medium 1277 to ensure no optional solution or stock block was introduced.
- Check `git diff --check` before committing.

## Additional Notes

This review used TOGO M1991 and NBRC Medium 1277 as inspected source records. Searches used to resolve the normalized owner and the generated merge entry were exact-ID searches with `--no-ignore --hidden`, so ignored files were included.
