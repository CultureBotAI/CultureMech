# YAML Record Review: 1_2_ytss_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml`
- Started UTC: 20260921T051305Z
- Finished UTC: 20260921T051400Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:002156` |
| Slug | `1_2_ytss_medium` |
| Original name | `1/2 YTSS MEDIUM` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact |
| Maintained owners | `data/normalized_yaml/bacterial/1_2_ytss_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_974_1_2_YTSS_medium.yaml` |
| Source accessions | `mediadive.medium:974`, `komodo.medium:974` |
| Merge fingerprint | `b0a9567b5e7c0be8562ea6be2c7ee2592b821b80f7ff5ea312a6f27c48ac6ce3` |

Reviewed the generated merge for the DSMZ Medium 974 import and the KOMODO copy of the same DSMZ source. A gitignore-independent exact search for both CultureMech IDs, both source accessions, the merge fingerprint, and the DSMZ source name covered the normalized YAML, generated YAML, raw/import/reference trees, and recipe catalogs; it found only these two maintained owners, the generated merge artifact, and the expected catalog/index/inventory rows for this duplicate pair.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml` | Passed with no output |
| Strict validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml --out /private/tmp/1_2_YTSS_MEDIUM.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 `ERROR` rows |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 checks |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded `curation_history` | Not checked | Not checked: no focused validator is documented for generated merge-file `MediaRecipe.curation_history`; the repository's `validate-history` target validates standalone history files under `history/`. |

The documented `just` validator wrappers remain blocked by the project `llvmlite==0.46.0` build under Python 3.13; the no-project `uv` commands above were used as focused equivalents.

## Identity and Grounding

- DSMZ Medium 974 is exactly `1/2 YTSS MEDIUM`, and the KOMODO owner states DSMZ Medium 974 provenance with the same ingredient signature. The `SOURCE_DUPLICATE` parent/child relationship is appropriate.
- The inspected DSMZ PDF supports 2 g/L yeast extract, 1.25 g/L tryptone, 20 g/L Sea salts (Sigma), 1000 ml/L distilled water, final pH 7.0, and 15 g/L agar for solid medium.
- The generated merge and both maintained owners omit the 1000 ml distilled-water row.
- The generated merge is stale relative to both Sep 13 normalized owners: `data/normalized_yaml/bacterial/1_2_ytss_medium.yaml` and `KOMODO_974_1_2_YTSS_medium.yaml` now have exact FOODON/MICRO terms for yeast extract and tryptone, but `data/merge_yaml/merged/1_2_YTSS_MEDIUM.yaml` does not.
- The sea-salt row is intentionally ungrounded. The local ingredient label index has only broader or unresolved mappings for sea salt/Sea Salt, and no inspected exact term was found for DSMZ's `Sea salts (Sigma)`.

## Evidence

- The DSMZ Medium 974 PDF was inspected directly and lists the YTSS component quantities, final pH, and solid-medium agar addition.
- The local label index maps `Yeast extract` to `FOODON:03315426` and `Tryptone` to `MICRO:0000182`, matching the Sep 13 repairs in the two normalized owners.
- No source-specific sterilization or incubation condition is present in the DSMZ Medium 974 PDF; those optional fields can remain empty.

## Completeness

- Consequential gap: the 1000 ml distilled-water component is missing.
- Consequential gap: the generated merge lacks the exact extract groundings present in both maintained owners.
- Non-blocking provenance gap: the DSMZ PDF URL is only free text in top-level `notes`; no structured `references` row exists.
- Non-blocking label gap: `Sea Salt` should preserve the source qualifier `Sea salts (Sigma)` in the preferred term or source note.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is missing from the DSMZ and KOMODO 1/2 YTSS records. | DSMZ Medium 974 lists 1000 ml distilled water, but neither maintained owner nor the generated merge has a water row. | `data/normalized_yaml/bacterial/1_2_ytss_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_974_1_2_YTSS_medium.yaml` |
| Major | The generated merge is stale relative to the maintained owners. | The Sep 13 repairs added exact `FOODON:03315426` and `MICRO:0000182` terms to both normalized owners; the generated Aug 6 merge still has ungrounded yeast extract and tryptone rows. | Regenerate `data/merge_yaml/merged/` from normalized YAML. |
| Minor | The DSMZ source URL is not structured. | The PDF URL appears only in `notes`, and reference validation runs 0 checks. | `data/normalized_yaml/bacterial/1_2_ytss_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_974_1_2_YTSS_medium.yaml` |
| Minor | The sea-salts supplier detail was dropped. | DSMZ specifies `Sea salts (Sigma)`; CultureMech stores only `Sea Salt`. | `data/normalized_yaml/bacterial/1_2_ytss_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_974_1_2_YTSS_medium.yaml` |

## Recommended Edits

1. Add the DSMZ `Distilled water` row to both maintained owners as `1000 ML_PER_L`.
2. Add a structured reference for `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium974.pdf`.
3. Preserve `Sea salts (Sigma)` as the source label or as a source note while leaving the row ungrounded unless an exact salt-mix term is selected.
4. Regenerate the merge layer so `1_2_YTSS_MEDIUM.yaml` inherits the Sep 13 yeast-extract and tryptone groundings plus the new water/reference corrections.

## Follow-up Checks

- Re-run strict, term, and reference validators for both maintained owners after curation.
- Re-run merge generation and verify the generated record contains the water row, exact yeast/tryptone terms, and structured DSMZ provenance.
- Re-run duplicate checks to confirm the DSMZ and KOMODO owners remain a single `SOURCE_DUPLICATE` merge after adding water.

## Additional Notes

- The record correctly represents DSMZ's optional solid formulation by including the 15 g/L agar row and `physical_state: SOLID_AGAR`.
- The KOMODO record has no independent formulation claim beyond copying DSMZ Medium 974, so it should stay aligned with the DSMZ owner.
