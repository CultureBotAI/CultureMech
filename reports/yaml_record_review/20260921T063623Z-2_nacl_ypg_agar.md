# YAML Record Review: 2_nacl_ypg_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2_nacl_ypg_agar.yaml`
- Started UTC: 2026-09-21T06:35:26Z
- Finished UTC: 2026-09-21T06:36:23Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:010177` |
| Name | `2_nacl_ypg_agar` |
| Original name | `2% NaCl-YPG Agar` |
| Category | `bacterial` |
| Source term | `TOGO:M76`, `2% NaCl-YPG Agar` |
| Source URL | `https://togomedium.org/medium/M76`; notes also name `JCM_M85` and `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=85` |
| Merge fingerprint | `20bacdb0c0f983cdec122d200bd4b7284775b9a5ecbc0fb3f36f755d5ddd8521` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO M76 import, which its own notes describe as sourced from JCM Medium 85. Future corrections belong in `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2_nacl_ypg_agar.yaml` | Pass: `No issues found` |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2_nacl_ypg_agar.yaml --out /private/tmp/2_nacl_ypg_agar.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2_nacl_ypg_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2_nacl_ypg_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M76`, the label is `2% NaCl-YPG Agar`, the live TOGO API payload identifies M76 with the same label, and its metadata names `JCM_M85` plus the JCM GRMD 85 URL. The inspected JCM page resolves GRMD 85 to `2% NaCl-YPG AGAR`.

Ingredient identity is sound. Distilled water, yeast extract, NaCl, glucose, agar, and peptone all appear in the JCM source and TOGO payload, and the CHEBI groundings for water, sodium chloride, glucose, and agar are chemically compatible with those labels.

## Evidence

The inspected JCM Medium 85 page and live TOGO M76 API payload both support this formulation:

- 20 g NaCl.
- 20 g glucose.
- 10 g peptone.
- 10 g yeast extract.
- 20 g agar.
- 1 L distilled water.
- pH adjusted to 5.2-5.4.
- A JCM source-wide default to sterilize by autoclaving at 121 C for 15 minutes unless otherwise stated.

The generated target preserves the five dry components and the water row. It stores 1 L distilled water as `1 G_PER_L`, however, and omits both the pH 5.2-5.4 instruction and the JCM default autoclaving instruction.

## Completeness

Consequential gaps:

- The distilled-water amount has the wrong dimension.
- The pH 5.2-5.4 adjustment is missing from the generated TOGO record.
- The JCM default autoclaving instruction is missing from the generated TOGO record.
- TOGO M76 duplicates `data/normalized_yaml/bacterial/2_nacl_ypg_agar.yaml`, the MediaDive/JCM J85 import from the same JCM original source, but neither record has a reciprocal `SOURCE_DUPLICATE` link.
- The record has no structured `references`, so neither the TOGO M76 nor JCM 85 URL is validator-visible.

Empty optional slots:

- `solutions` is correctly absent. The inspected JCM recipe is a flat one-part formulation.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM and TOGO sources support a formulation, not organism-specific growth evidence.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2_nacl_ypg_agar.md` report before this report was created.
- A gitignore-independent `find` under `data/normalized_yaml` for `*2_nacl_ypg_agar*` found the same-name MediaDive/JCM J85 import at `data/normalized_yaml/bacterial/2_nacl_ypg_agar.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, and `data/raw` for `CultureMech:010177`, exact TOGO M76/JCM 85 source tokens, the generated fingerprint, and `2% NaCl-YPG Agar` found the expected TOGO M76 normalized/generated records, the JCM J85 same-source record, and derived indexes/manifests/archive rows. Ignored files were included under the searched paths. Short JCM 85 tokens also match unrelated JCM 850-859 records, so those longer IDs were ignored.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is represented with the wrong dimension. | JCM Medium 85 and live TOGO M76 list 1 L distilled water. The generated and normalized TOGO M76 records store `Distilled water` as `1 G_PER_L`, turning a final solvent volume into a 1 g/L mass concentration. | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` |
| Major | pH 5.2-5.4 is missing. | JCM Medium 85 says to adjust pH to 5.2-5.4, and the live TOGO M76 payload carries the same pH range. The generated and normalized TOGO M76 records have no `ph_value`, `ph_range`, or pH adjustment step. | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` |
| Major | The JCM default autoclaving instruction is missing. | JCM states that media should be autoclaved at 121 C for 15 min unless otherwise stated; the generated and normalized TOGO M76 records have no preparation steps. | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` |
| Major | TOGO M76 duplicates MediaDive/JCM J85 but is not linked to it. | TOGO M76 names `JCM_M85` and GRMD 85 as its original source; `data/normalized_yaml/bacterial/2_nacl_ypg_agar.yaml` already represents JCM J85. Neither record has a reciprocal `SOURCE_DUPLICATE` link. | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` and `data/normalized_yaml/bacterial/2_nacl_ypg_agar.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO and JCM URLs only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` |

## Recommended Edits

1. Correct the distilled-water row in `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` so it represents the source's 1 L solvent volume rather than `1 G_PER_L`.
2. Add `ph_range: {min: 5.2, max: 5.4}` or the schema-equivalent pH representation, plus the pH adjustment instruction.
3. Add a JCM-supported preparation step for autoclaving at 121 C for 15 minutes.
4. Add structured references for `https://togomedium.org/medium/M76`, `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M76`, and `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=85`.
5. Add reciprocal `SOURCE_DUPLICATE` links between `data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` and `data/normalized_yaml/bacterial/2_nacl_ypg_agar.yaml`.
6. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` after correcting distilled water.
- `just validate-references data/normalized_yaml/bacterial/TOGO_M76_2_NaCl-YPG_Agar.yaml` after adding structured TOGO and JCM references.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 85 and TOGO M76 for 20 g/L NaCl, 20 g/L glucose, 10 g/L peptone, 10 g/L yeast extract, 20 g/L agar, 1 L distilled water, pH 5.2-5.4, and default autoclaving.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
