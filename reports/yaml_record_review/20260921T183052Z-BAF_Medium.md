# YAML Record Review: BAF Medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/BAF_Medium.yaml`
- Started UTC: 2026-09-21T18:29:40Z
- Finished UTC: 2026-09-21T18:30:52Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:005176` |
| Merged record | `data/merge_yaml/merged/BAF_Medium.yaml` |
| Maintained owners | `data/normalized_yaml/bacterial/KOMODO_392_BAF_medium.yaml`; `data/normalized_yaml/bacterial/baf_medium.yaml` |
| Source identities | KOMODO `komodo.medium:392`; DSMZ / MediaDive `mediadive.medium:392` |
| Merge status | Generated from two normalized source-duplicate recipes |

The reviewed record is the generated duplicate merge for BAF Medium. Its canonical ID comes from the KOMODO 392 import, and the merge links `data/normalized_yaml/bacterial/baf_medium.yaml` as a DSMZ Medium 392 source duplicate. An ignored-inclusive search of `reports/yaml_record_review` for `BAF_Medium`, `BAF Medium`, `KOMODO_392_BAF_medium`, `CultureMech:005176`, and `CultureMech:001499` found no prior review report.

## Validation

| Check | Result |
| --- | --- |
| Open schema | Passed with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BAF_Medium.yaml`. |
| Strict schema | Passed with `scripts/validate_strict.py`; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Reference snippets | Passed with `linkml-reference-validator validate data ...`; 1 file validated, 0 snippet checks, all validations passed. |
| Term labels | Passed with `linkml-term-validator validate-data ... --labels -c conf/oak_config.yaml`. |
| Embedded curation history | Not checked: no documented focused validator exists for embedded `MediaRecipe.curation_history` on one generated merge record; `just validate-history` targets standalone history files. |

The direct `just` validators remain unavailable in this checkout because project `uv` tries to build `llvmlite==0.46.0` under Python 3.13 and exits inside `setuptools`. I used the no-project Python 3.11 validator workaround for the focused schema, strict, reference, and term checks above.

## Identity and Grounding

The merge correctly represents the local conclusion that KOMODO Medium 392 and DSMZ Medium 392 are source duplicates of BAF Medium. Both normalized source owners have the same solid-agar physical state, pH 5.8-6.3 range, and fourteen solute or gelling-agent ingredients.

The DSMZ Medium 392 PDF supports the title "BAF Medium" and the imported formula: 30 g/L glucose, 2 g/L peptone, 0.2 g/L yeast extract, 0.5 g/L KH2PO4, 0.5 g/L MgSO4 x 7 H2O, 10 mg/L FeCl3 x 6 H2O, 1 mg/L ZnSO4 x 7 H2O, 5 mg/L MnSO4, 100 mg/L CaCl2 x 2 H2O, 50 ug/L thiamin HCl, 1 ug/L biotin, 100 ug/L folic acid, 50 ug/L inositol, and 15 g/L agar. The generated g/L conversions for the mg and ug rows are arithmetically correct.

All exact-form salts, glucose, vitamins, inositol, and agar are grounded consistently with exact or specific local MIM rows. `Peptone` and `Yeast extract` remain ungrounded even though `src/culturemech/data/mediaingredientmech/label_index.csv` has exact rows for `Peptone` to `MICRO:0000178` and `Yeast extract` to `FOODON:03315426`.

## Evidence

The DSMZ recipe source supports every present ingredient amount, pH 5.8-6.3, and the `SOLID_AGAR` state implied by 15 g/L agar. It also specifies 1000 ml distilled water, which neither normalized source owner nor the generated merge includes.

The generated merge omits the DSMZ parent's preparation step. The inspected DSMZ Medium 392 PDF says only `pH 5.8 - 6.3.` after the distilled-water line, so the omission of the parent's combined pH/autoclave `HEAT` step does not remove any inspected sterilization instruction from the generated target.

The record has no target-organism or growth-evidence claims, so no organism assertion is over-generalized from the DSMZ formulation.

## Completeness

The formulation is materially incomplete because the 1000 ml distilled-water row is absent from both maintained owners. Peptone and yeast extract are the only ungrounded formula rows that can be resolved by exact packaged MIM labels.

The absent target-organism, atmosphere, storage, and evidence-reference fields are optional and not automatically defects for this source-only medium record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | DSMZ's 1000 ml `Distilled water` row is missing from both duplicate owners and the generated merge. | The inspected DSMZ Medium 392 PDF lists 1000 ml distilled water after 15 g agar; `KOMODO_392_BAF_medium.yaml`, `baf_medium.yaml`, and `BAF_Medium.yaml` all stop the formula at agar. | `data/normalized_yaml/bacterial/KOMODO_392_BAF_medium.yaml`; `data/normalized_yaml/bacterial/baf_medium.yaml` |
| Major | `Peptone` and `Yeast extract` are left ungrounded despite exact local MIM mappings. | The two rows have only `preferred_term` and concentration in both normalized owners; the packaged MIM label index has exact `Peptone` and `Yeast extract` rows. | `data/normalized_yaml/bacterial/KOMODO_392_BAF_medium.yaml`; `data/normalized_yaml/bacterial/baf_medium.yaml` |

## Recommended Edits

1. Add 1000 ml distilled water to both `data/normalized_yaml/bacterial/KOMODO_392_BAF_medium.yaml` and `data/normalized_yaml/bacterial/baf_medium.yaml`, then regenerate the duplicate merge.
2. Ground `Peptone` to `MICRO:0000178` and `Yeast extract` to `FOODON:03315426` in both normalized owners.
3. Revisit `data/normalized_yaml/bacterial/baf_medium.yaml`'s `HEAT` preparation step during the same curation pass. The pH range is supported by DSMZ 392; the embedded `Sterilize at 121 C for 15 min.` text was not present in the inspected PDF and should either be sourced separately or removed before a future merge can reselect it.

## Follow-up Checks

1. Re-run the focused schema, strict, term, and reference validators on `data/merge_yaml/merged/BAF_Medium.yaml` after regeneration.
2. Re-open DSMZ Medium 392 and confirm the regenerated merge has exactly the fourteen DSMZ solutes or gelling agents, 1000 ml distilled water, pH 5.8-6.3, and no unsupported sterilization instruction.
3. Verify `verify-merges` or `audit-merge-freshness` so the two normalized duplicate owners still merge to one canonical `BAF_Medium.yaml` after the water and grounding fixes.

## Additional Notes

The exact ignored-inclusive report search covered `reports/yaml_record_review`. A broader ignored-inclusive search across repository data and scripts found the two live normalized owners, the generated merge, index rows, registry rows, and unrelated NBAF records whose names contain `baf`; the unrelated NBAF records were not reviewed here.
