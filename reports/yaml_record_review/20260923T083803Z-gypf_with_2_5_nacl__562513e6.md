# YAML Record Review: GYPF WITH 2.5% NaCl

- Repository: CultureMech
- Record: data/merge_yaml/merged/gypf_with_2_5_nacl__562513e6.yaml
- Started UTC: 2026-09-23T08:36:48Z
- Finished UTC: 2026-09-23T08:38:03Z
- Verdict: pass with minor issues

## Target

- Reviewed generated YAML for `GYPF WITH 2.5% NaCl`.
- Stable ID: `CultureMech:003142`.
- Primary source in generated record: direct JCM/MediaDive `mediadive.medium:J798`.
- Merge fingerprint: `562513e6d61d509bb9535bc7bee67c8e14031f29b0dbc57f9ab0ebf9c9cd7fd6`.

## Validation

- LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`: pass.
- Strict validation with `scripts/validate_strict.py`: pass, 0 error rows in `/private/tmp/gypf_with_2_5_nacl_562513e6.strict.tsv`.
- LinkML reference validation: pass, 0 checked references.
- LinkML term validation with `conf/oak_config.yaml`: pass.
- Embedded `curation_history`: Not checked: `just validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

- The generated record is the direct JCM/MediaDive import for medium `J798`, with medium term `mediadive.medium:J798`.
- The title, pH 8.5, and GYPF with 2.5 percent NaCl ingredient signature identify the same recipe as the separate Togo/JCM record `TOGO:M833` / `JCM_M798`.
- An exact `rg --no-ignore --hidden` search for `TOGO:M833`, `JCM_M798`, `GRMD=798`, and `mediadive.medium:J798` found the expected direct JCM/MediaDive source, the Togo source, their index entries, and no unrelated same-source duplicate in `data/merge_yaml` or `data/normalized_yaml`.

## Evidence

- Generated formula: glucose 10 g/L, yeast extract 5 g/L, Bacto peptone 5 g/L, K2HPO4 1 g/L, NaCl 25 g/L, agar 15 g/L, pH 8.5.
- The Togo `M833` API record carries the same JCM `M798` origin and the same gram-per-liter recipe, with the same instruction to adjust pH to 8.5.
- The generated preparation step preserves the pH adjustment.
- The live JCM `GRMD=798` endpoint returned `Nothing found`; the generated direct MediaDive import and the Togo API record were sufficient to cross-check the record.

## Completeness

- Required source fields, medium identity, concentrations, units, pH, preparation step, application, and generated merge metadata are present.
- `Yeast extract` and `Bacto peptone` are expected complex ingredients and do not yet have primary ChEBI terms or MediaIngredientMech ChEBI links in this generated record.
- No target-organism assertions are present; none were evident in the generated source record.

## Findings

- Minor: The Togo `M833` and direct JCM/MediaDive `J798` imports are equivalent but remain split into two generated records, `gypf_with_2_5_nacl.yaml` and `gypf_with_2_5_nacl__562513e6.yaml`.
- Minor: `Yeast extract` and `Bacto peptone` are ungrounded complex ingredients.

## Recommended Edits

- Add source-equivalence handling so `TOGO:M833` / `JCM_M798` merges with direct `mediadive.medium:J798`.
- Curate local MediaIngredientMech grounding for `Yeast extract` and `Bacto peptone` if reusable terms are available or should be added.

## Follow-up Checks

- After regenerating merged records, verify that the Togo and direct JCM/MediaDive GYPF 2.5 percent NaCl records collapse into one merged output with both source identifiers retained.

## Additional Notes

- None found.
