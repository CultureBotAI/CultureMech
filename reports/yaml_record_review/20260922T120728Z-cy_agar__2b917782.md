# YAML Record Review: CY-Agar

- Repository: CultureMech
- Record: `data/merge_yaml/merged/cy_agar__2b917782.yaml`
- Started UTC: 2026-09-22T12:07:28Z
- Finished UTC: 2026-09-22T12:07:28Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:008922` / `cy_agar`, a one-source generated merge from `data/normalized_yaml/bacterial/TOGO_M2335_CY-Agar.yaml`.

## Validation

- Open schema validation passed for `MediaRecipe`.
- Strict validation passed and wrote `/private/tmp/cy_agar__2b917782.strict.tsv`.
- LinkML reference validation passed with zero checks.
- LinkML term validation passed.
- Embedded `curation_history` was not separately checked because the available `validate-history` target validates standalone files under `history/`, not history entries embedded in generated `MediaRecipe` YAML.

## Identity and Grounding

The source identity is a valid copy of DSMZ Medium 67, `CY-AGAR`. TogoMedium M2335 names `CY-Agar` and points to `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium67.pdf`.

DSMZ Medium 67 lists 3 g/L Casitone, 1.36 g/L CaCl2 x 2 H2O, 1 g/L yeast extract, 15 g/L agar, 1000 mL/L distilled water, and pH 7.2. The generated record carries the four non-water ingredients at the correct amounts, but it records the 1000 mL water row as `1000 G_PER_L` and omits pH 7.2.

## Evidence

Primary source checks:

- TogoMedium M2335 was fetched to `/private/tmp/togo_M2335.json`; it has name `CY-Agar` and `src_url` pointing to DSMZ Medium 67.
- `https://www.dsmz.de/microorganisms/medium/pdf/DSMZ_Medium67.pdf` was fetched and extracted to `/private/tmp/DSMZ_Medium67.txt`.

Local record checks:

- `data/normalized_yaml/bacterial/cy_agar.yaml` is the direct DSMZ Medium 67 owner with pH 7.2 and the same Casitone/CaCl2/yeast extract/agar formula.
- `data/normalized_yaml/bacterial/KOMODO_67_CY-AGAR.yaml` is already linked under the direct DSMZ owner as a source duplicate.
- `data/merge_yaml/merged/CY_Agar.yaml` already merges `KOMODO_67_CY-AGAR`, `cy_agar`, and `medium_67_modified_for_dsm_21789`.
- `data/merge_yaml/merged/cy_agar__2b917782.yaml` is a split generated duplicate from TOGO M2335.

## Completeness

The generated record is compositionally simple and has the right non-water solutes. It is incomplete because it lacks the DSMZ pH 7.2 adjustment and has not been linked into the existing DSMZ/KOMODO CY-Agar source-duplicate group.

## Findings

1. Needs curation: M2335 is split from the equivalent DSMZ/KOMODO CY-Agar group.

   The generated `CY_Agar.yaml` record already covers DSMZ Medium 67 plus the KOMODO 67 source duplicate and a strain-specific KOMODO 67 duplicate. TOGO M2335 cites the same DSMZ Medium 67 PDF and should merge into that source-duplicate cluster after its water row is normalized.

2. Needs curation: distilled water is encoded with a mass unit.

   DSMZ Medium 67 lists 1000 mL distilled water. The TOGO M2335 normalized and generated records store that row as `1000 G_PER_L`, which is the recurring stale TOGO liter-to-gram conversion error.

3. Needs curation: pH 7.2 was dropped.

   DSMZ Medium 67 ends with `Adjust pH to 7.2.`, and the direct DSMZ owner has `ph_value: 7.2`; the TOGO M2335 generated record has no pH value or pH preparation step.

## Recommended Edits

- Convert the TOGO M2335 distilled-water row to 1000 mL/L or remove it if the canonical DSMZ Medium 67 representation intentionally omits water.
- Add pH 7.2 to `TOGO_M2335_CY-Agar.yaml`.
- Link `TOGO_M2335_CY-Agar.yaml` under `data/normalized_yaml/bacterial/cy_agar.yaml` as a `SOURCE_DUPLICATE`.
- Regenerate `data/merge_yaml/merged/` and confirm TOGO M2335 appears in `CY_Agar.yaml` instead of producing `cy_agar__2b917782.yaml`.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation for regenerated CY-Agar.
- Search generated output for `cy_agar__2b917782` and `TOGO_M2335_CY-Agar` with ignored files included and confirm no split duplicate remains.
- Confirm the canonical generated CY-Agar retains pH 7.2 and no `1000 G_PER_L` water row.

## Additional Notes

The M2335 duplicate search used `rg --no-ignore --hidden`, so ignored report files and generated outputs were included.
