# YAML Record Review: 1_10_sabourauds_agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_10_sabourauds_agar.yaml`
- Started UTC: 20260921T050119Z
- Finished UTC: 20260921T050259Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009437` |
| Label | `1_10_sabourauds_agar` |
| Original label | `1/10 Sabouraud's Agar` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml`, `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml`, their import normalization, or merge regeneration |
| Merge owner | `merged_from: [1_10_sabourauds_agar]` |
| Maintained owner | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` |

This generated singleton is the TOGO Medium M28 import of JCM Medium 35.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_10_sabourauds_agar.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_10_sabourauds_agar.yaml --out /private/tmp/1_10_sabourauds_agar.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/1_10_sabourauds_agar.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_10_sabourauds_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_10_sabourauds_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The generated record correctly carries the TOGO Medium M28 accession and traces back to JCM Medium 35, `1/10 SABOURAUD'S AGAR`.

The same JCM Medium 35 source is also represented as `data/merge_yaml/merged/1_10_sabourauds_agar__6d784de4.yaml`, a direct MediaDive/JCM import. The two records should be source duplicates once water, pH, and source normalization are reconciled.

## Evidence

Supported by inspected source text:

- JCM 35 supports the Glucose, Peptone, KH2PO4, MgSO4 x 7 H2O, NaNO3, Agar, and Distilled water rows.
- JCM 35 supports the `physical_state: SOLID_AGAR` assignment because the source lists 20 g agar.

Unsupported or incomplete in the generated record:

- JCM says to adjust pH to 6.8-7.0; the TOGO record has no `ph_value` and no pH preparation step.
- JCM's default instruction to autoclave media at 121 degrees C for 15 min unless otherwise stated is not represented.
- JCM lists distilled water as 1.0 L, not as `1 G_PER_L`.
- The TOGO and JCM URLs are present only in free-text `notes`; there is no structured `references` block for `linkml-reference-validator` to resolve.

## Completeness

- The core solid-medium solutes are all present.
- Empty growth-evidence slots are acceptable for this imported provider recipe because the JCM page does not state an organism-specific growth observation.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:009437`, `CultureMech:010521`, exact `TOGO:M28`, exact `mediadive.medium:J35`, `JCM_M35`, and both Sabouraud merge fingerprints found only this TOGO owner, the direct JCM owner, their generated singletons, and ID/catalog entries.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The TOGO M28 and direct JCM 35 imports of the same source recipe failed to merge as source duplicates. | Both generated records trace to the JCM 35 URL, but they have distinct merge fingerprints because the TOGO import includes a mis-unit water row and lacks the pH retained by the direct JCM import. | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` and `data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml`, followed by merge regeneration. |
| Major | JCM's pH 6.8-7.0 is missing. | The JCM page says to adjust pH to 6.8-7.0; the TOGO record has no pH field or pH step. | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` or the TOGO import mapping. |
| Major | JCM's default sterilization condition is missing. | The JCM page says media should be autoclaved at 121 degrees C for 15 min unless otherwise stated; the generated record has no preparation steps. | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` or the TOGO import mapping. |
| Major | The distilled-water row has the wrong dimension. | JCM 35 lists distilled water as 1.0 L, while the TOGO record stores it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` or the TOGO volume import mapping. |
| Minor | Structured references are absent. | The TOGO and JCM URLs are present only in `notes`; the generated file has no `references` block, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` or the TOGO import mapping. |

## Recommended Edits

1. Normalize the TOGO M28 and direct JCM 35 owners so both represent JCM 35 water, pH, autoclaving, and source accessions consistently enough to collapse into one generated source-duplicate cluster.
2. Add pH 6.8-7.0 and the JCM default autoclave instruction to the TOGO owner.
3. Correct TOGO M28 distilled water to the JCM 1 L volume.
4. Add structured references for TOGO Medium M28 and JCM Medium 35.
5. Regenerate `data/merge_yaml/merged/` and confirm the TOGO and JCM copies merge as source duplicates.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml data/normalized_yaml/fungal/1_10_sabourauds_agar.yaml` after normalized edits.
- `just validate-references data/normalized_yaml/bacterial/1_10_sabourauds_agar.yaml` after adding structured references.
- `just verify-merges` to prove the TOGO and JCM generated outputs coalesce after regeneration.
- `just validate-strict` and `just validate-terms` on the regenerated 1/10 Sabouraud's Agar merge record.
- Manual comparison to JCM Medium 35 for all ingredients, pH 6.8-7.0, 1 L water, and the default autoclave instruction.

## Additional Notes

- A direct fetch of TOGO Medium M28 returned only the TogoMedium JavaScript shell; the local TOGO record points back to the JCM 35 source page, and that source page resolved.
- `linkml-reference-validator` performed zero checks because the generated record has no `references` block.
