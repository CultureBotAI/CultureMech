# YAML Record Review: maltose_bennetts_agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/maltose_bennetts_agar.yaml
- Started UTC: 2026-09-23T22:55:26Z
- Finished UTC: 2026-09-23T22:56:01Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| CultureMech ID | CultureMech:009761 |
| Label | maltose_bennetts_agar |
| Original label | Maltose-Bennett's Agar |
| Category | bacterial |
| Source identity | TOGO M37, originally JCM Medium J45 |
| Merge fingerprint | dcded20dce672a71083b68be0c11b9078e6cb3448234d5fc4c92dbe8f87cb470 |
| Merged from | `TOGO_M37_Maltose-Bennett_s_Agar` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M37_Maltose-Bennett_s_Agar.yaml` |
| Generated status | Generated by `merge_recipes.py` on 2026-08-06 |

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/maltose_bennetts_agar.yaml` | Passed |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/maltose_bennetts_agar.yaml --out /private/tmp/maltose_bennetts_agar.strict.tsv --workers 1 --quiet` | Passed with 1 file, 0 errors; the TSV contained only the header row |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/maltose_bennetts_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 1 file and 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/maltose_bennetts_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| Embedded history | `just validate-history` | Not checked: this repository validator targets standalone `history/` records, not embedded `MediaRecipe.curation_history` entries |

## Identity and Grounding

- The generated record consistently denotes TOGO M37, derived from JCM Medium J45 MALTOSE-BENNETT'S AGAR.
- The generated TOGO record has two unmerged same-label siblings: `data/normalized_yaml/fungal/maltose_bennetts_agar.yaml` is the MediaDive/JCM J45 import of the same source, while `data/normalized_yaml/bacterial/maltose_bennetts_agar.yaml` is an NBRC 231 formula with 20 g/L agar instead of JCM's 15 g/L agar.
- The JCM page, MediaDive `J45`, and TOGO M37 all preserve the supplier-bearing JCM labels for yeast extract, beef extract, and N-Z amine type A.
- `Maltose` is grounded too narrowly as `CHEBI:18167` / alpha-maltose. The source does not specify an anomer, and the packaged MediaIngredientMech label index maps the exact label `Maltose` to `CHEBI:17306`.

## Evidence

| Claim | Support |
|---|---|
| JCM 45 contains 1 g BD-Difco yeast extract, 1 g BD-Difco beef extract, 2 g N-Z amine type A from Sheffield, 10 g maltose, 15 g agar, and 1 L distilled water | Supported by the live JCM GRMD 45 page and mirrored by MediaDive `J45` and TOGO M37 |
| JCM 45 is adjusted to pH 7.3 | Supported by the live JCM GRMD 45 preparation note, MediaDive `J45`, and TOGO M37 |
| JCM 45 uses generic maltose rather than alpha-maltose specifically | Supported by the source label `Maltose` in JCM, MediaDive, and TOGO |

## Completeness

- The generated record omits the source pH 7.3 despite TOGO M37 carrying `ph: "7.3"` and JCM GRMD 45 stating the pH adjustment.
- The water row uses `value: '1'` with `unit: G_PER_L`; JCM, MediaDive, and TOGO all state 1 L or 1000 ml distilled water.
- The generated record omits JCM's default 121 C for 15 min autoclave instruction.
- Empty optional slots for organism growth evidence, incubation temperature, salinity, atmosphere, storage, and variants were not treated as defects because the inspected JCM and MediaDive records do not state them.

## Findings

| Severity | Finding | Evidence | Maintained owner for a future fix |
|---|---|---|---|
| Major | The pH 7.3 source condition is missing. | JCM GRMD 45, MediaDive `J45`, and TOGO M37 all state pH 7.3, but the generated TOGO M37 record has no `ph_value` or `ADJUST_PH` step. | `data/normalized_yaml/bacterial/TOGO_M37_Maltose-Bennett_s_Agar.yaml` or the TOGO pH importer |
| Major | The water amount uses the wrong unit. | The inspected sources list 1 L or 1000 ml distilled water; the generated row stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M37_Maltose-Bennett_s_Agar.yaml` or TOGO import unit normalization for water rows |
| Major | Maltose is grounded to alpha-maltose, over-specifying the source reagent. | The JCM and TOGO labels are simply `Maltose`; the packaged MediaIngredientMech label index has an exact `Maltose` mapping to `CHEBI:17306`, while the generated row uses `CHEBI:18167` / alpha-maltose. | `data/normalized_yaml/bacterial/TOGO_M37_Maltose-Bennett_s_Agar.yaml` |
| Minor | The complex source ingredients are incompletely normalized and grounded. | Yeast extract and Beef extract have exact FoodOn mappings in the sibling JCM owner; N--Z amine carries an import hyphen artifact compared with JCM's `N-Z amine`; and the local label index keeps N-Z-Amine A explicit and unmapped. | TOGO import normalization plus the normalized TOGO M37 owner |
| Minor | The generated record omits JCM's default sterilization. | The live JCM GRMD 45 page says to autoclave media at 121 C for 15 min unless otherwise stated. | The normalized TOGO M37 owner or JCM import normalization |

No blocker findings were found.

## Recommended Edits

1. Add pH 7.3 as a structured pH value and `ADJUST_PH` step on the normalized TOGO M37 owner.
2. Convert the source 1 L distilled-water row from `1 G_PER_L` to the repository's accepted 1000 `ML_PER_L` representation.
3. Re-ground `Maltose` from alpha-maltose to the exact generic maltose identity, mirroring the packaged label-index mapping.
4. Normalize `N--Z amine, type A (Sheffield)` to the source spelling `N-Z amine, type A (Sheffield)` and leave it ungrounded unless a reviewed exact identity is added to the ingredient index.
5. Ground supplier-qualified yeast extract and beef extract as in the sibling JCM owner while preserving BD-Difco in the preferred label or notes.
6. Add the JCM default autoclave instruction as a source-level sterilization step.
7. Regenerate `data/merge_yaml/merged/maltose_bennetts_agar.yaml` and downstream media pages after the normalized owner is corrected.

## Follow-up Checks

- Re-run open schema, strict schema, term validation, and reference validation on `data/normalized_yaml/bacterial/TOGO_M37_Maltose-Bennett_s_Agar.yaml`.
- Re-run the same focused validators on regenerated `data/merge_yaml/merged/maltose_bennetts_agar.yaml`.
- Compare the regenerated TOGO M37 record against live JCM GRMD 45, MediaDive `J45`, and TOGO M37, checking pH 7.3, the 15 g/L agar row, the source supplier qualifiers, and the 1 L water row.
- Re-run the term validator and confirm `Maltose` no longer resolves to `CHEBI:18167`.

## Additional Notes

- The sibling NBRC 231 import has the same label but 20 g/L agar, so it was not treated as an exact source duplicate of JCM 45 in this review.
