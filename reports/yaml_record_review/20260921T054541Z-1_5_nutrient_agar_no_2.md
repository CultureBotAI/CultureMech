# YAML Record Review: 1/5 NUTRIENT AGAR NO. 2

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml`
- Started UTC: 2026-09-21T05:44:07Z
- Finished UTC: 2026-09-21T05:45:42Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:002803` |
| Name | `1_5_nutrient_agar_no_2` |
| Original name | `1/5 NUTRIENT AGAR NO. 2` |
| Category | `bacterial` |
| Source term | `mediadive.medium:J453`, `1/5 NUTRIENT AGAR NO. 2` |
| Merge fingerprint | `cae8463c728c9c8e97ddfc7a55c1d834475673ae6f2669f1ecc2cebe7289267f` |
| Maintained owner | `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is a direct MediaDive/JCM import of JCM Medium 453. A separate TOGO mirror of the same upstream JCM source is also present at `data/merge_yaml/merged/1_5_nutrient_agar_no_2__b809c568.yaml`, derived from `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml`.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml --out /private/tmp/1_5_nutrient_agar_no_2.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_5_nutrient_agar_no_2.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's identity is consistent with a direct JCM Medium 453 import:

- The generated target names `mediadive.medium:J453` and notes `Source: JCM | Link: https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=453`.
- The TOGO M453 mirror reports `original_media_id: JCM_M453`, the same JCM URL, `name: 1/5 Nutrient Agar NO. 2`, and `ph: "7.0-7.2"`.
- The live JCM `GRMD=453` page was checked, but it now returns `Nothing found`; it does not currently verify or contradict the imported JCM identity.

Grounding is partly complete:

- `NaCl` is grounded to `CHEBI:26710`, sodium chloride.
- `Agar` is grounded to `CHEBI:2509`, agar.
- `Bacto peptone` and `Beef extract` remain ungrounded. TOGO M453 preserves the BD-Difco qualifiers as `Bacto peptone (BD-Difco)` and `Beef extract (BD-Difco)`, which is a more exact commercial-product label than the direct generated record uses.

## Evidence

The inspected TOGO M453 payload, mirroring the same JCM_M453 source, supports:

- Distilled water, 1 L.
- NaCl, 1 g.
- Agar, 15 g.
- Bacto peptone (BD-Difco), 2 g.
- Beef extract (BD-Difco), 2 g.
- pH adjustment to 7.0-7.2.

The direct generated record is missing the water row and strips the BD-Difco qualifiers from the two undefined commercial ingredients. Its `ph_value: 7.1` plus `ADJUST_PH` step preserve the source pH range well enough for a scalar pH field plus procedural text.

## Completeness

Consequential gaps:

- The 1 L distilled-water row is absent.
- The Bacto peptone and beef extract labels are less exact than the source labels.
- There are no structured references for the JCM URL or the TOGO M453 mirror.
- The same upstream JCM Medium 453 recipe is duplicated by the TOGO-derived generated record `1_5_nutrient_agar_no_2__b809c568.yaml`.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected TOGO mirror verifies the recipe but does not support a target-organism or variant assertion.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data`, `scripts`, `history`, and `reports` for `CultureMech:002803`, `mediadive.medium:J453`, `JCM, ID: J453`, `GRMD=453`, the direct JCM merge fingerprint, and `1_5_nutrient_agar_no_2` found the direct normalized owner, the direct generated merge, the TOGO normalized owner, the separate TOGO generated merge, active registry/index rows, historical March validation rows, and media-content review rows.
- A second gitignore-independent `rg --no-ignore --hidden` search for `CultureMech:009840`, `TOGO:M453`, the TOGO merge fingerprint, `JCM_M453`, and `GRMD=453` confirmed that the only accession-bearing sibling is the expected TOGO M453 owner and generated merge.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The direct generated record omits Distilled water. | TOGO M453 mirrors JCM_M453 with a 1 L `Distilled water` row; the direct MediaDive/JCM record has only Bacto peptone, beef extract, NaCl, and agar. | `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` or the MediaDive/JCM importer |
| Minor | Two commercial ingredient labels are underspecified. | TOGO M453 preserves `Bacto peptone (BD-Difco)` and `Beef extract (BD-Difco)`; the direct record drops the BD-Difco qualifiers. | `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` |
| Minor | Structured references are absent. | The record cites the JCM URL only in free-text `notes`, so the reference validator had no structured references to check. | `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` |
| Minor | The direct JCM and TOGO M453 imports remain separate generated records for the same upstream source. | Exact hidden/ignored-inclusive searches found `CultureMech:002803` and `CultureMech:009840`, both named `1_5_nutrient_agar_no_2` and both pointing at `GRMD=453`; they do not merge because the direct copy omits water and the TOGO copy imports water as `1 G_PER_L`. | `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml`, `data/normalized_yaml/bacterial/TOGO_M453_1_5_Nutrient_Agar_NO._2.yaml`, and merge rules |

## Recommended Edits

1. Add `Distilled water` at `1 L` to `data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml`.
2. Restore the exact commercial labels `Bacto peptone (BD-Difco)` and `Beef extract (BD-Difco)` or document why MediaDive deliberately normalized them.
3. Add structured references for the imported JCM URL and, if used as corroborating evidence while the JCM page is unavailable, the live TOGO M453 payload.
4. Append a curation-history event describing the restored water row and label corrections.
5. Correct the TOGO M453 owner from `Distilled water 1 G_PER_L` to `1 L`, then regenerate `data/merge_yaml/merged/` and check whether the direct and TOGO JCM_M453 records merge into one canonical output.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` after any grounding or label edits.
- `just validate-references data/normalized_yaml/bacterial/1_5_nutrient_agar_no_2.yaml` after adding structured references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against TOGO M453 or an archived JCM Medium 453 source for all five components and the pH range.

## Additional Notes

- The current JCM URL returned `Nothing found` for `GRMD=453` on 2026-09-21, so the live JCM page could not be used as direct source text.
- The generated TOGO sibling has the missing water row but encodes that 1 L source amount as `1 G_PER_L`, so it is useful corroborating evidence for presence of water but not a complete replacement source.
- The reference validator passed with 0 checks because this record has no structured `references` entries.
