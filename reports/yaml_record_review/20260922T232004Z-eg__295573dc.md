# YAML Record Review: eg

- Repository: CultureMech
- Record: data/merge_yaml/merged/eg__295573dc.yaml
- Started UTC: 2026-09-22T23:18:08Z
- Finished UTC: 2026-09-22T23:20:04Z
- Verdict: needs curation

## Target

Generated bacterial CCAP/MediaDive C26 record `CultureMech:000334`, named `eg`.

## Validation

- Open LinkML validation against `MediaRecipe`: passed.
- Strict validation against `scripts/validate_strict.py`: passed with 0 error rows.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded `curation_history`: Not checked: the focused history validator targets standalone files under `history/`, not inline `MediaRecipe.curation_history` entries.

## Identity and Grounding

The generated C26 identity points to CCAP EG, but the recipe is categorized as `bacterial` even though the current CCAP PDF describes EG as Euglena gracilis medium for freshwater algae and protozoa. The same CCAP recipe also exists in the algae-side `EG.yaml`, and that copy was repaired in August 2026 against `MR_EG.pdf`.

Grounding is partly stale. The source compound is sodium acetate trihydrate, but the generated record is grounded to anhydrous sodium acetate; the repaired algae record uses the trihydrate term. The CaCl2 row is grounded to calcium dichloride, but its bigger problem is concentration rather than identity.

## Evidence

The current CCAP `MR_EG.pdf` lists, per litre, 1.0 g sodium acetate trihydrate, 1.0 g Lab-Lemco powder, 2.0 g tryptone, 2.0 g yeast extract, and 1.0 ml CaCl2 stock. That CaCl2 stock is prepared as 1.0 g per 100 ml. Agar is optional: for an agar formulation, add 15 g bacteriological agar per litre, then autoclave at 15 psi for 15 minutes.

MediaDive C26 already encodes CaCl2 as a stock addition, but from the older CCAP text: 10 ml of a 1 g/L stock. Both source variants imply a final 0.01 g CaCl2 per litre, not 10 g/L.

## Completeness

The carbon/complex ingredients, optional agar amount, water makeup, and autoclave instruction are present. The CaCl2 stock boundary is lost and the final calcium concentration is inflated 1000-fold relative to the current CCAP PDF. The generated record also has no structured CCAP source/reference entries.

## Findings

- `CaCl2` is rendered as `10 G_PER_L`; the current CCAP PDF requires 1 ml of a 10 g/L stock per litre final medium, equivalent to 0.01 g/L final CaCl2.
- Sodium acetate trihydrate is grounded to anhydrous sodium acetate.
- The record is on the bacterial generated path rather than reconciled with the repaired algae EG record.
- The optional agar variant is represented as the physical state of the whole record; the liquid EG base and 15 g/L agar formulation should be explicit variants or conditional ingredients.
- Structured `sources` and `references` are missing despite a stable CCAP PDF source.

## Recommended Edits

- Recurate EG from the current 2026 CCAP PDF and preserve CaCl2 as a stock or as a calculated 0.01 g/L final addition.
- Re-ground sodium acetate trihydrate to the trihydrate term and preserve Lab-Lemco, tryptone, and yeast extract as complex ingredients.
- Reconcile `eg__295573dc.yaml` with the repaired algae `EG.yaml` output so CCAP EG has one canonical generated record.
- Represent 15 g/L bacteriological agar as an optional solid variant rather than forcing the medium base to `SOLID_AGAR`.
- Add structured CCAP provenance and keep the 15 psi for 15 minutes autoclave step.

## Follow-up Checks

- Re-run open, strict, reference, and term validators after rebuilding.
- Compare the rebuilt record against current `MR_EG.pdf` and MediaDive C26.
- Search with ignored files included for stale `CaCl2` `10 G_PER_L` rows in generated EG outputs.

## Additional Notes

The generated record has no explicit organism growth data to review.
