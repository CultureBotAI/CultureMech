# YAML Record Review: 2xytg

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/2xYTG.yaml`
- Started UTC: 2026-09-21T06:39:35Z
- Finished UTC: 2026-09-21T06:40:32Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009326` |
| Name | `2xytg` |
| Original name | `2xYTG` |
| Category | `bacterial` |
| Source term | `TOGO:M2778`, `2xYTG` |
| Source URL | `https://togomedium.org/medium/M2778` |
| Merge fingerprint | `0f1f4fc3e1f8c877c293da8b59c35ef74368a23d9bda57c64b6a80b724144dad` |
| Maintained owner | `data/normalized_yaml/bacterial/2xytg.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO M2778 import. Future corrections belong in `data/normalized_yaml/bacterial/2xytg.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/2xYTG.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/2xYTG.yaml --out /private/tmp/2xYTG.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/2xYTG.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/2xYTG.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record's direct TOGO identity is internally consistent: `media_term.term.id` is `TOGO:M2778`, the label is `2xYTG`, and the live TOGO API payload identifies M2778 with the same label. No separate original-source URL is present in the inspected TOGO payload.

Ingredient identity is sound. Distilled water, yeast extract, NaCl, glucose, and tryptone all appear in the live TOGO M2778 payload, and the CHEBI groundings for water, sodium chloride, and glucose are chemically compatible with those labels.

Fetching `https://togomedium.org/medium/M2778` returned the TOGO single-page application shell without a static M2778 recipe table, so the inspected live source was the TOGO REST API route used by the repository's own `src/culturemech/fetch/togo_fetcher.py`.

## Evidence

The inspected TOGO M2778 API payload supports this ingredient list:

- 1 L distilled water.
- 10 g yeast extract.
- 5 g NaCl.
- 5 g glucose.
- 16 g tryptone.

The payload also carries a publication-derived comment reporting `Clostridium spp.` cultivation at 37 C in an anaerobic cabinet using CBM, 2xYTG, or CGM media supplemented with antibiotics when required. That sentence names 2xYTG but does not by itself isolate a strain-specific 2xYTG growth claim.

The generated target preserves all four dry components and their amounts. It stores the source's 1 L distilled water as `1 G_PER_L`.

## Completeness

Consequential gaps:

- The distilled-water amount has the wrong dimension.
- The record has no structured `references`, so the TOGO M2778 URL is not validator-visible.
- The original publication context and PMIDs from the TOGO source comment are not represented in notes or references.

Empty optional slots:

- `solutions` is correctly absent. The inspected TOGO M2778 payload is a flat formulation with no named stock solutions.
- `target_organisms` and `growth_metrics` are correctly absent. The inspected TOGO comment mentions Clostridium spp. and several alternative media in one sentence rather than a narrow strain/growth claim for 2xYTG.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-2xYTG.md` report before this report was created.
- A gitignore-independent `find` under `data/normalized_yaml` found `data/normalized_yaml/bacterial/2xytg.yaml` as the generated record's owner.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, and `data/raw` for `CultureMech:009326`, `TOGO:M2778`, `M2778`, the merge fingerprint, `2xYTG`, and `2xytg` found only the expected live normalized owner, generated merge, indexes, manifests, and historical archive rows. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Distilled water is represented with the wrong dimension. | The live TOGO M2778 payload lists 1 L distilled water. The generated and normalized records store `Distilled water` as `1 G_PER_L`, turning a final solvent volume into a 1 g/L mass concentration. | `data/normalized_yaml/bacterial/2xytg.yaml` |
| Minor | Structured source provenance is absent. | The target cites the TOGO URL only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | `data/normalized_yaml/bacterial/2xytg.yaml` |
| Minor | TOGO publication context is dropped. | The inspected TOGO M2778 payload includes a comment with PMIDs for CBM/CGM context and 2xYTG cultivation of Clostridium spp.; the CultureMech record retains only the ingredient rows. The comment is too broad for a target-organism assertion without inspecting the linked PMIDs. | `data/normalized_yaml/bacterial/2xytg.yaml` |

## Recommended Edits

1. Correct the distilled-water row in `data/normalized_yaml/bacterial/2xytg.yaml` so it represents the source's 1 L solvent volume rather than `1 G_PER_L`.
2. Add structured references for `https://togomedium.org/medium/M2778` and `https://togomedium.org/sparqlist/api/gmdb_medium_by_gmid?gm_id=M2778`.
3. Inspect the PMIDs named in the TOGO comment before deciding whether to preserve the Clostridium cultivation sentence as source context or to add narrow strain/media growth evidence.
4. Regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/2xytg.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/2xytg.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/2xytg.yaml` after correcting distilled water.
- `just validate-references data/normalized_yaml/bacterial/2xytg.yaml` after adding structured TOGO references.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against TOGO M2778 for 16 g/L tryptone, 10 g/L yeast extract, 5 g/L NaCl, 5 g/L glucose, and 1 L distilled water.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
