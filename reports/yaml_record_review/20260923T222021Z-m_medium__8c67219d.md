# YAML Record Review: m_medium

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/m_medium__8c67219d.yaml`
- Started UTC: `2026-09-23T22:20:21Z`
- Finished UTC: `2026-09-23T22:20:39Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:002350` |
| Record name | `m_medium` |
| Original name | `M MEDIUM` |
| Source accession | `mediadive.medium:J1179` |
| Source label | `M MEDIUM` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/m_medium.yaml` |
| Generated record | `data/merge_yaml/merged/m_medium__8c67219d.yaml` |

`data/merge_yaml/merged/m_medium__8c67219d.yaml` is a generated merge from
`data/normalized_yaml/bacterial/m_medium.yaml` with fingerprint
`8c67219d452db893b0629592847694eab5d68a1e16bc1d87e76a426aaa57b696`. Future
fixes should land in the normalized record, the source-specific MediaDive/JCM
normalization path that produced it, or merge logic before regenerating
`data/merge_yaml/merged/`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_medium__8c67219d.yaml` | Passed. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_medium__8c67219d.yaml --out /private/tmp/m_medium__8c67219d.strict.tsv --workers 1 --quiet` | Passed; the strict TSV had only the header row, so there were 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_medium__8c67219d.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_medium__8c67219d.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The record identity is stable and internally consistent: `CultureMech:002350`,
  `name: m_medium`, `original_name: M MEDIUM`, `media_term.term.id:
  mediadive.medium:J1179`, and the JCM URL in `notes` all point to JCM medium
  `GRMD=1179`, `M MEDIUM`.
- The inspected JCM `GRMD=1179` formula supports the first four dry ingredients
  as per-liter components: `KNO3` 1 g, `KH2PO4` 1 g,
  `MgSO4 x 7 H2O` 0.22 g, and `NaCl` 30 g.
- The CHEBI groundings for the seven listed root ingredients are label-aligned
  with their source ingredient names and do not create an identity conflict.
- An ignored-file-inclusive exact search for `CultureMech:002350`,
  `mediadive.medium:J1179`, `J1179`, and the merge fingerprint across
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`
  found this generated record, its maintained normalized input, generated
  indexes and reports, and the related `Main sol. J1179` stock-solution import.

## Evidence

- The source identity is supported by the inspected JCM record for `GRMD=1179`;
  that page names the recipe `M MEDIUM`.
- The inspected JCM formulation supports `ph_value: 9.0` as the final
  adjustment target in the preparation instructions.
- The inspected JCM formulation supports adding filtered `2 M NaHCO3` solution,
  `1 M Na2CO3` solution, and methanol after autoclaving.
- The inspected JCM formulation does not support the root record's dimensional
  claims that `NaHCO3`, `Na2CO3`, and methanol are `50`, `10`, and `5`
  `G_PER_L`, respectively. Those JCM rows are 50 ml of 2 M sodium bicarbonate
  stock, 10 ml of 1 M sodium carbonate stock, and 5 ml of methanol in a
  one-liter formula.
- The inspected JCM formulation includes 935 ml distilled water. The generated
  merged record has no distilled-water ingredient and no volume component that
  preserves that source line.
- `data/normalized_yaml/bacterial/mediadive_5290_Main_sol_J1179.yaml` preserves
  the same JCM formulation as a related `SolutionRecipe` named
  `Main sol. J1179`, but it stores the milliliter amounts as `PERCENT_V_V`
  values such as `50`, `10`, `5`, and `935`. That companion record is useful
  evidence that the import once saw the missing water line, but it is not a
  corrected representation.

## Completeness

- Consequentially incomplete: the merged medium omits the 935 ml distilled
  water line from JCM `GRMD=1179`.
- Consequentially incomplete: the final formula does not preserve the
  stock-solution boundary for `2 M NaHCO3` and `1 M Na2CO3`; consumers cannot
  tell that the `50` and `10` source values were milliliter aliquots of stock
  solutions rather than masses.
- The post-autoclave addition text and pH 9.0 adjustment text are present and
  source-supported.
- Empty optional organism/growth-evidence fields were not treated as defects;
  JCM `GRMD=1179` is a recipe source, not primary growth evidence for a target
  organism.
- An ignored-file-inclusive exact search for `CultureMech:014202`,
  `mediadive_5290_Main_sol_J1179`, and `Main sol. J1179` across
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`
  found the related normalized solution record, generated indexes, and old
  validation reports; it did not find an independently curated corrected J1179
  stock-solution record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | Stock additions were flattened into mass concentrations. The merged record stores `NaHCO3` 50 `G_PER_L`, `Na2CO3` 10 `G_PER_L`, and methanol 5 `G_PER_L`; JCM lists those as 50 ml of 2 M `NaHCO3`, 10 ml of 1 M `Na2CO3`, and 5 ml methanol. | Inspected JCM `GRMD=1179` formula and `data/merge_yaml/merged/m_medium__8c67219d.yaml`. | `data/normalized_yaml/bacterial/m_medium.yaml` or the MediaDive/JCM importer that materialized stock aliquot volumes as root `G_PER_L` ingredient values. |
| Major | The generated merge omits the JCM distilled-water component. JCM lists 935 ml distilled water in the one-liter recipe, but the merged record has no water ingredient or equivalent final-volume representation. | Inspected JCM `GRMD=1179` formula, `data/merge_yaml/merged/m_medium__8c67219d.yaml`, and `data/normalized_yaml/bacterial/mediadive_5290_Main_sol_J1179.yaml`. | `data/normalized_yaml/bacterial/m_medium.yaml` or the MediaDive/JCM importer that dropped source water lines during medium import. |
| Major | The related `Main sol. J1179` import preserved the water line but assigned milliliter quantities to `PERCENT_V_V`, indicating the source-volume unit bug affects the J1179 stock-solution normalization path too. | `data/normalized_yaml/bacterial/mediadive_5290_Main_sol_J1179.yaml` stores `NaHCO3` 50 `PERCENT_V_V`, `Na2CO3` 10 `PERCENT_V_V`, methanol 5 `PERCENT_V_V`, and distilled water 935 `PERCENT_V_V`. | `data/normalized_yaml/bacterial/mediadive_5290_Main_sol_J1179.yaml` or the MediaDive solution importer that maps source volume rows. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/m_medium.yaml`, or in the maintained
   MediaDive/JCM import step that owns that file, replace the three incorrect
   `G_PER_L` entries with a representation that preserves JCM's 50 ml `2 M
   NaHCO3` stock addition, 10 ml `1 M Na2CO3` stock addition, and 5 ml methanol
   addition.
2. Restore the JCM 935 ml distilled-water component in the maintained input or
   update the importer so JCM volume rows are carried through instead of
   dropped.
3. Review `data/normalized_yaml/bacterial/mediadive_5290_Main_sol_J1179.yaml`
   and its MediaDive solution-import path so JCM milliliter quantities are not
   serialized as raw `PERCENT_V_V` values.
4. Regenerate `data/merge_yaml/merged/` so
   `data/merge_yaml/merged/m_medium__8c67219d.yaml` is produced from the
   corrected maintained input.

## Follow-up Checks

- Run open-schema validation for the corrected normalized `m_medium` record and
  its regenerated merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and regenerated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference validation on the corrected outputs to catch broken CURIEs:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data <record> --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`.
- Run term validation for the corrected outputs to re-check CHEBI and
  MediaDive identifiers:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data <record> -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`.
- Manually compare the regenerated `m_medium` record against JCM `GRMD=1179`
  and verify that the four gram-scale salts, the three post-autoclave
  additions, the 935 ml distilled water, and the pH 9.0 adjustment are all
  preserved without converting a stock-volume source value to an unrelated mass
  concentration.

## Additional Notes

- The focused validators prove schema shape and resolvable identifiers, not
  scientific agreement with JCM. They pass despite the stock-volume and water
  omissions above.
- The source text inspection focused on JCM `GRMD=1179`, which is the upstream
  accession recorded in the generated YAML and normalized input.
