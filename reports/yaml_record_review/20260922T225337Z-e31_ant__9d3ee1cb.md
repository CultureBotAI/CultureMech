# YAML Record Review: e31_ant

- Repository: CultureMech
- Record: data/merge_yaml/merged/e31_ant__9d3ee1cb.yaml
- Started UTC: 2026-09-22T22:53:23Z
- Finished UTC: 2026-09-22T22:53:37Z
- Verdict: needs curation

## Target

Generated bacterial CCAP/MediaDive C32 record `CultureMech:000341`, canonicalized as `e31_ant`, with `CCAP Medium C32` / `mediadive.medium:C32` as the top-level source and `e26_biotin_ant` / `mediadive.medium:C29` as a merged synonym.

## Validation

- Open LinkML validation against `MediaRecipe`: passed.
- Strict validation against `scripts/validate_strict.py`: passed with 0 error rows.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded `curation_history`: Not checked: the focused history validator targets standalone files under `history/`, not inline `MediaRecipe.curation_history` entries.

## Identity and Grounding

The MediaDive C32 identity is present, but the generated record is categorized as `bacterial` even though the CCAP E31:ANT PDF is for marine algae. It was also merged with the distinct C29 / E26 + Biotin:ANT import; C29 follows the same E26/E31 plus ANT pattern, but the generated C32 record only keeps C29 as a `synonyms` entry and does not expose the C29 URL as a structured source or reference.

Most inorganic salts and organic components are grounded to plausible ChEBI terms. The source labels `Cyanocobalamine` and `Biotine` are MediaDive spelling artifacts, and the final record fails to coalesce `Biotin` from C32 with `Biotine` from the ANT block.

## Evidence

MediaDive C32 encodes E31:ANT as 500 ml `Mail sol. C31` plus 500 ml `Mail sol. C3`. `Mail sol. C31` is the 1 litre E31 recipe with 500 ml Part 1 plus 500 ml seawater, and its Part 1 has 0.1 g KNO3, 0.01 g K2HPO4, 0.01 g MgSO4 x 7 H2O, 0.1 ug cyanocobalamin, 50 ug thiamine HCl, 0.1 ug biotin, 50 ml SE1, and distilled water to 500 ml.

`Mail sol. C3` is the 1 litre ANT recipe with 0.05 g KNO3, 0.0078 g NaH2PO4 x 2 H2O, 1 g Tris, 0.3 g glycine, 2.5 ml chelated trace-metals stock, 500 ug thiamine HCl, 2 ug cyanocobalamin, 1 ug biotin, 800 ml seawater, and distilled water to 1 litre. The chelated trace-metals stock is itself a 1 litre stock and is not added wholesale to the final medium.

The CCAP E31:ANT PDF has the same nested design: prepare E31 and ANT separately, autoclave them separately, and mix them 1:1 when cool.

## Completeness

The record includes many leaf ingredients but loses every stock and submedium boundary. Volumes of seawater, SE1, C31, C3, and chelated trace-metals stock are not represented as volumes, distilled-water makeups are absent, and calculated final concentrations are not scaled through the nested 500 ml plus 500 ml C32 mixture.

Preparation text from C31, SE1, ANT, and the chelated trace-metals stock is concatenated into a flat ten-step list, so it no longer identifies which autoclave or pH-adjustment step belongs to which stock.

## Findings

- The record sums 500 ml E31 seawater plus 800 ml ANT seawater into `1300.0 G_PER_L` `Natural sea water`; the final 1 litre E31:ANT mixture receives only half of each component medium.
- `KNO3` is inflated to `0.25 G_PER_L` by adding the 500 ml E31 Part 1 `g_l` value `0.2` to the ANT value `0.05`; the final 1:1 mixture should scale E31 and ANT contributions separately.
- `K2HPO4` and `MgSO4 x 7 H2O` retain the 500 ml E31 Part 1 stock concentration `0.02 G_PER_L`, which is four times the final E31:ANT concentration implied by 0.01 g per litre E31 diluted 1:1 with ANT.
- Cyanocobalamin, thiamine, and biotin are not scaled through the E31/ANT 1:1 mixture; biotin is also split into two rows because C32 and ANT use different source spellings.
- The seven chelated trace-metal rows are stock concentrations, not final medium concentrations; each should be scaled through 2.5 ml stock per 1 litre ANT and then through the final 1:1 E31:ANT mixture if the record remains flat.
- `Soil` is a lossy placeholder for the 50 ml SE1 stock, and the SE1 stock's 2:1 water-to-air-dried-soil preparation is not structurally modeled.
- The C29 merge is provenance-lossy because `mediadive.medium:C29` is hidden as a synonym rather than kept as a structured source/reference.

## Recommended Edits

- Fix the MediaDive nested-solution importer before regenerating C32 and C29 so parent solution volumes, child solution volumes, and final mix ratios are all retained.
- Model E31, SE1, ANT, and the chelated trace-metals stock as nested recipes, or explicitly calculate final C32 concentrations through `0.5 L E31 + 0.5 L ANT`.
- Merge the `Biotin` and `Biotine` rows after label normalization and normalize `Cyanocobalamine` to `Cyanocobalamin`.
- Replace `Natural sea water 1300.0 G_PER_L` and `Soil VARIABLE` with source-faithful volume entries or subsolution references.
- Preserve both C32 / E31:ANT and C29 / E26 + Biotin:ANT as structured sources if one canonical recipe intentionally represents both PDFs.
- Reconcile this bacterial MediaDive copy with the algae E31:ANT record and correct the generated category/application to algae cultivation.

## Follow-up Checks

- Re-run open, strict, reference, and term validators after rebuilding the normalized and generated records.
- Compare regenerated C32 against MediaDive solution IDs 6023, 6157, 6159, 6156, 5994, and 6111, plus `MR_E31_ANT.pdf`.
- Compare the C29 merge source against MediaDive C29 and `MR_E26_Biotin_ANT.pdf`.
- Search with ignored files included for stale summed values such as `1300.0`, `0.25`, `550.0`, and `2.1` after regeneration.

## Additional Notes

The generated record has no explicit organism growth data to review.
