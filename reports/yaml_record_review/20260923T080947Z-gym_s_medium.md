# YAML Record Review: GYM+S medium

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gym_s_medium.yaml`
- Started UTC: 2026-09-23T08:08:44Z
- Finished UTC: 2026-09-23T08:09:47Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:004406`, `gym_s_medium`, merged from KOMODO medium 214 and DSMZ / MediaDive medium 214.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gym_s_medium.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The generated record correctly groups `data/normalized_yaml/bacterial/KOMODO_214_GYM_S_medium.yaml` with `data/normalized_yaml/bacterial/gym_s_medium.yaml` as a `SOURCE_DUPLICATE`: KOMODO 214 explicitly cites DSMZ medium 214, and both normalized parents have the same local ingredient and concentration signature. Exact ignored-file-inclusive search found no other `komodo.medium:214`, `mediadive.medium:214`, or `gym_s_medium` recipe records in `data/normalized_yaml/bacterial` or `data/merge_yaml/merged`.

Glucose, CaCO3, Agar, and Starch are grounded. Yeast extract and Malt extract are ungrounded complex components.

## Evidence

The MediaDive 214 REST payload describes `GYM+S MEDIUM`, pH 7.2, and one 1000 ml main solution with 4 g Glucose, 4 g Yeast extract, 10 g Malt extract, 2 g CaCO3 with condition `if required`, 12 g Agar, 20 g Starch, and 1000 ml Distilled water. The generated record preserves the six non-water ingredient concentrations and pH 7.2.

MediaDive also has one preparation step: adjust pH to 7.2 before adding agar, and delete CaCO3 if liquid medium is used. That step is present in the direct DSMZ normalized parent but missing from the generated merge.

## Completeness

The ingredient signature and pH target are materially complete for the solid DSMZ 214 formula. The generated record is incomplete for preparation because it drops the pH-before-agar and liquid-medium CaCO3 instruction. It also carries the KOMODO `2026-01-27T01:15:02.fZ` curation timestamp into embedded generated history.

## Findings

- The DSMZ preparation step is missing from the generated record. This loses both the timing of pH adjustment before agar addition and the instruction to delete CaCO3 when a liquid medium is used.
- The generated `media_term` keeps only `komodo.medium:214`; DSMZ / MediaDive 214 is represented indirectly via `parent_media`, so generated source identity is less explicit than the two-source merge warrants.
- The embedded KOMODO import history timestamp `2026-01-27T01:15:02.fZ` is malformed.
- Yeast extract and Malt extract remain ungrounded. This is a minor issue for complex components, but both should be checked against available mappings.

## Recommended Edits

- Preserve the DSMZ 214 `ADJUST_PH` preparation step in the generated merge.
- Make the DSMZ / MediaDive 214 source identity explicit in regenerated provenance, not only through `parent_media`.
- Fix the malformed KOMODO history timestamp in the normalized KOMODO parent.
- Attempt complex-component grounding for Yeast extract and Malt extract.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Revalidate embedded `curation_history` once the timestamp is corrected upstream.
- Recompare the regenerated record against MediaDive 214 to verify ingredient quantities, pH 7.2, and the liquid-medium CaCO3 instruction.

## Additional Notes

None found.
