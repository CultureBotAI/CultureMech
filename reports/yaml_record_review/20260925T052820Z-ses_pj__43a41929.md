# YAML Record Review: ses_pj__43a41929

- Repository: CultureMech
- Record: data/merge_yaml/merged/ses_pj__43a41929.yaml
- Started UTC: 2026-09-25T05:28:20Z
- Finished UTC: 2026-09-25T05:28:20Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` `CultureMech:000409`, `ses_pj`, from `data/merge_yaml/merged/ses_pj__43a41929.yaml`.

The target record is an older direct MediaDive/CCAP C94 import for CCAP `SES:PJ`.

## Validation

The generated record passed open LinkML validation, strict validation, reference validation, and term validation.

Strict validation wrote only the TSV header, so it reported 0 strict rows.

Embedded `curation_history` was not checked because the repository history validator targets standalone `history/` files rather than `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

The record is correctly grounded to CCAP `MR_SES_PJ.pdf`.

It is a same-source duplicate of the collection-level CCAP `SES_PJ` import, but both generated branches currently need different repairs before they can collapse cleanly.

## Evidence

CCAP `MR_SES_PJ.pdf` defines `SES:PJ` as a 1:1 mixture of separately autoclaved SES and PJ media.

SES is built from K2HPO4, MgSO4 x 7H2O, KNO3, soil extract, deionized water, and separate stock preparation steps.

PJ is built from CaCl2 x 2H2O, KCl, K2HPO4, MgSO4 x 7H2O, deionized water, and separate stock preparation steps.

## Completeness

The generated record flattens SES and PJ stock components into a single parent ingredient list instead of representing a 1:1 mixture of two constituent media.

The K2HPO4 rows from different source scopes were summed to 21 g/L.

The MgSO4 x 7H2O rows from different source scopes were summed to 21 g/L.

The SE2 soil-extract source text is represented as a variable `Soil` ingredient, and the SE2 soil-preparation prose is duplicated in the preparation steps.

The record category is `bacterial`; the collection-level CCAP import and source context identify `SES:PJ` as an algae/protozoa medium.

The repaired normalized collection source already models this medium with 500 ml/L SES and 500 ml/L PJ.

## Findings

Nested CCAP constituent media and stock recipes were flattened into final-medium ingredients.

Same-named stock components were summed across SES and PJ scopes.

Process text for SE2 soil extract was imported as both a pseudo-ingredient and duplicated preparation prose.

The older direct MediaDive/CCAP branch remains separate from the repaired collection-level CCAP branch.

## Recommended Edits

Repair the direct CCAP branch or suppress it in favor of the repaired collection-level `data/normalized_yaml/algae/ses_pj.yaml` source.

Represent `SES:PJ` as 500 ml/L SES plus 500 ml/L PJ; do not flatten the two constituent recipes into parent rows.

Remove the variable `Soil` ingredient and keep SE2 soil handling only as scoped preparation text on the SES constituent medium.

Set the category to algae if the direct CCAP branch is retained.

Regenerate the merged YAML after the direct and collection CCAP branches are reconciled.

## Follow-up Checks

Confirm the regenerated `SES:PJ` duplicate set has one canonical record for `MR_SES_PJ.pdf`.

Confirm the regenerated parent has no 20 g/L K2HPO4, 21 g/L MgSO4 x 7H2O, 20 g/L KNO3, variable `Soil`, or duplicated soil-extract steps.

Confirm the final record carries CCAP `SES_PJ` provenance and not bacterial category metadata.

## Additional Notes

Empty optional evidence and organism fields were not treated as defects in this generated record.
