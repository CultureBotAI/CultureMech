# YAML Record Review: rye_bran_mrs__984c5b2d

- Repository: CultureMech
- Record: `data/merge_yaml/merged/rye_bran_mrs__984c5b2d.yaml`
- Started UTC: `2026-09-25T03:22:18Z`
- Finished UTC: `2026-09-25T03:22:18Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `rye_bran_mrs__984c5b2d`, a single-source merge of `data/normalized_yaml/bacterial/rye_bran_mrs.yaml`.

The record represents the direct MediaDive JCM `J730` import for JCM Medium 730, `RYE-BRAN MRS`. The normalized JCM source has already been repaired to preserve Rye-bran extract as a nested 900 ml/L solution and to point at the TOGO `M753` copy as a source duplicate, but the generated merge YAML drops that repaired graph.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` exited 0 with no diagnostics.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to `mediadive.medium:J730`, and its notes link to the JCM 730 source page for `RYE-BRAN MRS`. The record should be recognized as the source duplicate parent of TOGO `M753`, but that `variant_children` relationship is present only in the normalized YAML, not in the generated merge YAML.

## Evidence

The live JCM 730 page lists a main RYE-BRAN MRS medium containing 55 g Lactobacilli MRS broth (BD-Difco), 900 ml Rye-bran extract, and 100 ml distilled water.

The same page defines Rye-bran extract as its own 1 L subrecipe containing 40 g rye-bran, 2 g malted wheat meal, 0.8 g Trypsin (Sigma-Aldrich), and 1 L distilled water. It instructs the user to mix those extract components, incubate at 50 C for 24 h, clarify the turbid liquid by centrifugation at 500 x g for 15 min, and then paper-filter it.

## Completeness

The generated record preserves the 55 g Lactobacilli MRS broth row and the three Rye-bran-extract solids.

It loses or corrupts these details:

- The 900 ml/L Rye-bran extract aliquot is absent.
- The 100 ml final-medium water and the 1 L Rye-bran-extract water are absent.
- Rye-bran, malted wheat meal, and trypsin are flattened from the extract into the parent ingredient list.
- The curated source-duplicate child link to TOGO `M753` is absent.
- The source description of 50 C incubation, 500 x g centrifugation, and paper filtration is collapsed into one preparation step and is no longer scoped specifically to Rye-bran extract.

## Findings

- `needs curation`: The generated record loses the defining 900 ml/L Rye-bran extract aliquot. Only Lactobacilli MRS broth and the extract's internal solids remain as parent ingredients.
- `needs curation`: The Rye-bran extract stock was flattened into the parent medium: 40 g/L rye-bran, 2 g/L malted wheat meal, and 0.8 g/L trypsin appear as final-medium ingredients instead of nested extract components.
- `needs curation`: Water is lost entirely. The generated file contains neither the 100 ml final-medium water nor the 1 L Rye-bran-extract water that JCM lists.
- `needs curation`: The generated file drops the normalized source's `variant_children` link to TOGO `M753`, so the two curated source-duplicate copies remain disconnected in generated output.
- `pass with minor issues`: Lactobacilli MRS broth remains ungrounded in the generated record even though the normalized source carries a `CultureMech:009017` grounding.
- `pass with minor issues`: The extract-production procedure survives only as one inherited preparation sentence; the repaired normalized source had already split it into a 50 C incubation step and a 500 x g plus paper-filtration clarification step.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/rye_bran_mrs__984c5b2d.yaml`; fix the solution-preserving merge path so repaired nested solutions in `data/normalized_yaml/bacterial/rye_bran_mrs.yaml` survive regeneration.
- Preserve Rye-bran extract as a nested 900 ml/L parent aliquot with 40 g/L rye-bran, 2 g/L malted wheat meal, 0.8 g/L trypsin, and 1 L distilled water scoped to the extract.
- Restore the 100 ml final-medium distilled water row.
- Preserve or materialize the curated `variant_children` source-duplicate relationship to TOGO `M753`.
- Preserve the `CultureMech:009017` grounding for Lactobacilli MRS broth during merge generation.
- Carry over the repaired normalized preparation steps instead of collapsing the extract workflow back into one generic `MIX` step.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/rye_bran_mrs.yaml` and the regenerated merge YAML.
- Regenerate `data/merge_yaml/merged/rye_bran_mrs__984c5b2d.yaml` and verify that Rye-bran extract remains a nested 900 ml/L solution rather than direct parent rye-bran, malted wheat meal, and trypsin rows.
- Verify that both source-scoped water rows are present.
- Verify that the generated JCM and TOGO Rye-Bran MRS records retain their source-duplicate relationship or are merged into one generated source-duplicate group.
- Run an exact ignored-file-inclusive search for `mediadive.medium:J730`, `TOGO:M753`, and `rye_bran_mrs` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. The normalized source is materially better than this generated YAML; the issue is that the generation path discards the repaired nested Rye-bran-extract graph.
