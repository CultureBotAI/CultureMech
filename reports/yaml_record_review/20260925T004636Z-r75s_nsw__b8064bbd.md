# YAML Record Review: r75s_nsw__b8064bbd

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml
- Started UTC: 2026-09-25T00:45:35Z
- Finished UTC: 2026-09-25T00:46:36Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:000402 |
| Name | r75s_nsw |
| Class | MediaRecipe |
| Category | bacterial |
| Source identity | CCAP Medium C88 / R75S:NSW |
| Maintained owner | data/normalized_yaml/bacterial/r75s_nsw.yaml |
| Generated path | data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml |

The double-hash suffix is a generated filename collision guard. This target is the older bacterial CCAP C88 import, not the same `CultureMech:000119` algae record in `data/merge_yaml/merged/r75s_nsw.yaml`.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml` exited 0 and printed `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml --out /private/tmp/r75s_nsw_b8064bbd.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

## Identity and Grounding

- The generated record identifies CCAP C88 / R75S:NSW, a two-part mixture of R75S and one part NSW.
- The maintained bacterial C88 owner now records that mixture as prepared R75S at 666.7 ml/L plus NSW at 333.3 ml/L, with a `SUPPLEMENTED_VARIANT` parent link to `data/normalized_yaml/bacterial/r75s.yaml`.
- The generated record was produced before that September 6, 2026 repair. It still merges the base CCAP C87 R75S record and the C88 R75S:NSW child, preserves only C87-style direct ingredients, and lists `r75s` as a synonym of `r75s_nsw`.

## Evidence

Supported source-backed claims:

- The CCAP R75S:NSW source supports the 2:1 R75S-to-NSW mixture, separate autoclaving, and aseptic mixing after cooling.
- The CCAP R75S source supports 750 ml natural filtered seawater, 250 ml deionized water, and 1 g dried milled rye grass per liter for the prepared R75S base medium.

Unsupported or over-scoped claims:

- The generated `merged_from` list claims `r75s` and `r75s_nsw` are duplicates. They are a parent and child in the maintained graph, and the C88 child is not identical to the C87 base.
- The generated `ingredients` flatten only the R75S portion. It omits the 333.3 ml/L NSW contribution and the top-level 666.7 ml/L dilution of prepared R75S documented in the maintained owner.
- The generated `preparation_steps` combine an initial "2:1 mixture" with expanded R75S and NSW instructions, but the generated formula no longer says how much prepared R75S and prepared NSW to mix.

## Completeness

- The review checked the complete generated C88 record, the repaired bacterial C88 and C87 normalized records, the sibling generated algae collision, and the exact CCAP R75S_NSW and R75S PDFs.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, `data/raw`, and `reports` for `r75s_nsw`, `R75S`, `R75S NSW`, and `R75S_NSW` found the active bacterial C87/C88 and algae owners, generated outputs, raw MicrobeMediaParam CCAP mappings, and archived reports.
- Empty `target_organisms` is not a defect; the inspected CCAP files are formulation recipes, not strain-growth reports.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Blocker | The generated record collapses a CCAP parent medium into its NSW child. | `merged_from` lists both `r75s` and `r75s_nsw`, and `synonyms` exposes CCAP C87 as a synonym of CCAP C88. The repaired normalized records model C88 as a `SUPPLEMENTED_VARIANT` of C87, not a source duplicate. | `src/culturemech/merge/fingerprint.py`, `src/culturemech/merge/matcher.py`, and regenerated `data/merge_yaml/merged/` |
| Major | The formula is incomplete for R75S:NSW. | The generated direct ingredients are natural sea water at 750 g/L and rye grass at 1 g/L, which describe the R75S base. The R75S:NSW child should contain prepared R75S and NSW solution amounts, as in `data/normalized_yaml/bacterial/r75s_nsw.yaml`. | Regenerated `data/merge_yaml/merged/r75s_nsw__b8064bbd.yaml` from the repaired normalized owner |
| Minor | The generated record is stale relative to the September 6, 2026 graph repair. | The maintained owner has a `repair_ccap_bacterial_score40.py` curation event with `sources`, `solutions`, `sterilization`, and `parent_media`; the generated record ends with the August 6, 2026 merge event. | Merge regeneration workflow |

## Recommended Edits

1. Change merge fingerprinting so a child made from a prepared base solution is not fingerprinted as only its base ingredient set.
2. Regenerate the bacterial C88 generated record from `data/normalized_yaml/bacterial/r75s_nsw.yaml` after the merge change.
3. Verify that regenerated CCAP C88 contains 666.7 ml/L R75S and 333.3 ml/L NSW solution references, and that CCAP C87 appears only as `parent_media`, not as a generated synonym.
4. Add a regression test for CCAP C87/C88 that fails if `r75s.yaml` and `r75s_nsw.yaml` merge into one record.

## Follow-up Checks

- Run `just verify-merges` and `just audit-merge-freshness` after regenerating merge outputs.
- Run focused schema, strict, term, and reference validation on the regenerated bacterial R75S:NSW generated record.
- Inspect the regenerated YAML and page for a lingering `Natural sea water` 750 g/L + `Rye grass` 1 g/L flattened child formula.
- Re-run a gitignore-independent exact search for `mediadive.medium:C87`, `mediadive.medium:C88`, and `r75s_nsw__b8064bbd` to verify the generated collision record no longer misrepresents the C87/C88 relationship.

## Additional Notes

- The generated algae `CultureMech:000119` record has a related N75S/R75S false merge, but this review covers only the bacterial `CultureMech:000402` collision record.
- This review did not modify generated YAML or normalized CCAP records.
