# YAML Record Review: r3_a_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r3_a_medium.yaml
- Started UTC: 2026-09-25T00:40:05Z
- Finished UTC: 2026-09-25T00:43:29Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:003863 |
| Name | r3_a_medium |
| Class | MediaRecipe |
| Category | bacterial |
| Source identity | KOMODO Medium 1153, explicitly mapped to DSMZ / MediaDive 1153 |
| Maintained sources | data/normalized_yaml/bacterial/KOMODO_1153_R3_A_medium.yaml and data/normalized_yaml/bacterial/r3_a_medium.yaml |
| Generated path | data/merge_yaml/merged/r3_a_medium.yaml |

The reviewed file is a generated merge product. Future fixes should change maintained normalized records only when their source-owned data are wrong, or otherwise change the merge grouping in `src/culturemech/merge/fingerprint.py` / `src/culturemech/merge/matcher.py` and regenerate `data/merge_yaml/merged/`.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r3_a_medium.yaml` exited 0 with no diagnostics. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r3_a_medium.yaml --out /private/tmp/r3_a_medium.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r3_a_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r3_a_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

These validators check shape and resolvable terms, but they do not check that a generated merged record still denotes one source medium.

## Identity and Grounding

- DSMZ / MediaDive medium 1153 is `R3 A MEDIUM`, pH 7.2, with one 1000 ml main solution. KOMODO medium 1153 mirrors DSMZ medium 1153, so the KOMODO and direct MediaDive records are real source duplicates.
- The generated record preserves the KOMODO 1153 identity in `media_term`, but its `merged_from` list combines 11 normalized recipes: DSMZ/KOMODO 1153 plus DSMZ/KOMODO 1320, DSMZ/KOMODO 919, DSMZ 830, DSMZ 830c, DSMZ 1492, DSMZ 1578, and DSMZ 1641. Those are related R2A-family recipes, not all source duplicates of R3 A.
- The repository's maintained links already distinguish that shape: `data/normalized_yaml/bacterial/r3_a_medium.yaml` is a `CONCENTRATION_VARIANT` child of DSMZ 830 `r2a_medium`, while `data/normalized_yaml/bacterial/KOMODO_1153_R3_A_medium.yaml` is only a `SOURCE_DUPLICATE` of DSMZ 1153.

## Evidence

Supported source-backed claims:

- DSMZ medium 1153 supports the R3 A identity, pH 7.2, and this per-liter formula: 1 g yeast extract, 1 g proteose peptone, 1 g casamino acids, 1 g glucose, 1 g starch, 0.5 g sodium pyruvate, 0.6 g K2HPO4, 0.1 g MgSO4 x 7 H2O, 15 g agar, and 1000 ml distilled water.
- DSMZ medium 830 supports R2A as a lower-concentration sibling with 0.5 g/L yeast extract, proteose peptone, casamino acids, glucose, and starch; 0.3 g/L Na-pyruvate and K2HPO4; and 0.05 g/L MgSO4 x 7 H2O.
- DSMZ media 1320, 1492, 1578, 830c, and 919 support related formulas named HALF STRENGTH R2A MEDIUM IN 75% SEAWATER, HALF STRENGTH R2A, 1/10 R2A MEDIUM, REACTIVATION WITH LIQUID MEDIUM 830, and LIMNOBACTER MEDIUM, respectively; they are not evidence that all of those source accessions are source duplicates of R3 A.

Unsupported or over-scoped claims:

- The generated `merged_from` claim that the 11 source recipes collapse to one source-duplicate record is unsupported. It is a consequence of the merge fingerprint ignoring concentrations and source relationships.
- The generated `ingredients` list is not the DSMZ 1153 R3 A formula. It carries the DSMZ 1641 double-concentrated 830 formula with both `Na-pyruvate` at 0.6 g/L and `Sodium pyruvate` at 0.6 g/L; R3 A has one sodium pyruvate row at 0.5 g/L.
- The generated synonyms that attach DSMZ 1578, 1641, 1492, 1320, 919, 830, and 830c source accessions to this KOMODO 1153 record are broader than source identity supports.

## Completeness

- The consequential false merge was confirmed from the complete generated YAML, both maintained R3 A records, all same-fingerprint normalized records named in `merged_from`, and the exact DSMZ / MediaDive REST records for 1153, 830, 1641, 1578, 1492, 1320, 919, and 830c.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, `data/raw`, and `reports` for `mediadive.medium:1153`, `komodo.medium:1153`, `KOMODO_1153_R3_A_medium`, and `DSMZ_1153_R3_A_MEDIUM` found only the active normalized DSMZ/KOMODO 1153 owners plus generated indexes and historical reports; `data/raw_yaml` is absent in this checkout.
- Empty `target_organisms`, `references`, and optional environmental fields are not defects for this generated source recipe because the inspected upstream formula records do not supply organism-growth assertions for this record.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Blocker | The generated R3 A record is a false merge of distinct R2A-family media. | `merged_from` combines 11 recipes, including 1/10 R2A, half-strength R2A, half-strength R2A in 75% seawater, Limnobacter medium, R2A, and 830c reactivation medium. DSMZ / MediaDive identifies those as separate source records with different names and concentrations. | `src/culturemech/merge/fingerprint.py`, `src/culturemech/merge/matcher.py`, and regenerated `data/merge_yaml/merged/` |
| Major | The generated ingredients describe DSMZ 1641, not KOMODO/DSMZ 1153. | The generated record has 0.6 g/L `Na-pyruvate` plus 0.6 g/L `Sodium pyruvate`; DSMZ 1153 has one `Sodium pyruvate` row at 0.5 g/L, while DSMZ 1641 has those two 0.6 g/L pyruvate rows. | Merge representative selection in `src/culturemech/merge/merger.py` after the false grouping is fixed |
| Major | Source provenance is over-scoped by the generated synonyms and `merged_from` fields. | The generated record exposes MediaDive source IDs 1578, 1641, 1492, 1320, 919, 830, and 830c as synonyms of KOMODO 1153 even though the maintained normalized files model them as concentration variants, source duplicates of other DSMZ records, or a reactivation medium. | Merge grouping and regenerated `data/merge_yaml/merged/r3_a_medium.yaml` |

## Recommended Edits

1. Change the merge grouping so concentration variants and supplemented variants with the same ingredient identities are not collapsed as duplicates. At minimum, the fingerprint or candidate grouping must include concentration signatures, source-duplicate assertions, or another discriminator that keeps DSMZ 1153 separate from DSMZ 830, 830c, 919, 1320, 1492, 1578, and 1641.
2. Regenerate `data/merge_yaml/merged/r3_a_medium.yaml` so it contains only the real DSMZ 1153 / KOMODO 1153 source-duplicate pair if generated merge output still coalesces source mirrors.
3. Confirm that the regenerated R3 A formula contains one sodium pyruvate entry at 0.5 g/L and no inherited 0.6 g/L `Na-pyruvate` / `Sodium pyruvate` duplicate from DSMZ 1641.
4. Confirm that R2A-family concentration variants remain connected by `parent_media` / `variant_children` under `data/normalized_yaml/bacterial/r2a_medium.yaml` and do not appear as generated synonyms of R3 A.

## Follow-up Checks

- Run `just verify-merges` and `just audit-merge-freshness` after changing merge logic and regenerating `data/merge_yaml/merged/`.
- Run a focused open-schema, strict-schema, reference, and term validation on regenerated `data/merge_yaml/merged/r3_a_medium.yaml`.
- Inspect the regenerated YAML and its rendered page to ensure the visible provenance names only DSMZ/MediaDive 1153 and KOMODO 1153 as sources for R3 A.
- Re-run or extend a merge regression test that places DSMZ 1153, DSMZ 1641, and DSMZ 1578 in the same input set and asserts they produce distinct generated records.

## Additional Notes

- `src/culturemech/merge/merge_recipes.py` explicitly documents the current behavior: matching is based on ingredient identity only and ignores concentration, pH, temperature, and other parameters. That behavior is too broad for generated records that are expected to preserve source-level medium identity.
- This review did not modify the generated record or its maintained normalized inputs.
