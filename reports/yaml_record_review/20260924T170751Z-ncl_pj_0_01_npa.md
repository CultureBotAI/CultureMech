# YAML Record Review: ncl_pj_0_01_npa

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ncl_pj_0_01_npa.yaml
- Started UTC: 2026-09-24T17:06:53Z
- Finished UTC: 2026-09-24T17:07:50Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/ncl_pj_0_01_npa.yaml` as a generated `MediaRecipe` for `CultureMech:000379`, label `ncl_pj_0_01_npa`, original name `NCL:PJ/0.01%NPA`, category `bacterial`, and physical state `SOLID_AGAR`.

The generated record merged two normalized MediaDive/CCAP imports: `data/normalized_yaml/bacterial/ncl_0_01_npa.yaml` for `CultureMech:000378`, CCAP medium C66, and `data/normalized_yaml/bacterial/ncl_pj_0_01_npa.yaml` for `CultureMech:000379`, CCAP medium C67. The generated YAML is derived; fixes belong in the normalized source records and in duplicate-detection or merge logic.

## Validation

| Check | Result |
|---|---|
| Open schema, `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ncl_pj_0_01_npa.yaml` | Passed; `No issues found`. |
| Strict schema, `python scripts/validate_strict.py data/merge_yaml/merged/ncl_pj_0_01_npa.yaml --out /private/tmp/ncl_pj_0_01_npa.strict.tsv --workers 1 --quiet` | Passed; TSV had the header only, 1 line and 0 errors. |
| Reference validation, `linkml-reference-validator validate data data/merge_yaml/merged/ncl_pj_0_01_npa.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were available. |
| Term validation, `linkml-term-validator validate-data data/merge_yaml/merged/ncl_pj_0_01_npa.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries inside a single merged YAML file. |

## Identity and Grounding

- The target nominally denotes `NCL:PJ/0.01%NPA`, the CCAP C67 record imported through MediaDive.
- The generated record's `SOURCE_DUPLICATE` parent relation to `NCL/0.01%NPA` is not supported by the inspected PDFs. `NCL:PJ/0.01%NPA` is a biphasic recipe containing an NCL:PJ liquid made as a 3:1 NCL:PJ mixture, plus the 0.01% NPA agar recipe; `NCL/0.01%NPA` is a biphasic NCL plus 0.01% NPA recipe without the 3:1 NCL:PJ phase.
- A gitignore-independent hidden-file search for `CultureMech:000379`, `CultureMech:000378`, `CultureMech:000104`, `CCAP Medium C67`, `CCAP Medium C66`, `CCAP:NCL_PJ_0_01_NPA`, `MR_NCL_PJ_0_01_NPA`, `NCL:PJ/0.01%NPA`, `NCL/0.01%NPA`, and `database_id: NCL_PJ_0_01_NPA` across `data/normalized_yaml` and `data/merge_yaml` found the two bacterial merge inputs and a separate algae CCAP import, `data/normalized_yaml/algae/ncl_pj_0_01_npa.yaml`, for the same `MR_NCL_PJ_0_01_NPA.pdf`.
- A second gitignore-independent hidden-file search for `CCAP:NCL_0_01_NPA`, `MR_NCL_0_01_NPA`, and `database_id: NCL_0_01_NPA` found the C66 PDF in `data/normalized_yaml/bacterial/ncl_0_01_npa.yaml`, `data/normalized_yaml/algae/ncl_0_01_npa.yaml`, and the related imported NPA subrecipe records.

## Evidence

The inspected C67 CCAP PDF supports a biphasic `NCL:PJ/0.01%NPA` recipe:

- An NCL:PJ phase that is itself a 3:1 mixture prepared from NCL and PJ.
- NCL prepared from 1.0 g cereal grass leaves and 1 liter PJ.
- PJ prepared from three 100 ml stock solutions, each added at 1.0 ml per liter.
- A 0.01% NPA phase prepared as 1 liter NCL, but with only 0.1 g cereal leaves and 15 g bacteriological agar.

The inspected C66 CCAP PDF supports the 0.01% NPA phase as a biphasic NCL/0.01% NPA recipe, but it does not contain the C67 3:1 NCL:PJ mixture. The generated record collapses both source media into one fingerprint and then flattens PJ stock ingredients into final `G_PER_L` values, so it neither preserves the C67 liquid-to-agar phase relationship nor the C66/C67 distinction.

## Completeness

- Consequential gap: the C67 NCL:PJ phase, the 0.01% NPA agar phase, NCL, and PJ are not represented as separate subrecipes or phase-specific formulation blocks.
- Consequential gap: the C66 and C67 source recipes are merged as duplicates rather than retained as distinct biphasic variants.
- Consequential gap: the CCAP direct import of C67 is a separate active normalized record with a different CultureMech ID and only one `KCl` ingredient.
- Empty optional fields are not defects. The inspected CCAP PDFs do not supply a pH, a target strain, or a temperature.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The generated record incorrectly merges C66 `NCL/0.01%NPA` and C67 `NCL:PJ/0.01%NPA` as `SOURCE_DUPLICATE` records. | The C67 PDF includes a 3:1 NCL:PJ recipe plus a 0.01% NPA agar phase; the C66 PDF describes NCL/0.01% NPA and does not include the 3:1 NCL:PJ phase. | Fix the relationship between `data/normalized_yaml/bacterial/ncl_0_01_npa.yaml` and `data/normalized_yaml/bacterial/ncl_pj_0_01_npa.yaml`, then regenerate merged YAML. |
| major | The ingredient list is flattened across nested phases and stock recipes. | `Cereal leaves` is stored as `1.1 G_PER_L`, summing a 1.0 g/L NCL row with a 0.1 g/L 0.01% NPA row; PJ stock salts are direct `G_PER_L` final-medium ingredients even though the source uses 1 ml of each stock per liter. | Recurate `data/normalized_yaml/bacterial/ncl_pj_0_01_npa.yaml` with phase and stock boundaries preserved. |
| major | Active algae-side CCAP records duplicate the same C66 and C67 source PDFs with unrelated CultureMech IDs. | Hidden-file exact searches under `data/normalized_yaml` and `data/merge_yaml` found `data/normalized_yaml/algae/ncl_0_01_npa.yaml` and `data/normalized_yaml/algae/ncl_pj_0_01_npa.yaml` for the same CCAP PDF URLs used by the bacterial MediaDive imports. | Resolve the bacterial and algae duplicate owners for C66 and C67, or document a real source-backed distinction that justifies separate active records. |

## Recommended Edits

1. Split the generated C66/C67 source-duplicate merge by changing the normalized relationship between `data/normalized_yaml/bacterial/ncl_0_01_npa.yaml` and `data/normalized_yaml/bacterial/ncl_pj_0_01_npa.yaml` away from `SOURCE_DUPLICATE`.
2. Recurate `data/normalized_yaml/bacterial/ncl_pj_0_01_npa.yaml` so it preserves C67 as a biphasic recipe with a 3:1 NCL:PJ phase and a 0.01% NPA agar phase.
3. Recurate `data/normalized_yaml/bacterial/ncl_0_01_npa.yaml` as the distinct C66 biphasic NCL/0.01% NPA medium.
4. Resolve the active algae CCAP imports for C66 and C67 against their MediaDive counterparts before regenerating `data/merge_yaml/merged/`.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on both normalized C66/C67 owners and the regenerated merged files.
- Re-run a gitignore-independent hidden-file search for `MR_NCL_0_01_NPA` and `MR_NCL_PJ_0_01_NPA` under `data/normalized_yaml` and `data/merge_yaml` to confirm the C66 and C67 source families are deliberately linked but not merged as one formula.
- Manually compare the regenerated C67 record against the CCAP C67 PDF and verify that the 3:1 NCL:PJ phase is still visible.

## Additional Notes

Both CCAP PDFs were fetched directly from the URLs in the bacterial and algae records and rendered with `mutool draw -F txt`. They use the older two-page CCAP layout with no revision block or review date on the rendered pages.
