# YAML Record Review: medium-H

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/medium_h.yaml
- Started UTC: 2026-09-24T02:14:33Z
- Finished UTC: 2026-09-24T02:15:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Record ID | CultureMech:008311 |
| Name | medium_h |
| Original name | medium-H |
| Category | bacterial |
| Medium source | TOGO:M1748, derived from NBRC_M959 |
| Target path | data/merge_yaml/merged/medium_h.yaml |
| Maintained owner | data/normalized_yaml/bacterial/medium_h.yaml |
| Generated status | Generated by merge_recipes.py from 1 normalized source on fingerprint ee41f8659cad4ec15967af16ec2a01f5b140b5ec93bbd9a772655a8c1a7c4239 |

## Validation

- `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/medium_h.yaml`: Passed with `No issues found`.
- `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/medium_h.yaml --out /private/tmp/medium_h.strict.tsv --workers 1 --quiet`: Passed; 1 file scanned and 0 error rows.
- `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/medium_h.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`: Passed; 0 reference checks.
- `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/medium_h.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`: Passed after the known `eutils/pkg_resources` warning.
- Embedded history validator: Not checked. The documented `just validate-history` path validates standalone files under `history/`, not `MediaRecipe.curation_history` embedded in generated merged YAML.

## Identity and Grounding

- The TOGO API confirms that `TOGO:M1748` is `medium-H`, original media ID `NBRC_M959`, with source URL `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=959`.
- NBRC Medium 959 is also `medium-H`, and its live table lists glucose 2 g, yeast extract 0.2 g, monosodium glutamate 0.5 g, agar if needed 12 g, and seawater 1 L.
- The reviewed generated YAML is stale relative to its maintained owner. `data/normalized_yaml/bacterial/medium_h.yaml` already has a 2026-09-12 `RESOLVED_TOGO_M1748_SCORE15` curation event, a corrected `Seawater` concentration of `1.0 L`, explicit TOGO/NBRC source notes on every ingredient, references, FOODON grounding for yeast extract, and CHEBI grounding for agar; `data/merge_yaml/merged/medium_h.yaml` was last merged on 2026-08-06 and still has `Seawater` at `1 G_PER_L`.
- `rg --no-ignore --hidden` over maintained bacterial YAML and generated merged YAML for exact `TOGO:M1748`, `NBRC_M959`, `NO=959`, and `NBRC Medium 959` found this TOGO owner plus `data/normalized_yaml/bacterial/NBRC_960.yaml`, a direct NBRC import that also denotes NBRC Medium 959 and generates stale `data/merge_yaml/merged/960.yaml`.

## Evidence

- The generated record's yeast extract, monosodium glutamate, glucose, and agar amounts match TOGO M1748 and NBRC Medium 959 after the source's gram amounts are interpreted per 1 L seawater.
- The generated `Seawater` concentration is not source-faithful: TOGO and NBRC both list `1 L`, while the generated record says `1 G_PER_L`. The maintained owner has already corrected this to `1.0 L`.
- The source phrase for agar is conditional, `Agar (if needed)`. The generated record preserves the label but also promotes the whole recipe to `physical_state: SOLID_AGAR`, which makes the agar form unconditional.
- NBRC Medium 959 is present twice in maintained YAML: once as TOGO M1748/`medium_h` and once as `NBRC_960.yaml` with `media_term` `nbrc.medium:959`. The duplicate NBRC owner now has the same five-component formula, including `Seawater` at `1 L`, after its 2026-09-07 source-metadata repair.

## Completeness

- Consequential gaps: the generated TOGO record needs regeneration from its repaired owner, and TOGO M1748 and the direct NBRC Medium 959 import still need source identity reconciliation.
- Empty optional slots: The absence of preparation steps in NBRC/TOGO is not a source mismatch; neither inspected source lists pH, sterilization, or incubation conditions.
- Bounded negative checks: The gitignore-independent exact source search found no maintained bacterial owner for NBRC Medium 959 beyond `medium_h.yaml` and `NBRC_960.yaml`; the broader `medium_h` slug is intentionally not used as an absence check because it has unrelated substring matches.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | The generated record is stale and re-exposes a fixed seawater unit defect. | TOGO M1748 and NBRC Medium 959 list seawater as `1 L`; the maintained owner stores `1.0 L`, but the generated YAML still stores `1 G_PER_L` and lacks the September repair history, ingredient sources, references, and yeast/agar grounding now present in `data/normalized_yaml/bacterial/medium_h.yaml`. | Regenerated `data/merge_yaml/merged/medium_h.yaml` from `data/normalized_yaml/bacterial/medium_h.yaml` |
| Major | NBRC Medium 959 is split across two normalized records and two generated records. | The exact ignored-inclusive source search found `medium_h.yaml` for TOGO M1748/NBRC_M959 and `NBRC_960.yaml` for direct `nbrc.medium:959`; both inspected maintained files now encode the same NBRC Medium 959 formula. | `data/normalized_yaml/bacterial/medium_h.yaml`, `data/normalized_yaml/bacterial/NBRC_960.yaml`, and merge identity logic |
| Major | Conditional agar is promoted to an unconditional solid-agar medium. | Both TOGO and NBRC call the component `Agar (if needed)` at 12 g; the reviewed record sets `physical_state: SOLID_AGAR` for the whole medium and has no liquid/base variant. | `data/normalized_yaml/bacterial/medium_h.yaml` and `data/normalized_yaml/bacterial/NBRC_960.yaml` |

## Recommended Edits

- Regenerate `data/merge_yaml/merged/medium_h.yaml` so the September 2026 normalized repair is reflected in generated YAML.
- Reconcile the TOGO M1748 and direct NBRC Medium 959 normalized owners so this source formula has one canonical generated identity.
- Model agar as conditional or variant-specific instead of making `SOLID_AGAR` unconditional for the whole medium.
- Regenerate `data/merge_yaml/merged/960.yaml` after the `NBRC_960.yaml` recovery so the stale concatenated-ingredient artifact no longer remains.

## Follow-up Checks

- Rerun the schema, strict, term, and reference validators on regenerated `medium_h.yaml`.
- Run `rg --no-ignore --hidden` with exact `TOGO:M1748`, `NBRC_M959`, `NO=959`, and `NBRC Medium 959` searches over `data/normalized_yaml/bacterial` and `data/merge_yaml/merged` to verify NBRC Medium 959 no longer has duplicate owners.
- Manually compare the regenerated record with the NBRC Medium 959 HTML table to verify it keeps 1 L seawater and preserves agar as an optional solidifying addition.

## Additional Notes

- The maintained TOGO owner intentionally leaves natural seawater unmapped; that is preferable to grounding it to water or a pure salt.
- `NBRC_960.yaml` appears to be an NBRC Medium 959 recovery under an off-by-one local filename/name. That identity cleanup is part of the duplicate-source reconciliation, not a defect introduced by `medium_h.yaml` itself.
