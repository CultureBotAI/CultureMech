# YAML Record Review: togo_medium_m1360

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1360.yaml`
- Started UTC: 2026-09-25T13:18:11Z
- Finished UTC: 2026-09-25T13:18:11Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1360, imported from JCM medium 1264.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1360.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/` records, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1360`; the TOGO API identifies JCM_M1264 as its original medium ID and links JCM medium 1264.

The JCM source lists 0.2 ml sodium lactate solution at approximately 60% and instructs post-autoclave pH adjustment to 10.0 with sterile 10% Na2CO3 solution.

## Evidence

Most generated parent ingredient amounts match the TOGO and JCM source rows.

The sodium lactate and Na2CO3 solution entries were migrated into `solutions`; the lactate volume became 0.2 g/L and both solution records have empty composition arrays with the placeholder name `Unknown solution`.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated record cannot reconstruct the source 0.2 ml sodium lactate solution addition.

## Findings

- Major issue: `Sodium lactate solution (approx. 60%)` is represented as an empty solution with 0.2 g/L, but the source gives 0.2 ml of that solution.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.
- Minor issue: pH 10.0 is absent.
- Minor issue: the variable 10% Na2CO3 pH-adjustment reagent appears as an empty `Unknown solution`.

## Recommended Edits

- Preserve the sodium lactate solution as a 0.2 ml/L source addition or convert it with explicit density/composition evidence if final sodium lactate mass is needed.
- Correct or omit the water row according to the repo's final-volume convention.
- Add `ph_value: 10.0` and a post-autoclave adjustment step that mentions sterile 10% Na2CO3 solution without inventing a volume.
- Remove or populate the empty `Unknown solution` stubs.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Verify that source solution volumes are not being coerced into g/L during solution migration.

## Additional Notes

Exact local source search found only the expected normalized TOGO M1360 source for this merge. Ignored files were included for duplicate-report checks with `find`, and no pre-existing ignored report for this generated record was found.
