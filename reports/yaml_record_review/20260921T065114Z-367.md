# YAML Record Review: 367

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/367.yaml`
- Started UTC: 2026-09-21T06:50:15Z
- Finished UTC: 2026-09-21T06:51:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007490` |
| Name | `367` |
| Original name | `367` |
| Category | `bacterial` |
| Source | NBRC Medium 366 |
| Source URL | `http://www.nbrc.nite.go.jp/NBRC2/NBRCMediumDetailServlet?NO=366` |
| Merge fingerprint | `415da4131cbcad746497717a4c0aeaa6bb385c66b7e3bb09082f12fa23959ca3` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_367.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated NBRC 366 import. Future corrections belong in `data/normalized_yaml/bacterial/NBRC_367.yaml`; the generated merge should then be regenerated rather than edited directly.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/367.yaml` | Pass: exit 0 |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/367.yaml --out /private/tmp/367.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/367.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: exit 0 |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/367.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated target is stale relative to the maintained owner. `data/normalized_yaml/bacterial/NBRC_367.yaml` keeps the same `CultureMech:007490` identity, adds `media_term.term.id: nbrc.medium:366`, adds `ph_value: 5.5`, adds the current NBRC reference, and replaces the generated target's two malformed ingredient rows with twelve NBRC Medium 366 rows.

The normalized owner still has a few ungrounded rows after the partial September repair. Meat extract is a complex extract and `MnSO4 x nH2O` has variable hydration, but `Diammonium hydrogencitrate` is a defined chemical row and is still listed as `UNRESOLVED_CHEMICAL` for `CultureMech:007490` in `data/import_tracking/reports/ungrounded_ingredients.tsv`. The `MgSO4 x 7H2O` row is also missing the specific magnesium sulfate heptahydrate grounding used elsewhere in this repository.

The old NBRC URL recorded in the import history now redirects to `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=366` with HTTP 200.

## Evidence

The live NBRC Medium 366 page contains a composition table with:

- 10 g peptone.
- 10 g meat extract.
- 5 g yeast extract.
- 20 g glucose.
- 1 g Tween 80.
- 2 g K2HPO4.
- 5 g sodium acetate.
- 2 g diammonium hydrogencitrate.
- 0.2 g MgSO4.7H2O.
- 0.05 g MnSO4.nH2O.
- 1 L distilled water.
- 15 g agar, if needed.
- pH 5.5.

The maintained owner captures the twelve source rows and final pH. The generated target instead has one row whose preferred term is the whole concatenated NBRC formula plus a second row with an empty `preferred_term` and `concentration.value: Peptone`.

## Completeness

Consequential gaps:

- The generated target is missing the maintained owner's recovered twelve-row composition.
- The generated target is missing the NBRC source term, final pH, structured NBRC reference, and ontology-grounding flags.
- The generated target still carries a malformed row with an empty ingredient label and a nonnumeric `Peptone` concentration value.
- The maintained owner still lacks CHEBI groundings for magnesium sulfate heptahydrate and diammonium hydrogencitrate.

Empty optional slots:

- `solutions` is correctly absent. NBRC 366 is a flat final-volume recipe in the live NBRC table.
- `preparation_steps` is correctly empty in the maintained owner. The live NBRC 366 table specifies the formula and final pH but no source preparation steps.
- `target_organisms` and `growth_metrics` are correctly absent. The NBRC medium page supports the recipe composition and pH, but it does not make a strain-specific growth claim.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-367.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, `reports`, `data/import_tracking`, `data/raw`, and `scripts` for `CultureMech:007490`, `NBRC_367`, and `NBRC, Media No. 366` found the normalized owner, generated merge, indexes/manifests, archived validation rows, and the ungrounded-ingredient report row.
- A gitignore-independent `find` under `data/raw` for `*366*` found no cached NBRC 366 raw source file. Ignored files were included under the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still carries the broken pre-recovery NBRC import. | The generated target has one collapsed full-formula ingredient and a second empty-label row whose value is `Peptone`; the maintained owner has already recovered NBRC Medium 366 as twelve ingredient rows, added `ph_value: 5.5`, added `nbrc.medium:366`, added the redirected NBRC reference, and recorded `RESOLVED_NBRC_PARTIAL_SCORE25`. | `data/normalized_yaml/bacterial/NBRC_367.yaml` |
| Minor | Magnesium sulfate heptahydrate is still ungrounded in the maintained owner. | The `MgSO4 x 7H2O` row in `NBRC_367.yaml` has no `term`, while the same ingredient is routinely grounded to `CHEBI:31795` in nearby NBRC records. | `data/normalized_yaml/bacterial/NBRC_367.yaml` |
| Minor | Diammonium hydrogencitrate is still an unresolved defined chemical. | The `Diammonium hydrogencitrate` row in `NBRC_367.yaml` has no `term`, and `data/import_tracking/reports/ungrounded_ingredients.tsv` still lists that name as `UNRESOLVED_CHEMICAL` for `CultureMech:007490`. | `data/normalized_yaml/bacterial/NBRC_367.yaml` |

## Recommended Edits

1. Add `CHEBI:31795` to the `MgSO4 x 7H2O` row.
2. Ground `Diammonium hydrogencitrate` to a specific term if one exists; otherwise keep it intentionally unmapped with row-level notes explaining why no exact term is available.
3. Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml` so `367.yaml` picks up the existing recovered twelve-row composition, source term, pH, and reference plus the edits above.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/NBRC_367.yaml` after curation.
- `just validate-strict data/normalized_yaml/bacterial/NBRC_367.yaml` after curation.
- `just validate-terms data/normalized_yaml/bacterial/NBRC_367.yaml` after grounding magnesium sulfate heptahydrate and reviewing diammonium hydrogencitrate.
- `just verify-merges` after regenerating generated records.
- `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against NBRC Medium 366 for the twelve component amounts and pH 5.5.

## Additional Notes

- `linkml-reference-validator` produced no diagnostics for this generated target.
- The current owner carries a generic `repair_nbrc_partial_score25.py` curation event, but a gitignore-independent repository-wide exact search found that filename only in normalized YAML histories; there is no current `scripts/repair_nbrc_partial_score25.py` file to inspect.
