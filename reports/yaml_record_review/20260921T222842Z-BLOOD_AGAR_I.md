# YAML Record Review: blood_agar_i

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/BLOOD_AGAR_I.yaml`
- Started UTC: 2026-09-21T22:28:42Z
- Finished UTC: 2026-09-21T22:30:55Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Generated record ID | `CultureMech:005245` |
| Generated label | `blood_agar_i` |
| Original name | `BLOOD AGAR I` |
| Canonical source term | `komodo.medium:420` / `BLOOD AGAR I` |
| Maintained owner | `data/normalized_yaml/bacterial/KOMODO_420_BLOOD_AGAR_I.yaml` |
| Other merge inputs | `data/normalized_yaml/bacterial/blood_agar_i.yaml`, `data/normalized_yaml/bacterial/medium_420_modified_for_dsm_4690.yaml` |
| Generated status | Derived from three active `data/normalized_yaml/bacterial/*.yaml` inputs by `merge_recipes.py`; future fixes belong in normalized YAML or import/repair rules, followed by merge regeneration |

The exact ignored-file-inclusive accession search
`rg --no-ignore --hidden -n 'CultureMech:005245|CultureMech:005244|CultureMech:001525|komodo\.medium:420|komodo\.medium:420_4690|mediadive\.medium:420' data/normalized_yaml data/merge_yaml`
found the three active normalized owners above plus generated normalized-index
and merged-record copies. It did not find a fourth maintained YAML owner for
the same stable IDs or source accessions.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open-schema LinkML | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/BLOOD_AGAR_I.yaml` | Passed, `No issues found` |
| Strict closed-schema validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/BLOOD_AGAR_I.yaml --out /private/tmp/BLOOD_AGAR_I.strict.tsv --workers 1 --quiet` | Passed, 1 file scanned, 0 error rows |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/BLOOD_AGAR_I.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with no diagnostics |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/BLOOD_AGAR_I.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils`/`pkg_resources` warning |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merged record; `just validate-history` targets standalone files under `history/` |

The documented `just validate-schema`, `just validate-strict`, and
`just validate-terms` entrypoints were not used directly because this checkout's
project `uv` environment currently tries to build `llvmlite==0.46.0` under
Python 3.13 and fails inside `setuptools` before a target-specific check runs.
The no-project Python 3.11 commands above exercise the same target validators.

## Identity and Grounding

- The canonical generated identity for `CultureMech:005245` is internally
  aligned with KOMODO source accession `420`: name `BLOOD AGAR I`, category
  `bacterial`, `physical_state: SOLID_AGAR`, and media term
  `komodo.medium:420`.
- The DSMZ duplicate `CultureMech:001525` correctly points at
  `mediadive.medium:420` for `BLOOD AGAR I`; the inspected DSMZ PDF is the
  authoritative formulation linked by both KOMODO pages.
- The generated record incorrectly co-merges
  `medium_420_modified_for_dsm_4690` as a source duplicate. The inspected
  KOMODO `420_4690` page is named `MEDIUM 420 MODIFIED FOR DSM 4690` and adds
  `V-factor` and `X-factor` rows to the base KOMODO `420` metabolite list, so
  it is a modified child medium rather than the same formulation.
- No ingredient evidence in DSMZ Medium 420 or the inspected KOMODO pages
  supports the added Brain Heart Infusion commercial-product expansion, the
  Trypticase Soy Agar-like casein/soy rows, or the standalone `Peptone` and
  `Meat extract` rows currently present in all three normalized owners.

## Evidence

Inspected sources:

| Source | Scope checked |
| --- | --- |
| `/private/tmp/DSMZ_Medium420.pdf` | DSMZ Medium 420 name, preparation text, and final horse-blood addition |
| `/private/tmp/komodo-media-420.html` | KOMODO source ID `420`, name `BLOOD AGAR I`, pH/aerobic flags, DSMZ instructions link, and KOMODO metabolite table |
| `/private/tmp/komodo-media-420_4690.html` | KOMODO source ID `420_4690`, name `MEDIUM 420 MODIFIED FOR DSM 4690`, DSMZ instructions link, and KOMODO metabolite table |

Supported:

- The base medium name and source identity are supported: KOMODO ID `420` is
  `BLOOD AGAR I`, and DSMZ Medium 420 is also `BLOOD AGAR I`.
- `SOLID_AGAR` is supported by the DSMZ instruction to sterilize Blood Agar No
  2 and by the KOMODO pages' agar rows.
- KOMODO's source page reports `PH Info: null`; the absence of a pH value is
  not itself a curation defect.

Unsupported or over-scoped:

- DSMZ Medium 420 does not support `Blood agar No 2` at `1000 G_PER_L`; the
  PDF names the commercial medium and then says to add `40 ml/l of sterile
  horse blood`, so the source distinguishes a base product from a
  post-sterilization final-volume blood addition.
- DSMZ Medium 420 does not support `Horse blood` at `40 G_PER_L`; the stated
  amount is volumetric, `40 ml/l`.
- None of the inspected DSMZ or KOMODO sources support the BHI rows for calf
  brain, beef heart, proteose peptone, dextrose, sodium chloride, and disodium
  phosphate or their `https://microbenotes.com/brain-heart-infusion-bhi-agar/`
  supplier notes.
- None of the inspected DSMZ or KOMODO sources support the independent
  `Casein peptone`, `Soy peptone`, or second `NaCl` rows.
- The DSMZ record's preparation steps 3-5 are from other formulations, and step
  2 is a rehydration note for freeze-dried ampoules rather than an instruction
  for preparing Blood Agar I.
- KOMODO `420_4690` supports `V-factor` and `X-factor` as modified-medium
  components, but `data/normalized_yaml/bacterial/medium_420_modified_for_dsm_4690.yaml`
  omits them and has no parent-media relationship after the Sept 2026 duplicate
  repair.

## Completeness

- Consequentially incomplete: `medium_420_modified_for_dsm_4690.yaml` is
  missing the `V-factor` and `X-factor` distinction that makes KOMODO `420_4690`
  a modified medium.
- Consequentially incomplete: the generated canonical record has no
  `preparation_steps` because the KOMODO duplicate became canonical, so the
  source-supported cool-to-50-C and horse-blood addition instruction from DSMZ
  is absent from the merged output.
- Acceptably empty: no pH should be added from the checked sources; DSMZ gives
  no pH for Medium 420 and KOMODO records `PH Info: null`.
- Acceptably empty: no organism-growth assertions are needed here; the checked
  DSMZ/KOMODO recipe pages do not assert strain-specific growth outcomes.
- Bounded search: ignored files were included in the exact accession search
  described under `Target`, and no additional maintained normalized owner for
  `CultureMech:005245`, `CultureMech:001525`, `CultureMech:005244`,
  `komodo.medium:420`, `komodo.medium:420_4690`, or `mediadive.medium:420`
  was found.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The Blood Agar I formulation blends several unrelated media into DSMZ/KOMODO 420. `Blood agar No 2` is modeled as `1000 G_PER_L`, horse blood is `40 G_PER_L`, and BHI/TSA-like constituents are asserted although DSMZ 420 only gives Oxoid Blood Agar No 2 with a `40 ml/l` sterile horse blood addition. | DSMZ Medium 420 PDF; KOMODO `420` source page. | Fix `data/normalized_yaml/bacterial/blood_agar_i.yaml` and `data/normalized_yaml/bacterial/KOMODO_420_BLOOD_AGAR_I.yaml`; remove the same unsupported expansion from `data/normalized_yaml/bacterial/medium_420_modified_for_dsm_4690.yaml` while preserving its true variant additions. |
| major | `medium_420_modified_for_dsm_4690` is incorrectly treated as an exact source duplicate of Blood Agar I. The source page for KOMODO `420_4690` is a DSM 4690 modification and contains `V-factor` and `X-factor`, which are absent from the maintained normalized record and should prevent a same-fingerprint source-duplicate merge. | KOMODO `420_4690` source page. | `data/normalized_yaml/bacterial/medium_420_modified_for_dsm_4690.yaml` |
| major | The only maintained DSMZ preparation block is polluted with unrelated steps and a non-preparation rehydration note. This loses the boundary between Blood Agar I preparation and DSMZ's freeze-dried ampoule usage guidance. | DSMZ Medium 420 PDF; `data/normalized_yaml/bacterial/blood_agar_i.yaml` steps 1-5. | `data/normalized_yaml/bacterial/blood_agar_i.yaml` |
| major | The generated merge is stale with respect to the Sept 2026 normalized duplicate-sum repair. It still carries `Agar` at `30.0 G_PER_L` with `Merged 2 duplicates: 15.0, 15.0`, while all three normalized owners have been repaired to `15.0 G_PER_L`. | Direct comparison of `data/merge_yaml/merged/BLOOD_AGAR_I.yaml` against the three active normalized inputs. | Regenerate `data/merge_yaml/merged/BLOOD_AGAR_I.yaml` after repairing normalized YAML; do not patch the generated merge directly. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/blood_agar_i.yaml`, recurate DSMZ Medium
   420 to the exact DSMZ source: an opaque Blood Agar No 2 base and `40 ml/l`
   sterile horse blood added after autoclaving and cooling. Remove the
   unsupported BHI/TSA-derived ingredient rows and move or delete the
   rehydration sentence so it is not a recipe step.
2. In `data/normalized_yaml/bacterial/KOMODO_420_BLOOD_AGAR_I.yaml`, recurate
   KOMODO `420` so it either preserves the KOMODO metabolite table or faithfully
   records its DSMZ 420 source formulation, but does not mix those sources with
   Brain Heart Infusion or Trypticase Soy Agar expansions.
3. In `data/normalized_yaml/bacterial/medium_420_modified_for_dsm_4690.yaml`,
   restore the `V-factor` and `X-factor` distinctions from KOMODO `420_4690`
   and model the record as a modified child of Blood Agar I rather than a
   source duplicate with the same ingredient fingerprint.
4. Rerun the merge generator after the normalized fixes so the derived
   `BLOOD_AGAR_I.yaml` drops the stale `30.0 G_PER_L` agar row and no longer
   co-merges KOMODO `420_4690` as an exact duplicate of `420`.

## Follow-up Checks

- Run `just validate-schema` and `just validate-strict` on each repaired
  normalized owner once the project `uv` environment can install under the
  maintained interpreter.
- Run the no-project open-schema, strict, term, and reference validators on the
  three repaired normalized owners while the Python 3.13 `llvmlite` failure is
  unresolved.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating
  merge outputs.
- Manually re-open the DSMZ 420 PDF and both KOMODO pages to confirm the final
  ingredient rows, volumetric horse-blood amount, V/X-factor variant rows, and
  preparation-step boundaries before accepting the curation.

## Additional Notes

- `find reports/yaml_record_review -name '20260921T222842Z-BLOOD_AGAR_I.md'`
  returned no path before this report was created.
- This review intentionally wrote only this Markdown report. The generated
  merge record and all three normalized owner YAML files were left unchanged.
