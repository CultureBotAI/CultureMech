# YAML Record Review: n_z_amine_a_medium__74b0c3f4

- Repository: CultureMech
- Record: data/merge_yaml/merged/n_z_amine_a_medium__74b0c3f4.yaml
- Started UTC: 2026-09-24T16:20:40Z
- Finished UTC: 2026-09-24T16:21:35Z
- Verdict: needs curation

## Target

Reviewed the generated MediaRecipe `CultureMech:003155` for
`n_z_amine_a_medium`, a direct JCM Medium J80 record grounded to
`mediadive.medium:J80`.

## Validation

Focused validation passed:

- LinkML `MediaRecipe` validation: passed; no issues found.
- Strict validation: passed with zero error rows in `/private/tmp/n_z_amine_a_medium__74b0c3f4.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded curation history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The direct JCM identity is correct: the live JCM `GRMD=80` page is
`N-Z AMINE A MEDIUM`. An exact ignored-inclusive search across
`data/normalized_yaml`, `data/merge_yaml/merged`, and top-level `data/*.tsv`
files also found the active TOGO M71 owner, `CultureMech:010127`, for the same
JCM source.

The two records are generated independently: this direct JCM owner is emitted
as `n_z_amine_a_medium__74b0c3f4.yaml`, while the TOGO owner is emitted as
`n_z_amine_a_medium.yaml`.

## Evidence

JCM `GRMD=80` lists:

- 5 g N-Z amine, type A (Sheffield)
- 1 g Beef extract (BD-Difco)
- 80 ml Glycerol
- 15 g Agar
- 1 L Distilled water

JCM also gives the default instruction to autoclave at 121 C for 15 minutes
unless a medium states otherwise. The TOGO `M71` mirror points to the same JCM
`M80` page and has the same five source ingredients.

## Completeness

This generated direct JCM record is missing the 1 L water row and has only four
ingredients. It also downscales the dry source masses: 5 g N-Z amine becomes
4.62963 g/L, 1 g Beef extract becomes 0.925926 g/L, and 15 g Agar becomes
13.8889 g/L. That is consistent with dividing the dry quantities by 1.08 L
after counting the 80 ml glycerol addition, but JCM gives those amounts in
the recipe with 1 L water and 80 ml glycerol.

`Glycerol` also remains a liquid source addition but is represented as
`80 G_PER_L`.

## Findings

1. The generated record omits `Distilled water`, 1 L.

2. The N-Z amine, Beef extract, and Agar amounts are scaled down from the JCM
   source values.

3. `Glycerol` is modeled as `80 G_PER_L` instead of 80 ml.

4. The default JCM autoclave instruction is missing.

5. TOGO M71 represents the same JCM 80 source and remains active but unlinked.

## Recommended Edits

Repair the direct JCM owner and reconcile it with TOGO M71:

- Restore the JCM dry ingredient values: 5 g/L N-Z amine, 1 g/L Beef extract,
  and 15 g/L Agar.
- Add `Distilled water`, 1 L.
- Preserve `Glycerol` as an 80 ml source addition.
- Add the default JCM autoclave step.
- Link this owner to `TOGO_M71_N-Z_Amine_A_Medium.yaml` as a source duplicate.

## Follow-up Checks

- Regenerate `data/merge_yaml/merged` and confirm JCM J80 and TOGO M71 no
  longer appear as unrelated generated recipes.
- Compare the regenerated record against JCM `GRMD=80` and TOGO `M71`.
- Re-run LinkML, strict, reference, and term validation on the regenerated
  record.

## Additional Notes

None found.
