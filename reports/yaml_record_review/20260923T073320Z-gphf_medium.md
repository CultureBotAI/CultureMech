# YAML Record Review: gphf_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/gphf_medium.yaml
- Started UTC: 2026-09-23T07:31:05Z
- Finished UTC: 2026-09-23T07:33:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | MediaRecipe |
| Generated status | Generated merge under `data/merge_yaml/merged/` |
| ID | `CultureMech:006001` |
| Name | `gphf_medium` |
| Original name | `GPHF-MEDIUM` |
| Category | `bacterial` |
| Canonical media term | `komodo.medium:553` |
| Merged sources | `KOMODO_553_GPHF-MEDIUM`, `gphf_medium` |

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/gphf_medium.yaml` | Passed; no issues found. |
| Strict validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/gphf_medium.yaml --out /private/tmp/gphf_medium.strict.tsv --workers 1 --quiet` | Passed; 0 ERROR rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/gphf_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/gphf_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | Not run | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` rows. |

## Identity and Grounding

This generated record correctly groups the KOMODO 553 import with MediaDive DSMZ 553. A gitignore-independent exact search for `komodo.medium:553`, `mediadive.medium:553`, `gphf_medium`, `GPHF-MEDIUM`, and `DSMZ_Medium553` across `data/normalized_yaml/` and `data/merge_yaml/merged/` found only the two expected normalized parents, this generated duplicate group, and source index references to those parents.

The ingredient grounding is narrow for glucose, calcium chloride dihydrate, and agar. Complex undefined ingredients such as Casein peptone, Yeast extract, and Beef extract are intentionally ungrounded.

## Evidence

The MediaDive REST record for medium 553 currently reports the same final recipe as the generated record except for the missing `1000 ml` Distilled water row: 10 g Glucose, 5 g Casein peptone, 5 g Yeast extract, 5 g Beef extract, 0.74 g CaCl2 x 2 H2O, 20 g Agar, and pH 7.2.

The linked DSMZ Medium 553 PDF gives the same name, calcium chloride hydrate, pH, and non-agar masses, but gives Agar at 15 g rather than 20 g. The generated record inherits the MediaDive REST value and therefore disagrees with the PDF linked as the DSMZ evidence source.

## Completeness

The KOMODO/DSMZ duplicate merge is appropriate and no split duplicate was found by the exact gitignore-independent search. The generated record is incomplete because it drops the final water row, and it needs source reconciliation for the DSMZ agar amount. Empty optional fields such as `target_organisms`, `references`, and `discussion` are not defects by themselves.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The final Distilled water row is missing. | MediaDive DSMZ 553 and the linked DSMZ Medium 553 PDF both include 1000 ml Distilled water, but the generated `ingredients` list stops after Agar. | MediaDive/KOMODO DSMZ 553 normalization and merge output. |
| Major | The agar amount needs authoritative DSMZ reconciliation. | MediaDive REST reports 20 g Agar and the linked DSMZ Medium 553 PDF reports 15 g Agar; the generated record currently picks the REST value without representing or resolving the conflict. | MediaDive DSMZ 553 import and source reconciliation. |
| Minor | The inherited KOMODO curation timestamp is malformed. | The generated `komodo-web-import` history row contains `2026-01-27T01:15:03.fZ`, which is not an ISO timestamp. | KOMODO import history normalization. |

## Recommended Edits

1. Restore the required 1000 ml/l Distilled water row to the generated DSMZ 553 ingredient list.
2. Reconcile DSMZ 553 Agar between the current MediaDive REST payload and the linked DSMZ PDF; once the authoritative source is chosen, encode the same value in both normalized parents before regenerating the merge.
3. Normalize the inherited KOMODO `komodo-web-import` curation timestamp to a valid ISO timestamp.
4. Regenerate the duplicate group from `data/normalized_yaml/bacterial/gphf_medium.yaml` and `data/normalized_yaml/bacterial/KOMODO_553_GPHF-MEDIUM.yaml`.

## Follow-up Checks

- Re-run open LinkML, strict, reference, and term validation on the regenerated DSMZ 553 record.
- Compare the regenerated ingredient list against the current MediaDive 553 REST payload and DSMZ Medium 553 PDF.
- Re-run the exact gitignore-independent search for `komodo.medium:553`, `mediadive.medium:553`, `gphf_medium`, `GPHF-MEDIUM`, and `DSMZ_Medium553` across `data/normalized_yaml/` and `data/merge_yaml/merged/` to confirm there are still no split source duplicates.

## Additional Notes

None found.
