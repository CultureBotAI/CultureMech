# YAML Record Review: MRS MEDIUM (pH 5.5)

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mrs_medium_ph_5_5.yaml
- Started UTC: 2026-09-24T14:55:12Z
- Finished UTC: 2026-09-24T14:56:45Z
- Verdict: needs curation

## Target

Reviewed generated `MediaRecipe` record `CultureMech:002293` / `mrs_medium_ph_5_5` at `data/merge_yaml/merged/mrs_medium_ph_5_5.yaml`.

- Generated source: `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml`
- Merge source: `mrs_medium_ph_5_5`
- Merge fingerprint: `d28ad947536bcdc1a81719a16939cc6cd93adc8b414d67a9963bce97e92b679a`
- Category: `bacterial`
- Medium term: `mediadive.medium:J111`, label `MRS MEDIUM (pH 5.5)`
- Source: JCM GRMD 111

## Validation

| Check | Result |
|---|---|
| Open LinkML schema with `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium_ph_5_5.yaml` | Passed. |
| Strict validator with `scripts/validate_strict.py data/merge_yaml/merged/mrs_medium_ph_5_5.yaml --out /private/tmp/mrs_medium_ph_5_5.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator with `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium_ph_5_5.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 0 checks were applicable. |
| Term validator with `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium_ph_5_5.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded `curation_history` | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml` records, not embedded `MediaRecipe.curation_history` events. |

## Identity and Grounding

The generated record identifies JCM GRMD 111, MRS MEDIUM at pH 5.5. The inspected JCM source is headed `111 MRS MEDIUM (pH 5.5)`, lists 1 L of MRS medium by reference to Medium No. 1, and says to adjust pH to 5.5.

The generated YAML preserves the JCM 111 identity, the pH 5.5 value, and the adjust-pH instruction, but the single base-medium ingredient is wrong:

- JCM: MRS medium, 1 L
- Generated YAML: `MRS medium`, 1000 `G_PER_L`
- Current maintained YAML: the complete JCM Medium 1 formulation expanded to component rows

The exact `CultureMech:002293` search across `data/normalized_yaml` and `data/merge_yaml` used `rg --no-ignore --hidden`. It found one maintained YAML owner, this generated merge, generated indexes, and the reciprocal child reference in `data/normalized_yaml/bacterial/TOGO_M1_MRS_Medium.yaml`.

## Evidence

JCM GRMD 111 makes MRS MEDIUM (pH 5.5) as a pH-adjusted variant of JCM Medium 1. The current normalized record was repaired on 2026-09-07: it expands JCM Medium 1, adds a `parent_media` link to `TOGO_M1_MRS_Medium.yaml`, carries references to GRMD 111, GRMD 1, and TOGO M1, and adds quality flags. The generated merge predates that repair and still contains the opaque 1000 `G_PER_L` base-medium row.

An exact ignored-file-inclusive search for `GRMD=111` using a negative lookahead for following digits found `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` and `data/normalized_yaml/bacterial/TOGO_M103_MRS_Medium_pH_5.5.yaml` as the two maintained records that cite this exact JCM medium. Both are pH 5.5 variants of JCM Medium 1 / TOGO M1, so they are duplicate curation targets.

The maintained expansion still has two inherited source-fidelity issues:

- JCM Medium 1 uses `MnSO4 xH2O`, a variable manganese sulfate hydrate, but the pH 5.5 expansion grounds `MnSO4 x n H2O` to generic `CHEBI:86360`.
- JCM pages include a default 121 C for 15 min autoclave instruction when no medium-specific exception is stated; the maintained pH 5.5 expansion only dissolves and adjusts pH.

## Completeness

The generated record is incomplete because it predates the 2026-09-07 normalized repair and therefore lacks the expanded formula, parent variant link, quality flags, and source references. The maintained owner is more complete but still needs the variable manganese hydrate and JCM default autoclave issues resolved. Empty target-organism and growth-metric slots are not defects; JCM GRMD 111 only specifies a recipe variant.

## Findings

### Major

1. The generated record is stale relative to the repaired maintained source.
   - Evidence: `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` has a 2026-09-07 repair event, expanded ingredients, parent-variant metadata, data-quality flags, and references; `data/merge_yaml/merged/mrs_medium_ph_5_5.yaml` still ends at the 2026-08-06 merge event.
   - Impact: generated consumers still see a single `1000 G_PER_L` wrapper row instead of the supported JCM Medium 1 formula at pH 5.5.
   - Owner: regenerate `data/merge_yaml/merged/mrs_medium_ph_5_5.yaml` from `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml`.

2. The JCM 111 pH variant is duplicated across MediaDive/JCM and TOGO imports.
   - Evidence: `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` and `data/normalized_yaml/bacterial/TOGO_M103_MRS_Medium_pH_5.5.yaml` both cite GRMD 111 and both describe the pH 5.5 variant of JCM Medium 1 / TOGO M1.
   - Impact: the same JCM recipe variant can publish as two generated records instead of one merged pH-variant recipe.
   - Owner: the two normalized records plus the merge/de-duplication rule for equivalent JCM and TOGO sources.

3. The maintained expansion over-grounds the variable manganese sulfate hydrate.
   - Evidence: JCM Medium 1 lists a variable manganese sulfate hydrate; the current normalized pH 5.5 record stores `MnSO4 x n H2O` with exact generic `CHEBI:86360`.
   - Impact: regenerating now will publish an exact CHEBI identity for a source row whose hydrate count is unresolved.
   - Owner: `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` and the duplicate TOGO M103 record, then regenerate merged output.

4. The maintained expansion omits the JCM default autoclave instruction.
   - Evidence: the JCM GRMD 111 page includes the default 121 C for 15 min sterilization statement, and it does not list a contrary exception.
   - Impact: the complete preparation would still be missing after regeneration.
   - Owner: `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` and the duplicate TOGO M103 record, then regenerate merged output.

### Minor

None found.

### Blocker

None found.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/mrs_medium_ph_5_5.yaml` so it picks up the 2026-09-07 normalized JCM J111 repair.
2. Merge or otherwise mark equivalent `data/normalized_yaml/bacterial/mrs_medium_ph_5_5.yaml` and `data/normalized_yaml/bacterial/TOGO_M103_MRS_Medium_pH_5.5.yaml` so JCM GRMD 111 is not emitted twice.
3. Remove exact CHEBI grounding from the `MnSO4 x n H2O` variable hydrate unless an exact variable-hydrate term is available.
4. Add the JCM default 121 C for 15 min autoclave step to the pH 5.5 variant preparation.
5. Append curation-history events to any changed normalized records and regenerate `data/merge_yaml/merged/`.

## Follow-up Checks

After curation, run the same focused generated-record checks:

- `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mrs_medium_ph_5_5.yaml`
- `python scripts/validate_strict.py data/merge_yaml/merged/mrs_medium_ph_5_5.yaml --workers 1 --quiet`
- `linkml-reference-validator validate data data/merge_yaml/merged/mrs_medium_ph_5_5.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
- `linkml-term-validator validate-data data/merge_yaml/merged/mrs_medium_ph_5_5.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`

Also rerun the merge freshness check and an exact ignored-file-inclusive `GRMD=111` search to prove the JCM 111 pH variant is no longer duplicated.

## Additional Notes

None found.
