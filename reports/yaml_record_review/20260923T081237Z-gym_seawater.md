# YAML Record Review: GYM + SEAWATER

- Repository: CultureMech
- Record: `data/merge_yaml/merged/gym_seawater.yaml`
- Started UTC: 2026-09-23T08:10:49Z
- Finished UTC: 2026-09-23T08:12:37Z
- Verdict: needs curation

## Target

Reviewed generated record `CultureMech:006706`, `gym_seawater`, merged from KOMODO medium 871 and DSMZ / MediaDive medium 871.

## Validation

- LinkML validation: passed.
- Strict validation: passed with 0 ERROR rows in `/private/tmp/gym_seawater.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded history validation: Not checked: the available `just validate-history` target validates standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The generated record correctly groups `data/normalized_yaml/bacterial/KOMODO_871_GYM_SEAWATER.yaml` with `data/normalized_yaml/bacterial/gym_seawater.yaml` as a `SOURCE_DUPLICATE`: KOMODO 871 explicitly cites DSMZ medium 871, and both normalized parents have the same ingredient and concentration signature. Exact ignored-file-inclusive search found no other `komodo.medium:871`, `mediadive.medium:871`, or `gym_seawater` recipe records in `data/normalized_yaml/bacterial` or `data/merge_yaml/merged`.

Glucose, CaCO3, and Agar are grounded. Yeast extract, Malt extract, and Sea Salt are ungrounded complex or mixture rows.

## Evidence

The DSMZ Medium 871 PDF lists 4.0 g Glucose, 4.0 g Yeast extract, 10.0 g Malt extract, 2.0 g CaCO3, 15.0 g Agar, and 1000.0 ml Sea water, with a pH target of 7.0-7.4 and a seawater definition of 32 g sea salt in 1000 ml distilled water. The MediaDive 871 REST payload agrees on pH 7.0-7.4 and the 32 g/L seawater definition.

The generated record has the right final ingredient concentrations: it represents the source 1000 ml seawater as 32 g/L Sea Salt, and it correctly uses 15 g/L Agar. The MediaDive REST row has an inconsistent `amount: 20` for Agar, but DSMZ's PDF and the REST `g_l: 15` field both support the generated 15 g/L value.

## Completeness

The ingredient signature and pH range are materially complete. The generated record is incomplete for preparation because it drops the DSMZ seawater definition and pH note from the direct DSMZ normalized parent. It also carries the KOMODO `2026-01-27T01:15:03.fZ` curation timestamp into embedded generated history.

## Findings

- The DSMZ preparation notes are missing from the generated record. This drops both `pH should be between 7.0 - 7.4` and `32 g seasalt in 1000 ml distilled water`; the latter is the evidence that the top-level 32 g/L Sea Salt row came from a seawater replacement.
- The generated `media_term` keeps only `komodo.medium:871`; DSMZ / MediaDive 871 is represented indirectly via `parent_media`, so generated source identity is less explicit than the two-source merge warrants.
- The embedded KOMODO import history timestamp `2026-01-27T01:15:03.fZ` is malformed.
- Yeast extract, Malt extract, and Sea Salt remain ungrounded. These are mixture terms, but they should be checked against available mappings.

## Recommended Edits

- Preserve the DSMZ 871 preparation steps in the generated merge.
- Make the DSMZ / MediaDive 871 source identity explicit in regenerated provenance, not only through `parent_media`.
- Fix the malformed KOMODO history timestamp in the normalized KOMODO parent.
- Attempt mixture or complex-component groundings for Yeast extract, Malt extract, and Sea Salt.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validators after recuration.
- Revalidate embedded `curation_history` once the timestamp is corrected upstream.
- Recompare the regenerated record against DSMZ Medium 871 and verify pH 7.0-7.4, 15 g/L agar, and the explicit 32 g/L seawater definition.

## Additional Notes

None found.
