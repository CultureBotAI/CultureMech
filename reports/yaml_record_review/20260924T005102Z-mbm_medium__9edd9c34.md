# YAML Record Review: mbm_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mbm_medium__9edd9c34.yaml
- Started UTC: 2026-09-24T00:49:18Z
- Finished UTC: 2026-09-24T00:51:02Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:002700 |
| Label | mbm_medium |
| Original label | MBM MEDIUM |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mbm_medium__9edd9c34.yaml |
| Maintained owner | data/normalized_yaml/bacterial/mbm_medium.yaml |
| Source accession | mediadive.medium:J340 |

The generated record is derived from `data/normalized_yaml/bacterial/mbm_medium.yaml`.
The reviewed merge predates a 2026-09-02 duplicate-collapse repair in the
normalized owner and also carries broader JCM/MediaDive source conflicts that
remain unresolved upstream.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mbm_medium__9edd9c34.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mbm_medium__9edd9c34.yaml --out /private/tmp/mbm_medium__9edd9c34.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mbm_medium__9edd9c34.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mbm_medium__9edd9c34.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The validators accept both the stale duplicate sums and the flattened stock
ingredients because they are syntactically valid ingredient rows.

## Identity and Grounding

- MediaDive J340 identifies `MBM MEDIUM`, pH 7.0, and source JCM GRMD 340, so
  the imported source identity matches the reviewed record.
- A `find` lookup resolved `data/normalized_yaml/bacterial/mbm_medium.yaml` as
  the normalized owner. A scoped exact search with ignored files included found
  `CultureMech:002700` and `mediadive.medium:J340` in that owner, the generated
  target, the ID registry, and normalized MediaDive/recipe indexes.
- A gitignore-independent exact scan of the generated target and maintained
  owner found no `solutions`, `references`, or `target_organisms` sections.
- The live JCM 340 page no longer agrees exactly with MediaDive J340: JCM lists
  NaHCO3, Trace vitamins from JCM 197, and 10 ml of a 5 percent Na2S x 9 H2O
  stock before inoculation, while the MediaDive payload omits the bicarbonate
  and vitamins and represents sodium sulfide as a top-level mass.

## Evidence

### Supported

- The pH 7.0 scalar is supported by both MediaDive J340 and JCM 340.
- The main mineral rows NaNO3, KH2PO4, NH4Cl, MgCl2 x 6 H2O, KCl, and
  CaCl2 x 2 H2O are present in both MediaDive J340 and the live JCM 340 table.
- The SL-6 trace-element component identities match MediaDive solution 3822 and
  the SL-6 table on JCM 167.

### Unsupported or Over-scoped

- The reviewed generated file has doubled SL-6 rows. For example, `ZnSO4 x 7 H2O`
  is `0.2 G_PER_L` with a duplicate note of `0.1, 0.1`; the normalized owner has
  already collapsed those rows back to the source stock values.
- SL-4 and SL-6 trace elements are flattened into MBM. JCM 340 adds 2 ml SL-4;
  SL-4 contains 100 ml SL-6 and 900 ml water, so no SL-4 or SL-6 component is
  present in MBM at its one-liter stock concentration.
- The live JCM source has NaHCO3 and Trace vitamins, but the record has neither.
  Conversely, the record contains Resazurin although the inspected live JCM 340
  page does not.
- The source Na2S x 9 H2O handling is not preserved. Live JCM adds 10 ml of 5
  percent Na2S stock prior to inoculation, but the record has a direct
  `Na2S x 9 H2O` row at `0.499002 G_PER_L`.
- `NiCl2 x 6 H2O` is grounded to `CHEBI:34887` labeled `nickel dichloride`,
  losing the source hexahydrate.
- `NaNO3` still carries a deprecated `mediaingredientmech_term` instead of the
  current `mediaingredientmech_chebi_term`.

## Completeness

- The record is missing the source solution structure for SL-4 and nested SL-6.
- If the live JCM 340 formulation is authoritative, the record is also missing
  the 10 ml Trace vitamins addition from JCM 197 and the top-level bicarbonate
  row.
- `references` is empty. The JCM/MediaDive source is recoverable from
  `media_term`, `notes`, and curation history, but JCM 340, JCM 167, and JCM 197
  are not structured citations.
- `target_organisms` is empty and acceptable; the inspected source recipe does
  not assert a strain-specific growth result.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The generated record is stale relative to its normalized owner. | `data/merge_yaml/merged/mbm_medium__9edd9c34.yaml` still sums seven identical SL-6 duplicate pairs, while `data/normalized_yaml/bacterial/mbm_medium.yaml` has a 2026-09-02 `REPAIRED_SUMMED_DUPLICATE_MERGE` event that collapsed them. | Already fixed in `data/normalized_yaml/bacterial/mbm_medium.yaml`; regenerate and verify. |
| major | The live JCM 340 formula conflicts with the MediaDive J340 import. | Live JCM 340 includes NaHCO3, Trace vitamins from JCM 197, a 10 ml 5 percent Na2S stock addition, and no visible Resazurin row; the MediaDive import omits bicarbonate and vitamins, includes Resazurin, and treats Na2S as a direct mass. | `data/normalized_yaml/bacterial/mbm_medium.yaml` plus provenance metadata that distinguishes MediaDive's captured formula from the live JCM page. |
| major | SL-4 and nested SL-6 trace solutions are flattened into top-level MBM ingredients. | MediaDive J340 adds 2 ml SL-4 to the main solution; SL-4 adds 100 ml SL-6; the record lists EDTA, FeSO4 x 7 H2O, and every SL-6 component as direct MBM ingredients. | `data/normalized_yaml/bacterial/mbm_medium.yaml` or the MediaDive importer. |
| major | The Na2S stock addition is represented as a direct final ingredient. | The live JCM 340 source adds 10 ml of 5 percent Na2S x 9 H2O stock before inoculation; the YAML stores `Na2S x 9 H2O` as `0.499002 G_PER_L` under direct `ingredients`. | `data/normalized_yaml/bacterial/mbm_medium.yaml` or the MediaDive importer. |
| major | Nickel chloride hexahydrate has an exact-form grounding error. | The source ingredient is `NiCl2 x 6 H2O`, but the term is `CHEBI:34887` / `nickel dichloride`; hydrate state is identity-significant. | `data/normalized_yaml/bacterial/mbm_medium.yaml`. |
| minor | Trace-solution preparation is mis-scoped as a main-medium pH adjustment. | The SL-6 stock pH 3.6 step appears as `ADJUST_PH` step 2 of MBM itself. | `data/normalized_yaml/bacterial/mbm_medium.yaml` or the MediaDive importer. |
| minor | `NaNO3` still uses the deprecated `mediaingredientmech_term` key. | The NaNO3 row has a CHEBI primary `term` but a legacy `MediaIngredientMech:000171` link instead of `mediaingredientmech_chebi_term`. | `data/normalized_yaml/bacterial/mbm_medium.yaml`. |

## Recommended Edits

1. Regenerate the merged artifact so the 2026-09-02 duplicate-collapse repair is
   reflected in `data/merge_yaml/merged/mbm_medium__9edd9c34.yaml`.
2. Reconcile MediaDive J340 against live JCM 340 before changing the maintained
   formula: decide whether the record should preserve an older MediaDive/JCM
   snapshot, update to the current JCM table, or be split into explicit source
   variants.
3. Represent the SL-4 2 ml addition and SL-4's nested 100 ml SL-6 addition as
   stock solutions instead of direct MBM ingredients.
4. Restore the current JCM stock-addition scope for Na2S x 9 H2O and, if the
   live JCM 340 page is adopted, add the missing NaHCO3 and Trace vitamins rows.
5. Recheck exact grounding for NiCl2 x 6 H2O and leave it unresolved if no exact
   verified CHEBI/MIM term exists.
6. Move the pH 3.6 step under SL-6 preparation, keep final pH 7.0 under MBM, add
   structured references for JCM 340, JCM 167, and JCM 197 as applicable, and
   migrate the NaNO3 legacy MIM key.

## Follow-up Checks

- Rerun the focused open-schema, strict, term, and reference validators on the
  maintained owner and regenerated merged record.
- Manually compare the chosen curated formula with live JCM GRMD 340, JCM GRMD
  167, JCM GRMD 197, and MediaDive J340.
- Run a focused duplicate-ingredient check so SL-6 rows are neither doubled in
  the generated output nor collapsed into the MBM top-level ingredient list.
- Repeat a gitignore-independent exact search for `CultureMech:002700`,
  `mediadive.medium:J340`, and `mbm_medium` to verify the corrected owner and
  indexes.

## Additional Notes

- MediaDive J340 exposes two SL-6 solution IDs with the same formula, `3822` and
  `25`; the J340 payload nests solution ID `25` through SL-4, and the standalone
  REST solution `3822` matches the live JCM 167 SL-6 table.
