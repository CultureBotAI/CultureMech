# YAML Record Review: mq116_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mq116_medium__05c54119.yaml
- Started UTC: 2026-09-24T14:21:54Z
- Finished UTC: 2026-09-24T14:23:15Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/mq116_medium__05c54119.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:002454`
- Label: `mq116_medium`
- Original label: `MQ116 MEDIUM`
- Category: `bacterial`
- Media source: MediaDive/JCM `J1292`
- Generated status: generated merge output from one normalized source on fingerprint `05c541198594d9330d447511c0c030bd54fbfa489f85cadab0cedc50e3108390`
- Maintained owner: `data/normalized_yaml/bacterial/mq116_medium.yaml`

The generated record matches its maintained normalized owner except for the generated merge metadata.

## Validation

Validation was run on the generated merged YAML.

- Open LinkML schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mq116_medium__05c54119.yaml`
  - Result: passed with `No issues found`.
- Strict validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mq116_medium__05c54119.yaml --out /private/tmp/mq116_medium__05c54119.strict.tsv --workers 1 --quiet`
  - Result: passed with 0 errors; `/private/tmp/mq116_medium__05c54119.strict.tsv` contained only its header row.
- Reference validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mq116_medium__05c54119.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 0 reference checks were evaluated.
- Term validator:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mq116_medium__05c54119.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed after the known `eutils` / `pkg_resources` warning.
- Embedded merge history:
  - Result: not checked. The repository history validator targets standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The record is a MediaDive import of JCM `J1292`, `MQ116 MEDIUM`.

The direct JCM GRMD 1292 URL currently returns `Nothing found`, but both MediaDive `J1292` and TOGO `M1388` still identify the same old JCM source URL. An exact gitignore-independent search of `data/normalized_yaml` and `data/merge_yaml/merged` for `CultureMech:002454`, bounded `mediadive.medium:J1292`, bounded `ID: J1292`, and `GRMD=1292` found this MediaDive record, its normalized owner, and the separate TOGO `M1388` normalized/generated pair for the same JCM page. Ignored files were included.

The hydrate groundings are mostly narrow, except for `NiCl2 x 6 H2O`: the preferred term names nickel chloride hexahydrate, but `term` and `mediaingredientmech_chebi_term` use `CHEBI:34887` / `nickel dichloride`.

## Evidence

The live MediaDive `J1292` payload models MQ116 as:

- Main solution, `1021 ml`, with basal salts, yeast extract, beef extract, `0.5 mg` resazurin, `0.5 g` L-cysteine HCl x H2O, `1000 ml` water, `1 ml` trace element solution, `10 ml` of `1.0 M` sodium pyruvate, and `10 ml` clarified rumen fluid.
- A `Trace element solution`, `1000 ml`, with nitrilotriacetic acid, FeCl3 x 6 H2O, MnCl2 x 4 H2O, CoCl2 x 6 H2O, CaCl2 x 2 H2O, ZnCl2, CuCl2 x 2 H2O, H3BO3, Na2MoO4 x 2 H2O, NiCl2 x 6 H2O, and water.

The CultureMech record flattens both source layers:

| Source expression | CultureMech expression | Status |
| --- | --- | --- |
| `1 ml` trace element solution per `1021 ml` | all trace element members as top-level stock-strength `G_PER_L` rows | stock flattened |
| `0.1 g` CaCl2 x 2 H2O in the main solution plus `0.1 g/L` CaCl2 x 2 H2O in the trace stock | one CaCl2 x 2 H2O row summed to `0.19794319999999999 G_PER_L` | rows from different levels merged |
| `10 ml` of `1.0 M` sodium pyruvate after cooling | `Sodium pyruvate`, `10 G_PER_L` | volume addition cast to mass concentration |
| `10 ml` clarified rumen fluid after cooling | `Rumen fluid`, `10 G_PER_L` | volume addition cast to mass concentration |

The basal salt, yeast extract, beef extract, resazurin, and L-cysteine rows match MediaDive's final grams-per-liter conversion over the modeled `1021 ml` final volume.

## Completeness

The record is missing the source's solution boundaries for the trace element solution, sodium pyruvate stock, and rumen fluid addition. It is also missing the Medium M278 and Medium M258 cross-reference provenance that TOGO exposes for trace elements and rumen fluid, respectively.

The separate TOGO `M1388` import preserves the same old JCM identity, but it is not a better canonical record: its trace element, sodium pyruvate, and rumen fluid rows are empty `Unknown solution` entries with `G_PER_L` units.

Empty optional fields are not defects in this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The trace element stock was flattened into final top-level ingredient concentrations. | MediaDive `J1292` adds `1 ml` trace element solution to the main solution; the reviewed record imports all trace stock members at stock strength as top-level rows. | `data/normalized_yaml/bacterial/mq116_medium.yaml` |
| Major | CaCl2 x 2 H2O from two source levels was summed into one row. | The main medium contains CaCl2 directly, and the trace stock contains a separate CaCl2 row; the record's `Merged 2 duplicates` note shows those two concentrations were collapsed to `0.19794319999999999 G_PER_L`. | `data/normalized_yaml/bacterial/mq116_medium.yaml`; duplicate-ingredient cleanup |
| Major | Sodium pyruvate and clarified rumen fluid additions use wrong unit semantics. | Both additions are `10 ml` post-autoclave additions in the MediaDive payload; the reviewed record stores `10 G_PER_L` for each. | `data/normalized_yaml/bacterial/mq116_medium.yaml` |
| Major | A direct JCM duplicate remains separate as a TOGO-derived generated record. | TOGO `M1388` has `original_media_id: JCM_M1292` and the same stale GRMD 1292 URL; the exact ignored-inclusive search found `data/normalized_yaml/bacterial/TOGO_M1388_MQ116_Medium.yaml` and generated `MQ116_MEDIUM.yaml`. | `data/normalized_yaml/bacterial/mq116_medium.yaml`; `data/normalized_yaml/bacterial/TOGO_M1388_MQ116_Medium.yaml`; merge de-duplication logic |
| Major | `NiCl2 x 6 H2O` is grounded to an anhydrous nickel chloride CHEBI term. | The source and preferred term both name nickel chloride hexahydrate, but the record uses `CHEBI:34887` / `nickel dichloride`. | `data/normalized_yaml/bacterial/mq116_medium.yaml` |
| Minor | The direct JCM URL is stale. | JCM `GRMD=1292` now returns `Nothing found`; only MediaDive and TOGO still expose the historic formula. | `data/normalized_yaml/bacterial/mq116_medium.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/mq116_medium.yaml`, remodel the `Trace element solution`, `1.0 M Sodium pyruvate solution`, and clarified rumen fluid rows as volume additions instead of final `G_PER_L` rows.
2. Keep the direct main-medium CaCl2 row separate from the trace-solution CaCl2 row; undo the summed duplicate merge across source levels.
3. Add the M278 and M258 cross-reference provenance for the trace element solution and rumen fluid addition, or use the expanded MediaDive `Trace element solution` payload for a structured stock recipe.
4. Add a cross-source duplicate mapping so MediaDive `J1292` and TOGO `M1388` / `JCM_M1292` collapse to one maintained MQ116 recipe.
5. Re-ground `NiCl2 x 6 H2O` to a supported nickel chloride hexahydrate term, or leave it ungrounded until a hydrate-specific CHEBI term is available.
6. Regenerate `data/merge_yaml/merged/mq116_medium__05c54119.yaml` and remove the separate generated `MQ116_MEDIUM.yaml` duplicate through the normal merge pipeline.

## Follow-up Checks

- Re-run open schema validation on the regenerated canonical MQ116 merged YAML.
- Re-run `scripts/validate_strict.py` and verify that the TSV remains header-only.
- Re-run `linkml-reference-validator` and `linkml-term-validator` to confirm every retained CHEBI and MediaIngredientMech grounding still resolves.
- Re-fetch MediaDive `J1292` and TOGO `M1388`; verify that the regenerated recipe keeps main-medium ingredients, trace stock ingredients, sodium pyruvate stock, and rumen fluid addition in distinct rows or solutions.
- Repeat the exact ignored-inclusive search for `GRMD=1292`, `JCM_M1292`, and `TOGO:M1388` to verify that the direct generated duplicate has been removed or explicitly linked.

## Additional Notes

- JCM GRMD 266 describes clarified rumen fluid preparation, but the reviewed MQ116 import only preserves the reference to that medium indirectly through MediaDive's row attribute and TOGO's `M258` reference. A future curation pass should check the reference crosswalk before importing rumen-fluid preparation details.
