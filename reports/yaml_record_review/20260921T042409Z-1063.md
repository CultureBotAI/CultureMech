# YAML Record Review: 1063

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1063.yaml`
- Started UTC: 20260921T042151Z
- Finished UTC: 20260921T042409Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:007453` |
| Label | `1063` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in `data/normalized_yaml/bacterial/NBRC_1063.yaml` or the merge pipeline, not this file |
| Merge owner | `merged_from: [NBRC_1063]` |
| Maintained owner | `data/normalized_yaml/bacterial/NBRC_1063.yaml` |

`data/merge_yaml/merged/1063.yaml` is the Aug 6 generated merge for `NBRC_1063`. The maintained normalized record has since been repaired on Sep 11 by `repair_nbrc_1063_score15.py` to represent NBRC Medium 1062, "Pae I Medium + 0.1% (w/v) Urea", with individual ingredients, pH 8.5, a `media_term`, a reference URL, and the source-supported preparation steps.

## Validation

Repository `just` entrypoints were blocked before the target-specific validators could run: project `uv` currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1063.yaml` | Pass; `No issues found` |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1063.yaml --out /private/tmp/culturemech-review-1063-rerun.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-1063-rerun.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1063.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1063.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/`, not this embedded list |

The passing schema gates only prove that the file is structurally valid YAML. They do not catch the parsed-composition loss below.

## Identity and Grounding

The record's intended source identity is NBRC Medium 1062:

- The import event says `Media No. 1062` and links `http://www.nbrc.nite.go.jp/NBRC2/NBRCMediumDetailServlet?NO=1062`.
- The live NBRC `NO=1062` page resolves to Medium No. 1062 with title `Pae I Medium + 0.1% (w/v) Urea`.
- The maintained owner, `data/normalized_yaml/bacterial/NBRC_1063.yaml`, now carries `media_term: nbrc.medium:1062` / `NBRC Medium 1062`, the official title, and the NBRC reference URL.

The generated file no longer agrees with that identity:

- `name` and `original_name` are both the opaque string `1063`, even though live NBRC Medium No. 1063 is a different recipe, `ALB Medium`.
- The generated record has no `media_term` or `references`.
- Its only source-specific identifier is a stale `merged_from: NBRC_1063` pointer, which points at a maintained record that has already diverged materially from this generated artifact.

## Evidence

Supported by inspected source text:

- NBRC Medium 1062 is the intended upstream source for `CultureMech:007453`.
- NBRC Medium 1062 lists `Pae I Medium + 0.1% (w/v) Urea`.
- NBRC Medium 1062 lists tryptone 5 g, yeast extract 3 g, NaCl 10 g, urea 1 g, distilled water 1 L, optional agar 15 g, and pH adjustment to 8.5 with 50 mM Tris-HCl buffer.

Unsupported or malformed in the generated record:

- The first `ingredients` row concatenates the entire NBRC formulation into one `preferred_term`, then assigns it `variable` / `VARIABLE`.
- The second `ingredients` row has `preferred_term: ''` and `concentration.value: Tryptone` with `unit: G_PER_L`; this is dimensionally and semantically invalid even though it passes schema validation.
- No row represents yeast extract 3 g/L, NaCl 10 g/L, urea 1 g/L, water 1 L, agar 15 g/L if needed, or Tris-HCl buffer as separate components.
- `preparation_steps[3].action` is `DISSOLVE` while its description says `Sterilize by autoclaving`.
- The record omits the NBRC pH 8.5 condition and has no narrow NBRC reference object.

The NBRC `NO=1063` page was also inspected to check the ambiguous display name. It is `ALB Medium`, with tryptone 10 g, yeast extract 5 g, NaCl 5 g, Na2CO3 2 g, 1 L water, optional agar 15 g, pH 8-9, and air containing 5% CO2; this confirms that a label of plain `1063` is a near miss for a different NBRC source accession, not a safe display name for NBRC Medium 1062.

## Completeness

- The record is not complete enough for downstream use: the official NBRC title, source term, pH, reference URL, and all source ingredients were already recovered in `data/normalized_yaml/bacterial/NBRC_1063.yaml` but are absent or stale in `data/merge_yaml/merged/1063.yaml`.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `NBRC_1063`, `Pae I Medium`, `nbrc.medium:1062`, `NBRC Medium 1062`, and the NBRC 1062 servlet URL found this stale generated `1063.yaml`, the repaired maintained `NBRC_1063.yaml`, normalized index entries, and the separately imported TOGO M1829 record in `pae_i_medium_0_1_w_v_urea.yaml`; it did not find a repaired generated NBRC Medium 1062 merge artifact.
- The TOGO M1829 `pae_i_medium_0_1_w_v_urea.yaml` generated record is a separate source import of the same NBRC formulation. It should be considered during future merge repair so the corrected NBRC import and the TOGO import are either intentionally merged or intentionally kept apart with explicit provenance.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The generated record has a misleading `1063` identity for NBRC Medium 1062 and is stale relative to its maintained source. | The generated artifact has `name: '1063'` and `merged_from: [NBRC_1063]`; the maintained owner now says `Pae I Medium + 0.1% (w/v) Urea` with `media_term: nbrc.medium:1062`; live NBRC `NO=1063` is a different ALB Medium. | `data/normalized_yaml/bacterial/NBRC_1063.yaml` is already repaired; the merge products under `data/merge_yaml/merged/` need regeneration or stale-output cleanup. |
| Blocker | The ingredient list is malformed and omits the real NBRC composition. | Live NBRC Medium 1062 has separate rows for tryptone, yeast extract, NaCl, urea, distilled water, optional agar, and a Tris-HCl pH adjustment; this generated file has one concatenated formula string and one empty ingredient with `concentration.value: Tryptone`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1063.yaml`; if regeneration preserves the malformed rows, fix the NBRC importer or merge rule that selects the pre-repair source. |
| Major | NBRC pH and source grounding are absent from the merge record. | The NBRC page states pH 8.5 adjustment with 50 mM Tris-HCl buffer, and the maintained normalized record has `ph_value: 8.5`, `media_term: nbrc.medium:1062`, and a reference URL; the generated file has none of these fields. | Regenerate `data/merge_yaml/merged/` after the Sep 11 NBRC normalized repair. |
| Major | The autoclave step is typed as dissolution. | `preparation_steps[3]` has `action: DISSOLVE` and `description: Sterilize by autoclaving`, while the maintained owner now correctly uses `action: AUTOCLAVE`. | Regenerate from `data/normalized_yaml/bacterial/NBRC_1063.yaml`. |
| Minor | The generated corpus still contains an unmerged TOGO copy of the same NBRC formulation. | `data/merge_yaml/merged/pae_i_medium_0_1_w_v_urea.yaml` comes from TOGO M1829 and cites original NBRC source `NBRC_M1062`, but it remains a separate `CultureMech:008401` merge product. | Merge-fingerprint rules for NBRC/TOGO duplicate handling. |

## Recommended Edits

1. Regenerate merge artifacts from the repaired normalized corpus and confirm that `CultureMech:007453` no longer appears as `data/merge_yaml/merged/1063.yaml` with malformed ingredient rows.
2. If a plain regeneration keeps the stale `1063.yaml`, fix the merge pipeline path that is reusing an obsolete NBRC source snapshot instead of the current `data/normalized_yaml/bacterial/NBRC_1063.yaml`.
3. After regeneration, inspect the generated NBRC Medium 1062 artifact for the official title, `nbrc.medium:1062` source grounding, seven separate ingredient/preparation claims, `ph_value: 8.5`, `AUTOCLAVE`, and the NBRC 1062 HTTPS reference.
4. Decide whether the NBRC direct import and TOGO M1829 import of NBRC Medium 1062 should fingerprint together. If they stay separate, add source-specific rationale; if they should merge, update the fingerprinting or normalization rule that keeps `NBRC_1063` and `pae_i_medium_0_1_w_v_urea` apart.

## Follow-up Checks

- `just verify-merges` to prove generated merge products are fresh relative to `data/normalized_yaml`.
- `just audit-merge-freshness` to catch any other generated records older than their repaired normalized owners.
- `just validate-strict data/merge_yaml/merged/<regenerated-nbrc-1062>.yaml` to recheck the regenerated file under the closed schema.
- `just validate-terms data/merge_yaml/merged/<regenerated-nbrc-1062>.yaml` once the regenerated NBRC record contains source and ingredient term blocks.
- A manual duplicate check between the regenerated NBRC Medium 1062 artifact and `data/merge_yaml/merged/pae_i_medium_0_1_w_v_urea.yaml`.

## Additional Notes

- Network access was initially unavailable in the sandbox for the live NBRC pages and was retried with approval; both NBRC 1062 and NBRC 1063 were inspected successfully.
- `linkml-reference-validator` performed zero checks because this stale generated record has no `references` block.
