# YAML Record Review: Minimal Medium; Teusink et al

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/minimal_medium_teusink_et_al.yaml
- Started UTC: 2026-09-24T07:19:30Z
- Finished UTC: 2026-09-24T07:20:02Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| class | MediaRecipe |
| id | CultureMech:007202 |
| name | minimal_medium_teusink_et_al |
| label | Minimal Medium; Teusink et al |
| category | bacterial |
| media term | MEDIADB:2 |
| generated status | generated by merge_recipes.py from `data/normalized_yaml/bacterial/minimal_medium_teusink_et_al.yaml` |

## Validation

| Check | Result |
|---|---|
| LinkML schema validation | Passed with `No issues found`. |
| Strict validation | Passed: 1 file scanned, 0 files with errors, 0 total error rows; `/private/tmp/minimal_medium_teusink.strict.tsv` contained only its header. |
| LinkML reference validation | Passed: 1 file validated, 0 checks, all validations passed. |
| LinkML term validation | Passed. |
| Embedded history validation | Not checked: the repository history validator targets standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in generated YAML. |

## Identity and Grounding

The record identifies MediaDB Medium 2, Minimal medium; teusink et al. The maintained single-source owner was found with `rg --files --no-ignore --hidden data/normalized_yaml`, so ignored files were included in the owner lookup.

The MediaDB identity and 23-compound mM formula are preserved. The `Ascorbate` and `Ammonium citrate` rows are ungrounded even though MediaDB supplies identifier context, and `Cysteine-HCl` still carries a legacy `mediaingredientmech_term`.

## Evidence

MediaDB Medium 2 lists 23 compounds with millimolar amounts. The generated YAML has the same 23 compound names and the same numeric amounts from the tab-delimited MediaDB `media_text/2` view.

MediaDB links the medium to source 2, Teusink et al. 2005, and to growthdata 2 for `Lactobacillus plantarum wcfs1` at pH 5.5 and 37.0 C. The YAML has no target organism or structured MediaDB source/growth reference, and its preparation steps are generic importer text rather than instructions visible on the MediaDB medium, source, or growth-data pages.

## Completeness

The ingredient table is complete for MediaDB Medium 2. The gaps are minor metadata and provenance issues: two ungrounded ingredients, one legacy MediaIngredientMech field, generic preparation instructions, and missing structured source/growth context.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Minor | `Ascorbate` and `Ammonium citrate` are ungrounded. | MediaDB supplies identifier context for both compounds, including an ammonium citrate CHEBI cross-reference; the YAML rows have no `term` or MediaIngredientMech link. | `data/normalized_yaml/bacterial/minimal_medium_teusink_et_al.yaml` |
| Minor | `Cysteine-HCl` still has a legacy MediaIngredientMech link. | The row has a CHEBI primary `term` but retains `mediaingredientmech_term: MediaIngredientMech:000223` instead of a CHEBI-backed MediaIngredientMech link. | `data/normalized_yaml/bacterial/minimal_medium_teusink_et_al.yaml` |
| Minor | Preparation steps are not source-backed by MediaDB. | The YAML says to dissolve all ingredients, adjust pH if specified, and filter sterilize at 0.22 um; MediaDB Medium 2, source 2, and growthdata 2 do not provide those preparation instructions. | `data/normalized_yaml/bacterial/minimal_medium_teusink_et_al.yaml` |
| Minor | MediaDB organism and growth context are not structured. | MediaDB links Medium 2 to `Lactobacillus plantarum wcfs1`, pH 5.5, and 37.0 C; the YAML has no target organism or growth-condition evidence. | `data/normalized_yaml/bacterial/minimal_medium_teusink_et_al.yaml` |

## Recommended Edits

1. Ground `Ascorbate` and `Ammonium citrate` to specific chemical terms where justified by source identifiers.
2. Replace the legacy `mediaingredientmech_term` on `Cysteine-HCl` with a modern CHEBI-backed MediaIngredientMech link or intentionally omit it with an explanatory note.
3. Remove generic preparation steps unless they can be tied to Teusink et al. 2005 or another cited source.
4. Add structured MediaDB source 2 and growthdata 2 references, and add target-organism or growth-condition evidence if the schema supports it.
5. Regenerate `data/merge_yaml/merged/minimal_medium_teusink_et_al.yaml`.

## Follow-up Checks

1. Run `linkml-validate` against the regenerated merged record.
2. Run `scripts/validate_strict.py` against the regenerated merged record.
3. Run `linkml-reference-validator` and `linkml-term-validator` against the regenerated merged record.
4. Recompare the regenerated ingredient table with MediaDB `media_text/2` and verify it still has exactly 23 compounds with matching millimolar amounts.

## Additional Notes

None found.
