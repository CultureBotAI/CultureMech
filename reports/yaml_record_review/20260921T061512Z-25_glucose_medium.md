# YAML Record Review: 25_glucose_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/25_glucose_medium.yaml`
- Started UTC: 2026-09-21T06:13:54Z
- Finished UTC: 2026-09-21T06:15:12Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:003216` |
| Name | `25_glucose_medium` |
| Original name | `25% GLUCOSE MEDIUM` |
| Category | `bacterial` |
| Source term | `mediadive.medium:J86`, `25% GLUCOSE MEDIUM` |
| Source URL | `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=86` in free-text `notes` |
| Merge fingerprint | `c0270a7524fec1c6185d5d3f7b364eeb7f94c7608be5e234379df2d4f8ec60ba` |
| Maintained owner | `data/normalized_yaml/bacterial/25_glucose_medium.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated MediaDive/JCM representation of JCM Medium 86, 25% GLUCOSE MEDIUM. Future corrections to the source record belong in `data/normalized_yaml/bacterial/25_glucose_medium.yaml` and should be followed by merge regeneration; the generated target itself is derived.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/25_glucose_medium.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/25_glucose_medium.yaml --out /private/tmp/25_glucose_medium.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/25_glucose_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/25_glucose_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The record correctly denotes JCM Medium 86:

- `media_term.term.id` is `mediadive.medium:J86`, matching the MediaDive/JCM import ID for JCM Medium 86.
- The free-text source URL points to the JCM `GRMD=86` page.
- The inspected JCM page resolves to medium number 86 and title `25% GLUCOSE MEDIUM`.

Ingredient grounding is only partially complete in the generated target. Glucose and agar carry exact CHEBI terms, but the generated artifact predates the maintained owner's repair of `Polypepton (Nihon Pharm. Co.)`, the yeast-extract FOODON grounding, and the missing distilled-water row.

## Evidence

The inspected JCM Medium 86 page supports this formulation for a 1 L distilled-water basis:

- 250 g glucose.
- 5 g `Polypepton (Nihon Pharm. Co.)`.
- 3 g malt extract.
- 3 g yeast extract.
- 25 g agar.
- 1 L distilled water.

The target preserves glucose, malt extract, yeast extract, and agar amounts. It loses the 1 L distilled-water row entirely and broadens the source's vendor-specific `Polypepton (Nihon Pharm. Co.)` to `Polypeptone`.

The JCM page also states a source-wide default to sterilize media by autoclaving at 121 C for 15 minutes unless otherwise stated. Neither the generated target nor its maintained normalized owner has a preparation step for that instruction.

## Completeness

Consequential gaps:

- The generated target has no distilled-water row.
- The generated target has no structured `references` entry, so the reference validator had no JCM URL to inspect.
- The generated target has no preparation step for the default JCM autoclave instruction.
- The duplicate TOGO M77 import remains a separate normalized and generated record for the same JCM Medium 86 source and still stores `Distilled water` as `1 G_PER_L`.

Empty optional slots:

- `target_organisms` and `growth_metrics` are correctly absent. The inspected JCM recipe supports a formulation, not organism-specific growth evidence.
- `ph_value` is correctly absent. The inspected JCM Medium 86 table does not list a pH.

Bounded searches:

- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing `*-25_glucose_medium.md` report before this report was created.
- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, `history`, and `reports` for focused `25_glucose_medium`, `CultureMech:003216`, `CultureMech:010188`, `mediadive.medium:J86`, `TOGO:M77`, `JCM_M86`, the JCM GRMD URL, and both merge fingerprints found the expected MediaDive/JCM and TOGO M77 normalized/generated pairs. That search included ignored files under the searched paths. The broad `M77` token also matched unrelated JCM and TOGO records such as `M770` and `M779`, so only exact JCM 86 and TOGO M77 hits were used for this review.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated target is stale and omits source-supported water while broadening the Polypepton product name. | JCM Medium 86 lists 1 L distilled water and `Polypepton (Nihon Pharm. Co.)`; `data/normalized_yaml/bacterial/25_glucose_medium.yaml` now carries those values, but `data/merge_yaml/merged/25_glucose_medium.yaml` was generated before that repair and still lacks water while using `Polypeptone`. | Generated `data/merge_yaml/merged/25_glucose_medium.yaml`; regenerate from `data/normalized_yaml/bacterial/25_glucose_medium.yaml` |
| Major | The default JCM sterilization instruction is missing. | JCM states that media should be autoclaved at 121 C for 15 min unless otherwise stated; neither the generated target nor the maintained normalized owner represents that step. | `data/normalized_yaml/bacterial/25_glucose_medium.yaml` |
| Major | The TOGO M77 source duplicate is unreconciled and still contains the same water import defect that the JCM owner already repaired. | `data/normalized_yaml/bacterial/TOGO_M77_25_Glucose_Medium.yaml` points to `Original source: JCM - JCM_M86` but stores distilled water as `1 G_PER_L`, lacks a JCM reference, and is not linked to `CultureMech:003216`; the generated layer keeps it as a separate `25_glucose_medium__2c0720bc.yaml` record. | `data/normalized_yaml/bacterial/TOGO_M77_25_Glucose_Medium.yaml` plus reciprocal variant links in `data/normalized_yaml/bacterial/25_glucose_medium.yaml` |
| Minor | Structured source provenance is absent from the generated target. | `data/normalized_yaml/bacterial/25_glucose_medium.yaml` now has a structured JCM reference; the stale generated merge has the JCM URL only in free-text `notes`, so `linkml-reference-validator` performed zero checks. | Generated `data/merge_yaml/merged/25_glucose_medium.yaml`; regenerate from `data/normalized_yaml/bacterial/25_glucose_medium.yaml` |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/25_glucose_medium.yaml` from `data/normalized_yaml/bacterial/25_glucose_medium.yaml` so the generated JCM record includes 1 L distilled water, `Polypepton (Nihon Pharm. Co.)`, yeast-extract grounding, and the structured JCM reference.
2. Add a JCM-supported preparation step to `data/normalized_yaml/bacterial/25_glucose_medium.yaml` for autoclaving at 121 C for 15 minutes.
3. Curate `data/normalized_yaml/bacterial/TOGO_M77_25_Glucose_Medium.yaml`: correct distilled water from `1 G_PER_L` to a 1 L final-volume representation, add the JCM reference, preserve the TOGO source ID, and link it to `data/normalized_yaml/bacterial/25_glucose_medium.yaml` as a `SOURCE_DUPLICATE` in both directions.
4. Regenerate `data/merge_yaml/merged/` after the maintained duplicate relationship is added so JCM J86 and TOGO M77 no longer appear as unrelated sibling records.

## Follow-up Checks

- `just validate-schema data/normalized_yaml/bacterial/25_glucose_medium.yaml` after adding the JCM preparation step.
- `just validate-strict data/normalized_yaml/bacterial/25_glucose_medium.yaml` after adding the JCM preparation step.
- `just validate-references data/normalized_yaml/bacterial/25_glucose_medium.yaml` after preserving structured JCM provenance.
- `just validate-terms data/normalized_yaml/bacterial/TOGO_M77_25_Glucose_Medium.yaml` after repairing the TOGO duplicate's water row and variant link.
- `just validate-media-variant-links` after adding reciprocal `SOURCE_DUPLICATE` links.
- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- Manual comparison against JCM Medium 86 for 250 g/L glucose, 5 g/L `Polypepton (Nihon Pharm. Co.)`, 3 g/L malt extract, 3 g/L yeast extract, 25 g/L agar, 1 L distilled water, and default autoclaving.

## Additional Notes

- The generated target correctly omits a pH; none was present in the inspected JCM Medium 86 source.
- `linkml-reference-validator` performed zero checks because this generated target has no structured `references` entries.
