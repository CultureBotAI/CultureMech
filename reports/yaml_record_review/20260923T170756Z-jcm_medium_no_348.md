# YAML Record Review: jcm_medium_no_348

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_348.yaml
- Started UTC: 2026-09-23T17:06:19Z
- Finished UTC: 2026-09-23T17:07:56Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Path | `data/merge_yaml/merged/jcm_medium_no_348.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:002708` |
| Label | `jcm_medium_no_348` |
| Source term | `mediadive.medium:J348` |
| Upstream source | JCM MEDIUM No. 348, mirrored by MediaDive as JCM `J348` |
| Generated from | `data/normalized_yaml/bacterial/jcm_medium_no_348.yaml` |

This is a generated merge artifact with `merge_fingerprint: c3c41489b97d6cdc632054e49151b9905965e29471d7ee7a14042f8335d874d1` and `merged_from: [jcm_medium_no_348]`. Future edits belong in the maintained MediaDive/JCM normalized owner, not in `data/merge_yaml/merged/`.

An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, `data/import_tracking`, `history`, and `data/culturemech_recipe_catalog.tsv` found the maintained JCM owner, the standalone `Main sol. J348` import, and TOGO M343 as an independently imported sibling for the same original JCM GRMD=348 formulation.

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_348.yaml` | Passed: `No issues found`. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_348.yaml --out /private/tmp/jcm348.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_348.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 0 reference checks. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_348.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. The only output before the pass line was the known `eutils` `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not `MediaRecipe.curation_history` embedded in merged YAML. |

The repository-level `just` validators were not used because this checkout's `uv` resolution currently attempts to build `llvmlite==0.46.0` under Python 3.13 and fails inside `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The focused Python 3.11 validators above exercise the relevant schema, strict, reference, and term gates for this one generated `MediaRecipe`.

## Identity and Grounding

The record denotes the intended JCM medium. It has ID `CultureMech:002708`, label `jcm_medium_no_348`, source term `mediadive.medium:J348`, and a JCM GRMD=348 note. Live MediaDive identifies `J348` as `JCM MEDIUM No. 348`, source `JCM`, `complex_medium: yes`, pH 7.4, and links back to the same JCM URL.

The main formulation is grounded correctly. Live JCM and MediaDive agree on 7.5 g proteose peptone, 5.0 g yeast extract, 20.0 g MgSO4 x 7 H2O, 2.0 g KCl, 250.0 g NaCl, and 20.0 g agar brought to 1.0 L and adjusted to pH 7.4. The generated `SOLID_AGAR`, `UNDEFINED` composition, pH 7.4, and `high_metal: true` values are consistent with that high-salt agar recipe.

TOGO M343 is the same JCM source through `original_media_id: JCM_M348`. Its generated record is not merged into J348, but it supports the same ingredient list and retained `Distilled water` as an explicit 1 L ingredient plus the `BD-Difco` qualifiers on the two complex ingredients.

## Evidence

The core recipe is supported. Every numeric ingredient amount in the generated JCM record matches live JCM and MediaDive's REST representation for `Main sol. J348`; the generated pH and preparation steps also match the inspected sources.

Two narrow source details were lost:

- Live JCM labels the first two complex components as `Proteose peptone (BD-Difco)` and `Yeast extract (BD-Difco)`, and MediaDive exposes `attribute: BD-Difco` for both rows. The generated JCM owner stores only `Proteose peptone` and `Yeast extract`.
- JCM displays its default sterilization instruction, 121 C for 15 min unless otherwise stated, before the medium table. The generated record does not retain sterilization.

## Completeness

The generated record has enough information to reconstruct the JCM formula and pH. It does not contain explicit water, but its first preparation step says to add components to distilled water and bring the volume to 1.0 L, which preserves the source instruction for final volume.

Empty optional evidence, target-organism, salinity, atmosphere, and storage fields are acceptable. The inspected JCM, MediaDive, and TOGO records are source recipes; none supplied growth evidence or incubation/storage conditions.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Minor | Two complex ingredient product qualifiers were dropped. | JCM and TOGO include `(BD-Difco)` on proteose peptone and yeast extract; MediaDive carries that value as `attribute: BD-Difco`; the generated JCM record omits it. | `data/normalized_yaml/bacterial/jcm_medium_no_348.yaml` or the MediaDive import mapping for row attributes. |
| Minor | JCM default autoclaving is absent. | The JCM GRMD=348 page states the default 121 C, 15 min autoclave rule before the table. No sterilization step is present in the generated record. | JCM/MediaDive normalized import. |

## Recommended Edits

1. Preserve the `BD-Difco` qualifier on the proteose peptone and yeast extract rows in `data/normalized_yaml/bacterial/jcm_medium_no_348.yaml`, either in the ingredient label or an attribute/notes field that survives generation.
2. Preserve the JCM default 121 C, 15 min autoclaving instruction when no medium-specific exception overrides it.

## Follow-up Checks

- Rerun the focused schema, strict, reference, and term validators on `data/normalized_yaml/bacterial/jcm_medium_no_348.yaml` after the maintained owner is edited.
- Regenerate `data/merge_yaml/merged/jcm_medium_no_348.yaml` and confirm the two complex ingredient qualifiers and default sterilization text survive into the merged artifact.
- Compare the regenerated record against live JCM GRMD=348, MediaDive `/rest/medium/J348`, and TOGO M343.
- Run the merge freshness and product validators after regeneration.

## Additional Notes

- JCM GRMD=348 failed from the sandbox with `curl: (6) Could not resolve host: www.jcm.riken.jp`; the same URL was re-fetched successfully outside the sandbox.
- The standalone solution record `data/normalized_yaml/bacterial/mediadive_4068_Main_sol_J348.yaml` has the same two missing `BD-Difco` qualifiers and still carries an obsolete `incomplete_composition` data-quality flag despite having a complete composition.
