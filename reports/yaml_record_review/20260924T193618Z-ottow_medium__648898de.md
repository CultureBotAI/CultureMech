# YAML Record Review: ottow_medium__648898de

- Repository: CultureMech
- Record: data/merge_yaml/merged/ottow_medium__648898de.yaml
- Started UTC: 2026-09-24T19:36:18Z
- Finished UTC: 2026-09-24T19:36:18Z
- Verdict: needs curation

## Target

Reviewed generated MediaRecipe `CultureMech:008903`, `ottow_medium`, generated from `data/normalized_yaml/bacterial/TOGO_M2316_Ottow_Medium.yaml` for TOGO M2316 / DSMZ Medium 467.

## Validation

- Open LinkML validation: Passed; exited 0 with `No issues found`.
- Strict validation: Passed; scanned 1 file with 0 ERROR rows. `/private/tmp/ottow_medium__648898de.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` embedded in merged YAML.

## Identity and Grounding

The generated record has the correct TOGO M2316 identity for an import of DSMZ Medium 467 / Ottow Medium. An ignored-inclusive exact search for `TOGO:M2316`, `DSMZ_Medium467`, `mediadive.medium:467`, `komodo.medium:467`, `OTTOW medium`, `Ottow Medium`, and `ottow_medium` across `data/merge_yaml` and `data/normalized_yaml` found the expected DSMZ 467 direct MediaDive source, KOMODO 467 duplicate, TOGO M2316 duplicate, and TOGO M3060 supplemented variant.

## Evidence

The DSMZ 467 PDF lists, per liter, Glucose 1.0 g, Peptone 7.5 g, Meat extract 5.0 g, Yeast extract 2.5 g, Casamino acids 2.5 g, NaCl 5.0 g, Tap water 1000.0 ml, and "Adjust pH to 8.5." TOGO M2316 points to that DSMZ PDF, imports the same rows, and carries the pH 8.5 adjustment.

The repaired normalized TOGO M2316 record stores Tap water as `1.0 L`, sets `ph_value: 8.5`, adds an `ADJUST_PH` preparation step, cites TOGO M2316 plus the DSMZ 467 PDF, and marks the record as a `SOURCE_DUPLICATE` of the direct DSMZ 467 normalized record.

## Completeness

The generated record preserves the seven DSMZ ingredient names but regresses tap water to `1000 G_PER_L`, omits pH 8.5, omits the pH-adjustment step, drops curated references and data-quality flags, and remains split from its direct DSMZ 467 / KOMODO 467 source-duplicate group.

## Findings

1. Tap water has the wrong unit.

   DSMZ 467 and TOGO M2316 specify Tap water 1000 ml per liter. The repaired TOGO M2316 normalized record stores that row as `1.0 L`, but the generated record still emits Tap water as 1000 `G_PER_L`.

2. The pH adjustment is missing.

   The source states pH 8.5 and the repaired normalized record has `ph_value: 8.5` plus an `ADJUST_PH` step. The generated record has neither field.

3. The TOGO source duplicate was not merged into the DSMZ 467 group.

   The repaired normalized record marks TOGO M2316 as a `SOURCE_DUPLICATE` of `data/normalized_yaml/bacterial/ottow_medium.yaml`. The generated record still has only `TOGO_M2316_Ottow_Medium` in `merged_from`, while the DSMZ/KOMODO side of the same formula generated separately as `OTTOW_MEDIUM.yaml`.

## Recommended Edits

- Preserve `ph_value`, `preparation_steps`, references, data-quality flags, and `SOURCE_DUPLICATE` relationship fields from `data/normalized_yaml/bacterial/TOGO_M2316_Ottow_Medium.yaml` in generated output.
- Include TOGO M2316 in the DSMZ 467 source-duplicate merge instead of emitting a singleton TOGO record.
- Audit the direct DSMZ 467 and KOMODO 467 normalized copies before regeneration so all source duplicates agree that 1000 ml Tap water is a volume, not 1000 `G_PER_L`.

## Follow-up Checks

- Re-run an ignored-inclusive exact search for `DSMZ_Medium467`, `TOGO:M2316`, `mediadive.medium:467`, and `komodo.medium:467` after regeneration and confirm they resolve to one generated DSMZ 467 source-duplicate record.
- Confirm the regenerated DSMZ 467 record has `ph_value: 8.5`, an `ADJUST_PH` step, and Tap water as `1.0 L` or an equivalent 1000 `ML_PER_L`.
- Re-run open, strict, reference, and term validation on the regenerated YAML.

## Additional Notes

TOGO M3060 / NBRC 1631 is an Ottow supplemented variant with distilled water plus optional gellan gum and should remain distinct from this DSMZ 467 source-duplicate group.
