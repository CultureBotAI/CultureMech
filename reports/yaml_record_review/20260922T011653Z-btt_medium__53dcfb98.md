# YAML Record Review: BTT medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/btt_medium__53dcfb98.yaml
- Started UTC: 2026-09-22T01:16:53Z
- Finished UTC: 2026-09-22T01:16:53Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008699 |
| Label | btt_medium |
| Original label | BTT medium |
| Category | bacterial |
| Source accession | TOGO:M2107 |
| Generated path | data/merge_yaml/merged/btt_medium__53dcfb98.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml |
| Merge fingerprint | 53dcfb9864eaa4a6ed7438be79b6b0279498593e71b604edab4c5676c321d3be |

The reviewed target is a generated merge under `data/merge_yaml/merged`,
derived from the single normalized owner `TOGO_M2107_BTT_medium`. It is a TOGO
wrapper around NBRC Medium 1430, not the direct JCM or DSMZ BTT formulations.

## Validation

| Check | Result |
| --- | --- |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/btt_medium__53dcfb98.yaml` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/btt_medium__53dcfb98.yaml --out /private/tmp/btt_medium__53dcfb98.strict.tsv --workers 1 --quiet` | Passed |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/btt_medium__53dcfb98.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 structured URL checks |
| `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/btt_medium__53dcfb98.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed |
| `just validate-history data/merge_yaml/merged/btt_medium__53dcfb98.yaml` | Not checked: `validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries |

Direct `just validate-schema`, `just validate-strict`, `just validate-terms`,
and `just validate-references` were not rerun because the project uv environment
tries to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with
`TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The
equivalent no-project Python 3.11 validators above passed.

## Identity and Grounding

The TOGO/NBRC identity is coherent: live TOGO M2107 resolves to `BTT medium`,
cites `NBRC_M1430` as its original medium, and links to NBRC Medium No. 1430;
the inspected NBRC page is `BTT medium`.

The generated merge is stale against its maintained owner. The owner was
repaired on 2026-09-11 and now has a source-preserving `1.0 L` water row,
`ph_value: 5.5`, an `ADJUST_PH` preparation step, structured TOGO and NBRC
references, FoodOn yeast-extract grounding, CHEBI agar grounding, and source
annotations on each row. The generated record still has the older `Distilled
water`, `1 G_PER_L` row, no pH, no preparation step, no structured references,
and an ungrounded `Agar (if needed)` row.

The glucose and water CHEBI mappings are source-faithful. Meat Extract and
Casitone are source-disclosed undefined products and should remain opaque. The
source marks agar as conditional, so the physical state should not imply that
NBRC 1430 is exclusively the solid-agar form.

## Evidence

Supported:

| Claim | Source support |
| --- | --- |
| TOGO M2107 denotes NBRC 1430 BTT medium | Live TOGO M2107 has `name: BTT medium`, `original_media_id: NBRC_M1430`, and the NBRC 1430 page as `src_url`; the inspected NBRC page says Medium No. 1430 is `BTT medium`. |
| Glucose, yeast extract, meat extract, Casitone, water, and conditional agar amounts | TOGO M2107 and NBRC 1430 list 10 g glucose, 1 g yeast extract, 1 g Meat Extract, 2 g Casitone, 1 L distilled water, and 20 g `Agar (if needed)`. |
| pH 5.5 | TOGO M2107 and NBRC 1430 both list pH 5.5. |

Unsupported or incomplete:

| Generated claim | Problem |
| --- | --- |
| `Distilled water`, `1 G_PER_L` | The source row is 1 L distilled water, not 1 g/L water; the maintained owner already corrected this to `1.0 L`. |
| No pH or pH-adjustment step | TOGO M2107 and NBRC 1430 both support pH 5.5, and the maintained owner already added an `ADJUST_PH` step. |
| Ungrounded `Agar (if needed)` | The maintained owner already added the source-faithful agar CHEBI mapping. |
| `physical_state: SOLID_AGAR` with mandatory-looking `Agar (if needed)` ingredient | NBRC marks the 20 g agar addition as conditional. The record should preserve that optionality or split broth and agar variants instead of implying agar is always present. |
| No structured TOGO or NBRC source reference | The owner now carries TOGO M2107 and NBRC 1430 references, but the generated merge has not been regenerated to include them. |

## Completeness

The normalized owner already contains the source-supported water, pH,
preparation, provenance, and exact agar-grounding corrections missing from the
generated target. It still leaves one consequential classification/modeling
choice unresolved: NBRC says agar is added only if needed, while the record is
typed as `SOLID_AGAR` without an explicit optional ingredient or broth/agar
variant split.

A gitignore-independent `rg --no-ignore --hidden` search of
`data/normalized_yaml`, `data/merge_yaml/merged`, the ID registry, the recipe
catalog, `reports`, and `data/import_tracking/reports` for
`CultureMech:008699`, `TOGO_M2107_BTT_medium`, `TOGO:M2107`, `NBRC_M1430`, and
merge fingerprint `53dcfb98...` found the active TOGO M2107 owner, this
generated merge, expected generated indexes and reports, and no existing YAML
review report for this record. `find reports/yaml_record_review -maxdepth 1
-type f -name '*btt_medium__53dcfb98.md'` found no pre-existing report,
including ignored files in the report directory.

Empty target-organism and growth-evidence slots are not defects for this
source-only recipe.

## Findings

| Severity | Finding | Maintained owner for a future fix |
| --- | --- | --- |
| Major | `data/merge_yaml/merged/btt_medium__53dcfb98.yaml` is stale relative to its repaired owner and still lacks the 2026-09-11 water, pH, reference, source-annotation, Yeast extract, and agar corrections. | Regenerate from `data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml` |
| Major | The conditional 20 g `Agar (if needed)` source row is modeled as a normal ingredient in a `SOLID_AGAR` recipe, which hides the NBRC source's broth-versus-agar optionality. | `data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml` |
| Major | The generated water row is still `1 G_PER_L` instead of the source 1 L final-volume row. | Already corrected in `data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml`; regenerate the merge |
| Minor | The generated record omits the source-supported pH 5.5, preparation step, agar grounding, and structured TOGO/NBRC references already present in the maintained owner. | Already corrected in `data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml`; regenerate the merge |

No blocker findings found.

## Recommended Edits

1. Decide whether to model NBRC 1430 as an optionally solidified base medium or
   as broth and agar variants, then update
   `data/normalized_yaml/bacterial/TOGO_M2107_BTT_medium.yaml` accordingly.
2. Regenerate `data/merge_yaml/merged/btt_medium__53dcfb98.yaml` from the
   repaired owner.
3. Inspect the regenerated diff to confirm `1.0 L` water, `ph_value: 5.5`, the
   `ADJUST_PH` preparation step, FoodOn yeast extract, CHEBI agar, and TOGO/NBRC
   references are present.

## Follow-up Checks

- Rerun focused schema, strict, term, and reference validation on the
  regenerated `data/merge_yaml/merged/btt_medium__53dcfb98.yaml`.
- Rerun `just verify-merges` after regeneration and inspect this generated
  record to confirm it no longer carries the stale water or provenance state.
- Manually compare the regenerated owner against live TOGO M2107 and NBRC 1430.

## Additional Notes

None found.
