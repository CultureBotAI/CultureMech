# YAML Record Review: bb_merds__c800a0bf

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bb_merds__c800a0bf.yaml
- Started UTC: 2026-09-21T19:08:28Z
- Finished UTC: 2026-09-21T19:09:50Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bb_merds__c800a0bf.yaml` |
| Maintained owner | `data/normalized_yaml/bacterial/bb_merds.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:000323` |
| Name | `bb_merds` |
| Original name | `BB:MErds` |
| Source | MediaDive/CCAP Medium `C16`, CCAP `MR_BB_MErds.pdf` |
| Merge status | Generated one-source merge from `bb_merds` with fingerprint suffix `c800a0bf` to avoid the algae `bb_merds.yaml` filename collision |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated `bb_merds__c800a0bf` merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this MediaDive import has no populated `references` list or evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated `bb_merds__c800a0bf` merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bb_merds__c800a0bf.yaml` is a generated merge for the MediaDive/CCAP `C16` copy of BB:MErds and resolves to `CultureMech:000323` from `data/normalized_yaml/bacterial/bb_merds.yaml`.
- The same CCAP `MR_BB_MErds.pdf` source also appears as the algae `CultureMech:000054` record reviewed at `data/merge_yaml/merged/bb_merds.yaml`; `data/import_tracking/reports/filename_collisions.tsv` records the `bb_merds.yaml` bacterial/algae collision and marks the ingredient sets as different.
- The source identity is wrong at the category layer: CCAP BB:MErds is for freshwater/brackish-water algae, not a bacterial medium.
- Exact gitignore-independent searches for `CultureMech:000323`, `mediadive.medium:C16`, `bb_merds__c800a0bf`, and `Source: CCAP, ID: C16` covered `data`, `src`, `scripts`, `.claude`, `justfile`, and `CLAUDE.md`. They found the maintained normalized owner, generated merge, generated indexes, the filename-collision report, duplicate-summing reports, and no local raw MediaDive or CCAP C16 dump.
- The ingredient groundings mostly match their formula strings, but `NaNO3` and `KNO3` still carry legacy MediaIngredientMech identifiers.

## Evidence

- The cited CCAP `MR_BB_MErds.pdf` supports BB:MErds as an 8:2 or 1:1 mixture, not a single flat 20-row chemical formulation.
- The YAML flattens chemical rows from nested BB, MErds, SES, SE1, and SE2 recipes into one top-level BB:MErds ingredient list.
- The import summed salts from different nested recipes: `NaNO3` is `11.0 g/L` with notes saying it merged `10.0` and `1.0`; `MgSO4 x 7 H2O` is `30.0 g/L` from `10.0` and `20.0`; and `K2HPO4` is `30.0 g/L` from `10.0` and `20.0`. The PDF keeps those occurrences in separate stock and constituent media boundaries.
- The PDF uses filtered seawater as a volume in MErds, but the YAML models `Sea water` as `1000 G_PER_L`.
- The YAML carries 12 preparation steps, including the BB, MErds, SES, SE1, and SE2 nested instructions, even though the top-level BB:MErds instruction only says to mix the constituent recipes and autoclave.

## Completeness

- Consequential gaps:
  - The record is in `data/normalized_yaml/bacterial` and `category: bacterial` despite having a CCAP freshwater/brackish-water algae source.
  - The record duplicates the same CCAP source identity as algae `CultureMech:000054` but has a divergent, obsolete representation.
  - Source stock and constituent-medium boundaries were flattened and duplicate ingredients were arithmetically merged across unrelated nested media.
  - `Sea water` and `Soil` are top-level ingredients instead of nested MErds/SES/soil-extract context.
  - Preparation instructions from BB, MErds, SES, SE1, and SE2 were concatenated under BB:MErds.
  - Two rows still retain legacy `mediaingredientmech_term` values after the June 2026 MIM migration.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the inspected CCAP PDF gives only a recipe for freshwater/brackish-water algae and no strain-specific growth assay.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw MediaDive or CCAP C16 source dump under `data/raw` or `data/import_tracking`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | The record is filed as bacterial even though the CCAP source recipe is for freshwater/brackish-water algae. | `MR_BB_MErds.pdf` titles the recipe BB:MErds for freshwater/brackish-water algae; the YAML owner is `data/normalized_yaml/bacterial/bb_merds.yaml` with `category: bacterial`. | `data/normalized_yaml/bacterial/bb_merds.yaml`, the category/indexes, and likely the MediaDive CCAP import mapping. |
| Major | The record duplicates the CCAP BB:MErds source identity already represented by `CultureMech:000054` but diverges from it. | Both records cite `MR_BB_MErds.pdf`; `filename_collisions.tsv` records `bb_merds.yaml` in bacterial and algae categories with different ingredient-row sets. | Reconcile `data/normalized_yaml/bacterial/bb_merds.yaml` against `data/normalized_yaml/algae/bb_merds.yaml` and the merge/deduplication rules. |
| Major | Nested BB, MErds, SES, SE1, and SE2 rows were flattened into one top-level ingredient list. | The PDF uses BB:MErds as a mixture of separate BB and MErds recipes; the YAML lists direct salts, filtered seawater, and soil from nested recipes as BB:MErds ingredients. | The normalized owner; broad repeats may require the MediaDive CCAP importer. |
| Major | Duplicate salts were summed across unrelated nested contexts. | The YAML records merged `NaNO3`, `MgSO4 x 7 H2O`, and `K2HPO4` values, while the PDF keeps those occurrences in separate stock recipes. | The normalized owner; broad repeats may require the MediaDive duplicate-cleanup/import logic. |
| Major | Preparation from nested recipes was over-scoped to BB:MErds. | The YAML has 12 steps spanning BB, MErds, SES, SE1, and SE2; the top-level CCAP BB:MErds statement only says to mix the constituent media and autoclave. | The normalized owner. |
| Minor | Two ingredients still use legacy MIM identifiers. | `NaNO3` and `KNO3` retain `mediaingredientmech_term` values while the other rows were migrated to CHEBI-keyed `mediaingredientmech_chebi_term` links. | The normalized owner. |

## Recommended Edits

1. Move the authoritative CCAP BB:MErds representation to the algae category and reconcile whether `CultureMech:000323` should merge into or be superseded by `CultureMech:000054`.
2. Replace the flat MediaDive C16 ingredient list with BB and MErds constituent-medium references plus the source-supported 8:2 and 1:1 mixture variants.
3. Remove summed duplicate salt amounts and preserve any repeated salts only inside their source stock or constituent medium.
4. Remove filtered seawater and soil rows from top-level BB:MErds ingredients; keep them scoped to MErds, SES, SE1, or SE2 as applicable.
5. Keep only the BB:MErds top-level mix/autoclave preparation, with nested preparation retained on the nested recipes that own it.
6. Replace the legacy `NaNO3` and `KNO3` MediaIngredientMech links with CHEBI-keyed representations.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the repaired normalized owner.
- Re-extract `MR_BB_MErds.pdf` and manually compare the BB/MErds mixture ratios, constituent references, and autoclave instruction against the YAML.
- Regenerate the generated merge and run `just verify-merges` plus `just audit-merge-freshness`.
- Confirm `data/import_tracking/reports/merged_duplicates.tsv` no longer reports summed salts for `bacterial/bb_merds.yaml`.

## Additional Notes

- The generated merge is in sync with `data/normalized_yaml/bacterial/bb_merds.yaml`; the scientific issues above are inherited from that maintained input.
- Exact gitignore-independent searches included ignored files where present.
