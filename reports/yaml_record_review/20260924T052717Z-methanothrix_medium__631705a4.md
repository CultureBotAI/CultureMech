# YAML Record Review: METHANOTHRIX MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/methanothrix_medium__631705a4.yaml
- Started UTC: 2026-09-24T05:26:00Z
- Finished UTC: 2026-09-24T05:27:17Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/methanothrix_medium__631705a4.yaml`, a generated `MediaRecipe` for `CultureMech:001433` with `name: methanothrix_medium`, `original_name: METHANOTHRIX MEDIUM`, pH 7.2, and source grounding `mediadive.medium:334`.

The record was merged from one normalized input:

- `data/normalized_yaml/archaea/methanothrix_medium.yaml`

## Validation

| Check | Result |
|---|---|
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/methanothrix_medium__631705a4.yaml` | Passed; exited 0 with no diagnostics. |
| `python scripts/validate_strict.py data/merge_yaml/merged/methanothrix_medium__631705a4.yaml --out /private/tmp/methanothrix_medium__631705a4.strict.tsv --workers 1 --quiet` | Passed; scanned 1 file and reported 0 error rows. |
| `linkml-reference-validator validate data data/merge_yaml/merged/methanothrix_medium__631705a4.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; the reference validator performed 0 checks for this file. |
| `linkml-term-validator validate-data data/merge_yaml/merged/methanothrix_medium__631705a4.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils` `pkg_resources` deprecation warning. |
| Embedded curation history | Not checked: the documented history validator is a standalone `history/` validator, not a focused check for `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

The source identity is correct: DSMZ/MediaDive medium 334 is `METHANOTHRIX MEDIUM`, a defined liquid archaeal medium with pH 7.2.

The formulation graph is not correct. DSMZ 334 is assembled from Solution A, Solution B, Solution C, Solution D, Solution E, and Solution F. Solution A contains Trace elements solution as a 10 ml nested stock, and Solution E contains 1 ml Wolin's vitamin solution. The generated record has no `solutions` array and represents every stock child as a top-level ingredient at that stock's G_PER_L concentration.

## Evidence

Supported source claims:

- DSMZ/MediaDive 334 supports the record's identity, defined liquid classification, pH 7.2, and Solution A ingredient amounts after MediaDive's per-solution G_PER_L calculation over the 951 ml Solution A volume.
- The retained preparation step accurately captures the DSMZ/MediaDive main instruction to sparge Solution A with 80% N2 and 20% CO2, dispense under that atmosphere, autoclave, add sterile Solutions B through F before inoculation, filter-sterilize vitamins, and adjust the complete medium to pH 7.2.
- The retained 20% inoculum note is present in DSMZ 334.

Unsupported or over-scoped generated claims:

- Na2CO3, NaCl, CaCl2 x 2 H2O, MgCl2 x 6 H2O, NH4Cl, Na2S x 9 H2O, all Trace elements constituents, and all Wolin vitamin constituents are stock constituents, not top-level final-medium ingredients.
- NaCl and CaCl2 x 2 H2O have been summed across Solution C and Trace elements solution; DSMZ keeps those amounts in distinct stocks.
- Na2CO3 at 50 G_PER_L, NH4Cl at 30 G_PER_L, and Na2S x 9 H2O at 50 G_PER_L are Solution B, D, and F stock concentrations, not final-medium concentrations.
- Biotin through (DL)-alpha-Lipoic acid are Wolin's vitamin solution components from DSMZ medium 120, not direct ingredients of medium 334.
- The Trace elements pH 6.5 instructions are scoped to that stock solution, but the YAML stores them as final-medium `preparation_steps` step 3.

## Completeness

The schema-optional evidence, discussions, growth, target organism, and recipe variant arrays are empty; empty optional slots were not treated as defects.

Consequential gaps:

- Solution A through Solution F are absent, so the record cannot represent the 951/25/10/10/1/10 ml final assembly.
- The 10 ml Trace elements solution nested under Solution A and the 1 ml Wolin's vitamin solution nested under Solution E are absent as stock references.
- Water rows are missing from Solution A, B, C, D, F, Trace elements solution, and Wolin's vitamin solution.
- The current DSMZ PDF includes a DSM 17206 variant supplement of 0.50 g/l yeast extract and 0.50 g/l Trypticase peptone from sterile anoxic stock solutions; the MediaDive REST response and generated record omit it.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Six final assembly stocks were flattened into top-level ingredients. | MediaDive 334 represents the final recipe as 951 ml Solution A, 25 ml Solution B, 10 ml Solution C, 10 ml Solution D, 1 ml Solution E, and 10 ml Solution F. The YAML has no `solutions` array and stores all stock children in `ingredients`. | `data/normalized_yaml/archaea/methanothrix_medium.yaml`; MediaDive importer. |
| Major | Nested Trace elements and Wolin vitamin stocks were flattened. | Solution A uses 10 ml Trace elements solution, and Solution E uses 1 ml Wolin's vitamin solution. Their constituent salts and vitamins are top-level ingredients in the YAML. | `data/normalized_yaml/archaea/methanothrix_medium.yaml`; MediaDive importer. |
| Major | Duplicate cleanup merged chemicals across different stock scopes. | NaCl appears in Solution C and Trace elements solution; CaCl2 x 2 H2O appears in the same two stocks. The YAML sums each pair into a single top-level row. | `data/normalized_yaml/archaea/methanothrix_medium.yaml`; duplicate cleanup. |
| Major | Water rows were dropped from all stocks. | DSMZ 334 lists water in Solution A, B, C, D, F, Trace elements solution, and Wolin's vitamin solution. The YAML has no Distilled water ingredient or solution composition. | `data/normalized_yaml/archaea/methanothrix_medium.yaml`; MediaDive importer. |
| Minor | Trace elements preparation lost its stock scope. | DSMZ/MediaDive attach the NTA dissolution and pH 6.5 adjustment instructions to Trace elements solution. The YAML stores that text as final-medium step 3. | `data/normalized_yaml/archaea/methanothrix_medium.yaml`; MediaDive importer. |

## Recommended Edits

1. Rebuild the normalized DSMZ 334 record with Solution A, Solution B, Solution C, Solution D, Solution E, and Solution F as final stock additions.
2. Preserve Trace elements solution under Solution A and Wolin's vitamin solution under Solution E instead of flattening their child ingredients.
3. Keep NaCl and CaCl2 x 2 H2O rows scoped to their own stocks.
4. Restore source water rows for every stock solution.
5. Scope the NTA/pH 6.5 instructions to Trace elements solution.
6. Check whether the DSM 17206 yeast-extract and Trypticase-peptone supplement from the current DSMZ PDF should be represented as a medium variant.
7. Regenerate `data/merge_yaml/merged/methanothrix_medium__631705a4.yaml`.

## Follow-up Checks

- Run focused schema, strict, reference, and term validation on the regenerated record.
- Diff the regenerated record against DSMZ/MediaDive 334 and verify that Solution A through Solution F, Trace elements solution, and Wolin's vitamin solution keep their stock scopes.
- Verify that the final medium has 951 ml, 25 ml, 10 ml, 10 ml, 1 ml, and 10 ml additions rather than stock child ingredients at final top level.
- Inspect the DSMZ PDF and MediaDive REST response for the DSM 17206 supplement and either add a scoped variant or record that the current import intentionally follows only MediaDive REST.

## Additional Notes

- Empty optional evidence and discussion fields were not treated as defects.
- Exact owner searches used `rg --no-ignore --hidden`, so ignored files were included when resolving `CultureMech:001433` and `data/normalized_yaml/archaea/methanothrix_medium.yaml`.
