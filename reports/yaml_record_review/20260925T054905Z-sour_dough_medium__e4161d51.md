# YAML Record Review: sour_dough_medium__e4161d51

- Repository: CultureMech
- Record: data/merge_yaml/merged/sour_dough_medium__e4161d51.yaml
- Started UTC: 2026-09-25T05:48:10Z
- Finished UTC: 2026-09-25T05:49:05Z
- Verdict: needs curation

## Target

Generated merged YAML for DSMZ 225, SOUR DOUGH MEDIUM. The generated target is a one-source merge from `data/normalized_yaml/bacterial/sour_dough_medium.yaml`.

## Validation

- LinkML open validation: Passed; `linkml-validate` reported no issues.
- Strict validation: Passed; `/private/tmp/sour_dough_medium.strict.tsv` contained only the header row.
- Reference validation: Passed; the reference validator ran 0 checks and reported no failures.
- Term validation: Passed.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in merged YAML.

## Identity and Grounding

The DSMZ identity is correct: DSMZ Medium 225 is SOUR DOUGH MEDIUM with final pH 5.6. Maltose and Tween 80 are grounded to reasonable CHEBI terms.

The yeast-extract identity is not source-faithful after cleanup. DSMZ has both `Yeast extract` at 3.0 g and `Fresh yeast extract` at 15.0 ml, while the normalized and generated records contain one generic `Yeast extract` row at 18.0 g/L.

## Evidence

DSMZ Medium 225 lists, up to 1000 ml, 20.0 g maltose, 3.0 g yeast extract, 15.0 ml fresh yeast extract, 0.3 g Tween 80, 6.0 g casein peptone, tryptic digest, and distilled water. MediaDive 225 represents the same two yeast-extract rows as 3 g `Yeast extract` plus 15 ml `Yeast extract` with attribute `fresh`, and it has no `g_l` value for the fresh-extract volume row.

The source also specifies pH 5.6 adjusted with 20% lactic acid or HCl and describes the fresh yeast extract preparation from a 20% baker's yeast suspension.

## Completeness

The generated record preserves the pH and the fresh yeast extract preparation note. It omits the final `Distilled water up to 1000.0 ml` row, loses the `fresh` attribute on the 15 ml yeast-extract ingredient, and drops `tryptic digest` from `Casein peptone, tryptic digest`.

## Findings

- Critical: the data-quality cleanup merged two distinct DSMZ source rows, `Yeast extract` 3.0 g and `Fresh yeast extract` 15.0 ml, into one `Yeast extract` 18.0 g/L row. That changes a volume addition of fresh extract into an extra 15 g/L of ordinary yeast extract.
- Major: the generated ingredient list omits the DSMZ `Distilled water up to 1000.0 ml` row.
- Minor: `Casein peptone, tryptic digest` is normalized to `Casein peptone` without preserving the `tryptic digest` attribute.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/sour_dough_medium.yaml` so the DSMZ source rows stay separate: 3 g/L ordinary yeast extract and 15 ml/L fresh yeast extract.
- Restore the 1000 ml distilled-water row from DSMZ 225.
- Preserve `tryptic digest` as a source attribute or in ingredient notes for casein peptone.
- Regenerate `data/merge_yaml/merged/sour_dough_medium__e4161d51.yaml` from the repaired normalized record.

## Follow-up Checks

- Re-run open, strict, reference, and term validation on the regenerated merged record.
- Confirm the regenerated ingredient list has six source rows, including distinct ordinary and fresh yeast-extract ingredients and distilled water.
- Search this record's exact curation lineage for cleanup code that merges duplicate preferred labels without checking source attributes or units, because the same merge rule could affect other MediaDive recipes.

## Additional Notes

None found.
