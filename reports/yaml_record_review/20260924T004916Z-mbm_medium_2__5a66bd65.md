# YAML Record Review: mbm_medium_2

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml
- Started UTC: 2026-09-24T00:47:39Z
- Finished UTC: 2026-09-24T00:49:17Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:003343 |
| Label | mbm_medium_2 |
| Original label | MBM MEDIUM 2 |
| Category | bacterial |
| Generated path | data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml |
| Maintained owner | data/normalized_yaml/bacterial/mbm_medium_2.yaml |
| Source accession | mediadive.medium:J994 |

The reviewed merge comes from one normalized owner and is identical to
`data/normalized_yaml/bacterial/mbm_medium_2.yaml` except for generated merge
metadata. Future fixes belong in that normalized owner or in the MediaDive
importer that flattened JCM/MediaDive solution rows.

## Validation

| Check | Result |
| --- | --- |
| `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml` | Passed with `No issues found`. |
| `python scripts/validate_strict.py data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml --out /private/tmp/mbm_medium_2__5a66bd65.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. The TSV had only its header. |
| `linkml-reference-validator validate data data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 reference checks. |
| `linkml-term-validator validate-data data/merge_yaml/merged/mbm_medium_2__5a66bd65.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the validator's expected `eutils`/`pkg_resources` warning. |
| Embedded `curation_history` validation | Not checked: the repository's `just validate-history` target validates standalone `history/` files, not embedded MediaRecipe history lists. |

The validators do not check that milliliter solution additions remain distinct
from grams-per-liter ingredients.

## Identity and Grounding

- MediaDive J994 and JCM GRMD 994 both identify the source as `MBM MEDIUM 2`
  with final pH 7.5.
- A `find` lookup resolved `data/normalized_yaml/bacterial/mbm_medium_2.yaml`
  as the normalized owner. A scoped, gitignore-independent exact search of the
  target, ID registry, and normalized MediaDive/recipe indexes found
  `CultureMech:003343` and `mediadive.medium:J994` pointing to that owner.
- A gitignore-independent exact scan of the generated target and maintained
  owner found no `solutions`, `references`, or `target_organisms` sections.
- The JCM 994 page delegates Trace minerals to JCM 151 and Trace vitamins to JCM
  197; those stock tables agree with MediaDive solution IDs 3804 and 3861.

## Evidence

### Supported

- The pH 7.5 scalar is source-supported.
- The main base salts and acetate/resazurin ingredient identities come from JCM
  994 and MediaDive J994.
- The preferred names for Trace minerals and Trace vitamins components match
  JCM 151/JCM 197 via MediaDive solution IDs 3804 and 3861.

### Unsupported or Over-scoped

- The record flattens `Trace minerals` and `Trace vitamins` into final MBM
  ingredients. JCM 994 adds 10 ml of each stock; it does not add every 1 L stock
  recipe component at stock concentration to MBM Medium 2.
- The cleanup history summed repeated salt labels across unrelated recipe
  boundaries. For example, `NaCl` is stored as `78.2779 + 1.0`, `MgSO4 x 7 H2O`
  as `5.87084 + 3.0`, and `CaCl2 x 2 H2O` as `0.391389 + 0.1`, but the second
  values in those sums came from the Trace minerals stock.
- The added stock solutions after the first autoclave are represented as direct
  grams-per-liter ingredients: `1 M Disodium fumarate solution` at 40 ml,
  filter-sterilized `8% NaHCO3 solution` at 50 ml, `5% L-Cysteine HCl x H2O`
  solution at 6 ml, and `5% Na2S x 9 H2O` solution at 6 ml.
- `Sodium acetate x 3 H2O` is grounded to `CHEBI:32954` labeled `sodium acetate`,
  losing the trihydrate form.
- `MnSO4 x n H2O` is grounded to `CHEBI:86360` labeled `manganese(II) sulfate`;
  the variable hydrate source form needs an exact-form review.

## Completeness

- The missing `solutions` block is a major representational gap because the
  source formulation has a base solution, autoclaved or filter-sterilized
  additions after cooling, anaerobic stock additions before inoculation, and two
  nested stock recipes with their own preparation scopes.
- `references` is empty. The JCM/MediaDive URL is recoverable from `media_term`,
  `notes`, and the import history, but JCM 994, JCM 151, and JCM 197 are not
  represented as structured citations.
- `target_organisms` is empty and that is acceptable here; the inspected recipe
  source does not make a strain-specific growth claim.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | Nested Trace minerals and Trace vitamins stocks were flattened into direct MBM ingredients. | JCM 994 adds 10 ml Trace minerals and 10 ml Trace vitamins; JCM 151/JCM 197 and MediaDive 3804/3861 define one-liter stock recipes, but the YAML records those stock components directly under `ingredients`. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml` or the MediaDive importer. |
| major | The duplicate-ingredient cleanup summed base-medium salts with trace-stock salts. | The YAML notes `NaCl` as `78.2779 + 1.0`, `MgSO4 x 7 H2O` as `5.87084 + 3.0`, and `CaCl2 x 2 H2O` as `0.391389 + 0.1`; the added terms are Trace minerals stock concentrations, not second final MBM additions. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml` and the duplicate-ingredient cleanup rule. |
| major | Milliliter stock additions are stored as grams-per-liter ingredients. | JCM 994 uses 40 ml 1 M disodium fumarate, 50 ml 8 percent NaHCO3, 6 ml 5 percent L-Cysteine HCl x H2O, and 6 ml 5 percent Na2S x 9 H2O; the record stores values `40`, `50`, `6`, and `6` with `unit: G_PER_L`. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml` or the MediaDive importer. |
| major | Hydrated ingredients have exact-form grounding errors. | `Sodium acetate x 3 H2O` maps to anhydrous `sodium acetate`, and `MnSO4 x n H2O` maps to unsuffixed `manganese(II) sulfate`; source hydration is identity-significant. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml`. |
| minor | The structured preparation actions blur main-medium and stock-solution scopes. | The Trace minerals preparation step is stored as step 4 of the main medium, and steps that include boiling, cooling under N2-CO2, aseptic stock addition, and anaerobic dispensing are all labeled `AUTOCLAVE` or `ADJUST_PH`. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml` or the MediaDive importer. |
| minor | Structured references are missing. | The formula relies on JCM 994 plus JCM 151/JCM 197 stock formulas, but the record has no `references` section. | `data/normalized_yaml/bacterial/mbm_medium_2.yaml`. |

## Recommended Edits

1. Preserve the JCM 994 recipe boundaries: the pre-autoclave base, 40 ml
   disodium fumarate solution, 50 ml bicarbonate solution, 10 ml Trace vitamins,
   and the two 6 ml anaerobic stock additions should not be direct `G_PER_L`
   ingredient rows.
2. Represent Trace minerals and Trace vitamins as solutions or linked
   `SolutionRecipe` records sourced to JCM 151 and JCM 197/MediaDive 3804 and
   3861, rather than flattening their stock components into MBM.
3. Undo the cross-boundary duplicate sums for NaCl, MgSO4 x 7 H2O, and
   CaCl2 x 2 H2O.
4. Recheck exact ontology groundings for Sodium acetate x 3 H2O and
   MnSO4 x n H2O, leaving them unresolved if no exact hydrated term is verified.
5. Split preparation into main-medium actions and stock-solution preparation
   actions so pH 6.5/7.0 belongs to Trace minerals and final pH 7.5 belongs to
   MBM Medium 2.
6. Add structured references for JCM 994, JCM 151, JCM 197, and MediaDive J994.

## Follow-up Checks

- Rerun the focused open-schema, strict, term, and reference validators on the
  maintained owner and regenerated merged record.
- Manually recalculate the final top-level formulation from the 1022 ml MediaDive
  volume after stock boundaries are restored.
- Run a focused duplicate-ingredient check to confirm repeated labels from a
  main solution and a stock solution are not merged together.
- Repeat a gitignore-independent exact search for `CultureMech:003343`,
  `mediadive.medium:J994`, and `mbm_medium_2` to confirm regenerated outputs and
  indexes all point at the corrected owner.

## Additional Notes

- JCM 151 is a full Pyrococcus medium page that also defines `Trace minerals`;
  JCM 994 uses only that page's Trace minerals stock.
- JCM 197 also contains a top-level medium recipe, but JCM 994 only references
  its `Trace vitamins` stock formula.
