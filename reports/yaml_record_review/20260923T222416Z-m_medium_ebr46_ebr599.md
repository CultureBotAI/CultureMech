# YAML Record Review: m_medium_ebr46_ebr599

- Repository: `CultureBotAI/CultureMech`
- Record: `data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml`
- Started UTC: `2026-09-23T22:24:16Z`
- Finished UTC: `2026-09-23T22:24:17Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `MediaRecipe` |
| Stable ID | `CultureMech:008287` |
| Record name | `m_medium_ebr46_ebr599` |
| Original name | `M medium (EBR46, EBR599)` |
| Source accession | `TOGO:M1724` |
| Source label | `M medium (EBR46, EBR599)` |
| Original source | `NBRC_M933` |
| Category | `bacterial` |
| Maintained input | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` |
| Generated record | `data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml` |

`data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml` is a generated merge from
`data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` with fingerprint
`ccba26ea9fbcb84cfd38150a5afe0c92869738647a1366879519cf9bf7818ff1`. Future
fixes should be made in the normalized input, the TOGO/NBRC importer or
solution-migration path that populated it, and then regenerated into
`data/merge_yaml/merged/`.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml` | Passed. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml --out /private/tmp/m_medium_ebr46_ebr599.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned and 0 error rows emitted to `/private/tmp/m_medium_ebr46_ebr599.strict.tsv`. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks executed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed. |
| Embedded history | `just validate-history` | Not checked: this validator targets standalone YAML files under `history/`, not `MediaRecipe.curation_history` entries embedded in merged recipe YAML. |

## Identity and Grounding

- The record identity is correct at the medium level: `CultureMech:008287`,
  `name: m_medium_ebr46_ebr599`, `original_name: M medium (EBR46, EBR599)`,
  `media_term.term.id: TOGO:M1724`, TOGO's `original_media_id: NBRC_M933`,
  and NBRC `NO=933` all denote the same recipe.
- An ignored-file-inclusive exact search for `CultureMech:008287`,
  `m_medium_ebr46_ebr599`, and `M medium (EBR46, EBR599)` across
  `data/normalized_yaml`, `data/merge_yaml`, `reports`, `src`, `tests`,
  `conf`, `scripts`, `.claude`, `pyproject.toml`, `justfile`, and `README.md`
  found one live maintained input at
  `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml`; the older
  `TOGO_M1724_M_medium_EBR46_EBR599.yaml` name appeared only in archived
  validation reports.
- The row set matches the source at the label level for the main undefined
  carbon source: this record correctly has `Bacto Tryptone (Difco)` rather than
  the EBR45 record's cellulose or cellobiose row.
- Several ingredient groundings are broader than the source chemicals:
  `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, and `NiCl2 x 6 H2O` are grounded to
  labels that omit the hydration state, and `Ca-pantothenate` is grounded to
  pantothenate rather than an inspected calcium salt.
- The solution CURIEs are unsupported. TOGO M1724 defines local
  `Vitamin solution*` and `Mineral solution**` formulas on the NBRC M933 page,
  but the YAML links to generic `mediadive.solution:6241` and
  `mediadive.solution:5281` records whose formulas do not match.

## Evidence

- TOGO M1724 and NBRC `NO=933` support the main-medium formula, including 1 L
  distilled water; 0.2 g yeast extract; 0.4 g `KH2PO4`; 0.4 g
  `K2HPO4 x 3 H2O`; 1 g `NH4Cl`; 0.1 g `MgCl2 x 6 H2O`; 6 g `NaHCO3`;
  0.5 g `Cysteine-HCl`; 0.25 g `Na2S x 9 H2O`; 1 mg resazurin; 10 g
  Bacto Tryptone (Difco); 10 ml `Vitamin solution*`; 10 ml
  `Mineral solution**`; and pH 7.5.
- The generated record stores `Resazurin` as `1 G_PER_L`; TOGO and NBRC state
  1 mg in the one-liter main medium.
- The generated record stores `Vitamin solution*` and `Mineral solution**` as
  `10 G_PER_L`. TOGO and NBRC state 10 ml additions of each stock per liter.
- The local M1724 vitamin stock has 1 L water plus ten mg-scale vitamin or
  growth-factor rows. The generated record flattens those ten stock rows into
  direct top-level final-medium `G_PER_L` rows without applying the 10 ml/L
  dilution or preserving the mg units.
- The local M1724 mineral stock has 1 L water plus eleven gram-scale mineral
  rows. The generated record flattens those eleven stock rows into direct
  top-level final-medium rows without applying the 10 ml/L dilution.
- TOGO and NBRC specify a nitrogen-gas preparation: omit `NaHCO3`,
  cysteine-HCl, sodium sulfide, and vitamin solution at first, distribute the
  medium under nitrogen gas, seal with butyl rubber stoppers, autoclave at
  121 C for 15 minutes, aseptically inject the held-back ingredients, and
  adjust pH to 7.5 with sterile 0.6 N sulfuric acid. The generated record has
  variable rows for nitrogen and sulfuric acid but no `preparation_steps` and
  no `ph_value`.
- The generated merge still stores `Distilled water` as `3.0 G_PER_L`, while
  the maintained normalized input has been repaired to `1.0 G_PER_L`.

## Completeness

- Consequentially incomplete: local `Vitamin solution*` and
  `Mineral solution**` are not represented as nested M1724 stock formulas with
  10 ml/L addition volumes.
- Consequentially incomplete: the record does not retain pH 7.5.
- Consequentially incomplete: the record does not retain the nitrogen-gas,
  sealed-vessel, autoclave, post-autoclave injection, and sulfuric-acid pH
  adjustment instructions.
- Empty optional target-organism and growth-evidence fields were not treated
  as defects; TOGO and NBRC M933 are recipe sources, not primary growth
  evidence for a named target organism.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Resazurin` is off by a factor of 1000 because a 1 mg source row became `1 G_PER_L`. | TOGO M1724 and NBRC `NO=933` list resazurin as 1 mg in the one-liter main medium; the generated YAML stores `1 G_PER_L`. | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` or the TOGO/NBRC importer. |
| Major | Local M1724 vitamin and mineral stocks were flattened into final top-level ingredients without the 10 ml/L dilution. | The source adds 10 ml of each one-liter stock to the main medium; generated vitamin and mineral rows retain stock-strength values as final `G_PER_L` ingredients. | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` and the TOGO/NBRC solution migration or flattening logic. |
| Major | The two stock additions have wrong identities and wrong units. | `Vitamin solution*` and `Mineral solution**` are local M1724 stocks added at 10 ml/L; the generated `solutions` rows use `10 G_PER_L` and point to generic `mediadive.solution:6241` and `mediadive.solution:5281` records with different formulas. | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml`; remove unsupported generic MediaDive solution CURIEs unless equivalence is source-verified. |
| Major | The source pH and preparation protocol are mostly absent. | TOGO/NBRC specify pH 7.5, nitrogen gassing, butyl rubber stoppers, autoclaving at 121 C for 15 minutes, aseptic injection of NaHCO3, cysteine-HCl, sodium sulfide, and vitamin solution, and pH adjustment with sterile 0.6 N sulfuric acid. | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` or the TOGO/NBRC importer fields for preparation comments. |
| Major | Several hydrated or salt-specific ingredient identities are grounded too broadly. | Source labels `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and `Ca-pantothenate` do not match the broader CHEBI labels stored for those rows. | `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml`; re-resolve these labels with exact hydrate and salt constraints. |
| Major | The generated merge is stale for the water duplicate repair. | The generated record still has `Distilled water` 3.0 with a merged-duplicates note; `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml` has already repaired that row to 1.0. | Regenerate `data/merge_yaml/merged/m_medium_ebr46_ebr599.yaml` from the normalized input after source-level fixes. |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/m_medium_ebr46_ebr599.yaml`, or in the
   TOGO/NBRC import transform that owns it, correct the main-medium amounts:
   keep resazurin as 1 mg per source liter, keep each stock addition as
   10 ml/L, and add `ph_value: 7.5`.
2. Model the local M1724 `Vitamin solution*` and `Mineral solution**` formulas
   as local stock recipes instead of final direct ingredients. If any stock
   members are intentionally flattened, apply both the 10 ml/L dilution and the
   source mg-to-g conversion.
3. Remove the unsupported `mediadive.solution:6241` and
   `mediadive.solution:5281` links, or replace them only with identifiers for
   the exact local M1724 stock formulas.
4. Add preparation steps that preserve the source order: mix all rows except
   `NaHCO3`, cysteine-HCl, sodium sulfide, and vitamin stock; distribute under
   nitrogen gas; seal with butyl rubber stoppers; autoclave at 121 C for
   15 minutes; aseptically inject the held-back ingredients; adjust to pH 7.5
   with sterile 0.6 N sulfuric acid.
5. Re-ground `K2HPO4 x 3 H2O`, `CoCl2 x 6 H2O`, `NiCl2 x 6 H2O`, and
   `Ca-pantothenate` against exact hydrated or salt forms, leaving unresolved
   rows explicit if exact ontology terms are unavailable.
6. Regenerate `data/merge_yaml/merged/` so the generated `m_medium_ebr46_ebr599`
   record reflects the current normalized record and no longer carries the
   stale `3.0` water sum.

## Follow-up Checks

- Run open-schema validation for the corrected normalized record and the
  regenerated merge:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe <record>`.
- Run strict validation for the corrected normalized record and regenerated
  merge: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py <record> --out /private/tmp/<record>.strict.tsv --workers 1 --quiet`.
- Run reference validation after removing or replacing the two generic
  MediaDive solution CURIEs:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data <record> --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`.
- Run term validation after re-grounding hydrate and salt rows:
  `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data <record> -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`.
- Manually compare the regenerated record against TOGO M1724 or NBRC `NO=933`
  and verify that the main formula, both stock formulas, 10 ml/L stock
  additions, pH 7.5, and nitrogen preparation instructions are preserved
  without flattening stock formulas at stock strength.

## Additional Notes

- The focused validators pass because the wrong stock references, stale water
  sum, and stock-strength flattened ingredients are valid YAML and valid
  LinkML.
- This record is source-distinct from `m_medium_ebr45`: NBRC M933 uses Bacto
  Tryptone and nitrogen gas where NBRC M905 uses cellulose or cellobiose and
  allows either `N2/CO2` 80/20 or `N2`.
