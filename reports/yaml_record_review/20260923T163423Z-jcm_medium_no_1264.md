# YAML Record Review: JCM MEDIUM No. 1264

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/jcm_medium_no_1264.yaml
- Started UTC: 2026-09-23T16:33:56Z
- Finished UTC: 2026-09-23T16:34:23Z
- Verdict: needs curation

## Target

- Generated MediaRecipe: `data/merge_yaml/merged/jcm_medium_no_1264.yaml`
- Stable ID: `CultureMech:002430`
- Name: `jcm_medium_no_1264`
- Original name: `JCM MEDIUM No. 1264`
- Category: `bacterial`
- Media term: `mediadive.medium:J1264`
- Source: MediaDive import of JCM Medium 1264, pointing to `https://www.jcm.riken.jp/cgi-bin/jcm/jcm_grmd?GRMD=1264`
- Maintained owner for this generated record: `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`
- Merge status: generated from one source recipe, `jcm_medium_no_1264`

## Validation

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/jcm_medium_no_1264.yaml` | Passed: no issues found. |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/jcm_medium_no_1264.yaml --out /private/tmp/jcm_medium_no_1264.strict.tsv --workers 1 --quiet` | Passed with 0 error rows. |
| Reference validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/jcm_medium_no_1264.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed with 0 checks. |
| Term validator | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/jcm_medium_no_1264.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the `eutils` startup warning was non-fatal. |
| Embedded curation history | `just validate-history` | Not checked: the documented history validator checks standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays in merged generated YAML. |

The validators did not detect the source-vs-generated differences in solvent, sodium-lactate unit/form, supplier qualifiers, or exact hydrate grounding.

## Identity and Grounding

The record identifies MediaDive `J1264` for JCM Medium 1264. MediaDive, live JCM `GRMD=1264`, and TogoMedium M1360 agree on a complex liquid pH 10.0 recipe that contains yeast extract, Bacto peptone, multiple organic-acid salts, high KCl and Na2CO3, and pH adjustment with sterilized 10% Na2CO3 solution after autoclaving.

The `COMPLEX` and `UNDEFINED` classifications are supported by the peptone and yeast-extract rows. Most inorganic salts match the source forms. Three represented ingredient identities need follow-up:

- `Sodium lactate` is represented as a dry `G_PER_L` ingredient, but JCM and MediaDive specify 0.2 ml sodium lactate solution, approximately 60%.
- `Trisodium citrate x 2 H2O` is grounded to generic `CHEBI:53258` / `sodium citrate`.
- `DL-Malic acid` preserves the source text as `preferred_term`, but its ontology grounding is the generic `CHEBI:6650` / `malic acid`; a curator should verify whether this is the intended racemate-level term.

## Evidence

Inspected evidence:

- The live JCM `GRMD=1264` page lists 0.05 g yeast extract (BD-Difco), 0.2 g sodium formate, 0.2 g sodium acetate, 0.25 g Bacto peptone (BD-Difco), 0.2 ml sodium lactate solution (approx. 60%), 0.2 g sodium pyruvate, 0.2 g disodium succinate x 6 H2O, 0.2 g DL-malic acid, 0.2 g fumaric acid, 0.2 g trisodium citrate x 2 H2O, 5.4 g KCl, 0.3 g K2HPO4, 0.29 g CaCl2 x 2 H2O, 0.27 g NaNO3, 0.1 g MgCl2 x 6 H2O, 9 g Na2CO3, and 1 L distilled water.
- JCM also instructs the curator to adjust pH to 10.0 with sterilized 10% Na2CO3 solution after autoclaving.
- MediaDive REST `medium/J1264` reports the same 16 non-water rows, 1000 ml distilled water, pH 10.0, source `JCM`, source link `GRMD=1264`, and the same post-autoclave pH-adjustment step.
- TogoMedium M1360 points to `JCM_M1264` and `GRMD=1264` and carries the same source formulation, including 1 L water and a 0.2 ml sodium lactate solution row.

Supported:

- The MediaDive ID, original name, JCM link, category, complex/liquid classification, pH value, 15 of the 16 non-water component amounts, and post-autoclave pH step are supported.
- The source gives no fixed volume of 10% Na2CO3 solution for pH adjustment, so a quantitative Na2CO3 stock row is not required for that adjustment reagent.

Unsupported or incomplete:

- The source formula includes 1 L or 1000 ml distilled water; the generated record has no water row.
- The generated record converts 0.2 ml of approximately 60% sodium lactate solution to `0.2 G_PER_L` sodium lactate.
- The generated record drops the `BD-Difco` supplier qualifier from both complex rows.
- The generated record grounds trisodium citrate dihydrate to generic sodium citrate.
- The generated record's `NaNO3` row still contains a legacy `mediaingredientmech_term`.

## Completeness

Consequential gaps:

- Missing distilled water makes the generated formula incomplete relative to JCM and MediaDive.
- Sodium lactate solution needs either a solution-style representation or an explicit unresolved liquid row; the current dry `G_PER_L` row is dimensionally wrong.

Empty slots that are acceptable:

- No target-organism or growth-metric claim is represented.
- The post-autoclave 10% Na2CO3 amount is variable by pH adjustment, not a fixed source quantity.

Bounded negative search:

- A gitignore-independent `rg --no-ignore --hidden` search of `data/normalized_yaml` and `data/merge_yaml/merged` for exact `GRMD=1264`, exact `JCM_M1264`, and exact `jcm_medium_no_1264` IDs found only the direct MediaDive/JCM record and the TOGO M1360 wrapper. No other normalized or merged copy of this JCM 1264 source was found in those searched YAML trees.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated direct MediaDive/JCM record omits the recipe solvent. | JCM lists 1 L distilled water, and MediaDive order 17 lists 1000 ml `Distilled water`; `data/merge_yaml/merged/jcm_medium_no_1264.yaml` stops after Na2CO3. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`, followed by merge regeneration. |
| Major | Sodium lactate solution is represented with the wrong form and unit. | JCM and MediaDive specify `Sodium lactate solution (approx. 60%)`, 0.2 ml; the generated direct record says `Sodium lactate`, 0.2 `G_PER_L`. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`. |
| Major | The same JCM source is represented twice and has not merged. | Exact ignored-file-inclusive search found `data/normalized_yaml/bacterial/togo_medium_m1360.yaml` and `data/merge_yaml/merged/togo_medium_m1360.yaml` pointing to `JCM_M1264` and `GRMD=1264`. The TOGO wrapper keeps the water row but stores 1 L as `1 G_PER_L`. | Both normalized owners, plus `scripts/merge_recipes.py` after source repair. |
| Major | Trisodium citrate dihydrate is grounded too broadly as generic sodium citrate. | JCM, MediaDive, and TOGO all identify a dihydrate citrate row, but the generated row uses `CHEBI:53258` labeled `sodium citrate`. | CHEBI enrichment or curation in `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`. |
| Minor | The `BD-Difco` supplier qualifiers are missing from Bacto peptone and yeast extract. | MediaDive keeps `attribute: "BD-Difco"` for both undefined rows, and JCM includes the qualifiers directly in both component names. | MediaDive import/normalization for `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`. |
| Minor | The `NaNO3` row still has a legacy MIM identifier. | The row contains `mediaingredientmech_term: MediaIngredientMech:000171`; other migrated rows use `mediaingredientmech_chebi_term`, and the record's migration event says legacy MIM links were refreshed to CHEBI keying. | MIM migration cleanup for `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`. |

## Recommended Edits

1. Add or import the 1000 ml distilled-water row in `data/normalized_yaml/bacterial/jcm_medium_no_1264.yaml`.
2. Restore sodium lactate as `Sodium lactate solution (approx. 60%)`, 0.2 ml, without converting that liquid volume to 0.2 `G_PER_L` sodium lactate.
3. Replace the generic sodium-citrate grounding with a verified hydrate-specific grounding for `Trisodium citrate x 2 H2O`, or leave the exact source form unresolved if no verified term is available.
4. Repair `data/normalized_yaml/bacterial/togo_medium_m1360.yaml` so its 1 L water row is not represented as `1 G_PER_L`.
5. Preserve `BD-Difco` qualifiers on yeast extract and Bacto peptone.
6. Replace the remaining `mediaingredientmech_term` on `NaNO3` with a CHEBI-keyed `mediaingredientmech_chebi_term` for `CHEBI:63005`.
7. Regenerate `data/merge_yaml/merged/` and verify whether `jcm_medium_no_1264` and `togo_medium_m1360` collapse to one canonical generated recipe.

## Follow-up Checks

- Re-run open-schema, strict, reference, and term validators on the repaired normalized records and the regenerated merged record.
- Compare the regenerated direct record against MediaDive `medium/J1264` recipe orders 1-17 and the pH-adjustment step.
- Manually inspect the sodium-lactate row to confirm it remains a 0.2 ml approximate 60% solution addition.
- Search with `rg --no-ignore --hidden 'GRMD=1264([^0-9]|$)|JCM_M1264([^0-9]|$)' data/normalized_yaml data/merge_yaml/merged -g '*.yaml'` to verify duplicate-source handling with ignored files included.

## Additional Notes

- The TOGO M1360 normalized input has two migrated empty solution stubs: one for the sodium lactate solution and one for the variable 10% Na2CO3 pH-adjustment solution. The sodium lactate row is a fixed ingredient and should stay in the final recipe as a 0.2 ml liquid addition; the 10% Na2CO3 reagent is only specified procedurally.
