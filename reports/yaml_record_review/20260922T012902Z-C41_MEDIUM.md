# YAML Record Review: C41 Medium

- Repository: CultureMech
- Record: data/merge_yaml/merged/C41_MEDIUM.yaml
- Started UTC: 2026-09-22T01:29:02Z
- Finished UTC: 2026-09-22T01:29:12Z
- Verdict: needs curation

## Target

- Generated record: `CultureMech:010426`, `c41_medium`, `C41 Medium`.
- Merge lineage: one source recipe on fingerprint `ad65ebe72b0a37064b65eff5955dee732d6221e07b6ea1f994c08054090d5152`.
- Authoritative owner: `data/normalized_yaml/bacterial/TOGO_M998_C41_Medium.yaml`.
- Claimed source identity: TOGO Medium `M998` over JCM medium `951`.

## Validation

- Open LinkML schema validation passed for the generated merge.
- Strict CultureMech validation passed for the generated merge.
- LinkML reference validation passed for the generated merge.
- LinkML term validation passed for the generated merge.
- Embedded `curation_history` was not separately validated because `just validate-history` checks standalone `history/` files, not `MediaRecipe.curation_history` entries embedded in a merged record.

## Identity and Grounding

- The TOGO identity is internally consistent: TOGO `M998` is `C41 Medium` and lists original accession `JCM_M951`.
- The live JCM page for medium 951 currently returns `Nothing found`, so the public TOGO API was the usable source for ingredient and pH comparison.
- Water, calcium chloride dihydrate, `KH2PO4`, `K2HPO4`, magnesium chloride hexahydrate, iron sulfate heptahydrate, urea, and L-cysteine hydrochloride monohydrate are grounded to source-compatible chemical forms.
- Yeast extract and tryptone are intentionally ungrounded undefined components; their BD-Difco brand text is retained in `preferred_term`.
- The SL-10 trace-element solution cross-reference to Medium `M433` is not resolved to a real CultureMech stock solution.

## Evidence

- TOGO `M998` specifies 1 L distilled water, 5 mg `CaCl2.2H2O`, 1 g `KH2PO4`, 0.5 g `K2HPO4`, 1 mg resazurin, 0.5 g `MgCl2.6H2O`, 1.25 mg `FeSO4.7H2O`, 2 g yeast extract, 2 g tryptone, 1 g urea, 0.6 g `L-Cysteine.HCl.H2O`, and 1 ml Trace element solution SL-10 referencing Medium `M433`.
- TOGO `M998` has pH `7.0` metadata and an explicit instruction to adjust pH to 7.0.

## Completeness

- The generated record keeps every source row, but it loses the source units for all milligram and milliliter rows.
- The pH 7.0 condition and the pH-adjustment instruction are absent.
- The record has no structured TOGO or JCM `references` entry even though its source metadata are known.

## Findings

- Distilled water was imported as `1 G_PER_L`, not 1 L.
- `CaCl2.2H2O` was imported as `5 G_PER_L`, not 5 mg/L.
- Resazurin was imported as `1 G_PER_L`, not 1 mg/L.
- `FeSO4.7H2O` was imported as `1.25 G_PER_L`, not 1.25 mg/L.
- Trace element solution SL-10 was converted from a 1 ml addition with a Medium `M433` cross-reference to an empty `Unknown solution` row with `1 G_PER_L`.
- The generated record omits the pH 7.0 target and the corresponding adjustment instruction.

## Recommended Edits

- Curate `data/normalized_yaml/bacterial/TOGO_M998_C41_Medium.yaml`, not the generated merge.
- Correct the water, calcium chloride dihydrate, resazurin, iron sulfate heptahydrate, and SL-10 amounts to preserve the TOGO units.
- Resolve Medium `M433` as the authoritative SL-10 trace-element stock recipe instead of leaving an empty migrated solution.
- Add pH 7.0 as a structured condition and add the pH-adjustment instruction.
- Add structured TOGO `M998` and JCM `951` provenance.
- Regenerate merged YAML and verify that the corrected normalized record remains a one-source merge.

## Follow-up Checks

- Re-run open schema, strict validation, reference validation, and term validation against the normalized owner.
- Re-run the import concentration plausibility checks and verify the current resazurin and FeSO4 flags are gone.
- Compare the regenerated merge against TOGO `M998`, paying specific attention to milligram rows and the 1 ml SL-10 stock addition.

## Additional Notes

- A gitignore-independent search for `CultureMech:010426`, `TOGO:M998`, `JCM_M951`, `JCM_J951`, `TOGO_M998_C41_Medium`, and the merge fingerprint found only the active owner, generated merge, current registry/index/report rows, and archived validation references to an older `JCM_J951_C41_MEDIUM.yaml` path.
