# YAML Record Review: ALB Medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ALB_Medium.yaml
- Started UTC: 2026-09-21T09:59:32Z
- Finished UTC: 2026-09-21T10:01:27Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/ALB_Medium.yaml`.

- Class: `MediaRecipe`
- ID: `CultureMech:008403`
- Label: `alb_medium`
- Original name: `ALB Medium`
- Category: `bacterial`
- Media term: `TOGO:M1830`
- Generated status: generated single-source record from `data/normalized_yaml/bacterial/alb_medium.yaml`

## Validation

All narrow record-level validators passed for the generated YAML shape:

- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ALB_Medium.yaml`
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/ALB_Medium.yaml --out /private/tmp/alb_medium.strict.tsv --workers 1 --quiet`
  - Summary: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/ALB_Medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Summary: 1 file validated, 0 total checks, all validations passed.
- PASS: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/ALB_Medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Summary: term validation passed; the command also printed the expected `eutils` / `pkg_resources` deprecation warning.
- Not checked: embedded `MediaRecipe.curation_history` semantics. The repository documents `just validate-history` for standalone records under `history/`, not as a focused one-file validator for generated embedded history.

The documented `just validate-schema`, `just validate-strict`, and `just validate-terms` entrypoints still fail before target-specific validation because this project resolves `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`; the no-project Python 3.11 invocations above exercise the same validators against the target file.

## Identity and Grounding

The generated record correctly identifies TOGO M1830 / NBRC Medium 1063 ALB Medium. Live TOGO M1830 and live NBRC Medium 1063 both list 10 g tryptone, 5 g yeast extract, 5 g NaCl, 2 g Na2CO3, 1 L distilled water, optional 15 g agar, pH 8-9, gas phase `air containing 5% CO2`, and a footnote that Na2CO3 should be sterilized separately by autoclaving and added to the other ingredients.

The authoritative normalized source was repaired on 2026-09-11 to match those source facts, but the generated target is still the older 2026-08-06 merge artifact. It has stale water units, lacks pH and gas-phase fields, models CO2 as a variable-concentration ingredient, and keeps Na2CO3 as an empty `solutions` entry named `Unknown solution`.

## Evidence

Supported:

- The target preserves the intended TOGO M1830/NBRC 1063 source identity.
- Tryptone 10 g/L, yeast extract 5 g/L, NaCl 5 g/L, and optional agar 15 g/L match the live Togo and NBRC pages.
- The 2026-09-11 normalized source now correctly grounds tryptone, yeast extract, NaCl, sodium carbonate, water, and agar.

Unsupported or stale:

- `Distilled water` is `1 G_PER_L`; the live sources say 1 L and the repaired normalized source says 1000 `ML_PER_L`.
- `Na2CO3*` is an empty `solutions` item with `name: Unknown solution` instead of a 2 g/L sodium carbonate ingredient whose source note preserves separate autoclaving.
- `CO2` is a variable ingredient; the source states that ALB should be used under air containing 5% CO2, and the repaired source stores that as `aeration`.
- `ph_range` 8-9, `incubation_atmosphere: AEROBIC`, `sterilization`, six preparation steps, two structured `references`, and curated `data_quality_flags` are all present in `data/normalized_yaml/bacterial/alb_medium.yaml` and absent from the generated target.

## Completeness

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*ALB_Medium*' -print` searched the ignored timestamped-report directory and found no pre-existing ALB Medium report.
- Exact `rg --no-ignore --hidden` searches for `TOGO:M1830\b`, `NBRC_M1063`, `alb_medium.yaml`, and `NBRCMediumDetailServlet\?NO=1063` covered tracked and ignored files. They found only the TOGO M1830 normalized source, its generated target, and index references.
- Live TOGO M1830 and NBRC Medium 1063 were fetched and both confirm the sodium carbonate, pH, gas-phase, and separate-autoclaving evidence now present in the normalized source.
- Togo's JSON still mis-parses the source page date `2021-08-09` into `meta.ph`, but the actual pH 8-9 is present in Togo comments and on the live NBRC page.

## Findings

### blocker: the generated ALB target is stale relative to its repaired normalized source

`data/normalized_yaml/bacterial/alb_medium.yaml` was repaired on 2026-09-11, but `data/merge_yaml/merged/ALB_Medium.yaml` is still the 2026-08-06 generated record. The generated copy drops the repaired pH, 5% CO2 atmosphere, separate Na2CO3 handling, distilled-water volume semantics, references, and preparation steps.

### major: Na2CO3 is represented as an empty unknown solution

The source row is 2 g Na2CO3 with a footnote to sterilize it separately by autoclaving. The generated record moved it to `solutions` with `composition: []` and `name: Unknown solution`, which leaves the sodium carbonate identity unusable.

### major: the 5% CO2 gas phase is modeled as an ingredient

NBRC lists `Gas phase: air containing 5% CO2`. The target stores `CO2` as a variable-concentration ingredient, losing both the air balance and the fact that this is incubation atmosphere rather than a recipe component.

### minor: generated references are absent

The normalized source has structured TOGO and NBRC references, but the generated target has no `references`, so the reference validator performed zero checks.

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/ALB_Medium.yaml` from the current `data/normalized_yaml/bacterial/alb_medium.yaml`.
2. Confirm regeneration carries forward 1000 `ML_PER_L` distilled water, 2 g/L sodium carbonate, `ph_range: 8-9`, `aeration: air containing 5% CO2`, `incubation_atmosphere: AEROBIC`, preparation steps, sterilization notes, structured references, and data-quality flags.
3. If regenerated output still drops those fields, fix the merge generator so a single repaired normalized source is copied through faithfully.

## Follow-up Checks

- Rerun the four one-file validators above against regenerated `data/merge_yaml/merged/ALB_Medium.yaml`.
- Fetch live TOGO M1830 and NBRC Medium 1063 again and compare the regenerated target against tryptone 10 g/L, yeast extract 5 g/L, NaCl 5 g/L, Na2CO3 2 g/L, water 1 L, optional agar 15 g/L, pH 8-9, 5% CO2, and the sodium-carbonate autoclaving footnote.
- Search with `rg --no-ignore --hidden 'Unknown solution|\\bCO2\\b|air containing 5% CO2|Na2CO3|ML_PER_L|NBRCMediumDetailServlet\\?NO=1063' data/merge_yaml/merged/ALB_Medium.yaml` and confirm the stale solution, gas, water, and reference representations are gone.

## Additional Notes

- Optional organism and growth metric data are absent from the available TOGO M1830/NBRC 1063 evidence and were not treated as defects.
