# YAML Record Review: EG MEDIUM

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/eg_medium__dd848cc7.yaml
- Started UTC: 2026-09-22T23:27:00Z
- Finished UTC: 2026-09-22T23:31:51Z
- Verdict: pass with minor issues

## Target

- Reviewed record: `data/merge_yaml/merged/eg_medium__dd848cc7.yaml`
- Class: `MediaRecipe`
- ID: `CultureMech:002508`
- Name: `eg_medium`
- Original label: `EG MEDIUM`
- Category: `bacterial`
- Medium term: `mediadive.medium:J14` / `JCM Medium J14`
- Generated status: generated merge record with fingerprint `dd848cc732a5ba8547b663a1fb2f9bdda21ee86a0edde1336da6e5a259d6bfaf`
- Maintained owner for future edits: `data/normalized_yaml/bacterial/eg_medium.yaml`

The merge record adds only the `merge_recipes.py` history entry, `merge_fingerprint`, and `merged_from` metadata on top of the maintained normalized record.

## Validation

Focused validation was clean.

| Check | Command | Result |
|---|---|---|
| LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/eg_medium__dd848cc7.yaml` | Passed; `No issues found`. |
| Strict schema wrapper | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/eg_medium__dd848cc7.yaml --out /private/tmp/eg_medium__dd848cc7.strict.tsv --workers 1 --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/eg_medium__dd848cc7.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file validated, 0 reference checks, all validations passed. |
| Term validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/eg_medium__dd848cc7.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; the only emitted non-result text was the expected `eutils` / `pkg_resources` deprecation warning. |
| Embedded history | `just validate-history` | Not checked: the documented history validator validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` in merged YAML. |

## Identity and Grounding

The record denotes JCM Medium 14, `EG MEDIUM`. The JCM source page names record `14 EG MEDIUM` and the MediaDive REST response for `J14` returns `id: J14`, `name: EG MEDIUM`, `source: JCM`, and the same JCM `GRMD=14` link stored in the record notes.

The category, complex/undefined classifications, solid-agar state, and pH 7.7 agree with the source formulation imported through MediaDive. The structured `media_term` correctly points to `mediadive.medium:J14`.

Small grounding and form simplifications remain:

- JCM lists `Lab-Lemco powder (Oxoid)`, `Proteose peptone No. 3 (BD-Difco)`, and `Yeast extract (BD-Difco)`; the record keeps only the base ingredient names and does not preserve the vendor attributes.
- JCM lists `Soluble starch`, and MediaDive keeps `compound: Starch` plus `attribute: soluble`; the record grounds the row only to `CHEBI:28017` / `starch`.
- JCM and MediaDive state `Horse blood` as `50.0 ml`, while the record's structured concentration stores `50 G_PER_L`.

## Evidence

The core ingredient table is source-supported. JCM and MediaDive agree on this ordered formulation: 2.4 g Lab-Lemco powder, 10.0 g Proteose peptone No. 3, 5.0 g yeast extract, 4.0 g `Na2HPO4`, 1.5 g glucose, 0.5 g soluble starch, 0.2 g L-cystine, 15.0 g agar, 0.5 g `L-Cysteine.HCl.H2O`, 50.0 ml horse blood, and 950.0 ml distilled water for a 1 L recipe.

The record correctly avoids treating the 950 ml distilled water line as a nutrient ingredient and retains it in preparation context with "bring volume to 950 ml".

The single preparation step preserves the source operations in order: dissolve L-cystine in 50 ml of 1 N HCl, add the remaining components, bring to 950 ml, adjust pH to 7.6-7.8, autoclave, cool to 50C, aseptically add 50.0 ml horse blood, mix, and pour or distribute.

No DOI, PMID, strain-specific growth assertion, stock solution reference, taxon claim, or internal evidence snippet is present in the reviewed record.

## Completeness

The record is complete enough to identify and reconstruct JCM Medium 14. The medium has no nested stock solutions, no cited organism-growth claims, and no curated target organism list; those empty optional areas are not defects for this imported MediaDive/JCM recipe.

No bounded negative absence search was needed for this report. The JCM page and MediaDive response were sufficient to support the medium identity, ingredient table, pH, and preparation sequence already present in the reviewed record.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| minor | Liquid horse blood is stored with a mass-concentration unit. | JCM and MediaDive both encode the row as `50.0 ml` horse blood in a 1 L recipe; `data/merge_yaml/merged/eg_medium__dd848cc7.yaml` stores `value: '50'`, `unit: G_PER_L`. | `data/normalized_yaml/bacterial/eg_medium.yaml` |
| minor | Source attributes for four ingredients were simplified away. | The authoritative JCM row text distinguishes `Lab-Lemco powder (Oxoid)`, `Proteose peptone No. 3 (BD-Difco)`, `Yeast extract (BD-Difco)`, and `Soluble starch`; the normalized record stores `Lab-Lemco beef extract`, `Proteose peptone no. 3`, `Yeast extract`, and `Starch`. | `data/normalized_yaml/bacterial/eg_medium.yaml` |

## Recommended Edits

1. In `data/normalized_yaml/bacterial/eg_medium.yaml`, change the `Horse blood` concentration to a volume-preserving representation, preferably `50 ML_PER_L` or 5 `PERCENT_V_V`, instead of `50 G_PER_L`.
2. In `data/normalized_yaml/bacterial/eg_medium.yaml`, preserve source-specific form and vendor qualifiers from JCM/MediaDive for Lab-Lemco, Proteose peptone no. 3, yeast extract, and soluble starch. Use ingredient notes, `source`, or another maintained field rather than widening the ontology grounding for these complex products.
3. Regenerate the merged YAML and downstream pages so `data/merge_yaml/merged/eg_medium__dd848cc7.yaml` reflects the maintained edit.

## Follow-up Checks

1. Rerun LinkML schema, strict schema, reference, and term validation on the regenerated `data/merge_yaml/merged/eg_medium__dd848cc7.yaml`.
2. Reopen JCM `GRMD=14` and MediaDive `J14` and manually verify that horse blood remains expressed as a volume addition and that the four source attributes remain recoverable.
3. Confirm the regenerated record still keeps the post-autoclave horse-blood addition in preparation text and does not create a distilled-water nutrient row.

## Additional Notes

- This review used the JCM `GRMD=14` page and the MediaDive `rest/medium/J14` response as inspected authoritative sources.
- The record is distinct from the nearby CCAP EG and EG:JM records and from TOGO records that copy a JCM EG formulation under separate accessions.
