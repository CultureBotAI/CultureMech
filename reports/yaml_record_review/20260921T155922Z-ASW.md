# YAML Record Review: ASW

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/ASW.yaml
- Started UTC: 2026-09-21T15:57:33Z
- Finished UTC: 2026-09-21T15:59:22Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/ASW.yaml`.

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| CultureMech ID | `CultureMech:000050` |
| Label | `ASW` |
| Normalized owner | `data/normalized_yaml/algae/asw.yaml` |
| Source | CCAP `ASW`, `https://www.ccap.ac.uk/wp-content/uploads/MR_ASW.pdf` |
| Generated status | Generated one-input merge of `asw`; future fixes belong in the normalized algae owner or the CCAP importer/repair rules and should be propagated by regenerating merges/pages. |

## Validation

| Check | Result |
|---|---|
| Open-schema LinkML validation | Passed: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/ASW.yaml` exited 0 and emitted no issues. |
| Strict validation | Passed: the no-project Python 3.11 invocation of `scripts/validate_strict.py data/merge_yaml/merged/ASW.yaml --out /private/tmp/ASW.strict.tsv --workers 1 --quiet` scanned 1 file and wrote 0 error rows. |
| Reference validation | Passed: `linkml-reference-validator validate data data/merge_yaml/merged/ASW.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` validated 1 file, ran 0 snippet checks, and reported all validations passed. |
| Term validation | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/ASW.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` reported validation passed after the known `eutils/pkg_resources` warning. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for standalone records under `history/`, not a focused embedded-`MediaRecipe.curation_history` validator for one generated merge record. |

The documented `just` entry points remain blocked by the project-level Python
3.13 `llvmlite==0.46.0` build failure, so the focused no-project Python 3.11
validator invocations above were used for this one record.

## Identity and Grounding

`CultureMech:000050` is registered to
`data/normalized_yaml/algae/asw.yaml`, and the generated target preserves that
record's CCAP ASW identity. The basename is ambiguous across the repository:
`data/normalized_yaml/bacterial/asw.yaml` is a separate MediaDive/CCAP `C4`
import with `CultureMech:000360`, while this generated target is the algae
CCAP import.

The maintained algae owner is newer than the generated merge. The target was
generated on 2026-08-06 and still has a `Make up to` pseudo-ingredient; the
normalized owner has 2026-08-25 and 2026-09-13 curation events that move the
make-up, pH, and autoclaving text to `preparation_steps` and add a structured
`sources` entry.

The scientific recipe itself still needs curation. The inspected CCAP PDF
defines ASW as a final medium made from Tricine, 7.5 ml stock solution 1,
2.5 ml vitamin solution, and 25 ml Soil Extract 1 per litre. The generated
record instead expands stock-solution ingredients directly into final
`G_PER_L` concentrations and omits the final 25 ml/L Soil Extract 1 addition.

## Evidence

The CCAP `MR_ASW.pdf` text was downloaded and extracted with `pypdf`. It
supports the record identity, the three stock recipes, the final medium recipe,
and the pH/autoclave instructions, but not the final concentrations asserted
for the stock components.

Supported claims:

- The CCAP source is `ASW (Artificial Seawater)`.
- Stock solution 1 contains NaNO3 15.00 g/L, Na2HPO4 0.60 g/L, and K2HPO4
  0.50 g/L.
- The vitamin solution contains biotin 0.0002 g/L, calcium pantothenate
  0.02 g/L, cyanocobalamin 0.004 g/L, folic acid 0.0004 g/L, inositol
  1.0 g/L, nicotinic acid 0.02 g/L, thiamine HCl 0.1 g/L, and thymine
  0.6 g/L.
- The final medium contains Tricine 0.50 g/L, stock solution 1 7.50 ml/L,
  stock solution 2 2.50 ml/L, and Stock solution 3 / SE1 25.00 ml/L, then is
  made up to 1 litre with filtered natural seawater or with deionized water
  plus Instant Ocean salts.
- The CCAP instructions say to adjust pH to 7.6-7.8 with 1 M NaOH or 1 M HCl
  before autoclaving at 15 psi for 15 minutes.

Unsupported or over-scoped assertions:

| Record assertion | Problem |
|---|---|
| Final-medium rows for 15.00 g/L NaNO, 0.60 g/L Na2HPO4, 0.50 g/L K2HPO4, and eight vitamin compounds | CCAP lists these as stock-solution concentrations; the final medium uses 7.50 ml/L stock 1 and 2.50 ml/L stock 2. |
| No Soil Extract 1 final-medium component | CCAP lists Stock solution 3 (SE1) at 25.00 ml/L in the final medium. |
| `NaNO` grounded to CHEBI `Azimilide` | The CCAP row is `NaNO3`, not `NaNO`, and denotes sodium nitrate. |
| `Make up to` as a `1 L` ingredient | The PDF line is a preparation/final-volume instruction, not an ingredient. |
| Generic light, temperature, and salinity prose | The inspected ASW PDF does not state the 50-100 umol photons m-2 s-1 light intensity, 12:12 or 16:8 light:dark cycles, or 15-30 C temperature range asserted by the record. |

## Completeness

- The generated merge is missing the maintained owner's 2026-08-25 preparation
  steps for filtered natural seawater, pH adjustment, and autoclaving.
- The maintained owner has also not finished the CCAP stock/solution modeling:
  it still carries the extra-salts and vitamin stock components at stock
  concentrations and does not reference SE1 as a 25 ml/L solution.
- `find data/normalized_yaml -maxdepth 2 -type f -name 'asw.yaml'` found both
  `data/normalized_yaml/algae/asw.yaml` and `data/normalized_yaml/bacterial/asw.yaml`.
- `find reports/yaml_record_review -maxdepth 1 -name '????????T??????Z-ASW.md'`
  found no prior report for this generated record.
- A gitignore-independent exact search for
  `CultureMech:000050|CCAP:ASW|MR_ASW|wp-content/uploads/MR_ASW|name: asw|original_name: ASW`
  under `data`, `scripts`, `history`, `src`, `reports`, and
  `references_cache` found the algae owner, the generated target, the
  bacterial CCAP/MediaDive owner with the same basename, CCAP ASW salinity
  variants, generated ASW variant pages, the August CCAP instruction-row repair
  script, source indexes, and archived validation output.

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Major | The recipe flattens per-litre stock recipes into final-medium `G_PER_L` ingredients. | CCAP's final medium uses 7.50 ml/L stock solution 1 and 2.50 ml/L vitamin solution, but the YAML copies all eleven stock ingredients at their stock concentrations. | `data/normalized_yaml/algae/asw.yaml`, or the CCAP importer/repair logic for stock recipes. |
| Major | Soil Extract 1 is missing from the final medium. | CCAP lists `Stock solution 3 (SE1)` at 25.00 ml/L in the final medium; the generated record has no SE1 ingredient or solution reference. | `data/normalized_yaml/algae/asw.yaml` |
| Major | Sodium nitrate is truncated and grounded to the wrong CHEBI term. | The source lists `NaNO3`; the generated record says `NaNO` and links CHEBI `Azimilide`. | `data/normalized_yaml/algae/asw.yaml`, or the CCAP parsing/mapping rule that dropped the `3`. |
| Major | The generated merge is stale and lacks the 2026-08-25 normalized preparation-step repair. | The generated file still emits `Make up to` as an ingredient, while the owner now stores the make-up, pH-adjustment, and autoclave instructions under `preparation_steps`. | Regenerate `data/merge_yaml/merged/ASW.yaml` from the normalized owner. |
| Minor | Generic light and temperature notes are unsupported by the inspected ASW recipe. | The CCAP PDF gives formulation, pH, and autoclaving instructions but no culture light cycle, photon flux, or incubation temperature range. | `data/normalized_yaml/algae/asw.yaml` |

## Recommended Edits

1. Re-model CCAP ASW with its final-medium Tricine row plus stock-solution
   additions for extra salts, vitamin solution, and SE1; do not copy
   stock-solution components directly into the final medium.
2. Correct `NaNO` to source `NaNO3` and ground it to sodium nitrate only inside
   stock solution 1.
3. Remove or evidence-scope the generic light-intensity, light-cycle, salinity,
   and temperature prose.
4. Regenerate `data/merge_yaml/merged/ASW.yaml` after curation so the generated
   record drops the stale `Make up to` ingredient and includes the current
   `sources` and `preparation_steps`.

## Follow-up Checks

- Re-run focused schema, strict, term, and reference validation on
  `data/normalized_yaml/algae/asw.yaml`.
- Re-run `just verify-merges` and `just audit-merge-freshness` after
  regenerating `data/merge_yaml/merged/ASW.yaml`.
- Re-fetch `MR_ASW.pdf` and compare final ASW, extra-salts stock, vitamin
  solution, and SE1 solution references against the curated YAML.
- Re-run an exact gitignore-independent search for `CultureMech:000050` and
  `MR_ASW.pdf` to confirm no stale generated or legacy placeholder ASW copy
  remains.

## Additional Notes

- The exact prior-report search used a timestamp-shaped filename pattern for
  `ASW.md` because a broad `*ASW.md` also matches the unrelated `2ASW` report.
