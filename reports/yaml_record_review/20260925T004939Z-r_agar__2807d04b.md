# YAML Record Review: r_agar__2807d04b

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_agar__2807d04b.yaml
- Started UTC: 2026-09-25T00:48:25Z
- Finished UTC: 2026-09-25T00:49:39Z
- Verdict: needs curation

## Target

| Field | Observed |
|---|---|
| ID | CultureMech:008585 |
| Name | r_agar |
| Class | MediaRecipe |
| Category | bacterial |
| Source identity | TOGO Medium M19 / JCM_M26, R Agar |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M19_R_Agar.yaml |
| Generated path | data/merge_yaml/merged/r_agar__2807d04b.yaml |

The reviewed file is a generated singleton derived from TOGO M19. Future fixes belong in the repaired normalized owner and then in regenerated `data/merge_yaml/merged/` output.

## Validation

| Check | Result |
|---|---|
| Open schema | Passed: `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_agar__2807d04b.yaml` exited 0 and printed `No issues found`. |
| Strict schema | Passed: `scripts/validate_strict.py data/merge_yaml/merged/r_agar__2807d04b.yaml --out /private/tmp/r_agar_2807d04b.strict.tsv --workers 1 --quiet` exited 0; the TSV had only its header line. |
| References | Passed structurally: `linkml-reference-validator validate data data/merge_yaml/merged/r_agar__2807d04b.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 with 0 total checks. |
| Terms | Passed: `linkml-term-validator validate-data data/merge_yaml/merged/r_agar__2807d04b.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 after the harmless `eutils` / `pkg_resources` warning. |
| Embedded curation history | Not checked: the documented `just validate-history` target validates standalone `history/*.yaml`, not `MediaRecipe.curation_history` embedded in generated recipe YAML. |

## Identity and Grounding

- The generated record correctly denotes TOGO M19, imported from JCM_M26, R Agar.
- The source itself supports R Agar as a 1 L, pH 7.2 solid R medium with Bacto peptone, yeast extract, malt extract, casamino acids, beef extract, glycerol, 50 mg Tween 80, 1 g MgSO4 x 7 H2O, 15 g agar, and 1 L distilled water.
- The generated record is stale relative to the maintained owner: it still stores 50 mg Tween 80 as 50 g/L and 1 L distilled water as 1 g/L.

## Evidence

Supported source-backed claims:

- The live JCM GRMD 26 page and TOGO M19 both support the R Agar source identity, pH 7.2, 1 L water volume, nutrient and agar amounts, 1 g MgSO4 x 7 H2O, and 50 mg Tween 80.
- The maintained normalized owner already reflects the corrected unit conversions: Tween 80 at 0.05 g/L, distilled water at 1000 ml/L, and an autoclave step at 121 C for 15 min.

Unsupported or incomplete claims:

- `Tween 80` at 50 g/L is unsupported. TOGO and JCM list 50 mg, which is 0.05 g/L in a one-liter recipe.
- `Distilled water` at 1 g/L is dimensionally wrong. The source lists 1 L, and the maintained owner stores it as 1000 ml/L.
- The generated record lacks the pH 7.2 value and the default JCM autoclaving condition that were added to the maintained owner.

## Completeness

- The review checked the generated YAML, the maintained TOGO_M19 owner, the TOGO M19 API response, and the live JCM GRMD 26 page.
- A gitignore-independent exact search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `TOGO_M19_R_Agar`, anchored `TOGO:M19` source rows, and generated `TOGO_M19_R_Agar.yaml` provenance found the active maintained owner, the generated singleton, source indexes, historical validation archives, and existing variant-link proposal rows.
- Empty `target_organisms` is not a defect because the inspected JCM/TOGO source only defines the medium formulation.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | The generated Tween 80 concentration is 1000-fold too high. | The source lists 50 mg per liter; the generated record says 50 g/L, while the maintained owner already says 0.05 g/L. | Regenerated data/merge_yaml/merged/r_agar__2807d04b.yaml from data/normalized_yaml/bacterial/TOGO_M19_R_Agar.yaml |
| Major | The generated water entry has the wrong unit and magnitude. | The source lists 1 L distilled water; the generated record says 1 g/L, while the maintained owner already says 1000 ml/L. | Regenerated data/merge_yaml/merged/r_agar__2807d04b.yaml from data/normalized_yaml/bacterial/TOGO_M19_R_Agar.yaml |
| Minor | Generated conditions are stale relative to the repaired owner. | JCM states final pH 7.2 and gives the default autoclave condition; the maintained owner has `ph_value: 7.2` plus an autoclave step, but the generated record lacks both. | Merge regeneration workflow |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/r_agar__2807d04b.yaml` from the current normalized TOGO M19 owner.
2. Confirm the regenerated output has Tween 80 at 0.05 g/L, distilled water at 1000 ml/L, pH 7.2, and the 121 C for 15 min autoclave step.
3. Keep the repaired FOODON/MICRO groundings and role annotations from the normalized owner when regenerating; do not reintroduce the earlier ungrounded extract and peptone rows.

## Follow-up Checks

- Run focused schema, strict, term, and reference validation on the regenerated R Agar output.
- Run `just verify-merges` and `just audit-merge-freshness` after regenerating the merge layer.
- Inspect the regenerated YAML for any remaining `Tween 80` 50 g/L or `Distilled water` 1 g/L rows.
- Re-run a gitignore-independent exact search for `TOGO_M19_R_Agar` and generated `TOGO_M19_R_Agar.yaml` provenance to confirm only the regenerated singleton represents TOGO M19 directly.

## Additional Notes

- An initial broad TOGO search was intentionally stopped after it matched unrelated `TOGO:M19xx` source IDs; the final search narrowed to the `TOGO_M19_R_Agar` filename and anchored M19 provenance.
- This review did not modify generated YAML or the maintained TOGO M19 record.
