# YAML Record Review: 10_mh_medium

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/10_mh_medium.yaml`
- Started UTC: 20260921T042600Z
- Finished UTC: 20260921T042700Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:001720` |
| Label | `10_mh_medium` |
| Category | `bacterial` |
| Generated or maintained | Generated merge artifact under `data/merge_yaml/merged/`; future edits belong in normalized DSMZ/KOMODO inputs or merge rules |
| Merge owners | `merged_from: [10_mh_medium, KOMODO_593_10_MH_medium]` |
| Maintained owners | `data/normalized_yaml/bacterial/10_mh_medium.yaml`; `data/normalized_yaml/bacterial/KOMODO_593_10_MH_medium.yaml` |

This merge combines MediaDive/DSMZ Medium 593 with the KOMODO record that declares the same DSMZ Medium 593 provenance. The generated file is fresh relative to its normalized inputs; the main issue is that both inputs already omit one MediaDive source row.

## Validation

Repository `just` entrypoints were blocked before target-specific validation because the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13 and fails in setuptools with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`.

| Check | Command | Result |
|---|---|---|
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/10_mh_medium.yaml` | Pass |
| Closed schema / strict | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/10_mh_medium.yaml --out /private/tmp/culturemech-review-10-mh.strict.tsv --workers 1 --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, TSV at `/private/tmp/culturemech-review-10-mh.strict.tsv` |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/10_mh_medium.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass; 1 file validated, 0 reference checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/10_mh_medium.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not checked | No focused embedded `MediaRecipe.curation_history` validator is documented for one merge record; `just validate-history` targets standalone files under `history/` |

## Identity and Grounding

The merged identity is coherent:

- MediaDive medium `593` resolves to `10% MH MEDIUM`, source `DSMZ`, pH 7.5, and the DSMZ Medium 593 PDF link.
- The KOMODO child record also points to DSMZ Medium 593 and has the same ingredient/concentration signature as the DSMZ parent.
- `data/merge_yaml/merged/10_mh_medium.yaml` preserves the DSMZ `CultureMech:001720` identifier as canonical and lists the KOMODO record as a `SOURCE_DUPLICATE` child.

Grounding is partial but not identity-breaking:

- The simple salts, glucose, and agar are grounded to CHEBI/MIM terms.
- `Proteose peptone no. 3` and `Yeast extract` remain ungrounded and omit MediaDive's `Difco` attributes.

## Evidence

Supported by inspected source text:

- MediaDive Medium 593 lists pH 7.5.
- MediaDive Medium 593 lists NaCl 81 g/L, MgCl2 7 g/L, MgSO4 9.6 g/L, CaCl2 0.36 g/L, KCl 2 g/L, NaHCO3 0.06 g/L, NaBr 0.026 g/L, Proteose peptone no. 3 5 g/L, Yeast extract 10 g/L, Glucose 1 g/L, and Agar 15 g/L for solid media.
- MediaDive Medium 593 says NaHCO3 is added from a sterilized stock solution once the medium has cooled, and agar is added at 15.0 g/L for solid media.

Unsupported or incomplete:

- MediaDive also lists `Distilled water` 1000 ml in `Main sol. 593`; the generated record omits it.
- MediaDive marks both `Proteose peptone no. 3` and `Yeast extract` with the `Difco` attribute; the generated record keeps only the generic ingredient names.

## Completeness

- A gitignore-independent `rg --no-ignore --hidden` search over `data/merge_yaml/merged` and `data/normalized_yaml` for `10_mh_medium`, `10% MH medium`, `M2069`, `NBRC_M1424`, and `NO=1424` found this DSMZ/KOMODO merge, both normalized DSMZ/KOMODO inputs, and an unrelated TOGO M2069/NBRC 1424 `my50g` record. The exact search did not find another generated 10% MH Medium artifact.
- The formula is nearly complete, but the 1 L water row is consequential because it establishes the volume basis for every gram-per-liter component.
- Empty optional organism and growth-evidence slots are acceptable for this record; MediaDive Medium 593 supports a recipe, not a target-organism growth claim.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The DSMZ water row is missing from both source records and the generated merge. | Live MediaDive Medium 593 has `Distilled water`, amount 1000, unit `ml`, optional 0 in `Main sol. 593`; neither normalized owner nor the generated merge has a water ingredient. | `data/normalized_yaml/bacterial/10_mh_medium.yaml`, `data/normalized_yaml/bacterial/KOMODO_593_10_MH_medium.yaml`, or the DSMZ-to-KOMODO copy rule that keeps them aligned. |
| Minor | The source vendor attributes for two complex ingredients are dropped. | MediaDive attaches `attribute: Difco` to `Proteose peptone no. 3` and `Yeast extract`; the generated record has no notes or labels preserving that supplied form. | The MediaDive importer or both normalized DSMZ/KOMODO records. |

## Recommended Edits

1. Add the MediaDive 1000 ml distilled-water row to the DSMZ parent and KOMODO duplicate, preserving DSMZ Medium 593 provenance and keeping the child duplicate relation valid.
2. Preserve the `Difco` attributes for `Proteose peptone no. 3` and `Yeast extract`, either in the preferred terms or source notes, so the record does not flatten supplier-specific source text into a generic peptone/yeast-extract claim.
3. Regenerate `data/merge_yaml/merged/10_mh_medium.yaml` and verify that it still merges exactly the DSMZ and KOMODO records on one fingerprint.

## Follow-up Checks

- `just validate-strict data/normalized_yaml/bacterial/10_mh_medium.yaml data/normalized_yaml/bacterial/KOMODO_593_10_MH_medium.yaml` after the normalized repairs.
- `just verify-merges` to prove the source-duplicate merge still resolves to the same canonical record after the water row is added.
- `just validate-terms data/merge_yaml/merged/10_mh_medium.yaml` after regenerating, because any new water grounding or vendor-note representation should remain schema- and label-valid.
- Manual MediaDive Medium 593 comparison to confirm all twelve source rows, pH 7.5, and the NaHCO3 cooled-stock instruction are still represented.

## Additional Notes

- `linkml-reference-validator` performed zero checks because this generated record has no `references` block.
