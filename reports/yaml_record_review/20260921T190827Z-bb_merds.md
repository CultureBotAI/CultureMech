# YAML Record Review: bb_merds

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/bb_merds.yaml
- Started UTC: 2026-09-21T19:07:02Z
- Finished UTC: 2026-09-21T19:08:27Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path reviewed | `data/merge_yaml/merged/bb_merds.yaml` |
| Maintained owner | `data/normalized_yaml/algae/bb_merds.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:000054` |
| Name | `bb_merds` |
| Original name | `BB:MErds` |
| Source | CCAP `BB_MErds`, `MR_BB_MErds.pdf` |
| Merge status | Generated one-source merge from `bb_merds` |

## Validation

| Check | Result |
|---|---|
| Open schema | Passed with the no-project LinkML invocation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`, and the generated BB:MErds merge. |
| Strict schema | Passed with the no-project invocation of `scripts/validate_strict.py`: 1 file scanned, 0 files with errors, 0 total error rows. |
| Reference validator | Passed with 0 reference checks because this CCAP import uses bare string references and has no populated evidence objects. |
| Term validator | Passed with `linkml-term-validator validate-data` against the generated BB:MErds merge. |
| Embedded curation history | Not checked: this repository documents `just validate-history` for the standalone `history/` tree, not a focused one-record `MediaRecipe.curation_history` check. |
| Project `just` wrappers | Not rerun here: direct `just validate-schema`, `just validate-strict`, and `just validate-terms` fail before target-specific validation in this checkout while the project `uv` environment attempts to build `llvmlite==0.46.0` under Python 3.13. The equivalent no-project Python 3.11 validators above exercised the target record. |

## Identity and Grounding

- `data/merge_yaml/merged/bb_merds.yaml` denotes CCAP `BB_MErds`, BB:MErds medium for freshwater/brackish-water algae, and resolves to `CultureMech:000054` from `data/normalized_yaml/algae/bb_merds.yaml`.
- There is a separate collision artifact, `data/merge_yaml/merged/bb_merds__c800a0bf.yaml`, for a different `CultureMech:000323` bacterial/MediaDive copy of the same CCAP PDF. This review is scoped only to the algae `bb_merds.yaml` merge.
- The generated merge is stale relative to `data/normalized_yaml/algae/bb_merds.yaml`: the normalized owner has an Aug 25, 2026 `repair_ccap_instruction_rows.py` event that replaced the eight bad direct ingredients with BB/MErds constituent-medium references, an explicit MIX step, and 8:2 and 1:1 variants, while the generated merge still has the bad eight-row extraction.
- Exact gitignore-independent searches for `CultureMech:000054`, `CCAP:BB_MErds`, `MR_BB_MErds.pdf`, and `bb_merds__c800a0bf` covered `data/raw`, `data/import_tracking`, `data/normalized_yaml`, and `data/merge_yaml`. They found the normalized owner, generated merge, generated indexes, the separate `bb_merds__c800a0bf.yaml` collision merge, the filename-collision report, and no local raw copy of the CCAP PDF.

## Evidence

- The cited CCAP `MR_BB_MErds.pdf` supports BB:MErds as an 8:2 or 1:1 mixture; it says to see separate recipes and then mix and autoclave the constituent media.
- The stale generated merge still carries six BB stock rows, one MErds stock row, and the SE2 instruction fragment `Soil is prepared as above.` as top-level ingredients.
- The normalized owner now models BB as `CultureMech:000053` and MErds as `CultureMech:000089`, both at `VARIABLE` concentrations, and records the two source-supported ratios as variants.
- The generated merge has none of the normalized owner's constituent-medium references, 8:2 variant, 1:1 variant, or source repair metadata.
- The PDF does not support the generic light-intensity, light-cycle, or temperature-range prose that remains in both the generated and normalized records.

## Completeness

- Consequential gaps:
  - The generated merge is stale and still exposes a known-bad direct ingredient list instead of the repaired BB/MErds constituent-medium structure.
  - `Soil is prepared as above.` is a procedure fragment from the SE2 page, not a BB:MErds ingredient.
  - The 8:2 and 1:1 BB/MErds mixture variants are missing from the generated merge.
  - The generated merge lacks the structured `sources` slot populated on the normalized owner on Sep 13, 2026.
  - Generic light, temperature, and application prose should still be reviewed on the normalized owner because it is not in `MR_BB_MErds.pdf`.
- Correctly empty optional slots:
  - `target_organisms` and `growth_metrics` are absent; the inspected CCAP PDF gives only a recipe for freshwater/brackish-water algae and no strain-specific growth assay.
- Bounded negative searches:
  - Exact gitignore-independent searches found no local raw copy of `MR_BB_MErds.pdf` under `data`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated merge is stale and still contains the pre-repair direct ingredient extraction. | The normalized owner moved from eight direct ingredients to BB/MErds constituent-medium references on 2026-08-25; the generated merge still contains six BB stock rows, `Na2HPO4`, and `Soil is prepared as above.` as direct ingredients. | Regenerate `data/merge_yaml/merged/bb_merds.yaml` from `data/normalized_yaml/algae/bb_merds.yaml`. |
| Major | The generated merge omits the source-supported BB:MErds 8:2 and 1:1 variants. | `MR_BB_MErds.pdf` says BB:MErds can be an 8:2 mixture or a 1:1 mixture; the normalized owner models those variants, while the generated merge does not. | Regenerate `data/merge_yaml/merged/bb_merds.yaml` from `data/normalized_yaml/algae/bb_merds.yaml`. |
| Major | The generated merge has a procedure sentence modeled as an ingredient. | `Soil is prepared as above.` comes from the SE2 soil-extract instructions; the normalized Aug 25 repair intentionally removed it from `ingredients`. | Regenerate the merge; broad repeats may require retaining `scripts/repair_ccap_instruction_rows.py` coverage for CCAP instruction rows. |
| Major | The generated merge lacks structured source provenance from the normalized owner. | The normalized owner populated `sources` on 2026-09-13; the generated merge was not regenerated after that source repair. | Regenerate `data/merge_yaml/merged/bb_merds.yaml`. |
| Major | Imported culture-condition prose is unsupported by the cited BB:MErds PDF. | The YAML says light intensity, light cycle, and temperature vary by species and lists typical ranges, but `MR_BB_MErds.pdf` contains no cultivation-condition ranges. | `data/normalized_yaml/algae/bb_merds.yaml`; the CCAP importer default text may need removal across CCAP records. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/bb_merds.yaml` from the repaired normalized owner so the generated record reflects BB and MErds constituent media, the MIX step, the two mixture variants, and the structured CCAP source.
2. Keep `Soil is prepared as above.` out of ingredient lists and preserve the `repair_ccap_instruction_rows.py` guard that maps this row to preparation/constituent context.
3. Remove unsupported generic light and temperature prose from `data/normalized_yaml/algae/bb_merds.yaml` or replace it with source-backed BB:MErds culture context.
4. Review the separate bacterial `CultureMech:000323` / `bb_merds__c800a0bf` duplicate for the same CCAP source identity.

## Follow-up Checks

- Rerun open-schema LinkML validation, `scripts/validate_strict.py`, `linkml-term-validator`, and the reference validator on the normalized owner and regenerated merge.
- Re-extract `MR_BB_MErds.pdf` and manually compare the BB/MErds mixture ratios, constituent references, and autoclave instruction against the regenerated YAML.
- Run `just verify-merges` plus `just audit-merge-freshness`.
- Confirm regenerated `data/merge_yaml/merged/bb_merds.yaml` no longer contains `Soil is prepared as above.`.

## Additional Notes

- The repaired normalized owner depends on `CultureMech:000053` for BB and `CultureMech:000089` for MErds; those constituent media need their own record-level reviews.
- Exact gitignore-independent searches included ignored files where present.
