# YAML Record Review: togo_medium_m1510

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/togo_medium_m1510.yaml
- Started UTC: 2026-09-25T14:13:00Z
- Finished UTC: 2026-09-25T14:17:57Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008055 |
| Label | togo_medium_m1510 |
| Generated record | data/merge_yaml/merged/togo_medium_m1510.yaml |
| Maintained owner | data/normalized_yaml/bacterial/togo_medium_m1510.yaml |
| Source | TOGO Medium M1510, NBRC M294 |

The reviewed file is a generated one-source merge of TOGO M1510. Future fixes
belong in the normalized owner or in duplicate links to the repaired NBRC 294
owner, not in the generated merge.

## Validation

| Check | Result |
| --- | --- |
| Open LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe` | Passed |
| `scripts/validate_strict.py` on the generated record | Passed; 0 errors in `/private/tmp/togo_medium_m1510.strict.tsv` |
| `linkml-reference-validator validate data` on the generated record | Passed; exited 0 with no diagnostics |
| `linkml-term-validator validate-data` on the generated record | Passed |
| Embedded `curation_history` validation | Not checked: the documented `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` objects in a merged YAML file |

The term validator emitted only the expected `eutils/pkg_resources is
deprecated` warning before passing.

## Identity and Grounding

`CultureMech:008055`, `TOGO:M1510`, the `NBRC_M294` original source note, and
the NBRC medium 294 page all identify a two-component yeast-extract medium
adjusted to pH 5.0 with 1 N HCl. The same NBRC medium is already represented
by the repaired normalized owner `data/normalized_yaml/bacterial/NBRC_296.yaml`
with `CultureMech:007485`.

## Evidence

TOGO M1510 and NBRC 294 support only 5 g Bacto Yeast Extract, 1 L distilled
water, and adjustment to pH 5.0 with 1 N HCl. The generated TOGO record keeps
the 5 g yeast extract but stores 1 L water as 1 g/L and turns the HCl
pH-adjuster into a variable final ingredient. It has no `ph_value`, no
`ADJUST_PH` step, and no structured link to the curated NBRC 294 owner.

## Completeness

The absent `target_organisms`, growth evidence, stock solutions, and variants
are not defects for this imported source record. The unreconciled duplicate
with `NBRC_296.yaml` is consequential because that owner already carries the
same source URL, a 1 L water row, `ph_value: 5.0`, and the 1 N HCl adjustment
step.

Before this report was written, an exact `find` search under the ignored
`reports/yaml_record_review` directory found no pre-existing
`*-togo_medium_m1510.md` report.

## Findings

| Severity | Finding |
| --- | --- |
| major | TOGO M1510 duplicates the repaired NBRC 294 owner but is still an unlinked generated record. Owner: `data/normalized_yaml/bacterial/togo_medium_m1510.yaml` plus duplicate links to `data/normalized_yaml/bacterial/NBRC_296.yaml`. |
| major | The HCl pH-adjuster is represented as a variable final ingredient and pH 5.0 is absent. Owner: `data/normalized_yaml/bacterial/togo_medium_m1510.yaml` or the TOGO/NBRC importer. |
| minor | Source water remains as `1 G_PER_L` rather than a 1 L final-volume row. Owner: `data/normalized_yaml/bacterial/togo_medium_m1510.yaml`. |

## Recommended Edits

1. Link M1510 and `NBRC_296.yaml` as source duplicates or consolidate the TOGO
   source onto the repaired NBRC 294 owner.
2. Replace the variable HCl ingredient with `ph_value: 5.0` and an `ADJUST_PH`
   step for 1 N HCl.
3. Correct distilled water from 1 g/L to a 1 L final-volume representation.
4. Regenerate `data/merge_yaml/merged/togo_medium_m1510.yaml`.

## Follow-up Checks

Run the focused schema, strict, reference, and term validators on the
regenerated record, then manually compare it with TOGO M1510, NBRC 294, and
`data/normalized_yaml/bacterial/NBRC_296.yaml` for duplicate status, pH 5.0,
and 1 N HCl handling.

## Additional Notes

An exact gitignore-independent search for `NBRC_M294`, `NO=294`,
`CultureMech:008055`, `TOGO:M1510`, and `NBRC_296` covered
`data/normalized_yaml/bacterial`, `data/merge_yaml/merged`, and ignored
`reports/yaml_record_review`; it found the expected TOGO M1510 owner, the
separate repaired NBRC 296 owner, and the earlier NBRC 296 review report that
had already flagged this duplicate.
