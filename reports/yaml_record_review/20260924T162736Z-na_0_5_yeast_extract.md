# YAML Record Review: na_0_5_yeast_extract

- Repository: CultureMech
- Record: data/merge_yaml/merged/na_0_5_yeast_extract.yaml
- Started UTC: 2026-09-24T16:26:25Z
- Finished UTC: 2026-09-24T16:27:36Z
- Verdict: needs curation

## Target

Reviewed the generated MediaRecipe `CultureMech:008124` for
`na_0_5_yeast_extract`, a TOGO import grounded to `TOGO:M1575`.

## Validation

Focused validation passed:

- LinkML `MediaRecipe` validation: passed; no issues found.
- Strict validation: passed with zero error rows in `/private/tmp/na_0_5_yeast_extract.strict.tsv`.
- Reference validation: passed with 0 checks.
- Term validation: passed.
- Embedded curation history: Not checked: `just validate-history` validates standalone `history/` records, not `MediaRecipe.curation_history` entries embedded in generated YAML.

## Identity and Grounding

The TOGO identity is correct. `TOGO:M1575` imports NBRC Medium 380,
`NA + 0.5% Yeast Extract`.

An exact ignored-inclusive search across `data/normalized_yaml`,
`data/merge_yaml/merged`, and top-level `data/*.tsv` files also found
`data/normalized_yaml/bacterial/NBRC_382.yaml`, an active direct NBRC owner for
the same NBRC Medium 380 page. Both active normalized owners have been repaired,
but the generated files are stale and the two source-equivalent owners are not
linked.

## Evidence

The live NBRC 380 page lists:

- 5 g Pepton
- 3 g Beef extract
- 5 g Yeast extract
- 3 g NaCl
- 15 g Agar, if needed
- 1 L Distilled water
- pH 7.0

The live TOGO `M1575` API mirrors the same NBRC source and reports the same six
component rows plus pH 7.0.

## Completeness

The generated TOGO record includes all six source ingredient names and has the
right dry quantities. It is stale relative to the 2026-09-12 repaired
normalized owner: water is still represented as `1 G_PER_L`, the source pH 7.0
is absent, and the repaired source-backed preparation steps are absent.

The direct NBRC owner was also repaired after the generated snapshot. Its
current normalized YAML contains the same six ingredient rows, but
`data/merge_yaml/merged/382.yaml` still contains the older malformed
single-string parse of the composition table.

## Findings

1. `Distilled water` is modeled as `1 G_PER_L` instead of 1 L.

2. The source pH 7.0 is missing from the generated TOGO record.

3. The generated TOGO record predates the 2026-09-12 owner repair and lacks the
   repaired source notes, FOODON/MICRO mappings, and preparation steps.

4. The equivalent direct NBRC 380 owner is active but remains unlinked to the
   TOGO mirror.

5. Generated `382.yaml`, from the direct NBRC owner, is also stale and still
   contains the original malformed NBRC composition blob.

6. `Agar (if needed)` should retain its optional solidifying semantics after
   regeneration instead of being treated as an unconditional agar requirement.

## Recommended Edits

Propagate and reconcile the existing normalized repairs:

- Regenerate the TOGO M1575 generated record from the repaired
  `na_0_5_yeast_extract.yaml` owner.
- Regenerate the direct NBRC `NBRC_382.yaml` owner so `382.yaml` no longer has
  the malformed ingredient blob.
- Link TOGO M1575 and NBRC Medium 380 as source duplicates.
- Confirm water remains a 1 L addition and pH 7.0 is represented in the
  regenerated TOGO and NBRC records.
- Preserve the optionality of `Agar (if needed)`.

## Follow-up Checks

- Re-fetch TOGO `M1575` and NBRC `NO=380` after regeneration and compare all
  ingredient rows.
- Re-run LinkML, strict, reference, and term validation on the regenerated
  TOGO and NBRC records.
- Confirm exact ignored-inclusive searches for `TOGO:M1575` and `NO=380` find
  linked source-duplicate owners rather than independent generated records.

## Additional Notes

None found.
