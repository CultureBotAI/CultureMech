# YAML Record Review: ppm__7a6a33ba

- Repository: CultureMech
- Record: data/merge_yaml/merged/ppm__7a6a33ba.yaml
- Started UTC: 2026-09-24T21:51:26Z
- Finished UTC: 2026-09-24T21:51:26Z
- Verdict: needs curation

## Target

Reviewed `CultureMech:008662`, the generated bacterial liquid `PPM` record merged from `data/normalized_yaml/bacterial/ppm.yaml` and grounded to TOGO `M2073` / NBRC Medium 1381.

## Validation

- LinkML validation: Passed; `linkml-validate` reported no issues.
- Strict validation: Passed; `scripts/validate_strict.py` scanned one file and reported zero `ERROR` rows.
- Reference validation: Passed; `linkml-reference-validator` validated one file and reported zero reference checks.
- Term validation: Passed; `linkml-term-validator` exited 0.
- Embedded history validation: Not checked: `just validate-history` validates standalone `history/` files, not `MediaRecipe.curation_history` blocks in generated YAML.

## Identity and Grounding

The identity is correct but the recipe graph is not. TOGO `M2073` maps to NBRC Medium 1381, both call the recipe `PPM`, and both expose the same main recipe, `Trace element solution (SL-10)`, and `Se/W solution`. An exact ignored YAML search for `TOGO:M2073`, `M2073`, `NO=1381`, and `NBRC_M1381` found this stale M2073 branch and the neighboring NBRC `M1381-2` PPM branch that cross-references Medium M2073 for the same two stock solutions.

## Evidence

TOGO and the NBRC page list a main PPM recipe containing 1 L distilled water, 2 g MES, 1 g MgSO4.7H2O, 0.053 g NH4H2PO4, 0.05 g each xylan, pectin, Bacto yeast extract, and Bacto casamino acids, 2 ml Trace element solution (SL-10), 2 ml Se/W solution, 0.1 g cycloheximide, optional 8 g Gelrite, and pH 5.0.

The NBRC page and TOGO API both define `Trace element solution (SL-10)` as its own 1 L stock with 10 ml 25% HCl, 1.5 g FeCl2.4H2O, 0.07 g ZnCl2, 0.1 g MnCl2.4H2O, 0.006 g B(OH)3, 0.19 g CoCl2.6H2O, 0.002 g CuCl2.2H2O, 0.024 g NiCl2.6H2O, 0.036 g Na2MoO4.2H2O, and 1 L water. They define `Se/W solution` as a separate 1 L stock with 0.4 g NaOH, 0.002 g Na2SeO3, 0.004 g Na2WO4, and 1 L water.

## Completeness

The generated record has most scalar masses from the main recipe and both stock recipes, but it is not a complete PPM recipe because it flattens stock-internal components into the final medium, loses pH 5.0, misrepresents stock aliquots as grams, and makes the optional solidifying component look mandatory for a liquid recipe.

## Findings

- Three separate 1 L water rows from the main recipe and the two stock definitions are flattened and merged into one `Distilled water` ingredient with `3.0 G_PER_L`, which is neither the correct volume unit nor the correct solution boundary.
- The `Trace element solution (SL-10)` and `Se/W solution` additions are 2 ml/L aliquots, but the normalized and generated records store each as `2 G_PER_L` solutions with empty or externalized composition.
- HCl, FeCl2.4H2O, ZnCl2, MnCl2.4H2O, B(OH)3, CoCl2.6H2O, CuCl2.2H2O, NiCl2.6H2O, Na2MoO4.2H2O, NaOH, Na2SeO3, and Na2WO4 belong inside stock-solution compositions and should not appear as full-strength top-level ingredients in the final PPM medium.
- The record omits the source pH 5.0 despite `ph` in the TOGO metadata and a pH row on the NBRC page.
- `Gelrite (if needed)` is optional for solid medium preparation, but the generated liquid record lists it as an unconditional 8 g/L ingredient.

## Recommended Edits

- Repair `data/normalized_yaml/bacterial/ppm.yaml` by keeping the main-liquid ingredients at the NBRC/TOGO quantities, setting distilled water to `1000 ML_PER_L`, adding `ph_value: 5.0`, and moving the SL-10 and Se/W internal recipes into `solutions` reached by 2 ml/L additions.
- Represent 10 ml 25% HCl as a liquid stock component rather than `10 G_PER_L`, and keep each stock's 1 L water inside its own solution instead of collapsing it into the final medium.
- Decide whether this liquid record should drop `Gelrite (if needed)` or explicitly model the NBRC solid-medium variant separately; do not leave optional Gelrite as a mandatory liquid ingredient.
- Add source notes and `references` for both `https://togomedium.org/medium/M2073` and `https://www.nite.go.jp/nbrc/catalogue/NBRCMediumDetailServlet?NO=1381`, then regenerate `data/merge_yaml/merged`.

## Follow-up Checks

- Re-run LinkML, strict, reference, and term validation on the regenerated merged record.
- Search exact source IDs with ignored files included after regeneration to verify that `TOGO:M2073`, `NBRC_M1381`, and the NBRC `NO=1381` URL still resolve to one M2073 PPM branch while the M2993/NBRC `M1381-2` branch only cross-references the M2073 stock definitions.

## Additional Notes

None.
