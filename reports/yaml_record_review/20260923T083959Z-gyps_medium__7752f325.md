# YAML Record Review: GYPS MEDIUM

- Repository: CultureMech
- Record: data/merge_yaml/merged/gyps_medium__7752f325.yaml
- Started UTC: 2026-09-23T08:38:56Z
- Finished UTC: 2026-09-23T08:39:59Z
- Verdict: pass with minor issues

## Target

- Reviewed generated YAML for `GYPS MEDIUM`.
- Stable ID: `CultureMech:002799`.
- Primary source in generated record: direct JCM/MediaDive `mediadive.medium:J449`.
- Merge fingerprint: `7752f325a4568baf294f7e14f5a13a8ded443a45a718defa5962aa0072fcfbe1`.

## Validation

- LinkML validation against `src/culturemech/schema/culturemech.yaml`, target class `MediaRecipe`: pass.
- Strict validation with `scripts/validate_strict.py`: pass, 0 error rows in `/private/tmp/gyps_medium_7752f325.strict.tsv`.
- LinkML reference validation: pass, 0 checked references.
- LinkML term validation with `conf/oak_config.yaml`: pass.
- Embedded `curation_history`: Not checked: `just validate-history` validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` entries in merged YAML.

## Identity and Grounding

- The generated record is the direct JCM/MediaDive import for medium `J449`, with medium term `mediadive.medium:J449`.
- An exact `rg --no-ignore --hidden` search for `JCM_M449`, `GRMD=449`, and `mediadive.medium:J449` found the expected direct JCM/MediaDive source plus a source-equivalent Togo `M450` / `JCM_M449` record in `data/merge_yaml` and `data/normalized_yaml`.
- The generated direct JCM record is stale relative to its normalized parent, which received exact local primary-term grounding for `Yeast extract` and `Peptone` on 2026-09-13.

## Evidence

- The live JCM `GRMD=449` page lists glucose 5 g, yeast extract 0.5 g, peptone 1 g, Sigma sea salts 30 g, MES 3.9 g, and distilled water 1 L, then instructs adjusting pH to 6.0 and replacing the gas phase with N2.
- The MediaDive REST record for `J449` reports the same `GYPS MEDIUM` identity, pH 6.0, ingredients, gram-per-liter values, and preparation step.
- The generated record preserves the non-water JCM concentrations, pH 6.0, and the combined pH/gas-phase preparation step.
- The Togo `M450` record has the same original JCM `M449` source and the same chemical recipe, but its generated import converts 1 L distilled water to `1 G_PER_L` and models gas-phase N2 as a variable top-level ingredient.

## Completeness

- Required source fields, medium identity, non-water concentrations, units, pH, the preparation step, application, and generated merge metadata are present.
- `Yeast extract`, `Peptone`, and `Sea Salt` are complex ingredients and do not have MediaIngredientMech ChEBI links in this generated record.
- The generated record does not retain the JCM source's `Sigma` qualifier on sea salts.
- No target-organism assertions are present; none were evident in the generated source record.

## Findings

- Minor: The source-equivalent Togo `M450` / `JCM_M449` and direct JCM/MediaDive `J449` imports remain split into `GYPS_MEDIUM.yaml` and `gyps_medium__7752f325.yaml`.
- Minor: `Yeast extract` and `Peptone` exact-term grounding has already landed in `data/normalized_yaml/bacterial/gyps_medium.yaml` but is stale in the generated merged copy.
- Minor: `Sea Salt` is ungrounded and omits the source's Sigma qualifier.

## Recommended Edits

- Add source-equivalence handling so `TOGO:M450` / `JCM_M449` merges with direct `mediadive.medium:J449`.
- Regenerate `data/merge_yaml/merged/gyps_medium__7752f325.yaml` so the September 2026 normalized-parent ingredient grounding is reflected in generated output.
- Preserve `Sea salts (Sigma)` or curate an equivalent local complex-ingredient grounding that does not lose the vendor qualifier.

## Follow-up Checks

- After regenerating merged records, verify that GYPS Medium has one merged output retaining both source identifiers and no longer carries water or N2 as top-level ingredients from the Togo import.

## Additional Notes

- None found.
