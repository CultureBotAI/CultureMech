# YAML Record Review: togo_medium_m1461

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1461.yaml`
- Started UTC: 2026-09-25T13:49:07Z
- Finished UTC: 2026-09-25T13:49:07Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1461, imported from NBRC medium 234.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1461.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1461`; the TOGO API identifies NBRC_M234 as its original medium ID and links NBRC medium 234.

The TOGO payload and NBRC page agree on 2 g ammonium sulfate, 0.1 g KCl, 0.5 g K2HPO4, 0.5 g MgSO4 x 7 H2O, 0.1 g Hipolypepton, 1 g glucose, 1 L distilled water, 15 g agar if needed, pH adjustment to 3.0 with 6 N H2SO4, and separate post-autoclave handling for glucose and polypeptone.

## Evidence

The generated salt, glucose, peptone, and agar rows match the source amounts.

The generated record adds the sourcerow H2SO4 pH-adjuster as a variable ingredient but does not preserve the pH 3.0 value or the 6 N stock concentration in the row.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The source preparation text says the glucose and polypeptone are autoclaved separately as 10% solutions and added to the other autoclaved ingredients after cooling; that handling is missing because both starred rows were moved into empty solution wrappers.

## Findings

- Major issue: `Glucose**` is modeled as an empty solution named `Unknown solution` instead of a separately sterilized ingredient.
- Major issue: `Hipolypepton*` is modeled as an empty solution named `Unknown solution` instead of a commercial peptone ingredient with a Wako manufacturer footnote.
- Minor issue: pH 3.0 and the 6 N H2SO4 adjustment are absent.
- Minor issue: `Distilled water` is represented as 1 g/L instead of 1 L.
- Minor issue: the glucose and polypeptone post-autoclave cooling and addition instruction is absent.

## Recommended Edits

- Move glucose and Hipolypepton out of empty solution wrappers and preserve their source footnotes as preparation or component notes.
- Add `ph_value: 3.0` and preserve the instruction to adjust pH with 6 N H2SO4.
- Correct or omit the water row according to the repo's final-volume convention.
- Add preparation steps for separately autoclaving glucose and polypeptone as 10% solutions and adding them after the other ingredients cool.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Add a targeted regression check for TOGO starred commercial ingredients so manufacturer and sterilization footnotes are not converted to empty solutions.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1461 source for this stem. Ignored files were included for source and duplicate-report checks, and no pre-existing ignored report for this generated record was found.
