# YAML Record Review: togo_medium_m1475

- Repository: CultureMech
- Record: `data/merge_yaml/merged/togo_medium_m1475.yaml`
- Started UTC: 2026-09-25T13:59:09Z
- Finished UTC: 2026-09-25T13:59:09Z
- Verdict: needs curation

## Target

Reviewed generated merged YAML for TOGO medium M1475, imported from NBRC medium 255.

The generated record is a single-source merge from `data/normalized_yaml/bacterial/togo_medium_m1475.yaml`.

## Validation

- LinkML schema validation: Passed; exited 0 with no diagnostics.
- Strict validation: Passed with 0 errors; the TSV contained only the header row.
- Reference validation: Passed with 0 checks.
- Term validation: Passed; the only diagnostic was the known eutils/pkg_resources startup warning.
- Embedded curation history: Not checked; the available history validator targets standalone `history/`, not `MediaRecipe.curation_history`.

## Identity and Grounding

The medium identity is grounded to `TOGO:M1475`; the TOGO API identifies NBRC_M255 as its original medium ID and links NBRC medium 255.

The TOGO payload and NBRC page agree on 7.5 g Bacto Casamino Acids, 10 g yeast extract, 3 g trisodium citrate, 2 g KCl, 20 g MgSO4 x 7 H2O, 0.05 g FeSO4 x 7 H2O, 0.2 mg MnSO4 x n H2O, 250 g NaCl, distilled water made up to 1 L, 20 g agar if needed, pH 7.4, and the instruction to dissolve agar by heating before adding NaCl.

## Evidence

The generated gram-scale casamino acid, yeast extract, citrate, KCl, MgSO4, FeSO4, and NaCl rows match NBRC medium 255.

The generated record mis-converts the trace MnSO4 x n H2O row from 0.2 mg to 0.2 g/L and moves the optional agar row into an empty `Unknown solution`, losing the instruction attached to the agar footnote.

The generated record also has `kg_microbe_match: mediadive.medium:1336`, but exact MediaDive REST and local generated MediaDive records identify `mediadive.medium:1336` as DSMZ SEHGAL AND GIBBONS MEDIUM with 40 g/L NaCl, 5 g/L tryptone, 5 g/L casein hydrolysate, 1000 ml distilled water, and pH 7.2-7.4. That is not the NBRC 255 formulation.

## Completeness

No target organisms are present; that empty optional field is not a defect in this formulation review.

The generated record does not preserve the source pH target or the agar heating order.

## Findings

- Major issue: `MnSO4 x n H2O` is represented as 0.2 g/L instead of the source 0.2 mg/L-scale addition.
- Major issue: `Agar (if needed)*` is modeled as an empty solution named `Unknown solution` instead of an optional final-medium solidifying agent.
- Major issue: `kg_microbe_match: mediadive.medium:1336` points to DSMZ SEHGAL AND GIBBONS MEDIUM, a different medium.
- Minor issue: `Distilled water, make up to` is represented as 1 g/L instead of a final volume of 1 L.
- Minor issue: pH 7.4 is absent.
- Minor issue: the instruction to dissolve agar by heating before adding NaCl is absent.

## Recommended Edits

- Move optional agar out of the empty solution wrapper and keep the source instruction to dissolve it by heating before adding NaCl.
- Correct `MnSO4 x n H2O` to preserve the 0.2 mg source amount instead of 0.2 g/L.
- Remove or replace the incorrect `mediadive.medium:1336` cross-source match.
- Add pH 7.4 and correct or omit the final-volume water row according to the repo's convention.

## Follow-up Checks

- Re-run schema, strict, reference, and term validation after editing the normalized source YAML and regenerating the merge.
- Add or run a regression check for starred optional agar rows so TOGO importer output does not convert them into empty solutions.
- Confirm any replacement MediaDive match has the NBRC 255 250 g/L NaCl formulation, not DSMZ Medium 1336.

## Additional Notes

Exact local source search under `data/` found only the generated merged YAML and the expected normalized bacterial TOGO M1475 source for this stem. An exact search for `mediadive.medium:1336` under `data/merge_yaml/merged` and `data/normalized_yaml` included ignored files and found the cross-source record used to evaluate the stale match. Ignored files were included for duplicate-report checks, and no pre-existing ignored report for this generated record was found.
