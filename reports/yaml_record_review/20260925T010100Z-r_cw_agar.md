# YAML Record Review: R-CW Agar

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/r_cw_agar.yaml
- Started UTC: 2026-09-25T00:59:20Z
- Finished UTC: 2026-09-25T01:01:00Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/merge_yaml/merged/r_cw_agar.yaml` |
| Class | `MediaRecipe` |
| ID | `CultureMech:008087` |
| Label | `R-CW Agar` |
| Category | `bacterial` |
| Source identity | `TOGO:M153`, imported from JCM `JCM_M162` |
| Generated status | Generated singleton with `merge_fingerprint: d7a687f54e41ab9852b019d98217ed63a50d1b4ee32a96cea08628b018a59c96`; owned upstream by `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml`. |

## Validation

| Check | Result |
| --- | --- |
| Open LinkML schema | Passed; `linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/r_cw_agar.yaml` reported no issues. |
| Strict validator | Passed; `scripts/validate_strict.py data/merge_yaml/merged/r_cw_agar.yaml --out /private/tmp/r_cw_agar.strict.tsv --workers 1 --quiet` reported 0 files with errors and the strict TSV had only its header row. |
| Reference validator | Passed; `linkml-reference-validator validate data data/merge_yaml/merged/r_cw_agar.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` exited 0 after validating the file and reported no failed checks. |
| Term validator | Passed; `linkml-term-validator validate-data data/merge_yaml/merged/r_cw_agar.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` exited 0 and reported validation passed. |
| Embedded curation history | Not checked: `just validate-history` validates the standalone `history/` tree, not embedded `MediaRecipe.curation_history` entries in merged YAML. |

## Identity and Grounding

- TOGO M153 identifies the medium as `R-CW Agar`, records `original_media_id: JCM_M162`, and cites the JCM GRMD 162 URL.
- Live JCM GRMD 162 resolves to `R-CW AGAR` with the same 10 components in the same gram, milliliter, and liter amounts.
- Live MediaDive J162 mirrors the same JCM GRMD 162 recipe, so `data/normalized_yaml/bacterial/r_cw_agar.yaml` is a direct-JCM source-equivalent sibling of this TOGO record.
- Most ingredient amounts and chemical groundings agree with the TOGO source. The two source unit mismatches are on Tween 80 and cheese whey.

## Evidence

- Supported:
  - TOGO M153 and JCM GRMD 162 support the `R-CW Agar` label, the original JCM M162 identity, and all 10 listed component names.
  - TOGO M153 supports 0.5 g MgSO4 x 7 H2O, 5 g KH2PO4, 5 g sodium acetate, 0.5 g MnSO4 x H2O, 2 g diammonium hydrogen citrate, 5 g yeast extract, 5 g tryptone, and 5 g trypticase peptone per 1 L cheese whey.
  - The MediaDive J162 REST payload independently maps the same source to its direct `mediadive.medium:J162` identity and confirms the raw source units for Tween 80 and cheese whey.
- Unsupported or over-scoped:
  - The generated record encodes Tween 80 as `1 G_PER_L`, but TOGO M153 and JCM GRMD 162 both list 1 ml Tween 80.
  - The generated record encodes Cheese whey as `1 G_PER_L`, but TOGO M153 and JCM GRMD 162 both list 1 L cheese whey.
  - The record lacks the JCM default autoclaving instruction that appears on the live GRMD 162 page.

## Completeness

- The current maintained owner has the same Tween 80 and cheese whey unit errors; this is a normalized-source curation issue, not merely a stale generated copy.
- The generated and maintained TOGO records both keep the TOGO and JCM source URLs in `notes` but do not expose them as structured `references`.
- An exact gitignore-independent search over `data/normalized_yaml`, `data/merge_yaml`, and `reports` for `TOGO:M153`, `JCM_M162`, `GRMD=162`, `CultureMech:008087`, `TOGO_M153_R-CW_Agar`, and `r_cw_agar` found the TOGO M153 owner, this generated artifact, the source-equivalent direct MediaDive/JCM J162 owner and generated artifact, indexes, and historical validation rows.
- Empty optional target-organism fields are not defects for this record.

## Findings

| Severity | Finding | Evidence | Owner |
| --- | --- | --- | --- |
| Major | Tween 80 has the wrong unit. | TOGO M153 and JCM GRMD 162 list 1 ml Tween 80; `data/merge_yaml/merged/r_cw_agar.yaml` and `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` encode it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` |
| Major | Cheese whey has the wrong unit. | TOGO M153 and JCM GRMD 162 list 1 L cheese whey; the normalized owner and generated artifact encode it as `1 G_PER_L`. | `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` |
| Major | The source-equivalent TOGO M153 and direct MediaDive J162 records are split in generated output. | TOGO M153 records original source `JCM_M162` and GRMD 162; MediaDive J162 points to the same JCM page. The exact search found separate generated files at `r_cw_agar.yaml` and `r_cw_agar__392caa15.yaml`. | Add a source-equivalence or merge-key repair between `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` and `data/normalized_yaml/bacterial/r_cw_agar.yaml`, then regenerate. |
| Minor | The JCM default sterilization instruction is missing. | Live JCM GRMD 162 states the default autoclave condition, and this record has no `preparation_steps`. | `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` |

## Recommended Edits

1. Change Tween 80 in `data/normalized_yaml/bacterial/TOGO_M153_R-CW_Agar.yaml` from `1 G_PER_L` to a source-faithful milliliter-per-liter representation.
2. Change Cheese whey from `1 G_PER_L` to a source-faithful liquid-volume representation, preserving that cheese whey is the 1 L carrier rather than distilled water.
3. Add structured TOGO M153 and JCM GRMD 162 `references` plus the default JCM autoclaving step.
4. Reconcile TOGO M153 with `data/normalized_yaml/bacterial/r_cw_agar.yaml` so the source-equivalent JCM 162 records collapse or are explicitly cross-linked after regeneration.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validators against the regenerated `data/merge_yaml/merged/r_cw_agar.yaml`.
- Re-run a gitignore-independent exact search for `TOGO:M153`, `mediadive.medium:J162`, and `GRMD=162` to confirm the TOGO and direct MediaDive/JCM imports are no longer emitted as unrelated generated records.
- Re-fetch TOGO M153, JCM GRMD 162, and MediaDive J162 to confirm the raw milliliter and liter source units were preserved.

## Additional Notes

- `physical_state: LIQUID` is surprising for a medium named `R-CW Agar`, but the inspected TOGO, JCM, and MediaDive ingredient tables do not list agar. The physical-state decision should be revisited with source context instead of changing it from the label alone.
- MediaDive normalizes the 5 g, 2 g, and 0.5 g ingredients over a 1001 ml final volume and therefore reports values such as 4.99501 g/L. The TOGO import preserves the JCM table's raw amounts against 1 L of cheese whey, so those small gram-value differences are expected across the sibling records.
