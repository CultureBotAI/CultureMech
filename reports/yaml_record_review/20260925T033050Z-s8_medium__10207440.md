# YAML Record Review: s8_medium__10207440

- Repository: CultureMech
- Record: `data/merge_yaml/merged/s8_medium__10207440.yaml`
- Started UTC: `2026-09-25T03:30:50Z`
- Finished UTC: `2026-09-25T03:30:50Z`
- Verdict: needs curation

## Target

Reviewed the generated merge record for `s8_medium__10207440`, a single-source merge of `data/normalized_yaml/bacterial/s8_medium.yaml`.

The record represents the direct MediaDive/JCM `J91` import for JCM Medium 91, `S8 MEDIUM`. The source recipe is simple: 1 L S6 medium, 5 g `KNO3`, and 0.5 g `NaHCO3`.

## Validation

- Open schema validation: Passed; `linkml-validate` exited 0 with `No issues found`.
- Strict validation: Passed; `scripts/validate_strict.py` exited 0 and reported 0 total error rows.
- Reference validation: Passed; `linkml-reference-validator` ran 0 checks and reported all checks passed.
- Term validation: Passed; `linkml-term-validator` exited 0 after the expected `eutils` / `pkg_resources` warning.
- Embedded `curation_history`: Not checked. `just validate-history` validates standalone `history/`, not `MediaRecipe.curation_history` in merged YAML.

## Identity and Grounding

The generated identity is correctly grounded to `mediadive.medium:J91`, JCM Medium 91 `S8 MEDIUM`. The source points at JCM Medium 90 as the base `S6 medium`, but the generated YAML does not retain a structured parent-medium link to the S6 record.

## Evidence

The JCM 91 page lists three rows: 1.0 L `S6 medium (see Medium No. 90)`, 5.0 g `KNO3`, and 0.5 g `NaHCO3`. The MediaDive REST record for `J91` carries the same data, with 1000 ml S6 medium, 5 g potassium nitrate, and 0.5 g sodium bicarbonate.

## Completeness

The generated record preserves the `KNO3` and `NaHCO3` amounts correctly.

It loses or corrupts these source details:

- 1000 ml S6 medium is represented as `1000 G_PER_L`.
- The JCM Medium 90 parent reference is retained only inside prose source links, not as a structured parent-media relationship.
- The `KNO3` row still carries a legacy `mediaingredientmech_term` slot instead of a CHEBI-keyed `mediaingredientmech_chebi_term`.

## Findings

- `needs curation`: The defining 1 L S6 medium base is modeled as a direct mass ingredient at `1000 G_PER_L`. It should be a parent-medium reference or 1000 ml aliquot of JCM Medium 90, not one kilogram of a component named `S6 medium`.
- `needs curation`: The JCM Medium 90 relationship is unstructured, so the generated record cannot traverse from S8 to the underlying S6 recipe.
- `pass with minor issues`: The `KNO3` 5 g/L and `NaHCO3` 0.5 g/L additive rows match the JCM and MediaDive sources.
- `pass with minor issues`: The `KNO3` row still uses a legacy `MediaIngredientMech:000170` link despite later migration history saying legacy MediaIngredientMech links were refreshed to CHEBI keying.

## Recommended Edits

- Do not hand-edit `data/merge_yaml/merged/s8_medium__10207440.yaml`; fix `data/normalized_yaml/bacterial/s8_medium.yaml`, then rerun the merge.
- Model S6 medium as a 1000 ml parent-medium aliquot linked to JCM Medium 90 or to the local direct `s6_medium` record.
- Preserve 5 g/L `KNO3` and 0.5 g/L `NaHCO3` as S8-specific additions.
- Replace the legacy `MediaIngredientMech:000170` link on `KNO3` with a CHEBI-keyed link or remove it.
- After fixing S6 itself, verify that S8 traverses to the corrected S6 base rather than to a stale or stock-flattened S6 artifact.

## Follow-up Checks

- Rerun open schema, strict, reference, and term validation on `data/normalized_yaml/bacterial/s8_medium.yaml`.
- Regenerate `data/merge_yaml/merged/s8_medium__10207440.yaml` and verify that `S6 medium` no longer appears as `1000 G_PER_L`.
- Verify that the regenerated S8 record has a structured pointer to JCM Medium 90.
- Run an exact ignored-file-inclusive search for `mediadive.medium:J91`, `mediadive.medium:J90`, and `s8_medium` before changing duplicate links.

## Additional Notes

The absence of `target_organisms` was not treated as a defect for this generated record review. S8 should not expand to the full S6 ingredient list unless the expansion preserves the corrected S6 recipe and keeps the two S8-specific additions distinguishable.
