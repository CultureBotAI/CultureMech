# YAML Record Review: 1% Tryptone Agar

- Repository: CultureBotAI/CultureMech
- Record: `data/merge_yaml/merged/1_tryptone_agar__551e7d77.yaml`
- Started UTC: 2026-09-21T05:55:00Z
- Finished UTC: 2026-09-21T05:55:37Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MediaRecipe` |
| ID | `CultureMech:009733` |
| Name | `1_tryptone_agar` |
| Original name | `1% Tryptone Agar` |
| Category | `bacterial` |
| Source term | `TOGO:M353`, `1% Tryptone Agar` |
| Original source | `JCM_M359` |
| Merge fingerprint | `551e7d77168f5950a28bf9c920e87592699e3636299f3eca4f22b84748d53e89` |
| Maintained owner | `data/normalized_yaml/bacterial/TOGO_M353_1_Tryptone_Agar.yaml` |
| Generated status | Generated by `merge_recipes.py` from one source recipe on 2026-08-06 |

The target is the generated TOGO Medium M353 mirror of JCM Medium 359. The maintained owner was repaired on 2026-09-11, so this generated artifact is stale relative to the source record that owns future fixes.

## Validation

| Check | Command | Result |
|---|---|---|
| Open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/1_tryptone_agar__551e7d77.yaml` | Pass: exit 0, no diagnostics |
| Strict schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/1_tryptone_agar__551e7d77.yaml --out /private/tmp/1_tryptone_agar__551e7d77.strict.tsv --workers 1 --quiet` | Pass: 1 file scanned; 0 ERROR rows |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/1_tryptone_agar__551e7d77.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Pass: 1 file validated; 0 total checks |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/1_tryptone_agar__551e7d77.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Pass |
| Embedded curation history | Not run | Not checked: this repo documents `just validate-history` for standalone records under `history/`; no focused validator is documented for embedded `MediaRecipe.curation_history` in one generated merge record. |

The repository-level `just validate-schema`, `just validate-strict`, and related wrappers currently fail before target-specific validation because the project environment tries to build `llvmlite==0.46.0` under Python 3.13 and crashes in `setuptools` with `TypeError: Popen.__init__() got an unexpected keyword argument 'dry_run'`. The no-project Python 3.11 invocations above exercised the same schema, strict, reference, and term validators against this generated record.

## Identity and Grounding

The generated record correctly denotes TOGO Medium M353:

- The YAML carries `media_term.term.id: TOGO:M353` and `media_term.term.label: 1% Tryptone Agar`.
- Its free-text notes preserve the TOGO URL, `JCM_M359` original source accession, and JCM `GRMD=359` URL.
- The inspected TOGO M353 payload reports the same TOGO accession, `original_media_id: JCM_M359`, JCM source URL, medium name, three component rows, and pH 7.0.

The live JCM `GRMD=359` page resolves and supports the same formulation. The generated record still lacks pH and maps only the water row to CHEBI; the maintained owner now has pH 7.0, exact BD-Difco labels, agar and water CHEBI grounding, and an explicit decision to leave the BD-Difco tryptone product unmapped.

## Evidence

The inspected TOGO M353 API payload and JCM Medium 359 page support:

- 10.0 g Tryptone (BD-Difco).
- 15.0 g Bacto agar (BD-Difco).
- 1.0 L Distilled water.
- pH adjustment to 7.0.
- Default sterilization by autoclaving at 121C for 15 min unless otherwise stated.

The generated TOGO artifact preserves the three row labels and the 10 g / 15 g masses. It imports the 1.0 L water row as `1 G_PER_L`, omits pH 7.0, omits the default JCM autoclave condition, leaves Bacto agar ungrounded, and lacks the maintained owner's structured TOGO/JCM references and source-duplicate parent link.

## Completeness

Consequential gaps:

- The 1.0 L distilled-water row is present but assigned the wrong unit.
- pH 7.0 and the 121C, 15 min default JCM autoclave condition are absent.
- Bacto agar is no longer grounded in the generated record.
- The generated record has only free-text TOGO/JCM URLs; the maintained owner's structured references are stale-missing.
- The generated record is not linked to the repaired direct JCM source duplicate.

Empty optional slots:

- `target_organisms`, `growth_metrics`, and `variants` are correctly absent. The inspected source pages verify the recipe but do not support a target-organism assertion.

Bounded searches:

- A gitignore-independent `rg --no-ignore --hidden` search over `data/normalized_yaml`, `data/merge_yaml/merged`, and `reports/yaml_record_review` for `CultureMech:009733`, `TOGO:M353`, `JCM_M359`, `GRMD=359`, and the `551e7d77` merge fingerprint found the expected TOGO M353 owner, expected generated TOGO M353 merge, the direct JCM owner and generated merge, source index rows, and the neighboring direct-record review report.
- A gitignore-independent `find` over `reports/yaml_record_review` found no pre-existing timestamped report for this exact target stem before this report was created.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The generated artifact is stale and preserves the imported water-unit artifact. | TOGO M353 and JCM Medium 359 list 1.0 L Distilled water, and `data/normalized_yaml/bacterial/TOGO_M353_1_Tryptone_Agar.yaml` now stores `1.0 L`; the generated merge still stores `1 G_PER_L`. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M353_1_Tryptone_Agar.yaml` |
| Major | The generated artifact is stale and omits pH plus the JCM default autoclave condition. | TOGO M353 reports pH 7.0 and JCM Medium 359 lists default 121C, 15 min autoclaving; the maintained owner has `ph_value`, `ADJUST_PH`, `AUTOCLAVE`, and structured `sterilization`, while the generated merge has none of those fields. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M353_1_Tryptone_Agar.yaml` |
| Minor | Bacto agar grounding and structured references are stale-missing. | The maintained owner grounds Bacto agar to `CHEBI:2509` and has structured TOGO/JCM references; the generated merge leaves Bacto agar ungrounded and has only free-text `notes`, so `linkml-reference-validator` performed zero checks. | Regenerate `data/merge_yaml/merged/` from `data/normalized_yaml/bacterial/TOGO_M353_1_Tryptone_Agar.yaml` |
| Minor | The TOGO M353 and direct JCM imports remain separate generated records for the same upstream source. | TOGO M353 mirrors JCM Medium 359, and the repaired TOGO owner links `data/normalized_yaml/bacterial/1_tryptone_agar.yaml` as a `SOURCE_DUPLICATE`; the generated merge predates that relationship. | Regenerate `data/merge_yaml/merged/` from the repaired direct JCM and TOGO M353 owners |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so `1_tryptone_agar__551e7d77.yaml` receives the maintained owner's corrected water unit, pH, default JCM autoclave condition, Bacto agar grounding, structured references, and direct-JCM `SOURCE_DUPLICATE` relationship.
2. After regeneration, check whether the direct JCM record and repaired TOGO M353 generated record merge into one canonical source-duplicate output.
3. Keep TOGO M1839 separate unless a future review explicitly establishes that NBRC Medium 1072 is the same source formulation rather than a same-name same-formula recipe from another collection.

## Follow-up Checks

- `just verify-merges` and `just audit-merge-freshness` after regenerating generated records.
- `just validate-strict` and `just validate-terms` on the regenerated TOGO M353 merge.
- `just validate-references` on the regenerated merge to confirm the structured TOGO/JCM references resolve.
- Manual comparison against TOGO M353 and JCM Medium 359 for 10.0 g Tryptone (BD-Difco), 15.0 g Bacto agar (BD-Difco), 1.0 L Distilled water, pH 7.0, and the 121C, 15 min default autoclave condition.

## Additional Notes

- The maintained normalized TOGO M353 owner is already repaired; no additional normalized edit was identified for this TOGO/JCM record in this review.
- The direct MediaDive/JCM sibling was also repaired on 2026-09-11 and now links this TOGO M353 record as a source duplicate.
- `linkml-reference-validator` performed zero checks because this generated record has no structured `references` entries.
