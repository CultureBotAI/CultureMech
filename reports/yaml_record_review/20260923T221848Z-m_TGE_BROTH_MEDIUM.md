# YAML Record Review: m_tge_broth_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml
- Started UTC: 2026-09-23T22:17:39Z
- Finished UTC: 2026-09-23T22:18:48Z
- Verdict: needs curation

## Target

Reviewed `data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml`.

| Field | Value |
|---|---|
| Class | MediaRecipe |
| ID | CultureMech:010241 |
| Name | m_tge_broth_medium |
| Original name | m TGE Broth Medium |
| Source accession | TOGO:M828 |
| Original source | JCM_M795 |
| Category | bacterial |
| Generated status | Generated canonical merge under `data/merge_yaml/merged/`; owned by `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml` and regenerated by `src/culturemech/merge/merge_recipes.py` |

The exact lowercase filename `m_tge_broth_medium.yaml` resolves to a different
MediaDive/JCM record, `CultureMech:003139`. The generated record reviewed here
is owned by the legacy-named TOGO file
`data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml`.

## Validation

| Check | Command | Result |
|---|---|---|
| LinkML open schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml` | Passed: `No issues found`. |
| Strict closed schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml --out /private/tmp/m_TGE_BROTH_MEDIUM.strict.tsv --workers 1 --quiet` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| References | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed: 1 file validated, 0 reference checks, all validations passed. |
| Terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed after the known `eutils`/`pkg_resources` warning. |
| Embedded history | Not run | Not checked: the repository `just validate-history` target validates standalone files under `history/`, not embedded `MediaRecipe.curation_history` arrays. |

## Identity and Grounding

JCM medium 795 and TOGO M828 agree that `m TGE Broth Medium` is 18.0 g Bacto
m TGE broth in 1.0 L distilled water. The generated TOGO record preserves the
right CultureMech ID, TOGO accession, JCM source URL, label, category, and
two-ingredient liquid composition.

The generated merge is stale relative to the September 2026 repaired
normalized owner:

- `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml` stores
  the water as `1000 ML_PER_L`, not `1 G_PER_L`.
- The normalized owner has source-specific `notes`, two `preparation_steps`,
  `sterilization.method: AUTOCLAVE`, data-quality flags, and TOGO/JCM
  references that are absent from the generated merged copy.

The same JCM 795 medium also exists as `CultureMech:003139` at
`data/normalized_yaml/bacterial/m_tge_broth_medium.yaml`. That MediaDive/JCM
record points to `mediadive.medium:J795`, cites the same JCM URL, and models
the same two ingredients. The TOGO and MediaDive records should be reconciled
as same-source duplicates rather than curated independently.

## Evidence

Supported by inspected source:

| Claim | Assessment |
|---|---|
| `TOGO:M828` mirrors JCM medium 795, `m TGE Broth Medium` | Supported by TOGO M828 metadata and the JCM GRMD 795 page. |
| The liquid recipe contains 18.0 g Bacto m TGE broth (BD-Difco) in 1.0 L distilled water | Supported by TOGO M828 and JCM 795; preserved by both the TOGO normalized owner and the generated record. |
| JCM gives a default sterilization of 121 C for 15 min | Supported by the JCM page boilerplate and represented by the September 2026 normalized-owner repair; absent from the generated copy. |
| Solid medium can be prepared by adding 15.0 g/L agar | Supported by JCM 795 and TOGO M828; not represented in CultureMech. |

Unsupported or mismatched in the record:

- The generated `Distilled water` quantity is stale. The maintained TOGO owner
  was corrected to `1000 ML_PER_L`; the generated record still has
  `1 G_PER_L`.
- The generated record is missing the maintained owner references to TOGO M828
  and JCM 795.
- The curation history entry `Source: JCM, ID: M828` mixes the TOGO M828
  accession with the original JCM M795 identity.

## Completeness

Consequential gaps:

- The generated record lacks the maintained JCM-derived preparation and
  autoclave steps.
- The generated record lacks references and the September 2026 curation event
  present in the normalized owner.
- JCM 795 is represented by two maintained CultureMech records:
  `CultureMech:010241` from TOGO and `CultureMech:003139` from MediaDive/JCM.

Empty optional slots for pH, target organisms, atmosphere, temperature, and
growth evidence are acceptable as empty because TOGO M828 and JCM 795 provide
only the two-component recipe, optional agar, and sterilization boilerplate.

Bounded local search for `CultureMech:010241`, `TOGO:M828`, `M828`, and
`JCM_M795` included ignored and hidden files under `data/normalized_yaml`,
`data/merge_yaml`, `reports`, `src`, `tests`, `conf`, `scripts`, `.claude`,
`pyproject.toml`, `justfile`, and `README.md`.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| major | The generated merge is stale relative to the repaired normalized TOGO owner. | The normalized owner has `1000 ML_PER_L` water, JCM-specific preparation/autoclave steps, references, and September 2026 curation events; the generated record does not. | Merge regeneration from `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml`. |
| major | JCM 795 is duplicated as separate TOGO and MediaDive records. | `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml` and `data/normalized_yaml/bacterial/m_tge_broth_medium.yaml` both describe JCM 795 as 18.0 g Bacto m TGE broth in 1.0 L water. | Reconciliation inputs for both maintained records. |
| minor | Original-source provenance is muddled in the TOGO import history. | The import event says `Source: JCM, ID: M828`; M828 is the TOGO ID, not JCM medium 795. | `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml` or the TOGO importer. |

## Recommended Edits

1. Regenerate `data/merge_yaml/merged/` so
   `data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml` reflects the September
   2026 repaired normalized TOGO owner.
2. Reconcile `CultureMech:010241` with the same-source MediaDive/JCM
   `CultureMech:003139` record and keep one source-supported JCM 795
   formulation.
3. Correct the TOGO import curation-history note so it does not call M828 a JCM
   accession.
4. Decide whether the optional 15.0 g/L agar clause should be modeled as a
   solid-medium variant or left only in discussion; do not add agar to the
   liquid root recipe.

## Follow-up Checks

- Re-run focused open-schema, strict, reference, and term validators on
  `data/normalized_yaml/bacterial/TOGO_M828_m_TGE_Broth_Medium.yaml`.
- Re-run the same validators on regenerated
  `data/merge_yaml/merged/m_TGE_BROTH_MEDIUM.yaml`.
- Run `just verify-merges` and `just audit-merge-freshness` after merge
  regeneration.
- Manually compare the reconciled JCM 795 record with the JCM GRMD 795 page and
  TOGO M828.

## Additional Notes

- This target's two generated ingredient values are source-faithful apart from
  water's presentation as `1 G_PER_L` instead of the repaired `1000 ML_PER_L`.
- The optional agar instruction does not make the current liquid
  `physical_state` wrong; it only leaves a possible solid variant unmodeled.
