# YAML Record Review: mk_medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml
- Started UTC: 2026-09-24T08:04:14Z
- Finished UTC: 2026-09-24T08:04:14Z
- Verdict: needs curation

## Target

- Reviewed record: `data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml`
- Class: `MediaRecipe`
- Record ID: `CultureMech:010374`
- Label: `mk_medium`
- Original label: `MK Medium`
- Source grounding: `TOGO:M950`, labelled `MK Medium`
- Category: `bacterial`
- Generated status: generated merged record with `merge_fingerprint: b1e5f5fcee0ad44280410647a6a1f2b709b4a63b21b9df4c8fe61d2324b5a2eb`
- Maintained owner for future edits: `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`

## Validation

- Open schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml`
  - Result: passed with `No issues found`.
- Strict schema validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml --out /private/tmp/mk_medium__b1e5f5fc.strict.tsv --workers 1 --quiet`
  - Result: passed; 1 file scanned, 0 files with errors, 0 total error rows. `/private/tmp/mk_medium__b1e5f5fc.strict.tsv` has 1 line, the header only.
- LinkML reference validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe`
  - Result: passed; 1 file validated, 0 reference checks, all validations passed.
- Term validation:
  - Command: `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml`
  - Result: passed.
- Embedded history validation:
  - Not checked: `just validate-history` validates standalone files under `history/`, not `MediaRecipe.curation_history` entries embedded in generated merged YAML.

## Identity and Grounding

- The generated record identity matches TOGO `M950`: the inspected TOGO API names `gm: http://togomedium.org/medium/M950`, `name: MK Medium`, `original_media_id: JCM_M906-2`, `src_url` for JCM medium 906, and pH 7.0.
- The record is specifically the solid MK variant: TOGO `M950` includes the JCM 906 base recipe plus 20 g Bacto agar, and the generated record sets `physical_state: SOLID_AGAR`.
- The inspected JCM medium 906 page supports the MK Medium base recipe and the conditional solid-medium instruction to add 20.0 g/L Bacto agar.
- TOGO `M950` preserves the source's 1 ml `SL-6 trace element solution` cross-reference to M158 and the separate 100 ml `0.5 M Glycine-NaOH buffer (pH 10.0)` subcomponent. The generated record has an empty `SL-6` solution with `name: Unknown solution` and a `1 G_PER_L` concentration, and it keeps the Glycine-NaOH buffer as a direct ingredient with `100 G_PER_L`.
- An exact gitignore-independent search with `rg --no-ignore --hidden` over `data/normalized_yaml` and this generated target found the maintained TOGO source at `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`; the same label also occurs in `data/normalized_yaml/bacterial/TOGO_M949_MK_Medium.yaml` and `data/normalized_yaml/bacterial/mk_medium.yaml`.

## Evidence

- The source identity, fixed pH, JCM provenance, solid-agar identity, main formula, and buffer and SL-6 subcomponents are supported by the inspected TOGO `M950` API payload and the inspected JCM medium 906 page.
- TOGO `M950` gives the main recipe's FeCl2 x 4 H2O amount as 36 mg and MnCl2 x 4 H2O as 0.36 mg. The generated record stores those as `36 G_PER_L` and `0.36 G_PER_L`, so both milligram quantities are off by a factor of 1000.
- TOGO `M950` gives the SL-6 trace element solution cross-reference as a 1 ml item with `reference_media_id: M158`. The generated `solutions` entry has `composition: []`, `notes: Cross-reference to Medium M158`, `name: Unknown solution`, and a `1 G_PER_L` concentration rather than a 1 ml volume.
- The inspected TOGO `M158` API payload contains the referenced SL-6 trace element solution and its recipe, including Na2MoO4 x 2 H2O, H3BO3, MnCl2 x 4 H2O, CoCl2 x 6 H2O, and other trace salts. None of those M158 solution members are present in the generated M950 record.
- TOGO `M950` gives the Glycine-NaOH buffer as a separate 100 ml subcomponent with `conc_value: 0.5` and `conc_unit: M`. The generated record drops the 0.5 M concentration and represents 100 ml as `100 G_PER_L`.
- An exact `rg --no-ignore --hidden` search for `preparation_steps:`, `step_number`, `Add components`, `Adjust pH`, and `autoclave` across `data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml` and `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml` returned no matches, with ignored files included by the search; the source preparation boundaries are absent from both YAML files.

## Completeness

- The record identifies the correct TOGO accession and keeps the solid-agar variant distinction.
- It is quantitatively incomplete for all milligram source rows because at least FeCl2 x 4 H2O and MnCl2 x 4 H2O were stored as grams per liter without converting units.
- It is structurally incomplete for the 1 ml SL-6 stock addition because the migrated `solutions` entry is an empty stub rather than a resolved M158 stock.
- It is structurally incomplete for the 100 ml Glycine-NaOH buffer because the record treats a molar stock volume as a mass concentration.
- It is missing the JCM preparation instructions for 900 ml main-solution assembly, pH 7.0 adjustment, main-solution autoclaving, separate buffer autoclaving, and post-autoclave buffer addition.
- Empty optional organism, source-data, and discussion fields are not defects for this generated import. The inspected TOGO and JCM entries support the recipe but do not attach a specific growth observation that should have produced a `target_organisms` claim here.

## Findings

1. **major - milligram source amounts are imported as grams per liter**
   - Evidence: TOGO `M950` supplies 36 mg FeCl2 x 4 H2O and 0.36 mg MnCl2 x 4 H2O. The generated record says `36 G_PER_L` and `0.36 G_PER_L`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`, via the TOGO importer unit conversion for milligram rows.
2. **major - the SL-6 stock cross-reference was migrated to an empty solution stub**
   - Evidence: TOGO `M950` supplies a 1 ml `SL-6 trace element solution (see Medium [M158])` item. The generated `solutions` entry has no composition, no reference field, `name: Unknown solution`, and `1 G_PER_L` instead of the source 1 ml volume.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml` and the solution migration that moved the source item into `solutions`.
3. **major - the 100 ml Glycine-NaOH buffer is represented as a mass concentration**
   - Evidence: TOGO `M950` represents `0.5 M Glycine--NaOH buffer (pH 10.0)` as a 100 ml subcomponent with `conc_value: 0.5` and `conc_unit: M`; the generated record stores the amount as `100 G_PER_L`.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`, via the TOGO importer.
4. **major - preparation instructions are missing**
   - Evidence: JCM medium 906 tells the reader to bring the main solution to 900 ml, adjust pH to 7.0, autoclave, separately autoclave the buffer, then add it after autoclaving. The generated M950 record has no `preparation_steps` field.
   - Maintained owner: `data/normalized_yaml/bacterial/TOGO_M950_MK_Medium.yaml`, via the TOGO importer or a JCM overlay.

## Recommended Edits

1. Correct TOGO milligram handling so 36 mg FeCl2 x 4 H2O and 0.36 mg MnCl2 x 4 H2O are represented as 0.036 g and 0.00036 g per liter, or retain source units explicitly if exact final-volume arithmetic is not available.
2. Replace the empty `SL-6 trace element solution` stub with a structured 1 ml reference to TOGO `M158`, or expand the referenced SL-6 stock while preserving the 1 ml dilution boundary.
3. Preserve the 100 ml, 0.5 M, pH 10.0 Glycine-NaOH buffer subcomponent as a solution addition instead of a `100 G_PER_L` direct ingredient.
4. Add the JCM 906 preparation steps to the maintained TOGO M950 record or to a source-specific overlay: main solution to 900 ml, pH 7.0 before autoclaving, separate Glycine-NaOH buffer autoclaving, and post-autoclave addition.
5. Regenerate `data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml` after the normalized source and any importer fixes land; do not patch the generated merged file directly.

## Follow-up Checks

- Re-run open schema, strict schema, reference, and term validation on the regenerated `data/merge_yaml/merged/mk_medium__b1e5f5fc.yaml`.
- Inspect the regenerated YAML for a non-empty 1 ml SL-6 solution reference and a 100 ml Glycine-NaOH buffer addition.
- Confirm that FeCl2 x 4 H2O and MnCl2 x 4 H2O no longer preserve milligram numeric values under `G_PER_L`.
- Re-run exact ignored-inclusive `rg --no-ignore --hidden` checks for `preparation_steps`, `SL-6 trace element solution`, and `Glycine` against the maintained normalized owner and regenerated merge output.

## Additional Notes

- `mk_medium__b1e5f5fc` is the solid TOGO M950 sibling of the JCM 906 MK formulation. Keep it distinct from the liquid MediaDive J906 import unless the merger gains enough stock and physical-state awareness to merge only exact source-equivalent variants.
