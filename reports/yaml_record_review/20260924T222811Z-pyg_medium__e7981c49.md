# YAML Record Review: PYG medium

- Repository: CultureBotAI/CultureMech
- Record: data/merge_yaml/merged/pyg_medium__e7981c49.yaml
- Started UTC: 2026-09-24T22:28:11Z
- Finished UTC: 2026-09-24T22:29:16Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | MediaRecipe |
| ID | CultureMech:008741 |
| Name | pyg_medium |
| Original name | PYG medium |
| Category | bacterial |
| Generated file | data/merge_yaml/merged/pyg_medium__e7981c49.yaml |
| Maintained owner | data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml |
| Merge source | TOGO_M2147_PYG_medium |
| Source grounding | TOGO:M2147, NBRC_M1499 |

The reviewed file is a generated merge product. Future corrections should be
made in the maintained TOGO/NBRC normalization and in the solution migration
heuristic that mistook the Hipolypepton manufacturer footnote for a solution
marker.

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Open LinkML schema | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml linkml-validate -s src/culturemech/schema/culturemech.yaml --target-class MediaRecipe data/merge_yaml/merged/pyg_medium__e7981c49.yaml` | Passed; exited 0 with no diagnostics. |
| Strict CultureMech validation | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml python scripts/validate_strict.py data/merge_yaml/merged/pyg_medium__e7981c49.yaml --out /private/tmp/pyg_medium__e7981c49.strict.tsv --workers 1 --quiet` | Passed with 0 strict errors; `/private/tmp/pyg_medium__e7981c49.strict.tsv` contained 1 line including the header. |
| LinkML references | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-reference-validator linkml-reference-validator validate data data/merge_yaml/merged/pyg_medium__e7981c49.yaml --schema src/culturemech/schema/culturemech.yaml --target-class MediaRecipe` | Passed; 1 file was validated and no reference checks were emitted. |
| LinkML terms | `uv --cache-dir /private/tmp/uv-cache-culturemech-review --no-config run --no-project --offline --python /usr/local/bin/python3.11 --with pyyaml --with linkml --with linkml-term-validator linkml-term-validator validate-data data/merge_yaml/merged/pyg_medium__e7981c49.yaml -s src/culturemech/schema/culturemech.yaml -t MediaRecipe --labels -c conf/oak_config.yaml` | Passed; only the known `eutils`/`pkg_resources` deprecation warning was printed. |
| Embedded curation history | Not run | Not checked: the documented `just validate-history` target validates standalone `history/`, not `MediaRecipe.curation_history` entries embedded in merged YAML. |

## Identity and Grounding

- `data/merge_yaml/merged/pyg_medium__e7981c49.yaml` is the generated TOGO
  M2147 branch for CultureMech:008741 and NBRC medium 1499 `PYG medium`.
- The TOGO M2147 API identifies original medium `NBRC_M1499`, pH 7.0, and the
  same NBRC `NO=1499` URL embedded in the reviewed YAML.
- The live NBRC page for `NO=1499` resolves to `Medium No. 1499` and
  `PYG medium`, confirming that the record is grounded to the intended NBRC
  recipe.
- Exact ignored-file search across `data/**/*.yaml` for `TOGO:M2147`, TOGO
  M2147 URLs, `gm_id=M2147`, `NBRC_M1499`, `NO=1499`, and
  `TOGO_M2147_PYG_medium` found only the reviewed generated record, its
  maintained normalized owner, and the `data/metal_ree_analysis.yaml` index
  entry for that exact TOGO source.
- A first exact search included `NBRC_M1546`/`NO=1546` by mistake; it found an
  unrelated R2A with NSW record and was discarded before any duplicate or
  absence finding.

## Evidence

- NBRC 1499 and TOGO M2147 support 5 g Hipolypepton, 0.2 g yeast extract,
  5 g glucose, 3 g beef extract, 0.5 g NaCl, 1.5 g MgSO4.7H2O, 15 g agar if
  needed, 1 L distilled water, and pH 7.0.
- The reviewed record incorrectly places `Hipolypepton*` under `solutions` with
  an empty composition. In the source, the asterisk marks only the vendor note
  `Wako Pure Chemical Ind., Ltd., Osaka, Japan.`
- The reviewed record marks the recipe `SOLID_AGAR` and emits 15 g/L
  `Agar (if needed)` as an unconditional ingredient. NBRC conditions the agar
  addition with `if needed`, so the recipe can be prepared without agar.
- The reviewed record encodes the source 1 L distilled water row as
  1 `G_PER_L`, which is not a supported final volume or amount.
- The reviewed record omits pH 7.0.

## Completeness

- Empty optional fields beyond general microbial cultivation are not defects.
- `ph_value` is missing even though both TOGO and NBRC give pH 7.0.
- Hipolypepton is absent from `ingredients` because it was migrated to an
  empty `solutions` row.
- The conditional agar addition is not represented as conditional.
- Exact ignored-file search included ignored files before assessing the exact
  TOGO/NBRC source scope.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | Hipolypepton was misclassified as a solution and removed from the main ingredients. | NBRC 1499 lists 5 g Hipolypepton and uses `*` only for a Wako manufacturer footnote. The YAML stores `Hipolypepton*` in `solutions` with an empty composition and no `ingredients` row. | data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml; solution migration |
| Major | Conditional agar is represented as an unconditional solid-medium ingredient. | NBRC lists `Agar (if needed)` at 15 g, but the YAML sets `physical_state: SOLID_AGAR` and has no indication that agar is optional. | data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml |
| Major | The source 1 L water row is misencoded as `1 G_PER_L`. | NBRC and TOGO both describe 1 L distilled water. The YAML stores the final water row as 1 g/L. | data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml; TOGO import |
| Minor | pH is absent. | NBRC 1499 and TOGO M2147 report pH 7.0, but the generated record has no `ph_value`. | data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml |

## Recommended Edits

1. Move `Hipolypepton*` back from `solutions` to `ingredients` in
   `data/normalized_yaml/bacterial/TOGO_M2147_PYG_medium.yaml`, preserving the
   5 g/L amount and the Wako footnote as a note rather than as a solution.
2. Fix the agar representation so `Agar (if needed)` is not modeled as an
   unconditional requirement; add a note if the schema has no conditional
   ingredient field.
3. Fix the source 1 L water row so it is not represented as 1 g/L.
4. Add pH 7.0 from NBRC/TOGO.
5. Regenerate `data/merge_yaml/merged/pyg_medium__e7981c49.yaml` after the
   maintained owner is fixed.

## Follow-up Checks

- Re-run open schema, strict, reference, and term validation on
  `data/merge_yaml/merged/pyg_medium__e7981c49.yaml`.
- Manually confirm Hipolypepton is a top-level ingredient and no empty
  Hipolypepton solution remains.
- Manually confirm the regenerated record preserves the conditional `if needed`
  scope of agar.
- Run an exact ignored-file search for `TOGO:M2147`, `NBRC_M1499`, and
  `NO=1499` to verify no stale duplicate remains after regeneration.

## Additional Notes

- The local source fetches for TOGO and NBRC initially failed with sandbox DNS
  errors. Both official URLs were then fetched successfully with the approved
  escalated `curl -L` rule.
