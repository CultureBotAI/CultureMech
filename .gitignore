# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv

# Local secrets
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
dmypy.json

# Output directories
output/
dashboard/

# Generated per-medium HTML (regenerated by `just gen-pages`; deployed
# to GitHub Pages from a workflow, not committed to main).
pages/media/
pages/index.html
pages/style.css

# Legacy per-category page output (renderer no longer writes here; the
# active targets are pages/media/ and pages/normalized/). Block re-add
# in case stale local copies exist from before the 2026-05-23 cleanup.
pages/bacterial/
pages/algae/
pages/archaea/
pages/fungal/
pages/specialized/

# OAK cache
.oak_cache/
references_cache/

# Pipeline runtime artifacts (proposals, validation reports, NCBI lookup cache)
workspace/

# Generated deep-research outputs
research/

# UMAP embedding cache (large pickle files - DO NOT commit)
.umap_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Logs
*.log

# Generated documentation
docs/_build/
docs/schema/

# Temporary files
*.tmp
*.bak
.cache/

# Runtime CHEBI/term cache (regenerated at runtime, not source)
cache/

# Data layers (see docs/DATA_LAYERS.md)
# Three-tier system: data/raw/ → data/raw_yaml/ → data/normalized_yaml/

# Layer 1: Raw data files (large, can be regenerated or fetched)
data/raw/**/*.json
data/raw/**/*.tsv
data/raw/**/*.csv
data/raw/**/*.tar.gz
data/raw/**/*.zip
data/raw/**/*.html
data/raw/**/*.sql
data/raw/**/.DS_Store

# Keep provenance documentation
!data/raw/**/README.md

# Keep MicrobeMediaParam chemical mappings (essential for CHEBI grounding)
!data/raw/microbe-media-param/compound_mappings_strict_final.tsv
!data/raw/microbe-media-param/compound_mappings_strict_final_hydrate.tsv

# NBRC scraped HTML cache
data/raw/nbrc/scraped/

# Layer 2: Raw YAML (unnormalized, regenerable from data/raw/)
data/raw_yaml/**/*.yaml
data/raw_yaml/**/.DS_Store

# Layer 3: Normalized YAML is VERSION CONTROLLED (curated recipes)
# Keep all data/normalized_yaml/ in git
!data/normalized_yaml/**/*.yaml
data/normalized_yaml/**/.DS_Store

# Processed data (regenerable)
data/processed/
dismech/
data/embeddings/*.tsv.gz

# KG-Microbe data files (too large for GitHub)
data/kgm/merged-kg_edges.tsv
data/kgm/merged-kg_nodes.tsv

# Local-only / generated directories (cleanup 2026-05-23)
# - backups/: ad-hoc pre-migration snapshots (git history is authoritative)
# - tmp/:     LinkML codegen artifacts (canonical copies live in
#             src/culturemech/schema/culturemech_dataclasses.py + docs/schema/)
backups/
tmp/

# Legacy pre-three-tier-migration media directory (kb/media_backup +
# kb/media_backup_old). Live data lives under data/normalized_yaml/.
kb/

# Runtime CHEBI/term cache, linkml-term-validator / OAK label cache
# (regenerated at runtime, not source)
cache/

# Regenerated diagnostic report, not an artifact anyone reads (#157). `just
# check-chebi-grounding` rewrites it from the corpus, and the chebi-consistency
# workflow uploads a fresh copy on every run (`if: always()`). Tracking it only
# produced silent rot: the committed copy drifted to 73 insertions / 82 deletions
# behind the corpus while the gate stayed green, because the gate compares against
# --max-allowed rather than against the file.
reports/chebi_consistency.tsv

# Regenerable content-review outputs with no reader; only the .tsv is consumed (#168).
# Regenerate with `just review-media-content`.
reports/media_content_review_manifest.json
reports/media_content_review_manifest_summary.md
reports/media_variation_candidate_groups.tsv
reports/media_variation_candidate_groups.json

# Growth-review manifest: derived from gitignored workspace/ growth-evidence
# proposals, so it is machine-local and not reproducible from tracked data (#224).
# Regenerate with `python scripts/build_media_growth_review_manifest.py` (needs the
# local proposals). The hand-written corpus_scope.md doc stays tracked.
reports/media_growth_review_manifest.tsv
reports/media_growth_review_manifest.json
reports/media_growth_review_manifest_summary.md

# Cocktail-nesting proposal worklist: a regenerable diagnostic for the #150 repair,
# not a tracked artifact. Regenerate with `just propose-cocktail-nesting`.
data/import_tracking/reports/cocktail_nesting_proposals.tsv
data/import_tracking/reports/cocktail_nesting_proposals.json

# MediaDive stock-solution volume cache for the #150 cocktail repair: a fetched
# diagnostic, regenerable with `just fetch-mediadive-volumes`.
data/import_tracking/reports/mediadive_solution_volumes.json

# MediaDive id audit report: a regenerable diagnostic (#244), needs the live API.
data/import_tracking/reports/mediadive_id_audit.tsv

# Cocktail research target list: regenerable from the corpus + volume cache (#150).
data/import_tracking/reports/cocktail_research_targets.json

# Cocktail stock identification report: regenerable diagnostic (#150).
data/import_tracking/reports/cocktail_stock_identification.tsv
